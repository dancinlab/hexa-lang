# RT-NATIVE-ZEROC

@goal: ls self/*.c == ∅ (양 타깃 gen3≡gen4 byte-eq 유지) — runtime.c+runtime_core.c 를 native-seed/intrinsic 으로 완전 대체

## milestones
- [ ] M6 runtime.c+runtime_core.c drop + ls self/*.c==∅ + gen3≡gen4 양타깃 검증
- [ ] M5 standalone-build 재설계 (native rt_*.o 링크, #else 제거)
  - 🧱 alloc r2 (x86 arena): briefed "mirror arm64 rt_arena_* → x86_64 + excise #else arena + runtime_core.c bytes DECREASE" = NO-GO, DUPLICATE of F-LEGB-STANDALONE-ELSE-NOGO. RAN(read real bodies): arm64 rt_arena_*(runtime_arm64.hexa:2237-2543)=hexa emitting RAW ARM64 MACHINE-CODE(svc mmap=197/munmap=73, _arena_state PAGE21/PAGEOFF12)=darwin-SPECIFIC, NOT portable; #else arena(runtime_core_emit.hexa:3882-4049)=SEPARATE portable-C bump. "mirror"=WRITE x86 machine-code emitter, no portable body to copy; no x86 analog. DECISIVE: arm64 rt_arena_*=PROVEN-BUT-DORMANT(only PoC/test .o, NEVER release_build/stage_resolve_runtime_a) → BOTH targets ship the C standalone arena(stage_resolve_runtime_a:271, no -DHEXA_HAS) = x86's shipped arena is C for the SAME reason arm64's is = NO mirror parity-gap; r1 "x86=bounded mirror" CORRECTED. briefed reduction reachable ONLY via the forbidden #else delete(breaks release_build CI judge) = NO-GO. real GO sub-unit(DEFERRED): x86 rt_arena_* ELF emitter twin IS buildable(elf_x86_64.hexa has .data/.bss + R_X86_64_64/PC32 reloc + multi-sym wrap) but DORMANT-parity(ZERO byte-delta) = a brick for M5 standalone-redesign(relink native rt_*.o), pick up WITH that relink. runtime_core.c byte delta=0; emitter/runtime/build UNCHANGED; byteeq-real main GREEN untouched. verdict F-ALLOC-R2-X86-ARENA. r3=map_* alloc(arena over __hx_syscall6 mmap+__hx_ptr_*)+construct(__hx_make_val #3488) half + map_core_gate.c C-diff depletion(coordinate: array lane owns array_core.hexa, alloc takes map_*).
  - rt_str PILOT: 메커니즘 ALREADY PROVEN+SHIPPED (Z2a #3455/#3457/#3465) — standalone 빌드가 native rt_hi_native.o(14 rt_str_* syms) 링크 + casefold/trim/contains/search-pred `#else` C바디 excise(에미터 body 3→0) + HEXA_ZEROC_RT_HI=1 auto-enable(양 타깃). aiden 실측: runtime_core.c 411120B, nm 잔여 rt_str body = int-return bridge 2개(starts_with/ends_with)뿐, byteeq-real CI GREEN(main). 잔여 bridge 2개 = ABI 어댑터(native body 없음, _b만 시드제공) → codegen/seed micro-unit 으로 defer(Z2a 템플릿 밖, no fake). verdict F-M5-PILOT-RTSTR. → M5 메커니즘 = fake 아님·rt_str 한정 DONE; 진짜 ls==∅ 레버는 M4 common-core port.
- [ ] M4 ~1190 common core fn 전수 hexa-native 포팅 (다배치, nanbox/syscall/map/array)
  - B1 nanbox-ctor: mechanism DONE (3중 native: _hv_load inline · HEXA_RT_SELFEMIT rt_hexa_* bytes · __hx_* read-intrinsics); C-diff gate b1_nanbox_gate.c 35/35 PASS (aiden x86_64). nanbox.hexa NOT viable (HexaVal struct-literal = self-recursive hexa_int call). #else drop BLOCKED-on-M5 (standalone runtime.a links the C bodies). verdict F-M4-B1-NANBOX.
  - B4 array-core: mechanism-BLOCKED — WALL-1 construct (B1 twin: get/pop/new return a 16B HexaVal element, no (tag,payload)→HexaVal intrinsic; every __hx_* boxes TAG_INT; `arr[idx]`→hexa_index_get circular) + WALL-2 alloc (new/push/reserve/snapshot = calloc/realloc/arena/heapify/stats, not intrinsic-expressible). Layout c2-measured: HexaArr{items@0,len@8,cap@16}/24, HexaVal/16, stride 16, cap<0=arena. C-diff gate b4_array_gate.c 32/32 PASS (summer x86_64) — layout+behaviour oracle locked. array_core.hexa NOT viable (circular). #else drop BLOCKED-on-M5. UNBLOCKING LEVER = __hx_make_val(tag,payload)→HexaVal (the symmetric write-half of __hx_tag; ADD-only; unblocks B4/B5 element-returns + B1 nanbox.hexa). verdict F-M4-B4-ARRAY. r2=__hx_make_val LANDED (#3488, tag survives round-trip exit=8). **r3 READ-half PORTED+PROVEN (F-M4-B4-ARRAY-R3-READHALF):** stdlib/runtime/array_core.hexa rt_array_{get,set,len,pop}_native raw-mem (__hx_ptr_load64/store64 + __hx_make_val tag re-stamp); native emit → nm T + ZERO undefined __hx_* (intrinsics inline, frozen-.s route FEASIBLE) + direct-call gate exit 12 (int/str/bool tag-faithful) + C oracle 32/32. 🧱 WALL-3 bootstrap-transpile gap: hexat (C-transpile build) can't lower the raw-mem intrinsics (only gen2 does) → import into compiler closure BROKE build → bodies enter ONLY via native frozen .s seed (rt_hi mechanism). delegation gated DEFAULT-OFF HEXA_RT_ARRAY_NATIVE → build_aprime PASS, fixpoint preserved, bytes +~1KB inert (ls-drop is r4). **r4 = freeze array_core_{x86_64,arm64}.s + ar into runtime.a + define HEXA_RT_ARRAY_NATIVE + DROP get/set #else = first non-string runtime-substrate ls byte-DECREASE (array twin of alloc-lane M5 relink brick).** ✅ **r4 DONE (F-M4-B4-ARRAY-R4-SEED-LIVE, byte-DECREASE ACHIEVED)**: 3rd seed array_core_arm64-linux.s 동결(254L globl 4/4, __hx_* inline) — 결정적 분기: build_native_linux_arm64:84 가 이미 stage_resolve_runtime_a(TARGET=linux-arm64) 호출 = 소비훅 존재, M-level 아님. get/set `#else HX_ARR_ITEMS[idx]` 무조건 native-call 로 드롭 + single-TU dev빌드(build_aprime stage4/5·build_native_linux_arm64 stage4) 가 array_core_native.o 링크(rt_hi Z2a 미러). aiden 실측: **runtime_core.c −1264 B/−24 L**(origin/main 412623→411359) + build_aprime smoke exit42 + nm rt_array_*_native=T. blast-radius 측정: 권위 게이트 전부 release_build→runtime.a(seed ar) + byteeq=오브젝트비교 → gate-safe; build_aprime/native_arm64 는 CI 아님(dev). 권위확정=PR CI(byteeq-real+faithful 3타깃, linux-arm64 aarch64 어셈블 검증).
  - 🧱 per-family-drop lane TERMINAL (m5 r2 census, F-M5-R2-RTCORE-CENSUS): runtime_core.c 의 37 `HEXA_HAS_HEXA_RT_STDLIB` guarded 블록 전수 — native-arm extern 43/43 전부 hexa-source body 보유 = ALREADY-NATIVE; PURE-PORTABLE(포트 필요) 버킷 = 0. 잔여 #else 는 CONSTRUCT-WALLED(arith/cmp/eq/to_string → array-r2 __hx_make_val 도메인) · ALLOC/stdio-WALLED(map _impl·println·deep-eq). per-family native-seed 드롭 = EXHAUSTED. ls 레버는 __hx_make_val unlock(array-r2) + M5 standalone 재설계 둘뿐.
  - ✅ ALLOC-floor FEASIBILITY = GO + NON-CIRCULAR (alloc r1, F-ALLOC-R1-FEASIBILITY): census 의 ALLOC-WALLED 버킷(bump-arena substrate + map _impl + to_string/array-push alloc-half)이 .hexa 로 표현 가능. 인트린식 커버리지 100%(MISSING 0): mmap/munmap=__hx_syscall6(6-arg ABI 확인) · raw 64/32/8 load+store=__hx_ptr_* · bump=int payload arith · construct=__hx_make_val(array-r2). 순환성 NON-CIRCULAR — bump arena 는 pre-mmap int64/pointer 공간 전부 상주, arena BODY 가 HexaVal box/string lit/array temp 안 만듦 ⇒ heap 재귀 없음. PoC RAN(aiden x86_64, aprime_m4): bump_arena_linux_poc.hexa mmap(9)→200cell i^2 왕복→munmap(11) exit=0; nm witness = arena_* T심볼·malloc/calloc/free 참조 0·syscall 정확히 2개(floor). ARENA 플로어(arm64 는 이미 runtime_arm64.hexa rt_arena_* hexa-source 방출)+calloc/realloc=GO; map_* 등 HexaVal-RETURNING 패밀리 = GATED-on-__hx_make_val(array-r2 #3488). 임의 블록 free+범용 malloc 만 accounting-floor 잔여(bump=bulk reset, Go/Rust 동급). 진짜 irreducible = syscall+libm, allocator 로직 아님. NEXT(r2, #3488 merge 후) = hexa_arena_alloc 를 __hx_syscall6 mmap+__hx_ptr_* hexa-source 바디로 포팅(x86_64 native runtime+standalone path).
- [x] M3 @syscall 일반 intrinsic 양타깃 (svc/syscall reg marshalling)
- [x] M2 raw-mem intrinsic 양타깃 완비 (__hx_ptr_load/store map·array)
- [x] M1 x86_64 leaf-intrinsic parity (25/25 __hx_* arm64→x86_64 codegen; GNU-as byte-id 인코딩; emit→as→link→run exit-correct; gen3≡gen4 byteeq 보존; +const_float payload root-fix. 잔여=const_str-assign·unop `!` 미구현 = M2 orthogonal. F-M1-X86-LEAF-PARITY)
- [ ] (first milestone — `harness domain ms <text>`)
