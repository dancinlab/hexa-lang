use "tests/interp_qforge_bio/modA"
fn main() {
    let r = 1.7682
    println(format_float((2.0 * r) / 1.122462048309373, 17))     // main, var
    println(format_float((2.0 * 1.7682) / 1.122462048309373, 17)) // main, all-lit
    println(format_float(qfb_conv(r), 17))                        // mod, param
    println(format_float(qfb_alllit(), 17))                       // mod, all-lit
}
