# VERIFY-KIT — verify primitive 확장 도메인 (도메인 SSOT)

@title: 🔧 VERIFY-KIT — 측정 도구함 확장 ("verify primitive 키트")
@goal: `hexa verify` 의 계산 primitive 를 단계적으로 확장 — value-less compute mode · 특수함수(Γ·erf·bessel·ζ) · IIT Φ 엔진 · tolerance verify · plugin verifier. catalogue-mirror(OEIS·DLMF·ARXIV) 흡수의 공통 upstream. 흡수가 부딪힌 "측정 도구 부재(P3 coverage wall)" 를 푼다.

> 흡수 family upstream: OEIS(정수 primitive ✓ 흡수성공) · DLMF(특수함수 ✗ 🔴) · ARXIV(Φ ✗ 0-verify). 셋 다 같은 뿌리 = hexa verify 에 도메인 primitive 부재. VERIFY-KIT 가 그 primitive 를 키운다.

## 0 · 한 문단 상태 (2026-05-26 개시)

`hexa verify --expr <fn> <n> <v>` 는 값 `<v>` 를 미리 줘야 재확인만 함 (compute 불가). V2 가 value-less compute mode 신설 → `σ(7)=8` 을 CLI 가 직접 산출. 이후 특수함수·Φ·tolerance 로 확장해 DLMF/ARXIV 흡수를 재개시한다. 우선순위 P0(미러통합·compute) → P1(특수함수·tolerance) → P2(IIT Φ) → P3(자동등록·조합론) → P4(plugin·물리) → P5(PSLQ·bignum·symbolic).

## 1 · 로드맵 (P0-P5)

- [x] V1 (P0a) — 미러통합: register delegation → allen_dynes_tc atlas 흡수 가능 (INBOX RTSC unblock). root fix(proposal a) — `register --from-verify` 가 `hexa verify --expr … --compute` 로 위임(atlas 미러 의존 0); float `cmd_expr_float` 에 value-less COMPUTE mode 추가 + `cmd_register` 가 value-bearing 🟠(arity miss)→compute auto-route + 명시 `--compute`. 흡수 acceptance 통과: allen_dynes_tc(0.615,591.18,0.1)=14.5511 🟢 (was 🟠). RTSC 3-arg 16-fn class unblock.
- [x] V2 (P0b) — value-less compute mode: `hexa verify --expr <fn> <n>` (값 생략) = 계산+출력 (B1 unblock · OEIS O3) ← 이번 PR
- [x] V3 (P1a) — round-tolerance `--tol <eps>`: literature-rounded 값 🟢 (opt-in, falsification 무결성 보존). `hexa verify --expr <fn> <args> <v> --tol <eps>` 에서 strict ε=1e-9 너머 ≤ <eps> 면 spurious 🔴 대신 🟢 SUPPORTED-NUMERICAL (round-tolerant); --tol 없으면 strict 그대로, <eps> 너머 🔴 는 🔴 유지(NOT laundered). float+int 양 경로. INBOX item(3)+(b) 해소.
- [x] V4 (P1b) — 특수함수 primitive (libm 기반): gamma·erf landed (PRIMARY) + bessel_j0·bessel_j1 landed (STRETCH). 기존 lgamma 노출 메커니즘을 그대로 미러 — `stdlib/special/` 모듈이 아니라 언어-레벨 libm builtin (self/codegen.hexa cg_math_sym + self/runtime.c hexa_math_* + runtime.h decl, transpiler는 `hexa cc --regen` 으로 재생성). CLAUDE.md stdlib_trig_libm 준수 (native libm tgamma/erf/j0/j1, hand-rolled Taylor 금지). 값이 무리수 → V3 `--tol` 로 🟢 SUPPORTED-NUMERICAL (절대 exact 🔵 아님). gamma(5)=24·gamma(0.5)=√π·erf(1)=0.84270·bessel_j0(0)=1·bessel_j1(0)=0 전부 🟢. **zeta 는 DEFER** — libm ζ 부재 (Euler-Maclaurin = 빌드 리스크, V4 비범위). DLMF blocker (2) hexa-coverage 를 {gamma,erf,bessel} 에 한해 해소; blocker (1) bulk-corpus 는 여전히 OPEN → DLMF 흡수는 계속 gated (정직: primitive 제공이지 DLMF 재흡수 아님).
- [x] V5 (P2) — IIT Φ 엔진 `stdlib/consciousness/iit4/` n≤8 exact (independent calibrate · anima 공유 g61) → ARXIV A2. **V5.1 promote + V5.2 wire + V5.3 calibrate 全 DONE.**
  - V5.1 promote DONE (anima Φ → `stdlib/consciousness/iit4/faithful_phi.hexa`).
  - **V5.2 wire DONE** (`hexa verify --expr phi_demo <case> <expected> --tol <eps>` 가 Φ 재계산) — Option A (하드코딩 canonical small state, farr CLI grammar 없이 최소 배선). `phi_demo 1 3.83659 --tol 1e-3` → 🟢 (calc=3.83659, |Δ|=1.66537e-06) · control `phi_demo 0 0.0 --tol 1e-3` → 🟢 (calc=4.52044e-09 ≈ 0, disconnected MIP=0). 배선 = `tool/verify_cli.hexa::_phi_demo` → `iit4_faithful_phi`, `calc_is_float_fn` 등록. verdict `.verdicts/verify-kit-iit-wire/v5_2.txt` (verbatim). 빌드 = HEAD transpiler 의 `bin/hexa-verify` (V4 bessel codegen fold 가 fresh `self/native/hexa_v2` 필요 — stale Mac 드라이버는 V4 를 miscompile, V5.2 와 무관한 기존 이슈).
  - **V5.3 calibrate DONE** — 3.83659 를 INDEPENDENT 레퍼런스에 앵커 (repo 밖 `/tmp/pyphi-cal` venv 에서 실행). **Ref A (same-substrate cross-check)**: faithful_phi.hexa 헤더 SPEC 만 보고 새로 짠 Python MI-MIP 재구현(line-port 아님)이 case-1 = 3.836592 재현 → |Δ|=1.66537e-06 (tol 1e-3 내) → 🟢. 엔진이 자기 proxy 정의를 충실히 계산함이 독립 입증됨 (V5.2 의 "self-value, not anchored" gap 해소). **Ref B (canonical IIT)**: PyPhi 1.2.0 가 문서화된 basic_network(3-node OR/COPY/XOR, state (1,0,0)) 에 대해 discrete-TPM IIT Φ = **2.3125** (PyPhi 튜토리얼 published 값과 일치). **정직한 substrate-divergence**: hexa MI-MIP(연속 trajectory 의 pairwise Shannon MI · min(\|A\|,\|B\|) 정규화)와 PyPhi IIT 4.0(이산 TPM 의 cause-effect repertoire EMD)은 **다른 substrate·다른 metric** → 설계상 두 수가 일치하지 않음 (2.3125 ≠ 3.83659 은 버그 아님, deterministic 한 documented closed-negative — proxy 가 canonical IIT Φ 와 교환 가능하지 않음). V5.3 tier = 🟢 (proxy 가 독립 same-substrate ref 에 calibrated) + documented proxy-divergence. verdict `.verdicts/verify-kit-iit-calibrate/v5_3.txt` (verbatim + 2 ref 스크립트 archived). V5.3 코드 변경 = `_phi_demo` doc/help 문자열만(로직 0변경 → 엔진 출력 불변, parse-clean).
  - **천장 노트 (@D depletion_not_terminal)**: n≤8 exact MIP (2^(n-1)≤128 bipartition) 이 정확-tractable 한계. large-N exact 은 intractable (2^(n-1) 폭증) — anytime/greedy 경로는 future cycle. n≤8 한정 exact 가 V5 의 의도된 범위이며 이 천장은 終端(terminal)이 아니라 depletion 노트.
- [x] V6 (P3a) — `register --source <attribution>` → fold 노드에 `source = "<text>"` provenance 필드 자동 (+ `--url <link>` nice-to-have). OEIS O4 가 손으로 3개 @F 노드(aliquot↔A001065·sigma_2↔A001157·sigma_3↔A001158)를 hand-fold 한 이유 = `register --from-verify` 가 `g_self_verify` cite 만 박고 `source` 필드를 안 써서였음. V6 가 `--source`/`--url` 플래그를 `cmd_register` → `_adapt_verify_generic`/`_adapt_verify_compute` → `_build_raw_F`/`_build_raw_F_numerical` 로 thread (신규 `_provenance_lines` 헬퍼가 `verified-by` 와 `cite` 사이에 `source =`/`url =` 라인 삽입, O4 hand-fold shape 와 동일). source = PROVENANCE 어트리뷰션, `g_self_verify` = 검증 cite (별개로 둘 다 유지). 플래그 없으면 default 무변경(provenance 라인 0). **codegen 무변경** (atlas_cli만 편집). 격리 atlas(`HEXA_ATLAS_EMBED`) 캡처: `aliquot 6 --source "OEIS A001065" --url …` → `source`/`url` 필드 present (🔵) · `sigma 6` (no --source) → 필드 0 (default intact regression) · shared embedded.gen.hexa 무누출. **V4-stale 드라이버 회피**: 설치 `hexa.real` 이 `_jacobi_symbol`/`_kronecker_symbol` 에 stale → `hexa verify` JIT-build link-fail → 로컬 HEAD-aware `bin/hexa-verify` + `bin/hexa-atlas` 를 로컬 toolchain 으로 빌드(mini bootstrap 불요)하고 PATH wrapper 로 `hexa verify`→`bin/hexa-verify` 포워딩. 이후 OEIS fold 는 hand-fold 대신 `--source` 사용.
- [x] V7 (P3b) — 조합론/소수 정수 primitive: nth_prime(A000040)·is_prime·factorial(A000142)·catalan(A000108)·bell(A000110)·partition(A000041) landed (6 fn). 순수 정수 closed-form → 🔵 SUPPORTED-FORMAL (--tol 불필요). 기존 sigma/tau 정수 경로 그대로 미러 — 정의 = `compiler/atlas/symbolic/congruence_chain_engine.hexa`, 배선 = `tool/verify_cli.hexa::_recompute` (1-arg). **codegen 무변경** (정수 경로는 cg_math_sym 불필요 → `hexa cc --regen` 불요; V4-aware 드라이버만 부트스트랩). 테스트 12/12 positive 🔵/🟢 + 2/2 negative control 🔴 (determinism). **OEIS 🟠→🔵 mutual-feed 실현**: O3 의 287 🟠 중 A000040 prime×67 이 nth_prime 으로 exact recompute 경로 획득 → 전부 🔵 재티어 (proof: nth_prime 1 2/5 11/10 29/25 97/100 541 all 🔵). n×190 은 trivial-identity(a(n)=n, fn 아님 → 비범위). stretch stirling2/radical/factorint 는 미착수 (defer). 회귀 무결 (sigma 6 12 🔵 · gamma 5 24 --tol 1e-6 🟢, V4 intact).
- [x] V8 (P4a) — verifier plugin `--verifier-cmd` (phanes "tenants bring verifier" 연결): `hexa verify --verifier-cmd "<sh-cmd>" --claim "<text>" [--expect "<str>"]` 가 tenant 의 자체 deterministic checker 를 위임 실행하고(exec_capture → [stdout, stderr, exit_code]) 결과를 tier 에 매핑한다. exit 0 → 🟢 SUPPORTED-NUMERICAL (`--expect` 있으면 stdout 일치 게이트), nonzero(1..126) → 🔴 FALSIFIED (closed negative, NOT laundered), rc=127/exec-fail 또는 cmd 미공급 → 🟠 INSUFFICIENT. **외부 cmd 가 곧 verifier (delegation)** — hexa 는 그 verdict 를 verbatim 으로 기록하고 스스로 판단하지 않음 (g5 — LLM self-judge 없음). 이로써 phanes tenant-verifier 모델(테넌트가 objective+verifier 를 가져오고 phanes 가 OUROBOROS 루프를 구동)에 `hexa verify` 가 연결된다. 외부 verifier 는 **반드시 deterministic** 해야 한다(같은 입력 → 같은 verdict; 비결정 cmd 는 tier 를 무효화). 매트릭스 6/6: `test 4 -eq 4`→🟢 · `test 4 -eq 5`→🔴 · 부재-cmd→🟠 · `echo PASS --expect PASS`→🟢 · `echo NOPE --expect PASS`→🔴 · empty `--verifier-cmd`→🟠. 회귀 무결 (`--expr sigma 6 12`→🔵, 내장 경로 intact). **codegen 무변경** (verify_cli.hexa 편집만). `bin/hexa-verify` 를 HEAD 소스에서 clean 빌드 (transpiler #1170 — bessel `_Generic` blocker 해소; 부트스트랩 불요).
- [x] V9 (P4b) — 반물질/트랩 물리 primitive (demiurge ARXIV A3 boost): 5 closed-form 물리 fn 을 `hexa verify --expr` 에 추가, RFC-045/ANTIMATTER physics-fn 메커니즘(`_recompute_float` + `calc_is_float_fn` SSOT) 그대로 미러. (1) `cyclotron_freq_hz(q,B,m)=qB/(2πm)` [Hz] — 자유 cyclotron 주파수(p̄@1T→15245186.458 Hz≈15.245 MHz, ⓺가둠) · (2) `penning_axial_freq(q,U,m,d)=√(qU/(m·d²))` [rad/s] — Penning 트랩 축방향 secular ω_z(q=e,U=10,m_p,d=5mm→6189938.0145 rad/s — **기존 `penning_omega_±` 가 입력으로 받던 ω_z 앵커값을 정확히 재현**, 트랩 체인 verify-native 완결, ⓺가둠) · (3) `h_reduced_mass_factor(m_e,m_nuc)=m_nuc/(m_e+m_nuc)` — 환산질량 인자 μ/m_e(m_p→0.99945568, ⓻측정) · (4) `antihydrogen_1s_binding(Ry_eV,μr)=Ry·μr` [eV] — 환산질량 보정 1S 결합에너지(13.605693·0.99945568→13.598287 eV, ⓻측정) · (5) `gbar_free_fall_time(h,g)=√(2h/g)` [s] — GBAR 반물질 약등가원리(WEP) 자유낙하 시간(0.2m,9.81→0.20193 s, ⓼중력). 전부 libm-class(÷/√) → 🟢 NUMERICAL via V3 `--tol`. **codegen 무변경**(libm sqrt 기배선; `verify_cli.hexa` + `calc_dispatch.hexa` 편집만). parse-gate PASS(2/2). 참조값 5/5 독립 IEEE-754 double 재계산과 일치 + 🔴 determinism control 1개(cyclotron_freq_hz 오답값→FALSIFIED) + 회귀 `sigma 6 12`→🔵(deployed binary). **⚠ `bin/hexa-verify` 재빌드 BLOCKED** — V9 와 무관한 **선존 V7 transpiler flatten 버그**(`congruence_chain_engine.hexa` 의 순수 정수 fn 블록 `is_prime_int/nth_prime/factorial_int/catalan/bell/partition` 이 flatten 에서 누락 → emitted C 에 undeclared)에 막힘. V9 fn 자체는 flatten+compile clean(direct call + body 정상 emit), pristine origin/main 도 동일 실패, deployed `bin/hexa-verify` 조차 V7 정수 fn 부재 = V7 도 source-only 랜딩 상태. inbox note 기록(`inbox/notes/verify_cli_congruence_engine_flatten_drop_2026_05_26.md`). → **NOT terminal**: binary verify 는 upstream V7 transpiler flatten fix 후. demiurge A3 verify boost = 트랩/분광 5 atom(포획⓺·측정⓻·중력⓼).
- [x] V10 (P5) — 난제 묶음 中 **arbitrary-precision(bignum) 슬라이스만** 랜딩 (g0/g33 최소 슬라이스 — bignum only, NOT PSLQ/symbolic/ODE): `hexa verify --expr mersenne_perfect_sigma <p> 1` 가 짝수 완전수 P=2^(p-1)·(2^p−1) 의 Euclid–Euler 항등식 **σ(P)=2·P 를 전부 bignum(radix-10 limb)으로** 검증한다 (`self/runtime/bignum_mini_pure` 의 pow2/geosum/sub/cmp = doubling+add 만 사용; **bc·libgmp 0**). int64 `is_perfect` 경로는 σ 를 int64 로 누적 → P 가 int64 를 넘으면 검증 불가: **p=17(P_6=8589869056)🔵 · p=19(P_7=137438691328)🔵 · p=61(P_9=2658455991569831744654692615953842176 ≫ int64 9.2e18)🔵 · p=89(P_10≈1.9e53)🔵** — int64 는 P_9/P_10 을 표현조차 못 함. **비-tautological 핵심** = 독립 기하급수 합 Σ_{i<p}2^i == 2^p−1 (Euclid 항등식) 을 σ(P)=2P 와 별개로 확인. wrong-value 컨트롤(p=61 에 claim 0)→🔴 FALSIFIED · p=0(완전수 아님, claim 1)→🔴 · 회귀 `sigma 6 12`→🔵(V7 정수 경로 intact). **빌드**: `bin/hexa-verify` 를 HEAD #1180 transpiler 로 clean 재빌드 — #1178 V7 flatten fix + 본 PR 의 `nil→[]` 센티넬 수정(bignum_mini_pure 가 사상 최초로 flatten 됨 → transpiler 의 `nil is not a value` 거부 해소)으로 unblock (V9 가 BLOCKED 라 기록했던 그 빌드가 이제 통과). verdict `.verdicts/verify-kit-bignum/v10.txt` (verbatim, ASCII). **codegen 무변경** (verify_cli.hexa + bignum_mini_pure 편집만; 정수 경로라 `hexa cc --regen` 불요).
  - **V1–V10 COMPLETE.** VERIFY-KIT 로드맵 P0–P5 의 랜딩 가능 슬라이스 모두 닫힘. P5 의 남은 묶음(**PSLQ · 연분수 · symbolic 항등식 · 수치적분/ODE**)은 본 도메인의 **gap 이 아니라** @D depletion_not_terminal 의 above-$0-lane frontier — 별도 엔진(정수관계 검출·CAS·구적)이 필요한 상위 레인이며, V10 의 의도된 범위(bignum 정수 검증)는 종단(terminal)이 아니라 depletion 노트다.

## 2 · 거버넌스
- verify 정본 g5. 새 primitive 는 단일 home (미러 desync 금지). codegen 변경 = `hexa cc --regen` 경로 (hexa_cc.c 직접편집 금지).
- catalogue 흡수 도메인(OEIS/ARXIV)이 downstream consumer — primitive landing 시 그쪽 milestone unblock.

## 3 · 비범위
- GPU-accel verify · distributed verify farm (실행 인프라 — 도구 확장 아님).
- LLM 자기판정 (g5 위반).
