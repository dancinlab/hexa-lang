@title: 🛠️ MISCOMPILE-ZERO — post-self-host miscompile hardening

@goal: AFTER self-host completion (gen2 byte-eq fixpoint), continuously drive and KEEP gen2's native codegen at MISCOMPILE-ZERO — catch and fix every further native-codegen miscompile (ENCODE-MISS / spurious udf / wrong-codegen) exposed as the self-hosted compiler is exercised on broader code, refining onward after completion.

# MISCOMPILE-ZERO — current state

Pre-created hardening track. Like SELFHOST-NEXT, its START CRITERION is the
completion of the work in flight NOW — the self-host byte-eq fixpoint campaign
(tracked in memory `project_selfhost_gate_reduction` + the active self-host
domain). This domain does NOT begin until gen2 self-emits a byte-identical
object (byte-eq graduation). It is the "keep it at zero, then polish" lane that
follows graduation.

The pre-completion drilling (peel each linker + native-codegen miscompile wall
to first reach byte-eq) is NOT this domain — it is the active campaign. Landed
walls so far (context, on `cc-native/selfhost-ghost`): hexa_ld __literal8
(5f38d7eb4) · hexa_ld __DATA UNSIGNED (23207ce70) · hexa_ld __mod_init_func
(07d8556d1) · lower-aliasing _lr_ctx_clear (953c8824b) · gen2 `_ends_with`
native mis-eval (in flight). When byte-eq lands, MISCOMPILE-ZERO opens here.

## milestones

- [ ] ENTRY GATE — self-host byte-eq fixpoint achieved (gen2 emits cc-gen3.o == cc-prc2, 0 ENCODE-MISS / 0 udf)
- [x] regression guard: a CI gate that fails on ANY `gen2 --emit=obj` ENCODE-MISS / spurious udf (keep the floor at zero) — `tool/miscompile_zero_gate.sh` + corpus `self/test/miscompile_zero/` + `.github/workflows/miscompile-zero-gate.yml`; PASS 5/5 on graduated gen2_fix, FAIL on udf shim
- [x] broaden the self-emit corpus beyond the compiler flat — exercise gen2 native codegen on diverse stdlib / app programs, sweep for new miscompiles — 82-program broad sweep on GHOST gen2_fix = CLEAN (0 ENCODE-MISS / 0 udf / 82 oracle-asm-match modulo benign label hash); verdict `.verdicts/miscompile-zero-sweep/CLEAN-SWEEP-82.txt`; corpus widened 6→10 (c6 closures · c7 recursion+array · c8 string-methods · c9 branch-control · c10 try/catch)
- [x] broaden the real-program sweep to the FULL shipped tree — every `example/*.hexa` (236 programs) on GHOST gen2_fix vs aprime-C oracle = CLEAN (236/236: 0 ENCODE-MISS / 0 udf / rc=0 / objsize 928..58952 / 236 oracle-asm-match modulo benign label hash); 0 SKIP, 0 FINDING; reusable driver `tool/mczero_real_sweep.sh`; verdict `.verdicts/miscompile-zero-realsweep/REAL-SWEEP.txt` + results.tsv — real-program discovery surface broadly exhausted, MZ lane at steady-state
- [x] catalogue the native-codegen miscompile CLASS (index/slice/char_code, 2-reg value ABI, aliasing, string-intern) as targeted codegen tests so regressions (cf. the cycle-41 `.truncate` regression) can't silently return — `self/test/miscompile_class/` (7 PRIMITIVE-level tests m1..m7) + standalone runner `run.sh` (emit asm-oracle 0 ENCODE-MISS/0 udf → clang link → RUN real rc); 7/7 PASS on graduated gen2_fix (ghost), RUNNER-EXIT=0; g63-discriminating (tamper → rc=1 names the class); verdict `.verdicts/miscompile-class/CLASS-TESTS.txt`; additive, gates untouched
- [ ] each newly-exposed native-codegen miscompile → its own fix + byte-verify vs clang oracle, driven to zero
- [x] `hexa run` HANG on compute-heavy loop (sgemm triple-loop) — ROOT-CAUSED + FIXED (2 distinct gen2-codegen bugs, NOT regalloc/interp). Diagnosed via the anima 12-line `while`-hang + 21-line nested-`for` parse-crash repros (`cport/training-next:training/native/repro/`), reduced to a 5-line minimal each. Both are the SAME comptime-const-fold family in `self/codegen.hexa`: a plain immutable `let x = <lit>` registers a fold `x→<lit>`, and reassigning `x` inside a loop body breaks. (1) PARSE-CRASH: the body's AssignStmt invalidates the fold, shrinking the table below the for-loop's `_comptime_const_scope_mark`; `_comptime_const_scope_restore` then read past the live tail → `index 0 out of bounds (len 0)`, NO .c produced → `hexa run` hung on the failed-build retry. FIX = clamp restore to `min(mark, live-len)` (codegen.hexa `_comptime_const_scope_restore`). (2) WHILE-HANG: the WhileStmt arm folded the stale initializer into the CONDITION (`while 0 < 4`) while the body incremented the live counter → infinite loop. FIX = WhileStmt pre-invalidates the fold for every body-reassigned name (via `_gen2_collect_assigned`) BEFORE emitting the cond. VERIFIED end-to-end (patched hexat → clang → run): while_hang prints `kk=4` exit 0; nestfor prints `2.0`×4 exit 0; non-reassigned folded const still optimized (regression-safe). Corpus: `self/test/miscompile_class/m8_comptime_fold_loop_reassign.hexa` + `m9_while_cond_fold_reassign.hexa` (both crash/hang on OLD hexat, pass on patched). Verdict `.verdicts/miscompile-zero/HEXA-RUN-HANG-DIAG.txt`. ⚠ touches `self/codegen.hexa` → needs the ghost byte-eq gate (selfhost-gates-summary). Surfaced by cross-repo C-PORT (sidecar handoff `4b98348f`)
- [ ] (DEPENDS ↑ on the `hexa run` HANG fix — **now SATISFIED**) the anima C-PORT sgemm RUNEQ was RUN with a #2650-fixed `hexat` (rebuilt locally via `hexa cc --regen` with the fixed `self/codegen.hexa`; sanity repro `let s=0; for j in 0..2 {s=s+1}` → `s=2`, terminates). The port now TRANSPILES + COMPILES + RUNS in seconds (no hang). RUNEQ vs the C reference = **DIFFER** (g63-honest, anima PR NOT merged), for two reasons: (1) a NEW const-fold miscompile (see ↓ new milestone) makes `maxabs` wrong in all 8 cases; (2) an inherent fp-CONTRACTION (FMA) mismatch — C `cc -O2` contracts `s+=a*b` into a single-round FMA while the port double-rounds → ~1-ULP `sum` drift in 4 cases (baseline-fp-flag-dependent). cref elements proven byte-identical to C. Stays OPEN until the new miscompile is fixed AND a contraction-free fp contract is pinned on both sides. anima verdict `cport/training-next:.verdicts/c-port/sgemm-ref-runeq.txt` (commit `667839404`). (handoff `4b98348f`)
- [ ] NEW native-codegen miscompile surfaced by the sgemm RUNEQ (sibling of the #2650 WHILE-HANG, NOT covered by it): a plain immutable `let x = <lit>` reassigned inside an **`if`-body within a `for`-loop** has its comptime-const fold inlined into the **`if` CONDITION** (e.g. `let maxabs=0.0; for ... { if av > maxabs { maxabs = av } }` → emits `av > 0.0`, always-true → `maxabs` ends at the last element, not the max). Minimal repro: with `cref[0]=6.2588` set explicitly the reduction returns `1.0392` (the last element) instead of `6.2588`; `let mut maxabs` works around it (maxabs 8/8 correct). The #2650 WhileStmt fix pre-invalidates body-reassigned folds before the cond — the **IfStmt/elif arms need the same pre-invalidation before emitting the if condition**. FIX target: `self/codegen.hexa` IfStmt arm (mirror the WhileStmt `_gen2_collect_assigned` pre-invalidate); add corpus `self/test/miscompile_class/m10_if_cond_fold_reassign.hexa`. ⚠ touches `self/codegen.hexa` → ghost byte-eq gate.
- [x] linker determinism: make hexa_ld output byte-deterministic (relink gen2 == gen2b; prior diff @byte ~1924664 symtab tail) so byte-eq holds across rebuilds — VERIFIED + GATED: re-link of the 3.2MB cc-prc2-fix.o twice gives an identical SHA256 (symtab/strtab tail deterministic, wall closed); `tool/determinism_gate.sh` + `.github/workflows/determinism-gate.yml` lock re-emit + relink byte-eq; PASS 10/10 on graduated gen2_fix + hld_fixed

## deferred

- promote the miscompile-zero corpus into the standard `hexa verify` gate suite
- fuzz / differential-test gen2-native vs aprime-C codegen across random programs to surface latent miscompiles before users hit them
- track miscompile-zero as a perf-stable property (no codegen regression budget)
