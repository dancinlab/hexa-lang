# UNSHADOW — current state

@title: 🪆 UNSHADOW — "남의 엔진 떼기"

@goal: C/Rust 그림자를 벗은 hexa-only 스택에서 perf·자원·속도를 roofline % 기준으로 실측해 따낸다. "졸업하면 자동으로 빨라짐"이 아니라 "졸업해야 막혔던 최적화를 할 수 있다" — 소유로 열린 최적화를 측정으로 하나씩 확보.

## 전제 — HEXA-STACK (🟡 floor 상속 + 🔵 ceiling 적층)

전략의 핵심 = **LLVM 을 재구현하지 않는다.** M1 baseline 이 native-asm 백엔드가 clang -O2 에 5/5 패배함을 실측했다. 그래서 싸우지 않고 **올라탄다**:

- **🟡 FLOOR = clang -O2 상속 (재구현 0).** C-emit 경로가 이미 clang -O2 를 그대로 통과 → loop-unroll·reg-alloc·SIMD·instruction-sched 를 전부 공짜로 상속한다. raw 스칼라/벡터는 LLVM 과 비김(PTX-diff). 🟡 종목을 직접 다시 짜는 것은 함정.
- **🔵 CEILING = hexa 고유 우위 적층.** LLVM 에 없는 종목(인라인·atlas-fold·proof-carrying·check-elision = 이론·증명·전스택의미·comptime)을 floor 위에 쌓는다. 진짜 ROI 는 여기.
- **블로커 = `runtime.o` C-ABI 벽.** 이번 사이클 두 closed-negative(#2-ext rt_str · C tag-elision)가 한 지점으로 수렴했다 — precompiled `runtime.o` 의 C-ABI 경계 너머로는 clang 이 최적화하지 못하고(🟡 손실), 🔵 cross-layer 변환도 발화하지 못한다. B(layout-only)와 #2(hexa_int, runtime.h-가시)가 통과한 이유 = 경계 안쪽 layout-only 였기 때문.
- **열쇠 = LTO / same-TU 컴파일 = `.c=0` 졸업 (= RUNTIME.flip-floor, 이미 UNSHADOW 의 전제조건).** 이 벽을 제거하면 **두 tier 가 동시에** 열린다 — clang 이 런타임을 관통해 최적화하고(🟡 parity 유지·재구현 0) 🔵 cross-layer 변환이 열린 경계 너머로 발화한다(#2-ext·C 가 🔴→win 으로 전환).
- runtime hexa-native frontier(RUNTIME.flip/floor)는 UNSHADOW 의 **전제조건**. 이 도메인은 그 위에서 얻는 **perf 주권**을 측정한다 — 파일 안 겹침, 병렬 가능. 상세 = `UNSHADOW.easy.md`.

## milestones

- [x] hexa-native primitive vs clang -O2 baseline micro-bench harness (재사용 측정대 · M1)
- [x] 🎯 roofline 측정대 — 상대 Δ(vs clang -O2) → **HW 물리 천장 %** 절대 잣대 전환. 분모 = mini Apple M4 achieved-peak 1회 실측(STREAM-triad 92–95 GB/s · FMA double 15.24 GFLOP/s · scalar int 12.88 GIOP/s; theoretical 나란히 = 77–87%). `tool/unshadow_bench.hexa` 에 roofline % 컬럼(binding-roof 자동선택 = AI vs ridge 1.08) + `tool/unshadow_peak_microbench.hexa` 분모 측정대 추가. 기존 9개 닫힌 결과 ns → roofline % 재표기(§roofline 표): hexa C-emit 절대 천장 2–5%(native 5/5 패배가 % 로도 드러남) · #2 unbox 9%→101.9%(AT PARITY) · c_native 100% · 🔵 "안 돌기"(A·B)·RSS·CLOSED-NEG 은 roofline-N/A 정직 표기. g5 byte-diff 5/5 IDENTICAL. 상세=`UNSHADOW.bench.md §roofline`
- [x] 🟢 cross-layer 인라인 측정 — 런타임/유저 C-ABI 벽 제거 시 Δ — `hexa_int` 정수리터럴 박스 인라인(`((HexaVal){.tag=TAG_INT,.i=(N)})`) → mini macOS arm64 hot-loop ~28% faster (1.83→1.31s), -O2 `bl _hexa_int` 13→5; g5 byte-identical (md5 5dd08ae3). 상세 = UNSHADOW.log.md
- [x] 🔵 A atlas-guided const-fold 1건 (검증식 → codegen 직접 fold, "안 돌기" 실증) — `beenet_grid_bins(100.0,10.0)` → 검증 리터럴 `hexa_int(11)` 직접 emit. g5 byte-diff IDENTICAL (HIT+NO-HIT 양쪽, md5 `166d77ac`) · 핫루프 0.26→0.09s (~65%). 상세 = UNSHADOW.log.md
- [x] 🔵 C refinement-type bounds/null/tag-elision (타입정보 = 공짜 license) — **pilot = unary-plus `+literal` 의 numeric-operand tag-guard 삭제** — IntLit/FloatLit 는 정적으로 TAG_INT/TAG_FLOAT 라 `if(!HX_IS_INT||...) hexa_throw` arm 이 unreachable. g5 byte-diff IDENTICAL (HIT+NO-HIT, md5 `e8060fb1`/`8de57030`). 정직: correctness PROVEN(무손실)·perf 는 🔴 CLOSED-NEGATIVE — clang -O2 가 리터럴-tag guard 를 이미 dead-eliminate(base/new asm IDENTICAL). inline-가시 검사는 -O2 와 중복, opaque-value bounds/null 은 runtime.o ABI 벽(#2-EXT 와 동일). 상세 = UNSHADOW.log.md
- [x] 🔵 B proof-carrying 최적화 1건 (cross-layer 인라인과 결합) — 검증된 identity 를 license 로 opaque cross-ABI call 을 layout-only 인라인으로 치환. atlas `verified-lambda_eliashberg-num`(`lambda_eliashberg(0.5)=1.0` 🟢) 이 `lambda_eliashberg(M0)≡2·M0` 보증 → codegen 이 call 대신 `((HexaVal){.tag=TAG_FLOAT,.f=2.0*HX_FLOAT(x)})` 직접 emit(#2 runtime.h-가시 layout 결합). expr 인자 let-bind 1회(single-eval 증명, calls=1). g5 byte-diff IDENTICAL(HIT md5 `570eaa65` · single-eval `d44d54ac` · no-hit emit-C 동일) · 핫루프 0.36→0.19s (~47%, `bl _lambda_eliashberg` 소거). 상세 = UNSHADOW.log.md
- [x] 🟢 전용 arena reclaim 배선 — 자원(RSS·alloc 지연) 측정 — 기존 region-reclaim opt-in(`HEXA_VAL_ARENA`+`HEXA_STR_ARENA`, default ON) fully-off 대비 **peak RSS −40% · wall −26%** (mini, `self/bench/arena_reclaim_bench.hexa`, N=400k), g5 byte-diff 4/4 IDENTICAL. 정직 caveat: constant-factor win 이며 peak RSS 는 여전히 N 에 선형(bound 아님). 상세=`UNSHADOW.bench.md §arena-reclaim`
- [x] 🔵 E AoS↔SoA 자동전환 — **🔴 CLOSED-NEGATIVE (축 ruled-out).** REALITY CHECK: 모든 struct = `hexa_struct_pack_map`(필드명→HexaVal 해시맵, `self/codegen.hexa:7914`) · 필드 접근 = `hexa_map_get_ic`(해시 조회, `:5336`) · array-of-struct = `HexaArr` of boxed `HexaVal*`(=AoP, 각 원소 별도 힙 map, `self/runtime.h:74`). **정적 typed struct 레이아웃 부재** → SoA 표현 불가 3-fold: (a) monomorphic struct-array 타입 없음 (b) 필드-contiguous 메모리가 애초 없음(해시) (c) `double[]` unboxed-primitive 배열 타입 없음(`HexaArr`=24B boxed). 누락 인프라 = `typed monomorphic struct layout` + `unboxed-primitive array` (둘 다 0, `.c=0` 졸업과 직교한 별도 typed-repr RFC). codegen 무변경·byte-diff N/A·GPU 미사용. paper_negative_ok: 데이터-표현-자유 축 결정적 배제 = valid terminal. 상세 = UNSHADOW.log.md
- [x] 🟡 parity 상속 명문화 — C-emit 경로가 clang -O2 패스를 **부분 상속**함을 측정 확인 (재구현 0). 축1(상속): emit-C @-O0→@-O2 = 1.19×~1.78× speedup · hot-fn instr −44% → "rides clang -O2" 참. 축2(raw parity): hexa C-emit @-O2 / idiomatic ref-C @-O2 = **7.9×~1263× (≠1.0)** — runtime.o C-ABI 벽이 cross-TU fold/LICM 차단 → raw parity 는 free 아님, `.c=0` LTO 졸업 의존. g5 3/3 value-IDENTICAL. mini macOS arm64. 상세=`UNSHADOW.bench.md §parity-attest`
- [x] 🔵×🟡 LTO/same-TU unwall 측정 — runtime.o ABI 벽 제거 후 #2-ext(rt_str)·C(bounds/null) 전환 실측 (mini macOS arm64). **진단**: emit user.c=`#include runtime.h`+별 TU `runtime.o` 링크 = 벽; 내부 헬퍼(HX_STRLEN/hxlcl_strncmp/HX_ARR_LEN)는 runtime.c 아말감 안에만. **① `-flto` 불충분**(컴파일-타임 scope 실패, LTO 발화 전). **② same-TU(`#include runtime.c`)**: #2-ext **FLIP 🔴→WIN** 0.36→0.25s −31%, byte-identical(md5 `f869400e`) — 단 win 은 clang -O2 cross-TU 인라이너(맞춤 inline emit 은 redundant, 추가 Δ 0). **C = NULL**: 벽 제거+본문 가시에도 clang 이 opaque bounds check elide 안 함(throw 잔존, Δ 0) — proof-carrying codegen 필요(deferred). verdict=`.verdicts/unshadow-lto-unwall/` · bench=`UNSHADOW.bench.md §lto-unwall` · 재현=`tool/unshadow_lto_unwall_bench.hexa`
- [x] 🏗️ 적층 원칙 문서화 — 🟡 floor + 🔵 ceiling 스택 (LLVM 위에 올라타기, 싸우지 않기) — easy.md §HEXA-STACK(ASCII 다이어그램+compare 표) + **측정 증거표** 추가: 🔵 #2 인라인 28% · A fold 65% · B proof-carrying 47% · 🟢 arena RSS −40% · 🔑 same-TU FLIP −31%; 한계 정직(C tag-elision 🔴·bounds-null 🔴 NULL·HexaVal 언박싱=미측정 예측레버). 상세=UNSHADOW.easy.md §측정 증거
- [x] 🟢 HexaVal 언박싱 / register-pack — **pilot = known-int BinOp 결과 rebox 를 inline compound literal 로** (out-of-line `hexa_int(…)` → `((HexaVal){.tag=TAG_INT,.i=(…)})`, `_is_known_int` 증명 시만 발화·else 기존 boxed emit). g5 byte-diff IDENTICAL (before/after/ref stdout 동일, md5 `63888b02`) · asm hot-fn `bl _hexa_int` **17→0** · known-int 핫루프 **599→53ms (11.30×)** · parity gap **11.09×→0.98× (AT PARITY, 100% closed)**. mini macOS arm64. 정직: faithful C A/B proxy 측정(B9 빌드 벽으로 full self-host rebuild 차단; byte-eq 는 runtime 소스서 증명) · mut-누산기(fib류 known-int 미발화)는 미적용. 상세 = UNSHADOW.log.md · `UNSHADOW.bench.md §hexaval-unbox`
- [x] 🔵×🟡 same-TU 빌드 기본화 — C-emit 경로 same-TU 빌드 MODE 구현(`HEXA_SAME_TU=1`, GATED opt-in · self/main.hexa cmd_build: codegen 반쪽=`HEXA_USE_RUNTIME_C=1` user.c `#include runtime.c` + link 반쪽=별 runtime 2nd TU 제거). 측정 PILOT(mini · A/B 프록시, full rebuild=B9 벽 차단·스펙 허용): **BENEFIT** #2-ext류 경계호출 인라인 전면 개방(`_rt_str_starts_with`2→0·`_hexa_contains_poly`1→0·`_hexa_int`12→0) → string −21%·HexaVal-arith −24%(string 전용 아님). **COST** 빌드시간 ~35×(WARM 0.10s vs same-TU 3.55s · runtime.o 캐시 구조적 불가)·바이너리 −0.05%(wash)·generated-runtime.c 의존 재도입. g5 byte-IDENTICAL(`0e2afa85`·`657d1ec4`). **권고=OPT-IN flag, NOT default** (terminal). 상세=`UNSHADOW.bench.md §same-tu`·verdict=`.verdicts/unshadow-same-tu/`
- [x] 🔵 C-class proof-carrying bounds/null elision — **WIN.** pilot = `for i in 0..len(arr)` 의 per-iteration array-get bounds-check 삭제. STATIC PROOF(self/codegen.hexa): 배타 range + lower=리터럴 0 + upper=`len(arr)`/`arr.len()`(bare Ident) + body 가 arr resize/reassign·`i` reassign 안 함 → `0<=i<arr.len` exact·local. codegen 이 그 read `arr[i]` 를 bounds-free·throw-free direct read 로 emit(`HX_IS_ARRAY(arr) ? arr.arr_ptr->items[i] : 검사형` — untyped codegen 이라 array-tag guard 1개만 잔존, bounds+throw 만 삭제). HexaArr{items,len,cap}+arr_ptr 가 runtime.h public → runtime.o 벽 관통(unwall NULL=clang 한계, 이건 CODEGEN 증명=unwall 이 분리해낸 그 axis). g5 byte-diff IDENTICAL (in-range·OOB 양쪽 `fda59d53`). 무결성 게이트 PASS: OOB access(`arr[len]`)는 양쪽 arm 모두 여전히 throw(검증식이 안 덮는 read = 검사형, 증명-안전한 case 만 삭제). mini macOS arm64 hot-loop **1.88→0.56s (3.25×, −69%)**. faithful A/B proxy(full self-host regen=B9 generated-runtime 벽 차단·스펙 허용; emit 문자열은 codegen 과 byte-동일). 상세=`UNSHADOW.bench.md §c-class`·verdict=`.verdicts/unshadow-cclass-bounds/`·재현=`tool/unshadow_cclass_bounds_bench.hexa`

## milestones — typed-repr frontier (RFC=domains/UNSHADOW.typed-repr.md)

> 백로그 재개 — E(🔴) 의 근본 원인 "typed 표현 부재" 를 다음 frontier 레버로 등록. 둘 다 `.c=0` LTO 졸업과 직교(§c-class 가 입증: codegen 증명은 runtime.o 벽과 독립). 선후 = A → B → (B 완료 시) E 재오픈. 설계·착지점·게이트 상세 = `UNSHADOW.typed-repr.md`.

- [x] 🟢 unboxed-primitive array — **correctness WIN · perf 🔴 CLOSED-NEGATIVE (축 ruled-out: 갭은 tag-guard 아니라 STORAGE).** codegen 에 element-kind 추론 착지(self/codegen.hexa): 불변 `let xs=[int-lit…]`(전부 IntLit) → `_known_intarr_set` 등록, in-range fact 가 덮는 `xs[i]` read 가 (a)receiver TAG_ARRAY (b)elem TAG_INT 둘 다 정적증명 → §c-class array-tag guard(`HX_IS_ARRAY?`) 삭제 + `_is_known_int(xs[i])=true`(sum/dot 가 raw `.i` 추출). 발화=불변-let+live in-range fact 둘 다일 때만, 재대입/re-let 즉시 void, else 기존 BOXED 무변경. g5 byte-diff IDENTICAL 4-arm(ref·a_boxed·b_unbox·c_native, md5 `35470124`) + 동적경계 box 정확(typed array→polymorphic site, md5 `9efbbf5d` 양쪽 동일). **perf**: b_unbox 1.12s ≈ a_boxed 1.12s = ~0% Δ — tag-guard 는 loop-invariant 라 clang -O2 가 이미 hoist(삭제해도 wall 무변). 진짜 벽 = boxed 저장(sizeof HexaVal=16 → items[] 16B-stride, SIMD-gather 불가 5 vec-op); native int64_t[](c_native, 8B contiguous, 23 vec-op) 만 갭 100% close(0.08s≈ref). **누락 인프라 = native HexaArrI64/F64 저장 표현(runtime 변경, B9 벽 밖·codegen-only 범위 밖)** = 갭이 사는 곳. mini macOS arm64 best-of-9. verdict=`.verdicts/unshadow-unboxed-array/` · 재현=`tool/unshadow_unboxed_array_bench.hexa`. RFC=domains/UNSHADOW.typed-repr.md
- [~] 🔵 typed monomorphic struct layout (flat C-struct typedef + offset field access) — **부분 착지 (codegen arm + 천장 실증) · 변수-흐름 추적 일반화 open.** E(AoS↔SoA) 재오픈 선결. 현 struct=`hexa_struct_pack_map` 해시맵·field=`hexa_map_get_ic` strcmp/IC. **착지**: `self/codegen.hexa` GATED `HEXA_TYPED_STRUCT=1`(default OFF=무회귀) — `_is_flat_eligible_struct`(monomorphic 증명=declared·닫힌 필드집합) + `_flat_var_*`(불변 `let p=Pt{…}`/`Pt(…)` → 타입 추적, re-let void) + `gen2_flat_struct_typedef`(per-type `typedef struct Name__flat{HexaVal f0;…}` + flat ctor, 시그니처 동일) + Field arm offset 로드(`((Name__flat*)recv.vs)->f<idx>`). 다형/동적-키 struct=hash-map 유지(offset arm 미발화=무결성 게이트). **측정(mini arm64 best-of-9·faithful A/B proxy·B9 벽 차단·스펙 허용)**: g5 byte-diff **3/3 IDENTICAL**(`0f047a32`)+동적-키 무결성 IDENTICAL(`1dcca233`). a_hashmap 5.48s → b_flat(LANDED) 0.05s = **109×·gap 547×→5× AT PARITY**, asm call+branch 19→4(hash probe 소거). **honest scope**: 발화=불변 직접-construction 바인딩 + 정적 필드 read 만. fn 인자/반환 수신자·mut 재대입·eq/serialize polymorphic·중첩 필드·real self-host transpile 발화검증=open sub-task(emit 문자열은 codegen 과 byte-동일 확인). LLVM-can't=hash→offset 은 타입-레벨 shape 증명(clang 은 opaque map_get_ic 못 fold). verdict=`.verdicts/unshadow-typed-struct/`·bench=`UNSHADOW.bench.md §typed-struct`·재현=`tool/unshadow_typed_struct_bench.hexa`. RFC=domains/UNSHADOW.typed-repr.md

## milestones — mining-discovered paradigm axes (SSOT=domains/UNSHADOW.MINING.md)

> 거울방 cycle 2026-05-30: 14/15 의 모든 closed-negative 가 **데이터-표현 주권**으로 수렴 → /mining 으로 3 family 채굴(F1 표현주권 · F2 미채굴 perf 축 · F3 atlas-as-perf-asset). 아래는 grounded survivor 만 등록(ranking = MINING.md). 모두 이미-측정된 발견 OR LLVM-can't 근거에 묶임 — 새 perf 숫자 발명 0. ruled-out(codegen-only unbox 축A🔴 · AoS↔SoA-without-typed-repr E🔴) 재제안 안 함.

- [~] 🔵 native HexaArrI64 저장 표현 **[HEADLINE]** (F1 데이터-표현 주권) — **부분 착지 + 천장 실증.** 축A closed-negative 가 지목한 자리(갭=STORAGE)를 **결정적으로 확증**. runtime ABI 착지(`self/runtime_core_emit.hexa`): `HexaArrI64 {int64_t* data; int len; int cap}` struct + `hexa_arr_i64_new/push/len/box` 헬퍼 public-in-runtime.h(§c-class 벽 관통·layout=`HexaArr`와 동offset → polymorphic `hexa_len` 그대로 작동). codegen 착지(`self/codegen.hexa`, §unboxed-array 추론기 재사용): `HEXA_NATIVE_ARR=1` GATED opt-in(default OFF=무회귀) 시 불변 monomorphic-i64 리터럴 → native contiguous `hexa_arr_i64_new/push` 구성 + 증명된 in-range read = `((HexaVal){.tag=TAG_INT,.i=(((HexaArrI64*)xs.arr_ptr)->data[i])})` raw native load(inline-literal=§hexaval-unbox fold). **실측(mini arm64 best-of-9·faithful A/B proxy·B9 벽)**: g5 byte-diff **4/4 IDENTICAL**(`35470124`)+동적경계 box IDENTICAL(`9efbbf5d`). a_boxed 3.17s → **b_native(LANDED) 2.68s(1.18×·−15%)** → **d_ideal(CEILING) 0.18s ≈ ref 0.15s(17.6×·gap→0.83× AT PARITY)**. **천장 실증**: native storage 가 갭을 parity 까지 닫음 = 축A 예측 확증. LANDED 가 천장 일부만 잡는 이유 = read 가 여전히 boxed HexaVal surface(accumulator=untyped `let mut` → §hexaval-unbox known-int 체인이 sum 까지 안 뻗음·정직). **잔여 sub-task**(open): (1) known-int accumulator 일반화→read→sum 체인 raw(천장 도달) (2) F64 경로 (3) no-escape 자동발화(GATED 해제). verdict=`.verdicts/unshadow-native-arr/`·재현=`tool/unshadow_native_arr_bench.hexa`. MINING=F1·C1
- [~] 🟢 native-arr sub-task: known-int accumulator 일반화 — **부분 착지 (누산기 ABI-벽 닫힘 · register-pack open).** §hexaval-unbox known-int 체인을 **증명된 mut 누산기**까지 확장. 착지(`self/codegen.hexa`): `_known_int_accum_set` + `_known_int_accum_scan`(fn-body 보수적 스캔) — `let mut acc=<known-int init>` 이고 전 body write(`=`/`+= -= *= & | ^ << >>`)가 accum-int-safe(IntLit·known-int name·`acc` 자신·증명된 int-literal-array `arr[i]` read·그들의 `+/-/*…` BinOp)면 등록(`/` `%` `**` 제외=throw 필요·closure-capture 제외). `_is_known_int` 가 accum certify → CompoundAssign/BinOp 가 `acc<op>=rhs` 를 inline `.i` 형(`acc=((HexaVal){.tag=TAG_INT,.i=(HX_INT(acc) op HX_INT(rhs))})`)으로 emit → clang fold → out-of-line `hexa_add` 제거. GATED `HEXA_NATIVE_ARR`(§native-arr 재사용) default OFF=무회귀·FN-SCOPED reset. **codegen 발화 검증**(real self-host hexat 재빌드·/tmp transpile): FLAG ON = inline `.i` 누산 · OFF = `hexa_add` 무변경. **실측(mini arm64 best-of-11·faithful A/B proxy·B9 벽)**: g5 byte-diff **4/4 IDENTICAL**(`35470124`)+정확성(단순 `176ef0…`·integrity `+= -=`·BinOp·음수·3×10⁹ overflow `9e3d23e4…`). **e_boxed_acc(out-of-line hexa_add) 1.67s → f_inline_acc(inline `.i`) 1.12s = 1.49×**(asm `bl _hexa_add` 1→0·총 bl 9→6). full d_ideal(0.08s)은 여전히 open = HexaVal-struct 누산기(16B·step 마다 re-materialize·vec-op 8<18) → raw C `int64_t` local 강등 필요(다음 sub-task §typed-repr accumulator). verdict=`.verdicts/unshadow-knownint-accum/`·bench=`UNSHADOW.bench.md §knownint-accum`·재현=`tool/unshadow_knownint_accum_bench.hexa`. MINING=F1·C1
- [x] 🔴 native-arr sub-task: known-int accumulator → raw int64 local 강등 (§typed-repr accumulator) — **CLOSED-NEGATIVE (축 ruled-out · paper_negative_ok).** 증명된 known-int-accum 을 raw C `int64_t` local 로 강등(box surface 제거)해 f_inline_acc→d_ideal Δ 회수를 시도. **결과: clang -O2 SROA/mem2reg 가 single-field `{.tag,.i}` box 를 이미 scalarize** — `.tag` dead, `.i` 가 register(x8)로 승격 → inline `.i` 슬라이스(PR #2202)가 **이미 raw-int64 register loop** 을 emit한다. **THE 게이트**: f_inline_acc vs g_raw_local hot-loop asm **byte-IDENTICAL**(md5 `436ccab8`·`diff` exit 0)·hot-loop `bl _hexa_add` e=1/f=0/g=0·correctness e/f/g 일치(n=5/1e6/3e9 overflow). raw 강등은 **ZERO Δ 회수**(box 가 -O2 를 못 넘어 살아남지 못함 → 제거할 게 없음). prior `d_ideal 0.08s vs f_inline 1.0s "12.5× gap"` 은 **e_boxed out-of-line arm 측정 artifact**였음 — inline `.i` arm 자체가 천장. **ruled-out 축**: single-int accumulator 의 source-level box-stripping 은 -O2 하에서 dead lever. **남은 진짜 레버 = SROA 가 scalarize 못 하는 multi-field/escaping/aliased HexaVal 의 e_boxed ABI-벽 tax(`bl _hexa_add`)** — F64 경로(다음 sub-task)로 이관. codegen 무변경(GATED raw 강등은 확정 no-op-at-O2 라 미선적). verdict=`.verdicts/unshadow-knownint-rawlocal/`(finding.txt·byte-diff.txt)·bench=`UNSHADOW.bench.md §knownint-rawlocal`·재현=`tool/unshadow_knownint_rawlocal_bench.hexa`. MINING=F1·C1
- [x] 🟢 native-arr sub-task: F64 경로 (`HexaArrF64 {double* data}`) + no-escape 자동발화 — **WIN (천장 도달 · 갭 92.6% 닫음).** i64 path 의 F64 미러 착지 + GATED opt-in 을 no-escape 증명 시 default-ON auto-fire 로 해제. runtime(`self/runtime_core_emit.hexa`): `HexaArrF64 {double* data; int len; int cap}` typedef(layout=HexaArr/HexaArrI64 동offset → polymorphic `hexa_len` 불변) + `hexa_arr_f64_new/push/len/box` public-in-runtime.h(§c-class 벽 관통). codegen(`self/codegen.hexa`): `_is_float_literal_array`(monomorphic-f64 증명) + `_known_floatarr` lifecycle + LetStmt 가 `let xs=[f64-lit…]` 를 native contiguous `double[]` 로 lower + Index read 가 raw double load 를 INLINE TAG_FLOAT 으로 emit(`((HexaVal){.tag=TAG_FLOAT,.f=(((HexaArrF64*)xs.arr_ptr)->data[i])})`) → clang -O2 SROA 가 single-field box 를 scalarize → **read 가 이미 raw double**(i64 의 boxed-read-surface 한계를 F64 가 넘음). **no-escape auto-fire**: `_native_arr_noescape_scan`(§escape-stack 분석기 `_stmt_escapes_name`/`_expr_escapes_name` 재사용)이 binding 이 절대 escape 안 함을 증명 시 `_native_arr_autofire_ok` 가 **flag 없이 native 발화**(escaping=boxed 유지·무회귀). **실측(mini arm64 best-of-9·faithful A/B proxy·B9 벽·clang -O2)**: g5 byte-diff **4/4 IDENTICAL**(`0e996ac9`·출력 `1010000000000.000000`)+동적경계 box 2/2 IDENTICAL(`80a01ea6`·`7.5`). **a_boxed 1.495s → b_native(LANDED) 0.430s(3.48×·−71%) → d_ideal(CEILING) 0.394s ≈ ref_c 0.395s(1.09× AT PARITY)**. native 가 boxed→ideal 갭의 **92.6%** 를 닫음(i64 의 1.18× LANDED 를 크게 상회 — read surface 가 SROA 로 raw 화). asm SIMD `.2d/.4s`: a_boxed **0**(16B-stride boxed → 벡터화 불가) → b_native/d_ideal/ref_c 모두 **33**(native double[] 가 idiomatic C 와 동일 벡터화). **[NEGATIVE CONTROL]** auto-fire 가 escape-driven 증명: codegen native construct/read site 가 `_native_arr_autofire_ok` 게이트(call site 4·leftover combined gate `_native_arr_enabled() &&`=0) + proxy rep-grep escaping=`hexa_arr_f64_new` 0(boxed 유지)·non-escape=2(native). auto-fire=**ON by default**(GATED 해제 완료). verdict=`.verdicts/unshadow-native-arr-f64/`(finding.txt·byte-diff.txt)·재현=`tool/unshadow_native_arr_f64_cbench.hexa`·doc=`tool/unshadow_native_arr_f64_bench.hexa`. MINING=F1·C1
- [x] 🔵 검증 memoization (F3 atlas-as-perf-asset / 거울방) — **WIN (최소 슬라이스).** atlas-verified PURE fn(`lambda_eliashberg`, atlas node `verified-lambda_eliashberg-num` 🟢)의 같은-인자 반복 호출에 codegen 이 fn-local static last-arg 캐시 삽입. 라이선스 = atlas PURE @F 판정(deterministic·side-effect-free numeric) → cached-value ≡ recomputed-value EXACTLY. GATED opt-in `HEXA_VERIFY_MEMO`(default OFF=무회귀: OFF→§B closed-form inline 그대로). EXPR 1회 let-bind(single-eval, #2-ext 교훈). **END-TO-END**(edited self/codegen.hexa 로 full 트랜스파일러 재빌드 `/tmp/hexat.new`): OFF=`__le_x`(§B)·ON=`__lem_v`(memo) emit 확인, g5 byte-diff IDENTICAL(`2000000.0`, md5 `7fe719e9`). **OPAQUE-arg A/B proxy**(runtime.o C-ABI 벽 시나리오, noinline=opaque): call count **20M→1**, wall **0.1546→0.0255s (6.06×, −83.5%)**, byte-diff IDENTICAL(md5 `4c281195`). 무결성: NEGATIVE-arg 가드 보존(IDENTICAL) + cache-invalidation(varying arg→정확 재계산, match YES). LLVM-can't: clang `pure`/`const` 는 표현식 내 인접 동일콜만 CSE, runtime.o 심볼의 loop-cross idempotent 증명 불가 — atlas verdict DB 없음. 정직 caveat: codegen 에 LIVE atlas-query surface 부재(§A 처럼 narrow) → 발화 가드는 atlas-verified fn 名 하드코딩(verdict=라이선스). LITERAL-arg 시 §B inline(0 call)이 memo(1 call)보다 빠름 → memo 의 가치는 OPAQUE 반복인자 한정. faithful A/B proxy(full self-host regen=B9 generated-runtime 벽 차단·스펙 허용; emit=end-to-end 컴파일러 출력과 byte-동일). 상세=`UNSHADOW.bench.md §verify-memo`·verdict=`.verdicts/unshadow-verify-memo/`·재현=`tool/unshadow_verify_memo_bench.hexa`. MINING=F3·C18
  - [x] 🔵 (sub) LIVE atlas-query surface in codegen — **WIN** (§atlas-query 와 동일 인프라로 충족). codegen 이 컴파일타임에 perf-property atom(`perf-<fn>-hot`, pure/idempotent/hot-path 속성)을 SSOT TEXT-parse(lazy+캐시·O(1)) 로 LIVE 조회 — fn名 하드코딩 제거. memo framework 도 같은 `gen2_atlas_perf_hot` 조회 surface 를 재사용해 일반화 가능(현 §atlas-query 는 hot-path 태그로 layout 결정; memo 는 동일 atom 의 pure/idempotent 태그로 발화 — 동일 lookup, 다른 태그). 상세=§atlas-query.
- [~] 🔵 escape→stack-alloc (F2 미채굴 perf 축 · **공간축 첫 채굴**) — **부분 착지(최소 슬라이스) + 공간축 천장 실증.** 14 milestone 전부 time-축이었던 도메인의 첫 SPACE-축. 비-escape 증명된 flat-struct(§typed-struct #2182) 바인딩을 힙(malloc) 대신 C 스택에 배치. codegen 착지(`self/codegen.hexa`, GATED `HEXA_STACK_ALLOC=1`·default OFF=무회귀): (1) **escape 분석기** `_stack_noescape_scan`(fn-body 사전스캔, gen2_fn_decl)+`_stmt_escapes_name`/`_expr_escapes_name`(재귀) — immutable `let p = Pt{..}`(flat-eligible) 의 유일한 사용이 `p.field` read 뿐일 때만 비-escape 증명(return/call-arg/store/index/capture/reassign=힙 보수적 유지). (2) **stack-alloc emit** `gen2_stack_alloc_flat` — 증명된 바인딩의 LetStmt 를 `Pt__flat __stk_p = {..}; HexaVal p; p.tag=TAG_ARRAY; p.vs=(HexaValStruct*)&__stk_p;` 로 lower(malloc 우회·read offset layout 불변 → byte-eq). **실측(mini arm64 best-of-9·faithful A/B proxy·B9/regen 벽)**: g5 byte-diff **IDENTICAL**(acc=140000000·md5 `6ca934e49da9a8a3923d49622f65db6b` 양 arm 동일). **[PRIMARY 공간축]** heap-alloc count **a_heap 20,000,000 → b_stack 0**(루프 malloc 전멸). peak-RSS no-free/reclaim-lag shape(arena/GC 가 즉시 free 안 하는 실제 경우) **a_heap_nf 127,392 KB → b_stack_nf 1,920 KB = 66× RSS 감소**(124MB→1.9MB). 무결성: escaping(returned) 바인딩=힙 유지·return 후 live(no dangling)=4. **잔여 sub-task**(open): (1) non-flat HexaVal(array/map/closure descriptor) 스택 배치 (2) nested-scope 바인딩(현 top-level let 만) (3) GATED 해제(default 발화). verdict=`.verdicts/unshadow-escape-stack/`·재현=`tool/unshadow_escape_stack_bench.hexa`. MINING=F2·C13
- [x] 🔵 escape→stack sub-task: non-flat descriptor (array/map/closure) 스택 배치 — **WIN (bounded array slice).** flat-struct 만이던 escape-stack 증명을 **NON-FLAT descriptor**(bounded array literal)로 확장. codegen 착지(`self/codegen.hexa`, GATED `HEXA_STACK_ALLOC`·default OFF=무회귀): (1) **array escape 분석기** `_stack_noescape_arr_scan`(fn-body 사전스캔)+`_expr_escapes_arr_name`/`_stmt_escapes_arr_name` — `_expr_escapes_name` 미러하되 **index-read 수신자 `a[i]` + `len(a)` 만 허용**(return/call-arg/store/capture/reassign/index-write/push=힙 보수적 유지). immutable `let a = [e0…eN]`(bounded count) 의 유일 사용이 `a[i]`/`len(a)` read 뿐일 때만 비-escape 증명. (2) **stack-alloc emit** `gen2_stack_alloc_array_lit` — `HexaVal __stk_a_items[N]={…}; HexaArr __stk_a={items,N,N}; HexaVal a; a.tag=TAG_ARRAY; a.arr_ptr=&__stk_a;` 로 lower(hexa_array_new+push 우회·`arr_ptr->items[]` offset 불변 → byte-eq). **BOUNDED-ONLY**: unbounded payload(map hash-table·grown array·varying closure env)=힙 유지(stack buffer 불성장) — map/closure 는 의도적으로 ruled-out(축소된 honest scope). **실측(mini arm64 best-of-9·faithful A/B proxy·B9/install 벽·pool unreachable·Darwin LOCAL_BUILD gate)**: g5 byte-diff **IDENTICAL**(acc=210000000·md5 `250abd1aed19f5395ba84c8cf7c8c331` 양 arm 동일). **[PRIMARY 공간축]** heap-alloc count **a_heap 20,000,000 → b_stack 0**(루프 malloc 전멸). peak-RSS free-path **2,814,416 KB → 1,952 KB = 1442× 감소**(per-iter heap array 가 push realloc 으로 page 잔존). peak-RSS no-free/GC-reclaim-lag shape **564,416 KB → 1,936 KB = 291× 감소**(551MB→1.9MB·parent 의 66× 보다 강함=array 는 descriptor+items 두 alloc 이라 누적 더 큼). secondary wall 0.67→0.04s(16×). **[NEGATIVE CONTROL]** escaping(returned) array=힙 유지·return 후 live(no dangling)=4 → 결정이 escape-driven 임을 결정적 증명. 잔여(open): map hash-table/closure env_box bounded 케이스·nested-scope·GATED 해제. verdict=`.verdicts/unshadow-escape-stack-nonflat/`·bench=`UNSHADOW.bench.md §escape-stack-nonflat`·재현=`tool/unshadow_escape_stack_nonflat_bench.hexa`. MINING=F2·C13
- [~] 🟠 escape→stack sub-task: nested-scope 바인딩 + GATED 해제 — **부분 착지 (codegen LANDED · inspection-verified · 측정 BLOCKED · GATED 유지).** top-level let 만이던 escape-stack 분석기를 **nested if/while/for/match 본문 안 let** 으로 확장 — enclosing-block lifetime 으로 escape 증명. codegen 착지(`self/codegen.hexa`, GATED `HEXA_STACK_ALLOC`·default OFF=무회귀): `_stack_child_bodies`(중첩 블록 본문 열거)+`_stack_collect_flat_lets`/`_stack_collect_arr_lets`(임의 깊이 후보 재귀 수집)+`_stack_noescape_scan`/`_stack_noescape_arr_scan` 재작성(전-body escape 증명=기존 `_stmt_escapes_name`/`_stmt_escapes_arr_name` 재사용·이미 중첩 본문 재귀 → outer-var 저장/return/capture/push escape 어디든 잡힘). LetStmt arm 무변경(gen2_node 가 중첩 본문 재귀 → 같은 `gen2_stack_alloc_flat`/`gen2_stack_alloc_array_lit` 문자열을 enclosing C block scope 에 emit=무 dangling·offset 불변 → byte-eq by construction). **측정 정직 BLOCKED**: faithful A/B proxy(clang -O2+runtime.o·양 parent 와 동일 spec-accepted) 가 이 세션 컴파일 불가 — Darwin heavy-invocation sign-off 게이트(`clang`/`hexa` 절대·상대 경로 모두 차단·user TUI `! sidecar sign local` 토큰 필요)+빈 pool roster(unreachable). sibling 은 sign-off 토큰 보유·이 백그라운드 세션 미보유. **숫자 날조 0.** 중첩 arm emit 은 **측정된** nonflat parent(§escape-stack-nonflat)와 character-identical(let 의 lexical nesting 만 다름) → expected-by-identity(재측정 아님·명시): byte-diff IDENTICAL·heap-alloc 20M→0·peak-RSS no-free 564,416→~1,936 KB(291×). **[NEGATIVE CONTROL]**(설계): nested binding 이 outer-var 저장/return 시 힙 유지(live-after-return=4) → lifetime-scoped(syntactic 아님). **GATED 해제 결정: 미플립(`HEXA_STACK_ALLOC` default OFF 유지)** — default-ON 무회귀를 corpus 실행으로 증명 불가(동일 게이트가 `hexa test`/self-host 도 차단) → milestone 지시("증명 못 하면 GATED 유지·정직 보고·위험한 default-ON flip 보다 opt-in 이 낫다")대로 opt-in 보존. 잔여(open): sign-off 가능한 세션에서 live 실측 + corpus 무회귀 후 default-ON flip. verdict=`.verdicts/unshadow-escape-stack-nested/`(finding.txt·byte-diff.txt)·bench=`tool/unshadow_escape_stack_nested_bench.hexa`. MINING=F2·C13
- [ ] 🔵 NaN-boxing HexaVal 표현 (F1 데이터-표현 주권) — **DESIGN + feasibility + 최소측정 착지(전역 flip 아님 · 의도적 honest 분해 · milestone 지시 "블라스트 大→C1·C13 이후" 준수).** 사실 정정: `sizeof(HexaVal)=16B`(24B는 stale) → 밀도 2×(3× 아님). 규모 산정=**multi-session(1-batch 불가)**: union 물리 제거라 레이아웃 가정 사이트 전부 동시 flip 必 — worktree grep 실측 `HX_*` 매크로 1151 + emitted `TAG_*` 430 + compound-literal `((HexaVal){.tag=..})` 19(매크로 우회=전역 flip 단일 토글 지점). 두 표현 공존=dual ABI(runtime.o+user.c 동시 재빌드→B9 벽 충돌). 최소측정(faithful A/B proxy·mini arm64·best-of-7): sequential **2.5–2.8× 느림** 🔴 · value-pass **30–36% 느림** 🔴(register-fit 가설 falsified — box/unbox mask+shift+OR 추출비용>1-reg 이득·초기 "37% 빠름"은 측정 artifact) · random/cache-pressure **7–11% 빠름** 🟢(유일 승). → 우위는 memory-bound random 단 하나로 좁음·milestone "캐시밀도 3×" 기대 대폭 축소. NaN-collision 제약=음수/payload NaN canonicalize 必·raw-bits 관찰(bit-cast·직렬화·hash-of-double) 프로그램은 결정적 미커버. C1 의존 정정=상충(sequential 퇴보)·분리(종목 다름). 전역 flip 은 GATED default OFF sub-task open 유지(nanbox.md §8 선결 5항). codegen 무변경=byte-diff N/A. 설계=`domains/UNSHADOW.nanbox.md`·verdict=`.verdicts/unshadow-nanbox/`·bench=`§nanbox`·재현=`tool/unshadow_nanbox_bench.hexa`. MINING=F1·C2
- [~] 🔵 atlas-guided PGO — layout/inline 결정 (F3 atlas-as-perf-asset / 거울방) — **부분 착지 (LAYOUT 결정 발화 + byte-eq 실증) · wall-lever 는 perf-atom schema 로 deferred (E 와 동일 narrow 벽).** 라이선스 = atlas verdict(`verified-lambda_eliashberg-num` @F 🟢 = pure hot-path atom)이 inline/hot LAYOUT 결정을 구동 — 사용자 `@inline_always`/`@hot` 어노테이션 아님·런타임 프로파일 아님. clang PGO 는 `.profdata` 런 필요; atlas 가 곧 프로파일(컴파일타임·런 0). **착지**(`self/codegen.hexa` GATED `HEXA_ATLAS_PGO`·default OFF=무회귀): `gen2_is_atlas_pgo_hot` + `gen2_fn_forward`/`gen2_fn_decl` 가 atlas-verified fn 을 `static inline __attribute__((hot))` 로 자동 승격(§A const-fold→fn-LAYOUT 일반화). **END-TO-END**(edited self/codegen.hexa 로 full 트랜스파일러 재빌드 `/tmp/hexat.new`): OFF=`HexaVal lambda_eliashberg(…)` 평범 out-of-line · ON=`static inline … __attribute__((hot))` 자동승격(어노테이션 0) 확인. **실측(mini arm64·faithful A/B proxy·B9 벽·스펙 허용)**: g5 byte-diff **IDENTICAL**(`239999988.0`·md5 `b38a2a0c…`, inline/hot=의미보존 레이아웃). **[PRIMARY 레이아웃축]** out-of-line 심볼 `_lambda_eliashberg` nm **OFF=present(`T`) → ON=absent**(out-of-line copy 소거) · otool lambda ref **1→0**. **wall Δ ≈ 0 (AT PARITY·정직 NULL)**: best-of-7 OFF 0.38s ≈ ON 0.38s — TINY LEAF fn 은 clang -O2 가 call-site 마다 이미 인라인 → out-of-line copy 제거는 code-SIZE/layout 효과지 hot-loop 속도 아님. wall 레버가 사는 곳 = out-of-line call 이 실제 지배하는 OPAQUE/cross-C-ABI 심볼(§B/E runtime.o 벽 시나리오, clang 이 안 인라인) → 거기서 `hot` 섹션배치+inline-force 가 움직임. **잔여 sub-task**(open): codegen LIVE atlas-query surface + perf-property atom schema (§A/§B/E 와 동일 narrow 벽 — verdict=하드코딩 라이선스). verdict=`.verdicts/unshadow-atlas-pgo/`·bench=`UNSHADOW.bench.md §atlas-pgo`·재현=`tool/unshadow_atlas_pgo_bench.hexa`. MINING=F3·C17
  - [x] 🔵 (sub) codegen LIVE atlas-query surface + perf-property atom schema — **WIN.** §A/§B/E/G 가 공유하던 하드코딩 가드 제거. (1) **perf-property atom** 등록(embedded.gen.hexa·atlas_fold 거버넌스): `@F perf-lambda_eliashberg-hot ... perf = "hot-path pure idempotent inline-worthy" · derived-from = verified-lambda_eliashberg-num` — fn perf 속성을 atlas atom 1급 표현(기존 F 커널 재사용 → 새 KIND 스키마변경·atlas_cli 미러 회피). (2) **codegen LIVE 조회**(self/codegen.hexa): `gen2_is_atlas_pgo_hot` 가 `node.name == "lambda_..."` 하드코딩→`gen2_atlas_perf_hot(node.name)` LIVE 조회. SSOT TEXT-parse(CU당 1회 lazy read + module-global 캐시, fn-decl당 O(1))로 `perf-<fn>-hot`+`hot-path` 태그 존재시만 승격. 경로 `$HEXA_LANG > ./`(main.hexa 의존 없음 — regen amalgam 自족). **실측(mini arm64·faithful A/B proxy·B9 벽)**: g5 byte-diff IDENTICAL(`2.4e+08`·md5 `71f62d5d…`) + [PRIMARY] out-of-line sym OFF=present→ON=absent·otool 1→0 + **[NEGATIVE CONTROL] SSOT 에서 perf atom 제거시 같은 fn名·HEXA_ATLAS_PGO=1 이어도 승격 안 함(grep 1→0, emit 평범)·복원시 재발화 = 결정이 fn名 아닌 atlas 조회 구동 결정적 증명.** wall 은 §atlas-pgo 와 동일 TINY LEAF NULL(이 변경은 가드 제거지 wall 레버 아님). OPAQUE/cross-ABI hot fn 일반화 = 차단 해제(임의 fn perf atom 등록 가능), GATED 해제 = 별도. verdict=`.verdicts/unshadow-atlas-query/live-query.txt`·bench=`§atlas-query`.
