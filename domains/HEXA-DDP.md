# HEXA-DDP — current state

@title: 🔗 HEXA-DDP — multi-GPU data-parallel training (hexa-native collectives)

@goal: hexa flame 트레이너가 2+ GPU 에서 data-parallel(DDP)로 학습 — gradient all-reduce 를 hexa-native 로 (NCCL 의존 없이) 구현해, 단일-GPU 효율(이미 [[HEXA-FUSION]] own-GEMM PARITY + conv cure 로 확보)을 다중-GPU throughput 로 확장한다. **정직 경계 (g5)**: 병렬은 throughput(처리량)↑이지 단일-step util 해법이 아님 — N-GPU 가속은 항상 N배 미만(통신·동기화·Amdahl 세금). 모든 단계 byte-eq 게이트(1-GPU == N-GPU 결과) FIRST, 그 다음 speedup 측정. cuBLAS/NCCL = roofline, 우위 주장 0.

@blocker: ① 통신 프리미티브(all-reduce) — M1 GREEN(hexa-native ring, byte-eq) ✅ ② 멀티-GPU pod rent 인프라 — M2 INFRA-READY(`DDP_NUM_GPUS` param + topo probe) ✅; wildcard 사실 = vast 2-GPU offer 64건 LIVE, 최저 reliable NVLink 2x A100_SXM4 $1.47/hr → 하드웨어 가용·저렴, 실 렌트/지출만 사용자 결정. 남은 율속 = M3(실 2-GPU P2P all-reduce, 실측 topo + 하드웨어 세션 필요).

## ── milestones (의존성 순) ──

- [x] **DDP-M1 — ring-all-reduce 통신 프리미티브 (hexa-native) ★ FIRST, 하드웨어 불요** — gradient sum 을 N-rank 가 나눠 합치는 ring-all-reduce 를 hexa 로 구현. **단일-GPU 안에서 2-rank in-process 시뮬**로 정합성 선검증: all-reduce(g0,g1) 결과 == 직렬 sum, byte-eq max|Δ|=0 (g5). 하드웨어 없이 지금 착수 가능 — DDP 의 진짜 심장. falsifier: 2-rank sim all-reduce == serial-sum byte-eq. **🟢 GREEN (#2886)** — stdlib/ddp/ring_all_reduce.hexa (reduce-scatter + all-gather, staged per-step copy) + in-process N-rank sim; 9/9 pass, N∈{2,4}, S not mult of N (7/10/13/64), byte-eq max|Δ|=0 FP64, steps=2(N-1) 확인. verdict: .verdicts/hexa-ddp-m1/F-DDP-RING-BYTEEQ.txt. **정직 경계: 이건 COLLECTIVE-CORRECTNESS leg only — 실 multi-GPU transport(P2P/NCCL)=M3, multi-GPU 하드웨어=M2; M1 GREEN ≠ 병렬 학습 동작.**
- [x] **DDP-M2 — 멀티-GPU pod rent 인프라** — tool/ 의 rent 경로가 `num_gpus≥2` 인스턴스(vast/runpod)를 잡고 NVLink/PCIe 토폴로지를 프로브하도록 확장 (현재 num_gpus=1 고정). 하드웨어-접근 wildcard — 전체 일정의 율속단계. falsifier: 2-GPU 한 노드 rent + `nvidia-smi topo -m` 캡처. **🟢 INFRA-READY** — (a) `tool/ddp_rent_lib.sh` `ddp_gpu_pred()` env `DDP_NUM_GPUS`(기본1)로 num_gpus 파라미터화 — unset/=1 → 정확히 `num_gpus=1` 리터럴(1-GPU 경로 byte-eq 불변), ≥2 → `num_gpus>=N`; (b) `tool/ddp_topo_probe.sh` on-pod `nvidia-smi topo -m` 캡처+NVLink/PCIe/SYS 파싱(합성 2/4-GPU 행렬로 파서 검증); (c) **wildcard 사실**: vast 2-GPU offer 64건 LIVE(NVLink-class reliable 35건), 최저 reliable NVLink 2x A100_SXM4 = **$1.4676/hr** — `.verdicts/hexa-ddp-m2/availability_probe.md` + raw JSON. **정직 경계(g5): topo 실측 행렬은 DRY-RUN(leak-0 rent 명령 명시)이고 실제 캡처는 DDP-M3 TODO; 멀티-GPU 학습 동작 보증 아님 — M2=인프라+가용성 사실, 실 학습 정합성=M3/M4.** 실 렌트/지출은 사용자 결정.
- [x] **DDP-M3 — 단일노드 2-GPU P2P all-reduce (실 하드웨어)** — DDP-M1 의 ring 을 실제 2-GPU 에 cudaMemcpyPeer/NVLink P2P 로 land. 의존: M1+M2. falsifier: 2-GPU all-reduce == 1-GPU serial byte-eq. **🟢 GREEN (#2889)** — vast 2x RTX 3090 한 노드. stdlib/ddp/m3_p2p/ring_p2p.cu (M1 의 2(N-1) ring schedule + chunk partition 그대로, per-step transport 만 실 cudaMemcpyPeer device-to-device 로 교체). N=2, S=7(S%N=1 boundary) ∧ S=1<<20(large), FP64 result == serial elementwise sum, **byte-eq max|Δ|=0 둘 다 PASS**. verdict: .verdicts/hexa-ddp-m3/F-DDP-M3-REAL-P2P-BYTEEQ.txt. pod destroyed (leak 0). **TRANSPORT FINDING (정직)**: 렌트 노드 topo = PHB(PCIe host-bridge, NVLink 아님), `cudaDeviceCanAccessPeer==0` → GeForce 직접 P2P driver-disabled → cudaMemcpyPeer 가 host-staged 로 라우팅 (정확성 동일, perf caveat: NVLink/직접-P2P 대역폭 미사용). **정직 경계: M3 GREEN = COLLECTIVE 가 실 multi-GPU 하드웨어에서 동작 → 관문3 transport 절반 CLOSED. end-to-end 병렬 학습(1-GPU vs 2-GPU same-model byte-eq over a flame step)=M4, 4-GPU scale=M5 아직 남음.**
- [x] **DDP-M4 — DDP 정합성 게이트 (1-GPU vs 2-GPU)** — 같은 모델·시드·step 을 1-GPU 와 2-GPU(글로벌 batch 동일)로 돌려 weight/loss byte-eq. 의존: M3. **g5 HARD GATE** — 이게 통과해야 DDP 가 '정확'. falsifier: 1 vs 2 GPU max|Δ|=0. **🟢 GREEN** — vast 2x RTX 3060 한 노드 (instance 39793396, label hexa-ddp-m4, DESTROYED leak-0). stdlib/ddp/m4_train/ddp_train.cu (3-layer MLP 16→32→32→8 ReLU,ReLU,linear · NPARAM=1864 · FP64 · B_GLOBAL=64 split 32/32 over W=2). 1-GPU reference = shard-partials 를 RING 순서대로 합산 후 /B (true byte-eq, tolerance 아님 — order-matched 라 FP-assoc 무관); 2-GPU DDP = grad SUM ring all-reduce (M1 schedule, M3 실 cudaMemcpyPeer transport) 후 동일 /B + 동일 SGD step. **W_ddp == W_ref max|Δ|=0 BYTE-EQ PASS** + loss match 0 + grad match 0 + rank-agreement 0 (4 게이트 전부 byte-exact). loss_ref==loss_ddp=2.1718481686851199. transport=staged-host (GeForce PHB, P2P driver-disabled — 정확성 transport-독립). verdict: .verdicts/hexa-ddp-m4/F-DDP-M4-TRAIN-BYTEEQ.txt (verbatim). **정직 경계(g5): M4 = DDP TRAINING 불변식을 작은 모델·1 step 으로 실 2-GPU collective 위에서 증명 (병렬 학습 byte-exact = "새 frontier" 완료). full 7B DDP·multi-step accum·4-GPU speedup(M5)=out of scope — 그러나 grad-of-sum=sum-of-grad 대수는 임의 규모에서 동일.**
- [x] **DDP-M5 (collective leg) — 4-GPU ring all-reduce 확장 + scaling 측정** — M3 의 실-P2P ring 을 2→4 rank 로. **🟢 GREEN (#2892)** — vast 4x RTX 3060 한 노드. stdlib/ddp/m5_4gpu/ring_p2p_m5.cu (M3 의 2(N-1) ring schedule·chunk partition·cudaMemcpyPeer transport 그대로, rank count 만 env `DDP_NUM_GPUS`=4 로). N=4, S=13(S%4=1 boundary) ∧ S=1<<20(large), FP64 result == serial elementwise sum, **byte-eq max|Δ|=0 둘 다 PASS**; step-count 2(N-1)=6 확인; scaling sweep (same total bytes) per-step wall N=2 11.12ms → N=4 9.78ms (all-reduce wall 22.24→58.70ms = step 2→6 탓, per-step ≈flat). topo = PHB(GPU0-2 NUMA0)+SYS(GPU3 NUMA1), `canAccessPeer==0` 전 pair → host-staged cudaMemcpyPeer (정확성 동일, NVLink/직접-P2P 대역폭 미사용). verdict: .verdicts/hexa-ddp-m5/F-DDP-M5-4GPU-COLLECTIVE.txt. pod destroyed (leak 0). **정직 경계 (g5): collective 가 4-GPU byte-exact 로 scale ≠ 4-GPU 학습 가속. end-to-end 병렬 학습 speedup = M5b (M4 에 chain). N-GPU 가속 < N× (통신세, g83).**
- [x] **DDP-M5b — 4-GPU end-to-end 학습 speedup** — M4(1 vs N-GPU byte-eq) + M5 collective 위에서, 실 flame step 의 4-GPU step/s vs 1-GPU 측정. **🟢 GREEN (#2897)** — vast 4x RTX 3090 한 노드. stdlib/ddp/m5b_speedup/ddp_train_m5b.cu (M4 의 flame MLP fwd/bwd train step + M5 의 4-rank ring all-reduce 합성; N=1/2/4 per-step wall + N=4 byte-eq). **GATE1 정합성**: 1-GPU W_ref == 4-GPU W_ddp **byte-eq max|Δ|=0** (FP64, grad·weights·rank-agreement 전부 0) — M4 DDP invariant 를 N=2→4 로 확장. **핵심 N=4 content (FP 비결합성)**: ring reduce-scatter 는 각 chunk 를 chunk-종속 RIGHT-nested tree 로 합산 (chunk0=(3+(2+(1+0))) 등; 4-rank 시뮬로 도출) — naive left-fold reference 는 ULP 차이(측정 1.42e-14→3.55e-15)였고 이는 transport 오류가 아니라 순수 결합성. reference 를 동일 right-nested tree 로 합산 → 진짜 max|Δ|=0 (N=2 는 add 1개라 M4 가 trivially order-invariant; tree 는 N≥3 에서만 작동). **GATE2 speedup** (median 30 reps, per-step wall = fwd+bwd+all-reduce+sync, size sweep H={64,256,1024,2048}): 4-GPU speedup 0.608x→0.621x→0.689x→**0.725x** (eff 15.2%→18.1%), 2-GPU 0.804x→0.901x (eff 40→45%). **정직 (g5/g83)**: 가속 < 1× — host-staged GeForce 노드(전 12-pair `canAccessPeer==0`, NVLink 아님, topo NODE+SYS)에서 작은 모델은 통신>연산 → 4-GPU 가 더 느림 (실패 아님, Amdahl/통신세 현실). 단 efficiency 가 모델크기에 **monotonic 상승** → crossover 방향 측정됨; 4-GPU 우위는 (a) 훨씬 큰 모델 ∧/또는 (b) NVLink/직접-P2P 필요. verdict: .verdicts/hexa-ddp-m5b/F-DDP-M5B-4GPU-SPEEDUP.txt. transport=staged-host cudaMemcpyPeer. pod 39798076 destroyed (leak 0, project-tag-checked).
- [x] **DDP-M6 — 노드간(멀티노드) all-reduce — socket/TCP** — 노드 경계 넘는 all-reduce (단일노드 P2P 불가). 의존: M5. falsifier: 2-노드 byte-eq. **두 leg (provider-isolated) 으로 증명 — 각 provider 의 inter-node networking 을 독립 검증:**
  - [x] **DDP-M6 (vast leg) — 2-vast-node TCP ring all-reduce** — **🟢 GREEN** — vast 2 노드 별개 호스트·별개 public IP·별개 국가: rank0 = instance 39797909 Poland 91.150.160.38, rank1 = instance 39797928 Ukraine 82.193.103.124 (둘 다 label hexa-ddp-m6, DESTROYED leak-0). stdlib/ddp/m6_multinode/ring_tcp_m6.c — M1/M3/M5 의 2(N-1) ring schedule·chunk_start partition 그대로, per-step transport 만 cudaMemcpyPeer(intra-node) → **host-to-host TCP sendall/recvall(inter-node socket)** 로 교체. 각 rank = 별도 프로세스(별도 호스트), predecessor listen + successor connect, parity-ordering 으로 2-cycle deadlock-free. host-memory FP64 ring (no CUDA) — cross-node TRANSPORT 정확성만 격리. vast pod 의 container 포트는 Docker port-map 으로 public IP 에 노출(direct_port_count>0) → 두 pod 가 public IP+external port 로 직접 도달(proxy/relay 불요, data-plane). N=2, S=7(S%2=1 boundary) ∧ S=1<<20(large), FP64 result == serial elementwise sum, **byte-eq max|Δ|=0 — 두 케이스·양 rank 전부 PASS** (동유럽 두 데이터센터 간 public Internet 을 FP64 바이트가 건너 bit-identical 도착; TCP reliable+ordered). loopback intermediate proof 도 4 케이스 byte-eq PASS. verdict: .verdicts/hexa-ddp-m6/F-DDP-M6-MULTINODE.txt (verbatim).
  - [x] **DDP-M6 (runpod leg) — 2-runpod-node socket ring all-reduce** — 🟢 **GREEN**. M1/M3/M5 의 canonical ring schedule (2(N-1)=2 steps, `chunk_start` partition BYTE-IDENTICAL) 그대로, transport 만 cudaMemcpyPeer(단일노드 P2P) → D2H + **TCP send/recv (host network)** + H2D 로 교체해 **노드 경계를 넘김**. 두 개의 별도 runpod SECURE-cloud H100 pod (node A 216.243.220.230 / pod wosowf0qknt0au · node B 216.243.220.219 / pod iz1e1v3h4n6n5y — 다른 머신, 다른 internal /16). cross-host 증명: pod A 가 자기 인터페이스(172.23.0.2)에서 pod B PUBLIC IP(216.243.220.219:16917)로 ESTAB TCP 보유 + 모든 all-reduce chunk 가 그 wire 통과. **GATE (g5): N=2, S=7 (S%N=1 boundary) ∧ S=1<<20 (large), FP64 result == serial elementwise sum, BOTH ranks byte-eq max|Δ|=0 — 둘 다 PASS.** verdict: .verdicts/hexa-ddp-m6-runpod/F-DDP-M6-RUNPOD.txt (verbatim). 코드 stdlib/ddp/m6_socket/ring_socket_m6.cu (deadlock-free SYN/ACK 핸드셰이크 wiring — tunnel-far-end-refused race 방어). 두 pod 즉시 DESTROYED (leak 0). **TRANSPORT FINDING (정직)**: runpod 커스텀 TCP 포트(5700/5701)는 public proxy(`runtime.ports`)로 안 떴음 → cross-node TCP 는 두 pod 의 public SSH 엔드포인트 간 SSH port-forward 로 수립 (여전히 real host-network 커널 TCP, reliable+ordered — 게이트가 증명하는 transport 정확성은 carrier 가 직접 proxy 포트든 SSH forward 든 무관). **정직 경계 (g5): collective 가 runpod 노드 경계를 byte-exact 로 넘음 ≠ multi-node 학습 가속, ≠ interconnect(RDMA/IB) 대역폭 주장 (여기 경로는 pods public link 위 TCP). 정확성은 transport-독립.** 두 leg = provider 별 inter-node networking 독립 증명.
- [x] **DDP-M7 — PRODUCTION 7B-scale DDP byte-eq (1-GPU == N-GPU)** — M4/M5b 가 작은 MLP(H≤2048, ≈4M)에서 증명한 DDP 학습 byte-eq 불변식을 **생산 규모(target 7B)에서 실측 검증**. grad-of-sum=sum-of-grad 대수는 scale-invariant 라 작은 모델에서 carry 돼야 함 — M7 은 그걸 7B 에서 empirically 확인. **🟢 GREEN** — vast 4x **H200**(143GB VRAM/GPU, 2015GB host RAM) 한 노드 (instance 39809972, label hexa-ddp-m7, DESTROYED leak-0, project-tag-checked). stdlib/ddp/m7_7b/ddp_train_m7.cu (M4/M5b 의 flame-MLP fwd/bwd + canonical ring all-reduce 그대로, NPARAM 을 H 로 production 규모까지 push; gate 는 **dtype 무관 true max|Δ|=0** — byte-eq 는 reduction-ORDER 로만 깨지지 dtype 으로 안 깨지므로, reference 가 ring 의 per-chunk **right-nested tree(M5b finding)** 로 reduce). **두 leg 가 production 영역을 bracket**: (A) **FP64 5.493B params** (H=74048) max|Δ|=0 [VRAM-CAPPED — FP64 7B 는 168GB/GPU>143GB 라 안 맞음] · (B) **fp32 7.011B params** (H=83664, **HIT 7B TARGET**) max|Δ|=0. 두 leg 모두 weights·grad·loss·rank-agreement 전부 정확히 0, 실 **NVLink P2P** cudaMemcpyPeer ring (H200 SXM, canAccessPeer enabled). 실 4-GPU ring 이 7.011B-element grad vector 를 1-GPU right-nested reference 와 byte-identical 하게 reduce+SGD. verdict: .verdicts/hexa-ddp-m7/F-DDP-M7-7B-BYTEEQ.txt (양 leg verbatim stdout). **정직 경계 (g5)**: 단일 step·wide 3-layer MLP(ring 은 architecture 무관하게 NPARAM-length grad vector 만 carry)에서 증명. **7B 는 FP64 로 못 돌림** (VRAM 부족) — 명시; 7B leg 은 fp32 의 true max|Δ|=0(order-matched, tolerance 아님). multi-step accum·7B speedup 은 out of scope (speedup=M5b). 작은 모델 max|Δ|=0(M4/M5b) + 7B max|Δ|=0(M7) 이 production 영역을 gap 없이 bracket — **DDP 불변식은 7B 규모에서 HOLD.**
- [x] **DDP-M5c — NVLink crossover: multi-GPU 학습이 1-GPU 를 이기는 모델 크기** — M5b 의 예측(NVLink → crossover 작아짐) 을 **REAL NVLink** 노드에서 TEST. vast 4x A100-SXM4-40GB **두 노드 A/B** (동일 실리콘, P2P transport 만 다름): NVLink 노드 = instance 39811910 (mach 41067, **NV12 bonds, cudaDeviceCanAccessPeer=1 ALL 12 pairs** → direct-P2P ring) vs staged 노드 = instance 39809783 (mach 38233, **PHB, canAccessPeer=0 ALL pairs** → host-staged). 둘 다 label hexa-ddp-m5c, DESTROYED leak-0. 모델 = M5b MLP 64→H→H→64 FP64, B=64, H 스윕 {512,1024,2048,2560,3072,3456} (H≥4096 은 A100 167KB shared-mem optin cap 초과). **GATE(1) 정합성: N=4 byte-eq max|Δ|=0** (grad·weight·rank-agreement 전부 0, FP64, ring right-nested reduce tree — transport-독립, 양 노드 동일). **GATE(2) crossover: NVLink 가 comm tax 를 거의 완전 제거** — eff 2-GPU 43%→**49.5%**, 4-GPU 18%→**24.2%** (이상치 50%/25% 에 근접). 하지만 **per-step wall 은 NVLink 에서도 1.0x 를 넘지 못함** (2-GPU 0.99x asymptote, 4-GPU 0.974x). **정직 FINDING (g83)**: comm 이 원인 아님(NVLink 가 해결) — 순수 DATA parallelism 은 batch(B=64) 만 샤딩하는데 H→H MLP 의 step cost 는 H² GEMM = **weight-bound** 이라 64행을 16행으로 쪼개도 per-rank FLOPs 가 거의 안 줆 → 각 rank 가 1-GPU 의 거의 전체 compute + (이제 싼) all-reduce 수행. 따라서 small-batch data-parallel per-step wall 은 **구조적으로 1.0x 가 상한** (어떤 transport 든). 1-GPU 를 실제로 이기려면 (a) batch-bound regime (큰 B → throughput 가속, DDP 의 진짜 용도) 또는 (b) model/tensor parallelism (H² weight 샤딩) 필요. **DURABLE: ① N=4 byte-eq 가 real NVLink P2P 에서 max|Δ|=0 ② NVLink 가 DDP 효율을 NEAR-IDEAL 로 끌어올림 (comm tax 제거, A/B 측정) ③ small-batch per-step 1.0x 상한은 weight-bound 구조 속성 (transport 결함 아님).** **TRANSPORT 발견 (정직)**: vast SXM4 = NVLink P2P 보장 안 됨 — 첫 노드(동일 A100-SXM4 실리콘)는 P2P disabled(PHB), 둘째 노드는 enabled(NV12). canAccessPeer CUDA 프로브가 유일한 권위 (SXM4 form factor ≠ NVLink active). 코드 stdlib/ddp/m5c_crossover/ddp_train_m5c.cu + run_m5c.sh. verdict .verdicts/hexa-ddp-m5c/F-DDP-M5C-CROSSOVER.txt (양 노드 wall table + canAccessPeer + byte-eq verbatim).
- [x] **DDP-M6b — M6 WAN ring all-reduce 의 THROUGHPUT 특성화 (perf, NOT 정합성)** — M6 (#2899/#2900) 은 노드 경계 byte-eq 만 증명(byte-eq=latency-독립, throughput 명시적 미측정). M6b = 그 EXACT transport 의 perf envelope 실측: (1) RTT floor, (2) all-reduce BW(GB/s) vs payload 1KB..256MB sweep, (3) latency→bandwidth crossover, (4) TCP window(BDP) 효과. **🟢 GREEN (perf-char)** — vast 2 노드 대륙간 WAN: rank0 = instance 39810496 Poland 91.150.160.38(ring ext :16989), rank1 = instance 39810517 California US 74.48.140.178(ring ext :44291) (둘 다 label hexa-ddp-m6b, DESTROYED leak-0). stdlib/ddp/m6b_throughput/ring_perf_m6b.c — M1/M3/M5/M6 의 2(N-1) ring schedule·chunk_start partition·parity send/recv 그대로(transport=M6 host-to-host TCP), RTT ping-pong + per-size median-wall BW sweep + `--single`(대형 단일사이즈 probe)·`--sockbuf`(SO_SND/RCVBUF window 축) 계측. **측정 결과**: RTT ≈ **300-320 ms** (PL↔CA, 3 독립 측정 314.8/300.7/319.1); BW-vs-size 곡선이 1KB..1MB 전 구간 **flat ~3-4e-5 GB/s (~30-40 KB/s)** — 소형(≤16KB)=~0.6-1.0s=~2 RTT floor(순수 latency), 1MB all-reduce=31s. **핵심 NEGATIVE (4)**: 32MB SO_SNDBUF/RCVBUF(kernel rmem/wmem_max 128MB로 상향) sweep 이 default-buffer 곡선과 **구분 불가** → 율속은 TCP window/BDP 가 **아님**; ring 의 phase 당 blocking send-then-recv 가 매 phase RTT 를 직렬화 → RTT-dominated. **effective GB/s ceiling ≈ 3-4e-5 GB/s** (링크 nominal ~800Mbit 대비 2-3 orders 아래) — 측정 가능 전 사이즈에서 **bandwidth-bound 진입 안 함**. verdict: .verdicts/hexa-ddp-m6b/F-DDP-M6B-WAN-THROUGHPUT.txt (RTT·BW table·sockbuf probe verbatim). **정직 경계 (g5): 이건 commodity-WAN-TCP perf 현실이지 RDMA/datacenter 수치 아님, 정합성 주장 아님(M6 소유). 실용 takeaway = cross-country 동기 DDP 는 latency-지배 → compute>>comm 이거나 phase-overlap pipeline 재구성 시에만 유효.**
- [x] **DDP-M5d — THROUGHPUT scaling: DDP 가 실제로 이기는 regime (per-GPU batch FIXED, global batch ∝ N)** — M5b/M5c 의 핵심 발견(per-step latency 는 fixed-batch 를 N 으로 쪼개면 weight-bound 라 1.0x 가 상한)을 받아, DDP 의 **설계 용도**인 THROUGHPUT 을 측정: per-GPU batch 고정(B_perGPU=64)·global batch = B_perGPU×N 으로 키워 각 GPU 가 동일 work·step 당 N× samples → samples/sec. **🟢 GREEN** — vast 4x **A100-SXM4-40GB** 한 노드 (instance 39860052, label hexa-ddp-m5d, DESTROYED leak-0, project-tag-checked). **topo = NV12 ALL pairs (full NVLink mesh), `cudaDeviceCanAccessPeer=1` 전 12 directed pair** (M5c "SXM4≠NVLink 보장" gotcha 명시 확인 — 이 노드는 real NVLink). model MLP 64→H→H→64 FP64, B_perGPU=64 고정, H 스윕 {256,1024,2048,3072}. **GATE(1) 정합성: N=4 byte-eq max|Δ|=0** (grad·weight·rank-agreement 전부 0; 1-process ref 가 same B_global=256 을 ring 의 per-chunk right-nested tree 로 reduce — M5c finding, transport-독립). **GATE(2) THROUGHPUT SCALING (핵심 — per-step 과 정반대로 N 에 따라 UP)**: samples/sec scaling 2-GPU 1.903×→**1.989×** (eff 95.2%→**99.5%**), 4-GPU 3.342×→**3.918×** (eff 83.5%→**97.9%**) — 모델 크기 ↑ 마다 ideal N× 로 **monotonic 접근** (comm tax = 동일 NPARAM grad all-reduce 가 더 큰 compute 에 amortize). H=3072: 4-GPU = 3.918× = perfect-4× 의 **97.9%**. **명시 대조 vs M5b/M5c**: M5b/M5c 는 FIXED global batch B=64 를 N 으로 쪼개 per-step wall 측정 → NVLink 에서도 1.0x 못 넘음 (2-GPU 0.99x, 4-GPU 0.974x = weight-bound H² GEMM, batch 만 샤딩). M5d 는 per-GPU batch 고정·global batch ∝ N → samples/sec 가 ~N× scale = **DDP 가 마침내 1-GPU 를 이기는 metric, 설계 regime**. **정직 (g5/g83)**: eff<100% (comm tax 현실) 이나 per-step <1× 와 달리 throughput 은 N 에 따라 UP 하고 gap 이 모델크기에 shrink. 단일 step·wide MLP·FP64·real NVLink; ring 은 architecture 무관 NPARAM grad vector 만 carry 라 throughput 대수(상수 per-rank compute·N× samples/step)는 scale-invariant, eff 는 compute/comm 비율이 결정. verdict: .verdicts/hexa-ddp-m5d/F-DDP-M5D-THROUGHPUT.txt (topo+canAccessPeer+samples/sec table @1/2/4-GPU·eff·byte-eq verbatim). 코드 stdlib/ddp/m5d_throughput/ddp_train_m5d.cu + run_m5d.sh.

- [ ] **DDP-M8 — multi-step grad-accum + optimizer-state byte-eq (correctness 심화)** — K-step 동안 1-GPU vs N-GPU 가 weight AND optimizer-state(momentum/Adam moment) byte-eq 유지 — 단일-step(M4) 넘어 누적 step + optimizer 상태까지. falsifier: K=10 step 후 W·m·v max|Δ|=0. 의존: M4.
- [ ] **DDP-M9 — bf16/fp16 real-dtype DDP 재현성** — production dtype(bf16/fp16)에서 1-GPU vs N-GPU DDP step 재현성 — FP64 max|Δ|=0 가 아니라 same-dtype rel-RMS 게이트(dtype 명시). reduction-order 고정 시 bit-exact 가능한지 확인. falsifier: bf16 1==N-GPU rel-RMS ≤ dtype eps. 의존: M4.
- [ ] **DDP-M10 — end-to-end 수렴: 1 vs N-GPU loss-curve 다단계 일치** — "진짜 같은 모델 학습" — 짧은 학습 run(수십 step)을 1-GPU 와 N-GPU 로 돌려 loss-curve 가 step-by-step 일치. M4 의 단일-step 불변식이 학습 궤적 전체로 carry 됨을 증명. falsifier: 1 vs N-GPU per-step loss max|Δ|=0 over the run. 의존: M8.
- [ ] **DDP-M11 — tensor/model parallelism — H² weight 샤딩 (per-step 이기는 축)** — M5c 가 가리킨 길: data-parallel(batch 샤딩)은 weight-bound 라 per-step 1.0x 상한 → tensor-parallel 로 H² weight 자체를 N-rank 가 분할(column/row-parallel GEMM + all-reduce). per-step latency 가 1-GPU 를 실제로 이기는 유일한 병렬축. falsifier: TP per-step wall < 1-GPU ∧ byte-eq max|Δ|=0. 의존: M5(collective).
- [ ] **DDP-M12 — ZeRO optimizer-state sharding (메모리 축)** — optimizer state(Adam m·v)·gradient·param 을 N-rank 에 샤딩해 per-GPU 메모리 ÷N — 7B+ 를 작은 VRAM 에 적재. byte-eq 유지 + per-GPU peak memory 측정. falsifier: ZeRO-1/2/3 vs 1-GPU byte-eq ∧ mem ÷N 확인. 의존: M8.
- [ ] **DDP-M13 — pipeline parallelism — layer 분할 + micro-batch** — 모델 layer 를 stage 로 분할, micro-batch 파이프라인. bubble overhead 측정 + byte-eq. tensor/data 와 직교하는 3번째 병렬축. falsifier: PP 1==N-stage byte-eq ∧ bubble 측정. 의존: M11.
- [ ] **DDP-M14 — 8-GPU throughput scaling (M5d ~N× 가 8 까지 유지되나)** — M5d 의 near-ideal throughput scaling(4-GPU 97.9%)이 N=8 에서도 버티나 — NVLink-섬/NUMA 가로지르는 8-GPU 노드에서 samples/s eff. 정합성은 N-generic(recheck) 이라 제외, scaling 데이터만 새것. falsifier: 8-GPU samples/s eff + byte-eq 유지. 의존: M5d.
- [ ] **DDP-PAPER — /paper — DDP 캠페인 출판 (자본화, GPU 0)** — M1~M7+M5b/c/d·M6/b/c 의 검증 결과를 arxiv-style paper 로: §finding = (1) bracket 논증(작은 모델+7B max|Δ|=0 이 production 을 gap 없이 감쌈), (2) per-step↔throughput metric 정정(M5c weight-bound 천장 → M5d ~N× regime), (3) WAN byte-eq(2국·2provider) + RTT-bound throughput. paper_gate: 전 section verdict terminal(전부 🟢). 비용 0.
- [ ] **DDP-DOC — README trinity + dojo 갱신 (north-star 축④, 자본화 GPU 0)** — README 의 flame·forge·hexa-cuda trinity 섹션에 HEXA-DDP(NCCL-free hexa-native 분산학습) 추가 + dojo DDP 레시피 cross-link. doc-only, blast radius 0. 비용 0.
