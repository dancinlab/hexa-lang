C-ZERO GPU-substrate shim — runtime_cuda byte-equality oracle verdict (2026-06-03)
host: summer (pool · NVIDIA RTX 5070 12GB · nvcc 13.x · /usr/local/cuda) — no rental, no teardown.
context: self/cuda/runtime_cuda.c graduated (git rm) in 3dd5b9c93 (#1884, B9.C-8). The ONLY
  restorable baseline is 3dd5b9c93^:self/cuda/runtime_cuda.c (145049B, 3250 LOC).

HONEST FINDING (g63) — NOT byte-eq-retirable against the available baseline:
  The runtime_cuda_emit.hexa SSOT has EVOLVED SUBSTANTIALLY beyond the #1884 baseline.
  Emitter output (gen) = 241115B vs baseline (orig) = 145049B (+66%). The gen file carries
  newer content absent from the baseline (e.g. F-CLM-DEVFEED device-feed path + UTF-8 (×, →)
  comment glyphs). gate-1 source-text sha DIFFERS (orig=9b2e0c33.. gen=7e4f2639..).

  Real nvcc runs on summer RTX 5070 (-O2 -DHEXA_CUDA -x cu -I/usr/local/cuda/include -I.):
    orig (3dd5b9c93^ baseline) → nvcc -c OK, orig.o = 447968B.
    gen  (emitter output)      → nvcc -c FAIL: 'extended character × is not valid in an
       identifier' at runtime_cuda.c:3289 / '→' at :3292 (UTF-8 glyphs the emitter injects
       into a comment region nvcc's tokenizer rejects).

  Unlike forge_tier_v1 (same 3dd5b9c93^ baseline → .o BYTE-IDENTICAL, emitter unchanged),
  runtime_cuda's emitter is a LIVE, still-evolving SSOT: the .c was already removed in #1884
  and the emitter kept advancing, so there is NO current authored .c to byte-compare against.
  A byte/.o diff vs the 4-year-stale baseline is the WRONG comparison and is not claimed.

VERDICT: NOT-RETIRABLE-BY-BYTE-DIFF (emitter diverged from stale baseline; live SSOT).
  - The .c is ALREADY graduated (git rm in #1884) — no tracked C remains to retire.
  - The emitter output is newer than the only restorable baseline; behavioural nvcc .o parity
    is not establishable against a stale reference, and the gen output additionally trips
    nvcc on UTF-8 comment glyphs (a latent emitter-text quality issue worth a separate fix).
  - 3795 LOC stays in the C-ZERO ledger REMAINING column, attributed honestly (not faked PASS).
