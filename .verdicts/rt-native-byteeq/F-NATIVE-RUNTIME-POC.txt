F-NATIVE-RUNTIME-POC — 2026-06-16
═══════════════════════════════════════════════════════════════
leg B native runtime 토대 작동 확인 — aprime 가 stdlib/runtime/*.hexa 를
native obj 로 컴파일(rt_* global 심볼) = leg A byte-eq 졸업의 직접 활용.

PoC (aprime_nr, --emit=obj --target=arm64-apple-darwin, 캡처출력):
- math.hexa   → 30,464B · T 심볼 _rt_cos/_rt_sin/_rt_exp/_rt_log/_rt_fmod/_rt_acos/log2/log10
- ctype.hexa  → 52,720B OK
- posix.hexa  →  4,136B OK
- net.hexa    →    808B OK
- numeric.hexa → rc=1 FAIL (의존성 — 별도 진단).
∴ aprime 가 .hexa 런타임 모듈을 native obj(global rt_* 심볼)로 컴파일 = leg B
  native runtime.a 의 핵심 토대 작동 확인.

ABI 갭 (통합 시 wrapper 필요):
- math.hexa `pub fn rt_cos(x: float) -> float` → native obj 가 float ABI.
- runtime.c `extern HexaVal rt_cos(HexaVal x)` → HexaVal 16B ABI.
- 심볼명(_rt_cos) 동일하나 ABI 불일치 → 그대로 링크 시 호출 깨짐.
- 통합 = HexaVal↔float ABI wrapper(active hexa_math_cos wrapper 패턴) OR
  hexa-source 를 HexaVal 시그니처로 재작성.

leg B native runtime 작업 (확정 경로):
- (1) stdlib/runtime/*.hexa 를 aprime native obj 로 → rt_*.o (PoC ✅, numeric 수리)
- (2) runtime.c/core.c 코어(HexaVal struct/macro/값op/arena/dispatch ~1190 fn)도
  .hexa native (메커니즘 전부 증명됨 이번 세션 — 값op·arena·dispatch).
- (3) ABI wrapper 통합 + native runtime.a 구성 + restore C-seed 경로 제거.
- (4) self/runtime.c/core.c build 미생성 → ls self/*.c == ∅ + native(C-free).
거대 다세션 전수. leg A 양 타깃 byte-eq 졸업(#3412/3432)이 native 컴파일 토대.

NEXT: numeric.hexa FAIL 진단·수리 → 8개 모듈 전부 native obj → ABI wrapper PoC →
점진 코어 fn .hexa 화.


## numeric FAIL 해결 (2026-06-16, flatten)
numeric.hexa 단독 컴파일 FAIL = `undefined name rt_log10`(:220) — rt_log10 은
math.hexa 정의(PoC 의 _rt_log10 T 심볼). 단독 `--emit=obj` 가 import 미해결 →
undefined (진짜 버그 아님). math+numeric flatten(import 라인 제거 concat) →
native obj OK 118,912B, `_rt_array_*` 등 T 심볼 다수. ∴ stdlib/runtime/*.hexa
전부 flatten → aprime native obj 가능(import 함께 해결) = native runtime.a 의
stdlib 부분 토대 완성.

leg B native runtime 진척:
- stdlib/runtime/*.hexa flatten native obj ✅ (math/ctype/posix/net 단독 + numeric flatten).
- 남은: (1) HexaVal struct/macro/값op/arena/dispatch 코어 ~1190 fn .hexa 화(거대,
  메커니즘 전부 증명됨 이번 세션) (2) ABI wrapper(float↔HexaVal) (3) native runtime.a
  통합 + restore C-seed 제거. 거대 다세션 전수.


## 코어 native 범위 측정 (2026-06-16, nm build/runtime.a)
runtime.a(567448B) 심볼 분포:
- 총 T(global) 심볼: 744.
- hexa_* (코어 값op/생성자/string/array/map/arena): **591**.
- rt_* (stdlib 위임, native obj PoC ✅): 42.
- 기타: 111.
∴ leg B 코어 native 대상 = **591 hexa_* fn**(1190 추정 → 591 정확화). stdlib 42 는
native obj PoC 완료. 코어 591 = HexaVal 값op(hexa_int/bool/float/void/truthy/add/cmp)
+ string/array/map(arena 기반) + arena. 이번 세션 메커니즘 전부 증명:
- 값op leaf intrinsic(__hx_tag/payload_*/str_byte/to_double, #3400-3409).
- native arena(stateful, mmap/munmap/ptr R/W, #3413-3416).
- dispatch(rt_cmp_lt==C 11/11, #3405).

leg B 잔여 (확정): 591 코어 fn .hexa native(leaf intrinsic+arena 기반) + ABI wrapper
+ native runtime.a 통합 + restore C-seed 제거 → ls self/*.c==∅ + C-free. 거대 다세션
누적. leg A 양 타깃 byte-eq 졸업(#3412/3432)이 native 컴파일 토대.


## 코어 native 의 성격 확정 (2026-06-16, value_logic_fixture.hexa)
stdlib/runtime/value_logic_fixture.hexa 주석이 명시: 코어 hexa_* fn(hexa_truthy/eq/
is_type 등)은 HexaVal tagged-union 내부(HX_TAG/HX_INT/HX_BOOL)를 조작 → "hexa source
가 HexaVal tag 를 직접 construct/inspect 못 함 = **codegen-emit pass 의 일**(frontier
#1 phase-H), stdlib 추가 아님". fixture 는 SEMANTICS 만 hexa 로 보존(A/B byte-eq 용).

★ 핵심: 이번 세션 leaf intrinsic(__hx_tag/payload_lt/gt/le/ge/eq/nz/add/sub/mul/
fadd~/str_byte/to_double/arr_len/syscall0/6/ptr_store/load, #3400-3417)이 정확히
그 "codegen-side HexaVal 조작"을 구현 = 코어 native 의 직접 진전. value_logic_fixture
가 "codegen-side work 필요"라 한 것을 이번 세션이 leaf intrinsic 으로 착수.

leg B 코어 591 fn native 성격 (확정):
- = codegen-emit 확장(leaf intrinsic + typed-glue lowering) + 일부 stdlib hexa-source
  (numeric.hexa 에 rt_eq/add/cmp_lt/len 존재). 순수 stdlib 추가 아님.
- 이번 세션 ~18종 leaf intrinsic = 코어 native 의 codegen 토대. 591 fn 전수는
  각 fn 의 codegen-emit 포팅(leaf 조합 + 글루) = 거대 다세션 누적 codegen 작업.
- leg A 양 타깃 byte-eq 졸업(#3412/3432) = 이 codegen 이 양 타깃에서 정확·결정적.

leg B 완료 경로 (최종 확정): 591 코어 fn 의 codegen-emit native 포팅(leaf+글루) +
ABI wrapper + native runtime.a + restore C-seed 제거. 다세션 누적, 메커니즘 완비.
