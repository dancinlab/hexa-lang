F-HEXA-CC-NATIVE-N5PRE-STEP0B-R2 -- runtime-source drift decomposed (regen works; __map_raw_len genuine-missing)
==============================================================================================================
date: 2026-06-01   host: mini (darwin-arm64)   base: worktree off origin/main 9be1ed393
follow-up to F-HEXA-CC-NATIVE-N5PRE-STEP0B-DARWIN.txt (round 1 = build-recipe gap)
compiler-source parity: `git diff origin/main HEAD -- compiler/ self/` = EMPTY (current-main source)

PROGRESS THIS ROUND (the recipe gap is now traversable, exposing the real frontier)
-----------------------------------------------------------------------------------
Populated the self/ runtime tree with .c-only copies (NEVER clobbering .hexa sources;
round-1 failure was `cp -R .../*` dumping non-.c entries onto the flatten input):
  - self/{runtime.c,runtime_core.c,runtime_hi_gen.c,runtime.h}  <- build/self/ (2026-05-26)
  - self/native/*.c (18 needed, 33 copied)                      <- build/self/native/
  - self/forge/forge_tier_v1.c                                  <- build/self/forge/
runtime.c's full local #include set enumerated up front (forge/ + 18 native/ + core + hi_gen).
=> All #include resolution PASSED; clang reached actual C compilation of the 40k-line ap_post.c.

clang then surfaced UNDEFINED RUNTIME SYMBOLS = current-codegen vs runtime DRIFT:
  (1) float_to_bits / bits_to_float  -- STALE-runtime. The current self/runtime_core_emit.hexa
        L1505-1507 EMITS `HexaVal float_to_bits(HexaVal)`, but the 2026-05-26 build/self/runtime_core.c
        has 0 occurrences. FIXED by regen: `hexa run self/runtime_core_emit.hexa self/runtime_core.c`
        (ran LOCALLY, routing-immune, wrote 382923 B with float_to_bits x4). Symbol resolved.
  (2) __map_raw_len  -- GENUINE-MISSING. Referenced ONLY in self/codegen.hexa (an EMITTED call); no
        definition in ANY runtime source -- not runtime_core_emit (regenerated), not runtime_hi_gen,
        not the 2026-05-26 runtime.c, nowhere. So current codegen emits a map-length intrinsic call
        that NO runtime implements. plus a function-to-HexaVal arg type mismatch at ap_post.c:40446.

ROOT CAUSE (refined)
--------------------
Between the 2026-05-26 aprime_cc build and current main, self/codegen.hexa gained an emit for
`__map_raw_len` (and the float_to_bits intrinsic) but the runtime SSOT was only partially updated:
runtime_core_emit got float_to_bits; the map-length helper was never added. The native self-host
build of CURRENT main is therefore broken at link/compile on a genuine-missing runtime symbol --
independent of the type-checker's 181 diagnostics, and UPSTREAM of them (the build can't even
produce aprime_cc to run the --emit=obj measurement).

This is a real defect on main's native self-host path, not merely a stale artifact:
  codegen emits __map_raw_len  ==>  runtime defines __map_raw_len   : MISSING.

HONEST STATE
------------
- regen mechanism PROVEN (emit SSOT -> .c, local). float_to_bits class auto-heals on regen.
- __map_raw_len is a genuine runtime gap requiring a real definition added to the runtime SSOT
  (self/runtime_core_emit.hexa or a native/*.c), implemented to match codegen's calling convention
  -- NOT guessable safely here without the map-repr contract; deferred to a scoped runtime fix.
- top runtime.c amalgamation file is untracked with no single regen entry identified (candidates:
  self/runtime_core.hexa / self/runtime_hi.hexa) -- separate thread.
- No binary kept; no milestone flipped; live tree untouched; worktree disposable.

NEXT
----
N5-pre-rt: add __map_raw_len (+ resolve the :40446 fn/HexaVal mismatch) to the runtime SSOT to
match self/codegen.hexa's emit contract; regen; rebuild aprime_cc; if smoke exit42 PASSES, run the
181 --emit=obj re-measure (the original STEP0 goal) on the now-current-source binary.
Falsifier unchanged: an intentional undefined-name source must emit HX2001 + non-zero exit.
