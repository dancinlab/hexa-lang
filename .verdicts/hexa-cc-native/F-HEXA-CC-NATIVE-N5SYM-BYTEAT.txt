F-HEXA-CC-NATIVE-N5SYM-BYTEAT — byte_at → hexa_str_byte_at (N5-sym pt4)
falsifier: after mapping, bare `_byte_at` must vanish from cc-cb2.o undefined set.

STATUS: ⚪ UNVERIFIED — verification build FAILED (infra, not code).

method: rebuild aprime (transpiler=hexa_v2; hexat was cleaned from the shared
tree) with the byte_at-only edit → re-emit cc-cb2.o → nm -u.

verbatim (real log):
  [3/5] post-process: s4_flatc_post + builtin sed + runtime.c inline
  build_aprime: clang failed
  self/runtime.c:13567:10: fatal error: 'forge/forge_tier_v1.c' file not found
  BUILD FAILED — aprime_cb2 absent

cause: the populate step copied self/native/*.c but NOT self/forge/*.c, which
the amalgam runtime.c #includes. A populate gap, not a code defect.

byte_at edit is correct BY SIGNATURE ANALYSIS (hexa_str_byte_at(HexaVal s,
HexaVal idx) matches the generic raw-HexaVal STMT_CALL emit) but is NOT yet
build-verified. Re-run with forge/*.c populated before claiming 🟢.

get/set: DEFERRED (not mapped). Runtime ABI hexa_map_get(HexaVal, const char*)
— a bare rename passes a raw HexaVal where const char* is expected (links
clean, miscompiles at runtime). Needs a hexa_to_cstring(key) coercion branch.
