F-BUILDFLOOR-M1-BUILD — HEXA-BUILDFLOOR M1 build/smoke gate
Verdict: 🟠 PARTIAL — bootstrap + build progressed to/through link; final smoke
         UNVERIFIED this session due to a total tool-readback (stdout/Read) outage.
Date: 2026-05-31  (sign window active)
Worktree: /Users/mini/dancinlab/hexa-lang/.claude/worktrees/agent-ad2240d8ec720abeb
HEAD: 0a2b8c2f857b5bb486177a36c0be164f3045ef72

(NOTE: this file SUPERSEDES an earlier intra-session honest-STOP draft that was
written during a transient output lag which later proved to be delayed, not lost.
Those delayed outputs DID arrive and are incorporated below.)

============================================================
A. IN-PROCESS VERIFIED FACTS (read back successfully, cross-checked ≥2×)
============================================================

SAFETY PRE (hashlib.md5, in-process):
  ~/.hx/bin/hexa.real  size=1651728  md5=7493583ecd76b9214b0b9d6d7726ec64
  (saved: _evidence/hexa_real_md5_PRE.txt; value confirmed identical across 3 reads)

M1 CODE deliverable — the 3 path fixes in tool/build_hexa_cloud.hexa — PRESENT
(file md5 7aa0f397ccb10939936ae9e4dcb52d60, 12686 bytes, read in-process):
  L192-194  transpiler = _resolve(..., repo+"/build/hexa_v2", repo+"/self/native/hexa_v2")
  L201-203  runtime_c  = _resolve(..., repo+"/build/self/runtime.c", repo+"/self/runtime.c")
  L206      inc_gen    = repo+"/build/self"   (-I build/self for runtime.h)
  => builder code is correct & merged. M1 code portion DONE (not re-edited).

PREREQ artifacts in worktree at session start — ALL ABSENT (os.path.getsize):
  build/hexa_v2, build/hexa_module_loader, build/self/runtime.c, build/self/runtime.h.
  self/runtime.c & fragments ABSENT (gitignored/generated); only self/runtime.h (107932).
  self/native/hexa_cc.c ABSENT. => canonical build_hexa_cli.hexa bootstrap CANNOT
  cold-boot here (Step 0 L623-637 requires build/hexat OR hexa_cc.c; WARM-SEED-ONLY
  per L613-622). So I bootstrapped from the installed toolchain (~/.hx) instead.

BOOTSTRAP (gate a) — seeded from a single matched generation, in-process verified:
  Sources (md5 in-process):
    ~/.hx/bin/build/hexa_v2            sz=1930080 md5=099c73d97db9478fb036452310826a2d (Mach-O)
    ~/.hx/bin/build/hexa_module_loader sz=490496  md5=053968ceb300ed9115b9daf8bc237911 (Mach-O)
    ~/.hx/src/self/runtime.c          sz=629579  md5=32a08eb7cab63e3aa4ef5c64f58582d0
    ~/.hx/src/self/runtime.h          sz=100189  md5=fad4a4cd09c7c3b56647c62184ca8833
  Copied -> worktree build/ (md5 re-verified identical post-copy):
    build/hexa_v2            1930080  099c73d97db9478fb036452310826a2d
    build/hexa_module_loader 490496   053968ceb300ed9115b9daf8bc237911
    build/self/runtime.c     629579   32a08eb7cab63e3aa4ef5c64f58582d0
    build/self/runtime.h     100189   fad4a4cd09c7c3b56647c62184ca8833
  Darwin codesign -s - on both Mach-O binaries: rc=0 ("replacing existing signature").
  => gate (a) MET (in-process getsize each, non-zero). saved: _evidence/artifacts_after_bootstrap.txt

BUILD ATTEMPT #1 (NO_SMOKE=1) — rc=1, read back in full:
  module_loader flatten + transpile (build/hexa_v2) SUCCEEDED against THIS worktree's
  stdlib/cloud/cloud_cli.hexa (proves seeded transpiler+loader work). clang link FAILED:
    build/self/runtime.c:1570:10: fatal error: 'runtime_core.c' file not found
  Root cause: runtime.c amalgamates ~20 transitive #includes (runtime_core.c,
  native/*.c, forge/*) resolved via -I build/self; I'd seeded only runtime.c+.h.
  (saved verbatim: _evidence/build_log.txt, build_result.txt)

FIX: copied the FULL matched-generation ~/.hx/src/self C/H subtree into build/self/
  (runtime_core.c, runtime_hi_gen.c, native/*.c, forge/forge_tier_v1.{c,h}, …) so every
  transitive include resolves. (initiated: _evidence/build_self_seed.txt)

============================================================
B. UNVERIFIED THIS SESSION (readback outage — NOT claimed as pass)
============================================================
After the build/self full-seed, the tool READBACK channel (both Bash stdout AND the
Read tool) went fully silent for the remainder of the session — every call returned
empty. The following were EXECUTED (Python ran; filesystem side effects occur) and
PERSISTED to files, but I could NOT read them back, so per the anti-fabrication
mandate I do NOT assert their values:
  - BUILD ATTEMPT #2 (post full-seed): _evidence/build_log2.txt / build_result2.txt
  - bin/hexa-cloud existence + size + md5
  - bin/hexa-cloud --help smoke (exit code + help text)  [gate c — NOT taken cleanly]
  - SAFETY POST md5 of ~/.hx/bin/hexa.real: _evidence/hexa_real_md5_POST.txt
    (PRE==POST assertion computed in-process but the boolean was not read back)

GATE STATUS:
  (a) 3 bootstrap artifacts exist : MET (verified).
  (b) bin/hexa-cloud built        : UNVERIFIED (build2 ran; result unread).
  (c) --help exit 0 + help text   : UNVERIFIED (not cleanly captured/read).
  SAFETY hexa.real PRE==POST      : PRE verified; POST written but unread.
  No mutation of ~/.hx was attempted at any point (read-only source reuse + local copies).

============================================================
C. RESUMABLE — exact steps to close (run when readback recovers)
============================================================
The build is one verified-read away from done. Next session:
  1. Read _evidence/build_result2.txt — if rc==0 & bin_exists, gate (b) MET.
  2. If bin missing, Read build_log2.txt for the next clang include error and seed
     that fragment from ~/.hx/src/self (same generation) — repeat until link clean.
  3. Run: bin/hexa-cloud --help > _evidence/help_smoke.txt 2>&1 ; echo rc=$? ; Read it.
     Confirm exit 0 + contains 'hexa cloud' and 'cloud run' (gate c).
  4. Read _evidence/hexa_real_md5_POST.txt; assert md5==7493583ecd76b9214b0b9d6d7726ec64.
  5. If a+b+c+safety all pass -> flip this verdict to 🟢 and HEXA-BUILDFLOOR.md M1 -> [x].
NOTE: build/ and build/self/ are .gitignore'd per-machine artifacts — they are the
bootstrap, NOT a code deliverable; do not commit them. The only committable output of
this gate is the verdict + log docs.
