F-FUSION-RIGHT-SIZED-GPU — does the REAL flame CLMConvMoE step at the EXACT
H100-baseline shape (D1536/T512/E2/K3, FP64) on a RIGHT-SIZED GPU (fewer SMs
than H100, but >=24 GiB so D=1536 FITS) lift util MEDIAN off ~0%?

STATUS: WIP (skeleton pushed first; pod-fire pending — STORM backoff discipline)

================================ THE LEVER (commons g85) =======================
g85 — beyond the ~3x cap, the only honest uncap levers are:
  (a) precision-change  -> REFUTED #2917 (PRECISION-CHANGE closed-neg, on main)
  (b) right-sized GPU   -> THIS verdict (the LAST remaining lever)
If right-sizing also fails to lift the MEDIAN off the occupancy-wall floor,
the ~3x cap is unliftable except by a single fused megakernel (already
structurally closed: M2/M3/M5 + L3 fusion lane).

================================ PRE-REGISTERED FALSIFIER ======================
Running the SAME flame CLMConvMoE step (D=1536 T=512 E=2 K=3, FP64) — with
its serial sub-ms kernel DAG (norm/elementwise/MoE-glue between the GEMMs,
median util ~0-1% on an idle 132-SM H100, F-FUSION-OCCUPANCY-WALL /
F-FUSION-INTERP-ELIM MEAN 0.43% MEDIAN 0%) — on a RIGHT-SIZED GPU
(~50-80 SMs, batch=1 grid ~528-660 CTAs CAN fill the device) lifts the
util MEDIAN off ~0% (the grid FILLS the smaller device; the step becomes
EFFICIENT relative to that GPU's peak).
  If YES -> right-sizing fills the device; efficiency win (NOT absolute
            throughput — fewer/slower SMs => step/s <= H100). "Match the GPU
            to the shape."
  If NO  -> even a right-sized GPU stays bimodal-low-util: the per-op
            launch-dispatch gaps persist REGARDLESS of SM count. TERMINAL:
            the wall is launch-dispatch serial idle, fillable by nothing
            short of a single fused megakernel (structurally closed). The
            ~3x cap is unliftable on the right-sized axis.

================================ WHAT THIS ADDS OVER PRIOR =====================
F-FUSION-D2-RIGHTSIZED (#2713): standalone GEMM/util DRIVER saturated an
  RTX 5070 to ~98% — but that was the ISOLATED GEMM, not the real trainer.
F-FUSION-P2-RIGHTSIZED-REALSTEP: ran the REAL trainer step on an RTX 5070
  (12 GiB) and found it STAYS bimodal-low (eager MEDIAN 9%, fused MEDIAN 2%
  — indistinguishable from idle-H100 median 2%). 🔴 FALSIFIED.
  CAVEAT it logged: D=1536 OOMs at 12 GiB, so P2 measured D=1024, NOT a
  byte-for-byte H100-shape replay.
THIS verdict CLOSES that OOM caveat: a right-sized GPU with >=24 GiB so the
  EXACT D=1536/T512 H100-baseline shape FITS and runs the full serial DAG.
  If the median still sits at the floor at the TRUE H100 shape, the P2
  finding upgrades from "D=1024 right-sized step" to "exact-H100-shape step
  idles a right-sized GPU too" — removing the last caveat on the lever.

================================ H100 BASELINE (verbatim, prior verdicts) ======
F-FUSION-INTERP-ELIM (H100, real clm_prod_gpu, D=1536):
  full native real-trainer util  MEAN 0.43%  MEDIAN 0%
F-FUSION-OCCUPANCY-WALL / GRAPH-AB (H100 idle, D=1536):
  eager MEAN ~11.85-12.50%  median 2%  PEAK 71-100% (brief GEMMs only)
  graph MEAN 13.17%  median 2%
step/s baseline ~0.175 @ B=1 (from #2913/#2912)

================================ METHOD (planned) =============================
  GPU: right-sized ~50-80 SM card with >=24 GiB (A10 72 SMs 24GB / L4 58 SMs
       24GB / RTX 4090 128 SMs 24GB — pick the smallest-SM that fits D=1536).
  binary: clm_prod_gpu (eager when HEXA_FUSE_GN_GELU unset; fused GN+GELU =1),
          built on-pod (-DHEXA_CUDA, -lcudart -lcublas -lcuda).
  workload: CLMConvMoE D=1536 T=512 E=2 K=3 NSAMP=8 EPOCHS=8, FP64,
            DEVRESIDENT=1 DEVFEED=1 BATCHED=1 ASYNC=0 (the real serial DAG).
  measure: nvidia-smi utilization.gpu @0.1s over sustained run after 5s
           warmup; MEAN/PEAK/MEDIAN/pct>=20/pct>=70 verbatim for idle / eager
           / fused. step/s from the run's iters/secs. Batch sweep B=1,2,4.
  NO faked util. Report BOTH absolute step/s (likely <= H100) AND
  util/efficiency (likely >> H100 if grid fills).

================================ VERIFICATION =================================
  [PENDING POD FIRE]

================================ FINDING ======================================
  [PENDING]

  pod [PENDING] DESTROYED [PENDING] (leak 0, project-tag-checked hexa-rsg).
