F-BENCH-14 — per-CTA wgmma INNER-LOOP microarch tuning (deeper pipeline + vec epilogue)
================================================================================
STATUS: WIP — kernel + driver landed; H100 measurement PENDING.

GOAL: close the BENCH-12/13 D=2048 single-wave ~1.36x residual off cuBLAS-TF32 with the LAST
identity-preserving lever — per-CTA wgmma MAINLOOP tuning (deeper wgmma-group pipeline /
dual-issue depth PDEP + overlapped vectorized .v2.f32 epilogue) on the descriptor-direct
own-GEMM. These are SCHEDULING/overlap changes only — the per-tile FMA accumulation order is
UNCHANGED, so rel-RMS MUST stay 0 (a non-zero value = a pipelining/ring-race bug = reject).
NO split-K (forfeits bit-exact identity, forbidden by g5).

KERNEL: self/native/wgmma/wgmma_tf32_b14.cu MODE 8 (gemm_og17_b14) — clones gemm_og17_pipe's
descriptor-direct 128x128 route-a math, generalizes the fixed wait_group 1 to a parameterized
wait_group PDEP (PDEP groups in flight; needs NST>=PDEP+1 ring buffers), and replaces the
scalar C-store with a vectorized .v2.f32 store of the contiguous gmma col-pair. PDEP=1
reproduces MODE 6 exactly (apples self-check). Driver: self/native/wgmma/bench14_run.sh.

MEASURED: (pending H100 run — verbatim block to be filled).
