VERIFY-KIT V6 — atlas register --source <attribution> → provenance field
================================================================
driver: bin/hexa-atlas + bin/hexa-verify (HEAD-aware, built local; installed
        ~/.hx/bin/hexa.real is V4-stale on _jacobi_symbol/_kronecker_symbol)
isolated atlas: HEXA_ATLAS_EMBED=/tmp/wt-vkit-v6-iso (shared tree NOT touched)

--- TEST 1: register --from-verify aliquot 6 --source "OEIS A001065" --url "https://oeis.org/A001065" --compute ---
hexa atlas register --from-verify aliquot(6)=6  (compute)
  tier   = 🔵 SUPPORTED-FORMAL  (delegated via `hexa verify --expr … --compute`)
  atlas  = folded @F verified-aliquot-6 → /tmp/wt-vkit-v6-iso/embedded.gen.hexa (binary-builtin SSOT)
  note   = visible to `hexa atlas lookup` immediately — no rebuild

hexa atlas register: landed in the live atlas. Commit to share it:
  git -C ~/core/hexa-lang add compiler/atlas/embedded.gen.hexa && git commit -m 'atlas(F): register verified-aliquot-6'

--- folded node (grep verified-aliquot-6) — source + url fields present ---
@F verified-aliquot-6 = aliquot(6) = 6 :: formula [d=2026-05-25 active]
  verified-by = "hexa verify --expr aliquot 6 6"
  source = "OEIS A001065"
  url = "https://oeis.org/A001065"
  cite = "@D g_self_verify · TECS-L Tier1 — hexa-native NT closed-form"
  provenance = "tool/atlas_cli.hexa register --from-verify (direct node-gen → embedded.gen.hexa SSOT)"

--- TEST 2: register --from-verify sigma 6  (NO --source, REGRESSION) ---
hexa atlas register --from-verify sigma()=6
  tier   = 🔵 SUPPORTED-FORMAL  (delegated via `hexa verify --expr`)
  atlas  = folded @F verified-sigma- → /tmp/wt-vkit-v6-iso/embedded.gen.hexa (binary-builtin SSOT)
  note   = visible to `hexa atlas lookup` immediately — no rebuild

hexa atlas register: landed in the live atlas. Commit to share it:
  git -C ~/core/hexa-lang add compiler/atlas/embedded.gen.hexa && git commit -m 'atlas(F): register verified-sigma-'

--- folded node (grep verified-sigma-) — NO source/url field (default unchanged) ---
@F verified-sigma- = sigma() = 6 :: formula [d=2026-05-25 active]
  verified-by = "hexa verify --expr sigma  6"
  cite = "@D g_self_verify · TECS-L Tier1 — hexa-native NT closed-form"
  provenance = "tool/atlas_cli.hexa register --from-verify (direct node-gen → embedded.gen.hexa SSOT)"

--- shared-tree leak check: git status compiler/atlas/embedded.gen.hexa ---
(empty above = NO leak; all folds went to isolated /tmp/wt-vkit-v6-iso)

RESULT: --source/--url → provenance fields present (🔵); no-flag → default unchanged; shared embedded.gen.hexa clean.
TIER: 🟢 SUPPORTED-NUMERICAL (CLI capability — byte-grep confirmed field presence + regression + no-leak)
