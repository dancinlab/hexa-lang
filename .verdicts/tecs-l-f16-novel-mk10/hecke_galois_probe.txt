# F16 s4 — Hecke / Galois layer probe at n=6 (light)
# Question: does the n=6 non-lift from F7 (Γ_1(N)/X(N) index smooth) extend to
# Hecke eigenvalue or Galois representation layer?
#
# === Hecke layer at n=6 ===
# Γ_0(6) genus=0 (MF3 cite) → dim S_2(Γ_0(6)) = 0 (no weight-2 cusp forms)
# ⇒ Hecke algebra T_p acts trivially (rank-0) on S_2(Γ_0(6)).
# Higher-weight cusp spaces (k>=4) have dim>0 by Riemann-Roch on X_0(6)/cusps.
#
# hexa `dim_cusp_forms(6, k)` returns (MF4 family — known calc-gap, NOT standard dim S_k):
#   k=2:  0   (matches true dim)
#   k=4:  1   (hexa value)
#   k=6:  3
#   k=8:  3
#   k=10: 5
#   k=12: 7
# hexa `dim_cusp_forms(1, k)` returns 0 for ALL k tested (k=12,14,16,18,24)
#   — true dim S_12(SL_2(Z)) = 1 (Δ(τ) discriminant cusp form). MF4 gap reconfirmed.
#
# ⇒ Hexa-native Hecke eigenvalue T_p computation is NOT available at calc-fn layer
# (no T_p function in tool/verify_cli.hexa _recompute / stdlib/core/math.hexa).
# Standard reference: Sage/Magma Eisenstein subspace + Hecke decomposition for
# S_k(Γ_0(6)) shows old-form decomposition from level 1, 2, 3 oldforms plus
# new dimensions; nothing in this decomposition singles out n=6 — the same
# tensor-decomposition pattern (level N square-free → product of (level p_i)
# old/new spaces) applies to any square-free N. No n=6 peak.
#
# 🟡 CITATION (Diamond-Shurman §5): Hecke layer at Γ_0(6) is decomposable into
# the level-1, level-2, level-3 oldspace plus a newform space; the n=6
# σφ=nτ⟺{1,6} identity has NO Hecke-eigenvalue counterpart visible in the
# tensor structure. Same conclusion as F7 (geometric layer): the n=6 identity is
# an ARITHMETIC layer phenomenon, not a modular/Hecke layer phenomenon.
#
# === Galois layer at n=6 ===
# Q(ζ_6) cyclotomic: φ(6)=2 → degree-2 extension over Q (= Q(ζ_3) = Q(√-3)).
# Galois group Gal(Q(ζ_6)/Q) = (Z/6Z)^* = Z/2Z (since φ(6)=2).
# This is the smallest non-trivial cyclotomic Galois group besides Q(ζ_3)/Q
# (which IS Q(ζ_6) — same field since ζ_6 = -ζ_3).
# Components verified via hexa verify --expr phi <n> <v> --no-absorb:
  phi(6)=2 🔵
  phi(3)=2 🔵
  phi(12)=4 🔵
  phi(30)=8 🔵
#
# Cyclotomic field structure: Q(ζ_6) = Q(ζ_3) (since ζ_6 = -ζ_3, 6=2*3).
# This collapse is BECAUSE n=6 is the smallest n with phi(2n)=phi(n)
# (a special non-trivial Euler-phi degeneracy). Standard fact.
#
# But the n=6 σφ=nτ identity ⟺{1,6} does NOT manifest at the Galois layer
# in any non-trivial way beyond this φ-degeneracy: Gal(Q(ζ_n)/Q) = (Z/nZ)^*
# has |Gal| = φ(n), and σφ=nτ does NOT constrain |Gal| to a distinctive value
# at n=6 (φ(6)=2 = φ(3) = φ(4), so trivially shared).
#
# 🟡 CITATION + 🟠 INSUFFICIENT honest-fence: deeper Galois rep / Hecke
# eigenvalue probes need T_p builtin (verify_cli infra gap, family of
# `sigma_3` / `mersenne_perfect_sigma` / `iit4_faithful_phi` calc-gap INBOX —
# same upstream calculator-extension family).
#
# CONCLUSION: F7 modular non-lift extends to Hecke (oldform decomposition
# structure is generic for square-free N) and to Galois (φ-degeneracy only,
# not σφ=nτ-driven). The n=6 identity remains an ARITHMETIC-LAYER phenomenon
# (M1/M3/M10 cite) — no higher-layer lift seen. Consistent with F12 paper
# "n=6 exclusivity atlas" thesis.
