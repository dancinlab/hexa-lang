F-OP27-TF32-DOJO — deterministic TF32 fast-mode dojo recipe (0-pod) + commons directive draft
=============================================================================================
STATUS: GREEN (0-pod docs half SHIPPED). The validated deterministic TF32 fast-mode (OP-20 #2999
        + OP-23 #3005 + OP-24 #3009 + OP-25 #3007) is reflected into docs/hexa-dojo.md as a
        contributor recipe. The commons.tape governance half is DRAFTED here ONLY and AWAITS the
        user's `sidecar sign commons` (the agent CANNOT self-sign a sign-gated SSOT). NO .tape
        file was edited.

SCOPE: pure docs reflection. $0 · 0-pod · NO GPU · NO vast · NO pod · NO foreign-pod touch.

------------------------------------------------------------------------------------------
1. DOJO RECIPE ADDED (docs/hexa-dojo.md) — SHIPPED 0-pod
------------------------------------------------------------------------------------------
New section: "### deterministic TF32 fast-mode (precision-uncap)" inserted after the g86
"flame vs PyTorch — fair-bench parity recipe" section, before "## references". Elephant-rule
(current-state facts only, no journey narration). Every number traces to a verdict (g5):

  * WHEN: HEXA_TF32_FASTMODE=1 for the >3x speedup keeping run-to-run determinism; FP64 default
    is byte-identical with the flag unset (keep FP64 for cross-machine/cross-dtype byte-eq).
  * DETERMINISM GUARANTEE: TF32 step self-byte-eq run-to-run max|d(W',m,v,loss)|=0 (single step
    8/8 cells AND whole 100-step trajectory); DEFAULT mode already deterministic on 5070/cuBLAS
    13.0, but PIN CUBLAS_PEDANTIC_MATH as the portable cross-card guarantee (costs ~0).   [OP-20/23/24]
  * W14 CONTRACT: rel-RMS 1.13e-6 (post-AdamW weight delta) — 4 orders inside W14 1e-2; raw
    single-GEMM through the live dispatch ~2.9e-4 (still ~34x inside W14).                [OP-20/24]
  * REAL FAST-MODE (not 1-step illusion): N=100 loss tracks FP64 ~1e-7/step (worst 2.5e-5 at
    cold-start step1, no growth); weight rel-RMS BOUNDED ~5e-7 (shrinks).                 [OP-23]
  * CARD-ROBUST SPEED: B=1 latency-bound 4.2x (4.19-4.63x) clears the ~3x cap; B=8 19-21x
    INFLATED by the 5070's ~1/64-rate FP64 (caveat quoted).                              [OP-20]
  * PRECISION PARETO + ASCII diagram (g3 minimal): FP64 exact 1x -> TF32 e-6 4.2x SWEET SPOT
    -> BF16 e-6 4.1x DOMINATED (same acc same speed; TF32/BF16 1.01-1.12x, B=1 dead heat). [OP-25]
  * LIVE-WIRE DISPATCH SITE: HEXA_TF32_FASTMODE read in self/cuda/runtime_cuda_emit.hexa,
    _hx_cuda_farr_matmul_gpu; dispatch if(OWN_GEMM) elif(tf32_fastmode){cast->GemmEx(COMPUTE_
    32F_FAST_TF32, PEDANTIC handle)->cast} else cublasDgemm; FP64 default byte-identical.  [OP-24]
  * verdict-pointer footer cites all four verdicts (g5 traceability).

------------------------------------------------------------------------------------------
2. COMMONS DIRECTIVE — DRAFTED FOR USER SIGN (NOT APPLIED; commons.tape is sign-gated)
------------------------------------------------------------------------------------------
GOVERNANCE NOTE: commons.tape is a sign-gated governance SSOT. The agent NEVER self-issues a
sign token. To apply the directive below the USER must first run `sidecar sign commons` (mints
a 5-min sign-off token), then the directive can be appended to commons.tape. Until then this is
a DRAFT only. The agent did NOT edit any .tape file in this PR.

Proposed directive (ASCII; every do/dont line <=100 chars per the hexa-lang lint cap) — paste
VERBATIM into commons.tape after the most recent g8x directive, then `sidecar sign commons`:

------------------------------ BEGIN DRAFT (verbatim) ------------------------------
@D g87_tf32_fastmode := "deterministic TF32 = the precision-uncap sweet spot" :: governance [required active]
  do   = "use HEXA_TF32_FASTMODE for >3x flame speedup; self-byte-eq run-to-run + W14 rel-RMS<=1e-2 vs FP64"
  do   = "pin CUBLAS_PEDANTIC_MATH on the TF32 handle for the portable cross-card determinism guarantee (~0 cost)"
  do   = "keep FP64 the default (byte-identical, flag-off) for cross-machine/cross-dtype byte-exact identity"
  dont = "prefer BF16 over TF32 on consumer cards: Pareto-DOMINATED (same e-6 acc, same speed, B=1 dead heat)"
------------------------------- END DRAFT (verbatim) -------------------------------

Numbers in the directive trace to: self-byte-eq + 4.2x@B=1 + 1.13e-6 [F-OP20]; N-step real-not-
illusion [F-OP23]; live-wire PEDANTIC handle [F-OP24]; BF16-DOMINATED [F-OP25].

------------------------------------------------------------------------------------------
HONEST FRAMING (g5)
------------------------------------------------------------------------------------------
- The DOJO half is SHIPPED 0-pod (a normal contributor doc, no sign needed). The COMMONS half
  is DRAFTED ONLY and awaits the user's `sidecar sign commons` — the agent cannot self-sign.
- No new measurement was performed; every number is CITED from the four landed verdicts (g5).
- 4.2x@B=1 is the card-robust headline; the B=8 19-21x is FP64-throttle-inflated (5070-specific).
- Determinism is PROVEN for the 5070 / these shapes / cuBLAS 13.0; PEDANTIC is the portable pin.
- $0 · 0-pod · NO GPU · NO vast · NO pod rented · foreign vast pod 40306156 NOT touched.

VERDICT: GREEN (0-pod docs). Dojo recipe added + accurate (every number verdict-cited) +
elephant-rule. Commons directive DRAFTED (verbatim, <=100char/line, ASCII) for the user to apply
after `sidecar sign commons`. NO .tape edited. The dojo half is shipped; the commons half awaits
the user's sign.
