════════════════════════════════════════════════════════════════════════════
F-OP12-ADAMW-UPDATE-ORACLE — flame AdamW optimizer UPDATE-arithmetic identity
HEXA-0POD OP-12 · CPU-LOCAL byte-eq ORACLE · 0-GPU (`hexa run`) · $0
════════════════════════════════════════════════════════════════════════════

IDENTITY LOCKED
  The flame CLMConvMoE training step applies a decoupled-weight-decay AdamW update
  per parameter element. The PRODUCTION SSOT is the CPU kernel
      _hx_farr_adamw_step_cpu   (self/runtime.c:10783)
  and its byte-eq device twin
      _hx_k_adamw_step          (self/cuda/runtime_cuda.c:1236, "modulo sqrt ULP").
  OP-6/OP-6b touched this kernel for PERF (fuse the update into the bwd-GEMM
  epilogue) but never oracle-locked the UPDATE MATH IDENTITY itself. OP-12 does.

  Per element (the canonical op order, runtime.c:10815-10830):
      b1t = Π_{e<step_t} β1 ;  b2t = Π_{e<step_t} β2   (βᵗ via REPEATED MUL, NOT pow)
      c1  = 1 − b1t ;  c2 = 1 − b2t                     (bias-correction stays SEPARATE,
                                                         NOT folded into lr)
      mi   = β1·m + (1−β1)·g                            (first moment, m FIRST)
      vi   = β2·v + ((1−β2)·g)·g                        (second moment — LEFT-assoc:
                                                         (1−β2)·g·g = ((1−β2)·g)·g)
      mhat = mi / c1                                    (m̂ computed BEFORE v̂)
      vhat = vi / c2
      denom = sqrt(vhat) + eps                          (ε OUTSIDE the √: √v̂ + ε)
      wi   = W − lr·wd·W − lr·mhat/denom                (TWO separate subtractions:
                                                         decoupled-wd term first, then
                                                         the Adam term lr·m̂/denom)
      m,v written IN PLACE (optimizer-state contract); wi → fresh W.

THE ORACLE  (stdlib/flame/clm_prod_adamw_update_eq.hexa, `hexa run`, 0-GPU)
  _adamw_prod — replays _hx_farr_adamw_step_cpu VERBATIM (the SSOT op order above).
  _adamw_ref  — a clean, independently-structured Loshchilov-2017 AdamW update,
                written to MATCH the production associativity (separate c1/c2,
                m̂-before-v̂, ε-outside-√, two-subtraction decoupled-wd update, and
                the LEFT-assoc squared-grad grouping). The oracle's point: a "clean
                rewrite" lands on the IDENTICAL bytes ONLY when it preserves this
                op order — so a future refactor that re-associates ANY term breaks
                the lock and is caught.
  Both forms run on PRIVATE copies of (m,v) starting from the SAME fixture bytes
  (m,v are mutated in place by each), and max|Δ| is taken over the FULL state
  transition — W AND m AND v — not just W.
  LCG deterministic fixtures (no rng); v forced ≥0 (running mean of squares).
  7 configs sweeping every knob: lr∈{3e-4..1e-2}, β1∈{.8,.9,.95}, β2∈{.99..​.9999},
  ε∈{0, 1e-8, 1e-7, 1e-6}, wd∈{0, .01, .05, .1}, step_t∈{1,3,5,10,50,100}, n∈{1,
  64,96,128,200} — incl. t=1 (max bias-correction), t=100 (late), ε=0, wd=0, n=1.

SQRT IMPL  (HONEST, g5)
  The production CPU SSOT uses libm `sqrt`; its comment (runtime.c:10781) states the
  boxed 24-iter Newton `dt_sqrt` is "verified ≡ the same double", and the CUDA twin
  uses the device `sqrt` (same symbol). `hexa run` exposes no libm-sqrt builtin on the
  float path, so the oracle holds the sqrt impl CONSTANT across BOTH forms — both
  _adamw_prod and _adamw_ref call the SAME `_adamw_sqrt` (Newton-Raphson 24-iter from
  g = max(x,1), BYTE-IDENTICAL discipline to flame_math dt_sqrt / gn_lib _gn_sqrt used
  by the OP-7/OP-8/OP-9 oracles). Holding sqrt identical ISOLATES the update-ORDERING
  identity — the thing OP-12 locks. A sqrt-impl swap is out of scope (it is the same
  `sqrt` symbol on both the CPU and CUDA production paths). ε is OUTSIDE the √ in BOTH
  the production SSOT and the oracle (denom = √v̂ + ε).

HONEST ASSOCIATIVITY FINDING (g5) — REAL, found + resolved (NO faked max|Δ|=0)
  A first _adamw_ref that grouped the squared-grad term as the "natural" `(1−β2)·(g·g)`
  diverged from production across the 7 cases by up to 8.88e-16:
      1.11e-16 / 1.11e-16 / 1.11e-16 / 8.88e-16 / 2.78e-17 / 0 / 1.11e-16.
  Cause: the production SSOT writes `(1−β2)·g·g`, which the language groups
  LEFT-associatively as `((1−β2)·g)·g` — a DIFFERENT IEEE-754 double than
  `(1−β2)·(g·g)`. This is precisely the determinism contract OP-12 pins. After
  matching the grouping the oracle is GENUINE max|Δ|=0 (NO eps, NO tolerance).

  CANONICAL ORDER (SSOT — runtime.c:10819-10830):
    v = (β2·v) + (((1−β2)·g)·g)      [LEFT-assoc squared-grad term]
    m = (β1·m) + ((1−β1)·g)
    m̂ = m/c1   computed BEFORE   v̂ = v/c2
    denom = sqrt(v̂) + ε             [ε OUTSIDE √; sqrt = libm/device sqrt (oracle: the
                                      ≡ 24-iter Newton _adamw_sqrt)]
    W' = ((W − lr·wd·W) − lr·(m̂/denom))   [two separate subtractions, decoupled-wd first]
    c1,c2 = 1 − βᵗ   with βᵗ by REPEATED MUL over step_t (not pow)

HEXA-RUN OUTPUT  (verbatim — `hexa run stdlib/flame/clm_prod_adamw_update_eq.hexa`)
  clm_prod_adamw_update_eq — F-OP12 AdamW update prod-order==reference byte-eq (CPU oracle)
    max|Δ| n64  lr1e-3 b.9/.999 wd.01 t=1   = 0.0
    max|Δ| n64  lr1e-3 b.9/.999 wd.01 t=10  = 0.0
    max|Δ| n64  lr3e-4 b.9/.999 wd.1  t=100 = 0.0
    max|Δ| n128 lr1e-2 b.8/.99  wd0   t=5   = 0.0
    max|Δ| n96  lr5e-3 b.95/.9999 wd.05 t=50= 0.0
    max|Δ| n1   lr1e-3 b.9/.999 wd.01 t=1   = 0.0
    max|Δ| n200 lr2e-3 b.9/.999 eps0 wd0 t=3= 0.0
  F-OP12-ADAMW-UPDATE = 1
  PASS — AdamW update prod-order == clean reference byte-eq (max|Δ|=0 over W,m,v, all cases)

VERDICT
  F-OP12-ADAMW-UPDATE = 1 · PASS · max|Δ| = 0.0 over (W, m, v) for ALL 7 configs.
  The flame AdamW decoupled-wd UPDATE-arithmetic identity is byte-exactly LOCKED:
  a future refactor that re-associates any term (squared-grad grouping, ε placement,
  folding bias-correction into lr, collapsing the two subtractions, reordering m̂/v̂)
  is caught by this `hexa run` CPU oracle. Behavior-preserving: oracle/verification
  addition ONLY — NO trainer logic changed. 0-GPU, $0, LOCAL. HONEST: NOT a trivial
  pass — a real LEFT-assoc associativity gap (≤8.88e-16) was found and resolved by
  replaying the exact production op order (production order = SSOT), not by adding eps.
════════════════════════════════════════════════════════════════════════════
