F-OP32B-STDP-HOST — spiking_lib CPU link hole: hexa_forge_dispatch_stdp_pair host fallback
══════════════════════════════════════════════════════════════════════════════
DATE: 2026-06-12 · HEXA-0POD OP-32b · 0-pod ($0, NO GPU, NO vast, NO pod)
HOST: local arm64-macos (Darwin 25.5.0), CPU-only
STATUS: GREEN — gate g5 all checks pass

────────────────────────────────────────────────────────────────────────────
THE HOLE (OP-32 HOLE-2, pre-existing, now CLOSED)
────────────────────────────────────────────────────────────────────────────
  `use "stdlib/flame/spiking_lib"` failed to LINK on every CPU-only host:
  spiking_lib::flame_stdp_pair_gpu calls the 8-arg builtin
  forge_dispatch_stdp_pair, lowered by self/codegen.hexa L7800 to
  hexa_forge_dispatch_stdp_pair. The seam landed (2026-05-20, anima LEGO
  §141 patch archive/patches/archive/flame-stdp-pair-gpu-kernel.md) as
  header + codegen + CUDA-emit ONLY:
    · prototype:   self/runtime.h:1504 (hexa_ + bare seam twin)
    · CUDA emit:   self/cuda/runtime_cuda_emit.hexa _hx_cuda_kern_stdp_pair
                   + launcher _hx_cuda_farr_stdp_pair_gpu
    · codegen:     self/codegen.hexa L7799-7800 (builtin lowering)
  but the runtime.c BODY was never committed — self/runtime.c is the
  gitignored frozen seed (151c52c8 .c-graduation parent, regenerated by
  tool/restore_frozen_seeds), and the frozen blob predates the seam.
  hexa codegen emits ALL module fns (no DCE), so merely IMPORTING the lib
  pulls the undefined symbol even if flame_stdp_pair_gpu is never called.

  REPRODUCED LIVE (pre-fix, this host):
    Undefined symbols for architecture arm64:
      "_hexa_forge_dispatch_stdp_pair", referenced from:
          _flame_stdp_pair_gpu in hexa_run-11dde4.o
    ld: symbol(s) not found for architecture arm64

────────────────────────────────────────────────────────────────────────────
THE HOST BODY (the OP-16/18 host-fallback pattern)
────────────────────────────────────────────────────────────────────────────
  tool/restore_frozen_seeds gains an OP-32b idempotent marker-guarded
  ('OP-32b (HEXA-0POD') post-restore append (the exact OP-17/18/19e landing
  mechanism — the durable channel for gitignored-frozen-seed fixes):

  · `#ifndef HEXA_CUDA` HexaVal hexa_forge_dispatch_stdp_pair(8×HexaVal):
    canonical SEQUENTIAL STDP pair update, scalar-order-IDENTICAL to the
    CPU reference spiking_lib.hexa::flame_stdp_pair —
      i ascending, j ascending; idx = i·n + j
      diagonal (i==j): dW = 0, weight passes through clip unchanged
      else: ltp = (A_plus·s_i)·tr_pre[j]   (left-assoc, 2 separate muls)
            ltd = (A_minus·trpost_i)·spike[j]
            wnew = w_ij + (ltp − ltd)      (same subtraction order)
      clip: > w_max → w_max ; < (0.0 − w_max) → −w_max
    n DERIVED from the spike farr length (= t_len(spike), exactly the hexa
    wrapper's n). Shape/id guards mirror the CUDA launcher (ids ≥ 0,
    < _hx_farr_count; W/out len ≥ n²; tr_pre/tr_post/spike len ≥ n).
    Returns hexa_int(0) ok / hexa_int(-1) error — the documented contract.
  · `#pragma STDC FP_CONTRACT OFF` around the block: clang -O2 would
    otherwise contract `w_ij + (ltp − ltd)`'s feeding muls into fma/fms
    (~1 ULP divergence) — the SAME hazard the CUDA kernel defeats with
    __dmul_rn/__dadd_rn/__dsub_rn (F-STDP-GPU-1 heritage).
  · bare `forge_dispatch_stdp_pair` bootstrap seam wrapper delegating to
    hexa_… (same pattern as forge_dispatch_matmul — covers a deployed
    hexat that predates the 8-arg builtin registration).
  · Appended at runtime.c EOF where _hx_farr_table / hexa_as_num /
    __hx_to_double / hexa_int are in scope (OP-18 precedent).
  Local install ALSO patched: same marker-guarded block appended to
  ~/.hx/bin/self/runtime.c (backup kept), so the deployed `hexa run`
  links immediately; future seed regenerations get it from the tracked
  tool/restore_frozen_seeds.

────────────────────────────────────────────────────────────────────────────
PROOF (all 0-GPU, this host)
────────────────────────────────────────────────────────────────────────────
  (a) SYMBOL U → T — patched self/runtime.c compiles clean (clang -O2 -c;
      only the seed's pre-existing -Wcomment/-Wincompatible-pointer
      warnings); nm runtime.o:
        T _hexa_forge_dispatch_stdp_pair
        T _forge_dispatch_stdp_pair
      (pre-fix: U _hexa_forge_dispatch_stdp_pair in the hexa_run TU → ld fail)
  (b) LINKS + RUNS — `hexa run` harness with `use "stdlib/flame/spiking_lib"`
      links + runs: "link-ok mask0=1.0". The OP-32 link failure is GONE.
  (c) BYTE-EQ vs the PROVEN CPU REFERENCE —
      stdlib/flame/op32b_stdp_hostdispatch_eq.hexa (committed): imported
      production flame_stdp_pair_gpu (→ new host dispatch) A/B vs
      flame_stdp_pair (the OP-32-proven primitive), n=24, OP-32 LCG fills
      (real-valued W near clip range scale 0.9, real-valued traces, binary
      spikes), A+=0.21 A−=0.17 w_max=0.875 (clip ENGAGES):
        OP32B-BYTEEQ n=24 mismatch=0 maxd=0.0
        OP32B-CKSUM ref=347631115 gpu=347631115   (bitwise fingerprint EQ)
        OP32B-DIAG bad=0 · OP32B-CLIP bad=0 · moved=0.372003 (non-trivial)
        OP32B-GATE GREEN
  (d) OP-32 ORACLE RE-RUN — stdlib/flame/op32_spiking_determinism_eq.hexa
      PASS (all max|Δ| run-to-run = 0.0, 25 spikes, STDP movement 0.254879):
      F-OP32-4TH-ARCH = 1. Header annotated: HOLE-2 closed by OP-32b
      (verbatim copies kept — still a valid run-to-run oracle).
  (e) GPU PATH UNTOUCHED — block is `#ifndef HEXA_CUDA` ONLY. Syntax check:
      `clang -E -DHEXA_CUDA self/runtime.c -I self` succeeds (rc=0) and the
      preprocessed TU contains ZERO stdp definitions (grep
      hexa_forge_dispatch_stdp_pair = 0) → a -DHEXA_CUDA build is
      byte-identical to today; no duplicate symbol with any CUDA-side
      dispatcher is possible. The CUDA kernel emit + launcher are untouched.
  (f) IDEMPOTENT — tool/restore_frozen_seeds re-run twice: marker count
      stays 1 (no double-append); extracted block == runtime.c tail (diff
      clean).

GATE (g5) — ALL GREEN
  [x] host fallback body added (#ifndef HEXA_CUDA, OP-16/18 pattern,
      durable via tool/restore_frozen_seeds marker-guarded append)
  [x] symbol U → T (nm: T _hexa_forge_dispatch_stdp_pair + bare seam)
  [x] spiking_lib links + runs 0-GPU (hexa run, import-based)
  [x] BYTE-EQ host dispatch == flame_stdp_pair CPU reference (max|Δ|=0,
      identical bitwise checksums, clip engaged, non-trivial movement)
  [x] GPU path (#ifdef HEXA_CUDA) untouched; no duplicate symbol (-E proof)
  [x] OP-32 oracle re-run green; import link-failure GONE

HONEST RESIDUAL
  · On a -DHEXA_CUDA build the wrapper body is (as before this fix) NOT
    supplied by the frozen seed — the CUDA-side dispatcher comes from the
    GPU build flow's own runtime glue (runtime_cuda emit). That state is
    UNCHANGED by design (task scope: GPU path untouched; GPU-build-gated,
    not verifiable 0-pod). If a future CUDA build of the FROZEN seed needs
    the wrapper, the natural fix is an #ifdef HEXA_CUDA twin calling
    _hx_cuda_farr_stdp_pair_gpu — one GPU session to verify.
  · No other missing sibling symbol surfaced: the full spiking_lib import
    (all module fns emitted, no DCE) now links with ONLY this body added.

PROVENANCE
  fix:     tool/restore_frozen_seeds (OP-32b block) — tracked, durable
  oracle:  stdlib/flame/op32b_stdp_hostdispatch_eq.hexa (committed)
  hole:    F-OP32-4TH-ARCH HOLE-2 · op32_spiking_determinism_eq.hexa header
  pattern: OP-16 (#3008 groupnorm_gelu) · OP-18 (gelu2/moe_block2) ·
           OP-19e (musl environ) — same marker-guarded post-restore channel
  semantics SSOT: stdlib/flame/spiking_lib.hexa::flame_stdp_pair (CPU ref) ·
           self/cuda/runtime_cuda_emit.hexa _hx_cuda_kern_stdp_pair (GPU twin)
