use "stdlib/core/math"

fn main() -> int {
    println("start")
    let s = sigma(6)
    println("got sigma")
    if s == 12 { println("sigma6 ok 12") }
    if s != 12 { println("sigma6 WRONG") }
    0
}
