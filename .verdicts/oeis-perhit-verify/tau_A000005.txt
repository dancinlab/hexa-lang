# O3 per-hit verify — tau <-> A000005 (number of divisors d(n))
# OEIS A000005 first-10 tuple (from O2 sweep): 1,2,2,3,2,4,2,4,3,4
# verb: hexa verify --expr tau <n> <expected>  (3-arg VERIFY, VERIFY-KIT V2-era CLI)
# tier: BLUE (canonical-source direct hexa-native recompute; tau == sigma_0 in hexa)

$ hexa verify --expr tau 10 4
verify --expr tau(10)=4
  calc   = 4  == expected 4
  tier   = 🔵 SUPPORTED-FORMAL  (hexa-native closed-form, g_self_verify · TECS-L Tier1)
  absorb = · already in atlas — idempotent skip (default · @D g69)

$ hexa verify --expr tau 6 4
verify --expr tau(6)=4
  calc   = 4  == expected 4
  tier   = 🔵 SUPPORTED-FORMAL  (hexa-native closed-form, g_self_verify · TECS-L Tier1)
  absorb = · already in atlas — idempotent skip (default · @D g69)

# NOTE: sigma_0 is the SAME hexa fn (len(divisors)) and resolves to A000005 too
# (tool/verify_cli.hexa::_recompute: divisor_count|tau -> len(divisors(n));
#  sigma_0 -> sigma_k(n,0)). The tau and sigma_0 survivors against A000005 are
#  the one identical hexa recompute on the same sequence (1 distinct theorem).
