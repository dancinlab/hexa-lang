// tool/probe_exp_f64.hexa — F-EXPF64-NUMERIC kernel (#1333 silicon re-validation)
@gpu_kernel
fn exp_f64_probe(a: [f64], c: [f64], n: i64) {
    let mut tid: i64 = to_i64(gpu_thread_id_x())
    if tid < n {
        c[to_i64(tid)] = exp(a[to_i64(tid)])
    }
}
