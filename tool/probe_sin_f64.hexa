// tool/probe_sin_f64.hexa — F-SINF64-NUMERIC kernel (#1463 silicon validation)
@gpu_kernel
fn sin_f64_probe(a: [f64], c: [f64], n: i64) {
    let mut tid: i64 = to_i64(gpu_thread_id_x())
    if tid < n {
        c[to_i64(tid)] = sin(a[to_i64(tid)])
    }
}
