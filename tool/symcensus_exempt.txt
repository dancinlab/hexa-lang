# symcensus_exempt.txt — axis-③ codegen↔runtime symbol-census exemption list
#
# Consumed by tool/symcensus_lint.sh (guard C-1). Each line is a checker-gate
# builtin name (from check/types.hexa::_is_builtin_method ∪ check/bind.hexa::
# _bind_builtin_names) that is INTENTIONALLY not yet in codegen's
# _builtin_runtime_sym mapping table. The lint asserts:
#
#   (whitelist ∪ bind_gate) − (mapped ∪ _is_cabi ∪ special-op ∪ THIS FILE) = ∅
#
# so adding a new builtin to a checker gate without either (a) a mapping entry
# or (b) an exemption line here turns the PR RED — the whack-a-mole recurrence
# channel is sealed at the source (3-surface desync).
#
# This set = the Rung-B outstanding population (axis-③ symcensus part-2 target):
#   · identity-OK — bare name already == a runtime.a def (fallthrough is correct)
#   · need-map    — def exists only under a hexa_*/rt_* prefix (part-2 will map)
#   · triage      — interp-only / HEXA_CUDA-gated / native-thread aliased
# Entries MOVE OUT of this file as part-2 lands each mapping. Empty = drained.
# Comments (#) and blank lines are ignored. Keep sorted for review-diff clarity.
#
# Regenerate the candidate set (mini read-only, no build):  tool/symcensus_lint.sh --list-residual

arange
assert
atomic_cell_add
atomic_cell_cas
atomic_cell_load
atomic_cell_new
atomic_cell_store
atomic_cell_sub
clock_ms
copy_file
crc32
f32_bits
f32_from_bits
file_modtime
getenv
getpid
gpu_matmul
gpu_matmul_NT_a
gpu_matmul_NT_b
ham_pack
hash_string
hexa_str_concat
hexa_str_parse_int
is_digit
nanos
now_ms
now_unix_ms
panic
ptr_from_int
read_line
remove_file
rename_file
thread_channel_close
thread_channel_new
thread_channel_recv
thread_channel_send
thread_join
thread_spawn
