@title: 🪆 HEXA-CC-NATIVE — "C 부산물 0 self-host"
@goal: hexa 가 자기 자신을 .c·.o·.a 어떤 C 중간물도 만들지 않고 native 백엔드(hexa codegen → machine-code → hexa_ld)만으로 처음부터 끝까지 재빌드하고, 그 native-built hexa 가 다시 자신을 재빌드해 byte-eq fixpoint 에 도달한다 (cross-arch: darwin-arm64 + linux-x86_64). C front(손-작성 .c)은 HEXA-CC-ZERO 가 이미 0 으로 닫음; 이 도메인은 BUILD front(빌드 중 생성되는 C 중간물)를 0 으로 닫는다.
@policy: COMPLETENESS-FIRST LOOP (사용자 고정 결정 2026-05-31). 다음 마일스톤 선택 시 항상 4축 중 **① 완성도(complete)** 축을 따른다 — 골(C 중간물 0 · full self-host fixpoint)에 가장 직접 다가가는 마일스톤을 우선. 다른 축(단순/안전/표준)은 완성도 안을 막는 blocker(예: 재빌드 위험·CI red)일 때만 선행 우회로로 삽입하고, 해소 즉시 완성도 본선으로 복귀. 진행은 LOOP: 한 마일스톤 종결(verify green + main 머지) → 즉시 다음 완성도 마일스톤 착수, open 마일스톤 0 까지 멈추지 않음. 완성도 본선 순서 = **N5(full-compiler self-host via hexa_ld) → N6(cross-arch) → N7(CI gate)**; N1(x86_64 runtime)·N2(self-as, moot)는 N5/N6 가 요구할 때만 종속 작업으로 끌어온다. 각 마일스톤은 g5/g63 정직 검증(falsifier + verbatim) 필수 — green 없는 flip/머지 금지.

# HEXA-CC-NATIVE — current state

HEXA-CC-ZERO 가 **커밋된 C**(트랜스파일러 씨앗 hexa_cc.c·cold-seed·native_gate.c)를 0 으로 만들었다. 그러나 빌드는 아직 `hexa → C → clang → .o → ar → .a → ld → 실행파일` 경로(C 백엔드)를 통과한다 — 즉 **빌드 도중 .c/.o/.a 가 생긴다**. 이 도메인은 그 마지막 C 의존을 없앤다: hexa 의 native 백엔드(자체 codegen + 자체 linker)만으로 컴파일러 자신을 self-host 하여, 빌드 산출물에 C 중간물이 0 이 되게 한다. CLAUDE.md `@I` 의 "no LLVM · no C-transpile" 정체성을 "지향"에서 "달성"으로 옮기는 트랙.

## 부품 인벤토리 (이미 repo 에 존재 — 재사용)

| 부품 | 파일 | 상태 |
|---|---|---|
| native codegen (asm emit) | `self/codegen/{arm64_darwin,x86_64_linux}.hexa` · `self/codegen.hexa` (`--emit=asm`) | ✅ 존재 |
| 실행파일 포맷 emit | `self/codegen/elf.hexa` (ELF64 · "no linker needed") + Mach-O | ✅ 존재 |
| 자체 링커 | `compiler/link/hexa_ld.hexa` v1.7 (ELF + Mach-O arm64 + ad-hoc 서명 + `--shared`) | ✅ 존재 |
| native 런타임 | `self/codegen/runtime_arm64.hexa` ("replaces runtime.c", raw ARM64 바이트) | ✅ 존재(arm64) |
| native build 드라이버 | `tool/build_hexac.hexa` (S4 native-path: `--emit=asm` → clang as+ld) | 🟠 S1-S4 |
| 결정성 검증 | `self/verify_native_determinism.hexa` | ⚠ scaffold(stub) |

## 현 frontier (정직)

- `tool/build_hexac.hexa` 가 native-path SSOT: S2 `aprime_cc --emit=asm` 로 **native codegen 까지는 C 없이** 도달. 그러나 **S4 가 clang 을 assembler+linker 로 shell** 하고 link 에 `runtime.c`(또는 release.yml Stage 0b 의 `runtime.a`)를 사용 → 여기서 C 중간물이 발생.
- 헤더 명시: "S7 (own assembler + `hexa_ld`) eliminates that last toolchain dependency" — S7 이 미완 = 이 도메인의 핵심 frontier.
- `.verdicts/`: `hexa-ld-*`(linker 단위 검증 다수 PASS) · `macho-*` · `verify-rebuild-identity7` · `unshadow-native-arr` 존재 → 부품 단위는 검증돼 있으나 **end-to-end C-free self-host fixpoint 는 미증명**.

## progress
- [x] N0 — frontier 측정 (baseline) · native-path 가 .c/.o/.a 를 어디서 만드는지 1회 실측: build_hexac.hexa S1-S4 를 darwin-arm64 에서 돌려 각 stage 산출물 종류 + clang/ar/ld 호출 지점 + runtime.c|.a 참조를 verdict 로 캡처 (현 C-중간물 inventory) · 🟢 N0 MEASURED(2026-05-31): native `--emit=obj --backend=native` 가 이미 C-free + 0-fork(.o 직접 emit · F-P3-ZERO-EXTERN + F-P0-OBJEQ corpus PROVEN). 잔여 frontier = **exec-link** 만 (hexa_ld 가 runtime.o+crt 미링크 = `--emit=exec` gap). shipping build_hexac 는 아직 --emit=asm+clang. verdict: `.verdicts/hexa-cc-native/F-HEXA-CC-NATIVE-N0-FRONTIER.txt`
- [ ] N1 — native runtime: linux-x86_64 런타임 바이트 (runtime_arm64.hexa 대응 x86_64 버전) 완성 · arm64 는 존재, x86_64 emit 경로 PASS (syscall write/exit/mmap raw bytes) verdict
- [ ] N2 — 자체 assembler (S7-as): clang assemble 단계 제거 — native codegen 이 .s(asm text) 대신 object/machine-code 바이트를 직접 emit (elf.hexa/macho 경로로) → clang -c 불요. 단일 모듈 round-trip PASS · ⚠ N0 재평가: native obj-emit 가 .s/as 경로를 우회하므로 self-assembler 는 대체로 MOOT — asm fallback 용으로만 보류 (N0 verdict 참조)
- [x] N3 — hexa_ld 링크 배선 (S7-ld): clang link 단계 제거 — hexa_ld.hexa 로 native object → 실행파일 링크. build_hexac S4 를 self-as + hexa_ld 로 교체, clang 호출 0. ./hexac --version PASS (darwin-arm64) · 🔬 N3-SMOKE(2026-05-31): exec-link frontier = 정확히 2개 배선 — (N3a) native runtime `.o` 가 `_hexa_exit`/`_hexa_set_args` export 안 함 + (N3b) `--emit=exec` 가 hexa_ld 대신 system `ld` fork. 링커 자체는 PROVEN(P1_LINKEXEC: 2 .o→실행 exit42, clang/ld 0). build/aprime_cc 직접 측정(설치 hexa 는 stale-codegen 버그로 우회). verdict: `.verdicts/hexa-cc-native/F-HEXA-CC-NATIVE-N3-SMOKE.txt` · ✅ **N3a CLOSED(2026-05-31)**: `tool/emit_runtime_obj.hexa` 가 `_hexa_exit`+`_hexa_set_args` export 하는 native Mach-O `.o`(315B, clang/as 0) emit → nm -U 두 심볼 defined·nm -u empty → native trivial.o + 이 runtime.o 링크 → **exit 42** (end-to-end). runtime-object half 닫힘. 잔여 = N3b(--emit=exec 를 hexa_ld 로). verdict: `.verdicts/hexa-cc-native/F-HEXA-CC-NATIVE-N3A-RUNTIME-OBJ.txt` · ✅ **N3b CLOSED(2026-05-31)**: compiler/main.hexa `--linker=hexa` 분기 구현(N3a runtime.o emit + hexa_ld 링크). 동등성 증명: native --emit=obj user.o + runtime.o → hexa_ld → Mach-O **executable** → exit 42, clang/as/ld fork 0 (hexa_ld 는 P1_LINKEXEC 로 fork-free 증명). opt-in 플래그라 default 무변경. 잔여: 라이브 플래그용 aprime_cc 재빌드(pool/pod) + N5 full fixpoint. verdict: `.verdicts/hexa-cc-native/F-HEXA-CC-NATIVE-N3B-EXEC-LINK.txt`
- [x] N4 — C-중간물 0 확인: native-path full build 산출물 트리에서 .c/.o/.a 파일 0 개 (find 검증) · runtime.c/.a 미참조 · clang/gcc/ar/ld 외부 호출 0 (strace/exec-trace) verdict · ✅ **CLOSED 2026-05-31**: `compiler/test/macho_p0_corpus/run_F_N4_CHAIN_ZERO_EXTERN.hexa` PASS — native user.o + N3a runtime.o + hexa_ld → Mach-O executable → exit 42 · 작업트리 `.c=0 .a=0 native.o=2` · clang/as/ld fork 0 (stripped-PATH 재실행으로 입증). SKIP-safe on Linux/no-aprime_cc. verdict: `.verdicts/hexa-cc-native/F-HEXA-CC-NATIVE-N4-CHAIN.txt`
- [ ] N5 — self-host fixpoint (byte-eq): native-built hexac 가 자기 SSOT 를 재컴파일(gen2) → 그 gen2 가 다시 재컴파일(gen3) → gen2≡gen3 실행파일 byte-eq (per-arch deterministic) darwin-arm64 · 🔬 N5a MEASURED(2026-05-31): 진짜 frontier = **native type-checker** (flatten된 compiler/main.hexa 40246L 를 aprime_cc 가 `--emit=obj` 시 codegen 前 **181 diagnostic abort** (분포: HX2001×208(undefined-name, e.g. float_to_bits) dominant · HX3004×36(return-type) · HX3001×34(HexaVal↔bool) · HX2003×32(not-callable) · HX4001×26(int-div narrow) · HX0011×26(parse `as`) — 6 클래스, 단일원인 아님)). runtime.o/hexa_ld 아님. 재정의 → **N5-pre(typecheck 181 mismatch 해소) → N5-sym(emit_runtime_obj 를 full _hexa_* 셋으로 확장; corpus union 이미 8개·runtime_arm64 181 rt_ 가 raw material) → N5-link(hexa_ld 링크→byte-eq fixpoint)**. verdict: `.verdicts/hexa-cc-native/F-HEXA-CC-NATIVE-N5A-SYMSET.txt` · 🔬 **N5-pre TRIAGE(2026-05-31)**: 181 diagnostic 재현(verbatim EXIT=1) + de-dup 분석 — raw histogram 는 각 진단을 `[HX]`태그+`explain HX` 줄로 2배 카운트한다. 실제 진단수(태그 기준): HX2001×104(undefined-name) · HX3004×18(WARN) · HX3001×17 · HX2003×16 · HX4001×13(WARN) · HX0011×13 = **181** (FATAL 150 = HX2001+HX3001+HX2003+HX0011; HX3004+HX4001 31개는 non-fatal warn). HX2001 104개 분해: ~70 parser `as`-cast cascade(=HX0011 동근) · **~28 STALE-BINARY**(intrinsic 들이 이미 `compiler/check/bind.hexa` _bind_builtin_names() allowlist 에 등록돼 있음(16:32 reland) — 현 aprime_cc 가 그 reland 前 빌드라 거부 → 재빌드만으로 해소) · ~9 genuine-missing(bytes_to_str_raw·__arr_alloc_items_zero(_int) — allowlist 3줄 추가) · ~2 `.push` member-call. HX2001 emit site = `compiler/check/types.hexa:431` _tc_resolve_ident. HX2003/HX3001/HX3004 = HexaVal 캐리어 모델 갭(types.hexa). **N5-pre 실행순서: STEP0 현소스 재빌드+재측정(~28 stale 분리) → ①parser as-cast(parser.hexa+ast.hexa, ~83 clear, 최대레버리지) → ②bind.hexa allowlist 3줄(~9) → ③types.hexa HexaVal callee/condition 타이핑(HX2003+HX3001+HX3004) → ④HX4001/.push(low prio)**. COLLAPSE-ESTIMATE: STEP0+①②③ 으로 FATAL 150→~0-2(warn 잔여). ⚠ 다음 스텝(STEP0 포함)은 aprime_cc 재빌드 필요 = pool/pod 사용자 승인 게이트(로컬 Mac 무가드 금지). verdict: `.verdicts/hexa-cc-native/F-HEXA-CC-NATIVE-N5PRE-TRIAGE.txt` · 🔴 **N5-pre STEP0 BLOCKED(2026-05-31)**: pool host **summer**(x86_64 Linux, Ubuntu clang 18.1.3)에서 origin/main `00aef842` aprime_cc 재빌드 시도 → **Linux aprime_cc 바이너리 생성 실패**, 따라서 진단 재측정 미획득(stale ~28 HX2001 분리 INDETERMINATE, 소스 레벨 주장으로 잔존). 원인 2갈래: (A) 정식 `tool/build_aprime.sh` 가 stage-4 `clang -arch arm64`(Mach-O 전용) 하드코딩 → Ubuntu clang `unsupported option '-arch'` 로 실패(stage 1-3 성공: flatten 46f/39932L → hexat transpile 43502L C → post-process). (B) x86_64 네이티브 포팅(-arch 제거·`--gc-sections`)도 clang 에서 실패 — glibc `<malloc.h>` "function cannot return function type" 헤더 충돌 + `self/runtime_core.c` 의 `hxlcl_mkdir`/`hxlcl_backtrace`/`hxlcl_backtrace_symbols_fd`/`hxlcl_longjmp` 미선언(darwin libc-shim 이 Linux 에 미배선). `/tmp/aprime_fresh` 미생성 확인. summer build/ 에 prebuilt aprime_cc 없음(hexat/hexat_real 만). N5 미플립. 다음: **STEP0b**(arm64-darwin pool/pod 재측정 — 정식 빌드 그대로 작동, 또는 `tool/build_aprime.sh` 타겟-포터블화: uname-gated stage-4 + hxlcl_* Linux shim 배선 + glibc malloc.h typedef 충돌 해소; falsifier = 의도적 undefined-name 소스가 HX2001 emit + non-zero exit) 으로 측정 unblock → 이후 STEP1 parser as-cast 우선. verdict: `.verdicts/hexa-cc-native/F-HEXA-CC-NATIVE-N5PRE-STEP0-REBUILD.txt`
- [ ] N6 — cross-arch: N2-N5 를 linux-x86_64 에서도 PASS (native as+ld+runtime+fixpoint). per-arch byte-eq (cross-arch strict byte-eq 는 codegen arch-specific 이라 비요구, P1/P3 모델 일치)
- [ ] N7 — CI 게이트: native C-free self-host 를 nobaseline-gate 류 워크플로로 승격 (3-platform) — `.c/.o/.a=0` 산출물 assert + fixpoint assert green. release 무손상(병렬 옵션, green 될 때까지 C 경로 유지)
