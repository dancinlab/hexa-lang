# INBOX — current state

@goal: cross-project handoff 수신함 — 다른 repo가 hexa-lang으로 넘긴 gap·request를 추적하고 해소

(현재 상태만 기록 — 열린 handoff는 `- [ ]` 로, 처리 이력은 `INBOX.log.md` 로)

- [x] **hexa cloud pod 생성(provision) verb 부재** (from: demiurge RTSC) — 수신·라우팅 완료 (g48): **기 추적 갭**(신규 아님). provisioning verb = `archive/patches/archive/hexa-cloud-preflight-stub-and-provisioning-gap` gap#2 (→ RFC 088 P-series) · DFT/HPC preflight = **RFC 091**. RTSC SrAuH₃를 RFC 091 witness로 보강 권고. 상세 → `INBOX.log.md`
- [ ] **(wrapper 해소 2026-05-26 · drill-runtime 잔존) pool 호스트 hexa CLI — atlas-loop/drill 발사** (from: this-session 2026-05-25T20:50Z · anima G1) — **✅ wrapper 1차 블로커 해소**: ubu-1 이미 작동(`/home/aiden/.local/bin/hexa` → `hexa_real` May 25, `hexa --version` OK) · **ubu-2 fix**: `~/.hx/bin/hexa`(May 26 실바이너리) 직접실행은 OK였으나 non-interactive ssh PATH 에 `~/.local/bin`/`~/.hx/bin` 부재(`.profile` 은 login-only) → bare `hexa` not-found. `~/.bashrc` 최상단에 `export PATH="$HOME/.local/bin:$HOME/.hx/bin:$PATH"` prepend(reversible bak) → `ssh ubu-2 hexa` 정상, `hexa parse` OK, `hexa drill` 이제 **REACHABLE·RUN**(더는 not-found). **🔸 잔존 = drill-runtime**: ubu-2 `hexa drill --rounds 1` 가 `overlay+ 517 lines` 까지 가다 `map key 'f_a' not found` + `cannot compare tag 4 with tag 0 (operator >)` 런타임 에러 — wrapper 아닌 별도 drill-engine/stale-binary 이슈(ubu-2 hexa May-26 바이너리 vs 현 main drift; item의 `hexa cc --regen` 잔여부 [[reference_linux_transpiler_stale_build_recipe]]). 다음 = ubu-2 hexa 현-main 재빌드(native gcc amalgam) 후 drill 재smoke. 상세 → `INBOX.log.md`
- [x] **`hexa cloud nohup` 조기-사망 미감지 — silent class-1 launch 실패 ($3.92/158분 idle burn)** (from: anima `inbox/patches/cloud-launch-trainer-script-arg-missing.md`) — launcher 가 exec 직후 즉사(잘못된 script-path argv)해도 dispatcher 는 pid 만 받고 즉시 리턴 → watchdog 타임아웃까지 죽은 pod 가 과금. **해소(Option C, hexa-lang 측)**: `hexa cloud nohup … --early-life-check <sec>` 추가 — launch 후 `<sec>`초 sleep + pid 1회 poll, 이미 죽었으면 **exit 3** 으로 호출자가 즉시 teardown (#1120). Option A(dispatcher argv 수정)는 anima-local — 이미 anima #423 으로 closed (script-path argv 포함, 버그 형태 트리 전체 소멸). 상세 → `INBOX.log.md`
- [ ] **flame V3 coverage 갭 — full-position CE + V3-extension backward 등 (anima ConsciousDecoderV3 포팅)** (from: anima `inbox/patches/anima-flame-v3-coverage-gaps.md` · 2026-05-26) — anima V3 학습 스택(torch)을 `stdlib/flame` 로 포팅, forward smoke PASS 했으나 fallback 로 진행. **P1**(학습정확도 직접): ① full-position CE (`decoder_lib` 가 last-position only → `nn_lm_head_fwd`+CE 전위치 확장) ② V3-extension backward (purefield·head_g·cross·tension_proj reverse-mode → `ag_tape` multi-objective, **RFC 059** planned). **P2**: Qwen BPE 토크나이저 부재(byte-level V=256 fallback) · `from_qwen` warm-start `.pt` 로더 부재 · `nn_rope_build_tables` base 10000 하드코딩(Qwen=50000 → 인자화). **P3**: bnb 8-bit · cross-attn · KV-cache generate · mitosis gaussian RNG · ln learned gains. 차단 아님(fallback 동작). 상세 → `INBOX.log.md`
- [ ] **import 시 `fn main()` auto-invoke 가 모듈 라이브러리 재사용 차단** (from: anima `inbox/patches/anima-discovered-2gaps-2026-05-25.md` G2) — `import <mod>` 만 해도 모듈의 top-level `main()` 이 auto-fire → eval/probe 모듈을 라이브러리로 못 쓰고 로직 inline 복제 강제 (anima `corpus_quality_probe.hexa` TTR 로직). 트리에 가드 컨벤션 전무(확인됨). **언어-semantics 변경 = blast-radius 有 → 설계결정 필요** (RFC 후보). 제안 택1: import 시 auto-main 억제(Python `__main__` 등가) · 또는 `fn _selftest()` 분리 컨벤션 문서화. 상세 → `INBOX.log.md`
- [ ] **Linux CI 빌드 게이트 — forge/farr32 codegen→clang smoke + Apple-only 사일런트 회귀 방지** (from: anima forge d768 GPU fire · `inbox/notes/linux-ci-build-gate-cross-platform-silent-regression.md` · 2026-05-26 · 기존 #1206 = note-filing 만, 제안 미구현) — V=151643 fire 가 Linux x86_64 빌드를 계층별로 깨뜨림(farr32 bare emit #1187 · `HXLCL_SYS_SELECT` Apple-arm64 #1194 · raw-trap 블록 #1198). 근본 = ① 개발·CI 가 전부 Mac → runtime.c 의 Linux `#else`/`#elif` 갭이 영영 안 빌드됨 ② `hexa check` = parse/lint 만(codegen→clang 안 함) ③ farr32(FP32 forge) 경로 희귀. **제안 잔존**: ② forge/farr32 codegen→clang smoke(`hexa build --c-only` → clang `-fsyntax-only`) + ③(선택) `hexa check --compile` 는 **CI 에 부재**(farr32 smoke 0건 확인). ① Linux 빌드 게이트는 `bootstrap.yml` 의 linux-x86_64/arm64 매트릭스(pull_request 트리거·실 clang+e2e smoke)로 **부분 커버** — 단 forge-특화 farr32 emit 은 미커버. 상세 → `INBOX.log.md`
- [ ] **stdlib 모듈 테스트 부재(CI) + RFC builtin-first 확인 규칙** (from: anima STDLIB M3/cycle-full · `inbox/notes/stdlib_module_test_and_builtin_findings_2026_05_24.md` · 2026-05-24) — anima 8 stdlib 모듈(#769·#780~#783) land 중 발견 3건. ① **CI coverage gap**: `bootstrap.yml` 은 self-bootstrap + 단일 `hi.hexa` smoke 만, `stdlib/**/*_test.hexa` 는 CI 미실행(확인됨) → stdlib 회귀/링크오류 안 잡힘. test runner 단계 권장(module_loader flatten→build→run all). ② **DX**: 단일파일 `hexa build <f>` 가 import flatten 안 함 → imported `pub fn` extern → `ld: undefined symbol`(CI 의 module_loader 2-step 만 동작). 자동 flatten 또는 docs 명시. ③ **builtin-first 규칙**: `log2`/`abs`/`fabs`/`sqrt`/`pow`/`floor`/`log` 전부 동작 builtin → 새 수치 primitive 제안 전 `git grep 'if s == "<name>"' self/codegen.hexa` 확인 (byte-eq frozen baseline consumer 는 swap 금지). 상세 → `INBOX.log.md`

> 참고: hexa-lang 내부 upstream-patch staging `inbox/` 폴더는 폐기됐다(rehome → `archive/patches/` · `archive/fires/` · `docs/notes/` · `docs/rfc/`). upstream 변경은 일반 PR 워크플로로 흐른다. 이 INBOX 도메인은 다른 repo 가 넘긴 cross-repo handoff 전용으로 별개 시스템이며 그대로 유지된다.
