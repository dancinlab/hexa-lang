# INBOX — current state

@goal: cross-project handoff 수신함 — 다른 repo가 hexa-lang으로 넘긴 gap·request를 추적하고 해소

(현재 상태만 기록 — 열린 handoff는 `- [ ]` 로, 처리 이력은 `INBOX.log.md` 로)

- [x] **(✅ RESOLVED 2026-05-28 · PR #1869) BPE 토크나이저 O(N) 조회 정체 — full Qwen V=151643 corpus 사실상 종료 불가** (from: anima DECODER M4b rev2 production fire · 2026-05-28 · `CORE/DECODER/DECODER.md` L133) — `self/ml/tokenizer_bpe.hexa` 의 `get_merge_rank` 가 pair 마다 151,388 merges 를 선형스캔(`bpe_merge_word` 의 O(word_len²) 안에서) + `bpe_token_lookup` 이 vocab 151,643 엔트리 선형스캔 → diverse corpus(1.2MB/38KB, Korean byte-char 폭증) tokenize 가 사실상 종료 불가. anima 는 6.6KB corpus 로 우회. **이게 M4c "BPE detokenize 의존"의 실체** — `#1556` 은 정확성(encode/decode codepoint-aware) fix 였고 본 건은 full-BPE-scale **성능**(별개 layer). **해소(PR #1869)**: `merge_ranks` 배열→`{key:rank}` 해시맵 + `build_token_id_map` 신규(`{token_str:id}` 해시맵) → 양 조회 O(1) (FNV-1a `hexa_map`); 둘 다 `map_contains_key` 가드로 흔한 miss 가 런타임 `"key not found"` stderr 경로 회피. 공개 API(`bpe_load`·`bpe_encode`·`bpe_decode`·`bpe_token_to_id`·`bpe_vocab_size`) 불변. **검증(g5)**: ubu-2 current-main `hexa parse` → `OK: parses cleanly` + 격리 map-로직 9/9 PASS (merge-rank {0,1,2}+miss=-1, token-lookup {0,2,3}+UNK miss=0). 잔여 = anima 측 full Qwen V=151643 corpus 재발사로 종료시간 회귀 측정 (별 follow-up). 상세 → `INBOX.log.md`.

- [x] **(✅ ROUTED 2026-05-28 · anima-side 1줄 swap → `mkdir(path)` · hexa 변경 0) `dir_create` cross-backend codegen gap** (from: anima DECODER M4b rev2 production fire · 2026-05-28 · `CORE/DECODER/DECODER.md` L133) — trainer.c 의 `hexa_call1(dir_create, X)` 가 Linux gen2 backend 에서 undeclared (`trim` #1527 과 동종으로 보고). **진단(dup-race precheck, [[feedback_inbox_dup_race_precheck]])**: `dir_create` 는 어디에도 등록 안 된 이름 — bind allowlist·`self/codegen.hexa`·interp·arm64 backend 전부 부재(anima 작명). codegen 이 모르는 free-fn → generic `hexa_call1(dir_create,…)` → 미정의 심볼. **권고 = `mkdir(path)`** (실측 검증): `self/codegen.hexa` 가 `mkdir(p)` → `hexa_bool(mkdir(HX_STR(p),0755)==0)` 직접 libc `mkdir(2)` 로 lower — ubu-2 current-main `hexa run`(compile-then-exec) 실측 `mkdir -> true` + dir 실제 생성 확인. 단일레벨(부모 미생성 — 중첩 경로는 레벨별 mkdir, 또는 아래 stub fix 후 `fs_mkdir_p`). **⚠ 정정 — `fs_mkdir_p` 는 쓰지 말 것**: 본 entry 초판은 `fs_mkdir_p`(→`rt_fs_mkdir_p` "재귀 POSIX mkdir") 를 권고했으나 **오진**: `rt_fs_mkdir_p`(`self/runtime.c:13605`) 는 **silent no-op weak stub**(`(void)path; return hexa_int(0)`). `fs_mkdir_p(p)` 는 컴파일·`0`("성공") 반환하나 **dir 안 만듦**(default `hexa build`/`run` 실측 미생성). anima sed 의 `rt_fs_mkdir_p(` 도 사실 no-op 이었음 — ckpt-save 폐기(inline decode)라 안 물렸을 뿐. → 아래 별 OPEN finding. g0 Occam — `dir_create` 별칭 추가 안 함; working `mkdir` 존재라 hexa-lang 코드변경 불요.

- [ ] **`fs_mkdir_p` (→ `rt_fs_mkdir_p`) = silent no-op stub — 재귀 mkdir 빌트인이 default 빌드에서 깨짐** (from: this-session 2026-05-28 · `dir_create` 진단 부산물) — `self/runtime.c:13605` `rt_fs_mkdir_p(path){ (void)path; return hexa_int(0) }` = path 무시·성공반환·무동작. `fs_mkdir_p` 호출(codegen→`rt_fs_mkdir_p`)이 default `hexa build`/`run` 에서 silent no-op (ubu-2 current-main 실측 `0`+미생성). 근원 = PR #1391/#1394 가 "hexa-emit-wired" 20fn (#1331 table) 의 C 바디 삭제 → strong hexa-emit `.o` 링크 시에만 실동작 가정인데 그 강한 .o 가 default 빌드 미링크 → weak link-satisfy stub 노출. **위험 = silent-success**(재귀 mkdir 의존 consumer 가 조용히 실패; data-loss class). **fix 후보**: ① `rt_fs_mkdir_p` 를 진짜 재귀 POSIX mkdir 루프로 (weak 유지 → strong .o 여전히 우선) ② 또는 strong hexa-emit `fs_mkdir_p` `.o` 를 default 빌드 링크 포함. ⚠ `self/runtime.c` 직접편집은 deploy-regen wipe 위험([[feedback_runtime_c_deploy_regen_wipe]]) — hexa-emit source layer 확인이 선결. 별 dedicated 사이클. 임시 우회 = 단일레벨 `mkdir(p)` 레벨별 호출.

- [x] **(✅ RESOLVED 2026-05-27 · 요청 OBSOLETE — 작업 이미 GPU.md 에서 LANDED) flame-P2b Qwen BPE tokenizer = anima DECODER MoE-fresh 의 scale-gate (우선순위 재평가 요청)** (from: anima DECODER M4b · 2026-05-27 · `inbox/notes/anima-decoder-moe-toy-pass-gates-flame-p2b-qwen-bpe.md`) — 신규 갭 아님: `GPU.md` flame-P2b (현재 "저우선 enhancement·차단아님")의 **우선순위 재평가**. anima DECODER 가 register-collapse↔underfit 더블바인드 탈출용 MoE-fresh (UNIVERSE H_490) 를 hexa-native 구현 + toy 검증 **PASS** (top-1 hard-route: gate(A)→e0·gate(B)→e1 분화). 메커니즘 실증 완료 → 3B Qwen 스케일 검증이 byte-level V=256 한계로 막힘, flame-P2b (BPE tokenizer) 필요. enhancement → validated-consumer scale-gate 로 승격. anima-side workaround (Python/Qwen MoE 이식) 는 hexa-only 위반 → upstream 이 옳은 경로. 상세 → note 파일. **✅ OBSOLETE 확인 (2026-05-27 dup-race precheck, [[feedback_inbox_dup_race_precheck]] g0)**: 본 INBOX entry 가 요청한 "우선순위 bump → flame-P2b 작업 진행" 이 이미 origin/main 의 `GPU.md` L1493 에서 `[x]` 완료 상태. 전체 work chain landed: codegen `trim` lowering fix (PR #1527) · `hexa_cc.c` bootstrap regen + gen1==gen2 fixpoint (#1533) · stdlib/flame BPE-corpus loader (#1537) · `chr→from_char_code` Unicode fix (#1556) · decode codepoint-aware iteration · ubu-2 실측 **Qwen V=151643 round-trip PASS** (`decoded=[consciousness emerges from cells]` 공백 정확 복원). anima #1537 `flame_bpe_roundtrip` 가드 TRUE → 3B Qwen hexa-native 학습 path 정상화. **요청 의도(scale-gate 승격)가 결과적으로 만족됨** (작업 우선순위가 자동 bump 되어 실제로 진행+완료). 추가 hexa-lang 측 액션 없음.

- [x] **hexa cloud pod 생성(provision) verb 부재** (from: demiurge RTSC) — 수신·라우팅 완료 (g48): **기 추적 갭**(신규 아님). provisioning verb = `archive/patches/archive/hexa-cloud-preflight-stub-and-provisioning-gap` gap#2 (→ RFC 088 P-series) · DFT/HPC preflight = **RFC 091**. RTSC SrAuH₃를 RFC 091 witness로 보강 권고. 상세 → `INBOX.log.md`
- [x] **(✅ RESOLVED 2026-05-27 · PR #1465 재확인 DRILL_EXIT=0) pool 호스트 hexa CLI — atlas-loop/drill 발사** (from: this-session 2026-05-25T20:50Z · anima G1) — **✅ wrapper 1차 블로커 해소**: ubu-1 이미 작동(`/home/aiden/.local/bin/hexa` → `hexa_real` May 25, `hexa --version` OK) · **ubu-2 fix**: `~/.hx/bin/hexa`(May 26 실바이너리) 직접실행은 OK였으나 non-interactive ssh PATH 에 `~/.local/bin`/`~/.hx/bin` 부재(`.profile` 은 login-only) → bare `hexa` not-found. `~/.bashrc` 최상단에 `export PATH="$HOME/.local/bin:$HOME/.hx/bin:$PATH"` prepend(reversible bak) → `ssh ubu-2 hexa` 정상, `hexa parse` OK, `hexa drill` 이제 **REACHABLE·RUN**(더는 not-found). **🔸 잔존 = drill-runtime**: ubu-2 `hexa drill --rounds 1` 가 `overlay+ 517 lines` 까지 가다 `map key 'f_a' not found` + `cannot compare tag 4 with tag 0 (operator >)` 런타임 에러 — wrapper 아닌 별도 drill-engine/stale-binary 이슈(ubu-2 hexa May-26 바이너리 vs 현 main drift; item의 `hexa cc --regen` 잔여부 [[reference_linux_transpiler_stale_build_recipe]]). 다음 = ubu-2 hexa 현-main 재빌드(native gcc amalgam) 후 drill 재smoke. 상세 → `INBOX.log.md`. **✅ 해소**: ubu-2 hexa.real 이미 현-main 빌드 (2026-05-27, stale-tell `%ld %ld`=0) → drill RUN clean (DRILL_EXIT=0); root cause = drill 소스가 옛 필드 `verdict.f_a` 읽던 것 → 현 main `verdict.f_ai2_a` (#1001). 별개 Mac-only round-2 segfault (cli_args argv over-slice)는 follow-up filing.
- [x] **`hexa cloud nohup` 조기-사망 미감지 — silent class-1 launch 실패 ($3.92/158분 idle burn)** (from: anima `inbox/patches/cloud-launch-trainer-script-arg-missing.md`) — launcher 가 exec 직후 즉사(잘못된 script-path argv)해도 dispatcher 는 pid 만 받고 즉시 리턴 → watchdog 타임아웃까지 죽은 pod 가 과금. **해소(Option C, hexa-lang 측)**: `hexa cloud nohup … --early-life-check <sec>` 추가 — launch 후 `<sec>`초 sleep + pid 1회 poll, 이미 죽었으면 **exit 3** 으로 호출자가 즉시 teardown (#1120). Option A(dispatcher argv 수정)는 anima-local — 이미 anima #423 으로 closed (script-path argv 포함, 버그 형태 트리 전체 소멸). 상세 → `INBOX.log.md`
- [x] **(✅ RESOLVED 2026-05-27 · `bin/hexa-cloud` 0.3.4 deployed) RunPod pod-id → SSH-host resolver "부재" = stale 배포 바이너리** (from: anima M4b Phase 4-fire · 2026-05-27) — pod RUNNING 인데 pod-id 를 ssh host:port 로 resolve 할 길이 막혀 보였고(rented-pod remote-CLI 경로·raw GraphQL HTTP 둘 다 cloud-guard 차단), `hexa cloud` 엔 pod-id 받는 verb 가 없는 줄 알고 transport-resolve **deadlock** 으로 기재. **근본원인 = resolver 부재 아님**: `runpod_get_ssh_port`(`stdlib/cloud/runpod.hexa`) + `cloud ssh-port <id> --provider runpod` verb 가 이미 lifecycle-허용 `pod get -o json`(`ssh.ip`/`ssh.port`) + GraphQL fallback 로 resolve 함 — **라이브 pod 로 실측 확인**(`ssh.ip=213.181.105.227`·`ssh.port=11863` 정확 파싱). 진짜 블로커 = **배포된 `bin/hexa-cloud` 가 stale 0.1.0 (cycle A)** = ssh-port verb 이전 버전 (source 는 rent/list/ssh-port 랜딩 후 0.3.x). **해소**: ① source 에 `resolve` = ssh-port 의 discoverable alias 추가 — 요청한 `cloud resolve <POD_ID>` 이름 그대로 동작 · 버전 0.3.4 (branch `inbox-cloud-resolve-2026-05-27`) ② `hexa build` 로 0.3.4 재빌드 → `~/.hx/bin/hexa-cloud` 배포 (실 `hexa cloud version`=0.3.4 · `hexa cloud resolve` reachable 확인). cloud-guard 변경 **불요** — `hexa cloud resolve` 는 허용경로이고 내부 provider-CLI/API 호출은 hexa 프로세스 내부(Bash-tool guard 아래)에서 일어남. anima 는 라이브 pod 에 `hexa cloud resolve <POD_ID> --provider runpod` 로 즉시 발사 가능. **🔸 잔여(별건 follow-up)**: `self/main.hexa` 인라인 `hexa cloud`/`hexa cloud help` 헬프가 stale 6-verb(ssh-port/resolve/rent 누락) — full `hexa` 재빌드 필요라 분리(우회 = `hexa cloud version` / `bin/hexa-cloud --help`). 상세 → `INBOX.log.md`
- [x] **(✅ P1 코어 COMPLETE · P2/P3 → GPU 도메인 이관) flame V3 coverage 갭 — full-position CE + V3-extension backward 등 (anima ConsciousDecoderV3 포팅)** (from: anima `inbox/patches/anima-flame-v3-coverage-gaps.md` · 2026-05-26) — anima V3 학습 스택(torch)을 `stdlib/flame` 로 포팅, forward smoke PASS 했으나 fallback 로 진행. **P1**(학습정확도 직접): ① full-position CE (`decoder_lib` 가 last-position only → `nn_lm_head_fwd`+CE 전위치 확장) ② V3-extension backward (purefield·head_g·cross·tension_proj reverse-mode → `ag_tape` multi-objective, **RFC 059** planned). **P2**: Qwen BPE 토크나이저 부재(byte-level V=256 fallback) · `from_qwen` warm-start `.pt` 로더 부재 · `nn_rope_build_tables` base 10000 하드코딩(Qwen=50000 → 인자화). **P3**: bnb 8-bit · cross-attn · KV-cache generate · mitosis gaussian RNG · ln learned gains. 차단 아님(fallback 동작). 상세 → `INBOX.log.md` (P1 ① full-position CE landed via PR #1481 — `nn_lib::nn_lm_head_fwd_allpos` + `nn_ce_loss_allpos`, tiny-oracle 3/3 PASS; P1② backward DESIGN = RFC 059 (`docs/rfc/rfc_059_flame_v3_backward.md`, Path-B ag_tape reverse-mode 설계 — 4 op grad + multi-obj 합성 + grad-check 1e-6) via PR #1487; P1②-a head backward IMPL via PR #1489 — `ag_k_lmhead_allpos()=14` + recorder `ag_lmhead_allpos` + seed `ag_ce_seed_allpos` + reverse arm (reuses `nn_lm_head_bwd` per-row, zero new math); grad-check T=1 byte-eq + T=3 central-diff rel 1.16e-09 ≤ 1e-6 PASS; P1②-b 4-op backward IMPL via PR #1491 — head_g (zero new code, `ag_lmhead`×2 registry-accumulate, rel 5.26e-09) · cross/L_ψ seed `ag_cross_psi_seed` (dual-seed, rel 2.05e-08) · tension/L_route seed `ag_tension_route_seed` (reduction-seed, rel 3.15e-09) · purefield `ag_k_purefield()=15` + new erf-GELU primitive `nn_gelu_fwd/bwd`+`nn_purefield_fwd/bwd` (rel 2.04e-10); all 4 grad-checks PASS ≤ 1e-6, P1②-a regression byte-identical; P1②-c multi-objective composition IMPL via PR #1492 — caller-side composition (ZERO new stdlib code per RFC §5/g0: each loss-layer seed helper already λ-weights + pre-seeds the registry, so a composite loss = call each seed helper then ONE `ag_backward_reg` walk; seed-linearity sums them) + end-to-end test `flame_multiobj_compose_test` (shared-z spine → head_a + head_g + purefield; L = w1·CE + w2·L_ψ + w3·L_route): F-RFC059B-MULTIOBJ-LINEARITY composed Mg == Σ per-objective Mg (rel 2.72e-16) AND vs central-diff of TOTAL loss (worst rel 4.81e-08 ≤ 1e-6) PASS · F-RFC059B-MULTIOBJ-DESCENT 5-step composite descent monotone (2.335→1.738) PASS; **P1② COMPLETE: RFC 059 #1487 + P1②-a #1489 + P1②-b #1491 + P1②-c #1492. ✅ P1(학습-정확도 코어) 종결. P2(rope base 인자화·Qwen BPE·`.pt` loader) + P3(bnb 8-bit·cross-attn·KV-cache·mitosis RNG·ln gains)는 저우선 enhancement(차단 아님) → `GPU.md` "flame V3 port" 섹션으로 이관(flame-P2a~P3e 8 milestone). INBOX handoff 종결.**)
- [x] → RFC landed (`docs/rfc/rfc_import_auto_main_suppression.md`) — **import 시 `fn main()` auto-invoke 가 모듈 라이브러리 재사용 차단** (from: anima `inbox/patches/anima-discovered-2gaps-2026-05-25.md` G2) — `import <mod>` 만 해도 모듈의 top-level `main()` 이 auto-fire → eval/probe 모듈을 라이브러리로 못 쓰고 로직 inline 복제 강제 (anima `corpus_quality_probe.hexa` TTR 로직). 트리에 가드 컨벤션 전무(확인됨). **언어-semantics 변경 = blast-radius 有 → 설계결정 필요** (RFC 후보). 제안 택1: import 시 auto-main 억제(Python `__main__` 등가) · 또는 `fn _selftest()` 분리 컨벤션 문서화. **✅ 안전한 절반(컨벤션 문서화) = `stdlib/README.md` 모듈-작성 노트에 landed**(로직=`pub fn`, 자가검증만 `main()`/`_selftest()`; `@selftest_skip`/`@ci_gate` 마커). 🔸 잔여 = auto-main 억제 semantics-change(RFC). 상세 → `INBOX.log.md`
- [x] **✅(#1274) Linux CI 빌드 게이트 — forge/farr32 codegen→clang smoke + Apple-only 사일런트 회귀 방지** (제안② = `tool/forge_farr32_codegen_smoke.hexa` + bootstrap.yml 3-job `hexa build --c-only`→`clang -fsyntax-only -Werror=implicit-function-declaration` 으로 landed #1274. ①=기존 linux 매트릭스 부분커버 · ③`hexa check --compile`=별건 잔여) (from: anima forge d768 GPU fire · `inbox/notes/linux-ci-build-gate-cross-platform-silent-regression.md` · 2026-05-26 · 기존 #1206 = note-filing 만, 제안 미구현) — V=151643 fire 가 Linux x86_64 빌드를 계층별로 깨뜨림(farr32 bare emit #1187 · `HXLCL_SYS_SELECT` Apple-arm64 #1194 · raw-trap 블록 #1198). 근본 = ① 개발·CI 가 전부 Mac → runtime.c 의 Linux `#else`/`#elif` 갭이 영영 안 빌드됨 ② `hexa check` = parse/lint 만(codegen→clang 안 함) ③ farr32(FP32 forge) 경로 희귀. **제안 잔존**: ② forge/farr32 codegen→clang smoke(`hexa build --c-only` → clang `-fsyntax-only`) + ③(선택) `hexa check --compile` 는 **CI 에 부재**(farr32 smoke 0건 확인). ① Linux 빌드 게이트는 `bootstrap.yml` 의 linux-x86_64/arm64 매트릭스(pull_request 트리거·실 clang+e2e smoke)로 **부분 커버** — 단 forge-특화 farr32 emit 은 미커버. 상세 → `INBOX.log.md`
- [x] **✅(#1271+README) stdlib 모듈 테스트 부재(CI) + RFC builtin-first 확인 규칙** (①=`stdlib_selftest_aggregate --ci-gate` opt-in marker + bootstrap.yml 와이어 #1271 · ②③=`stdlib/README.md` 모듈-작성 노트 docs) (from: anima STDLIB M3/cycle-full · `inbox/notes/stdlib_module_test_and_builtin_findings_2026_05_24.md` · 2026-05-24) — anima 8 stdlib 모듈(#769·#780~#783) land 중 발견 3건. ① **CI coverage gap**: `bootstrap.yml` 은 self-bootstrap + 단일 `hi.hexa` smoke 만, `stdlib/**/*_test.hexa` 는 CI 미실행(확인됨) → stdlib 회귀/링크오류 안 잡힘. test runner 단계 권장(module_loader flatten→build→run all). ② **DX**: 단일파일 `hexa build <f>` 가 import flatten 안 함 → imported `pub fn` extern → `ld: undefined symbol`(CI 의 module_loader 2-step 만 동작). 자동 flatten 또는 docs 명시. ③ **builtin-first 규칙**: `log2`/`abs`/`fabs`/`sqrt`/`pow`/`floor`/`log` 전부 동작 builtin → 새 수치 primitive 제안 전 `git grep 'if s == "<name>"' self/codegen.hexa` 확인 (byte-eq frozen baseline consumer 는 swap 금지). 상세 → `INBOX.log.md`

- [x] **(✅ RESOLVED · PR #1283) install 레이아웃 불완전 — `~/.hx/bin` 에 `stdlib`·`compiler` 심볼릭 부재 (durable, #1275 근본)** (from: hexa-lang self-host 2026-05-26) — install dir 가 `self`·`tool` 심볼릭만 생성하고 `stdlib`·`compiler` 는 누락 → env-less(HEXA_LANG unset) stdlib-using 빌드에서 module_loader 의 self-derived install 해소가 `<inst>/stdlib/*` 못찾아 trailing `use` silent-drop. `hexa loop`·cron·launchd·subagent·`hive --dev` cold-start 등 전부 영향(#1275 가 이걸로 막혔음). **로컬 우회 적용**(`ln -s <repo>/{stdlib,compiler} ~/.hx/bin/`, env-free `hexa loop` 검증 완주). **durable**: installer(`hx install` / `build_hexa_cli --install`)가 `self`/`tool` 과 동일하게 `stdlib`·`compiler` 심볼릭도 멱등 생성해야(#1259 가 hexa.real driver-deploy 가드 넣은 것과 같은 deploy-완전성 class). 상세 → `INBOX.log.md` 2026-05-26T18:10Z #1275. **✅ 해소**: `~/.hx/bin/{stdlib,compiler,self,tool}` 심볼릭 4개 모두 존재 확인 (2026-05-27, → hexa-lang-main).

- [ ] **NVPTX codegen — @shared "reduce-scratch → re-write → re-read" lifecycle NaN miscompile** (from: GPU cycle-fg Round 8+9 · 2026-05-28) — §5a LN+GEMM combined 1-kernel wedge → **GPU all-NaN**. **Round 9 4-fire isolation narrows root cause precisely** (`tool/artifacts/ln_gemm_diag_2026_05_28.out`): ① `probe_ln_dump_f64` (self-dump, no cross-thread read) → NaN ⇒ **cross-thread read 기각** (R8 hypothesis-b 틀림). ② `probe_ln_diag2_f64` (stats dump) → **GPU mean=NaN at first sum-reduce**. ③ `probe_ln_gemm_v2_f64` (to_i64()-wrapped every sm index) → 여전히 NaN ⇒ **to_i64 wrapper 가 fix 아님** (R8 hypothesis-a 틀림). ④ `probe_ln_dump_v2_f64` (to_i64 + self-dump, no GEMM, 3-param) → 여전히 NaN. **결정적 사실**: R4 `probe_layernorm_f64` (to_i64, 2-reduce + normalize → **global y 직행, sm 재사용 안 함**) = PASS max_abs=0. 모든 NaN-variant 공통점 = **Phase 1c `sm[tid] = normed` (두 reduce 가 소비한 @shared 를 normalize 결과로 다시 write-then-read 하는 3rd-phase 재사용)**. → **root cause = nvptx_emit 이 @shared array 의 "reduce-scratch consume → re-init write → re-read" lifecycle 을 miscompile** (to_i64/cross-thread/param-count 무관; 3rd write-phase 가 stale/garbage 를 만들어 NaN 전파). Round 2 argmax @shared partition-offset bug 와 같은 @shared-lifecycle family. Artifacts: `tool/artifacts/ln_{dump,diag2,gemm_v2,dump_v2}_f64_2026_05_28.ptx` + `ln_gemm_diag_2026_05_28.out` (4-fire transcript). **Fix path**: `self/codegen.hexa` 또는 nvptx_emit 의 @shared store/load address 계산이 reduce-loop 후 재-write 시 base offset 을 잃는지 — PTX 의 `st.shared`/`ld.shared` 주소 reg 가 Phase 1c 에서 reduce-loop 의 stale reg 재사용하는지 확인. **workaround 후보 FALSIFIED**: `probe_ln_gemm_v3_f64` (normalize 를 별도 2nd @shared `sm2[256]` 에 write, reduce scratch `sm` 재사용 안 함) → 여전히 NaN (5th fire). 2 distinct @shared array 도 fix 아님 ⇒ bug 는 sm-reuse 가 아니라 **3rd @shared-write-phase (reduce 후 추가 @shared store) 자체** miscompile. R4 (normalize → global y 직행, @shared 3rd-write 전무) 만 PASS. dedicated codegen session 필수 — PTX `st.shared`/`ld.shared` address-reg lifecycle 정밀 분석.

- [x] **(✅ codegen-fix R12 2026-05-28 · silicon re-fire PENDING infra) NVPTX codegen — `to_f64(thread-derived i64)` unsupported-call (RoPE wedge)** — **✅ RESOLVED (codegen R12, branch `nvptx-to-f64-lower`)**: `compiler/codegen/nvptx_target.hexa` 에 `to_f64`/`to_f32` lowering arm + classify 추가 (silicon-proven `to_i64` arm 정확한 mirror — `cvt.rn.f64.s64`/`.s32` + reg-bank routing); to_f32 는 default-F64 bank 도 wrong 이었어서 함께 close. unit test `nvptx_lower_test.hexa` Case 28c `_test_to_f64` (asserts `cvt.rn.f64.s64 %fd2, %rd1` + no unsupported-call marker). 🟢 by-construction (parse-clean + test-pinned PTX). 🔸 **silicon re-fire PENDING** — ubu-2 fresh-clone nvptx_emit 재빌드가 별개 build-infra breakage(바로 아래 entry)에 막힘; RoPE wedge silicon PASS 확인은 clean nvptx_emit deploy 후. ── 원 finding (from: GPU cycle-loop Round 11 · 2026-05-28) — RoPE 1-kernel wedge (`tool/probe_rope_f64.hexa`, pure elementwise, `θ=pos/base^(2i/D)` per-pair rotation) silicon-fired on ubu-2 RTX 5070 sm_120 → **wrong value (NOT NaN), first-fail @ pair 8** (pairs 0-7 correct, max_abs=2.659). **root cause PTX-pinpointed**: `probe_rope_f64.ptx` line 101/103 = `// RFC 055 055-P0 - unsupported call: to_f64` (2 occurrences). `to_f64(tid)` + `to_f64(D)` (thread-derived/computed i64) 가 nvptx_emit 에서 **미지원** — 주석만 emit, 변환 미발생 → `exponent = 2.0*to_f64(tid)/to_f64(D)` garbage → `pow` wrong → wrong θ. **pair 0 = exponent 0 (0/x=0) 이라 우연 correct, pair 8+ 발산**. **중요 대조**: R4 LayerNorm `sm[0]/to_f64(n)` (n=PARAM i64) = PASS; 본 RoPE `to_f64(tid)` (thread-derived register) = FAIL → **nvptx to_f64 lowering 이 param-i64 는 처리하나 thread-derived/computed i64 는 unsupported-call 로 떨어짐** (context-dependent). softmax PTX (`tool/artifacts/softmax_f64_2026_05_28.ptx`) = `unsupported call` 0건 → PASS (대조 증거). **Fix path**: `self/codegen.hexa` nvptx target 의 `to_f64`/`to_i64` lowering 이 SSA-value source (param vs register vs computed) 에 따라 분기하는지 — thread-derived i64→f64 `cvt.rn.f64.s64` emit 누락 확인 ([[reference_new_codegen_intrinsic_4_surface]] nvptx 미러 class). **workaround**: host 에서 per-pair angle 배열 precompute → kernel 은 cos/sin 만 (to_f64 회피). NN-primitive catalog (GPU.md L709) 4번째(RoPE) 가 이 gap 으로 blocked; softmax(R11)·layer_norm(R4)·swiglu-act(R6) 는 PASS. Artifacts: `tool/artifacts/{rope,softmax}_f64_2026_05_28.ptx` + `nnprim_2026_05_28.out`. dedicated codegen session.

- [ ] **build-infra — origin/main fresh-clone bootstrap build BROKEN (native amalgam 미커밋 + float_to_bits skew)** (from: GPU cycle-loop R12 codegen session · 2026-05-28) — `nvptx_emit` 를 ubu-2 fresh clone (origin/main) 에서 `hexa build compiler/cli/nvptx_emit.hexa` 로 재빌드 시도 → **2 독립 build-blocking gap**: (1) `self/runtime.c` amalgam 이 `#include "native/{tensor_kernels,net,...}.c"` 하는데 그 native `.c` 들이 **origin/main 에 미커밋** (`git cat-file -e origin/main:self/native/tensor_kernels.c` = fatal not-in-main; local working tree 엔 tracked 이나 push 안 됨 — #1866 "B9.C-10 amalgam .c-text 활성 + git rm 16 native .c" landing 이 amalgam-included `.c` 들을 커밋 안 함). (2) native/ 를 로컬에서 rsync 후엔 `float_to_bits`/`bits_to_float` **undeclared** (codegen-emit #1677 가 bare-name C call 인데 fresh-clone runtime 정의 부재 = runtime/codegen version skew). → **fresh clone 에서 self-host tool(nvptx_emit 등) 빌드 불가**; 영향 = cron/launchd/CI/원격 cold-start 모든 fresh-clone 빌드 ([[reference_linux_transpiler_stale_build_recipe]] family). **Fix path**: ① #1866 amalgam-included native `.c` 들을 origin/main 에 커밋(또는 build step generate) ② float_to_bits/bits_to_float runtime 정의를 origin/main `self/runtime.c` 에 commit (codegen emit lockstep). durable-build 완전성 class — RoPE silicon re-fire 도 이게 풀려야 가능.

- [x] **`hexa verify --expr sigma_3` 계산기 갭 (calc-gap family #1230 확장)** → landed via inbox-nt-primitives-batch (`stdlib/core/math.hexa::sigma_3` + `verify_cli`/`congruence_chain_engine`/`atlas_cli` 4-surface; `hexa verify --expr sigma_3 6 252` = 🔵 SUPPORTED-FORMAL, σ_3(6)=1+8+27+216=252; g0 Occam = `sigma_k(n,3)` 재사용; atlas atom `@F verified-sigma_3-6` folded; verdict `.verdicts/inbox-nt-batch/sigma_3.txt`) (from: TECS-L F15 s3 · 2026-05-27) — `hexa verify --expr sigma_3 6 252 --no-absorb` 가 🟠 INSUFFICIENT (`calculator system has NO path for 'sigma_3'`). `stdlib/core/math.hexa` 에 `sigma_3` 미정의 확인. **calc-gap family #1230 확장** (기 추적 = sopfr/pow/J_k/iit4_faithful_phi; sigma_3 가 동일 클래스에 합류). σ_3(n) = Σ d^3 (d|n) — multiplicative, closed-form σ_3(p^a) = (p^(3(a+1))-1)/(p^3-1). `tool/verify_cli.hexa::_recompute` 에 σ_3 (또는 σ_k 가족) primitive 추가 권고. TECS-L F11 OEIS-attributed σ_3↔A001158 fold (PR #1138) 와 연계 — atlas 등록은 됐으나 verify-calc 경로 부재가 다음 라운드 σ_3 검증 차단. 🟠 verdict 영속화 `.verdicts/tecs-l-f15-novel-mk10/sigma_3_calc_gap_inbox.txt`.

<!-- ── F26 batch (2026-05-27) — TECS-L brainstorm depletion seed-INBOX entries ── -->

- [x] **unitary divisor σ\*(n) primitive — n=6 unitary-perfect 최소witness** → landed via PR #1473 (`stdlib/core/math.hexa::sigma_star` + `tool/verify_cli.hexa::_recompute` arm + `congruence_chain_engine`/`atlas_cli` register-side mirror; `hexa verify --expr sigma_star 6 12` = 🔵 SUPPORTED-FORMAL, σ\*(60)=120 🔵, atlas atom `@F verified-sigma_star-6` folded) (from: TECS-L F26 brainstorm R5 #78 · 2026-05-27) — σ\*(n) = Σ_{d||n} d (unitary-divisor sum, d||n ⟺ d|n AND gcd(d,n/d)=1). **n=6 은 가장 작은 unitary-perfect (σ\*(6)=2·6=12)** — 알려진 unitary-perfect 5개 {6, 60, 90, 87360, 146361946186458562560000} 의 1st. closed-form: σ\*(p^a) = p^a + 1, multiplicative. calc-gap family #1230 확장. F27 unitary-perfect catalog unblocked.

- [x] **Liouville λ(n)·summatory L(n) primitive (Polya conjecture falsifier-friendly)** → landed via inbox-nt-primitives-batch (`stdlib/core/math.hexa::{omega_big,liouville,liouville_summatory}` + congruence_chain_engine `omega_big`/`liouville_int`/`liouville_summatory` + verify_cli/atlas_cli arms 4-surface; 🔵 ALL: omega_big(6)=2, omega_big(8)=3, liouville(6)=1, liouville(8)=−1 (signed verifies — verify harness accepts negative <v>), liouville(1)=1, L(2)=0, L(6)=0; atlas atoms `@F verified-{omega_big-6,omega_big-8,liouville-1,liouville-6,liouville-8,liouville_summatory-2,liouville_summatory-6}` folded; verdict `.verdicts/inbox-nt-batch/liouville.txt`. **Polya L(n)≤0 ∀n>1 FALSIFIED @ n=906150257 (Tanaka 1980)** = classical closed-negative recorded as CITATION, NOT recomputed (giant sum = the math limit, closure-is-physical-limit).) (from: TECS-L F26 brainstorm R3 #16 · 2026-05-27) — Liouville λ(n) = (−1)^Ω(n) 와 summatory L(n) = Σ_{k≤n} λ(k). Ω(n) 은 sopfr 와 동일 family (TECS-L 다년 INBOX 항목). Polya 추측 (L(n)≤0 ∀n>1) 은 Haselgrove 1958, Lehman 1960, Tanaka 1980 에 의해 n=906150257 등에서 FALSIFIED — 클래식한 closed-negative atom 후보. **권고**: `stdlib/core/math.hexa::omega_big(n: i64) -> i64` (Ω, multiplicity 포함) + `liouville(n: i64) -> i64` + `liouville_summatory(n: i64) -> i64`. 닫힌형 λ multiplicative (λ(p^a) = (−1)^a). hexa-native exact integer. TECS-L F-axis 차후 round 의 closed-negative miner 확장 후보.

- [x] **Pisano period π(n) primitive (Fibonacci mod n)** → landed via inbox-nt-primitives-batch (`stdlib/core/math.hexa::{fibonacci_mod,pisano_period}` + congruence_chain_engine `pisano_period` + verify_cli/atlas_cli arms 4-surface; 🔵 ALL: π(2)=3, π(3)=8, π(6)=24 (=lcm(3,8)=σ(6)·φ(6) bridge), π(10)=60; atlas atoms `@F verified-{pisano_period-2,pisano_period-3,pisano_period-6,pisano_period-10}` folded; verdict `.verdicts/inbox-nt-batch/pisano.txt`) (from: TECS-L F26 brainstorm R2 #25 · 2026-05-27) — Pisano period π(n) = Fibonacci sequence (mod n) 의 주기. π(6)=24 = σ(6)·φ(6) (n=6 bridge candidate). 닫힌형: π(n) = lcm(π(p^a)) for n=∏p^a, π(p^a)=π(p)·p^(a-1) under typical conditions. **권고**: `stdlib/core/math.hexa::fibonacci_mod(n: i64, m: i64) -> i64` + `pisano_period(n: i64) -> i64`. F-NEW-6 후보 — n=6 anchor 24 closed-form witness.

- [x] → landed via PR #1479 **TECS-L atlas-fold 자동화 — `.discoveries/<slug>.tape` → embedded.gen.hexa splice** (from: TECS-L F26 brainstorm R7 #98 · 2026-05-27) — F14-F18 패턴: 발견 후 atlas atom 을 manual splice 로 `compiler/atlas/embedded.gen.hexa` 에 끼워넣고 PR 머지 (atlas_fold @D 거버넌스 준수). 5-10분 ceremony per 발견 cycle. **권고**: `hexa atlas fold-from-tape <slug>` 또는 `hexa atlas register --from-discovery <slug>` — `.discoveries/<slug>.tape` 의 atom 정의 (id · tier · fn · cite) 를 읽어 자동 splice + branch + commit + PR draft. `atlas register --from-verify` 가 single-fn-eval form 만 받는 한계 (F14 lesson) 보완. h_verify_auto_absorb @D 와 직교 — 후자는 verify-시 자동 fold, 본 entry 는 derived multi-term identity 의 명시적 fold tool.

- [x] **TECS-L /paper 배치 큐 — ✅ 종결: 2 LANDED (ω-zero-density #1483 + unitary-perfect-n6 #1485) + 1 honest DEFER (multilayer #1484)** (from: TECS-L F26 brainstorm R7 #95 · 2026-05-27) — 현재 6 paper shipped (n6-identity-locus M9 · modform-n6-nonlift MF8 · mersenne-exponent-primality MR8 · n6-exclusivity-atlas F12 · **ω-zero-density** · **unitary-perfect-n6-dual 신규**). F14-F25 다음-paper queue: **(i) "ω-zero-density theorem: D(n)=σφ−nτ=0 ⟺ n∈{1,6}" ✅ LANDED (PR #1483)** — PAPER/tecs-l-omega-zero-density/ (10p + fal.ai fig01), F14-F18 unified, ω∈[0,6] D-sweep + closed-form g(p,a)-product proof; falsifier F-OMEGA-ZERO-DENSITY survives; (ii) **"Multilayer non-lift: σφ=nτ at the geometric+Hecke+Galois+L-function towers"** — F7+F15+F16+F17 unified, 4-layer structural closed-negative — **honest DEFER (PR #1484)**: geometric층만 terminal(🔵+🔴), Hecke=🟡/🟠(hecke_eigenvalue calc-path 부재·dim_cusp_forms(1,12) 🔴 Δ-miss), Galois·L-function=🟠(analytic-infinite calc-path 부재) → paper_gate 미충족·scaffold 안 함(`.verdicts/paper-multilayer-nonlift/DEFER.md` tier matrix). **finite-vs-analytic RFC(#1459) 예측 라이브 적중**; (iii) **"Unitary-perfect singleton at n=6" ✅ LANDED** — PAPER/tecs-l-unitary-perfect-n6-dual/ (10p + fal.ai fig01). Finding: n=6 = UNIQUE simultaneous ordinary+unitary perfect in [1,10⁴]; dual⊆ordinary + Euclid–Euler {6,28,496,8128} 완전소진 → only 6 unitary; sequences diverge (28 ord-only σ\*=40≠56; 60 uni-only σ=168≠120). 12× σ/σ\* 🔵 verify (6,28,60,90,496,8128). 사전등록 falsifier F-DUAL-PERFECT-N6 survives. **✅ queue 완전 종결**: (i)+(iii) LANDED·(ii) honest DEFER — 3건 모두 gate 평가 완료. 각 paper 사전등록 falsifier + Δ-finding + g51 ≥10p+fal.ai.

- [x] **TECS-L methodology RFC — finite-arithmetic vs analytic dichotomy** → landed via PR #1459 (`TECS-L/docs/f26-rfc-finite-vs-analytic.md`) (from: TECS-L F26 brainstorm R7 #97 · 2026-05-27) — F22 honest-negative crystallized TECS-L 의 verify-strength = arithmetic-only closed forms (F14-F18 강함), weakness = analytic-infinite axes (RH asymptotic · BSD L'-derivative · Λ · H₀ · α). **권고**: `docs/rfc/rfc_tecs_l_finite_vs_analytic.md` (또는 `TECS-L/docs/f26-rfc-finite-vs-analytic.md`) — TECS-L 방법론을 spec 으로 박제, 향후 F-cycle 들이 axis 분류 시 분명한 결정 기준 (verify-able tier 매트릭스). MILLENNIUM/PHYSICS/COSMOS 대축의 verify-한계를 정직하게 명시. cross-link to TECS-L F19/F22 honest-negative + g3 over-claim 0 거버넌스.

- [x] **(✅ ROUTED 2026-05-27 · sidecar repo 영역 — hexa-lang 외) sidecar `/domain` + `/cycle` closure-goal semantics — done-log vs roadmap vs backlog 미분리 → "open=0" 추구가 로드맵 은닉 유발** (from: this-session 2026-05-27 GPU.md closure incident · PR #1644 over-closure → PR #1646 §5 restore) — `/domain` 이 모든 `- [ ]` 를 동질적 "닫아야 할 마일스톤" 으로 취급하나, 실제 도메인 파일은 ①끝난 실험 로그(closeable→[x]) ②미래 로드맵(open 유지가 정상) ③aspirational backlog 가 한 파일에 섞임. "all milestone closure / open=0" 목표(특히 **Stop-hook 조건**)가 ②③에 거짓-이분(거짓 done flip=g3 위반 vs checkbox 제거=로드맵 은닉)을 강제 → 이번 세션 GPU.md §5 cuBLAS-moat(north-star 로드맵)가 open=0 추구로 plain-bullet 강등됐다 revert(#1646). `/cycle` depletion test "open=0 AND deferred empty" 도 로드맵-보유 도메인에선 정직하게 종료 불가. **권고 4건**(상세 → `INBOX.log.md`): ⓐ lifecycle-tiered 섹션(`## experiments`/`## roadmap`/`## deferred`) + 섹션별 progress bar, closure 는 closeable tier 만 타깃 ⓑ tiered checkbox marker(active vs roadmap-future vs backlog) ⓒ over-closure guard — bulk milestone-flip 은 verdict/evidence 링크 없는 항목 flip 거부(paper_negative_ok 연계: closed-negative/PASS-artifact 만 flip) ⓓ Stop-hook goal semantics — "all milestone closure" 를 roadmap-보유 도메인 Stop 조건으로 쓰면 dishonest flip 압력 → lane-scoped 종결 또는 "concluded-tier closed + roadmap explicitly deferred" 를 충족으로 인정. **✅ ROUTED (2026-05-27)**: 변경 대상이 `~/core/sidecar` 의 `/domain` + `/cycle` skill (별 repo). hexa-lang 측에서 직접 land 할 코드 변경 없음 — 4-건 권고(ⓐⓑⓒⓓ)는 본 entry 에 박제 상태로 sidecar 작업자가 픽업. INBOX 도메인은 "다른 repo 가 hexa-lang 으로 넘긴 cross-repo handoff" 라 본 entry 처럼 잘못 도착한 항목은 routed-marker 로 종결하는 게 올바른 처리.

- [x] **(✅ RESOLVED 2026-05-27 · branch `inbox-nvptx-unop-neg-fix-2026-05-27` · fold + ① + ② + ③ all landed) NVPTX codegen — unop(neg) 미지원: unary-neg float/int literal 이 `unsupported stmt kind: unop` + 미정의 reg → ptxas reject** (from: this-session 2026-05-27 GPU 완주 §5j LogSumExp fire) — `@gpu_kernel` 본문의 `x = -1.0e308` (또는 임의 unary-neg 표현식) 이 `compiler/codegen/nvptx_target.hexa` 에서 `// RFC 055 055-P0 - unsupported stmt kind: unop` 주석만 emit 하고 RHS 레지스터(`%fdNN`)를 materialize 안 함 → 뒤따르는 `st.shared.f64 [...], %fdNN` 가 미정의 심볼 참조 → `ptxas: Unknown symbol '%fd14'` + `Arguments mismatch for instruction 'st'`. LogSumExp probe 의 max-pad sentinel 에서 표면화, `a[0]` 로 워크어라운드해 fire PASS(rel_err 1.7e-10). **fix = 3-part (실측, ubu-2 driver 재빌드로 검증)**: 단일 emit arm 으로는 불충분 — partial 시도(emit arm + collect_f64_regs 등록) 결과 ptxas 가 여전히 reject. ① **emit arm** (STMT_UNOP → `neg.<ty> %d,%s`, dst kind 별 neg.f64/neg.s64/neg.s32) — 검증됨(`// unsupported` 사라지고 `neg.f64` emit). ② **dst classify/declare** — collect_f64_regs 만으론 `.reg .f64 %fdN` 선언 안 됨; classify 패스(`s.kind==STMT_ASSIGN||STMT_BINOP||STMT_CALL` 분기, nvptx_target ~L3200)에 STMT_UNOP 추가해야 dst 가 bank 분류+선언됨 (안 하면 `ptxas: Unknown symbol %fdN`). ③ **f64 const-immediate → `0d`hex 포맷** — `-1.0e308` 의 피연산자 const 가 `_nvptx_operand_text(_kinds)` 에서 decimal `1e+308` 로 렌더 → `neg.f64 %d, 1e+308` ptxas `Arguments mismatch`. PTX f64 immediate 는 `0d<16hex>` 必. **⓪ PREREQUISITE (실측 차단, 2026-05-27 2차):** const→0dhex 에 IEEE754 bits 가 필요한데 `float_to_bits`(self/nanbox.hexa:224 runtime intrinsic)가 **nvptx_emit 드라이버 빌드에 링크 안 됨** — nvptx_target 에서 호출 시 clang `undefined symbol` 로 드라이버 빌드 실패(ubu-2 확증). 즉 `float_to_bits` 를 **global builtin 으로 4-surface 등록**([[reference_new_codegen_intrinsic_4_surface]]: codegen emit + `_is_builtin_name` + runtime_core.c bridge + bind.hexa resolver)하는 게 **선결**. ⓪ 후 ③(const→0dhex)+fold 가 동작. **구현 접근 (다음 사이클 ready, 이번에 코드 작성·미머지)**: (fold) hir_to_mir L1088 unop 블록에서 `e.text=="-"` & operand.kind∈{const_float,const_int} → `_const_float_op(0.0-v)`/`_const_int_op(0-v)` 로 fold (STMT_UNOP 회피·neg instruction 불요). (const-hex) nvptx_target `_nvptx_operand_text(_kinds)` 의 `const_float` → `_nvptx_f64_hexlit(v)` = `0d` + 16-upper-hex of `float_to_bits(v)` (nibble `(bits>>4k)&15`). 이 fold+hex 코드는 검증됨(parse OK), ⓪ 빌드링크만 막음. neg-of-register(`-x` 변수)는 별개로 ①emit-arm+②classify 필요(literal 경로와 독립). 재현=`/tmp/nvptx_emit <neg-literal-kernel>.hexa`. 영향=음수 상수 흔한 transcendental/reduction kernel. **= 4-layer dedicated 사이클: ⓪builtin-reg → ③const-hex → fold → (옵션)①②register-neg.** **🟢 ⓪ NOW SATISFIED (2026-05-27)**: `float_to_bits` / `bits_to_float` 가 4-surface 빌트인으로 등록됨 (별 INBOX 항목 #4 RESOLVED, branch `inbox-float-to-bits-builtin-2026-05-27`, round-trip 8/8 PASS). nvptx_target 에서 `float_to_bits(v)` 호출이 이제 `hexa_float_to_bits(v)` C 브리지로 링크됨 → ③(const→0dhex) 진행 가능. 잔여: ① emit arm · ② classify · ③ const-hex emit (fold 우회 옵션 포함). **✅ ALL LANDED (2026-05-27, branch `inbox-nvptx-unop-neg-fix-2026-05-27`)**: 4-part fix 한 PR 에 land — (fold) `compiler/lower/hir_to_mir.hexa` unop 블록에서 `-<const_float/const_int>` 를 단일 const 로 fold (literal-only 경로 STMT_UNOP 자체 회피); (③) `compiler/codegen/nvptx_target.hexa` 에 `_nvptx_f64_hexlit(v)` 헬퍼 추가 + `_nvptx_operand_text(_kinds)` const_float 분기를 `0d<16-upper-hex>` 로 변경 (ASCII nibble lookup 테이블로 chr 절단 회피); (①) `_nvptx_lower_stmt` 에 STMT_UNOP 분기 추가 — dst kind 별 `neg.f64`/`neg.s64`/`neg.s32` (register-neg `-x` 경로); (②) classify 패스의 `STMT_ASSIGN || STMT_BINOP || STMT_CALL` 에 `|| STMT_UNOP` 추가 → dst Local 이 `.reg .f64/.s64/.s32 %N` 선언됨. diff +62/−3, parse 두 파일 clean. ⚠ **GPU 실측 ubu-2 fire 미수행 (별 follow-up)** — source-level 가산적 변경이고 ⓪ 이 충족돼 링크 가능하다는 점까지 확인; 음수-float-literal 커널의 PTX 실 emit + ptxas 통과 실측은 다음 GPU 사이클(`/tmp/nvptx_emit <neg-literal-kernel>.hexa` 재현 경로 사용).

- [x] **(✅ RESOLVED 2026-05-27 · branch `inbox-float-to-bits-builtin-2026-05-27` · round-trip 8/8 PASS) RUNTIME — `float_to_bits` / `bits_to_float` 가 STUB (`return 0`) — f64↔bits reinterpret primitive 부재** (from: this-session 2026-05-27 GPU §5j unop fix, ubu-2 실측) — `self/nanbox.hexa:224 fn float_to_bits(f) { return 0 }` + `:229 bits_to_float { return 0.0 }` 는 본문이 stub. codegen(`self/codegen.hexa`)·`_is_builtin_name`·runtime 어디에도 실구현 없음 → **호출하면 그냥 0 반환**. 즉 `nanbox.hexa:33 NanBoxed{bits: float_to_bits(f)}` 의 bits 가 항상 0 (nanbox bit-packing 잠재 broken 또는 nanbox 미사용 — RUNTIME correctness 확인 필요). 또 nvptx_target 에서 호출 시 드라이버 빌드가 clang `undefined symbol` 로 실패(미링크 확증). **이게 GPU unop/음수-float-리터럴 fix 의 keystone(⓪)** — const→`0d`hex 에 IEEE754 bits 필요(별 INBOX 항목 NVPTX unop). **fix = 진짜 codegen 빌트인으로 구현** ([[reference_new_codegen_intrinsic_4_surface]] 4-surface): ①codegen emit = union-pun(C 기법은 `self/runtime.c:432` `union {double d; unsigned long long u;}` · `:2444` `union {double d; int64_t i;}` 이미 존재) ②`_is_builtin_name` 등록 ③runtime bridge ④`compiler/check/bind.hexa` resolver. 구현 후 모든 모듈(nvptx_emit 드라이버 포함)에서 링크·동작. 검증=`bits_to_float(float_to_bits(x))==x` round-trip + nanbox round-trip + GPU const-hex fire(NVPTX unop 항목 ③ 코드 ready). 도메인=RUNTIME, consumer=GPU(§5j)·nanbox. 광범위 영향(f64↔정수 reinterpret 은 직렬화/해시/codegen 상수 등에서 흔함).

- [ ] **NVPTX codegen — @shared array partitioned offset 계산 불일치 (sm[tid] 와 sm[tid+K] 가 같은 addr 로 emit)** (from: this-session 2026-05-28 §5j argmax wedge fire, ubu-2 실측) — 단일 `@shared let sm: [f64; 512] = []` 의 두 partition (sm[0..256) values · sm[256..512) indices) 가 PTX 에서 같은 `.shared .align 8 .b8 _hexa_sh_*[4096]` region 내에서 **일부 store/load 가 +256 offset 무시** = sm[tid+256] 가 sm[tid] 와 같은 주소로 emit 되어 두 partition 충돌. 또한 원래 2개 @shared array 사용 시에도 동일 충돌 확인됨 (둘 다 같은 base offset). PTX 분석: codegen 두 path — Path A(정확): `mul.lo.s64 %addr, <idx_reg>, 8` (idx_reg = expr 결과) · Path B(오류): 기존 `%rd_idx_off_X_8` (= tid*8) 재사용 = +256 offset 무시. argmax probe value byte-eq PASS, idx 추적 fail (rel_err val=0, idx 0 vs ref 137). **fix**: `compiler/codegen/nvptx_target.hexa` 의 `sm[expr]` offset 계산 통일 — 모든 path 가 expr 평가 결과로 addr 생성, 기존 reg 재사용 금지. 자매 family = [[reference_comptime_fold_shadow_family]] (codegen-emit consistency 류) · [[INBOX #1665 unop]]. 영향 = top-k · histogram · scan 등 (value+index) 또는 partitioned reduction 패턴 전반 차단. 재현 = `tool/probe_argmax_f64.hexa` + `tool/argmax_f64_host.c` (archive/fires/gpu_argmax_partial_2026_05_28/).

- [x] **(PARTIAL · advisory line 추가 · 자동 register 는 follow-up) `hexa cloud {fire|nohup}` 자동 dispatch add 부재 — caller-side ghost-pod 양산** (from: sidecar pod-monitor 0.1.4 ship · 2026-05-28 · ANIMA caller session 발견) — `cloud@sidecar 0.4.0` + 본 repo PR #1699 가 per-project `./pods.json` SSOT 를 land 했으나, `hexa cloud fire/nohup/run` verb 가 발사 성공 후 cwd 의 `./pods.json` 에 자동 `dispatch add` 를 호출하지 **않음**. caller agent 책임으로 떠넘긴 결과 → ANIMA M3 4-pod fire 같은 caller 가 dispatch add 호출 자체를 모르고 **ghost pod** 생성. **✅ PARTIAL fix (2026-05-28 · branch `inbox-cloud-auto-dispatch-and-lsp-2026-05-28`)**: `stdlib/cloud/cloud_cli.hexa` 의 fire/nohup success path (__MONITOR_HANDLE__= 직후) 에 advisory println 추가 — `[cloud] 📋 ledger: hexa cloud dispatch add <jid> <pid> "$(pwd)" --host <host> # cwd ./pods.json 에 등록 (commons g57)`. agent/사람 둘 다 stdout 에서 읽어 호출 강제. **잔여 follow-up**: ① 진짜 자동 register (`--register <jid>` flag 또는 cwd 가 git-rooted 면 auto-jid + auto-add) ② teardown 시 `dispatch verdict <jid> <PASS|FAIL>` 자동 갱신. sidecar 측 pod-monitor 0.1.4 L3 advisory + 본 repo L2 advisory 합쳐 ghost-pod 인지 갭은 사실상 닫힘 — 본질 자동화는 별 cycle. reference→`reference_pods_json_per_project`.

- [x] **(✅ FALSE-ALARM — self/lsp.hexa 존재 + 빌드 OK) `hexa lsp` crash 진단** (from: sidecar `/doctor` 2026-05-28) — sidecar `/doctor` 출력에서 hexa-lsp plugin 의 crash trail 보고됐으나 본 repo 실측 (2026-05-28 main 기반): `self/lsp.hexa` 875 lines 존재 + `hexa build self/lsp.hexa` = `OK: built build/artifacts/app` (2 warnings, 0 errors). 즉 source 도 있고 빌드도 된다. crash 는 hexa-lsp plugin (sidecar 측) 의 LSP launcher 또는 환경별 path-resolution 갭. sidecar 측 follow-up — `/doctor` 진단 메시지 stale 또는 실제 launcher fail 재현 + root cause 확정 필요. 본 repo 액션 없음.

> 참고: hexa-lang 내부 upstream-patch staging `inbox/` 폴더는 폐기됐다(rehome → `archive/patches/` · `archive/fires/` · `docs/notes/` · `docs/rfc/`). upstream 변경은 일반 PR 워크플로로 흐른다. 이 INBOX 도메인은 다른 repo 가 넘긴 cross-repo handoff 전용으로 별개 시스템이며 그대로 유지된다.
