# cloud gpu openmm fep provisioning confirmed

> Materialized from cross-repo handoff `5e6545d0` (from demiurge, 1780781583) per demiurge @D d8
> (vast/cloud finding -> hexa-lang/inbox/patches so `hexa cloud` absorbs upstream).

hexa cloud GPU-pod OpenMM/FEP provisioning — FINAL CONFIRMED (SENOLYX R12, supersedes 535ab138). VERIFIED end-to-end 2026-06-07 on vast pod 39776913 (RTX_3090, driver 580.126.09=CUDA13.0): cuda-version pin FIXED PTX-222 and production ran GPU-accelerated (344W/67% util). CONFIRMED ROOT CAUSE: micromamba/conda-forge default-installs openmm built against the NEWEST cuda-version (here 13.3); its PTX exceeds the host driver JIT max -> CUDA_ERROR_UNSUPPORTED_PTX_VERSION (222) at mm.Context(CUDA). Driver recency does NOT help — the openmm BUILD cuda-version must be <= driver CUDA. CONFIRMED FIX: micromamba install -p <env> -c conda-forge cuda-version=12.4 openmm (pulled openmm 8.2.0 CUDA-12.4 PTX; SMOKE PASSED, GPU ramped 344W/67%). Cap at 12.4 as safe default. THREE more validated gotchas: (1) vast/runpod base images may LACK bzip2 (needed to untar micromamba .tar.bz2) -> apt-get install -y bzip2 first, else tar bzip2 Cannot exec. (2) low-reliability vast hosts go GONE mid-provision -> filter verified=true reliability>0.985. (3) runpod RTX_3090/4090 frequently out-of-stock -> vast verified is the reliable fallback. PROPOSED hexa cloud changes: (a) on env-bootstrap read pod nvidia-smi driver CUDA and inject cuda-version<=detected (cap 12.4) into any openmm/cudatoolkit/jaxlib-cuda install; (b) ensure bzip2 present before micromamba untar; (c) hexa cloud preflight --kind openmm = 2-atom CUDA Context smoke failing fast with this remediation; (d) default vast rent filter verified=true reliability>0.985; (e) doc 222 remediation in cloud-guard trailer. Full recipe in demiurge exports/SENOLYX/round12-rbfe/pod_bootstrap_17aag.sh + memory vast-b4-cuda-ptx-resolved.

---
source: sidecar handoff `5e6545d0` (demiurge -> hexa-lang) · status: open (awaiting hexa-lang absorb)
