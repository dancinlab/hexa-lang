# incoming patch: wilson-pi-port-6-gap-prereq

> **id**: `wilson-pi-port-6-gap-prereq` · **opened**: 2026-05-10 · **status**: `spec`
> **trees**: `self/` + `compiler/`
> **source**: `~/core/wilson/docs/hexa-lang-gap-audit.md` (commit `dancinlab/wilson`)
> **why**: wilson core = pi-mono `{agent→core, ai, tui, coding-agent}` 의 hexa-native 재포팅. hive 가 그걸 하다 `core`/`ai` 핵심 모듈을 BLOCKED 낸 게 hexa-lang 부족 때문. wilson 착수 전 hexa-lang 이 채워야 할 갭.
>
> **갱신 2026-05-10**: (1) hexa-lang = 컴파일 언어 전환 (인터프리터 retire) → **G2 의 "interp parity" 하위갭 소멸** (`exec_stream_async` 가 AOT-only 인 게 더는 갭 아님 — 컴파일 언어가 그냥 갖는다). G2 = "async 실행 의미론 완성 + codegen 통합" 만. (2) wilson 의 모든 것(core+모든 plugin+모든 도구)은 hexa-lang 으로 — TS 0. (3) plugin 아키텍처(`wilson/docs/plugin-interfaces-comms-aot-brainstorm.md` Part C 개정판)가 강제하는 신규 갭 후보 **G7/G8** 추가.
>
> **갱신 2026-05-13 — G6 LANDED**: `stdlib/jsonl_pool.hexa` v0.1.0 + `stdlib/channel.hexa` v0.2.0 구현·테스트 완료 (이 세션). `stdlib/test/test_jsonl_pool.hexa` 22/22 PASS (interp, macOS arm64). `.roadmap.stdlib` 의 `S-NEW-JSONL-POOL` = CLOSED. (channel.ai.md C3 노트 갱신은 follow-up.) wilson 쪽 후속 = `plugins/swarm/main.hexa` de-STUB (`use "stdlib/jsonl_pool"`) + `wilson build --bundle cell` (core/dispatch_table.hexa regen 의존). 나머지 갭(G4 jsonschema / G5 fs atomic-append / G7 dlopen / G8 incremental link) 미시작.

---

## 갭 목록 (상세는 wilson 문서)

| # | 갭 | 계층 | 현재 | 무게 | roadmap 초안 위치 |
|---|---|---|---|---|---|
| **G1** | payload enum 완성 (construction + type-binding + codegen extraction + 다중필드) | 언어/typechecker/codegen | A1-A3 land, A4 partial, A5 pending — `patches/rfc020-enum-payload-variants.md` | ★★★ | `.roadmap.codegen` CG-RFC020-A4-A5 / B1 / B2 / C1 (rfc020 patch 파일) |
| **G2** | async 실행 의미론 완성 (`async fn`/`await`/`yield`/`select`/async-generator) + codegen 통합 | 런타임/codegen/typecheck | `async fn`/`await`/`yield`/`spawn`/`select` 파싱 OK, `self/async_runtime.hexa`(cooperative scheduler, Rust 포팅) 존재, `stdlib/net/http_server` 가 live 사용 — 실행 의미론 미완 ("spawn 테스트는 syntax validation only"). ~~interp parity~~ → 인터프리터 retire 하니 불필요, codegen 통합만. | ★★★ | `.roadmap.runtime` RT-ASYNC-MODEL — 아래 (interp dispatch 항목 제거) |
| **G3** | cancellation handle (AbortSignal 등가물) — exec/http_sse/tool 중단 | 런타임/stdlib | `async_runtime` 에 cooperative `task_group_cancel`/`scheduler_shutdown` — exec/http 에 미연결 | ★★ | `.roadmap.runtime` RT-CANCEL-TOKEN — 아래 |
| **G4** | `stdlib/jsonschema.hexa` — JSON Schema draft-07 subset 런타임 validator (tool-arg 검증) | stdlib | 없음 (`stdlib/json`, `json_object` 는 traverse 만). 순수 stdlib — 저위험, 바로 가능 | ★ | `.roadmap.stdlib` S-NEW-JSONSCHEMA — 아래 |
| **G5** | `self/stdlib/fs.hexa` 보강 — atomic O_APPEND + rotation + stat parity | stdlib/rt | `self/stdlib/fs::append_file` 있음 (atomic 보장 미확인), `stdlib/portable_fs::{file_size,mtime,now_ns,exists}`. 먼저 `self/stdlib/fs.hexa` 실독으로 갭 확정 | ★ | `.roadmap.stdlib` S-FS-ATOMIC-APPEND — 아래 |
| **G6** | `stdlib/jsonl_pool.hexa` + `stdlib/channel.hexa` backpressure — JSONL-demux 서브프로세스 풀 (swarm cell, out-of-proc plugin IPC) | stdlib | ✅ **DONE 2026-05-13** — `stdlib/jsonl_pool.hexa` v0.1.0 (pool_spawn/request/broadcast/send/recv_any/shutdown + pool_pids/pool_alive) + `stdlib/channel.hexa` v0.2.0 (`channel_send_sync` backpressure + `channel_recv_lines` no-loss drain); `test_jsonl_pool.hexa` 22/22 PASS, `test_channel.hexa` 21/21 회귀 무손상. | ★ | `.roadmap.stdlib` S-NEW-JSONL-POOL (CLOSED) — 아래 |
| **G7** (신규 후보) | `hexa_ld` 동적 링킹 / dlopen — in-proc plugin 을 재컴파일 없이 추가 (`.so`/`.dylib` 런타임 로드) | linker/runtime | 없음 — `hexa_ld v1.1` 은 static only (ELF64+Mach-O static). 없으면 wilson 의 in-proc plugin = 전부 `wilson build` 로 정적 흡수 강제 (plugin 켜고 끄기 = 재컴파일). | ★ (있으면 plugin UX ↑, 없어도 동작 가능) | `.roadmap.codegen` 또는 `.roadmap.runtime` — 신규 (RFC 필요) |
| **G8** (신규 후보) | `hexa_ld` incremental link — 변경된 plugin object 만 재링크 | linker | 미확인 — `hexa_ld v1.1` 의 incremental 지원 여부 불명. 없으면 `wilson build --with X` = 매번 full relink (core 포함 수십초~분) → wilson dev UX 저하. | ★ (dev UX) | `.roadmap.codegen` — 신규 |

**우선순위**: G1 ∥ G2 → G3, G4/G5 끼워넣기 (저위험), G6 (out-of-proc plugin IPC + swarm), G7/G8 (plugin UX — 없어도 wilson 동작 가능, 있으면 좋음). **wilson core 착수 게이트 = G1 + G2 (+G3) done.**

**hexa-lang 갭이 아닌 것** (wilson 쪽 / 그냥 큰 것): pi extension host API (coding-agent surface), `core/agent-session.ts` 3110 LoC (deps 풀리면 그냥 큼), TUI cli 서브커맨드 (`self/tui` 쓰면 됨), macOS Keychain (`security` shellout 한 줄), image-resize/mime (`photon-node` Rust/WASM — `sips`/`magick` shellout or c_ffi), multi-provider routing (provider SDK 는 `self/stdlib/{anthropic,bedrock,google}_sdk` 로 부분), BM25/MinHash (hive `packages/compression/hexa/*` 이미 hexa-native).

## `.roadmap.runtime` 엔트리 초안 (G2, G3 — paste-ready)

> chflags-locked 가능 — 직접 안 씀. 스키마 `hexa-lang/roadmap/2`.

```json
{"id": "RT-ASYNC-MODEL", "kind": "feature", "title": "async runtime model — async fn/await/yield 실행 의미론 spec + 구현 검증 + exec_stream interp parity", "rationale": "SPEC.md §Unresolved 에 'Async runtime model' 미결. self/async_runtime.hexa (cooperative green-thread, src/async_runtime.rs 포팅) + async/await/yield/select 키워드 lex/parse 완료 (parse_async_fn→AsyncFnDecl, parse_yield_stmt) + stdlib/net/http_server.hexa 가 live 사용 — 하지만 self/test_concurrency_patterns.hexa 의 spawn 테스트가 'syntax validation only — may fork execution'. wilson core 의 agent-loop.ts (async tool scheduler over async generator) + agent.ts (async generator over AssistantMessageEventStream) 가 강제. async_runtime.hexa 를 design-only → spec 으로 승격.", "acceptance": "(a) RFC rfc_0XX_async_runtime_model.md — Future 슬롯 / await suspend point / scheduler 통합 / async generator (fn* + yield = lazy stream) / select (N-event first); (b) typecheck: async fn → Future<T>, await: Future<T>→T (self/type_checker.hexa); (c) codegen: await suspend/resume (self/native/hexa_cc.c, self/codegen_c2.hexa) + compiler/codegen/*; (d) exec_stream / exec_stream_async 의 인터프리터 dispatch (self/hexa_full.hexa) — 또는 'streaming exec = AOT-only' contract 명시; (e) self/test_async_runtime_e2e.hexa PASS — async fn round-trip + async generator over 100 items + select 3-way + spawn 진짜 concurrent (race-detect).", "files_modified": ["self/async_runtime.hexa", "self/type_checker.hexa", "self/native/hexa_cc.c", "self/hexa_full.hexa", "self/parser.hexa"], "files_added": ["proposals/rfc_0XX_async_runtime_model.md", "self/test_async_runtime_e2e.hexa"], "depends_on": [], "cross_link": ["inbox/patches/wilson-pi-port-6-gap-prereq.md", "wilson/docs/hexa-lang-gap-audit.md#g2", "self/_smoke_exec_stream_async.hexa"], "status": "PROPOSED", "since": "2026-05-10"}
{"id": "RT-CANCEL-TOKEN", "kind": "feature", "title": "cancellation handle (AbortSignal 등가물) — exec/http_sse/tool 중단", "rationale": "wilson core 의 exec.ts/bash-executor.ts 가 AbortSignal 로 in-flight 명령 중단 (ESC-interrupt / tool timeout / agent cancel). async_runtime 에 cooperative task_group_cancel/scheduler_shutdown 은 있으나 exec_stream/http_sse 에 미연결.", "acceptance": "(a) CancelToken 타입 — cancel() + is_cancelled() (self/async_runtime.hexa 1급화); (b) exec_stream_async(cmd, cancel_token) — token on → child SIGTERM→SIGKILL + cleanup (self/runtime.c hexa_spawn_reap 근처); (c) http_sse_open(url, headers, cancel_token, timeout) — 진행 중 curl 중단 (stdlib/http_sse.hexa); (d) timeout = cancellation 통일 (wait_timer + task_group_cancel); (e) self/test_cancel_token.hexa PASS — 장기 sleep cmd 를 500ms 후 cancel → wall-clock < 1500ms + child reaped.", "files_modified": ["self/async_runtime.hexa", "self/runtime.c", "stdlib/http_sse.hexa", "stdlib/proc.hexa"], "files_added": ["self/test_cancel_token.hexa"], "depends_on": ["RT-ASYNC-MODEL"], "cross_link": ["inbox/patches/wilson-pi-port-6-gap-prereq.md", "wilson/docs/hexa-lang-gap-audit.md#g3"], "status": "PROPOSED", "since": "2026-05-10"}
```

## `.roadmap.stdlib` 엔트리 초안 (G4, G5, G6 — paste-ready)

> 현재 `.roadmap.stdlib` 는 전부 CLOSED — 아래는 신규 엔트리.

```json
{"id": "S-NEW-JSONSCHEMA", "kind": "feature", "title": "stdlib/jsonschema.hexa — JSON Schema draft-07 subset 런타임 validator", "rationale": "wilson core 의 tool-call argument 검증 (TS = typebox + zod-to-json-schema). hexa 엔 schema validator 없음 — stdlib/json_object 는 traverse 만.", "acceptance": "(a) stdlib/jsonschema.hexa exports jsonschema_validate(schema, value) -> {ok: bool, errors: [string]} + jsonschema_coerce(schema, value) -> value; subset = type/properties/required/items/enum/minimum/maximum/pattern/additionalProperties; (b) stdlib/test/test_jsonschema.hexa PASS — 12+ 케이스 (valid/invalid/coerce/nested object/array-of-object); (c) 순수 함수 (stdlib/json_object 위, 외부 dep 0).", "files_added": ["stdlib/jsonschema.hexa", "stdlib/test/test_jsonschema.hexa"], "depends_on": [], "cross_link": ["inbox/patches/wilson-pi-port-6-gap-prereq.md", "wilson/docs/hexa-lang-gap-audit.md#g4"], "status": "PROPOSED", "since": "2026-05-10"}
{"id": "S-FS-ATOMIC-APPEND", "kind": "feature", "title": "self/stdlib/fs.hexa 보강 — atomic O_APPEND + rotation + stat parity", "rationale": "wilson 의 audit ledger writer = append-only JSONL, atomic O_APPEND + size-rotation. memory storage = sync read/write/mkdir/stat/rm + MEMORY.md auto-rebuild. self/stdlib/fs::append_file 있으나 atomic-append (concurrent-writer 안전) 보장 미확인, rotation rename 헬퍼 없음. PRE: self/stdlib/fs.hexa 실독으로 갭 확정.", "acceptance": "(a) fs_append_atomic(path, line) — O_APPEND + single write(2) (POSIX atomic-append <= PIPE_BUF); (b) fs_rotate_if_over(path, max_bytes, suffix); (c) fs_stat(path) -> {size, mtime, is_dir, exists}; (d) fs_mkdir_p(path), fs_rm_rf(path); (e) self/test_fs_atomic.hexa PASS — 동시 append 10-writer no-interleave + rotation + stat round-trip.", "files_modified": ["self/stdlib/fs.hexa", "stdlib/portable_fs.hexa"], "files_added": ["self/test_fs_atomic.hexa"], "depends_on": [], "cross_link": ["inbox/patches/wilson-pi-port-6-gap-prereq.md", "wilson/docs/hexa-lang-gap-audit.md#g5"], "status": "PROPOSED", "since": "2026-05-10"}
{"id": "S-NEW-JSONL-POOL", "kind": "feature", "title": "stdlib/jsonl_pool.hexa — JSONL-demux 서브프로세스 풀 + channel backpressure", "rationale": "wilson swarm (P5) 의 cell.ts/supervisor.ts 등가물 = N개 `wilson --harness rpc` 자식 + JSONL stdin/stdout demux + request-id 매칭 풀. stdlib/proc(supervised spawn) + stdlib/channel(bidirectional FIFO + proc_spawn_with_channels) 조각은 있으나 풀 헬퍼 없음 + channel backpressure/EPIPE 미surface (channel.ai.md C3) + channel_recv 가 buffered <$fh> 라 여러 줄 버퍼 시 1줄만 반환·나머지 유실 (ping-pong 엔 안 물리나 request-stream/demux 엔 치명). inbox/patches/wilson-pi-port-6-gap-prereq.md G6.", "acceptance": "(a) stdlib/channel.hexa v0.2.0 — channel_send_sync(fd,msg) 동기 송신(backpressure=write(2) block, ordered, FIFO-gone→false=EPIPE-등가) + channel_recv_lines(fd,timeout_ms) unbuffered sysread→\\n split 전체 줄(유실 0); (b) stdlib/jsonl_pool.hexa exports pool_spawn(n,cmd_tmpl)→Pool + pool_request[_to] + pool_broadcast[_to] + pool_send + pool_recv_any + pool_shutdown + pool_size/pool_pids/pool_alive (per-cell line buffer + request-id 매칭 demux); (c) stdlib/test/test_jsonl_pool.hexa PASS — 4-cell echo pool + concurrent 20-request id-correct(out-of-order) + broadcast + shutdown reaps all.", "files_added": ["stdlib/jsonl_pool.hexa", "stdlib/test/test_jsonl_pool.hexa"], "files_modified": ["stdlib/channel.hexa"], "depends_on": ["RT-CANCEL-TOKEN"], "cross_link": ["inbox/patches/wilson-pi-port-6-gap-prereq.md", "wilson/docs/hexa-lang-gap-audit.md#g6"], "status": "CLOSED", "closed_at": "2026-05-13", "evidence": ["stdlib/jsonl_pool.hexa 작성 ~330 LoC: pool_spawn/pool_request/pool_request_to/pool_broadcast/pool_broadcast_to/pool_send/pool_recv_any/pool_shutdown/pool_size/pool_pids/pool_alive — 셀=channel_pair_open+proc_spawn_with_channels(child sees plain stdin/stdout), demux=cell['lines']/['lpos'] line buffer + channel_recv_lines 리필 ⇒ 줄 유실 0, request-id round-robin 매칭, junk/stale-id skip. cmd_tmpl 의 {i}→cell idx 치환. 단일스레드/동기(hexa exec 모델). 미구현(follow-up): stdlib/cancel 연결(depends_on RT-CANCEL-TOKEN), dead-cell 자동 재시작(현재 supervisor 가 pool_pids+pool_alive 로 직접).", "stdlib/channel.hexa v0.1.0→0.2.0 (additive, 기존 surface touch 0): + channel_send_sync (no-`&` 동기 송신 — kernel pipe 버퍼 full 시 write(2) block=backpressure, 후속 송신 대비 ordered; FIFO inode 없음→false=EPIPE-등가 명시 신호) + channel_recv_lines (can_read(timeout) 후 1회 sysread(1MB) 전체→`s/\\r//`→split(/\\n/) 전체 줄 반환; channel_recv 단독은 perl 버퍼드 <$fh> 라 N줄 버퍼 시 1줄만 반환·나머지 close 시 유실 — atomic-write 계약(줄≤PIPE_BUF)하에 channel_recv_lines 는 유실 0).", "stdlib/test/test_jsonl_pool.hexa 22/22 PASS (interp, macOS arm64, RESOURCE_LOCAL_HEXA=1 build/hexa_interp): 4-cell echo pool 스폰+전원 alive(pool_alive)+distinct pid / pool_request 단일 라운드트립(auto-mint id=1 에코, payload 에코, 호출자 map 불변) / 20 동시요청 — {\"id\":100+k,\"payload\":\"req-k\"} 라운드로빈 송신(pool_send→set id 반환) 후 pool_recv_any 20회 — 모든 id↔payload 짝 정확 + 20 distinct id 정확히 1회씩(out-of-order 재정렬 OK, 중복/미지 id 0) / pool_broadcast 4 응답·distinct id·payload 에코 / pool_shutdown→true + pool_size==0 + 전 자식 사망 + idempotent. test_channel.hexa 21/21 회귀 무손상; 5× 반복 무flake; /tmp hexa_chan_* 누수 0(shutdown 이 channel_close 로 FIFO unlink).", "echo cell = `while IFS= read -r line; do printf '%s\\n' \"$line\"; done` — request 줄에 id 가 이미 박혀 있으니 verbatim echo 가 곧 id-에코(셀-측 jq/sed 불요, quoting 이슈 0).", "남은: channel.ai.md C3 노트 갱신(이번 세션 channel.hexa 본문 @changelog 만 — follow-up); wilson plugins/swarm/main.hexa de-STUB 는 wilson 쪽 (`use \"stdlib/jsonl_pool\"` + `wilson build --bundle cell`=core/dispatch_table.hexa regen 의존)."], "since": "2026-05-13"}
```
