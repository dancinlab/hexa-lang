# flame determinism contract (HEXA-0POD byte-eq oracles)

> WIP skeleton — being filled by HEXA-0POD OP-14. Consolidates the CPU byte-eq
> oracle invariants that lock the flame CLMConvMoE training-step determinism
> contract. Sources: `.verdicts/hexa-0pod/F-OP*-*.txt`.

## the cross-cutting rule

## per-phase locked identities

## step-phase map

## what breaks the contract

## adding a new oracle
