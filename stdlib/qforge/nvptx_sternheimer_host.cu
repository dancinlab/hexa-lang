// stdlib/qforge/nvptx_sternheimer_host.cu — WIP placeholder (skeleton commit).
// GPU-parity harness for the qforge Sternheimer DFPT kernels. Filled in the
// following commit (see nvptx_sternheimer_kernel.hexa for the kernel contract).
#include <cuda.h>
int main(){ return 0; }
