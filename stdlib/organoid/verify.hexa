#!hexa strict
// organoid verify — marker positivity ratio (sample IHC quantification).
fn main() {
    let N_pos = 80.0
    let N_total = 100.0
    let positivity = N_pos / N_total
    println("organoid_verify (marker positivity · sample IHC):")
    println("  N_positive = " + str(N_pos) + " / N_total = " + str(N_total))
    println("  positivity_fraction = " + str(positivity))
    println("__HEXA_ORGANOID_VERIFY__ PASS gate=GATE_OPEN absorbed=false")
}
