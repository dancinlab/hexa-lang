#!hexa strict
// aerogel specify — max density target.
fn main() {
    let density_kg_m3 = 50.0
    println("aerogel_specify (max density target):")
    println("  density_kg_m3 = " + str(density_kg_m3))
    println("__HEXA_AEROGEL_SPECIFY__ PASS gate=GATE_OPEN absorbed=false")
}
