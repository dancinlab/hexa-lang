#!hexa strict
// stdlib/green-nh3/specify.hexa — `GREEN_NH3 + specify` producer scaffold (substrate=hexa).
// cellrun manufactures the manifest record (gate_default=OPEN,
// absorbed_default=false · g3) — this scaffold exits 0 to trigger it.
// Real domain compute lands as a follow-up per-verb engine upgrade.
fn main() {
    println("__HEXA_GREEN_NH3_SPECIFY__ PASS gate=GATE_OPEN absorbed=false")
}
