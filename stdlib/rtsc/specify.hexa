#!hexa strict
// rtsc specify — Tc target above RT.
fn main() {
    let Tc_target_K = 300.0
    println("rtsc_specify (Tc target above RT):")
    println("  Tc_target_K = " + str(Tc_target_K))
    println("__HEXA_RTSC_SPECIFY__ PASS gate=GATE_OPEN absorbed=false")
}
