#!hexa strict
// electrocat handoff — stack area target.
fn main() {
    let stack_m2 = 1.0
    println("electrocat_handoff (stack area target):")
    println("  stack_m2 = " + str(stack_m2))
    println("__HEXA_ELECTROCAT_HANDOFF__ PASS gate=GATE_OPEN absorbed=false")
}
