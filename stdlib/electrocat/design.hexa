#!hexa strict
// electrocat design — catalyst loading.
fn main() {
    let loading_mg_cm2 = 1.0
    println("electrocat_design (catalyst loading):")
    println("  loading_mg_cm2 = " + str(loading_mg_cm2))
    println("__HEXA_ELECTROCAT_DESIGN__ PASS gate=GATE_OPEN absorbed=false")
}
