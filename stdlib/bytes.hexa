// stdlib/bytes.hexa — F1 (2026-05-10) backward-compat shim.
//
// Module relocated to stdlib/core/bytes.hexa under Option C
// stdlib_core_split. Pure byte/uint conversions + IEEE-754 reinterpret
// helpers — no syscall, no platform assumption. The IEEE-754 bit-cast
// builtins are available in any hexa runtime (firmware reuses them).
//
// Sunset: removed after F4 absorptions complete.
// New canonical path: stdlib/core/bytes.hexa

import "stdlib/core/bytes.hexa"
