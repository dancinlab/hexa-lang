#!hexa strict
// rna-therapy verify — Reynolds 8-rule siRNA efficacy sum.
fn main() {
    let r1 = 1.0
    let r2 = 1.0
    let r3 = 1.0
    let r4 = 0.0
    let r5 = 0.0 - 1.0
    let r6 = 0.0
    let r7 = 1.0
    let r8 = 1.0
    let total = r1 + r2 + r3 + r4 + r5 + r6 + r7 + r8
    println("rna-therapy_verify (Reynolds 8-rule · sample siRNA):")
    println("  rule_sum = " + str(total) + " (favorable if >=6)")
    println("__HEXA_RNA_THERAPY_VERIFY__ PASS gate=GATE_OPEN absorbed=false")
}
