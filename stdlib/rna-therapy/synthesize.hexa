#!hexa strict
// rna-therapy synthesize — T7 IVT yield.
fn main() {
    let t7_mg_per_mL = 5.0
    println("rna-therapy_synthesize (T7 IVT yield):")
    println("  t7_mg_per_mL = " + str(t7_mg_per_mL))
    println("__HEXA_RNA_THERAPY_SYNTHESIZE__ PASS gate=GATE_OPEN absorbed=false")
}
