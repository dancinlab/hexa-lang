// stdlib/json_object.hexa — F1 (2026-05-10) backward-compat shim.
//
// Module relocated to stdlib/alloc/json_object.hexa. JSON read-side
// helpers — pure but allocates maps/arrays during parse; alloc tier.
//
// Sunset: removed after F4 absorptions complete.
// New canonical path: stdlib/alloc/json_object.hexa

import "stdlib/alloc/json_object.hexa"
