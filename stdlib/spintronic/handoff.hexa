#!hexa strict
// spintronic handoff — STT-MRAM density target.
fn main() {
    let mram_Gbit = 1.0
    println("spintronic_handoff (STT-MRAM density target):")
    println("  mram_Gbit = " + str(mram_Gbit))
    println("__HEXA_SPINTRONIC_HANDOFF__ PASS gate=GATE_OPEN absorbed=false")
}
