#!hexa strict
// gene-edit handoff — IND-enabling tox species.
fn main() {
    let ind_tox_n = 2.0
    println("gene-edit_handoff (IND-enabling tox species):")
    println("  ind_tox_n = " + str(ind_tox_n))
    println("__HEXA_GENE_EDIT_HANDOFF__ PASS gate=GATE_OPEN absorbed=false")
}
