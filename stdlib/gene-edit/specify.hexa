#!hexa strict
// gene-edit specify — edit efficiency target.
fn main() {
    let edit_eff_target = 0.50
    println("gene-edit_specify (edit efficiency target):")
    println("  edit_eff_target = " + str(edit_eff_target))
    println("__HEXA_GENE_EDIT_SPECIFY__ PASS gate=GATE_OPEN absorbed=false")
}
