#!hexa strict
// photoredox handoff — flow reactor residence time.
fn main() {
    let flow_min = 30.0
    println("photoredox_handoff (flow reactor residence time):")
    println("  flow_min = " + str(flow_min))
    println("__HEXA_PHOTOREDOX_HANDOFF__ PASS gate=GATE_OPEN absorbed=false")
}
