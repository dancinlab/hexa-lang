// ══════════════════════════════════════════════════════════
//
// @version 0.1.0
// @capabilities [pure, ffi]
//
// Hexa Optimizer Standard Library
// ══════════════════════════════════════════════════════════

// Optimizer step: use `opt_adamw_step` (stdlib/flame/optim_lib.hexa) — the real,
//   verified AdamW builtin (`adamw_step`, RFC 034). The legacy `adam`/`safe_update`
//   wraps here referenced the FALSIFIED `adam_step`/`grad_clip_norm` builtins (no
//   runtime impl — codegen emitted broken C) and were removed (OP-33c).
// LR schedule: use `opt_lr_warmup_cosine` (stdlib/flame/optim_lib.hexa) — deterministic d5_cos (F-OP33).

println("[optim loaded]")
