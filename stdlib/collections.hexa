// stdlib/collections.hexa — F1 (2026-05-10) backward-compat shim.
//
// Module relocated to stdlib/alloc/collections.hexa under Option C
// stdlib_core_split. Operates on heap-backed array/map values — qualifies
// as platform-agnostic alloc, NOT core. Firmware that opts into the
// embedded allocator (arena_v1 / bump_allocator) can still consume.
//
// Sunset: removed after F4 absorptions complete.
// New canonical path: stdlib/alloc/collections.hexa

import "stdlib/alloc/collections.hexa"
