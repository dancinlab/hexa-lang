#!hexa strict
// memristor design — multi-level states target.
fn main() {
    let levels = 10.0
    println("memristor_design (multi-level states target):")
    println("  levels = " + str(levels))
    println("__HEXA_MEMRISTOR_DESIGN__ PASS gate=GATE_OPEN absorbed=false")
}
