#!hexa strict
// memristor specify — on/off ratio target.
fn main() {
    let ratio_target = 100.0
    println("memristor_specify (on/off ratio target):")
    println("  ratio_target = " + str(ratio_target))
    println("__HEXA_MEMRISTOR_SPECIFY__ PASS gate=GATE_OPEN absorbed=false")
}
