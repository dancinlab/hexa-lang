#!hexa strict
// memristor handoff — crossbar array size.
fn main() {
    let crossbar_size = 1024.0
    println("memristor_handoff (crossbar array size):")
    println("  crossbar_size = " + str(crossbar_size))
    println("__HEXA_MEMRISTOR_HANDOFF__ PASS gate=GATE_OPEN absorbed=false")
}
