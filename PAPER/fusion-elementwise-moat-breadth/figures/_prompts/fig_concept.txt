A clean technical diagram, flat vector infographic style, white background, for a GPU systems paper. Left side: four stacks of separate kernel-launch boxes labeled "EAGER (per-op)" each showing 3 to 4 small colored rectangles connected by curved arrows that loop down through a wide gray bar labeled "HBM" (high-bandwidth memory) between every box, illustrating repeated memory round-trips. Right side: four single large green rounded rectangles labeled "FUSED (1 launch)" each touching the HBM bar only once at top and once at bottom, showing registers and shared memory kept inside. Four rows labeled LayerNorm, RMSNorm, Softmax, SwiGLU. A bold percentage callout on each fused box: 66%, 59%, 66%, 63% faster. Minimalist, professional, blue and green palette, thin sans-serif labels, no photorealism, publication quality.
