A clean technical diagram, two side-by-side panels on a white background, flat
minimal scientific-paper style, thin dark-blue and dark-red line art, no text
labels baked in.

LEFT PANEL titled "structural": two data-flow schematics stacked. Top schematic
labeled "cuBLAS baseline": three boxes left-to-right (GEMM, softmax, GEMM) with
a large N-by-N grey square matrix "S" written to and read from a memory bar
below them four times, thick arrows down-and-up to a horizontal HBM memory bar,
emphasizing heavy round-trip traffic. Bottom schematic labeled "fused": a single
box that streams thin Q, K, V strips through it directly to an output strip O,
with the N-by-N S matrix crossed out, NO arrows to the memory bar, emphasizing
zero score-matrix traffic. A small annotation shows the ratio growing 9x, 17x,
33x, 65x as an upward staircase.

RIGHT PANEL titled "wall": a simple grouped bar chart, four shape groups
(512, 1024, 2048, 4096) on the x-axis, two bars each — a taller blue bar
(fused, slower) and a shorter red bar (cuBLAS baseline, faster) — visually
showing the fused kernel is about 1.5x taller (slower) at every group. A small
horizontal dashed line marks a 30 percent threshold the blue bars never beat.

Overall mood: an honest engineering result — the left panel a clear structural
win, the right panel a clear wall loss. Crisp, vector-like, publication figure.
