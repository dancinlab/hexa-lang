A clean, minimalist, publication-quality scientific schematic on a pure white
background, the aesthetic of a Nature or arXiv figure: thin clean lines, lots of
white space, no 3D, no heavy gradients, no clutter.

The figure has TWO stacked panels sharing the white canvas.

TOP PANEL — title "Exponent primality does NOT imply Mersenne primality". A
horizontal number line of small prime exponents p = 2, 3, 5, 7, 11, 13, 17, 19.
Above each p draw a small box labeled "M_p = 2^p - 1". For p = 2, 3, 5, 7, 13,
17, 19 the box is filled solid blue and labeled "PRIME" with a thin green
upward arrow pointing to a second box labeled "perfect number 2^(p-1) * M_p".
But for p = 11 the box is filled red and crossed out, labeled
"M_11 = 2047 = 23 x 89  COMPOSITE", with a red broken arrow and the annotation
"p prime, M_p NOT prime -> no perfect number (first counterexample)". The single
red p = 11 column visually breaks the otherwise blue regular pattern.

BOTTOM PANEL — title "The Euclid-Euler bridge: even perfect <=> Mersenne prime".
A clean two-way correspondence diagram. On the left a rounded box "EVEN PERFECT
NUMBER  n,  sigma(n) = 2n". On the right a rounded box
"MERSENNE PRIME  M_p = 2^p - 1,  n = 2^(p-1) * M_p". A thin double-headed blue
arrow labeled "if and only if (Euclid 300 BCE + Euler 1849)" connects them.
Below, a small honest grey footnote box reads
"ODD perfect numbers: OPEN frontier (none below 10^1500)".

Monochrome blue with green and red and grey accents only, thin lines,
publication clarity, generous white margins. No photographic elements, purely
a vector-style line schematic.
