A clean technical diagram, white background, comparing two GPU kernel pipelines for a neural network feed-forward layer. TOP ROW labeled "cuBLAS-using stack": three separate boxes in a row connected by arrows that each dip down to a memory bar labeled "HBM" and back up, boxes labeled "GEMM", "bias-add", "GeLU", showing three round-trips to memory, three launch icons. BOTTOM ROW labeled "fused hexa kernel": a single wide box labeled "GEMM + bias + GeLU (fused)" with one arrow to the HBM memory bar, one launch icon, one write. A large bold "3 to 1" reduction arrow on the right. Minimal flat vector style, blue and slate gray palette, crisp lines, publication quality, no text gibberish.
