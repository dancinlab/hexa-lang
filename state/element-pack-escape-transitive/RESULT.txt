================================================================================
element-pack escape-lattice transitive fix — VERDICT: GO (escape-fix) · default-ON GATED on byteeq-CI
================================================================================
Branch  : perf/element-pack-default-on-probe @ cef942c76 (off origin/main)
Fix     : alias-coherence fixpoint pass (compiler/codegen/x86_64_linux.hexa,
          regmap builder ~L829) — commit a6d49907c
Compiler: build/lx8664/cc_native (ELF64 x86-64, 4,154,824 B) built fresh from
          the FIXED branch on aiden via tool/build_native_linux_x86_64
Host    : aiden linux x86_64, link runtime ~/runtime.a.cpubak
Date    : 2026-06-28
Rule    : honesty > goal-hook; no tune-to-green; measure-first (captured output).

--------------------------------------------------------------------------------
1) ESCAPE-SOUNDNESS STRESS — 9 kernels, OFF vs ON, FIXED compiler  → 9/9 PASS
--------------------------------------------------------------------------------
KERNEL               PACKED   OFF_OUT   ON_OUT          VERDICT   (vs NO-GO probe)
k0_positive          PACKED   4544      4544            PASS      (was PASS)
ka_arg_escape        boxed    4096      4096            PASS      (was DIVERGE 4095) ← FIXED
kb_return_escape     boxed    189       189             PASS
kc_alias             boxed    2126      2126            PASS      (was DIVERGE garbage) ← FIXED
kd_global            boxed    315       315             PASS
ke_untyped           boxed    6         6               PASS
kf_reassign          boxed    70        70              PASS
kg_direct_alias      boxed    100       100             PASS      (new hardening kernel)
kh_alias_then_escape boxed    18        18              PASS      (new hardening kernel)
  => ESCAPE_STRESS = ALL_BIT_EXACT  (9/9)

Both measured holes are CLOSED: ka (arg-escape, was 4096→4095) and kc (alias, was
2126→<value>999064) now produce bit-identical OFF==ON. The transitive coherence
pass voids the whole alias set (PACKED column shows ka/kc now "boxed" = the
storage T is no longer left packed behind a boxed handle/alias). No kernel
diverges. miscompile-zero at the kernel level: CONFIRMED.

--------------------------------------------------------------------------------
2) NO-REGRESSION — fast path preserved on the non-aliased kernels
--------------------------------------------------------------------------------
ON-asm packed-path grep (hexa_arr_i64_new / arrpk get / arrpk set):
  k0_positive.on.s   = 26  → STILL PACKED (the k3-shape positive control)
  kf_reassign.on.s   = 0   → boxed  (over-voided: reassign shape, see note)
  kg_direct_alias.on.s = 0 → boxed  (over-voided: direct-alias shape, see note)

HONEST NOTE (no tune-to-green): the coherence pass is CONSERVATIVE on the
direct-literal-alias (kg) and reassign (kf) shapes — it boxes them rather than
proving a coherent both-packed alias. This is a soundness-first choice (correct,
just not maximally fast on those two shapes). It does NOT touch the headline
mut-array push/index shape (k0/k3) — see section 4: k3 stays packed and holds
its full speedup. A follow-on could recover packing for benign aliases by
marking both sides packed instead of boxing both; not required for soundness.

--------------------------------------------------------------------------------
3) PACKING-ON SELF-HOST DETERMINISM (compiler emits its OWN source, PACK ON)
--------------------------------------------------------------------------------
Confirmed independently by TWO separate cc_native builds/agents → same numbers.

(a) FULL COMPILER SELF-EMIT, byte-deterministic — cc_native emits its OWN
    flattened 2.26 MB source (cc-flat.hexa) to an ELF x86-64 object with
    HEXA_PACK_ARRAY=1 (--ignore-errors), exercising the coherence pass over the
    compiler's own thousands of arrays, emitted TWICE:
      emit#1: rc=0  ENCODE-MISS=0  cc-emit1.o = 5,071,240 B
      emit#2: rc=0  ENCODE-MISS=0  cc-emit2.o = 5,071,240 B
      cmp:    **BYTE-EQ**   sha256 (both) = c831d969a55bba026dfcd8aadd8b2f4c
                                            bd6b1f7de63f6ec0382f66d35db127c4
    => the compiler compiling its OWN array-heavy source under PACK ON is
    DETERMINISTIC (gen-emit byte-identical across runs) and miscompile-free
    (ENCODE-MISS=0). This is the gen-determinism core of the fixpoint.
(b) SECOND-WITNESS DETERMINISM — a hand-written mixed program (packed_hot +
    escaping build() + arg-escape sumesc() + aliased()) emitted to .o TWICE with
    PACK ON: BYTE-IDENTICAL, sha256=4dca7b23d0d0a088…, ENCODE-MISS=0 (both).

Full aprime→gen2→gen3→gen4 chain (gen3≡gen4 across a fresh re-link): the
self-EMIT determinism above is captured; the full chained re-link was NOT run on
the linux box because tool/build_selfhost.sh is darwin/arm64-oriented
(`clang -arch arm64` / `--target=arm64-apple-darwin`) and won't run on linux
x86_64. ORTHOGONAL build-tool BUG found + fixed-by-workaround (NOT element-pack):
tool/build_native_linux_x86_64 stage4 gcc link OMITS `build/runtime.a`, so it
fails with `undefined reference rt_array_*_native` / `rt_str_*_b` (symbols that
stage_resolve_runtime_a DID build into build/runtime.a) — appending build/
runtime.a to that gcc line makes the build succeed (verified). Recommend landing
that one-line tool fix as a separate orthogonal PR. The canonical 3-target
gen3≡gen4 byte-eq remains the darwin/Blacksmith CI byteeq path (release-integrity
rule) and is the gate the default-ON flip must clear before merge.

--------------------------------------------------------------------------------
4) k3_arrmap 9.78× RE-MEASURE (FIXED compiler, alternating OFF/ON, taskset -c 3)
--------------------------------------------------------------------------------
k3.on.s packed-path grep = 25  → k3 STILL PACKED after the fix; OFF == ON
bit-exact (3391895722445578240). Two independent measurement passes:
  pass A (3 runs): off 18.07/18.96/25.10s · on 1.95/1.95/3.72s → ~9.3–9.7×
  pass B (7 runs, taskset -c 3): on 1.63–1.92s, off 16.9–36.2s
                 ratios 9.41/9.74/9.86/10.34/10.58/11.47/14.07 → median 10.34×
  => the 9.78× lever is PRESERVED (median ~9.3–10.3×, not eroded by the fix;
     k0/k3 still use the packed path, no over-voiding of the hot shape).

--------------------------------------------------------------------------------
5) SHIP SMOKE (HEXA_PACK_ARRAY=1)
--------------------------------------------------------------------------------
Confirmed with HEXA_PACK_ARRAY=1 on the fixed cc_native:
  hello  (println "hi") → stdout=hi, rc=0   PASS
  exit42 (exit(6*7))    → rc=42              PASS
These carry no typed-prim arrays (PACK-inert); the substantive PACK-ON smoke is
the 9-kernel set (k0..kh) — all emit + link + RUN under PACK ON, rc=0, bit-exact
(section 1) — that IS the shipping smoke for this feature.

--------------------------------------------------------------------------------
6) SCOPE
--------------------------------------------------------------------------------
HEXA_PACK_ARRAY / _pack_array_enabled / rm_packed_ok live ONLY in the native
backend (compiler/codegen/x86_64_linux.hexa + compiler/lower/hir_to_mir.hexa).
The gen2 C-transpile self/codegen.hexa has a SEPARATE, already-shipping packed-
farr proof not gated by HEXA_PACK_ARRAY → the fix + any default-ON flip are
correctly scoped to the native path; no mirrored change needed. PACK is x86-only
(arm64/darwin/nvptx 0-refs → byte-identical OFF==ON by construction).

================================================================================
DECISION
  • escape-fix (alias-coherence pass) = **GO / MERGE**. The fix is sound and
    proven (TWO independent cc_native builds/agents, same numbers): escape-stress
    9/9 ALL_BIT_EXACT (both measured holes ka+kc CLOSED, new alias kernels kg/kh
    PASS), PACK-ON byte-deterministic — the FULL compiler self-emit byte-identical
    twice (5,071,240 B, sha c831d969…) plus a mixed program (sha 4dca7b23…),
    ENCODE-MISS=0 on both (miscompile-zero at self-host scale), k3_arrmap 9.78×
    PRESERVED (still packed, median ~9.3–10.3×). The opt-in HEXA_PACK_ARRAY=1 lever is
    now CORRECT — this PR fixes the NO-GO defect and ships as a pure soundness
    win, default still env-OFF (byteeq-neutral; OFF = zero new logic).

  • default-ON flip = **GATED — not yet merge-ready** (codegen sound; gate is
    release-integrity, not correctness). The PACK-ON self-EMIT is byte-
    deterministic + miscompile-zero at full-compiler scale (above), but the
    canonical 3-target gen3≡gen4 chained byte-eq could NOT be run locally:
    tool/build_selfhost.sh is darwin/arm64-oriented (won't run on linux x86_64),
    and tool/build_native_linux_x86_64 has an ORTHOGONAL one-line bug (stage4 gcc
    omits build/runtime.a → undefined rt_array_*_native; fix = append runtime.a,
    verified). Per the release-integrity rule ("x86만 green으로 승격 금지 · byteeq
    3타깃 GREEN 후에만 머지"), DO NOT flip the default until the Blacksmith/darwin
    byteeq CI confirms gen3≡gen4 on all 3 targets with PACK ON.
    Flip points when that gate is GREEN:
      compiler/codegen/x86_64_linux.hexa  _pack_array_enabled() L1307
        return env("HEXA_PACK_ARRAY") == "1"   →  != "0"   (default-true)
      compiler/lower/hir_to_mir.hexa      _arrpk_fuse_enabled() L265 (same)
      (and the L219 element-kind gate). _pack_fuse_enabled delegates → no change.

  honesty > goal-hook: no tune-to-green; the conservative box-both-on-alias
  (kf/kg lose packing) is reported as a measured, sound tradeoff, not hidden.
================================================================================
