OP-122 — stateful / streaming decoder cross-chunk state-corruption sweep (LANE 1)
================================================================================
Class: a parser/decoder that maintains STATE ACROSS multiple input pieces
(chunks/frames/feeds) where an adversarial chunk boundary corrupts the carried
state -> wrong output, lost/duplicated bytes, frame desync. Property tested:
streaming-equivalence  decode(concat(chunks)) == concat(decode-per-chunk-carry)
for EVERY split. A divergence at some split = real bug.

VERDICT: 🟢 CONVERGED — NO 🔴. Every in-hexa carried-state decoder passes
split-equivalence at every byte offset. Zero cross-chunk corruption.

COVERAGE MATRIX (in-hexa carried-state decoders — boundary parsed in HEXA):
  · websocket_native ws_recv_native  h["buf"] carry-over across frames via
    _ws_fill / _ws_take.  CLEAN — every-offset split-equivalence holds
    (mid-header / mid-2-byte-length / mid-mask / mid-payload, across the 126
    and 127 length-form boundaries, zero-length + control frames, multibyte).
  · http_sse  http_sse_parse_event / http_sse_feed  partial-event accumulator
    carried across feed() calls.  CLEAN — the parser is PER-LINE; lines are
    atomic (delivered whole by the runtime), so the carried `event` map is
    invariant to poll-grouping. one-shot == incremental feed.
  · jsonl_pool  _jp_cell_next_line  (cell["lines"]/["lpos"]).  CLEAN — the
    line-split is delegated to channel_recv_lines (runtime/perl), not hexa;
    the cell carry only re-indexes already-split whole lines.
OUT OF SCOPE / documented-precondition (boundary NOT parsed in hexa):
  · channel_recv_lines — single 1MB sysread + split("\n"); a line split across
    two reads fragments ONLY for a single write > PIPE_BUF (512 macOS /4096
    Linux). DOCUMENTED at channel.hexa:400-404 ("atomic for lines <= PIPE_BUF;
    for larger use a dedicated channel pair"). Documented precondition = not a
    bug; the split itself is in perl, not hexa.
  · websocket.hexa python/websocat backends — framing done by python/websocat,
    hexa side carries no decode state.  · http.hexa chunked — curl de-chunks.
  · self/serve/http_server.hexa parse_request — ONE-SHOT (whole `raw`, no
    feed-more API) = OP-92's single-buffer class, not OP-122.

ORACLE: Python port-of-source of the ws frame parser (_ws_fill/_ws_take carry)
exhaustively verified streaming-equivalence — one-shot vs all-1-byte chunks vs
EVERY two-way split vs 2000 random three-way splits, 0 fails (boundary frames
straddling the 126/127 header-length forms included). hexa int is 64-bit SIGNED
(OP-118/121); the 127-form top-bit-set wrap is guarded upstream (val<0 reject,
locked by websocket_native_len64_overflow_test) and NOT re-tested here.

HERMETIC LOCK (hexa run, LOCAL, $0):
  stdlib/net/streaming_state_equivalence_op122_test.hexa

  $ hexa run stdlib/net/streaming_state_equivalence_op122_test.hexa
  EXIT=0
  __HEXA_OP122_STREAMING_STATE__ PASS 5/5

  5 checks: (1) one-shot decode has no short-read sentinel; (2) all-1-byte
  fragmentation == one-shot; (3) EVERY two-way split offset == one-shot;
  (4) representative three-way splits == one-shot; (5) SSE feed-with-carry
  event sequence == one-shot. Parser logic verbatim-inlined (stale-bundle
  immune). The carried state (frame buf / partial event) is the OP-122 axis.

HONEST g5: the in-hexa streaming-decode surface is split-equivalence-correct BY
CONSTRUCTION (buf carry pops exactly the consumed bytes; SSE carry is per-atomic
-line). Evidence-backed "class clean / LANE-1 converged" is the truthful SUCCESS;
no 🔴 fabricated. The guard's exhaustive every-offset two-way sweep + the Python
oracle are the falsifier that found no divergence.

FLAGS: NO source edit (CLEAN=🟢) -> ZERO byte-eq/fixpoint impact. NEW leaf
stdlib/net/*_test.hexa is build_selfhost-closure-OUT (standalone test) -> fixpoint
UNAFFECTED. wipe_guard N/A (1 new file, 0 deletions). NO .tape. LANE-1 only —
disjoint from sibling lanes (OP-120 recursion-depth, OP-121 comparator).
$0 · 0-pod · NO GPU · no vast · no foreign-pod · leak-0.
