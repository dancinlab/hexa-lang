RUNTIME-PORT · M2 · hexa_bytes_to_str_raw → rt_bytes_to_str_raw (B1 CLOSE)
date: 2026-06-03   host: mini (LOCAL — no ghost/pool)   base: origin/main 53c91d784
verdict: PORT-EQ  (9/9 byte-exact, incl all 3 embedded-NUL cases)
prior:   DIFFER (6/9, see M2-hexa_bytes_to_str_raw.txt) — closed by B1
########################################################################

C SSOT  : HexaVal hexa_bytes_to_str_raw(HexaVal arr)   self/runtime.c L6105
hexa port: @pure fn rt_bytes_to_str_raw(arr:[int]) -> string  self/rt/string.hexa

THE B1 DELTA (STRBUILDER-FEAS Class B — codegen surface, NUL-clean builder)
------------------------------------------------------------------------
Added a length-carrying byte→string constructor codegen builtin,
`str_from_bytes_n(arr)`, mapped to the EXISTING NUL-clean C constructor
hexa_bytes_to_str_raw (which assembles via hexa_strbuf_alloc(n) — an
explicit perf-31 length header, so HX_STRLEN sees all n bytes and embedded
0x00 survive). NO new runtime symbol · NO ABI change (the constructor and
the header rep already exist). Registered in all 3 builtin tables:
  • self/codegen.hexa            (tier-2 transpiler — 1-arg builtin block)
  • compiler/codegen/arm64_darwin.hexa (native codegen — cg_string_sym)
  • compiler/check/bind.hexa     (bind allow-list — name resolution)
rt_bytes_to_str_raw keeps its hexa range-guard loop (b<0 / b>255 → "")
and now delegates final assembly to str_from_bytes_n instead of
chr()/join (hexa_str_join), which derived length from the NUL-truncated C
string and collapsed any NUL-bearing result to "".

CODEGEN FIXPOINT PROOF (end-to-end, LOCAL on mini)
------------------------------------------------------------------------
The codegen edit was folded into hexa_cc.c via `hexa cc --regen` (transpile
self/{lexer,parser,type_checker,codegen}.hexa → merge → hexa_cc.c.new →
build hexat). The regenerated transpiler lowers the builtin correctly:

  HexaVal rt_bytes_to_str_raw(HexaVal arr) {
      ... range-guard loop (hexa_cmp_lt/gt on 0 / 255) ...
      return __hexa_fn_arena_return(hexa_bytes_to_str_raw(arr));  // <- B1
  }

(prior, un-patched transpiler emitted `hexa_call1(str_from_bytes_n, arr)`
 — a function-pointer fallback, undefined → the gap. B1 closes it.)

METHOD (real RUNEQ, not self-judge)
------------------------------------------------------------------------
Minimal-TU RUNEQ: the ACTUAL self-host transpiler output (emitted by the
B1-regenerated hexat) for rt_bytes_to_str_raw, linked vs the shipping
runtime.c C SSOT hexa_bytes_to_str_raw, byte-exact compare (HX_STRLEN +
memcmp, NUL-aware) over 9 array inputs: empty, "Hi", a lone NUL,
NUL-interleaved, the full {0,1,127,128,254,255} byte set, three
out-of-range (256 / -1 / 300), and the raw 3-byte UTF-8 of '한'.

RAW HARNESS OUTPUT (verbatim stdout — REAL transpile)
------------------------------------------------------------------------
ok   empty        C=C[len=0:] hexa=H[len=0:]
ok   Hi           C=C[len=2: 48 69] hexa=H[len=2: 48 69]
ok   nul          C=C[len=1: 00] hexa=H[len=1: 00]
ok   mix-nul      C=C[len=5: 00 41 00 42 00] hexa=H[len=5: 00 41 00 42 00]
ok   full         C=C[len=6: 00 01 7f 80 fe ff] hexa=H[len=6: 00 01 7f 80 fe ff]
ok   oob256       C=C[len=0:] hexa=H[len=0:]
ok   oob-neg      C=C[len=0:] hexa=H[len=0:]
ok   oob-low      C=C[len=0:] hexa=H[len=0:]
ok   kor-raw      C=C[len=3: ed 95 9c] hexa=H[len=3: ed 95 9c]

RUNEQ-SUMMARY (rt_bytes_to_str_raw, REAL transpile) total=9 fails=0 verdict=PORT-EQ

The 3 previously-DIFFER cases (nul / mix-nul / full) — all embedded-NUL —
now match byte-for-byte. nul: len 1 [00]. mix-nul: len 5 [00 41 00 42 00].
full: len 6 [00 01 7f 80 fe ff]. These are the exact leading-NUL patterns of
PNG IHDR length fields (the wilson image_read caller).

BYTE-EQ RISK NOTE (codegen fixpoint → ghost)
------------------------------------------------------------------------
The B1 change is a SELF-HOST CODEGEN change. The local proof above used a
LOCALLY regenerated hexat (hexa cc --regen, byte-clean compile, restored to
pristine after). The repo's frozen-seed hexa_cc.c / runtime.c are gitignored
build artifacts NOT mutated by this PR — they are regenerated by the build's
documented `_regen_runtime_core` / `hexa cc --regen` path. The codegen
fixpoint (that the shipped transpiler re-emits byte-identically with the new
builtin folded in) is the NON-REQUIRED `selfhost-byteeq-real` CI check that
runs on ghost; this PR does NOT run/wait on it — the parent gates merge on it.
Local semantic + lowering proof is 9/9 PORT-EQ; ghost confirms the byte-eq
fixpoint.

DISPOSITION: PORT-EQ — B1 builder builtin wired LIVE. rt_bytes_to_str_raw is
now NUL-clean and byte-identical to the C SSOT. STRBUILDER-FEAS Class B
delta landed. Closes the M2 BLOCKED leaf.
