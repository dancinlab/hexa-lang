# L-FUNCTION layer — L(s, X_0(6)) spectral fingerprint probe @ n=6
# date: 2026-05-27 · hexa verify --expr (g5 CLI-only) · VERBATIM

## (a) Genus-0 -> trivial Jacobian -> no weight-2 newform (structural closed-negative basis)
$ hexa verify --expr gamma0_genus 6 0 --no-absorb   # X_0(6) genus 0 => J_0(6) trivial
verify --expr gamma0_genus(6)=0
  calc   = 0  == expected 0
  tier   = 🔵 SUPPORTED-FORMAL  (hexa-native closed-form, g_self_verify · TECS-L Tier1)
$ hexa verify --expr dim_cusp_forms 6 2 0 --no-absorb   # dim S_2(Gamma_0(6))=0 => no wt-2 L(s,f)
verify --expr dim_cusp_forms(6,2)=0
  calc   = 0  == expected 0
  tier   = 🔵 SUPPORTED-FORMAL  (hexa-native closed-form, g_self_verify · TECS-L Tier1)

## (b) Actual L-function machinery (Euler product / special value / non-vanishing) — NO calc-path
$ hexa verify --expr l_value 6 1 --no-absorb
verify --expr l_value(6)=1
  tier   = 🟠 INSUFFICIENT
  reason = calculator system has NO path for 'l_value'
  gap    = extend tool/verify_cli.hexa::_recompute (계산기시스템 개선 후보)
$ hexa verify --expr l_function_special_value 6 1 --no-absorb
verify --expr l_function_special_value(6)=1
  tier   = 🟠 INSUFFICIENT
  reason = calculator system has NO path for 'l_function_special_value'
  gap    = extend tool/verify_cli.hexa::_recompute (계산기시스템 개선 후보)
$ hexa verify --expr lfunction_nonvanishing 6 1 --no-absorb
verify --expr lfunction_nonvanishing(6)=1
  tier   = 🟠 INSUFFICIENT
  reason = calculator system has NO path for 'lfunction_nonvanishing'
  gap    = extend tool/verify_cli.hexa::_recompute (계산기시스템 개선 후보)

# => The structural closed-negative basis (genus 0 => J_0(6) trivial => no wt-2 newform) is 🔵-backed,
#    BUT the L-FUNCTION object itself (L-value, Euler product, special value, non-vanishing) is
#    analytic-infinite with NO hexa calc-path (🟠 INSUFFICIENT). The weight-4 piece leans on the
#    calc-gapped dim_cusp_forms. NET L-FUNCTION TIER = 🟠 (analytic-infinite, out-of-scope).
