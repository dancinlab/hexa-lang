F-FUSION-P2-RIGHTSIZED-REALSTEP — does the REAL clm_prod training step
(serial sub-ms kernel DAG) reach util-GREEN on a right-sized RTX 5070?

STATUS: TERMINAL 🔴 FALSIFIED (RTX 5070 measured 2026-06-05, pod 39499371)

PRE-REGISTERED FALSIFIER
  The REAL clm_prod training step — with its serial sub-ms kernel DAG
  (norm/elementwise/MoE glue between the GEMMs, median 2% util on idle H100,
  F-FUSION-OCCUPANCY-WALL) — reaches util-GREEN (>=20%, ideally >=70%) on a
  right-sized consumer RTX 5070, vs ~13% (eager) / ~18% (fused) on idle H100.
  If YES  -> anima's 3B/7B forge gets util-GREEN immediately by running on the
            right GPU, no code change.
  If NO   -> even a small GPU idles between the sub-ms kernels; the DAG is the
            wall regardless of GPU size (only the ISOLATED GEMM saturated, D2).

CONTRAST WITH D2 (#2713, F-FUSION-D2-RIGHTSIZED)
  D2 saturated an RTX 5070 to ~98% — but D2 was a STANDALONE GEMM/util DRIVER,
  NOT the real trainer. H1a (#2734) showed the GEMMs saturate the H100 too.
  P2 measures the REAL TRAINER STEP (the full serial DAG), which is the open
  question OCCUPANCY-WALL left for the right-sized axis.

WORKLOAD (right-sized to the 12 GiB RTX 5070 — see NOTE)
  CLMConvMoE D=1024 T=512 E=2 NSAMP=8 EPOCHS=8 (sustained)
  DEVRESIDENT=1 DEVFEED=1 BATCHED=1 ASYNC=0
  binary: clm_prod_gpu from fusion_l3a_wired.tgz (one build; eager when
  HEXA_FUSE_GN_GELU unset, fused one-kernel GN+GELU when =1)

  NOTE — the H100-baseline workload D=1536 T=512 does NOT FIT 12 GiB: it
  OOMs hard on the RTX 5070 (`cudaMalloc(7077888 doubles) failed: out of
  memory`, process Killed/rc=137 — captured below). D=1024 T=512 is the
  largest config that fits 12 GiB AND runs the FULL real serial kernel DAG
  end-to-end (windows 8/8, mean CE computed, L3A groupnorm_gelu DEVICE path
  fired). This is the genuine right-sized real trainer step — the finding
  is same-GPU valid (it IS the full serial DAG, just sized to the card).

METHOD
  nvidia-smi utilization.gpu sampled @ 0.1s (180 samples = 18s) over a
  sustained EPOCHS=8 run, after a 5s warmup (skips corpus load + first
  alloc); MEAN / PEAK / MEDIAN / pct>=20 / pct>=70 reported verbatim for
  idle baseline, eager, and fused (HEXA_FUSE_GN_GELU=1).

H100 BASELINE (idle, F-FUSION-OCCUPANCY-WALL / GRAPH-AB)
  eager  MEAN ~11.85-12.50%  median 2%  PEAK 71-100% (brief GEMMs only)
  graph  MEAN  13.17%        median 2%
  L3-a fused  +1.01pp stacked (F-FUSION-L3A-GN-GELU-AB)

VERIFICATION (RTX 5070, 12227 MiB, driver 580.95.05, CUDA 12.4, pod 39499371)
  GPU model  : NVIDIA GeForce RTX 5070 (Blackwell; compute_89 PTX JIT-forwards)
  build      : runtime_cuda.o 257832 B, launchers 59/59 (-DHEXA_CUDA), gn_gelu 3
               clm_prod_gpu 767880 B (gcc link rc=0, -lcudart -lcublas -lcuda)

  D=1536 T=512 (H100-baseline size) — DID NOT FIT:
    cudaMalloc(7077888 doubles) failed: out of memory; process Killed rc=137
    -> H100-size workload is not measurable on a 12 GiB card (OOM, not idle)

  D=1024 T=512 (right-sized, FULL real serial DAG, sustained EPOCHS=8) —
    idle baseline   MEAN  8.62%  PEAK 100%  MEDIAN  0%   n=16
    EAGER  (fuse=0) MEAN 26.29%  PEAK 100%  MEDIAN  9%   MIN 0%
                    pct>=20=37.2%  pct>=70=16.7%  n=180
    FUSED  (fuse=1) MEAN 24.90%  PEAK 100%  MEDIAN  2%   MIN 0%
                    pct>=20=29.4%  pct>=70=20.0%  n=180  [L3A-FIRED device path]

  Distribution is BIMODAL (the OCCUPANCY-WALL signature): a large mass of
  0-9% idle samples (the sub-ms norm/elementwise/MoE-glue kernels under-fill
  the GPU) PLUS ~23-28/180 samples pinned at 100% (the brief GEMM bursts).
  MEAN is lifted to ~25-26% ONLY by the GEMM bursts; the MEDIAN (2-9%) shows
  the GPU sits IDLE most of the step — exactly as on the idle H100.

FINDING (🔴 FALSIFIED — right-sizing alone does NOT give anima util-GREEN)
  The REAL clm_prod training step does NOT reach util-GREEN on a right-sized
  RTX 5070. The serial sub-ms kernel DAG idles THIS GPU too: MEDIAN util is
  9% (eager) / 2% (fused) — indistinguishable from the idle-H100 median 2%.
  MEAN ~26% is an artifact of brief 100% GEMM bursts, not a saturated step;
  pct>=70 is only 16.7% / 20.0% of samples (the GEMM windows). This is the
  SAME bimodal occupancy-wall profile measured on the idle H100, NOT the ~98%
  saturation D2 (#2713) got from the STANDALONE GEMM/util DRIVER.

  => The DAG is the wall, regardless of GPU SIZE. Only the ISOLATED GEMM
     saturates (D2); the full serial trainer step idles any GPU between the
     sub-ms kernels. Right-sizing alone gives anima's 3B/7B forge a higher
     MEAN (GEMM bursts dominate a smaller card) but NOT util-GREEN — the
     median stays at occupancy-wall floor. The path to util-GREEN remains
     kernel-DAG fusion / occupancy fill (L3 fusion lane), not GPU swap.

  Caveat (honest): the H100-size workload (D=1536) OOMs at 12 GiB, so this
  is the D=1024 right-sized step, not a byte-for-byte H100 replay. The DAG
  STRUCTURE (and thus the inter-kernel idle) is identical; the finding is
  that the serial-DAG idle SURVIVES right-sizing, which D=1024 establishes.

  pod 39499371 DESTROYED (hexa cloud rm --force; alive-probe = GONE; leak 0).
