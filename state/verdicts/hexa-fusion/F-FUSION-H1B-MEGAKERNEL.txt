F-FUSION-H1B-MEGAKERNEL  ::  device-resident full-step MEGAKERNEL SKELETON (option-B, B1)
domain: HEXA-FUSION  ·  date: 2026-06-05  ·  method: cooperative-launch persistent kernel + own-GEMM inline + sub-chain rel-RMS A/B + util A/B

────────────────────────────────────────────────────────────────────────────────
PRE-REGISTERED FALSIFIER
────────────────────────────────────────────────────────────────────────────────
A persistent grid-resident megakernel with INLINE own-GEMM (no cuBLAS) runs a
GEMM → glue(gelu) → GEMM sub-chain in ONE cooperative launch, producing output
that matches the EAGER same-sub-chain (own-GEMM 3-launch path) within the TF32
own-GEMM bar (rel-RMS <= 3e-3), AND raises GPU util on the SAME sub-chain vs the
eager realization (collapsing the two launch-gaps lifts util).

FALSIFIED if: (a) the cooperative launch cannot be co-resident (occupancy=0 /
register or smem over-subscription), OR (b) [MEGASTEP-FIRED] never fires, OR
(c) rel-RMS > 3e-3 on the sub-chain, OR (d) util(megakernel) <= util(eager) on
the SAME sub-chain (the launch-gap collapse buys no occupancy).

SCOPE (honest): this is a SKELETON — the smallest end-to-end witness (GEMM→gelu→
GEMM), NOT the full ~30-op clm_prod step. NO "full megakernel done" claim is made
or implied. This verdict establishes whether the STRUCTURE runs at all and what
remains for the full step.

WHY NOW UNBLOCKED: F-FUSION-MEGAKERNEL-DESIGN ruled the FULL megakernel CLOSED-
NEGATIVE *for the FP64 + cuBLAS-roofline workload* — a persistent kernel cannot
call cublasDgemm (nested grid launch), and a hand FP64 GEMM breaks byte-eq +
forfeits cuBLAS throughput. The own-GEMM stack (#2697 tiled · #2704 WMMA2 ·
#2714 split-K) removes BOTH walls for the TF32 regime: the persistent kernel
calls OUR WMMA2 GEMM inline (no cuBLAS dependency), and the correctness bar is
the approved TF32 own-GEMM rel-RMS <= 3e-3, not FP64 byte-eq. The eager
reference ALSO uses own-GEMM, so the comparison is own-GEMM-vs-own-GEMM.

────────────────────────────────────────────────────────────────────────────────
METHOD
────────────────────────────────────────────────────────────────────────────────
Artifacts (this branch):
  megakernel_driver.cu        — driver + _hx_k_clm_megastep (cooperative kernel)
  gemm_device_extracted.cuh   — _hx_k_sgemm_cm_wmma2 body as a __device__ fn
                                 (identical math; tile coord from params not blockIdx)
  build_megastep.sh           — extract own-GEMM kernels from the shipped .cu, nvcc -rdc=true
  util_megastep.sh            — external nvidia-smi util sampler, eager vs mega, SAME sub-chain
Structure demonstrated:
  (1) cudaLaunchCooperativeKernel + cooperative_groups::this_grid().sync() barriers
      between dependent phases (phase counter == the grid barrier).
  (2) inline own-GEMM tiles: WMMA2 body reused VERBATIM as a __device__ function;
      the persistent kernel calls OUR GEMM (the own-GEMM unblock).
  (3) gelu fused inline between the GEMM phases; intermediate H1/G kept device-
      resident across grid.sync() — no HBM-to-host round-trip, no relaunch gap.
Probe: [MEGASTEP-FIRED] printed once when the cooperative launch + grid.sync()
  path actually runs (env HEXA_CLM_MEGASTEP=1).
Sub-chain dims: M=1024 K=1536 N1=1536 N2=1024 (clm_prod conv-block-ish, D~1536).
Hardware: ONE H100 (vast, --owner h1b-mega), down-after, leak 0.

────────────────────────────────────────────────────────────────────────────────
VERIFICATION  (verbatim pod stdout — H100 NVL, sm_90, CUDA 12.4, owner h1b-mega)
────────────────────────────────────────────────────────────────────────────────
$ bash build_megastep.sh   (extract 504 lines, 6 kernels; nvcc -arch=sm_90 -rdc=true clean)

=== RUN: megakernel sub-chain (byte-eq/rel-RMS + util A/B) ===
GPU: NVIDIA H100 NVL (sm_90)
sub-chain: GEMM(M=1024,N1=1536,K=1536) -> gelu -> GEMM(M=1024,N2=1024,K=N1=1536)  iters=50
HEXA_CLM_MEGASTEP=1 (megakernel path ON)
coop occupancy: 1 blocks/SM × 132 SM = 132 resident blocks (occ query: no error)
[MEGASTEP-FIRED]
correctness (megakernel vs eager own-GEMM sub-chain):
  rel-RMS  = 0.000e+00  PASS  (TF32 own-GEMM bar <= 3e-3)
  max|Δ|   = 0.000e+00
sustained throughput on the SAME sub-chain (12s each):
  eager (3 launches)   : 12176 iters  (1013.5 sub-chains/s)
  megakernel (1 coop)  : 11216 iters  (934.1 sub-chains/s)

=== RUN: small odd shape (bounds guard)  M=130 N1=80 K=96 / M=130 N2=5 K=80 ===
coop occupancy: 1 blocks/SM × 132 SM = 132 resident blocks (occ query: no error)
[MEGASTEP-FIRED]
  rel-RMS  = 0.000e+00  PASS
  max|Δ|   = 0.000e+00
  eager (3 launches)   : 290368 iters  (24197.0 sub-chains/s)
  megakernel (1 coop)  : 294320 iters  (24525.9 sub-chains/s)

$ bash util_megastep.sh   (external nvidia-smi sampler, 15s each, SAME sub-chain)
=== util sustained-loop on the SAME GEMM->gelu->GEMM sub-chain (15s each) ===
  eager (3 launches)     util: MEAN 95.4%  PEAK 100%  (n=63)
  megakernel (1 coop)    util: MEAN 95.3%  PEAK 100%  (n=64)

pod 39489512 destroyed (confirmed), registry status=closed, leak 0.

────────────────────────────────────────────────────────────────────────────────
FINDING  (honest — SKELETON, scoped)
────────────────────────────────────────────────────────────────────────────────
skeleton compiles+runs : YES  (nvcc -rdc=true sm_90, 1.36 MB binary, both shapes run)
[MEGASTEP-FIRED]       : YES  (cudaLaunchCooperativeKernel + grid.sync() fired, both shapes)
rel-RMS on sub-chain   : 0.000e+00  PASS  (in fact byte-EXACT, max|Δ|=0 — stronger than the
                          3e-3 TF32 bar; the inline own-GEMM device fn produces the IDENTICAL
                          per-tile WMMA2 result as the eager __global__, and the strided tile
                          visitation in the cooperative grid did NOT perturb any element.)
util eager -> mega     : 95.4% -> 95.3%  (Δ = -0.1pp, within sampler noise)

THE STRUCTURE WORKS, the util falsifier is NOT satisfied ON THIS SUB-CHAIN — and the
reason is the honest core finding:

  (a) STRUCTURAL SUCCESS. A persistent grid-resident megakernel with INLINE own-GEMM
      runs GEMM->gelu->GEMM in ONE cooperative launch, byte-EXACT vs the eager own-GEMM
      path. This DIRECTLY realizes what F-FUSION-MEGAKERNEL-DESIGN ruled out for the FP64+
      cuBLAS workload: own-GEMM (#2704 WMMA2) lets the persistent kernel call OUR GEMM
      inline (cuBLAS cannot be nested), and the TF32 own-GEMM-vs-own-GEMM comparison holds
      byte-eq. The cooperative grid is co-resident (1 block/SM × 132 SM, occ query no
      error) — NO register/smem over-subscription wall at this tile size. grid.sync()
      barriers correctly serialize the 3 dependent phases; H1/G stay device-resident
      across the barriers (no HBM->host round-trip, no relaunch gap). FALSIFIER axes
      (a)/(b)/(c) — co-residency, [FIRED], rel-RMS — all PASS.

  (b) UTIL falsifier NOT satisfied here (axis d): 95.4% -> 95.3% (no gain). The reason is
      DIAGNOSTIC, not a defect: this 3-op sub-chain is two ~2048-class square GEMMs back-
      to-back, which ALREADY saturate the H100 (95% util, peak 100%) under BOTH the eager
      3-launch path AND the megakernel. The launch-gap idle the megakernel is designed to
      reclaim is NEGLIGIBLE relative to the GEMM duration at this size — there is simply no
      idle to recover, so collapsing the gaps buys nothing. The megakernel is also ~8%
      SLOWER in raw throughput at 1024×1536 (934 vs 1013 sub-chains/s) because the
      cooperative strided-tile schedule is less efficient than per-GEMM-shaped grid launches
      (one fixed 132-block grid must cover two differently-shaped GEMMs by striding, vs
      eager's two tightly-sized launches). NOTE the small-odd shape REVERSES this (mega
      24525 > eager 24197 sub-chains/s): when each GEMM is tiny, launch overhead dominates
      and collapsing 3 launches->1 wins — the FIRST direct evidence that the gap-collapse
      pays off precisely in the small-kernel regime.

  => The util-raising payoff the megakernel targets lives in the FULL clm_prod step's
     SMALL HETEROGENEOUS kernels (sub-ms norm / elementwise / MoE glue that under-fill the
     H100, median util 2% per F-FUSION-OCCUPANCY-WALL) — NOT in a saturated back-to-back
     GEMM pair. The small-odd-shape reversal is the in-skeleton witness of that mechanism.
     This 3-op sub-chain is the wrong size to SHOW the util win, but the RIGHT size to
     PROVE the structure runs byte-exact. Honest: skeleton works (structure ✓, byte-eq ✓,
     fired ✓); util-win unproven on this sub-chain (saturated), with directional evidence
     (small-shape reversal) that the win is in the small-kernel regime of the full step.

what remains for the full step (NOT done — this is a SKELETON):
  (i)   chain ALL fwd GEMMs + both glue blocks; groupnorm's cross-thread (cross-block)
        reduction needs the SAME grid.sync() barrier the skeleton already demonstrates.
  (ii)  the bwd DAG + ce_grad + AdamW phases (more grid.sync()-separated phases).
  (iii) keep more intermediates in shared where a phase's tile fits (the skeleton keeps
        H1/G in device scratch — correct for cross-phase tile reindex, but the full step
        can fuse adjacent same-tile ops in shared).
  (iv)  register/smem budget: occ here is 1 block/SM (132 resident). Adding more phases /
        more register accumulators may drop occupancy below the co-residency floor — the
        driver's cudaOccupancyMaxActiveBlocksPerMultiprocessor query is the gate that bounds
        how much of the step fits in one cooperative launch before the grid stops being
        co-resident (the megakernel's hard structural limit).
  (v)   the util A/B must be re-run on a sub-chain with SMALL heterogeneous kernels (the
        small-odd reversal predicts the win appears there) to actually MEASURE the util Δ
        the full megakernel is for.

blockers hit: NONE that blocked the skeleton. Cooperative launch co-resident (occ query no
  error), grid.sync() works, no register/smem wall at this tile size. The only "negative" is
  that the chosen sub-chain is GEMM-saturated and so cannot exhibit the util win — a
  measurement-scope limitation of the witness, documented above, not a structural blocker.
