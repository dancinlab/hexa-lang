F-FUSION-BATCHFILL — batch-fill THROUGHPUT (samples/s) sweep: the REAL ~1.3× lever
═══════════════════════════════════════════════════════════════════════════════════
STATUS    : 🟢 CONFIRMED — batch-fill REACHES (and blows past) 1.3× flame self-throughput.
            ≥1.3× hit at B=2 (1.504×); curve climbs to 2.95× at B=32. The interpreted
            per-position glue (∝ B·Tw) progressively eats the gains and is the cap.
DATE      : 2026-06-07
BRANCH    : domain/hexa-fusion-batchfill (off origin/main)
HARDWARE  : NVIDIA H100 80GB HBM3 (SXM), 81559 MiB, driver 580.159.03 — vast instance
            39893326 (offer 36371401, Washington US). GENUINE idle H100 SXM (nvidia-smi
            confirmed). Pod DESTROYED after capture (tag hexa-batchfill verified), leak 0.
SOFTWARE  : flame clm_prod_gpu (764544 bytes) rebuilt FROM CURRENT clm_prod.hexa (the M5
            CLM_PROD_BATCH data-batch lever wired, 4 occurrences in source). Build =
            hexa build --c-only (HEXA_MAC_BUILD_OK bypass) → post-process 8 forge_dispatch
            hexa_callN→direct + weak stubs for the 6 L1/L3 fusion dispatches absent from
            this P1 runtime_cuda.c (→ trainer takes the byte-eq HOST eager fallback, exact
            M5 semantics) → nvcc -DHEXA_CUDA compute_90 runtime_cuda.o (58/58 launchers) →
            gcc link. FP64 cuBLAS forge_dispatch_matmul GEMM path, device-resident
            (CLM_PROD_DEVRESIDENT+DEVFEED+BATCHED=1, HEXA_CUDA_ASYNC=0). Smoke B=1/B=4 PASS.

FALSIFIER (pre-registered)
──────────────────────────
"On a real H100/H200, the flame CLMConvMoE trainer swept over BATCH ∈ {1,2,4,8,16,32,64}
 at D1536/T512/E2/K3 reaches a THROUGHPUT (samples/sec) ≥ 1.3× its OWN batch=1 baseline
 at some batch — growing the batch FILLS the 132 SMs and amortizes the per-step overhead."
 throughput(samples/s) ≡ B / per-step-slope   [2-point timed, init/corpus-load subtracted]

RESULT: 🟢 CONFIRMED. ≥1.3× is reached already at B=2 (1.504×) and the curve keeps
 climbing to 2.95× at B=32 — the throughput lever is REAL. The vs-PyTorch verdict's
 prediction "fill alone can't close the gap" is CORRECT for the ~1656× TORCH gap (a
 different question), but for flame's OWN samples/s the fixed per-step overheads
 (kernel-launch fixed cost, the d2h metric readback, one-time corpus-load intercept)
 amortize across B for a genuine >2.9× self-speedup before the per-position glue caps it.

════════════════ BATCH × samples/s × util TABLE (verbatim stdout) ════════════════
 method: per-B 2-point timing — wall(short leg) & wall(long leg), per-step slope =
 (wall_hi-wall_lo)/(steps_hi-steps_lo), samples/s = B/slope (corpus-load intercept
 cancels in the slope). util = nvidia-smi @0.2-0.25s over the long leg.

  B    T_eff   wall_lo   wall_hi   samples/s   ×B=1     util mean/med/peak
  ---  -----   -------   -------   ---------   ------   ------------------
  1    512     26.123    60.474    0.1747      1.000×   10.4 / 0 / 100   (n=267)
  2    1024    25.611    71.271    0.2628      1.504×   14.9 / 0 / 100   (n=314)
  4    2048    25.930    94.448    0.3503      2.005×   21.2 / 0 / 100   (n=417)
  8    4096    41.002   156.178    0.4168      2.386×   26.9 / 0 / 100   (n=691)
  16   8192    74.12    141.0      0.4785      2.739×   30.0 / 0 / 100   (n=510)
  32   16384   69.71    131.71     0.5161      2.954×   in-GEMM 100% bursts
  64   32768   — UNMEASURABLE — a single M=32768 step exceeds many minutes (the O(B·Tw)
                interpreted host glue runs away); 1+2-step 2-point leg did not complete in
                a 20-min bound. This non-completion IS the cap signal (glue ∝ B·Tw).

  Verbatim raw lines:
   [SWEEP] 1  512   26.123 60.474 0.1747 1.000x 10.4/0/100  epoch-1 mean CE = 3.69702
   [SWEEP] 2  1024  25.611 71.271 0.2628 1.504x 14.9/0/100  epoch-1 mean CE = 4.11772
   [SWEEP] 4  2048  25.930 94.448 0.3503 2.005x 21.2/0/100  epoch-1 mean CE = 4.25875
   [SWEEP] 8  4096  41.002 156.178 0.4168 2.386x 26.9/0/100 epoch-1 mean CE = 4.14786
   [BIG]   16 8192  74.12 141.0 0.4785 30.0/0/100
   [B32]   B32 T_eff=16384 wall1=69.71(s=1) wall2=131.71(s=2) slope=62.00s/step sps=0.5161

≥1.3× reached at batch : B=2 (1.504×) — and stays ≥1.3× for every larger B.
saturation point       : throughput is MONOTONE-INCREASING through B=32 (0.1747→0.5161
                         samples/s) but with sharply DIMINISHING marginal returns per
                         doubling: +0.50× (B1→2), +0.50× (2→4), +0.38× (4→8), +0.35×
                         (8→16), +0.21× (16→32). The marginal gain is collapsing toward 0
                         — extrapolated asymptote ≈ 3× self-speedup. util MEAN climbs
                         10.4%→30.0% (B1→16) — the SM-fill is real — but MEDIAN stays 0%
                         (the bimodal {100% in-GEMM, 0% in interpreted glue} duty cycle of
                         M5/vs-PyTorch is unchanged: batch makes the GEMM burst taller, not
                         the device busier between bursts).
byte-eq (gate 1)       : DETERMINISM max|Δ|=0 — B=1 run-A vs run-B bit-identical CE
                         (epoch-1 4.18549 / epoch-3 3.10644, diff EMPTY). B=1 IS the exact
                         prior un-batched path (nbatch=nwin, one window/step). SEAM scope:
                         B>1 concatenates B windows into M=B·Tw; the ONLY value difference
                         vs B summed-separate is the K-1 causal-conv seam at the B-1 window
                         boundaries (B=8: CE 4.80456/3.52573 vs B=1 4.18549/3.10644 — the
                         documented seam perturbation; GEMM shapes IDENTICAL, both descend).
                         → max|Δ|=0 HOLDS for B=1-vs-prior; B>1 carries the known seam-only
                         Δ (NOT bit-identical — structural to this concat scheme, as M5
                         documented and the vs-PyTorch sibling predicted). HONEST: a true
                         max|Δ|=0 batched step needs a per-window-segmented causal conv (no
                         cross-window seam) — a separable change, not done here.

§FINDING — is the real flame speedup lever confirmed?
─────────────────────────────────────────────────────
  🟢 YES. Batch-fill DOES reach ~1.3× flame self-throughput — it blows past it at B=2
  (1.504×) and climbs to 2.95× at B=32 on a real H100 SXM. The data the vs-PyTorch bench
  pointed to is confirmed AS A SELF-SPEEDUP LEVER: batch=1 under-fills the SMs (util mean
  10.4%, median 0%) and the per-step FIXED overheads (launch fixed cost, d2h metric
  readback, one-time corpus-load) are paid ONCE per step regardless of B, so amortizing
  them across B lifts samples/s steeply at first.

  THE CAP (honest, measured): the gain is sub-linear and collapsing. Each batch doubling
  adds less throughput (+0.50× → +0.21× by B16→32) and the per-step WALL explodes
  super-linearly — B=64's single step is unmeasurable in a 20-min bound. The mechanism is
  exactly the vs-PyTorch wall: the INTERPRETED per-position driver glue (the window
  t_get/t_set token-pack = B·Tw scalar bytecode ops, the per-position CE/softmax-grad host
  glue, the per-param eager AdamW tail) scales WITH B·Tw, so the device-side GEMM gets B×
  taller while the host glue grows B× too. Throughput climbs only because the FIXED part of
  the step amortizes; once the per-position glue dominates (B≥16, T_eff≥8192) the curve
  flattens and the wall runs away. The asymptotic ceiling is ≈3× self-speedup, NOT
  unbounded — fill removes the fixed-overhead tax but cannot remove the per-token
  interpreter tax.

  RECONCILIATION with the campaign:
   · M5-BATCHFILL-UTIL (util axis): util MEAN flat ~0.45% B=1→32 — measured a DIFFERENT
     small-D regime + reported MEAN duty-cycle. Here util MEAN climbs 10→30% (B1→16) at
     D1536; both consistent — MEDIAN is 0% in BOTH (the device idles between GEMM bursts).
   · vs-PyTorch (torch gap): fill can't close the ~1656× torch gap because the glue grows
     with B — CONFIRMED. But fill DOES give a 2.95× flame SELF-speedup — a distinct, real,
     positive result on the samples/s metric.
   · The ACTIONABLE lever both verdicts named — interpreter-elimination of the per-step
     driver glue (compile the t_get/t_set window-pack + CE-grad + AdamW tail to native) —
     is what would UNCAP this curve. Batch-fill is the cheap first ~3×; the glue rewrite is
     the rest.

  VERDICT: 🟢 batch-fill is a CONFIRMED ~1.3×-and-beyond flame throughput lever on H100
  (≥1.3× at B=2, 2.95× at B=32, byte-eq determinism max|Δ|=0, CE descent holds), capped at
  ≈3× by the per-position interpreted glue (∝ B·Tw) — the same wall the vs-PyTorch verdict
  located. The real flame speedup lever the data pointed to is CONFIRMED.

ARTIFACTS: bench/vs_pytorch/batch_sweep.sh (2-point throughput sweep) ·
           bench/vs_pytorch/batch_byteeq.sh (determinism + seam-scope probe).
