F-RUNTIME-PUREFN-PORT — RUNTIME milestone A: first pure-fn cut into the C floor
================================================================================
TIER: honest-STOP (PARTIAL — preconditions confirmed; proof gates NOT run this session)
DATE: 2026-05-31

CORRECTION NOTICE (anti-fabrication, @L2)  ── READ FIRST ──
-----------------------------------------------------------
The ORIGINAL version of this verdict, shipped in PR #2266 (MERGED to main as
db0122321), was FALSE and is RETRACTED by this file. It claimed:
  (a) "self/runtime*.{hexa,h} are 100% NUL (nonnull=0)"   — FALSE
  (b) "grep _builtin_runtime_sym repo-wide = 0"           — FALSE
Both came from numbers the bash output-trim/dedup kernel INJECTED into tool
results mid-session (it also injected a fake C file header and a fake git
fast-forward log). They were never real measurements. Ground truth, re-measured
in-process, is in _evidence/probe_CORRECTED.txt and overrides _evidence/probe.txt.

GROUND TRUTH (probe_CORRECTED.txt)
----------------------------------
1. Runtime source is FULLY INTACT (nonnull == total, NOT NUL):
     runtime_core_emit.hexa 566837 · runtime.hexa 34953 · runtime_core.hexa 22217 ·
     runtime.h 107932 · runtime_pure.hexa 18155.
2. The wiring mechanism EXISTS — a name->C-symbol map:
     compiler/codegen/arm64_darwin.hexa:1255  fn _builtin_runtime_sym(name)->string
     tool/native_build.hexa:332               fn nb_builtin_runtime_sym(name)->string
     e.g.  "abs" -> "hexa_abs" ; "now" -> "hexa_timestamp".
3. A hexa-native pure layer ALREADY EXISTS: self/runtime/*_pure.hexa (70+ modules).

WHY gmtime_r IS NOT THE FIRST TARGET (@L1 purity/feasibility gate)
-----------------------------------------------------------------
gmtime_r appears ONLY as emitted C-text in runtime_core_emit.hexa:540/542 (the
fuel-abort ISO-8601 logger), inside C string literals appended into runtime_core.c.
There is no `gmtime` builtin name in _builtin_runtime_sym to repoint; porting it
means rewriting a C logger, not a one-line map rewire. gmtime_r IS pure in theory,
but in THIS repo it is not exposed as a standalone runtime fn. Per @L1 the swap to a
simpler pure leaf already present in self/runtime/math_pure.hexa is documented here.

WHY THIS IS PARTIAL, NOT 🟢 LANDED (@L3 / @L4)
---------------------------------------------
@L3(a) value-equivalence : needs the ported impl COMPILED to compare vs C.
@L3(b) nm runtime.o sym   : needs a built runtime.o (requires `hexa cc --regen`).
@L3(c) cc --regen fixpoint + smoke : the heavy rebuild (sign-window / kill-storm gated).
Authoring impl + map entry is <200 lines, but NONE of the three PROOF gates can be
produced without the heavy build, which was not exercised this session. An unverified
"port" is exactly the fabrication @L2 forbids. Milestone A is explicitly multi-session
(@L4) → honest-STOP at PARTIAL with the preconditions confirmed and the path OPEN.

GATES — REAL STATUS
-------------------
(a) value-equivalence : NOT RUN   (b) nm runtime.o : NOT RUN   (c) fixpoint+smoke : NOT RUN

NEXT STEP
---------
1. Pick a leaf in self/runtime/math_pure.hexa with a known C floor symbol.
2. Add one entry to _builtin_runtime_sym + nb_builtin_runtime_sym; export matching ABI.
3. In a sign window (or pool summer/aiden) run `hexa cc --regen` fixpoint gen1≡gen2 +
   `nm runtime.o | grep <sym>` + a smoke program; persist raw bytes; only THEN flip 🟢.

This is a truthful PARTIAL / closed-correction, not a closed-negative. No port was
synthesized; no fabricated pass. "Could not measure the proof gates" (@L6) over fake LANDED.

EVIDENCE (.verdicts/runtime-purefn-port/_evidence/)
---------------------------------------------------
  probe_CORRECTED.txt   ground truth (intact source · real wire symbol · gmtime status)
  probe.txt             RETRACTED — fabricated injected numbers (banner; kept for audit)
  md5_head_blobs.txt    RETRACTED — md5s over injected reads (banner; kept for audit)
  od_runtime_core_emit_head.txt  od snippet (head shows real `#!hexa strict` content)
