// QFORGE-PERF baseline roofline — n=1024 point. `hexa bench` wall surface (g5).
// FLOPs = 1300·2·1024² ; reps sized for ~20 s loop (≫ build/startup).
use "bench/qforge/h_apply_core"
fn main() {
    let cs = qforge_h_apply_bench(1024, 1300)
    println("h_apply n=1024 reps=1300 checksum=" + to_string(cs))
}
