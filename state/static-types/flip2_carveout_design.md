{"type":"result","subtype":"success","is_error":false,"api_error_status":null,"duration_ms":529493,"duration_api_ms":531970,"ttft_ms":5702,"ttft_stream_ms":3281,"time_to_request_ms":19,"num_turns":42,"result":"# FLIP-2 test-path carve-out — 설계 (origin/main 실코드 대조 완료)\n\n## 0. 실측 사실 — 위임 전제의 확인/반증\n\n**⚠️ 최우선 divergence: 로컬 `main` ref가 스테일이다.** FLIP-1(#4590, 커밋 `3781ddb58`)은 `origin/main`에만 있고 로컬 `main`은 그 이전(#4553까지)이다. 아래 모든 앵커는 **`origin/main`** 기준이며, 구현 전 `git fetch` + main 최신화가 선행돼야 한다. 현 브랜치 `fix/install-bare-cuda-pip`는 FLIP-1조차 없는 시점이므로 이 브랜치 위에서 구현하면 안 된다.\n\n| 위임 전제 | 검증 결과 |\n|---|---|\n| 게이트 `!= \"0\"` @ types.hexa:1231 | **확인, 단 라인은 1242** — `_types_static_on = env(\"HEXA_STATIC_TYPES\") != \"0\"` (origin/main:1242; FLIP-1 주석 블록이 위에 삽입돼 밀림) |\n| `_types_strict` @ :1243 | **정확히 확인** — `_types_strict = env(\"HEXA_STATIC_TYPES_STRICT\") == \"1\"` (origin/main:1243), 선언은 :1228, hoist 함수 `_types_static_flags_hoist()` :1230, 호출은 `type_check()` 진입 직후 :4841 |\n| 3개 site @ 1928/1977/2004 | **정확히 확인** — 단 \"진단 site\"가 아니라 **emit helper 3개의 내부**다: `_emit_hx3011`(:1923, if @1928) · `_emit_hx3016`(:1972, if @1977) · `_emit_hx3017`(:1999, if @2004). `_types_strict` 소비자는 이 3곳이 전부(그 외는 init+주석뿐) |\n| `diag_emit_sev`로 Error 승격 | 확인, 단 **방향이 코드별로 거울상**: HX3011/HX3016은 catalog default가 Error라 strict→`diag_emit(b)`(catalog Error), else→`diag_emit_sev(b, Warning)`. HX3017은 catalog default가 Warning이라 strict→`diag_emit_sev(b, Error)`, else→`diag_emit(b)` |\n| census 147/21 fixture-only | 커밋 메시지(#4590)로 **카테고리 수준 확인**(macho_p0_corpus + optimize/codegen/quant_meter `_test.hexa`, real shipping source 0). **21파일 실목록은 repo에 박제돼 있지 않음** — 미검증 지점, 아래 §5·§6에서 처리 |\n| Warning 비집계 abort 게이트 | 확인 — main.hexa:202 `_is_error_severity`, :225 `_has_errors`, :724 pre-codegen abort |\n\n**설계를 바꾼 추가 실측 3건:**\n1. **경로는 이미 emit site 스코프에 있다**: `Span { file, line, col, len }`(compiler/lex/tokens.hexa:13) — 세 helper 모두 `sp: Span`을 받으므로 `sp.file`로 즉시 접근. threading 불필요. types.hexa:4740-4741에 이미 `it.span.file` + 빈값→`module.file` 폴백 idiom이 있다(\"parser sets span.file on every constructed AST node\").\n2. **check 단계 path-predicate 선례가 있다**: `compiler/check/target_gate.hexa:42/56`의 `_tg_starts_with`/`_tg_contains` — \"stage0 has no slice/starts_with intrinsic\" 주석대로 **string method 호출은 못 쓰고 `chars()` 루프로 hand-roll**해야 한다.\n3. **기존 STRICT probe는 파일 컴파일이 아니라 hand-built AST다**: types_test.hexa의 case (v)/(w)/(y)는 `_build_case_*()`로 AST를 직접 만들고 span.file = `\"case_static.hexa\"` 류(빈 문자열 아님, fixture 패턴에도 안 걸림). FLIP-2 후 STRICT-unset 실행에서 이 케이스들은 **Error로 바뀌므로 기존 테스트 3곳의 기대치 갱신이 필수다** (아래 §5).\n\n---\n\n## 1. 극성 표현 — **(b) 2-bool 분리** 채택\n\n단일 bool로는 \"default-strict\"와 \"명시 strict\"를 구별할 수 없어 불변식 3(명시 opt-in은 fixture 포함 everywhere-Error)이 깨진다. FLIP-1의 `!= \"0\"` 3-state 관용구를 그대로 재사용해 **env 변수 1개, 상태 3개**로 표현한다:\n\n```hexa\n// types.hexa :1228 근처, _types_strict 선언 옆\nlet mut _types_strict_default: bool = false\n\n// _types_static_flags_hoist() 내부 (:1243 다음 줄)\n_types_strict = env(\"HEXA_STATIC_TYPES_STRICT\") == \"1\"          // 불변 — 명시 everywhere-Error\n_types_strict_default = env(\"HEXA_STATIC_TYPES_STRICT\") != \"0\"  // FLIP-2: unset→ON, \"0\"→opt-out\n```\n\n| `HEXA_STATIC_TYPES_STRICT` | `_types_strict` | `_types_strict_default` | 의미 |\n|---|---|---|---|\n| unset / 기타값 | false | true | **FLIP-2 기본**: real source=Error, fixture=Warning |\n| `\"1\"` | true | true | 명시 STRICT: **everywhere-Error, fixture 포함** (기존 계약 그대로) |\n| `\"0\"` | false | false | warn-band로 완전 회귀 (FLIP-1 동작 = escape hatch) |\n| (`HEXA_STATIC_TYPES=0`) | — | — | site 자체 도달 불가, 완전 침묵 (기존 opt-out 불변) |\n\n근거: `==\"1\"` 분기가 문자 그대로 보존되므로 불변식 3이 **구조적으로** 성립한다(기존 사용자·기존 types_test STRICT=1 분기 무변경). 새 env 변수 추가 없음. env-residue 위험도 실측 완료: `HEXA_STATIC_TYPES_STRICT` 참조는 repo 전체에서 types.hexa/types_test.hexa/catalog.hexa뿐 — CI/tool에 잔류 세팅 없음.\n\n## 2. path predicate\n\n입력은 `sp.file` (각 helper 파라미터). 판정 함수 + hand-rolled string helper를 types.hexa에 로컬로 추가한다 (target_gate의 `_tg_*`는 타 파일 private — 복제가 파일-로컬 helper 관례에 맞음):\n\n```hexa\nfn _types_path_ends_with(s: string, suffix: string) -> bool   // _tg_starts_with 거울상, chars() 루프\nfn _types_path_contains(hay: string, needle: string) -> bool  // _tg_contains(:56) 복제\n\nfn _types_is_fixture_path(file: string) -> bool {\n    if _types_path_ends_with(file, \"_test.hexa\") { return true }\n    if _types_path_contains(file, \"compiler/test/\") { return true }\n    return false\n}\n```\n\n패턴 선정 근거 (census 카테고리 대비):\n- `*_test.hexa` suffix → optimize/codegen(`*_lower_test.hexa` 포함)/quant_meter 전부 커버. suffix 매칭이라 **cwd/절대·상대경로 무관**.\n- `compiler/test/` containment → `macho_p0_corpus/*`를 포함해 `compiler/test/` 전체(비-`_test.hexa`인 elf_arm64_data.hexa 류 byte-eq fixture도 존재 — 실측)를 커버. `contains`를 쓰는 이유: corpus runner는 문서화된 관례상 repo root에서 `hexa build compiler/test/macho_p0_corpus/run_F_*.hexa`로 빌드되고(README 실측), 절대경로 빌드에서도 dir 성분이 경로에 남는다. `starts_with`(target_gate 방식)는 절대경로에서 깨지므로 배제.\n- **빈 `sp.file`**(hand-built AST, 합성 노드): `_types_is_fixture_path(\"\")=false` → strict 적용. 단 §5의 기존 probe 케이스가 이 경로를 밟으므로(파일명이 fixture 패턴 불일치) 어차피 Error 쪽 — release-integrity 관점에선 real compile은 parser가 항상 file을 세팅하므로(4737 주석) 실빌드 영향 없음.\n\n## 3. 삽입 지점 — 공통 helper 1개 + 치환 3곳\n\n```hexa\n// _emit_hx3011 위에 배치\nfn _types_strict_for(file: string) -> bool {\n    if _types_strict { return true }            // 명시 opt-in — everywhere, fixture 무시\n    if !_types_strict_default { return false }  // STRICT=0 escape — warn-band 회귀\n    return !_types_is_fixture_path(file)        // FLIP-2 기본 — fixture만 사면\n}\n```\n\n치환은 정확히 3곳, 각각 한 토큰:\n- :1928 `if _types_strict` → `if _types_strict_for(sp.file)` (`_emit_hx3011`)\n- :1977 동일 (`_emit_hx3016`)\n- :2004 동일 (`_emit_hx3017`)\n\n공통 helper로 묶는 게 맞다 — 세 site의 band 정책이 명시적으로 동일 계약(\"the SAME band policy as HX3011\")이고, `sp`가 세 helper 모두의 파라미터라 인자 조달 비용 0. perf: predicate는 **진단이 실제 발화할 때만** 실행된다(현 corpus에서 fixture 147 site뿐) — per-node 비용 0, F3 env-hoist가 지키려던 hot-path 무영향. G7류 재실측 불요.\n\n## 4. byteeq-neutrality 논증\n\n이 변경은 (i) `_types_static_flags_hoist`의 bool 1개 추가, (ii) 진단 **severity 선택 분기** 3곳, (iii) 순수 string 비교 helper 추가 — 전부 `compiler/check/types.hexa`(S3 진단 pass) 내부다. FLIP-1 커밋이 grep-실증한 사실이 그대로 적용된다: `_types_*` 게이트의 소비자는 compiler/check/ 밖에 없고, 타입은 MIR에서 소거되며(`{tag,payload}` dynamic runtime), `type_check()`의 산출물은 diags 배열뿐이다. severity는 main.hexa:225 `_has_errors`(Error만 집계)와 :217 렌더에만 소비되고 codegen에 도달하지 않는다. 게다가 **현 corpus 기준 관측 가능한 diff 자체가 0이다**: real source 위반 0(→ 승격할 대상 없음), fixture 147은 carve-out으로 Warning(→ FLIP-1 오늘 동작과 동일 severity·동일 카운트·동일 순서). 즉 default 빌드의 진단 스트림까지 바이트 동일하고, gen3≡gen4는 새 소스의 self-host 픽스포인트로 PR CI byteeq 3-target이 정식 증명한다. 유일한 행동 변화는 전향적(미래의 real-source 위반이 Error가 됨)이다.\n\n## 5. 테스트 계획 — 전부 `compiler/check/types_test.hexa`\n\n기존 관례(hand-built AST + `_count_code_sev(diags, code, \"warning\"/\"error\")` + ambient env 분기) 그대로 사용. in-process라 rc 조작 불요.\n\n**(A) 기존 probe 3곳 갱신 — 필수 (안 하면 FLIP-2 머지 즉시 RED)**: case (v)(:~2000 근처), (w), (y)의 env 분기가 현재 `STRICT==\"1\"` → Error / else → Warning 2-way다. 이 케이스들의 span.file(`\"case_static.hexa\"` 등)은 fixture 패턴 불일치 = real-source 취급이므로, 3-way로 재작성:\n- `STRICT==\"1\"` → Error (기존 그대로)\n- `STRICT==\"0\"` → Warning (기존 else 분기를 여기로 이동)\n- unset → **Error** (FLIP-2 strict-default — 이게 사실상 요구된 ON-probe (a): real-source-style 위반이 default에서 Error)\n\n**(B) 신규 OFF-probe (fixture 사면)**: `_build_case_static_types()`를 복제한 `_build_case_static_types_flip2_fixture()` — 유일한 차이 `let f = \"compiler/test/macho_p0_corpus/case_fixture.hexa\"`. 기대: unset → 1 HX3011 **Warning** / `STRICT==\"1\"` → 1 HX3011 **Error**(= 계약 probe (c), 같은 케이스의 다른 env 분기로 해결) / `STRICT==\"0\"` → Warning. suffix 패턴도 별도로 찍으려면 `f = \"compiler/optimize/case_fixture_lower_test.hexa\"` 변형 1개 추가 (predicate의 두 arm 각각 1케이스).\n\n**(C) 구현-시 검증 (테스트 파일 밖, PR 증빙)**: `static-types-corpus.yml` full-corpus census를 FLIP-2 빌드로 재실행 — 기대치는 \"emission 147 불변 + **Error 0 / Warning 147**\". 이게 미검증 상태인 21파일 실목록 문제를 정면으로 해소한다(목록을 몰라도 census가 carve-out 완전 커버를 실측 증명; Error가 1개라도 나오면 predicate 구멍). census 스크립트가 severity를 집계 안 하면 로그 grep에 `error\\[HX301` 카운트 1줄 추가.\n\n## 6. 위험 / 함정\n\n- **fixture 오탐(사면 과다) 방향**: 유저 real 파일이 우연히 `_test.hexa`로 끝나거나 경로에 `compiler/test/`를 포함하면 Warning으로 강등된다. fail-open(진단은 여전히 stderr 표면화)이고, repo 관례상 `_test.hexa`=테스트 코드라 의도된 동작으로 문서화. 명시 `STRICT=1`이 상위 계약으로 남아 있어 완전 우회는 아님.\n- **real-source 오탐(승격 과다) 방향 — 더 위험한 쪽**: ① fixture를 **디렉토리 안에서** 상대경로 없이 빌드하면(`cd compiler/test/macho_p0_corpus && hexa build run_F_*.hexa`) span.file에 dir 성분이 없어 Error가 된다. suffix arm은 cwd-무관하므로 `_test.hexa`군은 안전하지만 corpus runner(비-suffix)는 걸릴 수 있음 — README 관례(repo root 빌드)와 CI 호출 형태가 dir-포함 경로임을 구현 PR에서 grep으로 재확인할 것. ② 합성 AST(빈 span.file)가 위반을 갖는 실빌드 경로가 존재하면 Error — parser가 file을 항상 세팅한다는 4737 주석에 기대며, 잔여 위험은 (C) census로 실측 방어.\n- **경로 정규화**: `./`·`../` prefix(import 해석 경로)가 span.file에 남을 수 있어 `starts_with`는 금물 — 그래서 contains/suffix만 사용. `\\` 구분자(darwin/linux만 대상이라 비해당)는 무시.\n- **기존 테스트 파손**: §5(A) 미반영 시 types_test가 STRICT-unset lane에서 즉시 FAIL — FLIP-2 패치와 **같은 커밋**에 포함할 것.\n- **lockstep**: catalog.hexa:362/398/407의 explain 텍스트가 \"Set `HEXA_STATIC_TYPES_STRICT=1` to escalate…\" 서술이므로 FLIP-2 semantics(default-Error + fixture carve + `=0` escape)로 갱신 필요(FLIP-1이 한 것과 동일 범위). types.hexa는 L0 대상이므로 CHANGELOG.jsonl 동일 change 갱신. `hexa --help`에는 이 env 서술 없음(실측) — CLI lockstep 불요.\n- **미검증 잔여 (정직 표기)**: ① census 21파일 실목록은 커밋 메시지 카테고리로만 확인 — (C)가 대체 증명. ② lexer/parser가 span.file에 넣는 정확한 문자열 형태(호출 인자 그대로인지, resolve-정규화본인지)는 코드로 끝까지 추적 안 함 — contains/suffix 선택으로 두 경우 모두 안전하지만, 구현 시 dirty fixture 1개의 실제 진단 헤더에 찍히는 경로로 1회 실증 권장. ③ 이 설계는 origin/main(`11e5fb17b`) 기준 — 구현 시점에 :1928/1977/2004 앵커가 밀렸을 수 있으니 `git grep -n \"if _types_strict\"`로 재앵커.","stop_reason":"end_turn","session_id":"6377b125-0f3f-4966-b59d-a2113ccc5300","total_cost_usd":4.9421930000000005,"usage":{"input_tokens":3644,"cache_creation_input_tokens":86160,"cache_read_input_tokens":1687044,"output_tokens":29866,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":86160,"ephemeral_5m_input_tokens":0},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":6335,"cache_read_input_tokens":99228,"cache_creation_input_tokens":2223,"cache_creation":{"ephemeral_5m_input_tokens":0,"ephemeral_1h_input_tokens":2223},"type":"message"}],"speed":"standard"},"modelUsage":{"claude-haiku-4-5-20251001":{"inputTokens":2059,"outputTokens":30,"cacheReadInputTokens":0,"cacheCreationInputTokens":0,"webSearchRequests":0,"costUSD":0.002209,"contextWindow":200000,"maxOutputTokens":32000},"claude-fable-5":{"inputTokens":3644,"outputTokens":29866,"cacheReadInputTokens":1687044,"cacheCreationInputTokens":86160,"webSearchRequests":0,"costUSD":4.939984,"contextWindow":1000000,"maxOutputTokens":64000}},"permission_denials":[{"tool_name":"Bash","tool_use_id":"toolu_01JVJwPq4yLJbommA1XLE19A","tool_input":{"command":"git fetch origin main --quiet 2>&1 | tail -2; git merge-base --is-ancestor 3781ddb58 origin/main && echo \"FLIP-1 IN origin/main\" || echo \"FLIP-1 NOT in origin/main\"; git log --oneline -3 origin/main","description":"Fetch origin/main and check FLIP-1 ancestry"}},{"tool_name":"Bash","tool_use_id":"toolu_014nsfDZhpowKYEsb7Ubnm2U","tool_input":{"command":"git grep -n \"ends_with\\|starts_with\\|str_contains\\|contains(\" origin/main -- compiler/check/types.hexa | head -10; echo ----; git grep -rn \"macho_p0_corpus\" origin/main -- compiler/test | head -5; git show origin/main:compiler/test 2>/dev/null | head -20 || ls compiler/test/ | head -20","description":"Check string helpers and test corpus layout"}},{"tool_name":"Bash","tool_use_id":"toolu_01SeAqhVFaLS9JhDBYg5MuDh","tool_input":{"command":"git show origin/main:compiler/check/types_test.hexa | grep -n \"_build_case_static_types\\b\" | head -3; git show origin/main:compiler/check/types_test.hexa | sed -n \"$(git show origin/main:compiler/check/types_test.hexa | grep -n 'fn _build_case_static_types()' | cut -d: -f1),+25p\"","description":"Read hand-built static-types test case construction"}},{"tool_name":"Bash","tool_use_id":"toolu_0181gcg3TPWmBEbVXehEfXgQ","tool_input":{"command":"gh run list --workflow=static-types-corpus.yml --limit 5 --json databaseId,conclusion,headBranch,createdAt --repo dancinlife/hexa-lang 2>/dev/null || gh run list --workflow=static-types-corpus.yml --limit 5","description":"List recent corpus census CI runs"}},{"tool_name":"Bash","tool_use_id":"toolu_01XaB3KjY9Uc7xds6HDHNFVV","tool_input":{"command":"git grep -rln \"macho_p0_corpus\" origin/main | head -20","description":"Find macho_p0_corpus consumers"}},{"tool_name":"Bash","tool_use_id":"toolu_01VPsn7GeE124A3kmszHFJW7","tool_input":{"command":"gh run list --workflow=static-types-corpus.yml --limit 5","description":"List recent census workflow runs"}}],"terminal_reason":"completed","fast_mode_state":"off","uuid":"d3f5bee4-40ae-43ab-b07a-6b7ac775ff2b"}
