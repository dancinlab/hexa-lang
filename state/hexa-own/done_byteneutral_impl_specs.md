# done-byteneutral-impl-specs (clean ref 9d1e4d3c7·verify-confirmed)

{
  "summary": "Produce exact implementation specs for 3 independent byte-neutral DONE-campaign rounds (axis-② strcmp caller-drop via hxlcl_strcmp, §3.1 arm64-linux native cold-path de-hardcode, regex OFF/ON parity corpus) from a clean origin/main worktree",
  "agentCount": 6,
  "logs": [],
  "result": {
    "ref_sha": "9d1e4d3c7",
    "specced": 3,
    "rounds": [
      {
        "round": "axis-② strcmp-caller-drop — new default-OFF caller gate HEXA_RT_RANGE_STRCMP_NATIVE (redirect raw range_field strcmp callers to native hxlcl_strcmp; drop the strcmp libc nm-UND in the no-runtime.c config)",
        "head_confirmed": "9d1e4d3c7",
        "ready": true,
        "edits_apply": true,
        "confidence": "high",
        "corrections": [
          "Descriptive-only (does not affect edit application): hxlcl_strcmp's body is a byte-loop (unsigned-char compare per shim_emit.hexa:455-462 and runtime_emit_full.hexa:438-445), not a 'word-scan' as the spec repeatedly labels it.",
          "Descriptive-only: shim_emit.hexa:455 is the HEXA_ZEROC_SHIM_BYTEID_EMIT byte-identical body nested inside `#ifndef HEXA_RT_NATIVE_STRCMP` (:451) — it is NOT the native-strcmp resolver path itself. The real HEXA_RT_NATIVE_STRCMP path compiles the entire shim block (455-467, incl. the :464 `return strcmp` fallback) OUT and supplies hxlcl_strcmp from a self-emitted .o. The drop requirement (':464 must be compiled out for the ON nm-drop to complete') is still correct.",
          "Descriptive-only: the amalgam ordering citation '(see :373-374)' points at the F3-slot comment region; the actual global `#define strcmp(a,b) hxlcl_strcmp(...)` is at runtime_emit_full.hexa:2863 (its own comment at :2858+), emitted before `#include \"runtime_core.c\"` at :3018. The before-include claim holds."
        ],
        "edits": [
          "CORRECTION TO SCOPE (load-bearing): the lever is hxlcl_strcmp (NOT rt_strcmp — confirmed, rt_strcmp exists only under #ifdef HEXA_ZEROC_RT_CORE_STRCMP_DELEGATE). hxlcl_strcmp authoritative external-linkage def = self/runtime_core_hxlcl_shim_emit.hexa:455 (native word-scan, active under the native-strcmp resolver path) / :464 (`return strcmp` libc fallback — MUST be compiled out for the drop). Of the 7 cited sites, only 4 hold REAL libc strcmp: runtime_emit.hexa:1052/1053/1057 (HI-tier no-runtime.c seed; emits `#include <string.h>` at :426 and has NO `#define strcmp`) and runtime_core_emit.hexa:4253 (real when runtime_core.c is compiled standalone). runtime_emit_full.hexa:5622/5623/5627 (amalgam self/runtime.c) are ALREADY native — the global `#define strcmp hxlcl_strcmp` at amalgam-buf-line 2863 (emitted before `#include \"runtime_core.c\"`, see :373-374) rewrites them to the amalgam's static word-scan hxlcl_strcmp (:438); they carry ZERO libc strcmp UND, so they get NO edit (an extern there would collide with the static def at :438, and the file emits runtime.c VERBATIM/frozen 151c52c8).",
          "EDIT 1 — self/runtime_emit.hexa:1048 (INSERT the default-OFF gate block, with an ON-only extern, between the `// hexa_range_field` comment and the function; extern is safe+robust here because this seed .o is compiled STANDALONE, hxlcl_strcmp is external-linkage, and a compatible redeclaration is legal). OLD (two lines 1048-1049):\n    buf = buf + \"// hexa_range_field\\n\"\n    buf = buf + \"HexaVal hexa_range_field(HexaVal v, const char* key) {\\n\"\nNEW:\n    buf = buf + \"// hexa_range_field\\n\"\n    buf = buf + \"#ifdef HEXA_RT_RANGE_STRCMP_NATIVE  /* default-OFF caller-drop: route range key-cmp to native hxlcl_strcmp (word-scan); drops libc strcmp UND. OFF => hx_range_strcmp expands to strcmp, byte-identical */\\n\"\n    buf = buf + \"extern int hxlcl_strcmp(const char *a, const char *b);\\n\"\n    buf = buf + \"#define hx_range_strcmp hxlcl_strcmp\\n\"\n    buf = buf + \"#else\\n\"\n    buf = buf + \"#define hx_range_strcmp strcmp\\n\"\n    buf = buf + \"#endif\\n\"\n    buf = buf + \"HexaVal hexa_range_field(HexaVal v, const char* key) {\\n\"",
          "EDIT 2 — self/runtime_emit.hexa:1052. OLD:\n    buf = buf + \"    if (strcmp(key, \\\"len\\\") == 0) return hexa_int(n);\\n\"\nNEW:\n    buf = buf + \"    if (hx_range_strcmp(key, \\\"len\\\") == 0) return hexa_int(n);\\n\"",
          "EDIT 3 — self/runtime_emit.hexa:1053. OLD:\n    buf = buf + \"    if (strcmp(key, \\\"start\\\") == 0) {\\n\"\nNEW:\n    buf = buf + \"    if (hx_range_strcmp(key, \\\"start\\\") == 0) {\\n\"",
          "EDIT 4 — self/runtime_emit.hexa:1057. OLD:\n    buf = buf + \"    if (strcmp(key, \\\"end\\\") == 0) {\\n\"\nNEW:\n    buf = buf + \"    if (hx_range_strcmp(key, \\\"end\\\") == 0) {\\n\"",
          "EDIT 5 — self/runtime_core_emit.hexa:4253 (per-line #ifdef, OFF branch VERBATIM = current line; NO extern — hxlcl_strcmp is already declared via runtime_core_sysheaders.h:65 and used earlier at :976/:3667/:4815; an extern here is FORBIDDEN because this file is #included into the amalgam where hxlcl_strcmp is static at runtime_emit_full.hexa:438). OLD (single emit line):\n    buf = buf + \"        (strcmp(key, \\\"start\\\") == 0 || strcmp(key, \\\"end\\\") == 0 || strcmp(key, \\\"len\\\") == 0)) {\\n\"\nNEW (replace with 5 emit lines):\n    buf = buf + \"#ifdef HEXA_RT_RANGE_STRCMP_NATIVE\\n\"\n    buf = buf + \"        (hxlcl_strcmp(key, \\\"start\\\") == 0 || hxlcl_strcmp(key, \\\"end\\\") == 0 || hxlcl_strcmp(key, \\\"len\\\") == 0)) {\\n\"\n    buf = buf + \"#else\\n\"\n    buf = buf + \"        (strcmp(key, \\\"start\\\") == 0 || strcmp(key, \\\"end\\\") == 0 || strcmp(key, \\\"len\\\") == 0)) {\\n\"\n    buf = buf + \"#endif\\n\"",
          "NO EDIT — self/runtime_emit_full.hexa:5622/5623/5627 (amalgam). Already native via the global `#define strcmp hxlcl_strcmp` at :2863; editing is a byte-neutral no-op with zero floor value and would disturb the VERBATIM 151c52c8 emit. Leave verbatim.",
          "OPTIONAL hardening (omittable — hx_range_strcmp is a unique token used only in the 3 range_field lines): after the hexa_range_field closing `}` in runtime_emit.hexa, emit `buf = buf + \"#undef hx_range_strcmp\\n\"` to scope the alias. Harmless to skip.",
          "FOLLOW-UP ROUND (NOT this PR): the default-ON flip adds `-DHEXA_RT_RANGE_STRCMP_NATIVE` in tool/stage_resolve_runtime_a (mirroring the existing native gates), coupled to the native-strcmp shim path (HEXA_RT_NATIVE_STRCMP / SELFEMIT so shim_emit.hexa:464 `return strcmp` is compiled out). This round ships default-OFF only."
        ],
        "gate": [
          "byteeq 3-target GREEN (x86_64-linux + arm64-linux + darwin-arm64): default-OFF => every OFF token-stream is identical to today (runtime_emit/standalone runtime_core: hx_range_strcmp/`#else`-branch => strcmp; amalgam-include path: strcmp => 2863 => hxlcl_strcmp), and #ifdef/#define directives emit no code, so runtime.a is byte-identical. Run per protocol on aiden/summer (mini = git/gh only).",
          "faithful nm-DROP (existence proof) in the no-runtime.c / SELFEMIT config with the gate ON AND the native-strcmp shim active (HEXA_RT_NATIVE_STRCMP / resolver 'auto'->native seed present, so hxlcl_strcmp's own body is word-scan, NOT the :464 `return strcmp` fallback): `nm -u build/runtime.a | grep -w strcmp` MUST return empty, with ZERO new libc UND introduced (hxlcl_strcmp is defined in-archive: shim.o :455 or the native hxlcl_strcmp .o). Capture before/after floor count. CAVEAT to assert in the run: if the resolver falls back to the libc-shim strcmp (bad/missing seed), the drop does NOT complete — strcmp merely relocates into the hxlcl_strcmp def; the ON measurement is only valid on the native-shim (all-levers-ON) config.",
          "pool: heavy faithful/byteeq builds on self-hosted aiden/summer (linux-x86_64/arm64); darwin-arm64 byteeq on cloud. Default-OFF PR is byte-neutral/ungated-mergeable; the default-ON flip is the pool-gated follow-up round with install.sh consumer smoke 3/3 (never promote on x86-only)."
        ],
        "byte_neutral_arg": "Default-OFF (no -DHEXA_RT_RANGE_STRCMP_NATIVE) makes every edit compile to the exact same tokens as today. runtime_emit.hexa: `hx_range_strcmp` macro-expands to `strcmp`, so `hx_range_strcmp(key,\\\"len\\\")` => `strcmp(key,\\\"len\\\")` — identical to the current line; the ON-only `extern`/`#define hxlcl_strcmp` lines live inside the untaken `#ifdef` and are stripped by the preprocessor. runtime_core_emit.hexa:4253: the `#else` branch is a byte-for-byte copy of the current line, so standalone-core keeps `strcmp` (identical) and the amalgam-include path keeps `strcmp` which the pre-existing :2863 `#define strcmp hxlcl_strcmp` rewrites exactly as it does today. The amalgam range_field (5622/5623/5627) is untouched. Preprocessor `#ifdef`/`#define`/`#else`/`#endif` directives generate no code. Therefore the compiled runtime.a is byte-identical in the default-OFF build across all three configs (standalone seed, standalone core, amalgam), and byteeq 3-target is unaffected. The bytes only change when a build explicitly passes -DHEXA_RT_RANGE_STRCMP_NATIVE (the pool-gated flip round).",
        "wall": "After strcmp drops from the no-runtime.c libc floor, the last non-sanctioned reducible leaf is FLIP-6 (WALL-2 __libc_calloc/__libc_free default-ON) — measured-BLOCKED on the arm64/darwin fp-ABI Route-C else-arm (native calloc/free body lacks arm64/darwin xmm fp-ABI coverage; realloc F2 crossed this only after the magic-guard heap-family-split). Beyond FLIP-6 the axis-② floor bottoms out at the SANCTIONED terminal set (not reducible, no portable algorithm): net-FFI (~11-15 syms: socket/connect/getaddrinfo/DNS) · CRT __libc_start_main (M8 irreducible seed #3959) · exec-family (execve/execvp) · CUDA opt-in (__cudaRegisterFatBinary) · dlopen/dlsym/dlerror (FFI, partitioned to runtime_ffi_dyn.o). Literal `nm -u` == ∅ is UNREACHABLE; the honest ② endpoint for 'no runtime.c' is the sanctioned-floor, distinct from the emitted-C compile-step axis (③)."
      },
      {
        "round": "§3.1 arm64-linux native cold-path de-hardcode — rescope @ 9d1e4d3c7 (prior scope was 0657b2204, now 175 behind; line numbers re-verified live). Two arms: cmd_run leg-B (self/main.hexa:4539-4596) and the user's new cmd_build HEXA_BUILD_NATIVE arm (self/main.hexa:3541-3556). Reference-match confirmed against compiler/main.hexa: emit triple must be \"arm64-linux-gnu\" (host map :147 `Linux aarch64`→`arm64-linux-gnu`; aprime_cc emit gate :984 `target == \"arm64-linux-gnu\"` → serialize_elf_arm64 :986; NOT \"aarch64-linux-gnu\"), crt-dir /usr/lib/aarch64-linux-gnu (:1142), dynamic-linker /lib/ld-linux-aarch64.so.1 (:1144). The authoritative arm64 ld recipe (:1134-1145) links crt1.o + glibc _start (crt-KEEP), so arm64 forces the else-branch and own-start stays x86_64-only.",
        "head_confirmed": "9d1e4d3c7",
        "ready": true,
        "edits_apply": true,
        "confidence": "high",
        "corrections": [
          "CONFIRMED HEAD == 9d1e4d3c7. All 13 cited OLD-text edit sites (A1 :3541, A2-anchor :3542, A3 :3548, A4 :3551, A5 :3552, A6 :3554; B1 :4539-4540, B2a-anchor :4568, B2b :4569, B3 :4586, B4 :4587, B5 :4595; C1 :3539, C2 :4531-4533, C3 :4592-4593) match VERBATIM in self/main.hexa and would apply cleanly.",
          "CONFIRMED the reference-match that was the prior round's fatal defect is now correct: compiler/main.hexa:984 gates arm64 obj emit on `target == \"arm64-linux-gnu\"` (exact string), so the spec's emit triple `arm64-linux-gnu` (A2/A3, B2a/B2b) is right and the previously-cited `aarch64-linux-gnu` would have hit the no-recipe branch and defeated engage. Host map :147 `Linux aarch64` -> `arm64-linux-gnu` confirmed.",
          "CONFIRMED the FS-path spelling split is honored: crt-dir `/usr/lib/aarch64-linux-gnu` (compiler/main.hexa:1142) and dl `/lib/ld-linux-aarch64.so.1` (:1144) use the aarch64 spelling in A4/A5/B3/B4 — correct, do not unify with the emit triple.",
          "CONFIRMED crt-KEEP scope claim: compiler/main.hexa:1134-1145 arm64 ld recipe links crt1.o + glibc loader (no own _start), and self/main.hexa:3556/4596 select the crt1.o/crti.o/crtn.o else-branch when `_bnzc`/`_zc_own` is false — so A6/B5's `!_bn_arm &&`/`!_run_native_arm &&` correctly force arm64 onto crt-keep while leaving own-start x86_64-only; on x86_64 the prefix reduces to `true && …` (byte-neutral).",
          "CONFIRMED byte-neutral argument has no hole on x86_64: A1's first &&-term short-circuits default builds before any exec; the first OR-term `==Linuxx86_64` short-circuits true on x86_64 so the added arm-arch execs never run; every hardcoded x86_64 literal becomes the char-identical else-branch of an `if arm {…} else {…}` with the arm discriminant false on x86_64. The spec's own honest caveat (source-additive, compiler emitted-bytes DO change; arm64 branch is unreachable dead code on x86_64/darwin, byteeq-reconvergent) is accurate."
        ],
        "edits": [
          "A1 — self/main.hexa:3541 (cmd_build gate: broaden host to the 3 linux arches; env-check stays first &&-term so default builds are untouched; first OR-term is the char-identical Linuxx86_64 exec that short-circuits true on x86_64 so the 2nd/3rd arm-arch execs never run there). OLD:\n    if env_var(\"HEXA_BUILD_NATIVE\") == \"1\" && exec(\"uname -sm 2>/dev/null | tr -d ' \\\\n\\\\t'\") == \"Linuxx86_64\" && shared != \"1\" && c_only != \"1\" && len(target) == 0 && len(__actual_src) > 0 {\nNEW:\n    if env_var(\"HEXA_BUILD_NATIVE\") == \"1\" && (exec(\"uname -sm 2>/dev/null | tr -d ' \\\\n\\\\t'\") == \"Linuxx86_64\" || exec(\"uname -sm 2>/dev/null | tr -d ' \\\\n\\\\t'\") == \"Linuxaarch64\" || exec(\"uname -sm 2>/dev/null | tr -d ' \\\\n\\\\t'\") == \"Linuxarm64\") && shared != \"1\" && c_only != \"1\" && len(target) == 0 && len(__actual_src) > 0 {",
          "A2 — self/main.hexa: INSERT two lines at the top of the gate body, immediately BEFORE line 3542 `        let _bncc = resolve_native_cc()`. The gate above admits only Linux{x86_64,aarch64,arm64}, so `!= Linuxx86_64` ⟺ arm64 (single discriminant, one exec, false on x86_64):\n        let _bn_arm = exec(\"uname -sm 2>/dev/null | tr -d ' \\\\n\\\\t'\") != \"Linuxx86_64\"\n        let _bntgt = if _bn_arm { \"arm64-linux-gnu\" } else { \"x86_64-linux-gnu\" }",
          "A3 — self/main.hexa:3548 (cmd_build emit triple dispatch; on x86_64 _bntgt=\"x86_64-linux-gnu\" → constructed string char-identical). OLD:\n            let _bne = exec(\"'\" + _bncc + \"' _drv.hexa --emit=obj --target=x86_64-linux-gnu -o '\" + _bnobj + \"' '\" + __actual_src + \"' 2>&1\")\nNEW:\n            let _bne = exec(\"'\" + _bncc + \"' _drv.hexa --emit=obj --target=\" + _bntgt + \" -o '\" + _bnobj + \"' '\" + __actual_src + \"' 2>&1\")",
          "A4 — self/main.hexa:3551 (cmd_build crt-dir dispatch; else-branch char-identical to today). OLD:\n                let _bncrt = exec(\"for d in /usr/lib/x86_64-linux-gnu /usr/lib64 /usr/lib; do if [ -f $d/crt1.o ]; then printf %s $d; break; fi; done\")\nNEW:\n                let _bncrt = if _bn_arm { exec(\"for d in /usr/lib/aarch64-linux-gnu /usr/lib64 /usr/lib; do if [ -f $d/crt1.o ]; then printf %s $d; break; fi; done\") } else { exec(\"for d in /usr/lib/x86_64-linux-gnu /usr/lib64 /usr/lib; do if [ -f $d/crt1.o ]; then printf %s $d; break; fi; done\") }",
          "A5 — self/main.hexa:3552 (cmd_build dynamic-linker dispatch; arm64 = /lib/ld-linux-aarch64.so.1 per compiler/main.hexa:1144; else-branch char-identical). OLD:\n                let _bndl = exec(\"for f in /lib64/ld-linux-x86-64.so.2 /lib/ld-linux-x86-64.so.2; do if [ -e $f ]; then printf %s $f; break; fi; done\")\nNEW:\n                let _bndl = if _bn_arm { exec(\"for f in /lib/ld-linux-aarch64.so.1; do if [ -e $f ]; then printf %s $f; break; fi; done\") } else { exec(\"for f in /lib64/ld-linux-x86-64.so.2 /lib/ld-linux-x86-64.so.2; do if [ -e $f ]; then printf %s $f; break; fi; done\") }",
          "A6 — self/main.hexa:3554 (cmd_build: force crt-KEEP on arm64 = reference-matched compiler/main.hexa:1134 glibc-crt1 recipe; own-start -nostartfiles stays x86_64-only. On x86_64 `!_bn_arm`=true → `env_var(...) != \"0\"` identical to today). OLD:\n                    let _bnzc = env_var(\"HEXA_ZEROC_OWN_START\") != \"0\"\nNEW:\n                    let _bnzc = !_bn_arm && env_var(\"HEXA_ZEROC_OWN_START\") != \"0\"",
          "B1 — self/main.hexa:4539-4540 (cmd_run leg-B: add arm discriminant + broaden gate; uname -sm here is .trim()'d so the value carries a space, i.e. \"Linux aarch64\"/\"Linux arm64\"). OLD:\n    let _run_native_host = exec(\"uname -sm 2>/dev/null\").trim()\n    let _run_native_on = _run_native_host == \"Linux x86_64\" && env(\"HEXA_RUN_CTRANSPILE\") != \"1\"\nNEW:\n    let _run_native_host = exec(\"uname -sm 2>/dev/null\").trim()\n    let _run_native_arm = _run_native_host == \"Linux aarch64\" || _run_native_host == \"Linux arm64\"\n    let _run_native_on = (_run_native_host == \"Linux x86_64\" || _run_native_arm) && env(\"HEXA_RUN_CTRANSPILE\") != \"1\"",
          "B2a — self/main.hexa: INSERT one line immediately BEFORE line 4569 (after the `// 1) native object emit (no clang) — on the flattened source` comment at 4568):\n            let _ntgt = if _run_native_arm { \"arm64-linux-gnu\" } else { \"x86_64-linux-gnu\" }",
          "B2b — self/main.hexa:4569 (cmd_run emit triple dispatch; on x86_64 _ntgt=\"x86_64-linux-gnu\" → char-identical exec string). OLD:\n            let _ne = if len(_nsrc) > 0 { exec(\"'\" + _ncc + \"' _drv.hexa --emit=obj --target=x86_64-linux-gnu -o '\" + _nobj + \"' '\" + _nsrc + \"' 2>&1\") } else { \"flatten-failed-skip-native\" }\nNEW:\n            let _ne = if len(_nsrc) > 0 { exec(\"'\" + _ncc + \"' _drv.hexa --emit=obj --target=\" + _ntgt + \" -o '\" + _nobj + \"' '\" + _nsrc + \"' 2>&1\") } else { \"flatten-failed-skip-native\" }",
          "B3 — self/main.hexa:4586 (cmd_run crt-dir dispatch; else-branch char-identical). OLD:\n                let _crt = exec(\"for d in /usr/lib/x86_64-linux-gnu /usr/lib64 /usr/lib; do if [ -f $d/crt1.o ]; then printf %s $d; break; fi; done\")\nNEW:\n                let _crt = if _run_native_arm { exec(\"for d in /usr/lib/aarch64-linux-gnu /usr/lib64 /usr/lib; do if [ -f $d/crt1.o ]; then printf %s $d; break; fi; done\") } else { exec(\"for d in /usr/lib/x86_64-linux-gnu /usr/lib64 /usr/lib; do if [ -f $d/crt1.o ]; then printf %s $d; break; fi; done\") }",
          "B4 — self/main.hexa:4587 (cmd_run dynamic-linker dispatch; else-branch char-identical). OLD:\n                let _dl = exec(\"for f in /lib64/ld-linux-x86-64.so.2 /lib/ld-linux-x86-64.so.2; do if [ -e $f ]; then printf %s $f; break; fi; done\")\nNEW:\n                let _dl = if _run_native_arm { exec(\"for f in /lib/ld-linux-aarch64.so.1; do if [ -e $f ]; then printf %s $f; break; fi; done\") } else { exec(\"for f in /lib64/ld-linux-x86-64.so.2 /lib/ld-linux-x86-64.so.2; do if [ -e $f ]; then printf %s $f; break; fi; done\") }",
          "B5 — self/main.hexa:4595 (cmd_run: force crt-KEEP on arm64; own-start x86_64-only. On x86_64 `!_run_native_arm`=true → identical to today). OLD:\n                    let _zc_own = env_var(\"HEXA_ZEROC_OWN_START\") != \"0\"   // FLIP-7 flip-D: default-ON (opt-out =0). x86_64-linux-only path; a failed -nostartfiles ld link loud-falls-through to clang (comment below)\nNEW:\n                    let _zc_own = !_run_native_arm && env_var(\"HEXA_ZEROC_OWN_START\") != \"0\"   // FLIP-7 flip-D: default-ON on x86_64 (opt-out =0). arm64 forces crt-keep (else-branch = compiler/main.hexa:1134 glibc-crt1 recipe); own-start arm64 = separate round. A failed x86_64 -nostartfiles ld link loud-falls-through to clang",
          "C1 (comment-only) — self/main.hexa:3539 (cmd_build header). OLD:\n    // HEXA_RUN_NATIVE_TRACE=1). linux x86_64 only (the host the recipe is proven on); refuses\nNEW:\n    // HEXA_RUN_NATIVE_TRACE=1). linux x86_64 + aarch64/arm64 (triple/crt/dl dispatched off uname -sm); refuses",
          "C2 (comment-only) — self/main.hexa:4531-4533 (cmd_run host-gate rationale; keep the `On any other host (darwin CI runners)` tail so it flows into 4534). OLD:\n    // Host gate (2026-07-03): the native --emit=obj path below is x86_64-linux\n    // ONLY end-to-end — the emit hardcodes --target=x86_64-linux-gnu and the ld\n    // link needs linux crt1.o + ld-linux. On any other host (darwin CI runners)\nNEW:\n    // Host gate (2026-07-03; arm64 added 2026-07-07): the native --emit=obj path\n    // engages on Linux x86_64 AND aarch64/arm64 — the emit triple, crt-dir, and\n    // dynamic-linker are dispatched off uname -sm. On any other host (darwin CI runners)",
          "C3 (comment-only) — self/main.hexa:4592-4593 (own-start block; the stale `arm64 never reaches here`). OLD:\n                    // --start-group so libc still resolves. This block is x86_64-hardcoded (arm64 never\n                    // reaches here). Default-OFF byte-neutral: else-branch char-identical to today.\nNEW:\n                    // --start-group so libc still resolves. own-start (-nostartfiles) is x86_64-only:\n                    // _zc_own = !_run_native_arm gates it, so arm64 takes the else-branch (crt-keep,\n                    // = compiler/main.hexa:1134 glibc-crt1 recipe). x86_64 else-branch char-identical."
        ],
        "gate": [
          "byteeq 3-target GREEN (x86_64-linux · arm64-linux · darwin-arm64) via PR CI — proves the source addition self-hosts deterministically and gen3≡gen4 reconverges; x86_64 + darwin byte-identity proves the new arm64 branch is unreachable dead code on those targets (source-additive only).",
          "x86_64 behavior-identity (both arms): on an x86_64-linux host, capture `HEXA_RUN_NATIVE_TRACE=1 hexa run prog.hexa` and `HEXA_BUILD_NATIVE=1 HEXA_RUN_NATIVE_TRACE=1 hexa build prog.hexa` before vs after the edit — the emitted `--target=`, crt-dir probe, dl probe strings and the `[run-native]/[build-native] clang-free: aprime --emit=obj + ld →` trace lines must be character-identical (only arm64 branch added).",
          "arm64-linux ENGAGE-PROOF (the core gate): on a REAL aarch64-linux host (qemu-aarch64-on-aiden OR a cloud arm64 CI runner — no arm64 self-hosted pool host exists), `HEXA_RUN_NATIVE_TRACE=1 hexa run prog.hexa` must print `[run-native] clang-free: aprime --emit=obj + ld → tmpbin` (native ENGAGED) — NOT `native-emit failed`/`ld link failed → clang fallback`; likewise `HEXA_BUILD_NATIVE=1 HEXA_RUN_NATIVE_TRACE=1 hexa build prog.hexa` prints `[build-native] clang-free…`. The native binary's rc+stdout must byte-match the `HEXA_RUN_CTRANSPILE=1` path. A clang-fallback trace = feature NOT landed (do not merge on x86-only green).",
          "arm64 host-env precondition assertion: /usr/lib/aarch64-linux-gnu/{crt1.o,crti.o,crtn.o} + /lib/ld-linux-aarch64.so.1 present, and stage_resolve_runtime_a resolved an arch-matched (arm64) runtime.a — verify on the chosen aarch64 image before asserting engage.",
          "install.sh clang-18 3-target consumer smoke GREEN including a fresh arm64-linux install → `hexa run`/`hexa build` engages native per the engage-proof (no stale-pool-hexat CPU-fallback artifact).",
          "All builds/measurements on the pool (aiden/ghost) or qemu-aarch64-on-aiden / cloud arm64 CI — never mini (git/gh/read). If the SSH pool is down, verify via a PR (github-hosted 3-target byteeq is the release-integrity gate)."
        ],
        "byte_neutral_arg": "The x86_64 default path stays byte-identical by construction: every hardcoded x86_64 literal becomes the ELSE-branch of `if arm { <arm64> } else { <the existing string, char-for-char> }`, and the arm discriminant (_run_native_arm / _bn_arm) is FALSE on x86_64 — so the constructed exec strings (`--target=x86_64-linux-gnu`, the crt probe, the dl probe) and the own-start `_zc_own`/`_bnzc` link recipe are character-identical to today. The `!_run_native_arm &&` / `!_bn_arm &&` prefixes on the own-start gates are no-ops on x86_64 (arm flag false → the prefix is `true &&`). The cmd_build gate (A1) keeps `env_var(\\\"HEXA_BUILD_NATIVE\\\")==\\\"1\\\"` as the FIRST &&-term, so every default build (flag unset) short-circuits before any host exec — zero new subprocesses on the default path; and the first OR-term is the char-identical `... == \\\"Linuxx86_64\\\"` exec which short-circuits true on x86_64 so the 2nd/3rd arm-arch execs never run there (the only added cost is one extra uname exec inside the body on the x86_64 OPT-IN HEXA_BUILD_NATIVE=1 path, which does not affect emitted output). NOTE the honest scope of \\\"byte-neutral\\\": the compiler's OWN emitted bytes DO change (a new arm64 branch is added to source), so this is NOT a bit-for-bit no-op of the shipped binary — it is a deterministic source addition proven byteeq-reconvergent across all 3 targets; on x86_64 and darwin the arm64 branch is unreachable dead code so runtime behavior + emitted program output are unchanged. Release safety is absolute regardless: any arm64 emit/link failure loud-falls-through to the clang `hexa build`/`run` path (nothing that works today can break).",
        "wall": "This scope de-hardcodes triple/crt/dl ONLY; two walls remain, neither a release risk. WALL-1 (feature-gating, must be measured on qemu/cloud-arm64): whether prebuilt aprime_cc `--emit=obj --target=arm64-linux-gnu` produces a fully-linkable ELF arm64 object for REAL user programs (not just compiler/test/elf_arm64_exit42.hexa) is UNPROVEN from reading — serialize_elf_arm64 (compiler/main.hexa:986) + the codegen/arm64_linux path must be complete end-to-end. If incomplete, arm64 emit or ld link fails → loud-fail-safe → clang fallback (ZERO release risk) but the native cold path does NOT actually engage → the arm64 ENGAGE-PROOF gate is RED → feature not landed; next round = finish the arm64 elf emitter/codegen. This is exactly what the qemu-aarch64-on-aiden / cloud-arm64 engage-proof must measure — do NOT merge on x86-only green. WALL-2 (verification infra): no arm64 self-hosted pool host (CLAUDE.md) and mini is git/gh/read only — the arm64 build+engage-proof can only run on qemu-aarch64-on-aiden or a cloud arm64 CI runner. SCOPE-EXCLUSION (not a wall, deliberate): arm64 own-start (-nostartfiles crt-drop) is OUT — arm64 forces the crt-KEEP else-branch matching the proven compiler/main.hexa:1134 glibc-crt1 recipe, sidestepping any dependency on an arm64 runtime.a own `_start`; own-start-on-arm64 is a separate future round."
      },
      {
        "round": "regex-parity-corpus (zeroc #29) — EXACT implementation spec, re-verified at new HEAD (main +175 since the 0657b2204 scope)",
        "head_confirmed": "9d1e4d3c7",
        "ready": true,
        "edits_apply": true,
        "confidence": "high",
        "corrections": [
          "Non-blocking (allowlist CREATE-file doc, edit 6): D1 cites 'stdlib/regex/backtrack.hexa:657 _bt_run_repeat greedy prefer one-more-rep continuation', conflating two lines — :657 is the REPEAT dispatch (`if kind == 11  // REPEAT {min,max}`), while the fn `_bt_run_repeat` is actually defined at :755 (as edit 1 correctly states). Both are the correct REPEAT region; the allowlist's :657 pin is the dispatch, not the fn def.",
          "Non-blocking (run.sh CREATE-file, edit 4): the harness calls `bash tool/regen_regex_rt_native_s.sh \"$(uname -m | sed ...)\"` to bake build/regex_rt_native.o, but tool/stage_resolve_runtime_a's resolve_native_regex_rt_seed (:764-800) ALREADY assembles the per-target seed (self/native/regex_rt_{arm64,x86_64,arm64-linux}.s) into build/regex_rt_native.o itself when HEXA_REGEX_NATIVE!=0. The separate regen call is redundant, and the tool's actual arg convention (target-name mapping) was not verified against regen_regex_rt_native_s.sh — implementer should confirm it or drop the call and rely on the stage's own seed-assembly (both stage emit lines :788/:795/:797 already gate seed presence). Self-flagged as placeholder in the spec, so not a correctness hole.",
          "Confirmation (not a defect): the spec correctly captures the conditional-on-seed polarity — :762 is `:-1` (resolve proceeds by default) but the -D emission at :1372 is `:-0`, so the effective ON depends on resolve_native_regex_rt_seed export/assembly succeeding; the SEED-PRESENCE gate (step 2 grep of the :1375 emit line) correctly guards against a disguised-libc false-GREEN."
        ],
        "edits": [
          "RE-VERIFICATION @ 9d1e4d3c7 (all sites confirmed; line offsets refreshed vs the 0657b2204 plan): FLAG stage_resolve_runtime_a:762 `[ \"${HEXA_REGEX_NATIVE:-1}\" != \"0\" ] || return 0` (resolve default-ON) + :763 `[ -f build/regex_rt_native.o ] && { export HEXA_REGEX_NATIVE=1; return 0; }` + AR-WIRING :1372 `[ \"${HEXA_REGEX_NATIVE:-0}\" = \"1\" ] && [ -f build/regex_rt_native.o ]` → :1373 `-DHEXA_REGEX_NATIVE=1`, emit line :1375 `[stage_resolve_runtime_a] RT-NATIVE REGEX`. 6 builtins are SURFACE fns (self/codegen.hexa:14337-14342: regex_match, regex_match_full, regex_search, regex_findall, regex_split, regex_replace). ON shims stdlib/runtime/regex_rt.hexa: rt_regex_match:188(->bool) rt_regex_match_full:205(->bool) rt_regex_search:223(->[int] [so,eo]|[]) rt_regex_findall:243(->[[int]]) rt_regex_split:275(->[string]) rt_regex_replace:317(->string); reject probe _rt_re_has_backref_lookaround:132; prep _rt_re_prep_pat:171 (strip (?i):76, PCRE-literal:94). Route: thompson.hexa regex_compile_capped:437 `if bt_needs_backtrack` → backtrack VM else Thompson longest (_re_longest_from:593, regex_search:655). backtrack.hexa: bt_needs_backtrack:105, FIX-A {n,m} route :140 (`c==123 && next in 48..57 return true`), greedy-first doc :40, REPEAT dispatch :657→_bt_run_repeat:755. OFF oracle libc bodies self/runtime_emit_full.hexa: `#ifndef HEXA_REGEX_NATIVE`→`#include <regex.h>` :50-51, `_hexa_re_compile`=regcomp(REG_EXTENDED[|REG_ICASE]) :13833-13836, match regexec(nmatch=0) :13864, match_full regexec(nmatch=1,&m) :13884, search :13906, findall from-offset loop w/ REG_NOTBOL+zero-width skip :13934, split :13972, replace :14019. STALE HEADER regex_rt.hexa:5 still says 'Default-OFF' — contradicts shipped polarity; fix in this PR (edit 5 below).",
          "CREATE test/native_build/regex_parity_oracle.c — POSIX-libc golden generator + reverifier (the sole corpus source-of-truth, so the LCG lives in ONE language only; the .hexa driver merely REPLAYS corpus.txt). SKELETON:\n----\n// test/native_build/regex_parity_oracle.c\n// zero-c #29 regex OFF(libc)-vs-ON(native NFA) parity. Deterministic-LCG class-tagged\n// corpus + libc regcomp/regexec golden harvest. Mirrors feat/strtod-oracle-corpus.\n//   cc -O2 regex_parity_oracle.c -o oracle && ./oracle <corpus.txt> <golden.txt>\n// GRAMMAR (byte-identical in regex_parity_corpus.hexa so `diff` is line-clean):\n//   corpus line : IDX \\t TAG \\t OP \\t PAT \\t INPUT \\t REPL\n//   golden line : IDX \\t TAG \\t OP \\t RESULT\n//   RESULT: match->M:1|M:0  full->F:1|F:0  search->S:so,eo|S:-\n//           findall->A:k:so,eo,so,eo,...  split->P:k:seg,seg,...  replace->R:<str>\n// Controlled alphabet EXCLUDES \\t and ',' so segments/joins never collide the delims.\n#include <stdio.h>\n#include <stdint.h>\n#include <string.h>\n#include <regex.h>\nstatic uint64_t S=0x2545F4914F6CDD1DULL;\nstatic uint64_t nx(void){ S=S*6364136223846793005ULL+1442695040888963407ULL; return S; }\nstatic int pick(int k){ return (int)((nx()>>40)%(uint64_t)k); }\n// per-class random-input alphabet (incl. case+digit+space for icase/POSIX-class cov)\nstatic const char* AL=\"aAbBcC012 \";\nstatic void rndin(char*buf,int lo,int hi){int L=lo+pick(hi-lo+1);int i;for(i=0;i<L;i++)buf[i]=AL[pick((int)strlen(AL))];buf[L]=0;}\n// libc compile helper — VERBATIM mirror of _hexa_re_compile (runtime_emit_full.hexa:13833)\nstatic int recomp(const char*p,regex_t*re,int icase){int fl=REG_EXTENDED;if(icase)fl|=REG_ICASE;return regcomp(re,p,fl);}\n// RESULT harvesters — each mirrors the exact libc body it must match:\n// op_match: regexec(nmatch=0)!=0 => 0 (:13864). op_full: nmatch=1, ok && m.rm_so==0 && m.rm_eo==len (:13884).\n// op_search: nmatch=1 -> S:so,eo (:13906). op_findall/split/replace: the from-offset loop\n// with REG_NOTBOL for off>0 + zero-width (rm_eo==rm_so => off+=1) exactly as :13934/13972/14019.\nstatic void harvest(FILE*g,int idx,const char*tag,const char*op,const char*pat,const char*in,const char*repl){\n  regex_t re; int icase = strncmp(pat,\"(?i)\",4)==0; const char*cp = icase? pat+4 : pat;\n  // NB: the .hexa shim additionally does FIX-C \\d->d rewrite + FIX-D reject; the C oracle must\n  //     apply the SAME pre-transform so golden==OFF-ledger. Implement re_prep() = strip(?i)+pcre_literal+\n  //     has_backref_lookaround, ported 1:1 from regex_rt.hexa:76/94/132. On reject -> compile-fail value.\n  // ... (compile; on regcomp!=0 emit the op's compile-fail sentinel: M:0/F:0/S:-/A:0:/P:1:<in>/R:<in>)\n  // ... (else run the op-specific libc loop, format RESULT per grammar, fprintf(g,\"%d\\t%s\\t%s\\t%s\\n\",...))\n}\n// CLASS TABLE — {tag, op-set, pattern, #fixed-inputs[], #rand}. Fill per plan:\n//  C1 anchors:    ^abc  abc$  ^abc$  ^$        ops: match,full,search,findall\n//  C2 alternation:a|ab|abc  (cat|category)     ops: match,search   (POSIX longest-branch)\n//  C3 greedy:     a*  a+  a?  a.*c  .*          ops: search,findall\n//  C4 zero-width: a*(on '')  (a|)  a*           ops: findall,split,search\n//  C5 POSIX cls:  [[:digit:]]+ [[:alpha:]] [^[:space:]] [a-z]+   ops: search,findall\n//  C6 dot/lit:    a.c  a\\\\.c  \\\\*                ops: match,search\n//  C7 PCRE-lit:   \\\\d \\\\w \\\\s (libc=literal d/w/s; shim rewrites) ops: match,search\n//  C8 icase:      (?i)abc on ABC/AbC            ops: match,search\n//  C9 FIX-D rej:  (a)\\\\1  (?=a)  (?!a)  (?<=a)   ops: match,search,split,replace (both no-match/passthru)\n//  D1 {n,m}:      (a|ab){1,2} on 'aab'  (a|ab){2}  a{1,2}(ab|a)  (ab|a){1,3}  ops: search,findall  <-- NAMED DIVERGENCE (greedy-first vs POSIX-longest)\n//  D2 icase-rng:  (?i)[A-Z] on 'abc'           ops: match,search                <-- NAMED DIVERGENCE (class-range not widened by literal-fold)\n// main(): open corpus_out+golden_out; loop table; for each (pattern x fixed-input x rand) x op:\n//   assign IDX (monotone), fprintf corpus line (IDX TAG OP PAT INPUT REPL), call harvest() -> golden line.\nint main(int argc,char**argv){ /* argv[1]=corpus.txt argv[2]=golden.txt; deterministic, no external RNG */ return 0; }\n----\nFIDELITY GATE (the load-bearing detail): re_prep() and the three from-offset loops in this .c MUST be ported 1:1 from regex_rt.hexa:76/94/132 and runtime_emit_full.hexa:13934/13972/14019 — otherwise golden.txt != off_ledger.txt spuriously.",
          "CREATE test/native_build/regex_parity_corpus.hexa — the ON/OFF-agnostic driver (SAME source compiled against both runtime.a variants; only the linked runtime differs). Replays corpus.txt, calls the 6 surface builtins, writes IDX\\tTAG\\tOP\\tRESULT with grammar byte-identical to the C oracle. SKELETON:\n----\n// test/native_build/regex_parity_corpus.hexa — zero-c #29 regex OFF-vs-ON parity driver.\n// Reads HEXA_REGEX_CORPUS (IDX\\tTAG\\tOP\\tPAT\\tINPUT\\tREPL), runs the compiled-in\n// regex_* builtins, writes HEXA_REGEX_LEDGER (IDX\\tTAG\\tOP\\tRESULT). Build TWICE\n// (HEXA_PREBUILT_RUNTIME=rt_off.a / rt_on.a) per regex_parity_run.sh. No generation here.\nfn fmt_search(sp) {\n    if len(sp) == 0 { return \"S:-\" }\n    return \"S:\" + to_string(sp[0]) + \",\" + to_string(sp[1])\n}\nfn fmt_findall(a) {\n    let mut r = \"A:\" + to_string(len(a)) + \":\"\n    let mut i = 0\n    while i < len(a) {\n        if i > 0 { r = r + \",\" }\n        r = r + to_string(a[i][0]) + \",\" + to_string(a[i][1])\n        i = i + 1\n    }\n    return r\n}\nfn main() {\n    let lines = read_lines(env_var(\"HEXA_REGEX_CORPUS\"))\n    let mut out = \"\"\n    let mut i = 0\n    while i < len(lines) {\n        let ln = lines[i]\n        i = i + 1\n        if ln == \"\" { continue }\n        let f = ln.split(\"\\t\")          // [IDX,TAG,OP,PAT,INPUT,REPL]\n        let idx = f[0]; let tag = f[1]; let op = f[2]\n        let pat = f[3]; let inp = f[4]; let repl = f[5]\n        let mut res = \"\"\n        if op == \"match\"      { res = if regex_match(pat, inp) { \"M:1\" } else { \"M:0\" } }\n        else if op == \"full\"  { res = if regex_match_full(pat, inp) { \"F:1\" } else { \"F:0\" } }\n        else if op == \"search\"{ res = fmt_search(regex_search(pat, inp)) }\n        else if op == \"findall\"{ res = fmt_findall(regex_findall(pat, inp)) }\n        else if op == \"split\" { let p = regex_split(pat, inp); res = \"P:\" + to_string(len(p)) + \":\" + p.join(\",\") }\n        else if op == \"replace\"{ res = \"R:\" + regex_replace(pat, inp, repl) }\n        out = out + idx + \"\\t\" + tag + \"\\t\" + op + \"\\t\" + res + \"\\n\"\n    }\n    write_file(env_var(\"HEXA_REGEX_LEDGER\"), out)\n    println(\"WROTE \" + env_var(\"HEXA_REGEX_LEDGER\") + \" (\" + to_string(len(lines)) + \" cases)\")\n}\n----\nSurface ops all CONFIRMED present @9d1e4d3c7: read_lines(self/codegen.hexa:7969), write_file(:7330), env_var(:8038), .split(:4752/8966), .join(:4737/8951), .substring(:4770), .byte_at(:4731), to_string(:5118). regex_match_full is a real builtin (:14338).",
          "CREATE test/native_build/regex_parity_run.sh — POOL A/B gate harness (aiden/summer/ghost — NOT mini). Mirrors the strtod-tail run recipe (rm -rf ~/.hexa-cache between builds; HEXA_PREBUILT_RUNTIME seam). SKELETON:\n----\n#!/usr/bin/env bash\n# test/native_build/regex_parity_run.sh — zero-c #29 regex OFF-vs-ON parity gate. POOL ONLY.\nset -euo pipefail\nWT=\"${WT:-$(git rev-parse --show-toplevel)}\"; cd \"$WT\"\nCC=\"${CC:-clang}\"; HEXA=\"${HEXA:-hexa}\"; OUT=\"$(mktemp -d)\"; trap 'echo OUT=$OUT' EXIT\n# 1) libc golden + shared corpus (deterministic)\n$CC -O2 test/native_build/regex_parity_oracle.c -o \"$OUT/oracle\"\n\"$OUT/oracle\" \"$OUT/corpus.txt\" \"$OUT/golden.txt\"\n# 2) build runtime.a OFF then ON, run the SAME driver against each\nbuild_variant() { # $1=off|on  $2=HEXA_REGEX_NATIVE value\n  rm -rf \"$HOME/.hexa-cache\" build/regex_rt_native.o\n  if [ \"$1\" = on ]; then\n    bash tool/regen_regex_rt_native_s.sh \"$(uname -m | sed 's/aarch64/arm64-linux/;s/x86_64/x86_64/')\"   # bake+assemble seed -> build/regex_rt_native.o\n    [ -f build/regex_rt_native.o ] || { echo \"RED: ON seed regex_rt_native.o not built\"; exit 1; }\n  fi\n  env HEXA_REGEX_NATIVE=\"$2\" CC=\"$CC\" bash tool/stage_resolve_runtime_a 2>&1 | tee \"$OUT/${1}_stage.log\"\n  if [ \"$1\" = on ]; then grep -q \"RT-NATIVE REGEX: HEXA_REGEX_NATIVE=1; ar'ing native regex_rt_native.o\" \"$OUT/on_stage.log\" || { echo \"RED: ON build did NOT ar the native seed -> disguised-libc false-GREEN\"; exit 1; }\n  else grep -q \"RT-NATIVE REGEX\" \"$OUT/off_stage.log\" && { echo \"RED: OFF build unexpectedly enabled native regex\"; exit 1; } || true; fi\n  cp \"$(ls -t build/*runtime*.a 2>/dev/null | head -1)\" \"$OUT/rt_$1.a\"   # capture resolved runtime.a (adjust to the resolver's actual output path)\n  rm -rf \"$HOME/.hexa-cache\"\n  HEXA_PREBUILT_RUNTIME=\"$OUT/rt_$1.a\" $HEXA build test/native_build/regex_parity_corpus.hexa -o \"$OUT/drv_$1\"\n  HEXA_REGEX_CORPUS=\"$OUT/corpus.txt\" HEXA_REGEX_LEDGER=\"$OUT/${1}_ledger.txt\" \"$OUT/drv_$1\"\n}\nbuild_variant off 0\nbuild_variant on  1\n# 3a) OFF self-consistency: OFF-ledger must equal the independent libc golden (LCG-sync + real-libc proof)\nif ! diff -u \"$OUT/golden.txt\" \"$OUT/off_ledger.txt\" > \"$OUT/off_vs_golden.diff\"; then echo \"RED: OFF ledger != libc golden (broken build or oracle drift)\"; cat \"$OUT/off_vs_golden.diff\"; exit 1; fi\n# 3b) parity: ON vs OFF, classify each differing IDX by its corpus TAG\ndiff <(cut -f1,4 \"$OUT/off_ledger.txt\") <(cut -f1,4 \"$OUT/on_ledger.txt\") > \"$OUT/on_vs_off.diff\" || true\n# join differing IDXs back to TAG; any C1..C9 diff = FAIL; D1/D2 diffs matched vs allowlist:\nawk -F'\\t' 'NR==FNR{tag[$1]=$2;next} /^[<>]/{split($0,a,\" \");idx=a[2]; t=tag[idx]; if(t ~ /^C/){print \"RED parity-required diff \" idx \" \" t; bad++} else {seen[t]=1}} END{ if(bad) exit 1 }' \"$OUT/corpus.txt\" \"$OUT/on_vs_off.diff\"\n# 3c) intentional-divergence accounted: every D-class in the allowlist MUST actually appear in the diff\nfor D in $(grep -oE '^D[0-9]' test/native_build/regex_intentional_divergence.txt | sort -u); do\n  grep -qE \"\\b$D\\b\" \"$OUT/on_vs_off.diff\" || { echo \"RED: allowlisted $D silently agreed (divergence went unhandled/undocumented)\"; exit 1; }\ndone\necho \"ALL PASS — C1..C9 0-divergence; D1/D2 accounted per allowlist\"\n----\nNOTE: the runtime.a output path (`ls -t build/*runtime*.a`) is a placeholder — the implementer must read the exact resolver output path in tool/stage_resolve_runtime_a (the same one HEXA_PREBUILT_RUNTIME consumes) and pin it; the strtod recipe used the identical seam.",
          "CREATE test/native_build/regex_intentional_divergence.txt — named, reviewed allowlist (each entry cites its source site):\n----\n# zero-c #29 regex OFF(libc/POSIX)-vs-ON(native NFA) intentional-divergence allowlist.\n# Any ON!=OFF diff whose corpus TAG is NOT listed here is a parity FAILURE.\nD1  {n,m} counted-repeat interval = leftmost GREEDY-FIRST (PCRE) not leftmost-LONGEST (POSIX).\n    ROOT: stdlib/regex/backtrack.hexa:140 (FIX-A routes '{'+digit to the backtrack VM) + :657 _bt_run_repeat greedy 'prefer one-more-rep' continuation. Thompson (all other routes) is POSIX-longest (thompson.hexa:593/655).\n    EXAMPLE: (a|ab){1,2} on 'aab' -> native S:0,2 ('aa') vs libc S:0,3 ('aab').\n    DISPOSITION: PENDING PRODUCT CALL (document-as-intentional here, OR fix backtrack REPEAT to POSIX-longest). The harness makes it MEASURABLE; the gate treats D1 as allowlisted until the call is made.\nD2  (?i)[A-Z] icase character-class RANGE not widened by the shim's literal ASCII-fold.\n    ROOT: stdlib/runtime/regex_rt.hexa:42-45 (KNOWN DIVERGENCE header) — _rt_re_ascii_fold folds literals+subject but does not widen a class range; glibc REG_ICASE widens [A-Z] to also match a-z.\n    EXAMPLE: (?i)[A-Z] on 'abc' -> libc M:1 vs native M:0.\n----",
          "EDIT stdlib/runtime/regex_rt.hexa:5 (STALE-HEADER doc fix, //-comment only — byte-neutral to emitted C). OLD (lines 5-7):\n// #include <regex.h>) under -DHEXA_REGEX_NATIVE. Default-OFF: the emitter's\n// #else arm keeps the character-identical libc bodies, so the OFF-path emitted C\n// is byte-neutral.\nNEW:\n// #include <regex.h>) under -DHEXA_REGEX_NATIVE. Polarity: native default-ON\n// (zeroc #29 flip, tool/stage_resolve_runtime_a:762 `:-1`); opt-OUT with\n// HEXA_REGEX_NATIVE=0 selects the emitter's #else arm, which keeps the\n// character-identical libc bodies, so the OFF-path emitted C is byte-neutral."
        ],
        "gate": [
          "Golden determinism: `cc -O2 test/native_build/regex_parity_oracle.c -o /tmp/reoracle && /tmp/reoracle a.txt g.txt` twice -> a.txt/g.txt byte-identical across runs (fixed-seed LCG, no external RNG).",
          "OFF self-consistency (LCG-sync + real-libc proof): off_ledger.txt == golden.txt byte-for-byte (step 3a). A mismatch means the OFF hexa build's emitted libc bodies diverge from the independent C oracle -> broken build or a re_prep()/loop port error in the oracle; RED.",
          "SEED-PRESENCE assertion (the false-GREEN guard from the verify pass): the ON build MUST print '[stage_resolve_runtime_a] RT-NATIVE REGEX: HEXA_REGEX_NATIVE=1; ar'ing native regex_rt_native.o' and build/regex_rt_native.o must exist — else the ON path silently shipped the libc #else and on_ledger==off_ledger is a disguised-libc false 0-divergence. Step 2 greps the emit line and hard-fails if absent.",
          "Parity-required 0-divergence: for every corpus case tagged C1..C9, on_ledger RESULT == off_ledger RESULT. ANY C-class diff (step 3b awk) = RED. (Thompson route is POSIX leftmost-longest == libc regexec tie-break.)",
          "Intentional-divergence accounted (both directions): every D1/D2 case that diverges must be in the allowlist AND every allowlisted D-class must ACTUALLY appear in the on-vs-off diff (step 3c) — a silently-agreeing D-class flags an unhandled/undocumented change; RED.",
          "Pool 3-target: regex_rt.hexa ships inside runtime.a via self/runtime_emit_full.hexa, so build+run the whole harness on aiden/summer/ghost across x86_64-linux + arm64-linux + darwin-arm64 (mini = git/gh/read only). All-3-target GREEN before treating the parity verdict as authoritative; the ON seed is per-target (self/native/regex_rt_{x86_64,arm64,arm64-linux}.s via tool/regen_regex_rt_native_s.sh).",
          "Install-smoke (post default-ON, already shipped): a canonical install-path binary reproduces the ON ledger (no stale-pool-hexat CPU-fallback artifact); `rm -rf ~/.hexa-cache` between the two variant builds is mandatory (strtod-recipe gotcha)."
        ],
        "byte_neutral_arg": "The four new files live under test/native_build/ and are NEVER imported by the self/ compile closure (regex_parity_oracle.c is a standalone libc program; regex_parity_corpus.hexa is compiled as a user program against a prebuilt runtime.a, not into it; run.sh + the allowlist .txt are harness artifacts). Per stdlib/CLAUDE.md's byteeq-neutrality convention `grep -rl` of these paths in self/ is empty, so they cannot change the gen3=gen4 fixpoint — byteeq-exempt test infra. The only edit to a shipped file is stdlib/runtime/regex_rt.hexa:5-7, which is a `//`-comment-only correction of stale 'Default-OFF' doc text: the emitter concatenates code strings, not comments, so the emitted runtime.c bytes are unchanged and byteeq stays GREEN with no 3-target rebuild required for that edit.",
        "wall": "🧱 D1 {n,m} disposition is a product call the harness informs but does not settle; captures are ABI-unsurfaced (nmatch<=1) so sub-group parity is a separate engine-internal follow-on."
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Spec"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "spec:axis2-strcmp-callerdrop",
      "phaseIndex": 1,
      "phaseTitle": "Spec",
      "agentId": "abaac02002349857f",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783385289439,
      "queuedAt": 1783385289433,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "axis-② strcmp-caller-drop — new default-OFF caller gate HEX…",
      "promptPreview": "CRITICAL (conv-1): read files ONLY from the clean current-main worktree at /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3. FIRST run `git -C /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3 rev-parse --short HEAD` and confirm it is 9d1e4d3c7 or later. Do NOT read /Users/mini/dancinlab/hexa-lang directly — it is on a stale branch ~175 behind. Prior scope plans live at /Users/mini/dancinlab/he…",
      "lastProgressAt": 1783385950575,
      "tokens": 120314,
      "toolCalls": 12,
      "durationMs": 661135,
      "resultPreview": "{\"round\":\"axis-② strcmp-caller-drop — new default-OFF caller gate HEXA_RT_RANGE_STRCMP_NATIVE (redirect raw range_field strcmp callers to native hxlcl_strcmp; drop the strcmp libc nm-UND in the no-runtime.c config)\",\"head_sha_confirmed\":\"9d1e4d3c7\",\"edits\":[\"CORRECTION TO SCOPE (load-bearing): the lever is hxlcl_strcmp (NOT rt_strcmp — confirmed, rt_strcmp exists only under #ifdef HEXA_ZEROC_RT_CO…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "spec:arm64-linux-coldpath",
      "phaseIndex": 1,
      "phaseTitle": "Spec",
      "agentId": "af2e1f903890f37ab",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783385289440,
      "queuedAt": 1783385289434,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "§3.1 arm64-linux native cold-path de-hardcode — rescope @ 9…",
      "promptPreview": "CRITICAL (conv-1): read files ONLY from the clean current-main worktree at /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3. FIRST run `git -C /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3 rev-parse --short HEAD` and confirm it is 9d1e4d3c7 or later. Do NOT read /Users/mini/dancinlab/hexa-lang directly — it is on a stale branch ~175 behind. Prior scope plans live at /Users/mini/dancinlab/he…",
      "lastProgressAt": 1783385644909,
      "tokens": 80020,
      "toolCalls": 11,
      "durationMs": 355469,
      "resultPreview": "{\"round\":\"§3.1 arm64-linux native cold-path de-hardcode — rescope @ 9d1e4d3c7 (prior scope was 0657b2204, now 175 behind; line numbers re-verified live). Two arms: cmd_run leg-B (self/main.hexa:4539-4596) and the user's new cmd_build HEXA_BUILD_NATIVE arm (self/main.hexa:3541-3556). Reference-match confirmed against compiler/main.hexa: emit triple must be \\\"arm64-linux-gnu\\\" (host map :147 `Linux …"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "spec:regex-parity-corpus",
      "phaseIndex": 1,
      "phaseTitle": "Spec",
      "agentId": "a0f5c820df9d9a03c",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783385289440,
      "queuedAt": 1783385289434,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "regex-parity-corpus (zeroc #29) — EXACT implementation spec…",
      "promptPreview": "CRITICAL (conv-1): read files ONLY from the clean current-main worktree at /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3. FIRST run `git -C /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3 rev-parse --short HEAD` and confirm it is 9d1e4d3c7 or later. Do NOT read /Users/mini/dancinlab/hexa-lang directly — it is on a stale branch ~175 behind. Prior scope plans live at /Users/mini/dancinlab/he…",
      "lastProgressAt": 1783385701537,
      "tokens": 103512,
      "toolCalls": 19,
      "durationMs": 412097,
      "resultPreview": "{\"round\":\"regex-parity-corpus (zeroc #29) — EXACT implementation spec, re-verified at new HEAD (main +175 since the 0657b2204 scope)\",\"head_sha_confirmed\":\"9d1e4d3c7\",\"edits\":[\"RE-VERIFICATION @ 9d1e4d3c7 (all sites confirmed; line offsets refreshed vs the 0657b2204 plan): FLAG stage_resolve_runtime_a:762 `[ \\\"${HEXA_REGEX_NATIVE:-1}\\\" != \\\"0\\\" ] || return 0` (resolve default-ON) + :763 `[ -f buil…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "verify:arm64-linux-coldpath",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a1c5a46b78a95479f",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783385644925,
      "queuedAt": 1783385644915,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "§3.1 arm64-linux native cold-path de-hardcode — rescope @ 9…",
      "promptPreview": "CRITICAL (conv-1): read files ONLY from the clean current-main worktree at /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3. FIRST run `git -C /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3 rev-parse --short HEAD` and confirm it is 9d1e4d3c7 or later. Do NOT read /Users/mini/dancinlab/hexa-lang directly — it is on a stale branch ~175 behind. Prior scope plans live at /Users/mini/dancinlab/he…",
      "lastProgressAt": 1783385759421,
      "tokens": 84086,
      "toolCalls": 8,
      "durationMs": 114496,
      "resultPreview": "{\"round\":\"§3.1 arm64-linux native cold-path de-hardcode — rescope @ 9d1e4d3c7\",\"edits_apply\":true,\"corrections\":[\"CONFIRMED HEAD == 9d1e4d3c7. All 13 cited OLD-text edit sites (A1 :3541, A2-anchor :3542, A3 :3548, A4 :3551, A5 :3552, A6 :3554; B1 :4539-4540, B2a-anchor :4568, B2b :4569, B3 :4586, B4 :4587, B5 :4595; C1 :3539, C2 :4531-4533, C3 :4592-4593) match VERBATIM in self/main.hexa and would…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "verify:regex-parity-corpus",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a956463fa2463f373",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783385701557,
      "queuedAt": 1783385701547,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "regex-parity-corpus (zeroc #29)",
      "promptPreview": "CRITICAL (conv-1): read files ONLY from the clean current-main worktree at /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3. FIRST run `git -C /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3 rev-parse --short HEAD` and confirm it is 9d1e4d3c7 or later. Do NOT read /Users/mini/dancinlab/hexa-lang directly — it is on a stale branch ~175 behind. Prior scope plans live at /Users/mini/dancinlab/he…",
      "lastProgressAt": 1783385941235,
      "tokens": 86524,
      "toolCalls": 14,
      "durationMs": 239678,
      "resultPreview": "{\"round\":\"regex-parity-corpus (zeroc #29)\",\"edits_apply\":true,\"corrections\":[\"Non-blocking (allowlist CREATE-file doc, edit 6): D1 cites 'stdlib/regex/backtrack.hexa:657 _bt_run_repeat greedy prefer one-more-rep continuation', conflating two lines — :657 is the REPEAT dispatch (`if kind == 11  // REPEAT {min,max}`), while the fn `_bt_run_repeat` is actually defined at :755 (as edit 1 correctly sta…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "verify:axis2-strcmp-callerdrop",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "af7ac8efbd15c3bbd",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783385950584,
      "queuedAt": 1783385950576,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "axis2-strcmp-callerdrop",
      "promptPreview": "CRITICAL (conv-1): read files ONLY from the clean current-main worktree at /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3. FIRST run `git -C /Users/mini/dancinlab/hexa-lang/.worktrees/wf-ref3 rev-parse --short HEAD` and confirm it is 9d1e4d3c7 or later. Do NOT read /Users/mini/dancinlab/hexa-lang directly — it is on a stale branch ~175 behind. Prior scope plans live at /Users/mini/dancinlab/he…",
      "lastProgressAt": 1783386095203,
      "tokens": 53590,
      "toolCalls": 13,
      "durationMs": 144619,
      "resultPreview": "{\"round\":\"axis2-strcmp-callerdrop\",\"edits_apply\":true,\"corrections\":[\"Descriptive-only (does not affect edit application): hxlcl_strcmp's body is a byte-loop (unsigned-char compare per shim_emit.hexa:455-462 and runtime_emit_full.hexa:438-445), not a 'word-scan' as the spec repeatedly labels it.\",\"Descriptive-only: shim_emit.hexa:455 is the HEXA_ZEROC_SHIM_BYTEID_EMIT byte-identical body nested in…"
    }
  ],
  "totalTokens": 528046,
  "totalToolCalls": 77
}