# HEXA-OWN·static-types 다음 라운드 실행계획 (census 워크플로 wf_1fddcb1d-f44 · 2026-07-03)

5 프론티어 병렬 census(각 file:line 근거 인용) → 합성. CI 대기(#4467·#4457) 중 선행 정찰 산출물. 아래 census 요지는 발췌 — 세부 근거는 각 절 참조.

## native parser bare-return next-line swallow — value-vs-void decision in compiler/parse/parser.hexa lacks gen2's newline rule because the native lexer drops Newline tokens

**effort S** · blocked_by: 없음

- Native return-parsing site: parse_return_expr at compiler/parse/parser.hexa:1488; value-vs-void decision at line 1494 is `if !check_kind(TokenKind::RBrace) && !check_kind(TokenKind::Semicolon) && !at_eof() { kids.push(parse_expr()) }` — no line/newline awareness, so any ident-led token on the NEXT line is parsed as the return value.  
  ↳ `compiler/parse/parser.hexa:1488-1505`
- The native lexer intentionally drops newlines, so the parser cannot see a Newline token: 'Newlines are dropped — the parser uses braces/semicolons for statement framing'; '\n' only bumps line counter at lexer.hexa:121-124.  
  ↳ `compiler/lex/lexer.hexa:103-104 and 121-124`
- Tokens DO carry line info usable for a same-line check: `struct Span { file: string, line: i64, col: i64, len: i64 }` and `struct Token { kind, text, span }` — so `peek().span.line == kw.span.line` is implementable with zero lexer changes.  
  ↳ `compiler/lex/tokens.hexa:13-18 and 112-116`
- gen2 mechanism: its lexer EMITS Newline tokens (suppressed only inside ()/[]), and parse_return checks `if k == "Newline" || k == "Eof" || k == "RBrace"` → bare ReturnStmt, else parse_expr. i.e. gen2's rule = 'return value must start on the same line'.  
  ↳ `self/lexer.hexa:250-252 (Newline token push) and self/parser.hexa:1794-1806 (parse_return Newline/Eof/RBrace guard)`
- Same-hazard census (native): (a) parse_throw_expr uses the identical guard at parser.hexa:1582 — but gen2's parse_throw_stmt (self/parser.hexa:4380-4382) calls parse_expr UNCONDITIONALLY, so bare `throw` is invalid in both frontends; throw is a structural twin but NOT a gen2-divergence. (b) contextual `defer` dispatch (parse_stmt parser.hexa:1926-1929 + _defer_body_start 1824-1838) checks peek_at(1).kind with no line check — a bare stmt `defer` at end-of-line followed by a next-line expression-start token would swallow it as the defer body (rare: requires a binding named defer used as a bare expr-stmt). (c) break/continue are safe — value-less, no children (parser.hexa:1745-1769).  
  ↳ `compiler/parse/parser.hexa:1582, 1824-1838, 1926-1929, 1745-1769; self/parser.hexa:4380-4390`
- gen2's tolerance set is Newline|Eof|RBrace; native's is RBrace|Semicolon|Eof. Native additionally treats Semicolon as bare (gen2's `;` handling differs upstream), so the ONLY divergence is the missing Newline/same-line axis.  
  ↳ `self/parser.hexa:1797 vs compiler/parse/parser.hexa:1494`
- Existing native parser test file already covers return corner cases (malformed `fn main() return 0 }` at line ~124-134) and is the natural place to extend.  
  ↳ `compiler/parse/parser_test.hexa:124-134`

**제안**: Minimal fix (reference-match gen2): in parse_return_expr (compiler/parse/parser.hexa:1494) capture `kw` (already done, line 1489) and add a same-line condition to the value guard: parse a value only if `peek().span.line == kw.span.line` (in addition to the existing !RBrace/!Semicolon/!at_eof checks). This is byte-equivalent to gen2's Newline rule because gen2's lexer emits a Newline token exactly when the next non-suppressed token is on a later line — except inside ()/[] where gen2 suppresses Newline, which cannot occur here since the token immediately after `return` at stmt position is never inside an unclosed bracket opened before the keyword. Apply the same one-line condition to parse_throw_expr:1582 ONLY if gen2-parity is re-verified — gen2 always parses a throw value, so the conservative reference-match is to leave throw untouched (bare throw is invalid in both). Do NOT touch break/continue. Optionally file the defer dispatch line-check (parser.hexa:1927 add `peek_at(1).span.line == peek().span.line`) as a follow-up, matching gen2 where Newline would terminate the defer stmt. Test plan: (1) extend compiler/parse/parser_test.hexa with: bare `return` + next-line `defer { f() }`, bare `return` + next-line call `g()`, bare `return` + next-line `let`, and a positive control `return\n` where the value IS on the same line (`return a +\n b` multi-line binop must still parse as valued); assert Return node child-count 0 vs 1 and that the following stmt parses as a sibling. (2) gen2 regression: self/test_parser_full.hexa already exercises parse_return — add the same snippet to confirm both frontends now agree. (3) selfhost gate: full corpus is already gen2-clean (gen2 ships), so aprime recompiling compiler/+self/ must produce identical ASTs → byteeq 3-target GREEN on pool (aiden/summer) is the merge gate; any corpus file that relied on next-line return-values would already fail under gen2, so none can exist.

**함정**:
- Do NOT re-introduce Newline tokens into the native lexer — that changes token indices/spans corpus-wide and risks byteeq churn; the span.line same-line check is strictly local to parse_return_expr.
- Multi-line valued returns like `return foo(\n arg)` or `return a +\n b` must keep working — the check is on the FIRST token after `return`, which sits on the same line in those shapes; add the positive-control test.
- gen2 parse_throw_stmt has NO bare form (self/parser.hexa:4381-4382 parse_expr unconditional) — blindly copying the same-line fix to throw would DIVERGE from gen2, not match it.
- gen2 suppresses Newline inside ()/[] (self/lexer.hexa:250-251); the same-line rule and the Newline rule differ for `return` inside an already-open paren — impossible at stmt position, but do not generalize this fix to arbitrary expression contexts.
- This is a compiler-frontend change: both frontends must be verified separately (gen2 self/parser.hexa is UNCHANGED — verify no drift) and byteeq 3-target + shipping smoke gate applies before merge; builds on pool hosts only, mini is git-only.
- parse_return_expr's comment (parser.hexa:1492-1493) says 'we always emit a child here so resolve doesn't have to special-case empty-return' — it is already conditional (bare return before `}` emits 0 children today), so downstream handles 0-child Return; no resolver change needed, but confirm with the existing bare-return-before-RBrace path.

## @own L2 r2 — extend HEXA_OWN_LINT / HX3012 use-after-move lint from block-local `@own let` bindings to PARAM and RETURN positions in compiler/check/types.hexa (aprime frontend only), warning-band, flag-gated, byteeq-neutral OFF.

**effort M** · blocked_by: No hard blocker. One flagged user decision gates the PARAM half: the @own-on-param surface — fn-level `@own("pname")` (zero parser/AST change, non-Rust-orthodox) vs adding Param.annotations (orthodox, AST + both-parser churn + gen2 drift) — recorded as an open user call in state/hexa-own/L2_own_static_integration.md:23. The RETURN half is unblocked either way.

- r1 registry = 5 push-only parallel arrays (names/states/def_lines/move_lines/move_cols), newest-entry-wins end-scan, per-FN lifetime; own-state deliberately NOT in benv because _types_env_lookup is first-hit-wins.  
  ↳ `compiler/check/types.hexa:1230-1234 (arrays), :1236-1242 (_own_lint_reset), :1246-1253 (_own_lint_find scans from END), :1255-1261 (_own_lint_register), :1224-1229 (comment: benv key redefinition silently reads stale; push-only shadow-entry discipline)`
- r1 flags moves at exactly 3 bare-ident sites — let-RHS, assign-RHS, call-args — via _own_lint_touch; first touch pushes a state-1 shadow entry, second touch emits HX3012; same-span re-visit (scratch re-infer double-walk) is deduped by recorded move-site span.  
  ↳ `compiler/check/types.hexa:2304-2305 (let RHS children[0]), :2424-2425 (Assign RHS children[1]), :2774-2775 (call args children[1+i]), :1270-1287 (_own_lint_touch incl. span-dedup :1283-1285), :1605-1612 (_emit_hx3012)`
- Registration of `@own let` happens in the Block arm's ordered statement loop AFTER the child is inferred (so an RHS naming an outer same-named binding touches the outer entry first), fully env()-gated; the per-fn reset lives in type_check's fn loop.  
  ↳ `compiler/check/types.hexa:2101-2119 (Block-arm registration, flag gate :2111, _own_lint_has_own_ann via e.annotations :1292-1299), :3902-3909 (per-fn _own_lint_reset before body walk)`
- RETURN hook site: the Return arm infers the value child at :2203-2206 and validates HX3004 — no own-lint touch exists there today (r1 skipped it); the implicit tail-expression path is separate at :3916-3919 (tail_src = last body child).  
  ↳ `compiler/check/types.hexa:2190-2226 (Return arm; children[0] inferred :2204, no _own_lint_touch present), :3910-3926 (tail-expr HX3004 check in type_check loop)`
- PARAM hook site: params are bound into the fn-local env by _types_bind_params (name + lowered type, no annotation data) inside the per-fn loop; r2 param registration attaches naturally right after _own_lint_reset (:3907-3909) iterating it.params with p.span.  
  ↳ `compiler/check/types.hexa:3804-3823 (_types_bind_params walks it.params, records name+type only), :3886-3928 (per-fn loop: bind :3894, reset :3907-3909, body walk :3911)`
- Neither parser supports annotations ON a param: aprime parse_param expects Ident immediately (an `@` token would fail expect) and Param has no annotations field; gen2 parse_params likewise starts at `&`/`mut`/Ident with no @ handling. Fn-ITEM-level annotations DO flow: parse_fn_item(anns) → final_anns → Item.annotations, so a fn-level `@own("pname")` needs zero parser change.  
  ↳ `compiler/parse/parser.hexa:2188-2195 (parse_param: expect(Ident), eat(Colon)), compiler/parse/ast.hexa:52-63 (Param{name,typ,typs,span} — no annotations), parser.hexa:2197+2256-2266 (fn item anns→Item.annotations), self/parser.hexa:2346-2380 (gen2 parse_params, no @); recipe state/hexa-own/L2_own_static_integration.md:23 flags this exact fork as an open user call`
- gen2 stmt-level @own already parses (generic @attr absorption in parse_stmt) but prints a non-fatal '[warn] unknown attribute @own' because 'own' is absent from the _p_is_known_attr allowlist; HX3012/own-lint itself is aprime-only (recipe acknowledges the gen2 drift).  
  ↳ `self/parser.hexa:911-957 (generic @attr loop, unknown-attr println :942-944), :286-360 (_p_is_known_attr — no "own" entry, grep confirms), state/hexa-own/L2_own_static_integration.md:28 (aprime-only @own = gen2 drift, must be stated in PR)`
- HX3012 is registered as Severity::Warning stage S3 in the diag catalog, and its explain text EXPLICITLY names 'fn params, return values' as later rungs — r2 must update this explain same-PR (r6's stale HX3011 explain was recorded as a defect).  
  ↳ `compiler/diag/catalog.hexa:357-364 (HX3012 DiagSpec: Warning, template with name/def_line/moved_line, explain says 'Scope is HEXA-OWN L2 r1 … fn params, return values … are later rungs'), state/hexa-own/L2_own_static_integration.md:14 (r6 defect ②: stale explain)`

**제안**: r2 scope (aprime-only, single PR): (A) RETURN position — in the Return arm add a flag-gated `_own_lint_touch(e.children[0], out)` right after the value-child inference (types.hexa:2204-2206), bare-ident only, same idiom as :2424. Semantics: `return x` is a MOVE of an @own binding; the useful diagnostic is `return x` AFTER x was already moved (warns HX3012), and a moved-then-later-used-after-return-in-a-loop case falls out for free. Leave the implicit tail-expr path (:3916-3919) OUT of r2 — the Block arm already walks the last child through the existing let/assign/call touch sites, and adding a second touch there risks double-counting one source use. (B) PARAM position — adopt the fn-level `@own(\"pname\")` surface (zero parser/AST churn; recipe:23's non-orthodox fork, needs user sign-off): in the per-fn loop, immediately after `_own_lint_reset()` (:3908), under the same env() gate, scan it.annotations for name==\"own\" entries whose first arg is a string literal matching a param name in it.params, and `_own_lint_register(pname, p.span)` each — the existing body-walk touch sites then track param moves with no further change. (C) Diagnostics: reuse HX3012 (no new code); update catalog.hexa:361-362 explain to state r2 covers params + return position (borrows/fields/drop remain later rungs). Optionally allowlist \"own\" in gen2 _p_is_known_attr (self/parser.hexa:286) to silence the spurious unknown-attr warn — separate commit, gen2-binary-touching, gate on faithful/byteeq. Tests (state/hexa-own or existing lint smoke pattern, flag-ON): t1 `@own(\"p\") fn f(p:[i64])` p passed to two calls → warn at 2nd; t2 param moved via let-RHS then `return p` → warn at return; t3 `@own let x` moved then `return x` → warn; t4 `return x` as FIRST use → silent; t5 flag-OFF diagnostic stream byte-identical (byteeq 3-target GREEN); t6 shadowing `@own let p` over the param → fresh state; t7 move in fn A doesn't poison same-named param in fn B (reset). Merge order: independent of Wall A r7-prep (#4457) and gen2 C4 (#4467).

**함정**:
- benv key-redefinition ban: _types_env_lookup is first-hit-wins, so own-state must stay in the side registry, never encoded as an env redefine (types.hexa:1224-1225 comment; recipe L2_own_static_integration.md:20 calls it the mandatory-avoid trap)
- Statement order exists ONLY in the Block arm's sequential loop — but param registration must NOT go there; it belongs in the per-fn loop AFTER _own_lint_reset (types.hexa:3907-3909) and BEFORE the body walk, or fn-A state leaks / params register too late
- Every registry push/read must have env("HEXA_OWN_LINT")=="1" as the FIRST && operand at the call site — r1's structural-zero-cost contract (types.hexa:1218-1220); an unguarded push breaks flag-OFF byteeq neutrality of the diagnostic stream
- Double-visit dedup is span-based (types.hexa:1283-1285): opt-in passes (r2/r5 scratch re-infer) walk an expr twice — any new touch site must use _own_lint_touch unchanged, not a bespoke emit, or one source use double-counts as a second move
- Param annotations do not parse in EITHER parser (parser.hexa:2188-2195, self/parser.hexa:2346+): inline `@own p: T` syntax = AST Param field + both-parser change + gen2 drift; the fn-level @own("pname") route avoids all of it but is flagged as a USER DECISION (recipe :23)
- gen2 prints '[warn] unknown attribute @own' for @own let in user source (self/parser.hexa:942, 'own' not in _p_is_known_attr :286-360) — noise, non-fatal, but a PR reviewer will hit it; fixing it touches the gen2 binary → faithful/byteeq gates apply
- Catalog explain staleness is a recorded defect class (r6 lesson, recipe :14): HX3012's explain currently hard-codes 'r1 … fn params, return values … are later rungs' (catalog.hexa:362) and must be updated in the same PR
- Return arm HX3004 interplay: touch after the children[0] inference (post :2204) so the arm's existing pinned-frame HX3004 logic (:2199-2223) is untouched; do not reorder those reads
- Tail-expression (fn body last expr, types.hexa:3916-3919) looks like a return position but is already reached by the Block-arm walk — adding a touch there double-counts; keep it out of r2
- Builds/byteeq run on pool hosts (aiden/summer), mini is git-only; verify both frontends separately even though the check is aprime-only (gen2 smoke on an @own-bearing corpus file to prove non-fatal)

## static-types Wall A REJECT ladder r8 — next rung after r7-prep #4457 (HEXA_STATIC_TYPES_ARRAY_LOWER structured array lowering, branch feat/static-types-r7prep-array-lower, OPEN)

**effort M** · blocked_by: PR #4457 (feat/static-types-r7prep-array-lower) merge — r8 consumes its kind:\"array\" lowering and recursive _types_equal; can proceed earlier only as a stacked PR based on that branch. Gen2 C4 fix PR #4467 CI is unrelated to r8 code sites but shares the types.hexa file (merge-order conflict risk).

- r1: local `let x: T = <scalar-literal>` REJECT — literal RHS (int/float/string/bool/char) vs declared known-scalar annotation, HX3011  
  ↳ `compiler/check/types.hexa:2242-2262 (gate at 2242, literal arm 2252, emit 2260)`
- r2: `let x: T = <ident|call>` REJECT — inferred known-scalar ident/call RHS vs declared scalar; RHS re-inferred into a scratch sink to avoid double-report  
  ↳ `compiler/check/types.hexa:2263-2297 (soundness gates 2277/2291, emit 2293)`
- r3: decl/init-split assign REJECT — `let x: T` recorded in block env, later `x = <expr>` scalar mismatch; suppresses generic HX3001 via r3_rejected  
  ↳ `compiler/check/types.hexa:2314-2334 (env recording in Block case 2044-2057, emit 2332)`
- r4: index-assign element-fit REJECT — `a[i] = <expr>` where the block-local `let a: [i32]` recorded its scalar element under env key `[]<name>`; only that recorded case, params/module arrays silent  
  ↳ `compiler/check/types.hexa:2335-2363 (env key lookup 2356, recording 2073-2076, emit 2359)`
- r5: struct field-assign REJECT — `s.field = <expr>` via (struct,field)->typename side registry; receiver name from `{}<name>` env slot or scratch re-infer  
  ↳ `compiler/check/types.hexa:2364-2409 (registry lookup 2398, emit 2404)`
- r6: struct-literal field-init REJECT — `Point{x: <expr>}` per-field declared type vs inferred value scalar, reusing the r5 registry  
  ↳ `compiler/check/types.hexa:2512-2561 (gate 2535, emit 2550)`
- Two additional HX3011 emitters are NOT Wall A rungs: the shared emitter fn and the HEXA_HKT kind-arity warning  
  ↳ `compiler/check/types.hexa:1592 (_emit_hx3011) and 3587 (HKT, gated HEXA_HKT at 3600)`
- Candidate 'match-arm consistency' is ALREADY on-path default-ON: _check_match emits HX3005 when arm body types disagree via _types_equal — a new r8 rung here would be redundant; ARRAY_LOWER extends it to arrays for free once ArrayLit infers structurally. LOW value  
  ↳ `compiler/check/types.hexa:2853-2892 (equality check 2884, emit _emit_hx3005 2885)`
- Candidate 'fn-arg type check' is ALSO already on-path (HX3003) for resolved callees — callee param types are lowered via _types_lower_type_ref (so flag-ON they become structured kind:"array") and compared with _types_equal; PR #4457's test proves ident-arg array mismatch rejects in BOTH modes. The unchecked hole is an ArrayLit ARGUMENT: ArrayLit has NO inference arm, falls to the STUB v1 fallthrough returning empty type, which _types_equal tolerates — `g([1.0])` vs `fn g(xs: [i32])` passes silently in both modes. HIGH value, HIGH implementability  
  ↳ `compiler/check/types.hexa:2822-2845 (HX3003 emit 2840), callee sig lowering 3621, param binding 3804-3813, STUB fallthrough 2563-2571 (no ArrayLit arm — grep 'ArrayLit' hits only ExprKind match tables), PR diff test comment 'flag-OFF sentinels generic:Array:f64 ≠ generic:Array:i32'`
- Candidate 'index-assign element mismatch' residual gap = array-typed fn params / module lets (no `[]<name>` env record). With ARRAY_LOWER, params bind as structured kind:"array" (via _types_bind_params:3813 → _types_lower_type_ref), so the r4 arm can fall back to re-inferring the base ident and reading args[0]. MEDIUM value (narrower pattern than call-args), HIGH implementability (few lines inside the existing r4 arm)  
  ↳ `compiler/check/types.hexa:2354-2363 (r4 arm), 3813 (param lowering), PR #4457 diff to _types_lower_type_ref (kind:"array" with structured args[0])`
- Degraded sentinel at types.hexa:931-939 (the post-scalar fallthrough of _types_lower_type_ref) still blocks: ALL generic type refs (flag-OFF Array included; flag-ON only Array is rescued — Option<T>/Map<K,V> etc. still collapse), fn-type refs, tuples, and atlas_kind — all become string sentinel kind:"<kind>:<name>" with args DROPPED, so no structural element/param/field info reaches any rung; nested arrays [[i64]] DO lower flag-ON (the new branch recurses on args[0])  
  ↳ `compiler/check/types.hexa:931-939 ('generic / fn / tuple / atlas_kind — degrade gracefully', args: []), PR #4457 diff branch condition `tr.kind == "generic" && _types_is_array_name(tr.name)` with recursive `_types_lower_type_ref(tr.args[0])``
- r7-prep #4457 delivers exactly 3 pieces: structured `[T]` display in _type_display, opt-in kind:"array" lowering in _types_lower_type_ref, and recursive array equality in _types_equal — plus test case (o) using new helpers _generic_array_type_ref/_array_param  
  ↳ `git diff main...origin/feat/static-types-r7prep-array-lower — compiler/check/types.hexa +71, types_test.hexa +84; test case name '(o) static-types r7-prep — array element preserved across lowering'`
- Test naming convention: builder `_build_case_static_types_rN()` + runner label '(<letter>) static-types rN — <desc> (<FLAG>)'; letters run h(r1) i(r2) j(r3) k(r4) l(r5) m(r6) n(own_lint) o(r7-prep) → r8 = case (p)  
  ↳ `compiler/check/types_test.hexa:588-815 (builders), 954-1075 (runner labels), PR diff '+(o) …r7prep'`

**제안**: r8 = "array-literal call-arg element REJECT" (primary) + r8b index-assign param-base fallback (same PR, few lines). PRIMARY — flag-gated structural ArrayLit inference so the EXISTING default-ON HX3003 call-arg check catches `g([1.0])` vs `fn g(xs: [i32])`. Code sites: (1) new predicate `_types_is_array_lit(k: ExprKind)` mirroring `_is_match_expr` (compiler/check/types.hexa:773 idiom; ExprKind::ArrayLit already exists in every kind-match table). (2) Insert a gated arm immediately BEFORE the STUB v1 fallthrough at compiler/check/types.hexa:2563: `if env("HEXA_STATIC_TYPES") == "1" && env("HEXA_STATIC_TYPES_ARRAY_LOWER") == "1" && _types_is_array_lit(e.kind)` — walk all children into `out` (preserving the STUB's inner-error surfacing), unify element kinds: every child infers to the SAME known scalar (`_types_kind_is_scalar`) → return Type{kind:"array", args:[elem], …}; empty/mixed/non-scalar → return _types_empty_type() (conservative, mirrors r2 gate ②). (3) ZERO change at the HX3003 site (types.hexa:2822-2845) — callee params already lower structurally via _collect_item_types→_types_lower_type_ref:3621 and #4457's _types_equal array recursion does the reject. (4) r8b: in the r4 arm after the `[]<name>` lookup at types.hexa:2356 — when elem_t is non-scalar, re-infer `base` into a SCRATCH sink; if base_t.kind=="array" && args[0] scalar, fit-check vs rhs_t → _emit_hx3011 + r3_rejected=true (covers `fn f(xs: [i32]) { xs[0] = "s" }`). Gating: both env checks, flag first operand short-circuit (r1-r6 idiom) — default build byte-identical, byteeq 3-target neutral; HX3003/HX3011 gain firings only flag-ON. Test: `_build_case_static_types_r8()` + `_run_case("(p) static-types r8 — array-literal call-arg element mismatch (HEXA_STATIC_TYPES_ARRAY_LOWER)", …)` reusing #4457's _array_param/_generic_array_type_ref helpers; contract: exactly 1 HX3003 flag-ON, 0 flag-OFF (mode-DIFFERENT, unlike case (o)'s mode-identical contract — assert both legs). Base the branch on feat/static-types-r7prep-array-lower (stacked) or wait for #4457 merge.

**함정**:
- PR #4457 is OPEN, not merged — r8 depends on its _types_lower_type_ref/_types_equal changes; branch must stack on feat/static-types-r7prep-array-lower or wait for merge (rebase churn if #4457 changes)
- HX3003 is a default-ON Error-severity diag — the r8 gate MUST be the first short-circuit operand so ArrayLit keeps returning empty flag-OFF, else default diagnostic stream changes → byteeq 3-target break
- Structural ArrayLit inference flag-ON also feeds _check_match HX3005 (types.hexa:2884) and the generic HX3001 paths — new flag-ON diags beyond call-args are possible; keep the r8 test module minimal and don't assert total-diag-count==1 across unrelated constructs
- Two frontends: types_test.hexa must pass under BOTH gen2 C-transpile (self/*.hexa) and native aprime — aprime-only green is FALSE-green (memory: verify BOTH backends)
- Builds/byteeq run on pool hosts (aiden/summer); mini is git/gh only — do not build locally
- The r4 element path deliberately does NOT use _types_lower_type_ref (annotation lowered as kind:"named", element parsed from raw text via _types_array_elem_kind) — #4457's branch is kind:"generic"-gated and never fires there; r8b must not 'unify' these two paths (that would change r4 flag-ON behavior)
- Empty array literal `[]` has no element evidence — must return empty type, not kind:"array" with no args (though _types_equal tolerates len(args)==0, the display path would render bare 'array')

## Promote `tool/stdlib_selftest_aggregate.hexa --ci-gate --strict` from a never-run local tool to a real PR gate, closing the measured gap that @ci_gate-marked tests (e.g. stdlib/lang/defer_test.hexa) are never compiled/executed in CI — a t_cond-shaped compile error in defer_test shipped undetected until PR #4467.

**effort M** · blocked_by: 없음

- Discovery: the tool finds `stdlib self/stdlib` files named `*_test.hexa` or `test_*.hexa` (sorted LC_ALL=C), excludes self/stdlib/test_runner.hexa, excludes `@selftest_skip` in the first 10 lines, and with --ci-gate keeps only files whose FIRST 10 LINES contain `@ci_gate`. Discovery is cwd-relative (`find stdlib self/stdlib`), so it must run from the repo root.  
  ↳ `tool/stdlib_selftest_aggregate.hexa:26-49 (find cmd line 26; test_runner exclusion line 33; head-10 + @selftest_skip lines 34-35; @ci_gate filter lines 39-45)`
- Execution: per file it spawns `timeout 30 <hexa> <path>` (GNU timeout — needs a Linux runner or coreutils on macOS), captures combined output plus a `__RC=$?` marker, and classifies: any 'FAIL' substring in output → FAIL; rc==0 → PASS; rc==124 → TIMEOUT; other rc (including a compile error, or a missing __RC marker → rc=-1) → ERROR. So a t_cond-style compile failure is caught as ERROR. The hexa binary is PATH `hexa` by default, overridable via $HEXA_BIN (the old hardcoded /Users path was already removed for Linux CI).  
  ↳ `tool/stdlib_selftest_aggregate.hexa:53-62 (_hexa_bin/HEXA_BIN), 67-79 (run_capture/__RC + parse_rc idx<0→-1), 81-86 (classify), 88-92 (timeout 30 spawn)`
- Reporting/--strict: prints per-file `STATUS\tpath`, a `# summary pass= fail= timeout= error= total=` line, writes a jsonl side-file to state/stdlib_selftest_<iso>.jsonl, and with --strict exits 1 if fail+timeout+error > 0. `--ci-gate` IMPLIES strict (sets strict=true itself), so `--ci-gate --strict` is redundant-but-harmless and self-documenting.  
  ↳ `tool/stdlib_selftest_aggregate.hexa:104-111 (flag parse; line 109 `--ci-gate` sets strict=true), 130 (per-file line), 135 (summary), 137-147 (state/ jsonl), 149 (strict exit 1)`
- Census: reproducing the tool's exact discovery rule (test-name pattern + @ci_gate in head-10, no @selftest_skip) yields 95 files today: stdlib/cloud 45 · stdlib/qforge 13 (incl. 9 *_xval_test + elph selftest_test pairs) · stdlib/crypto 14 · stdlib/codec 9 · stdlib/deck 5 · stdlib/regex 2 · stdlib/time 2 · stdlib/core 2 · stdlib/lang/defer_test.hexa · stdlib/yosys/test/read_verilog_width_cap_test.hexa. self/stdlib currently contributes 0. A naive `grep -rl '@ci_gate'` over-counts (~230 hits) because it matches non-test files (stdlib/README.md, stdlib/crypto/ed25519.hexa, stdlib/demi/*.hexa etc.) and body-text mentions that the head-10 filter excludes.  
  ↳ `reproduction command `find stdlib self/stdlib -type f \( -name '*_test.hexa' -o -name 'test_*.hexa' \) | while … head -10 | grep @ci_gate` → 95 paths (captured in this session); filter semantics at tool/stdlib_selftest_aggregate.hexa:26-45; stdlib/lang/defer_test.hexa:1 carries the marker `// @ci_gate — pure · network-free · deterministic``
- Runtime risk: worst case is hard-bounded at 95 × 30s ≈ 48 min by the per-file timeout (tool line 89). The @ci_gate cohort is documented as the 'curated, network-free, deterministic subset' (line 36-38), so most files (cloud parse/routing tests, codec, crypto unit vectors, deck, regex, time) should run in seconds under the interp. The slow-risk cohort is stdlib/qforge: 9 QE-xval + elph tests (qforge_cah6/lah10/l3_qe_xval ~300 lines each of DFT numerics, al/nb/pb elph) — these are the plausible per-file multi-second-to-timeout candidates; crypto scrypt/pbkdf2/secp256k1 are second-tier risk (iterated KDF/EC math in an interp). Unmeasured on mini per repo rule (heavy runs go to the pool) — first advisory CI runs are the measurement.  
  ↳ `tool/stdlib_selftest_aggregate.hexa:36-38 (curated-subset comment), 89 (timeout 30); wc -l stdlib/qforge/*_xval_test.hexa → 2293 lines total, e.g. stdlib/qforge/qforge_lah10_qe_xval_test.hexa 334 lines`
- CI placement: do NOT touch selfhost-gates-required.yml — it is deliberately the SINGLE always-on required check (no path filter, ubuntu-24.04, runs tool/selfhost_gates_summary.sh) and is safe to require precisely because it never depends on anything else; a new job must be a separate workflow so the required-check rule is untouched. The house pattern to copy 1:1 is the fallback-dispatch picker: a cheap ubuntu-latest `dispatch` job runs `.github/actions/pick-runner` (kind: linux → self-hosted ["self-hosted","Linux","X64","hexa-build"] i.e. aiden/summer when online, else "ubuntu-latest"; fork-PR eligible-guard + RUNNER_PROBE_TOKEN||github.token), and the real job uses `runs-on: ${{ fromJSON(needs.dispatch.outputs.linux_label) }}`. A working `./hexa` is produced in-workflow by `bash tool/release_build` with the `.github/actions/ccache-setup` accelerator (determinism-gate and nobaseline-gate both do exactly this), then invoked via `./hexa run tool/<x>.hexa <args>` (existing precedent: HEXA_LD='./hexa run tool/hexa_ld.hexa').  
  ↳ `selfhost-gates-required.yml:1-35 (always-on required aggregator, ubuntu-24.04, bash tool/selfhost_gates_summary.sh, 'the SINGLE always-on required check' header); determinism-gate.yml:70-100 (dispatch/pick-runner, eligible fork guard, RUNNER_PROBE_TOKEN), 101-125 (needs: dispatch, fromJSON runs-on, ccache-setup, tool/release_build, ./hexa --version sanity), 137/152 ('./hexa run tool/hexa_ld.hexa' precedent); nobaseline-gate.yml:57-78 (kind: linux picker), 263-303 (linux job fromJSON label + release_build); tool/ci_pick_runner.sh:40-53 (linux → hexa-build labels, fallback ubuntu-latest), 30-36 (fail-safe fallback)`
- Concurrency/permissions boilerplate is uniform across gates and should be copied: `permissions: contents: read` and `concurrency: group: ${{ github.workflow }}-${{ github.ref }} / cancel-in-progress: ${{ github.event_name == 'pull_request' }}` (PR supersede-cancel only, main pushes complete).  
  ↳ `no-hardcode-gate.yml:24-35 and determinism-gate.yml:58-68 (identical block + rationale comment)`

**제안**: Job spec — new standalone workflow `.github/workflows/stdlib-selftest-gate.yml` (advisory-first, NOT added to branch protection and NOT referenced by selfhost-gates-required.yml): (1) triggers: pull_request + push[main] + workflow_dispatch, path-filtered to `stdlib/**`, `self/stdlib/**`, `compiler/**`, `self/**`, `tool/stdlib_selftest_aggregate.hexa`, and the workflow file itself (compiler/self paths are essential — the motivating defect was a compiler-side t_cond compile error, not a stdlib edit); standard permissions/concurrency block copied from determinism-gate.yml:58-68. (2) two jobs copied 1:1 from the determinism-gate/nobaseline-gate pattern: `dispatch` (ubuntu-latest, 5 min, `.github/actions/pick-runner` with kind: linux, same-repo eligible guard, `token: ${{ secrets.RUNNER_PROBE_TOKEN || github.token }}`) then `stdlib-selftest` with `needs: dispatch`, `runs-on: ${{ fromJSON(needs.dispatch.outputs.linux_label) }}` — Linux is required for GNU `timeout` used at tool line 89. (3) steps: checkout@v6 → `.github/actions/ccache-setup` → `bash tool/release_build` (env TARGET=linux-x86_64, CC=clang, HEXA_CCACHE=1, GH_TOKEN) → `./hexa --version` sanity → the gate itself, run from the repo root (discovery is cwd-relative, tool line 26): `HEXA_BIN=./hexa ./hexa run tool/stdlib_selftest_aggregate.hexa --ci-gate --strict` → `actions/upload-artifact` of `state/stdlib_selftest_*.jsonl` with `if: always()` for triage. (4) budget: `timeout-minutes: 60` initially (release_build ~10-20 min on ubuntu-latest with ccache per existing gates + hard-bounded 48-min worst case for 95×30s tests; expected real total is minutes since the cohort is curated network-free interp tests). Promotion ladder: merge advisory → capture wall-time + summary line from the first week of PR runs → if the qforge xval cohort dominates (>10 min) demote it via `@selftest_skip`-style curation or a `--ci-gate-fast` tier in a follow-up → only after a green week with total wall-time under ~25 min, add `stdlib-selftest` as its own required branch-protection check (path-filtered required checks deadlock PRs that don't touch the paths — the same reason the six selfhost gates aren't individually required, per selfhost-gates-required.yml header — so EITHER drop the path filter before requiring it, accepting the build cost on every PR, OR keep it advisory-but-monitored; recommend requiring it unfiltered since a compiler regression is exactly what it must catch).

**함정**:
- Do not edit selfhost-gates-required.yml or its summary script — it is the single always-on required check by design (header comment lines 1-8); a new gate must be a separate workflow.
- `grep -rl '@ci_gate'` over-counts (~230): the tool only honors the marker in the FIRST 10 LINES of files matching *_test.hexa/test_*.hexa (tool lines 26-45); real cohort today = 95 files.
- The aggregate must run from the repo root — `find stdlib self/stdlib` (line 26) and `state/` jsonl output (line 138) are cwd-relative.
- GNU `timeout` (line 89) is assumed — route the job to Linux (picker kind: linux); macos-15 fallback hosts lack `timeout` by default.
- classify() marks FAIL on ANY 'FAIL' substring in combined output (line 82) — a test whose PASS output prints e.g. '0 FAILED' would false-fail; audit new @ci_gate tests' output text.
- `hexa` on PATH does not exist on cloud runners and ~/.hx binaries on pool runners can be stale (local-hexa-stale-oracle precedent) — always pass HEXA_BIN=./hexa built by tool/release_build in the same job.
- Fork-PR RCE guard is mandatory: self-hosted is only a candidate when the same-repo eligible flag is 1 (determinism-gate.yml:93, ci_pick_runner.sh SAFETY section).
- Making the new check required WITH a path filter deadlocks non-matching PRs (pending-forever status) — require it only unfiltered, or keep advisory.
- Builds/measurement happen in CI or on the pool (aiden/summer) — do not benchmark the 95-file suite on mini (repo rule: mini = git/gh/read only).
- --ci-gate already implies --strict (line 109); keep both flags anyway for self-documenting intent, but don't 'fix' the redundancy by dropping --ci-gate.

## HEXA-OWN L2.5 region/arena escape checks — design census of the existing fn-arena substrate (runtime + gen2 codegen + @no_arena), existing escape/heapify analyses, the compile-time layer with lifetime-ish info, and the smallest useful lint rung.

**effort M** · blocked_by: 없음

- Arena runtime core lives entirely in self/runtime_core.c: __hexa_fn_arena_enter at :4600 (scope_push if arena on), __hexa_fn_arena_return at :4605 (primitive short-circuit :4612-4617, then heapify+scope_pop; optional region-promote via __hexa_val_region_returns_enabled at :4621-4624), scope_push :3850, scope_pop :3911, hexa_val_arena_on :3811 (default-ON, HEXA_VAL_ARENA env override :3819-3821), bump alloc hexa_val_arena_calloc :3829, hexa_val_arena_heapify_to_parent :4566 (temp-malloc 4-step overlap-safe region promote), env-hook heapify_return :4589. Decls exported via self/runtime_core_decls.h:169-170.  
  ↳ `self/runtime_core.c:4600-4627, 3811-3824, 3850, 3911, 4566-4574, 4589; self/runtime_core_decls.h:169-170`
- Codegen emission is GEN2-ONLY: self/codegen.hexa gen2_fn_decl computes has_no_arena = gen2_has_attr(node, "no_arena") at :2080; if absent it emits `__hexa_fn_arena_enter();` at fn top (:2084) and wraps every return — implicit tail-return `return __hexa_fn_arena_return(...)` (:2247-2249 region), defer-drain footer (:2293-2298), and the void safety footer `return __hexa_fn_arena_return(hexa_void());` (:2301-2304). The mirrored native-C artifact is self/native/hexa_cc.c:19795-19937. The native aprime pipeline emits NO arena calls — compiler/codegen/arm64_darwin.hexa:4193 and x86_64_linux.hexa:1051 only mention __hexa_fn_arena_return as the runtime behavior the compiler-as-a-gen2-program is subject to.  
  ↳ `self/codegen.hexa:2076-2085, 2247, 2296-2304; self/native/hexa_cc.c:19795-19937; compiler/codegen/arm64_darwin.hexa:4185-4200 (comment only, no emission); grep 'hexa_fn_arena' in compiler/codegen = comments only`
- @no_arena is a gen2-parser allowlisted attr (self/parser.hexa:335 in _p_is_known_attr; commented in the allowlist header :280) that sets _gen2_arena_wrap=0 and skips enter/return emission entirely (self/codegen.hexa:2080-2082, 2296-2303). It is UNKNOWN to the native aprime frontend — zero hits for no_arena in compiler/parse/ or compiler/check/. Contract per stdlib usage: helpers callable from inside heapify must not touch the arena (self/codegen.hexa:2078-2079); stdlib/runtime/numeric.hexa:1690-1693 documents the corruption class when it is missing (rt_cmp_le return → scope_pop → rewind re-entry during heapify) and :1287-1293 pins the 'returns bool only, zero alloc' contract with a 🚫 do-not-remove marker; self/parser.hexa:2079 records a real regression where p_advance's @no_arena 'vanished'.  
  ↳ `self/parser.hexa:280, 335, 487, 504, 528, 2079; self/codegen.hexa:2078-2082; stdlib/runtime/numeric.hexa:1287-1293, 1690-1693; grep no_arena compiler/parse compiler/check = 0 hits`
- Escape bugs possible TODAY (runtime comments admit them): (a) globals escape unheapified — only the RETURN value is heapified, so an arena-allocated composite stored into a module global or out-of-band sink before a void return dangles after scope_pop (self/runtime_core.c:3432 '__VAL_INT_CACHE, env escape unheapified'); (b) push-into-outer-array dangle was a shipped bug, now patched dynamically by heapify-on-push inside hexa_array_push (:2152-2161, 'FIX 2026-05-13 wilson-fn-arena-escapes-on-push'); (c) string-arena escapes via map inserts/closure captures have NO automatic escape analysis — safety relies on the heapify deep-walk (:4633-4637); (d) scope-pop rewind turning still-referenced elements into dangling memory (:3978-3980, T33 arena-dangling defence :6376, stale-tag HexaValStruct :3782); (e) region-returns mode (heapify_to_parent) promotes only ONE frame up — storage beyond the parent still dangles when the parent pops (:4566-4574); (f) a mis-dropped @no_arena silently re-enables arena wrapping inside heapify → corruption (numeric.hexa:1690-1693) with no compile-time guard.  
  ↳ `self/runtime_core.c:3432, 2152-2161, 4633-4637, 3978-3980, 3782, 6376, 4566-4574; stdlib/runtime/numeric.hexa:1690-1693`
- Existing compile-time escape analysis (reusable substrate): gen2 UNSHADOW F2 already does whole-fn-body, name-based escape proof — _stmt_escapes_name (self/codegen.hexa:12400-12455: return-of-name, any expr position other than name.field, and reassignment all count as escape), _stmts_escape_name :12458, _stack_noescape_scan :12595, _native_arr_noescape_scan :12662; driven from gen2_fn_decl at :1963-1990 and consumed at LetStmt lowering :3314 (stack struct) / :3331 (native array). It currently only POWERS OPTIMIZATIONS (stack/native promotion), never emits a diagnostic. Dynamic 'escape analysis' = heapify on return + heapify on push only.  
  ↳ `self/codegen.hexa:1963-1990, 12313, 12400-12466, 12595, 12662, 3314, 3331; self/runtime_core.c:3600-3601`
- Compile-time layers with lifetime-ish info: (1) native MIR already models regions — every Local carries arena_id (compiler/ir/mir.hexa:30, '0 = global/static, >0 = function-local arena') and every MFunc carries arena_root (:130), per RFC-018 §5 v1 = one arena per function (compiler/lower/hir_to_mir.hexa:13-16); mir_test.hexa:1186-1196 already asserts arena_id==arena_root. (2) The HEXA-OWN lint layer in compiler/check/types.hexa is the proven warning-band attach: per-fn parallel-array registry _own_lint_* :1211-1263, touch sites at let-RHS :2304, assign-RHS :2424, call-args :2774, registration :2111-2116, per-fn reset :3907-3908, emitter _emit_hx3012 :1600-1612, catalog entry compiler/diag/catalog.hexa:357 (Severity::Warning, flag-gated byteeq-neutral). (3) gen2 side has _stmt_escapes_name at AST level (above).  
  ↳ `compiler/ir/mir.hexa:30, 130; compiler/lower/hir_to_mir.hexa:13-16; compiler/lower/mir_test.hexa:1186-1196; compiler/check/types.hexa:1211-1263, 1600-1612, 2111, 2304, 2424, 2774, 3907-3908; compiler/diag/catalog.hexa:357`
- Diag code space: HX3012 (use of moved value, L2 r1 #4455) is the newest 3xxx code; HX3013 is free. HX3012's spec (catalog.hexa:357-364) establishes the exact pattern L2.5 should clone: opt-in env flag, Severity::Warning, stage S3, default diagnostic stream byte-identical.  
  ↳ `compiler/diag/catalog.hexa:277-364 (HX3001..HX3012 enumeration); a4f511afe 'feat(hexa-own L2 r1): HEXA_OWN_LINT — HX3012'`

**제안**: L2.5 r1 = HX3013 "arena-escape lint", opt-in `HEXA_ARENA_LINT=1`, warning-band, two sub-rules matching the two documented holes, wired to the two frontends where each is expressible. Rule A (gen2, guards hole (f)+(b)-class): warn when an `@no_arena` fn body contains a composite allocation site (array/map/struct literal, string concat) or returns one — violates the documented zero-alloc contract (numeric.hexa:1292) and hands out caller-frame memory with no heapify; attach in self/codegen.hexa gen2_fn_decl right after `has_no_arena` is computed (:2080), reusing the existing body-walk skeleton of _stack_noescape_scan/_stmt_escapes_name (:12400/:12595) as the scanner; emit as a stderr warning line (gen2 has no diag catalog). Rule B (native aprime, guards hole (a)): warn when a fn-local composite (`let x = [..]/{..}/Struct{..}`, arena_id > 0 conceptually) is assigned to a module-level `pub let mut` global — the runtime-admitted unheapified escape (runtime_core.c:3432); attach in compiler/check/types.hexa alongside the _own_lint registry (register locals where @own registers at :2111, check the assign-RHS site :2424 when the LHS resolves to a module global), new `_emit_hx3013` cloned from `_emit_hx3012` (:1600), catalog DiagSpec after HX3012 (catalog.hexa:~365, Severity::Warning, stage S3). Both rules fire only under the flag → default diag stream byte-identical → byteeq 3-target trivially neutral. If one rung must be picked as smallest: Rule A alone (single attr population ~40 stdlib sites, pure gen2, one file + tests), since the prompt's 'returns arena ref without heapify' case is exactly and only the @no_arena return path (non-@no_arena returns are always heapified by __hexa_fn_arena_return).

**함정**:
- The example rung as literally stated ('fn returns arena array without heapify') is a near-no-op on the default path: every non-@no_arena return IS heapified by __hexa_fn_arena_return (runtime_core.c:4605). The real unguarded escapes are @no_arena returns and global/out-of-band stores — target those.
- @no_arena exists only in the gen2 frontend (self/parser.hexa:335); the native aprime parser has zero no_arena handling, so Rule A cannot be mirrored natively without first porting the attr — don't promise two-frontend parity for it.
- Byteeq discipline: the lint must be env-flag-gated at every emission site (HX3012 pattern, types.hexa:2111 `env("HEXA_OWN_LINT")=="1"`); an unconditional registry populate or stderr print changes gen2-emitted C or the diag stream and breaks the 3-target gate.
- gen2 stderr output ordering: gen2_fn_decl warnings must not interleave into the emitted C chunks array — write to stderr only, never chunks, or the .c artifact changes.
- Repo greps are polluted by .claude/worktrees/* copies (ci-speedup-l5, wf_*) — always exclude them or you'll cite stale duplicate sites.
- Runtime heapify-on-push (runtime_core.c:2152-2161) already dynamically covers array-push escapes — a compile-time warn on push-into-outer-array would be noise, not safety; scope Rule B to globals where the runtime explicitly does NOT cover (:3432).
- @own L2 registry is per-fn and END-scanned for shadowing (types.hexa:1228-1229); an L2.5 registry sharing it must not cross-contaminate HX3012 state — use parallel arrays with their own reset at the :3907 fn-visit reset point.
- Builds/verification on pool hosts (aiden/summer) only; mini is git-only. Gen2 and native frontends must be verified separately (verify BOTH backends memory: aprime-only green = FALSE-green).

## 합성 실행계획 (synthesizer)

# EXECUTION PLAN — HEXA-OWN / static-types / parser / CI frontier synthesis

## (1) Recommended order

| # | Round | Effort | Start condition | Rationale |
|---|-------|--------|-----------------|-----------|
| R1 | native parser bare-return same-line fix | S | now, parallel worktree | Zero deps, unique files (`compiler/parse/parser.hexa`, `parser_test.hexa`) — touches nothing any other round touches. Correctness-bug class (silent next-line swallow), smallest CI footprint. Verified the site: value guard at compiler/parse/parser.hexa:1494 has no line check and `kw` is already captured at :1489. |
| R2 | stdlib-selftest CI gate (advisory) | M | prepare now; **merge after #4467** | Workflow-file-only — no source conflicts with anything. Advisory-first per its own proposal. Sequencing nuance: the motivating broken file `stdlib/lang/defer_test.hexa` is fixed *by* #4467 (verified: #4467's file list includes `stdlib/lang/defer_test.hexa`), so landing the gate before #4467 merges yields a red first advisory run on main. Merging R2 early-ish then protects every later types.hexa round with extra compiler-regression coverage. |
| R3 | @own L2 r2 (RETURN + PARAM positions) | M | now for RETURN half; PARAM half needs the user's @own-on-param surface decision (state/hexa-own/L2_own_static_integration.md:23) | First of the three types.hexa-serialized rounds because it is unblocked (no dependency on #4457) and its catalog.hexa HX3012-explain edit must land before L2.5's HX3013 append (adjacent lines). If the user decision is not obtained promptly, ship RETURN-only and keep PARAM as r2b. |
| R4 | static-types Wall A r8 (ArrayLit call-arg REJECT + r8b) | M | after #4457 merges (13/22 checks green, 0 failures as of now) — or stacked on `feat/static-types-r7prep-array-lower` | Hard-blocked on #4457's `_types_lower_type_ref`/`_types_equal` changes. Sequence after R3 so its types.hexa/types_test.hexa rebase is one-way (R3's Return-arm edit at :2204 and per-fn-loop edit at :3908 shift lines above/below r8's :2563 insertion point). |
| R5 | HEXA-OWN L2.5 arena-escape lint (HX3013) | M | last — after #4467 AND R3 | Triple-conflicted: `self/codegen.hexa` (Rule A at gen2_fn_decl :2080 — verified; #4467's file list includes self/codegen.hexa), `compiler/diag/catalog.hexa` (HX3013 appends directly after HX3012 at :364 — verified HX3012 spans catalog.hexa:356-364 and HX3013 is unused), `compiler/check/types.hexa` (Rule B shares the assign-RHS site :2425 with the own-lint registry). Also benefits from reusing R3's finalized registry idioms rather than forking r1's. |

CI-load note: R3/R4/R5 each trigger full byteeq 3-target + both-frontend verification on the pool — serializing them avoids rebase→re-CI churn on the same file. R1+R2 are safely parallel (disjoint files).

## (2) Cross-frontier conflicts (same files)

- **compiler/check/types.hexa** — touched by: #4457 (pending, verified file list), R3 (own-l2-r2: :2204-2206 Return arm, :3908 per-fn loop), R4 (static-r8: :2563 STUB fallthrough, :2356 r4 arm), R5 (Rule B: :2425 assign-RHS, new registry near :1263). Regions don't textually overlap, but line drift makes shared worktrees toxic — **one isolated worktree per round (repo rule), merge order #4457 → R3 → R4 → R5, rebase before each PR**.
- **compiler/diag/catalog.hexa** — R3 edits HX3012's explain (catalog.hexa:362, verified stale text naming "fn params, return values … are later rungs"); R5 appends the HX3013 DiagSpec immediately after (:364). Adjacent-line guaranteed conflict → strict R3-before-R5.
- **self/codegen.hexa** — #4467 (gen2 defer reroute) vs R5 Rule A (gen2_fn_decl :2080-2086, verified, plus escape-scan reuse ~:12400). Wait for #4467 merge; do not stack on a pending-CI gen2 PR.
- **self/parser.hexa** — R3's *optional* gen2 `@own` allowlist commit (`_p_is_known_attr` ~:335). Gen2-binary-touching → own faithful/byteeq gate; keep it a separate commit (or separate PR) so the aprime-only r2 body isn't held hostage.
- **stdlib/lang/defer_test.hexa** — fixed by #4467 and simultaneously in R2's @ci_gate cohort → merge-order dependency R2-after-#4467 (or accept a red advisory week).
- **compiler/check/types_test.hexa** — #4457 (+84 lines) and R4's case (p); resolved automatically by stacking/waiting.
- **⚠️ un-censused conflict**: stale open PR **#4088 `feat/borrow-checker`** also touches `compiler/check/types.hexa` AND `compiler/diag/catalog.hexa` (verified via `gh pr view 4088 --json files`), and #4089 `feat/lifetime-annotations` is conceptually the same ladder. Neither census mentioned them. Before R3 lands, decide their disposition (close-preserve-branch or rebase) — otherwise they become permanent conflict debt against the whole HEXA-OWN ladder.

## (3) Concrete steps — top-2 rounds

### R1 — native bare-return same-line fix (hand-off ready)

1. Worktree: fresh isolated worktree off `main` (repo rule feedback_parallel_agents_isolated_worktree), branch `fix/native-parser-bare-return-sameline`.
2. Edit `compiler/parse/parser.hexa` parse_return_expr — change line 1494 guard to add a same-line condition using the already-captured `kw` (line 1489):
   `if !check_kind(TokenKind::RBrace) && !check_kind(TokenKind::Semicolon) && !at_eof() && peek().span.line == kw.span.line { … }`
   (`Span.line` exists per compiler/lex/tokens.hexa:13-18 — no lexer change.)
3. **Do NOT touch parse_throw_expr (:1578-1585) in this PR** — see §4: the census's throw-parity claim is wrong; throw needs its own decision.
4. Also update the stale comment at parser.hexa:1492-1493 ("we always emit a child here") — it already lies today.
5. Extend `compiler/parse/parser_test.hexa` (near the existing return corner-cases ~:124-134): (a) bare `return` + next-line `defer { f() }` → Return has 0 children, defer parses as sibling; (b) bare `return` + next-line call `g()`; (c) bare `return` + next-line `let`; (d) positive controls `return a +\n b` and `return foo(\n arg)` → 1 child (first post-return token is same-line).
6. gen2 cross-check: add the same snippet to gen2's parser test corpus (self/test_parser_full.hexa) asserting both frontends agree; `self/parser.hexa` itself UNCHANGED.
7. Verify on pool (aiden/summer — mini is git-only): both-frontend test runs + byteeq 3-target. This changes native-parser behavior for the whole corpus, so byteeq is the real gate: any corpus file silently relying on next-line return values would surface here (none can exist — gen2 already rejects that shape).
8. PR; merge on 3-target GREEN + shipping smoke. No flags needed (this is a bug-fix to match gen2, not a feature).

### R2 — stdlib-selftest advisory gate (hand-off ready)

1. Branch `feat/ci-stdlib-selftest-gate`; single new file `.github/workflows/stdlib-selftest-gate.yml`. **Do not touch `selfhost-gates-required.yml`.**
2. Triggers: `pull_request` + `push: [main]` + `workflow_dispatch`; path filter `stdlib/**`, `self/stdlib/**`, `compiler/**`, `self/**`, `tool/stdlib_selftest_aggregate.hexa`, the workflow file itself.
3. Copy the boilerplate verbatim from `determinism-gate.yml:58-100`: `permissions: contents: read`; concurrency group `${{ github.workflow }}-${{ github.ref }}` with PR-only cancel-in-progress; `dispatch` job (ubuntu-latest, 5 min) running `.github/actions/pick-runner` with `kind: linux`, same-repo eligible guard, `token: ${{ secrets.RUNNER_PROBE_TOKEN || github.token }}`.
4. Main job: `needs: dispatch`, `runs-on: ${{ fromJSON(needs.dispatch.outputs.linux_label) }}` (Linux mandatory — GNU `timeout` at tool/stdlib_selftest_aggregate.hexa:89, verified), `timeout-minutes: 60`.
5. Steps: checkout@v6 → `.github/actions/ccache-setup` → `bash tool/release_build` (TARGET=linux-x86_64, CC=clang, HEXA_CCACHE=1) → `./hexa --version` sanity → from repo root (discovery is cwd-relative, tool :25): `HEXA_BIN=./hexa ./hexa run tool/stdlib_selftest_aggregate.hexa --ci-gate --strict` → `actions/upload-artifact` of `state/stdlib_selftest_*.jsonl` with `if: always()`. Keep both flags even though `--ci-gate` implies strict (verified at tool :109-110).
6. **Advisory only** — no branch-protection change, no reference from the required aggregator.
7. Merge-order: hold merge until #4467 lands (its fix to `stdlib/lang/defer_test.hexa` is what makes the cohort green), or merge earlier accepting a known-red advisory that #4467 turns green — the latter actually *demonstrates* the gate catching the exact motivating defect; either is defensible, state the choice in the PR body.
8. Week-1: capture wall-time + summary line from PR runs; if the qforge xval cohort dominates, curate via `@selftest_skip`/fast-tier follow-up. Promotion to required only unfiltered (path-filtered required checks deadlock non-matching PRs).

## (4) Census errors / unverifiable items

1. **WRONG (census 1, throw parity)**: "bare `throw` is invalid in both frontends; throw … NOT a gen2-divergence." Verified false at the parser level: native `parse_throw_expr` (compiler/parse/parser.hexa:1578-1590) has the **identical conditional guard** as return and happily produces a 0-child Throw before `}`/`;`/EOF, while gen2 `parse_throw_stmt` (self/parser.hexa:4380-4383, verified `parse_expr()` unconditional) always requires a value. So throw **is** a frontend divergence (native-tolerant), and it carries the identical next-line swallow hazard. The census's *action* (leave throw untouched in R1) is still correct-conservative, but the follow-up ticket should be "native throw: match gen2 by parsing the value unconditionally (or error on bare throw)", not "no divergence."
2. **Unverifiable as stated (census 4)**: the "95 files" @ci_gate cohort count is a session-local reproduction, not re-verified here (repo rule: no heavy runs on mini). Note the tool's own comment says "207 discovered tests" total (tool/stdlib_selftest_aggregate.hexa:36-38, verified) — treat 95 as approximate until the first advisory CI run prints its own `total=`.
3. **Unverifiable/likely-stale minor line numbers (census 2/3)**: own-lint touch sites are at types.hexa:2305/2425/2775 (verified via grep), i.e. censuses' "2304-2305" style ranges are fine but single-line citations drift by ±1; same for the r8 STUB region (verified the STUB comment + empty-type return sit in the 2561-2571 window). No substantive impact.
4. **Omission (all censuses)**: open PRs **#4088 feat/borrow-checker** (touches compiler/check/types.hexa + compiler/diag/catalog.hexa, verified) and #4089 feat/lifetime-annotations were not surfaced by any census despite sitting directly on the HEXA-OWN ladder's files and diag-code space (HX3013 collision risk if #4088 also claims new 3xxx codes — check before R5 assigns HX3013).
5. **Confirmed-accurate spot-checks** (no repetition needed): parse_return_expr guard (parser.hexa:1488-1505), Return-arm inference site (types.hexa:~2203-2206), per-fn `_own_lint_reset` gate (types.hexa:3907-3909), HX3012 catalog entry + stale explain text (catalog.hexa:356-364), HX3013 free (grep: no hits), `--ci-gate`⇒strict (tool :109-110), arena enter/return (self/runtime_core.c:4600-4610), gen2 `has_no_arena` emission (self/codegen.hexa:2076-2086), #4457 file list = types.hexa+types_test.hexa+CHANGELOG only, #4457/#4467 both OPEN with 0 failing checks.
