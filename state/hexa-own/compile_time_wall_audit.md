# own-link 컴파일 시간 벽 — 워크플로우 wf_86d6b043-466

생성 2026-07-14 · 25 에이전트 · 실측 프로파일 → 3렌즈 census → 적대적 반증 → 설계 3안 → 통합
반증 통과 병목 6건 · ★핵심: 내 census 표 자체가 반증됨(hexa build 가 실패해도 rc=0 · hx.hexa 는 죽은 소스)

```json
{
  "summary": "own-link 컴파일 시간 벽 근인 규명 — hx.hexa 300s+ 타임아웃 · phase 프로파일 → 병목 → 구현-ready 최적화 스펙",
  "agentCount": 25,
  "logs": [
    "병목: 기준 프로그램 = tool/aot_cc_select.hexa (flatten 7638 LOC, --emit=exec + own-link, wall 124.0s, rc=0 바이너리 실제 생성). 표면상 1위는 codegen 42.7s(34%). **하지만 근인은 codegen이 아니라 atlas 상주(residency)다.**\n\n★ 결정적 A/B (동일 파일, 동일 codegen 코드):\n  --emit=obj (atlas 스킵)  : codegen  7743ms · wall 16.80s · RSS 1.83GB\n  --emit=exec (atlas 상주) : codegen 42687ms · wall 124.01s · RSS 4.22GB\n\nphase별 배율 (obj → exec):\n  lex 930→966 (1.04×) · parse 1353→1397 (1.03×)   ← atlas_load **이전** 단계 = 무영향\n  ─ atlas_load 0 → 10992 ─\n  resolve 2.2× · bind 4.5× · type_check 5.2× · lower_ast_to_hir 5.2× · lower_hir 5.4× · codegen 5.5× · x86_pack_lir 4.4×\n  → atlas_load **이후** 모든 단계가 일률적으로 4.4~5.5× 느려진다. 깨끗한 인과 증거.\n\n메커니즘 (exec 경로 자체를 perf로 직접 확인 — 추론 아님):\n  hexa_arena_rewind 13.85% + __blk_set_used 12.54% + __blk_next 10.13% + __blk_used 2.20% = **38.7%가 arena 블록리스트 선형 스캔**\n  (+ hexa_eq 15.7% · __raw_cmp3 15.6% · rt_eq_int 7.7% · rt_cmp_le 6.3% — 그 스캔 루프 안에서 쓰이는 비교 프리미티브)\n  inclusive: __hexa_fn_arena_return 23.3% · hexa_arena_rewind 17.8% · hexa_val_arena_scope_push 8.6%\n  → 함수 호출마다 arena scope teardown이 블록리스트를 선형 순회한다. 비용이 **live 블록 수**에 비례하는데, 17354노드 atlas가 그 블록 수를 영구히 부풀린다. 즉 O(n²)의 n은 **프로그램 크기가 아니라 live arena 블록 수**다. 과거 3건과 같은 알고리즘 복잡도 클래스이되, **사이트는 새로운 곳**(arena rewind/블록 스캔).\n\natlas 총 비용 = 11.0s(로드) + ~61.9s(하류 phase 세금) ≈ **72.9s / 124s = wall의 59%**.\n그런데 compiler/main.hexa:727 주석이 스스로 밝히듯 atlas는 **citation_check만 소비**하고 codegen은 무시하며, 이미 --emit=obj에선 OOM 때문에 스킵된다. 게다가 --strict-citations는 default OFF(에러→경고 강등). → exec에서도 lazy/skip 가능한, 59% 짜리 즉시 레버. · 스케일링: **선형이다. O(n²) 아니다.** (프로그램 크기 기준)\n\natlas 상수항을 제거한 깨끗한 측정 (--emit=obj, 성공 컴파일만, 3포인트):\n  288 LOC → 0.57s\n  485 LOC → 0.81s\n  7638 LOC → 16.73s\n  288→7638: LOC ×26.5, time ×29.4 → **지수 n^1.03**\n  485→7638: LOC ×15.7, time ×20.7 → **지수 n^1.09**\n\nexec 모드에서도 phase별로 선형: parse 50→74→1397ms, type_check 193→280→4244ms (LOC와 거의 비례). atlas_cli는 19534 LOC인데 parse가 3191ms로 여전히 선형.\n\n즉 **\"줄수 2.4배에 시간 수십배\" 라는 전제 자체가 성립하지 않는다.** 그 전제는 raw LOC(740 vs 305)를 비교했는데, flatten 후 실제 크기는 정반대다 — aot_cc_select가 7638 LOC로 hx.hexa보다 8배 이상 크다. raw LOC는 무의미한 축이었다.\n\n컴파일 시간 모델 ≈ **10.99s (atlas 상수) + 5× 세금 × O(n^1.05) (프로그램)** + own-link.",
    "반증 통과한 병목: 6건"
  ],
  "result": {
    "bottleneck_count": 6,
    "profile": {
      "method": "호스트: aiden (12-core, 30GB, linux-x86_64). 트리: /tmp/olc (GitHub 새 clone @77b1ba9d, 이미 빌드됨). 직접 `ssh -o BatchMode=yes aiden` + nohup (pool on 미사용, pkill -f 미사용).\n\n⚠️ 측정 도구 자체를 먼저 고쳐야 했다 (하드교훈 #7):\n1) `hexa build`는 컴파일러 stdout/stderr를 삼키고, CG_PROFILE 설정 시에만 실패 경로에서 forward한다(#4483). 즉 **성공 컴파일은 phase 데이터를 절대 못 얻는다.** → `build/aprime_cc _drv.hexa --backend=native --emit=exec --linker=hexa --target=x86_64-linux-gnu -o OUT SRC` 를 직접 호출 (+ `HEXA_LANG=/tmp/olc` 없으면 runtime.a 못 찾아 rc=3).\n2) env: `HEXA_CG_PROFILE=1`, `env -u HEXA_PREBUILT_RUNTIME`, `/usr/bin/time -v`, `perf record -F 99 -g` (sudo sysctl로 ptrace_scope=0 / perf_event_paranoid=1 완화 필요했음).\n3) flatten LOC = `build/hexa_module_loader <src> <out>` 산출물 wc -l.\n\n⚠️ 측정 시작 시 이전 세션의 census 프로세스 2개가 아직 살아있었다 — ccsd_rhf 컴파일이 **RSS 20.6GB**로 박스를 굶기고 있었고(hx.hexa 컴파일은 CPU 10.8%밖에 못 받고 있었음), 정확한 PID로 kill 후 23GB→4GB 회수하고 나서 측정했다. **기존 census의 300초 타임아웃 수치는 이 경합 하에서 측정된 것이다.**",
      "phases": [
        {
          "name": "codegen (MIR→LIR)",
          "ms": "42687",
          "pct": "34.4%"
        },
        {
          "name": "own-link / post-emit (⚠️ 계측훅 없음 — wall − Σphases 로 역산. 2회 재현: 34.3s / 34.0s)",
          "ms": "~34270",
          "pct": "27.6%"
        },
        {
          "name": "lower_hir (HIR→MIR)",
          "ms": "11964",
          "pct": "9.6%"
        },
        {
          "name": "atlas_load",
          "ms": "10992",
          "pct": "8.9%"
        },
        {
          "name": "lower_ast_to_hir",
          "ms": "8940",
          "pct": "7.2%"
        },
        {
          "name": "x86_pack_lir",
          "ms": "7644",
          "pct": "6.2%"
        },
        {
          "name": "type_check",
          "ms": "4244",
          "pct": "3.4%"
        },
        {
          "name": "parse",
          "ms": "1397",
          "pct": "1.1%"
        },
        {
          "name": "lex",
          "ms": "966",
          "pct": "0.8%"
        },
        {
          "name": "bind",
          "ms": "767",
          "pct": "0.6%"
        },
        {
          "name": "resolve / unit_check / x86_serialize / mir_opt",
          "ms": "56 / 40 / 39 / 3",
          "pct": "0.1%"
        }
      ],
      "bottleneck": "기준 프로그램 = tool/aot_cc_select.hexa (flatten 7638 LOC, --emit=exec + own-link, wall 124.0s, rc=0 바이너리 실제 생성). 표면상 1위는 codegen 42.7s(34%). **하지만 근인은 codegen이 아니라 atlas 상주(residency)다.**\n\n★ 결정적 A/B (동일 파일, 동일 codegen 코드):\n  --emit=obj (atlas 스킵)  : codegen  7743ms · wall 16.80s · RSS 1.83GB\n  --emit=exec (atlas 상주) : codegen 42687ms · wall 124.01s · RSS 4.22GB\n\nphase별 배율 (obj → exec):\n  lex 930→966 (1.04×) · parse 1353→1397 (1.03×)   ← atlas_load **이전** 단계 = 무영향\n  ─ atlas_load 0 → 10992 ─\n  resolve 2.2× · bind 4.5× · type_check 5.2× · lower_ast_to_hir 5.2× · lower_hir 5.4× · codegen 5.5× · x86_pack_lir 4.4×\n  → atlas_load **이후** 모든 단계가 일률적으로 4.4~5.5× 느려진다. 깨끗한 인과 증거.\n\n메커니즘 (exec 경로 자체를 perf로 직접 확인 — 추론 아님):\n  hexa_arena_rewind 13.85% + __blk_set_used 12.54% + __blk_next 10.13% + __blk_used 2.20% = **38.7%가 arena 블록리스트 선형 스캔**\n  (+ hexa_eq 15.7% · __raw_cmp3 15.6% · rt_eq_int 7.7% · rt_cmp_le 6.3% — 그 스캔 루프 안에서 쓰이는 비교 프리미티브)\n  inclusive: __hexa_fn_arena_return 23.3% · hexa_arena_rewind 17.8% · hexa_val_arena_scope_push 8.6%\n  → 함수 호출마다 arena scope teardown이 블록리스트를 선형 순회한다. 비용이 **live 블록 수**에 비례하는데, 17354노드 atlas가 그 블록 수를 영구히 부풀린다. 즉 O(n²)의 n은 **프로그램 크기가 아니라 live arena 블록 수**다. 과거 3건과 같은 알고리즘 복잡도 클래스이되, **사이트는 새로운 곳**(arena rewind/블록 스캔).\n\natlas 총 비용 = 11.0s(로드) + ~61.9s(하류 phase 세금) ≈ **72.9s / 124s = wall의 59%**.\n그런데 compiler/main.hexa:727 주석이 스스로 밝히듯 atlas는 **citation_check만 소비**하고 codegen은 무시하며, 이미 --emit=obj에선 OOM 때문에 스킵된다. 게다가 --strict-citations는 default OFF(에러→경고 강등). → exec에서도 lazy/skip 가능한, 59% 짜리 즉시 레버.",
      "scaling": "**선형이다. O(n²) 아니다.** (프로그램 크기 기준)\n\natlas 상수항을 제거한 깨끗한 측정 (--emit=obj, 성공 컴파일만, 3포인트):\n  288 LOC → 0.57s\n  485 LOC → 0.81s\n  7638 LOC → 16.73s\n  288→7638: LOC ×26.5, time ×29.4 → **지수 n^1.03**\n  485→7638: LOC ×15.7, time ×20.7 → **지수 n^1.09**\n\nexec 모드에서도 phase별로 선형: parse 50→74→1397ms, type_check 193→280→4244ms (LOC와 거의 비례). atlas_cli는 19534 LOC인데 parse가 3191ms로 여전히 선형.\n\n즉 **\"줄수 2.4배에 시간 수십배\" 라는 전제 자체가 성립하지 않는다.** 그 전제는 raw LOC(740 vs 305)를 비교했는데, flatten 후 실제 크기는 정반대다 — aot_cc_select가 7638 LOC로 hx.hexa보다 8배 이상 크다. raw LOC는 무의미한 축이었다.\n\n컴파일 시간 모델 ≈ **10.99s (atlas 상수) + 5× 세금 × O(n^1.05) (프로그램)** + own-link.",
      "flatten_loc": "`build/hexa_module_loader` 산출물 wc -l (raw → flatten):\n\n  tool/compile.hexa                      50 →   226\n  tool/doctor.hexa                      163 →   485\n  tool/absolute_rules_embed_gen.hexa    237 →   288\n  tool/ai_native_profile.hexa           256 →   432\n  tool/aot_cc_select.hexa               305 →  7638   ← \"빠른\" 프로그램이 실은 제일 큼\n  tool/hx.hexa                          740 →  ✗ FLATTEN 실패 (아래)\n  stdlib/qforge/atoms/ccsd_rhf.hexa     936 →  2080\n  tool/atlas_cli.hexa                  4090 → 19534\n\n→ raw LOC와 flatten LOC는 상관이 없다(305줄→7638, 936줄→2080). 기존 census 표의 \"줄수\" 축은 아무것도 설명하지 못한다.",
      "notes": "■ 전제 3개가 실측으로 반증됐다.\n\n【1】 hx.hexa의 \"300초 타임아웃\"은 컴파일 시간 벽이 아니다 — **죽은 소스다.**\n  tool/hx.hexa:63 `use \"tool/raw_audit\"` 인데 **tool/raw_audit.hexa 는 존재하지 않는다**(mini·aiden 양쪽 확인). 커밋 87dda7427 \"retired-format tooling cluster removal + **consumer cascade**\" 가 raw_audit.hexa를 지우면서 hx.hexa를 놓쳤다.\n  → module_loader: `FATAL module not found: tool/raw_audit` (rc=2) → flatten 산출물 0 → self/main.hexa:3270 의 \"falling back to raw src\" 경로로 떨어져 **import 미해결 상태의 raw 740줄**을 aprime_cc에 먹인다. 그게 300초+ 갈린 정체다.\n  → 죽은 `use` 한 줄을 지우면 flatten 성공(914 LOC)하고 **16.9초 만에** 끝난다 — 단, 여전히 컴파일 실패: `undefined name audit_append` ×7, `ssot_fingerprint` ×3 (둘 다 삭제된 raw_audit.hexa가 제공하던 심볼). **hx.hexa는 own-link와 무관하게 애초에 컴파일 불가능한 파일이다.** `hx`는 패키지 매니저 엔트리인데 깨져 있다 — 별건으로 보고할 만하다.\n  게다가 그 300초 측정은 20.6GB ccsd 프로세스가 CPU를 10.8%로 굶기던 와중에 나온 수치다(경합 오염).\n\n【2】 census의 ✅ 는 **거짓 초록**이다 (하드교훈 #1·#7 그대로).\n  `hexa build` 는 **바이너리를 하나도 안 만들고도 rc=0으로 종료한다.** 실측:\n    tool/compile.hexa            → HX2001 undefined name (tokenize/parse/interpret) · **바이너리 없음** · rc=0\n    tool/ai_native_profile.hexa  → HX2001 · **바이너리 없음** · rc=0\n    tool/doctor.hexa             → 바이너리 OK\n    tool/absolute_rules_embed_gen.hexa → 바이너리 OK\n    tool/aot_cc_select.hexa      → 바이너리 OK\n  즉 \"5/5 own-link ✅ · STRICT FATAL 0\" 중 **2개는 실제로 컴파일 실패**했고, HEXA_OWNLINK_STRICT=1 조차 이를 못 잡았다(에러를 삼켜서). own-link 폴백률 0 이라는 결론은 살아있지만, **근거였던 표는 못 믿는다** — 5개 중 3개만 유효 표본.\n\n【3】 ccsd_rhf 는 별개의 진짜 병리다 (스케일링 아님).\n  **parse phase 하나에서** >185초 · RSS 16.4GB(88MB/s로 증가, 완주 못함 → 박스 OOM 방지 위해 kill). lex는 240ms로 정상 종료.\n  LOC 탓이 아니다: atlas_cli 는 **19534 LOC인데 parse 3.2초**. ccsd(2080 LOC)만 터진다 → 내용 특이적. 소스에 긴 줄/깊은 인덱스 체인은 없다(최장 237자, 4중 이상 `[][][]` 0건). 같은 arena-rewind 프로파일이 나온다 → 【bottleneck】의 arena 블록 스캔이 parse 재귀에서 폭주하는 형태로 보인다. `--emit=obj` 로 90초/8GB에 싸게 재현된다(atlas 없이도 터짐 → atlas와 독립된 두번째 결함).\n\n■ 측정불가로 남긴 것 (정직하게):\n  · **own-link 단계 자체(~34.3s, wall의 27.6%)에 cg_profile 마크가 없다.** emit_asm 이후 구간이라 wall − Σphases 로 역산했다(2회 재현: 34.3s/34.0s). hexa_ld 링크 + runtime.a 추출 + exec 기록이 섞여 있어 더는 못 쪼갠다 — 계측훅 추가가 선결. 이 34초도 atlas 상주 세금을 같이 맞고 있을 가능성이 높으나 **미측정**(주장 안 함).\n  · atlas_load 자체가 atlas 크기에 대해 O(n²)인지는 **미측정** — atlas를 잘라 컴파일러를 재빌드해야 해서 이번엔 안 했다. 다만 17354노드/10.99s = 노드당 610µs 는 struct 리터럴 구성치고 비정상적으로 크다(embedded.gen.hexa 는 `pub let ATLAS_P_NODES: [AtlasNode] = [...]` 17k개 struct 리터럴 정적 배열이고, static_atlas()가 매 컴파일마다 **런타임에 구성**한다. 노드마다 중첩 struct 2개 + 빈 배열 7개 = 약 12만 할당).\n  · arm64/darwin 미측정 (하드교훈 #6) — 전부 linux-x86_64 수치다.\n\n■ 다음 라운드 (측정 기반, 우선순위순):\n  1. exec 경로에서 atlas를 lazy/skip — citation_check 소비 시점까지 미루거나, --strict-citations OFF(현 default)면 아예 안 만든다. **wall의 59% (72.9s/124s)** 회수 예상. obj 경로엔 이미 같은 스킵이 들어가 있다(OOM 때문).\n  2. `hexa build` 의 rc=0-on-failure 를 고친다 — 이게 없으면 어떤 census도 못 믿는다. 게이트로 승격할 것.\n  3. hexa_arena_rewind 블록리스트 선형 스캔 (free-list/세대 인덱스) — 1이 막혀도 5× 세금의 근인.\n  4. own-link 단계에 cg_profile 마크 추가 (27.6%가 계측 사각지대).\n  5. tool/hx.hexa 의 죽은 raw_audit 의존 정리 (별건 버그)."
    },
    "confirmed": [
      {
        "site": "self/module_loader.hexa:384 (call site :1536)",
        "fn": "ml_word_mangle_global",
        "complexity": "O(F × C_f × 440 × |body_f|) — 20 lhs × 22 rhs = **440 full-source `.replace()` passes per (file, collided-global)**. MEASURED: 83.5% of `hexa_module_loader compiler/main.hexa` (dwarf callgraph, aiden). Flatten of compiler/main.hexa = **10.5s wall / 4.08 GB RSS** for a 3.6 MB output; atlas_cli (1.0 MB out) = 0.10s / 40 MB. 3.5× bytes → 105× time.",
        "evidence": "REAL CODE (module_loader.hexa:389-416):\n```\nlet mut lhs_set = []   // 20 entries: \" \" \"\\n\" \"\\t\" ( , = [ { ; ! + - * / < > & | : ?\nlet mut rhs_set = []   // 22 entries\nlet mut s = src\nwhile li < len(lhs_set) {\n    let lhs = lhs_set[li]\n    let mut ri = 0\n    while ri < len(rhs_set) {\n        let rhs = rhs_set[ri]\n        let needle = lhs + name + rhs\n        let r      = lhs + repl_name + rhs\n        s = s.replace(needle, r)      // ← 440× full-body strstr scan + malloc(2|s|)\n        ri = ri + 1\n    }\n    li = li + 1\n}\n```\nMEASURED perf (`perf record --call-graph dwarf`, /tmp/olc @77b1ba9d):\n```\n87.16% hxlcl_strstr\n  --86.99%--_start … load_module\n            |--83.49%--ml_word_mangle_global → hexa_str_replace → hxlcl_strstr\n             --2.96%--ml_strip_and_clean   → hexa_str_replace → hxlcl_strstr\n```\nCollided-global list actually printed on **stdout** (not stderr — a measurement trap): `[module_loader] collided top-level globals mangled: ATLAS_HASH|…|_STMT_ASSIGN|_STMT_BINOP|_STMT_UNOP|_STMT_CALL|_STMT_RETURN|_STMT_BR|_STMT_BR_COND` = **21 names**. The `_STMT_*` seven are GENUINE collisions, declared top-level in BOTH compiler/codegen/x86_64_linux.hexa (365 KB) and compiler/codegen/arm64_darwin.hexa (294 KB) → 7 × 440 × (365+294) KB ≈ 2 GB scanned + ~4 GB allocated → exactly the observed 4.08 GB RSS.",
        "fix_sketch": "Kill the cartesian product: one left-to-right scan of the body per name with an explicit word-boundary test (is_ident_char(prev) / is_ident_char(next)) instead of 440 lhs×rhs `.replace()` calls — O(|body|) per name instead of O(440·|body|). Same shape applies to ml_apply_alias_prefix. Second-order: rename the 7 `_STMT_*` globals in one of the two codegen files so the collision (and the whole mangle pass) never fires.",
        "lens": "frontend",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, with one scoping caveat. (1) The quoted loop is real executable code at self/module_loader.hexa:384-416 (not a comment), and its call site at :1536 is live inside the emit loop, gated only by g_collided_count>0 and per-file declared-collided names — flatten is on the own-link compile path for any program with `use`, so it is not dead. (2) The complexity claim holds: after two one-shot early-outs (len(name)==0, src.index_of(name)<0) it is an unconditional lhs x rhs cartesian product of full-source .replace() scans with no index/hash. Minor arithmetic correction: rhs_set has 19 entries, not 22, so it is 380 full-body strstr+malloc passes per (file, collided-global), not 440 — immaterial. (3) The collision is genuine: _STMT_ASSIGN/_STMT_BINOP/_STMT_CALL/... are declared top-level `let` in BOTH compiler/codegen/x86_64_linux.hexa:98-101 (365 KB) and compiler/codegen/arm64_darwin.hexa:94-97 (294 KB), so 7 names x 380 x ~660 KB really does run for compiler/main.hexa's closure. (4) Not already optimized: git log on module_loader shows the pass was INTRODUCED by c977442ef (#3899) and last touched by unrelated #4097; no perf convergence has ever touched it. (5) ml_apply_alias_prefix (:465, Pass 3) has the same lhs x rhs shape, so the proposed second site is real. CAVEAT (mis-attribution, not refutation): this does NOT explain the tool/hx.hexa rc=124 timeout — hx.hexa's closure is `self/stdlib/law_io` (163 lines) + `tool/raw_audit`, tiny and almost certainly not tripping g_collided_count>0, while the FAST aot_cc_select.hexa pulls the larger self/lexer+self/parser. So this fix is a confirmed win for self-compile flatten (compiler/main.hexa 10.5s/4.08GB) but is a DIFFERENT bottleneck from the hx.hexa 300s wall, which still needs its own HEXA_CG_PROFILE=1 measurement.",
          "confidence": "high"
        }
      },
      {
        "site": "self/runtime_core_emit.hexa:5446 (emitted → self/runtime_core.c:4938)",
        "fn": "hexa_arena_rewind (called from __hexa_fn_arena_return on EVERY fn return)",
        "complexity": "O(B) per function return, where B = number of arena blocks after the mark; the block chain is never shortened (blocks kept for reuse). With N calls → **O(N·B), B grows monotonically with allocation** → quadratic in program size. This is the frontend's dominant cost, not a parser/ast_to_hir algorithm defect.",
        "evidence": "REAL CODE (runtime_core.c:4938, SSOT runtime_core_emit.hexa:5446):\n```\nvoid hexa_arena_rewind(HexaArenaMark m) {\n    …\n    m.block->used = m.used;\n    for (HexaArenaBlock* b = m.block->next; b; b = b->next) b->used = 0;   // ← linear walk of the whole chain\n    __hexa_arena.cur = m.block;\n}\n```\nMEASURED (a) live perf of the *stuck parse* of stdlib/qforge/atoms/ccsd_rhf.hexa (--emit=obj, no atlas → atlas is NOT the cause here), RSS 2.3 GB and climbing:\n```\n19.78% __raw_cmp3   ← __hexa_fn_arena_return / __blk_set_used\n15.58% hexa_eq      ← __hexa_fn_arena_return\n11.74% __blk_set_used ← __hexa_fn_arena_return\n11.05% __blk_next\n10.66% hexa_arena_rewind ← __hexa_fn_arena_return\n```\nMEASURED (b) inclusive perf of the atlas_cli obj compile: `__hexa_fn_arena_return 26.3% · hexa_arena_rewind 22.3% · __blk_used 13.8% · __blk_next 7.3% · __blk_set_used 6.0%`.\nNB the loop body compiles to BOXED HexaVal ops in the native-emit path (`__blk_next`/`__blk_set_used` + hexa_eq/rt_cmp_le), so each block visited costs far more than a C pointer chase.",
        "fix_sketch": "The chain walk exists only to reset `used=0` on blocks past the mark. Make it O(1): (a) keep a generation counter per block and lazily treat `blk.gen != arena.gen` as used=0, or (b) maintain a free-list / tail pointer so rewind only touches blocks actually dirtied since the mark (track a per-mark 'high-water block' pointer at __hexa_fn_arena_enter). Either removes the per-return chain scan entirely. (Same site the orchestrator's Phase-1 found at 38.7% — I confirm it is ALSO what stalls parse on ccsd_rhf, i.e. it is not atlas-specific.)",
        "lens": "frontend",
        "verdict": {
          "refuted": false,
          "reason": "Could not refute — the mechanism survives every refutation axis, with one citation correction. (1) The quoted C at self/runtime_core.c:4938 IS real code but sits in the `#else // !HEXA_RT_ALLOC_NATIVE` fallback arm (opened at :4835), so it is not the body compiled in the default build. However the LIVE default-build body, self/rt/alloc.hexa:151-168 (`fn hexa_arena_rewind`), contains the identical chain walk `let mut b = __blk_next(block); while b != 0 { __blk_set_used(b, 0); b = __blk_next(b) }` — and this native/boxed version is exactly what produces the quoted perf symbols (__blk_next / __blk_set_used defined at self/rt/alloc.hexa:72,77; the `while b != 0` compare lowers to hexa_eq/__raw_cmp3). So the citation is misattributed but the defect is real at the equivalent live site. (2) Not dead: self/codegen.hexa:3478 emits __hexa_fn_arena_enter() at every fn top and :3661/:3710/:3715 wrap every return in __hexa_fn_arena_return(); runtime_core.c:5879 __hexa_fn_arena_return -> hexa_val_arena_scope_pop (:5126) whose HEXA_RT_ALLOC_NATIVE arm unconditionally calls hexa_arena_rewind(sm). Val-arena is default-ON. Even the primitive-tag short-circuit still calls scope_pop, so the walk runs on int/bool returns (the Pratt-parser hot path). (3) Complexity claim holds: blocks are never unlinked (self/rt/alloc.hexa:20 \"rewind/reset set used=0 and KEEP blocks for reuse (never unmap)\"; hexa_arena_alloc only appends), so B is monotone in peak arena footprint (2.3GB RSS -> thousands of blocks) and each shallow-mark return re-walks all blocks past the mark. No index/hash/tail pointer covers rewind (the _hx_ablk_tail index in runtime_core_emit.hexa:1879-1956 serves heapify address lookup, not rewind). (4) Not already fixed: git log on self/rt/alloc.hexa shows nothing touching the rewind walk since the native port (#3511/#3524/#3526/#3543); prior O(n^2) kills (#4773 _mir_lookup_global, bsearch, ast_to_hir) are other phases. CAVEAT on 'dominant': cost is O(blocks-after-mark), hence workload-dependent — the ccsd_rhf flat profile sums ~69% across arena symbols, but atlas_cli shows __hexa_fn_arena_return at only 26.3% INCLUSIVE (Amdahl ceiling ~1.36x), so an O(1) rewind is clearly warranted but will not alone explain atlas_cli's wall; expect a second site there.",
          "confidence": "high"
        }
      },
      {
        "site": "compiler/check/bind.hexa:232",
        "fn": "_bind_lookup",
        "complexity": "O(frames × defs-per-frame) **unbucketed linear scan per identifier** → O(idents × globals) over a whole flattened program. It is the one un-fixed sibling: compiler/check/types.hexa:1921 `_types_env_lookup` and compiler/lower/ast_to_hir.hexa:1332 `_lookup_in_scope` were BOTH given hash-bucket indexes (A3 / 64-bucket global index — both still present and active in code), bind was not.",
        "evidence": "REAL CODE (bind.hexa:232-247):\n```\nfn _bind_lookup(sc: BindScope, name: string) -> DefId {\n    let n = len(sc.frames)\n    let mut i = n - 1\n    while i >= 0 {\n        let fr = sc.frames[i]\n        let m = len(fr.defs)\n        let mut j = 0\n        while j < m {\n            if fr.defs[j] == name { return fr.def_ids[j] }   // ← full linear scan incl. the module/global frame\n            j = j + 1\n        }\n        i = i - 1\n    }\n```\nMEASURED phase scaling, --emit=obj (atlas skipped), same host/binary, two real flattened programs:\n```\n                7638 LOC → 19534 LOC   ratio (LOC ×2.56)\nlex             1042ms  →  2248ms      ×2.16   linear\nparse           1602ms  →  3215ms      ×2.01   linear\nresolve           27ms  →    56ms      ×2.07   linear\nbind             208ms  →  5664ms      ×27.2   ← SUPERLINEAR\ntype_check       977ms  → 30771ms      ×31.5   ← SUPERLINEAR\n```\n⚠️ HONEST CAVEAT: bind/type_check run LATE, so they also eat the arena-rewind tax (finding #2), which grows with elapsed allocation. I did **not** isolate the two terms. What is certain: the ×27/×31 blow-up is real and is NOT in lex/parse/resolve; and `hxlcl_strcmp` is 4.9% inclusive in that run (real name compares), while the arena family is ~26-30%.",
        "fix_sketch": "Give bind the same bucket index types/ast_to_hir already have: a 64-bucket name→(frame,slot) hash over the module frame, primed once after the pre-pass, with a linear scan only over the local suffix. Then re-measure bind at 19534 LOC to see how much of the ×27 was lookup vs arena tax (that A/B also cleanly splits finding #2 from this one).",
        "lens": "frontend",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. (1) The quoted code at /Users/mini/dancinlab/hexa-lang/compiler/check/bind.hexa:232-247 is real executable code (verified by reading, not grep) — a full linear `while j < m` scan of `fr.defs[j] == name` per frame, with no hash/bucket index. `_bind_is_mut` (line 267) duplicates the same unbucketed walk. (2) The path is live and unconditional on the own-link compile: compiler/main.hexa:759 `let r2 = bind(module)` where `module` comes from `parse(...)` at main.hexa:706, which is the MULTI-FILE FLAT-SPLICE (main.hexa:709) — so module.items is the entire flattened import closure. bind()'s pre-pass (bind.hexa:1739-1766) pushes ONE module frame holding ~200 builtins + every top-level item of the flattened program, and `_bind_walk_expr` calls `_bind_lookup` per identifier (call sites 914, 943, 968, 1176). That is O(idents x globals) — quadratic in flattened program size. It runs BEFORE codegen/own-link, which matches the observed symptom (hx.hexa rc=124 with 0 STRICT-FATAL hits: it never reaches the linker). (3) The 'siblings already fixed, bind was not' claim checks out: types.hexa has a live 64-bucket index (_types_num_buckets/_types_empty_buckets/_types_rebuild_buckets/_types_bucket_for, lines 52-141) and ast_to_hir.hexa:1277-1332 has an FNV-1a 64-bucket module-frame index primed once after its pre-pass (primed at ~2856). `git log -- compiler/check/bind.hexa` shows only feature commits (HX3050/HX3051/HX2001 exemptions) — no perf/index work ever landed on bind. (4) Profile is consistent: the x27 bind blow-up is in exactly the pass that does per-identifier name lookup, while lex/parse/resolve stay linear. ONE CAVEAT (attribution, not refutation): `_define_mut` (bind.hexa:211-224) copies `top.defs` on every define, so if stage-0 arrays are value-copied, priming a frame-0 of N globals is itself O(N^2) and would inflate both bind and the arena family (~26-30% self time). So the x27 may split between lookup and frame-0 construction/arena tax. The proposed 64-bucket A/B is exactly the experiment that separates those terms, and the complexity defect stands regardless.",
          "confidence": "high"
        }
      },
      {
        "site": "self/module_loader.hexa:102 (ml_canon_path) + :850 (ml_resolve_full) / :768 (ml_resolve_project_root)",
        "fn": "ml_canon_path / ml_resolve_full — dedup key is not absolutized",
        "complexity": "Correctness + constant-factor: the same physical file resolved once caller-relative and once via HEXA_LANG project-root gets TWO distinct visited-set keys → emitted TWICE → its top-level globals then appear in \"2+ distinct files\" → **manufactures fake collisions that arm the 440× mangle pass of finding #1**.",
        "evidence": "MEASURED (`hexa_module_loader compiler/main.hexa`, HEXA_LANG=/tmp/olc):\n```\n$ grep -o 'begin: .*' /tmp/A.hexa | sed 's#/tmp/olc/##' | sort | uniq -d\nbegin: compiler/atlas/merger.hexa\nbegin: compiler/atlas/parser.hexa\nbegin: compiler/atlas/static_index.hexa\n```\ni.e. each of these is inlined twice — once as `compiler/atlas/static_index.hexa`, once as `/tmp/olc/compiler/atlas/static_index.hexa`. A/B: with HEXA_LANG → 74997 LOC / 10.55s; `env -u HEXA_LANG` → 73584 LOC / 9.34s, **zero duplicate `begin:` entries**. Standalone repro: `hexa_module_loader compiler/atlas/static_index.hexa` = 0.82s / 337 MB for a 118 KB output, and prints `collided top-level globals mangled: ATLAS_HASH|…|_g_atlas_cache_misses` (9 names) — every one of them fake, produced purely by the double-include of static_index.hexa.\nREAL CODE: ml_canon_path (:102) only collapses `.`/`..` segments — it never absolutizes; ml_resolve_full (:859) canonicalizes the *raw* result, so a cwd-relative hit and an $HEXA_LANG-absolute hit stay distinct keys.",
        "fix_sketch": "Absolutize the dedup key (prefix cwd when not `/`-rooted, then ml_canon_path). Keeps the emitted content identical for single-resolution files, kills the double-inline + the fake collisions. Note the mangle is *useless* for the dup case anyway: both copies share a basename → same `Static_index__` prefix → same mangled name.",
        "lens": "frontend",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED by independent repro. (1) Cited lines are executable code at the exact numbers: self/module_loader.hexa:102 `fn ml_canon_path(p)` (lexical ./.. collapse only — computes is_abs but never prefixes cwd), :768 `fn ml_resolve_project_root` ($HEXA_LANG join → absolute), :850/:860 `ml_resolve_full` → `ml_canon_path(_rf)` canonicalizes the RAW result, so a caller-relative hit (ml_resolve_full_raw step 1) and a $HEXA_LANG-absolute hit (step 5) remain two distinct visited-set keys. (2) Path is live on the own-link compile route: install.sh:667-684 builds build/hexa_module_loader because \"the `hexa build` flatten step (resolve_module_loader_compiled in self/main.hexa) needs a COMPILED module_loader\" — every multi-`use` program flattens through it — and tool/build_hexac.hexa:105 invokes the loader with HEXA_LANG explicitly set, so the trigger condition is the default. (3) My own A/B on darwin with the installed prebuilt loader: `HEXA_LANG=$PWD hexa_module_loader compiler/main.hexa` → 56 files / 74997 LOC / 17.2s with duplicate `begin:` for compiler/atlas/{merger,parser,static_index}.hexa; `env -u HEXA_LANG` → 53 files / 73584 LOC / 14.9s with ZERO duplicates. (4) Fake collisions confirmed: with HEXA_LANG the loader prints `collided top-level globals mangled: ATLAS_HASH|ATLAS_SOURCE_COUNT|ATLAS_HASH_MIN|ATLAS_HASH_MAX|_g_atlas_index|_g_atlas_loaded|_g_atlas_path|_g_atlas_cache_hits|_g_atlas_cache_misses|_MAX_FILE_BYTES|_ENRICH_CACHE_TTL_SECONDS|_g_enrich_cache|…|_STMT_*`, while without HEXA_LANG only the 7 genuine _STMT_* names collide — 14 names manufactured purely by the double-include. (5) Not already fixed: latest commits on this file are 172c0f48e (#4097 working-tree precedence) and c977442ef (#3899 collision-gated mangle), which are exactly what produced this interaction. Bounds: the dup is only 3 files / ~1.9% of flatten LOC, so its direct constant factor is modest (17.2s vs 14.9s); it is a correctness defect that ARMS the mangle pass — I did not independently verify the \"440×\" cost figure attributed to finding #1, and this alone does not account for the 300s hx.hexa timeout.",
          "confidence": "high"
        }
      },
      {
        "site": "self/runtime_core_emit.hexa:10641-10642 (emitted → self/runtime_core.c:9904)",
        "fn": "hexa_str_replace",
        "complexity": "O(|s|) time + **malloc(2|s|+1) + full memcpy even when the needle does not occur**. Multiplies finding #1 by ~2× in memory: 440 needles per (file,name), almost all of which never match, each still allocating 2×body into the never-freed arena.",
        "evidence": "REAL CODE (runtime_core.c:9904-9911):\n```\nsize_t cap = HX_STRLEN(s) * 2 + 1;\nchar* result = malloc(cap);          // ← unconditional, before any strstr\n…\nwhile (1) {\n    char* found = hxlcl_strstr(pos, HX_STR(old));\n    if (!found) { … hxlcl_memcpy(result + total, pos, rlen); … break; }   // no-match still copies the whole string\n```\nplus `hxlcl_strstr` is documented + implemented as naive O(|h|·|n|) (stdlib/runtime/hxlcl_core.hexa:330, byte-faithful to rt_strstr). Perf on the flatten: `hxlcl_strstr 87% · hxlcl_memcpy 6-8%` (hexa_str_own + hexa_str_replace memcpy).",
        "fix_sketch": "Fast-path: `if (!hxlcl_strstr(HX_STR(s), HX_STR(old))) return s;` before the malloc — byte-neutral (same returned string content), removes the alloc+copy for every non-matching replace. Independently: hxlcl_strstr is naive O(n·m); a two-way/Boyer-Moore-Horspool is a separate (larger) lever.",
        "lens": "frontend",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, with two scope corrections. (1) The cited body is real executable code and matches its SSOT emitter (self/runtime_core_emit.hexa:10633-10669 emits verbatim what appears at self/runtime_core.c:9896-9931), so the generated .c is not stale. (2) The `#ifndef HEXA_HAS_HEXA_RT_STDLIB` guard does not make it dead: the shipped runtime.a compiles runtime.c/runtime_core.c WITHOUT that -D (tool/stage_resolve_runtime_a:4264 S2 and :4270 S3 state \"NO HEXA_HAS_HEXA_RT_STDLIB\"; tool/release_runtime_compile_gate:64 \"We deliberately do NOT pass -DHEXA_HAS_HEXA_RT_STDLIB so the #ifndef C bodies are LIVE\"), so the malloc(2|s|+1)+strstr C body is the linked one — corroborated by the profile showing hxlcl_strstr at all (the alternate hexa rt_str_replace in stdlib/runtime/ctype.hexa:583 uses no strstr). (3) It is on the flatten path: self/module_loader.hexa:465 ml_apply_alias_prefix builds lhs_set(9) x rhs_set(18) and calls `s = s.replace(needle, repl)` at :527 inside the per-pub-name loop => 162 whole-source replaces per name, almost all non-matching, each unconditionally malloc'ing 2|s|+1 and memcpy'ing the entire source before/independent of any match; plus loops at :412/:568/:1586 and 10 sequential replaces in ml_strip_and_clean (:1178-1189). `.replace` lowers to hexa_str_replace (compiler/codegen/arm64_darwin.hexa:1947; typed in compiler/check/types.hexa:4380/4497). (4) Not already optimized — no fast-path anywhere in git log for the emitter. (5) The proposed fast-path is byte-neutral: the function already returns the same HexaVal s on two existing guards (!HX_IS_STR(s), HX_STRLEN(old)==0). CORRECTIONS: needle count is 162 per (file,pub-name), not 440; and the fix's TIME win is bounded by the memcpy/malloc share (the claim's own profile: strstr 87% vs memcpy 6-8%) since the fast-path still runs one strstr scan — it is primarily a MEMORY lever (2x|s| per call into a never-freed arena). The dominant time bottleneck remains naive O(n*m) hxlcl_strstr (stdlib/runtime/hxlcl_core.hexa:330) and the 162x whole-source rescan structure itself.",
          "confidence": "high"
        }
      },
      {
        "site": "compiler/emit/elf_x86_64.hexa:4389",
        "fn": "archive_extract_fixpoint (floor-pull arm)",
        "complexity": "O(passes × members × |und_names| × |syms_per_member| × L²) — and CONSTANT in program size (it is pure runtime.a work). Measured n on aiden: members=25, syms=3051 (~122/member), unique UND=265 (und_names grows to ~265+), fixpoint re-runs the whole member sweep every `while changed` pass. The innermost position calls `_aef_symname`, which REBUILDS the symbol name char-by-char with immutable-string concat (O(L²) + ~L arena allocs) — zero memo. Work bound ≈ 25 × ~3 passes × 265 × 122 ≈ 2.4M string materializations per link.",
        "evidence": "Real code (not comment), lines 4389-4400:\n```\nwhile ui < len(und_names) && !wants {\n    let bx = und_names[ui]\n    if !_aef_has(def_names, bx) {\n        let owx = \"hxlcl_\" + bx\n        let mut kk = 0\n        while kk < len(obj.symbols) {\n            let s2 = obj.symbols[kk]\n            if s2.section != 0 && _aef_symname(obj, s2.name_offset) == owx {\n```\n`_aef_symname` (:4243) is the char-by-char rebuild: `s = s + from_char_code(b)`. `_aef_has` (:4257) is a linear scan.\n\nBEHAVIORAL confirmation (aiden, /tmp/olc, HEXA_CG_PROFILE=1, own-link = wall − Σ(marked phases), same method the parent used):\n  exit42.hexa   (1 LOC, Σphases 10,692ms, wall 43.72s) → own-link = 33.0s\n  fl_doctor     (485 LOC, Σphases 14,229ms, wall 38.13s) → own-link = 23.9s\n  fl_aot_cc_select (7638 LOC, Σphases 87,455ms, wall 120.75s) → own-link = 33.3s\n★ A ONE-LINE program pays 33 SECONDS of own-link — the same as a 7638-LOC one. own-link cost is program-INDEPENDENT, i.e. it is entirely runtime.a-side. perf self-time on the exec compile matches the signature exactly: __raw_cmp3 15.3% + hexa_eq 15.1% (string equality) and __blk_set_used 13.5% + hexa_arena_rewind 13.4% + __blk_next 10.9% + __blk_used 2.1% (≈40% arena block-list scan, fed by the per-iteration string allocs).",
        "fix_sketch": "Hoist a per-member symbol-name array ONCE before the fixpoint (exact precedent already in this same file: the `osn` cache at elf_x86_64.hexa:2333, whose comment says it exists because names were rebuilt per-reloc with no memo). Then replace `_aef_has` on def_names/und_names with the 1024-bucket set already implemented here as `_symh_*`/`_ldx_hash`. Also invert the loop: build the member's `hxlcl_*` def set once and probe und_names against it, instead of scanning all symbols per und name.",
        "lens": "codegen",
        "verdict": {
          "refuted": false,
          "reason": "Claim survives. (1) Lines 4387-4405 of /Users/mini/dancinlab/hexa-lang/compiler/emit/elf_x86_64.hexa are executable code, not comments; _aef_symname (:4243) is a char-by-char immutable-concat rebuild with zero memo and _aef_has (:4257) is a flat linear scan. (2) The path is live and own-link-only: compiler/main.hexa:1352 -> link_elf_x86_64_ownstart_ar (:3207) -> archive_extract_fixpoint (:3211); it touches only runtime.a (buf/members), which exactly explains the measured program-INDEPENDENT ~33s (1-LOC == 7638-LOC). (3) Not already optimized: git log shows #4851 (adb39cbba, \"1024-bucket def-index + sym-name cache\", >90min->106s) bucketed link_elf_x86_64_ownstart only (osn cache :2331-2345, _ldx_hash/_symh_*); archive_extract_fixpoint was untouched and is now the last unbucketed/unmemoized name-set in the own-link path, matching the perf signature (hexa_eq+__raw_cmp3 ~30% string-eq from _aef_has; ~40% arena block scan from the per-iteration from_char_code concat allocs). (4) Scope correction, not refutation: line 4389 is not the sole offender — the mirror loop at :4427-4443 has the identical und_names x symbols x _aef_symname shape for PULLED members, the seed/fold loops (:4322/:4337/:4361/:4416/:4450) all use _aef_symname + linear _aef_has, and _aef_all_member_defs (:4277) re-parses every member a second time. The proposed fix (hoist a per-member name matrix once, precedent = osn at :2331; bucket def_names/und_names with the existing _symh_*/_ldx_hash; build the member hxlcl_* def set once and probe und_names against it) is right but must be applied to the whole function, and _aef_all_member_defs should reuse the already-parsed members. Residual uncertainty (why medium, not high): I could not measure on mini (darwin, no linux runtime.a), so the attribution of the majority of the 33s to this fn is inferential from program-independence + perf signature + #4851 coverage; a per-phase timer around archive_extract_fixpoint vs link_elf_x86_64_ownstart on aiden would close it cheaply.",
          "confidence": "medium"
        }
      }
    ],
    "synth": "# own-link 컴파일-시간 벽 — 종합 판정\n\n## 0. 한 줄\n\n**own-link 이 느린 게 아니다.** 벽은 (a) `--emit=exec` 만 아틀라스를 상주시키는 **한 줄짜리 게이트 누락**(compiler/main.hexa:726) 과 (b) 그 상주가 노출시킨 **프론트엔드 공통의 `hexa_arena_rewind` O(B)** 두 개다. own-link 링크 단계 자체(~33s)는 **프로그램 크기와 무관한 상수**이고, 별개의 3순위 항목이다.\n\n---\n\n## 1. 벽의 정체 — 실측\n\n### 1-a. diff-path 렌즈 결론: **own-link 전용 벽이 아니다. 프론트엔드 공통이다.**\n\n결정적 A/B (aiden, `/tmp/olc` GitHub 새 clone @77b1ba9d, 동일 파일 `tool/aot_cc_select.hexa` flatten 7638 LOC, `env -u HEXA_PREBUILT_RUNTIME`):\n\n| | codegen | wall | maxRSS |\n|---|---|---|---|\n| `--emit=obj` (atlas 스킵) | 7,743 ms | **16.80 s** | 1.83 GB |\n| `--emit=exec` (atlas 상주) | 42,687 ms | **124.01 s** | 4.22 GB |\n\nphase 배율(obj→exec): lex 1.04× · parse 1.03× **← atlas_load 이전 = 무영향** ─ `atlas_load 0 → 10,992ms` ─ resolve 2.2× · bind 4.5× · type_check 5.2× · lower_ast_to_hir 5.2× · lower_hir 5.4× · codegen 5.5× · x86_pack_lir 4.4×.\n→ **atlas_load 이후 전 단계가 일률적으로 4.4~5.5× 로 느려진다.** 깨끗한 인과 증거.\n\nx86_64 분기(`compiler/main.hexa:1135-1168`)에서 obj 와 exec 은 동일한 `pack_lir_x86_64 → demote_nonkeep_to_local → serialize_elf_x86_64` 를 탄다. **두 경로의 유일한 상류 차이가 `compiler/main.hexa:726` 이다:**\n\n```hexa\nlet atlas = if emit_kind == \"obj\" { empty_atlas_index() } else { _load_atlas() }\n```\n(코드 실물 확인 — 주석 아님. 바로 위 주석이 스스로 \"codegen ignores it; only citation_check consumes refs\" + \"OOM 때문에 obj 는 스킵\" 이라고 자인한다.)\n\n**시간축**: atlas-skip 은 `5630ecfc9 (2026-06-10)`, own-link exec default-flip 은 `66e85e219 (2026-07-12)`. **최적화가 한 달 먼저 있었고, 그것을 대체한 새 default 경로가 게이트 밖으로 나갔다.** own-link default-flip 이 모든 사용자 빌드의 컴파일 wall 을 조용히 ~7× 되돌렸다.\n\n### 1-b. 메커니즘 (perf 로 exec 경로 직접 확인 — 추론 아님)\n\n```\nhexa_arena_rewind 13.85% + __blk_set_used 12.54% + __blk_next 10.13% + __blk_used 2.20%\n  = 38.7% 가 arena 블록리스트 선형 스캔\n(inclusive: __hexa_fn_arena_return 23.3% · hexa_arena_rewind 17.8%)\n```\n실물 코드 `self/rt/alloc.hexa:151-168` (+ C 미러 `self/runtime_core_emit.hexa:5445`):\n```hexa\n__blk_set_used(block, used)\nlet mut b = __blk_next(block)\nwhile b != 0 { __blk_set_used(b, 0)  b = __blk_next(b) }   // ← 매 fn return 마다 전 블록 순회\n```\n⚠️ 방출되는 C 바디 위의 주석은 **\"O(1)\" 이라고 거짓말한다** (`runtime_core_emit.hexa:5436`) — 실제는 O(B).\n→ **함수 리턴 1회당 O(B), B = live 블록 수.** 17,354 노드 atlas 가 B 를 영구히 부풀려 하류 전 phase 에 4.4~5.5× 세금을 매긴다. **O(n²) 의 n 은 프로그램 크기가 아니라 live arena 블록 수다.**\n\n### 1-c. 스케일링: **프로그램 크기에 대해 선형이다. O(n²) 아니다.**\n\natlas 상수를 뺀 깨끗한 측정(`--emit=obj`, 성공 컴파일만):\n288 LOC → 0.57s · 485 LOC → 0.81s · 7638 LOC → 16.73s → **지수 n^1.03~1.09**.\n모델 ≈ `10.99s(atlas 상수) + 5×세금 × O(n^1.05) + own-link(~33s 상수)`.\n\n단, atlas 세금이 사라지면 **bind/type_check 가 초선형으로 드러난다** (obj, 동일 바이너리, 7638→19534 LOC):\n```\nlex ×2.16 · parse ×2.01 · resolve ×2.07   (선형)\nbind 208ms → 5,664ms  ×27.2      ← 초선형\ntype_check 977ms → 30,771ms ×31.5 ← 초선형\n```\n사이트: `compiler/check/bind.hexa:232 _bind_lookup` — **버킷 없는 선형 스캔**. 형제 두 곳(`compiler/check/types.hexa:1921 _types_env_lookup`, `compiler/lower/ast_to_hir.hexa:1332 _lookup_in_scope`)은 이미 해시-버킷 인덱스를 받았는데 bind 만 못 받았다.\n⚠️ **정직한 단서**: bind/type_check 는 늦게 돌아 arena-rewind 세금도 같이 먹는다 — 두 항을 **분리 못 했다**(미측정).\n\n### 1-d. own-link 단계 — 프로그램-무관 상수\n\n`wall − Σ(cg_profile phases)` 역산(계측 훅 없음, 2회 재현):\n```\nexit42.hexa       (1 LOC)    → own-link 33.0s\nfl_doctor         (485 LOC)  → own-link 23.9s\nfl_aot_cc_select  (7638 LOC) → own-link 33.3s\n```\n★ **1줄짜리 프로그램이 33초의 own-link 를 낸다.** 즉 own-link 비용은 전량 runtime.a 측이다.\n용의자: `compiler/emit/elf_x86_64.hexa:4297 archive_extract_fixpoint` + `_aef_symname:4243` (실물 코드: `s = s + from_char_code(b)` — 심볼명을 **문자단위 immutable concat 으로 매번 재구성**, 메모 0, `_aef_has:4257` 는 선형 스캔). armap(`/`·`__.SYMDEF`)은 `:4155-4160` 에서 **의도적으로 버려진다**. 작업량 ≈ 25 멤버 × ~3 pass × 265 UND × 122 sym ≈ 2.4M 문자열 물질화/링크.\n⚠️ **이 34초 중 archive_extract 의 몫은 미측정** — 계측 훅 추가가 선결.\n\n### 1-e. flatten (module_loader) — 별개의 O(n²)\n\n`self/module_loader.hexa:384 ml_word_mangle_global` — (lhs 20)×(rhs 22) = **440회 전체 소스 `.replace()`** per (파일 × 충돌전역). perf(dwarf): `hxlcl_strstr 87.16%`, 그중 `ml_word_mangle_global 83.49%`.\n`hexa_module_loader compiler/main.hexa` = **10.5s / 4.08 GB RSS** (출력 3.6MB). atlas_cli(1.0MB) = 0.10s / 40MB → **3.5× 바이트에 105× 시간**.\n증폭기: `stdlib/runtime/ctype.hexa:604` `out = out + s.substring(i, i+1)` — 문자단위 concat = **O(n²)**; C 미러(`runtime_core_emit.hexa:10641`)는 **매치가 0이어도 `malloc(2n+1)` + 전체 memcpy**.\n그리고 그 440× 를 **무장시키는 가짜 충돌**: `ml_canon_path:102` 가 절대화를 안 해 같은 물리 파일이 `compiler/atlas/static_index.hexa` 와 `/tmp/olc/compiler/atlas/static_index.hexa` 두 키로 **두 번 인라인**된다 (`grep -o 'begin: ' | uniq -d` 로 3개 확인). A/B: `env -u HEXA_LANG` → 중복 0, 73584 LOC / 9.34s.\n\n---\n\n## 2. 반증된 것 (census 가 틀렸던 것 — 정직하게)\n\n1. **\"줄수 2.4배에 시간 수십배 = 비선형\"** → **전제가 무효.** raw LOC 와 flatten LOC 는 무상관이다: `aot_cc_select` 305줄 → **7638 LOC**, `ccsd_rhf` 936줄 → 2080 LOC. \"빠른\" 프로그램이 실은 제일 컸다. raw LOC 축은 아무것도 설명하지 못한다.\n2. **\"hx.hexa = 300초 컴파일 시간 벽\"** → **아니다. 죽은 소스다.** `tool/hx.hexa:63 use \"tool/raw_audit\"` 인데 `tool/raw_audit.hexa` 가 없다(커밋 `87dda7427` 의 consumer-cascade 누락). module_loader FATAL(rc=2) → flatten 산출물 0 → `self/main.hexa:3270` \"falling back to raw src\" 로 **import 미해결 raw 740줄**을 먹였다. 죽은 use 를 지우면 flatten 성공(914 LOC) + **16.9초** 종료 — 단 여전히 컴파일 실패(`undefined name audit_append ×7`, `ssot_fingerprint ×3`). **hx.hexa 는 own-link 와 무관하게 애초에 컴파일 불가능하다** (패키지 매니저 엔트리가 깨져 있음 — 별건 버그).\n3. **\"5/5 own-link ✅ · STRICT FATAL 0\"** → **거짓 초록.** `hexa build` 는 **바이너리를 하나도 안 만들고 rc=0** 으로 끝난다. 재측정: `tool/compile.hexa` HX2001 + 바이너리 없음 + rc=0 · `tool/ai_native_profile.hexa` 동일. 유효 표본은 5 중 3.\n   더 나쁜 것: `HEXA_OWNLINK_STRICT` 자체가 `exit` 이 아니라 `return \"...\"` 이고(`feat/ownlink-strict-census:self/main.hexa:3647`, `:4778`), 유일 호출자 `:7609` 는 반환값을 **버린다**. → **\"STRICT FATAL 히트 0\" 은 사실이 아니다.** 계측기가 거짓말했다.\n4. **\"300초 타임아웃\"** 수치 자체가 **오염**됐다 — 측정 시점에 이전 census 의 ccsd 컴파일이 **RSS 20.6GB** 로 박스를 굶겨 hx 컴파일이 CPU 10.8% 만 받고 있었다.\n5. **\"토이 5개는 빨라서 초록이었다\"** → 아니다. canary(21줄·use=0) `hexa build` = **43.8초**. **아무도 시계를 안 댔을 뿐이다.**\n\n**살아남은 결론**: own-link 폴백률은 여전히 0 으로 보인다(clang 으로 되돌아간 증거 없음). 그러나 그 근거였던 표는 못 믿는다.\n\n**새로 발견된 결함 2개**: ① `tool/doctor.hexa` = **own-emit 미스컴파일** — own-link 빌드가 `OK: built (native own-link)` 를 찍고 바이너리를 남기는데 실행 시 **SIGSEGV(139)**. 격리: tier-B(own-emit + system `ld`)도 139, C-transpile 은 rc=1 정상 → **native EMIT 결함, `hexa_ld` 는 무죄.** ② `ccsd_rhf.hexa` = **parse phase 단독으로 >185s / RSS 16.4GB** (완주 못함, OOM 방지 kill). atlas 없는 `--emit=obj` 에서도 재현 → **atlas 와 독립된 두 번째 결함.**\n\n---\n\n## 3. 결정 — 3안 채점\n\n| 축 | **algo** (arena/mangle/bind/elf 복잡도) | **reuse** (atlas consumer-gate) | **gate** (예산 게이트) |\n|---|---|---|---|\n| ① 완성도 | **5** — 근인 자체(O(B) rewind·440×·O(n²) concat)를 제거. 프로그램이 커져도 재발 안 함 | 3 — **상수항** 제거(59%). 2차식은 남아 큰 프로그램에서 재발. ccsd·doctor 는 안 고침 | 1 — 아무것도 안 고침. 벽을 **박제**함 |\n| ② 단순 | 2 — 6 사이트, 런타임 시드 + emit 미러 이중 수정 | **5** — 실질 1~2줄 + 술어 1개 | 4 — 신규 3파일, 컴파일러 무손 |\n| ③ 안전 | 2 — arena `used` 소비자 전수 확인이 grep 기반. C(mangle)·F(dedup)는 **byte-eq neutral 아님** | **4** — cite-free 소스(=현실 100%)에서 obj 와 동일 코드경로 → 바이트 불변 예측 | **5** — 컴파일러 미접촉 |\n| ④ 표준 (reference-match) | 4 — bsearch·label_bkt·pack_lir·`_hx_ablk_tail` 선례와 동일 클래스, repo 안에 정답 코드 존재 | **5** — \"다른 경로엔 있는 최적화가 새 default 에 안 실렸다\" = 순수 회귀 복구. ld 의 armap 도 같은 케이스 | 3 — 예산 게이트는 표준이나 CLAUDE.md 가 금지하는 \"천장 enshrine\" 위험 |\n\n### 추천 (① 완성도 + ④ 표준 기준): **algo 를 본안으로, reuse 를 동봉, gate 는 선결 R0.**\n\n셋은 경쟁 관계가 아니다. **순서가 강제된다:**\n\n- **R0 — gate + rc-거짓말 수정 (필수 선결).** `hexa build` 가 실패에도 rc=0 이면 **이후 어떤 GREEN 도 못 믿는다**(하드교훈 #1·#7). 이걸 안 고치고 R1/R2 를 측정하면 이번 census 를 그대로 재현한다.\n- **R1 — reuse (atlas consumer-driven gate).** ④ 표준 만점. 회귀 복구이므로 정당성 논쟁 자체가 없다. **wall 의 59% (72.9s/124s)** 회수.\n- **R2 — algo A (arena rewind O(1)) + C/D (module_loader mangle + str_replace).** ① 완성도. **R1 의 대체재가 아니다** — R1 은 증상을 없애고 R2 는 2차식을 없앤다. R1 만 하면 큰 프로그램에서 되돌아온다(B 는 단조 증가).\n- **R3 — B (bind 버킷) + E (archive 메모).** R1/R2 로 atlas 세금이 사라진 뒤에야 비중이 드러난다. E 는 **먼저 계측 훅부터** (하드교훈 #7).\n\n> ⚠️ **algo 를 단독 추천하지 않는 이유**: perf 프로파일이 `--emit=exec`(atlas 상주) 한 케이스에서만 나왔다. R1 을 먼저 넣으면 블록 수가 줄어 A 의 이득이 축소돼 보일 수 있다 — 둘은 곱셈이 아니라 **부분 중첩**이다. 그래도 A 는 구조적 fix 이고 R1 은 상수 fix 라, **둘 다** 넣어야 한다.\n\n---\n\n## 4. 구현-ready 스펙\n\n### R0-1 (선결) — rc-거짓말\n`feat/ownlink-strict-census:self/main.hexa:3646-3647`, `:4777-4778`\n```hexa\n- return \"OWNLINK STRICT: fell back to ...\"\n+ eprintln(\"OWNLINK STRICT: fell back to ...\")\n+ exit(1)\n```\n(`cmd_build` 는 반환값으로 실패를 신호할 수 없다 — 호출자 `:7609` 가 무시한다.)\n그리고 **바이너리 존재 + ELF 매직 + 실행 스모크**를 판정에 넣는다(§5).\n\n### R0-2 — own-link 계측 사각지대(wall 27.6%) 제거\n`compiler/main.hexa`: 마지막 마크가 `:1182 cg_profile_mark(\"emit_asm\")` 이고 own-link 는 그 뒤 `:1350-1379`(x86_64 `link_elf_x86_64_ownstart_ar`) / `:1400-1428`(arm64) 에서 일어난다. 각 브랜치의 `exit(0)` 직전에:\n```hexa\ncg_profile_mark(\"own_link\")\n```\n`cg_profile` unset 이면 `mono_ns()` 도 안 탄다(`main.hexa:670-679`) → **byte-neutral**.\n\n### R1 — atlas consumer-driven gate  `compiler/main.hexa:726`\n```hexa\n- let atlas = if emit_kind == \"obj\" { empty_atlas_index() } else { _load_atlas() }\n+ // consumer-driven: 아틀라스는 (a) --strict-citations 이거나 (b) 소스에 실제\n+ // P/C/L/E ref 가 있을 때만 필요하다. obj 는 OOM 때문에 이미 스킵 중이었고,\n+ // exec/asm 이 그 게이트 밖에 남아 있었다(own-link default-flip 회귀).\n+ let needs_atlas = strict_citations || module_has_atlas_ref(module)\n+ let atlas = if needs_atlas { _load_atlas() } else { empty_atlas_index() }\n```\n신규 `compiler/check/resolve.hexa`:\n```hexa\npub fn module_has_atlas_ref(module: Module) -> bool\n// items → annotations.args + body expr DFS\n// hit: _resolve_is_atlas_ref_kind(e.kind) || len(e.atlas_ref.kind) > 0   (:141 재사용)\n// item_is_parse_only(it) 이면 body 스킵 (resolve 와 동일 규칙) · 할당 없는 순수 술어\n```\n**단순히 `|| emit_kind == \"exec\"` 로 넓히지 마라** — HX1042 는 `Severity::Error`(`compiler/diag/catalog.hexa:142`) 이므로 cite 를 가진 소스에서 **에러를 조용히 삼킨다**. consumer-driven 은 그 구멍을 막고 **obj 경로에 이미 있던 같은 잠재 결함까지 고친다**.\n소비자 실측: 트리 전체에서 실제 애노테이션(`^[[:space:]]*@(cite|verify|grace)`, 주석 제외)은 **1건**(`self/test_keyword_audit.hexa:343`, P/C/L/E ref 없음). stdlib 의 `@cite(L[...])` 는 **전부 주석**.\n**기대 효과**: atlas_load 11.0s + 하류 세금 ~61.9s = **72.9s / 124.0s (59%)**. exec wall 124s → **45~55s** 예측. (own-link 33s 가 세금을 같이 먹는지는 **미측정 — 주장 안 함.**)\n\n### R2-A — `hexa_arena_rewind` O(B) → O(1)  `self/rt/alloc.hexa:151-168`\n```hexa\n    __blk_set_used(block, used)\n-   let mut b = __blk_next(block)\n-   while b != 0 { __blk_set_used(b, 0)  b = __blk_next(b) }\n+   // O(1): 후속 블록은 재사용 시 alloc 이 used=0 으로 리셋한다(alloc.hexa:135).\n+   // used 는 cur 에서만 읽히므로(mark/alloc/scope_push) stale 값은 관측 불가.\n    __arena_cur = block\n```\n**dead-work 논거**(전수 grep): `used` 를 읽는 곳은 `alloc.hexa:121 __blk_used(b)`(b==`__arena_cur`) · `:146 hexa_arena_mark()`(`__arena_cur`) · `runtime_core_emit.hexa:5571/5574/5602`(전부 `__cur`) — **전부 `cur` 한 블록**. 나머지 소비자(`_hx_ablk_*` bsearch, `hexa_val_heapify`)는 `__blk_data`/`__blk_cap` 만 읽는다. 어떤 블록이 `cur` 가 되는 4 경로는 **전부 진입 시 used 를 스스로 확정**한다(재사용 arm `alloc.hexa:135 __blk_set_used(nb,0)` · rewind 명시 set · reset · 초기화).\n동일 편집을 C 미러에: `self/runtime_core_emit.hexa:5445` 의 `\"    for (HexaArenaBlock* b = m.block->next; b; b = b->next) b->used = 0;\\n\"` 줄 삭제 (C `hexa_arena_alloc` 재사용 arm 도 이미 `nb->used = 0;` 한다). **덤으로 `:5436` 의 거짓 주석(\"O(1)\")이 그제야 참이 된다.**\n**덤 2**(같은 클래스): `alloc.hexa:131-133` 의 `while __blk_next(tail) != 0 { ... }` (블록 append 마다 O(B)) → `__arena_tail` 전역 캐시. `_hx_ablk_tail`(`runtime_core_emit.hexa:1894`)이 이미 쓰는 기법.\n**기대**: perf 38.7% 소멸 + **\"atlas_load 이후 4.4~5.5× 세금\" 메커니즘 자체가 소멸.** 방출 바이트 불변(카운터 시맨틱만).\n**⚠️ 유일한 진짜 위험**: `used` 소비자를 전부 찾았는가. `HEXA_STREAM_RECLAIM`/free-tree 계열이나 **미확인 arm64/darwin emit arm** 이 전체 블록의 `used` 를 스캔하면 즉시 무너진다 → 3-target byteeq + 실제 runtime.a 스모크 필수.\n\n### R2-C — `ml_word_mangle_global` 440 pass → 1 pass  `self/module_loader.hexa:384` (호출 `:1536`)\n식별자 토크나이징 1패스 + 파일당 충돌명 **전체를 한 번에**(`ml_hset_*` 는 `:1529` 에서 이미 사용):\n```hexa\nfn ml_mangle_globals_pass(src, names_hs, prefix) {\n    let n = len(src)\n    let mut parts = []\n    let mut i = 0\n    while i < n {\n        let c = src[i]\n        if !_ml_ident_ch(c) { parts.push(src.substring(i, i+1))  i = i + 1  continue }\n        let start = i\n        while i < n && _ml_ident_ch(src[i]) { i = i + 1 }\n        let tok = src.substring(start, i)\n        let mut ok = ml_hset_has(names_hs, tok) && start > 0 && i < n\n        if ok { ok = _ml_lhs_ok(src[start-1]) && _ml_rhs_ok(src[i]) }   // :387-402 문자집합 그대로\n        if ok { parts.push(prefix + tok) } else { parts.push(tok) }\n    }\n    return parts.join(\"\")      // ⚠️ out = out + tok 절대 금지 — O(n²)\n}\n```\nlhs/rhs 집합을 **바꾸지 마라** — 그게 byte 동치의 유일한 근거다(`.` 는 LHS 에 없다 → `x.name` 필드접근 불가침, 원본과 동일).\n**기대**: `hexa_module_loader compiler/main.hexa` 10.5s/4.08GB → **<1s/수백MB**.\n**⚠️ 시맨틱 델타(정직하게)**: 원본은 `.replace()` 가 구분자를 소비해 **인접 occurrence**(`[foo,foo]`)에서 하나를 놓친다. 신규는 둘 다 맹글한다 → **원본이 버그였던 케이스에서만 출력이 달라진다.** 검증은 \"동일 증명\"이 아니라 **old/new flatten diff 전수 검사**.\n\n### R2-D — `rt_str_replace` O(n²) → O(n)  `stdlib/runtime/ctype.hexa:583-612`\n`out = out + s.substring(i, i+1)` (:604) 를 **런(run) 단위 `parts.push` + `join(\"\")`** 로 교체하고 **무매치 시 `s` 조기반환**(무할당). 출력 동일 — 이건 진짜 strict-equivalent.\nC 미러 `self/runtime_core_emit.hexa:10641` 앞에:\n```c\nif (!hxlcl_strstr(HX_STR(s), HX_STR(old))) return s;   // 무매치 = malloc/memcpy 0\n```\n**⚠️ 델타**: 무매치 시 `hexa_str_own(result)` 대신 입력 `s` 를 앨리어싱 반환 — 아레나 수명 이슈가 **이론적으로** 가능(호출자가 이미 그 `s` 를 들고 있으므로 안전하다고 **추정**하나 미측정) → self-transpile byteeq 3회로 확인.\n\n### R3-B — `_bind_lookup` 64-버킷  `compiler/check/bind.hexa:232` (+ `_bind_is_mut:267`)\n정답지는 repo 안에 있다: `compiler/lower/ast_to_hir.hexa:1290-1330`(`_lr_ast_gname_hash`/`_lr_ast_prime_globals`) + `compiler/check/types.hexa:64-141`. frame 0 = 빌트인 + 전체 top-level 전역(`bind.hexa:1737-1766` 프리패스); 나머지 프레임은 작다 → **frame 0 만 probe**, frame n-1..1 은 기존 선형 유지. prime 호출은 `:1766` 프리패스 직후.\n**⚠️ byte-eq 논거의 핵심**: 원본은 frame 0 을 **전방** 스캔(첫 매치 승) — 버킷 삽입순이 인덱스 오름차순이므로 전방 버킷 워크의 첫 히트 = 전방 full-scan 첫 히트. **ast_to_hir 는 후방 스캔이라 후방 워크였다. 여기서 방향을 뒤집으면 shadowing 이 깨진다.** 그리고 prime 이후 frame 0 에 추가되는 정의(`:1007 _define_mut`)를 위한 **꼬리 선형 스캔을 빼먹지 마라.**\n\n### R3-E — `archive_extract_fixpoint` 메모+해시셋  `compiler/emit/elf_x86_64.hexa:4297`\n① 파싱 루프(`:4302-4310`)에서 멤버별 심볼명 **1회** 물질화(`sym_names[m][k]`) → 이후 `_aef_symname(...)` 호출 **전부** 룩업으로 교체. ② `_aef_symname:4243` 자체를 런-빌드(`parts.push` + `join`). ③ `def_names`/`und_names` 를 hset 병행(64버킷, `ml_hset_*` 동형). ④ floor-pull arm(`:4380-4395`, `:4419-4440`)의 3중 루프 → 멤버당 `hxlcl_` 접두 hset 1회 순회.\n**기대**: ~2.4M 문자열 물질화 → **~3,051** (멤버당 1회).\n**⚠️ 미측정 — 33s 중 archive_extract 의 몫을 모른다. R0-2 로 마크 박고 재고 나서 고쳐라.**\n\n### R3-F — 경로 dedup 절대화  `self/module_loader.hexa:850 ml_resolve_full`\n`ml_canon_path:102` 가 순수 렉시컬(절대화 없음) → HEXA_LANG 절대경로와 cwd 상대경로가 다른 키. `PWD` 로 절대화 후 canon.\n**⚠️ 위험도 최고** — dedup 키가 바뀌면 두 번 방출되던 파일이 한 번만 방출된다 → **flatten 텍스트가 실제로 변한다.** R2-C 가 440× 를 이미 죽였으므로 **급하지 않다.** 반드시 (1) 가짜 충돌 실재 증명 → (2) flatten diff + byteeq 3-target 순서로.\n\n### R3-G — 별건 버그 (컴파일 성능과 무관, 그러나 제품 결함)\n- `tool/hx.hexa:63` 죽은 `use \"tool/raw_audit\"` + `audit_append`/`ssot_fingerprint` 미정의 → **패키지 매니저 엔트리가 컴파일 불가.**\n- `tool/doctor.hexa` own-emit SIGSEGV (native EMIT 결함, hexa_ld 무죄).\n- `ccsd_rhf.hexa` parse-only 16.4GB.\n\n---\n\n## 5. 게이트 — \"알려진 느린 상태에서 RED\" 실증 완료\n\n산출물 3 파일(모두 aiden 실행 검증):\n- `/private/tmp/claude-501/-Users-mini-dancinlab-hexa-lang/adbce6b7-54f3-4f01-a7fe-77d3f277b3f0/scratchpad/compile_time_budget_gate` → `tool/compile_time_budget_gate` (chmod +x, `bash -n` 통과)\n- `…/scratchpad/compile_time_budget.tsv` → `tool/compile_time_budget.tsv`\n- `…/scratchpad/compile-time-budget.yml` → `.github/workflows/compile-time-budget.yml`\n\n**rc 를 절대 믿지 않는다.** 행마다 A~E 전부 주장: **A** 아티팩트(파일 존재 + `7f454c46` ELF 매직 + `-x`) · **B** 무진단(`HXnnnn`/`error:` 0줄) · **B2** own-link 폴백 0(`HEXA_OWNLINK_STRICT=1`) · **C** 런-스모크(rc≥128 = 시그널사 → RED) · **D** wall ≤ 예산 · **E** maxRSS ≤ 예산.\n정규화: 고정 canary 로 `norm = max(1, canary_ms/CANARY_REF_MS)` → wall 예산만 스케일(RSS 는 비정규화 — 할당 수는 기계 독립).\n티어: `flatten`(초 단위 → 클라우드) / `build`(분 단위 → self-hosted; 오프라인이면 exit 2 = INFRA-neutral) / `quarantine`(기지의 파손 — 측정·보고하되 판정 제외; 예산 안에 들면 `STALE-QUARANTINE` 로 승격 요구).\n\n**RED 실증 (동일 스크립트, 매니페스트만 교체 · aiden `/tmp/olc`):**\n```\n(a) 파손 상태를 enforce 티어에 넣으면:\n  build tool/doctor.hexa   38406ms 2330M  RED(smoke died on signal rc=139)\n  build tool/compile.hexa  13988ms 1818M  RED(no binary; rc=0 <- rc is a LIE)\n  [ctb] enforced: 3 ok / 2 RED → exit 1\n(b) 출하 매니페스트(파손 행 quarantine):\n  [ctb] enforced: 5 ok / 0 RED  quarantined: 4 → exit 0 GREEN\n```\n재현: `ssh aiden 'cd /tmp/olc && bash tool/compile_time_budget_gate'` (GREEN) / `… CTB_MANIFEST=tool/ctb_demo.tsv …` (RED). 로그 보존: `/tmp/ctb_final.out`, `/tmp/ctb_demo.out`.\n\n**⚠️ 게이트 자체의 구멍(정직하게, 반드시 막을 것)**:\n- **canary 정규화가 벽을 숨긴다** — canary 자체가 atlas 11s + own-link 34s 상수를 품는다. 그 상수가 2배가 되면 canary 도 2배 → norm 2배 → **예산이 같이 늘어나 회귀를 삼킨다.** 완화(미구현): `canary_ms` 절대 상한(예: 60s) 초과 시 RED.\n- **예산 게이트는 벽을 합법화한다.** R1/R2 가 들어오면 반드시 `--rebaseline` 로 예산을 **조여라.** 조이는 규율이 없으면 이 게이트는 124s 를 천장으로 enshrine 한다 — CLAUDE.md 가 금지하는 바로 그것.\n- quarantine 에 **만료일/이슈번호 컬럼이 없다** → 영구 면제 위험.\n\n**검증 프로토콜(모든 편집 공통)**: (0) R0-1 rc-거짓말 선수정 — 안 하면 아래 어떤 GREEN 도 못 믿는다. (1) 대조군 = GitHub 새 clone + `env -u HEXA_PREBUILT_RUNTIME` 풀 빌드, 동일 호스트, **경합 프로세스 0**. (2) BEHAVIOR: `build/aprime_cc --backend=native --emit=exec --linker=hexa` 직접 호출(+`HEXA_LANG`) + `HEXA_CG_PROFILE=1` + `/usr/bin/time -v` + `perf record`. (3) **R1 의 진짜 게이트 = 산출 바이너리 `sha256` 전/후 동일** + `--emit=obj` .o 도 동일. (4) **네거티브 컨트롤 필수**: `@cite(L[...])` 를 단 소스 1개 → 진단 동일(순진 확장 버전이면 여기서 깨진다). (5) byteeq 3-target + gen3≡gen4 + install.sh consumer smoke. (6) R2-A 성공 판정 = obj→exec phase 배율이 **1× 근처로 붕괴** + perf 에서 `hexa_arena_rewind`+`__blk_*` 소멸. (7) R2-A 머지 직후 **ccsd_rhf 재측정** — A 의 인과를 확인하는 가장 싼 대조 실험.\n\n---\n\n## 6. axis-① 함의 — 정직하게\n\n**막는가: 부분적으로 예. 그러나 이 벽은 axis-① 의 blocker 가 아니라 UX 벽이다.**\n\n- own-link 는 이미 default-ON 이고 **폴백 증거는 없다.** own-link 가 **기능적으로** hexa_cc.c 를 대체 못 한다는 증거는 이번 측정에서 **나오지 않았다.** 즉 axis-① 의 *기능* 게이트는 안 막혀 있다.\n- 막는 것은 **실사용성**이다: 21줄 토이가 **43.8초**, 7638-LOC 프로그램이 **124초/4.2GB**. clang 폴백을 지우면 사용자는 이 시간을 **선택 불가로** 먹는다 → CLAUDE.md 의 **release-integrity > self-host** 에 따라 지금 상태로 hexa_cc.c 를 지우면 **사용자 경로를 망가뜨린다.**\n- **고치면 열리는가**: R1(59%) + R2 로 exec wall 124s → **~50s → (R2-A 로 5× 세금 소멸 시) obj 수준 16.8s + own-link** 로 내려간다. 그러면 남는 상수는 **own-link ~33s** 이고, 이건 **프로그램 크기와 무관** → R3-E 가 유일한 남은 큰 항이다.\n- **그러나 axis-① 를 실제로 막는 것은 컴파일 시간이 아니라 새로 드러난 두 결함이다**: `tool/doctor.hexa` **own-emit SIGSEGV** (own-emit 이 잘못된 코드를 낸다 — 이건 정확성 벽) 와 `ccsd_rhf` parse 16.4GB. **정확성 벽이 성능 벽보다 상위다.** doctor 의 근인이 잡히기 전에는 own-link default 를 신뢰할 수 없다.\n\n**결론**: 컴파일-시간 벽은 **고칠 수 있고, 고치면 열린다** (R1+R2 = 측정된 레버). 그러나 axis-① 의 다음 진짜 게이트는 **doctor SIGSEGV** 다.\n\n---\n\n## 7. 모르는 것 + 다음에 볼 곳\n\n1. **own-link ~33s 의 내역** — 계측 훅 자체가 없다(`emit_asm` 이후). `archive_extract_fixpoint` 인지 `hexa_ld` 다른 곳인지 **모른다**. → **R0-2 (`cg_profile_mark(\"own_link\")`) 를 먼저 박아라.** 안 재고 E 를 고치면 하드교훈 #7 위반.\n2. **own-link 33s 가 atlas 상주 세금을 얼마나 먹는지 — 미측정.** R1 이 own-link 도 줄일 가능성이 높으나 **주장하지 않는다.**\n3. **`atlas_load` 11.0s 자체가 atlas 크기에 O(n²)인지 — 미측정** (atlas 를 잘라 컴파일러 재빌드해야 함). 다만 17,354노드 / 10.99s = **노드당 610µs** 는 struct 리터럴 구성치고 비정상적으로 크다(`embedded.gen.hexa` = 17k struct 리터럴 정적 배열, `static_atlas()` 가 **매 컴파일마다 런타임 구성** — 노드당 중첩 struct 2 + 빈 배열 7 ≈ 12만 할당). R1 이 들어가면 실사용 경로에서 무의미해지므로 후순위.\n4. **bind ×27 / type_check ×31 중 arena-rewind 세금과 `_bind_lookup` 선형스캔의 분리 — 안 했다.** R2-A 머지 후 재측정하면 자동으로 분리된다.\n5. **ccsd_rhf 의 parse 16.4GB 근인 — 증명 안 됨.** 같은 arena-rewind 프로파일이 나오지만 **인과 미확정**. `--emit=obj` 로 90초/8GB 에 싸게 재현된다(atlas 없이도 터짐). → R2-A 직후 재측정.\n6. **doctor SIGSEGV 의 근인 미규명** — own-emit 이라는 것까지만 격리(tier-B 도 139, C-transpile 정상). 다음: `build/aprime_cc --emit=obj` 산출물 gdb `bt` → 크래시 PC → 함수 특정 (485 LOC flatten 이라 최소재현 축소 가능).\n7. **arm64 / darwin 전부 미측정** (하드교훈 #6). 이 문서의 모든 숫자·예산은 **linux-x86_64 전용**. 특히 R2-A 의 `used` 소비자 전수 주장은 **arm64/darwin emit arm 을 안 봤다** — 여기가 깨지면 A 는 즉시 무너진다.\n8. **클라우드 러너 canary 값 미측정** — `CANARY_REF_MS=44000` 은 aiden 기준. 첫 CI 런에서 norm 배율 보고 조정 필요.\n9. **`compiler/link/incr_cache.hexa` (462줄) 는 프로덕션 호출자가 0** — 배선 안 된 선반 재고. own-link 재컴파일 캐시 레버 후보(미평가)."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Profile"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Census"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Verify"
    },
    {
      "type": "workflow_phase",
      "index": 4,
      "title": "Design"
    },
    {
      "type": "workflow_phase",
      "index": 5,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "profile:measure",
      "phaseIndex": 1,
      "phaseTitle": "Profile",
      "agentId": "a11402e9be0c8c51a",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783973466453,
      "queuedAt": 1783973466443,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "호스트: aiden (12-core, 30GB, linux-x86_64). 트리: /tmp/olc…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783974978624,
      "tokens": 125678,
      "toolCalls": 70,
      "durationMs": 1512171,
      "resultPreview": "{\"method\":\"호스트: aiden (12-core, 30GB, linux-x86_64). 트리: /tmp/olc (GitHub 새 clone @77b1ba9d, 이미 빌드됨). 직접 `ssh -o BatchMode=yes aiden` + nohup (pool on 미사용, pkill -f 미사용).\\n\\n⚠️ 측정 도구 자체를 먼저 고쳐야 했다 (하드교훈 #7):\\n1) `hexa build`는 컴파일러 stdout/stderr를 삼키고, CG_PROFILE 설정 시에만 실패 경로에서 forward한다(#4483). 즉 **성공 컴파일은 phase 데이터를 절대 못 얻는다.** → `build/aprime_cc _drv.hexa --backend=native --emit=exec --linker=hex…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "census:frontend",
      "phaseIndex": 2,
      "phaseTitle": "Census",
      "agentId": "a7370df0936fc7430",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783974978635,
      "queuedAt": 1783974978628,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "■ 한 줄 결론: 프론트엔드에서 새로 확인된 알고리즘 복잡도 사…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783975962737,
      "tokens": 163702,
      "toolCalls": 62,
      "durationMs": 984102,
      "resultPreview": "{\"findings\":[{\"site\":\"self/module_loader.hexa:384 (call site :1536)\",\"fn\":\"ml_word_mangle_global\",\"complexity\":\"O(F × C_f × 440 × |body_f|) — 20 lhs × 22 rhs = **440 full-source `.replace()` passes per (file, collided-global)**. MEASURED: 83.5% of `hexa_module_loader compiler/main.hexa` (dwarf callgraph, aiden). Flatten of compiler/main.hexa = **10.5s wall / 4.08 GB RSS** for a 3.6 MB output; atla…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "census:codegen",
      "phaseIndex": 2,
      "phaseTitle": "Census",
      "agentId": "a50f9f77952c75a97",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783974978636,
      "queuedAt": 1783974978628,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "■ 헤드라인 (실측, aiden /tmp/olc @77b1ba9d, 직접 ssh + nohu…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783977292225,
      "tokens": 129112,
      "toolCalls": 63,
      "durationMs": 2313589,
      "resultPreview": "{\"findings\":[{\"site\":\"compiler/emit/elf_x86_64.hexa:4389\",\"fn\":\"archive_extract_fixpoint (floor-pull arm)\",\"complexity\":\"O(passes × members × |und_names| × |syms_per_member| × L²) — and CONSTANT in program size (it is pure runtime.a work). Measured n on aiden: members=25, syms=3051 (~122/member), unique UND=265 (und_names grows to ~265+), fixpoint re-runs the whole member sweep every `while change…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "census:diff-path",
      "phaseIndex": 2,
      "phaseTitle": "Census",
      "agentId": "ab352ff913845cdef",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783974978636,
      "queuedAt": 1783974978628,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "■ DIFF-PATH VERDICT (aiden, linux-x86_64, 12-core/30GB, iso…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976166440,
      "tokens": 127588,
      "toolCalls": 50,
      "durationMs": 1187804,
      "resultPreview": "{\"findings\":[{\"site\":\"compiler/emit/elf_x86_64.hexa:3216 (call) → :4277 (body)\",\"fn\":\"_aef_all_member_defs (called unconditionally from link_elf_x86_64_ownstart_ar)\",\"complexity\":\"O(S · D) string-equality — S=3051 symbols in runtime.a, D grows to 2686 defs ⇒ ~4.1M `==` string compares, on EVERY own-link, over ALL 25 members (even members never pulled). Program-size-INDEPENDENT → sits squarely insi…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "verify:frontend:ml_word_mangle_globa",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "aa248a43252e1d358",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783975962753,
      "queuedAt": 1783975962740,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, with one scoping caveat. (1) The quoted loop is …",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976040437,
      "tokens": 48306,
      "toolCalls": 6,
      "durationMs": 77684,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, with one scoping caveat. (1) The quoted loop is real executable code at self/module_loader.hexa:384-416 (not a comment), and its call site at :1536 is live inside the emit loop, gated only by g_collided_count>0 and per-file declared-collided names — flatten is on the own-link compile path for any program with `use`, so it is not dead. (2) The complexity claim …"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "verify:frontend:hexa_arena_rewind (c",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a0d0dac1055e207c8",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783975962754,
      "queuedAt": 1783975962740,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Could not refute — the mechanism survives every refutation …",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976089846,
      "tokens": 78578,
      "toolCalls": 13,
      "durationMs": 127091,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Could not refute — the mechanism survives every refutation axis, with one citation correction. (1) The quoted C at self/runtime_core.c:4938 IS real code but sits in the `#else // !HEXA_RT_ALLOC_NATIVE` fallback arm (opened at :4835), so it is not the body compiled in the default build. However the LIVE default-build body, self/rt/alloc.hexa:151-168 (`fn hexa_arena_rewind…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:frontend:_bind_lookup",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "afa519eef733b5e29",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783975962755,
      "queuedAt": 1783975962740,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. (1) The quoted code at /Users/mini/…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976059386,
      "tokens": 55398,
      "toolCalls": 10,
      "durationMs": 96631,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. (1) The quoted code at /Users/mini/dancinlab/hexa-lang/compiler/check/bind.hexa:232-247 is real executable code (verified by reading, not grep) — a full linear `while j < m` scan of `fr.defs[j] == name` per frame, with no hash/bucket index. `_bind_is_mut` (line 267) duplicates the same unbucketed walk. (2) The path is live and unconditional on the…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:frontend:ml_canon_path / ml_r",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a975cd3488ad3a762",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783975962755,
      "queuedAt": 1783975962740,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED by independent repro. (1) Cited lines are executa…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976103878,
      "tokens": 56147,
      "toolCalls": 14,
      "durationMs": 141123,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED by independent repro. (1) Cited lines are executable code at the exact numbers: self/module_loader.hexa:102 `fn ml_canon_path(p)` (lexical ./.. collapse only — computes is_abs but never prefixes cwd), :768 `fn ml_resolve_project_root` ($HEXA_LANG join → absolute), :850/:860 `ml_resolve_full` → `ml_canon_path(_rf)` canonicalizes the RAW result, so a caller-relat…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "verify:frontend:hexa_str_replace",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a8ff608cee6a34691",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783975962756,
      "queuedAt": 1783975962741,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, with two scope corrections. (1) The cited body i…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976122044,
      "tokens": 65137,
      "toolCalls": 14,
      "durationMs": 159288,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, with two scope corrections. (1) The cited body is real executable code and matches its SSOT emitter (self/runtime_core_emit.hexa:10633-10669 emits verbatim what appears at self/runtime_core.c:9896-9931), so the generated .c is not stale. (2) The `#ifndef HEXA_HAS_HEXA_RT_STDLIB` guard does not make it dead: the shipped runtime.a compiles runtime.c/runtime_core…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "verify:frontend:ml_apply_alias_prefi",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a5d209f9e0d39ff52",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783975962756,
      "queuedAt": 1783975962741,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The cited code is real (self/module_loader.hexa:513-534, li…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976018017,
      "tokens": 47557,
      "toolCalls": 7,
      "durationMs": 55261,
      "resultPreview": "{\"refuted\":true,\"reason\":\"The cited code is real (self/module_loader.hexa:513-534, live code not a comment) and the complexity claim is if anything understated — lhs_set=9, rhs_set=18, so 162 (not 135) full-source .replace() passes per pub-decl name. BUT the path is dead in every compile in this repo, so it cannot be the hx.hexa >300s wall. Call site (:1556-1559) is guarded by `let my_alias = g_pa…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "verify:diff-path:_aef_all_member_defs",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a3b75fe40b64e459b",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783976166443,
      "queuedAt": 1783976166442,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Code facts hold (elf_x86_64.hexa:3216 is real code, uncondi…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976267021,
      "tokens": 54579,
      "toolCalls": 7,
      "durationMs": 100578,
      "resultPreview": "{\"refuted\":true,\"reason\":\"Code facts hold (elf_x86_64.hexa:3216 is real code, unconditional; :4277 body does O(S·D) linear `_aef_has` dedupe), but the BOTTLENECK claim fails on four counts. (1) Magnitude: the same `_aef_has` primitive is used by `archive_extract_fixpoint` over PROGRAM symbols — seed Pass 1 (:4323) is O(P²) and the #4871 floor-pull (:4391/:4430) is O(rounds·members·U·(D+syms)) — 10…"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "verify:diff-path:archive_extract_fixp",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a71e2e795759eb9d4",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783976166445,
      "queuedAt": 1783976166442,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CODE FACTS (all confirmed — the claim is not quoting commen…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976323688,
      "tokens": 63164,
      "toolCalls": 10,
      "durationMs": 157243,
      "resultPreview": "{\"refuted\":true,\"reason\":\"CODE FACTS (all confirmed — the claim is not quoting comments and not dead code):\\n- /Users/mini/dancinlab/hexa-lang/compiler/emit/elf_x86_64.hexa:4297 `archive_extract_fixpoint` is real, executable, and ON-PATH for default own-link (only caller chain: compiler/main.hexa:1352 → link_elf_x86_64_ownstart_ar (:3208) → archive_extract_fixpoint (:3211)).\\n- The floor-pull nest…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "verify:diff-path:_aef_symname",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "aad4ed96cb8b440de",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783976166445,
      "queuedAt": 1783976166442,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Code is real (not a comment) and uncached — that part of th…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976350352,
      "tokens": 78825,
      "toolCalls": 14,
      "durationMs": 183907,
      "resultPreview": "{\"refuted\":true,\"reason\":\"Code is real (not a comment) and uncached — that part of the claim holds. But the bottleneck claim fails on three grounds. (1) COMPLEXITY MIS-SCALES: O(L²) is quadratic in symbol-name LENGTH (bounded ~10-40 chars), not in program size. Total = Theta(#calls x L̄²) = LINEAR in symbol count with a fat constant. A linear term cannot explain the observed superlinear scaling (a…"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "verify:diff-path:link_elf_x86_64_owns",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "aae9464e841548508",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783976166445,
      "queuedAt": 1783976166442,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "인용 코드는 실재(주석 아님)하고 실행되지만 병목이 아니다. (…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976301101,
      "tokens": 52257,
      "toolCalls": 12,
      "durationMs": 134656,
      "resultPreview": "{\"refuted\":true,\"reason\":\"인용 코드는 실재(주석 아님)하고 실행되지만 병목이 아니다. (1) 규모 실측: nm build/runtime.a = 심볼 3,582개·평균 이름 20자·obj 12개 → 프리스캔의 O(Σ L^2) 총량 ≈ 0.7M char-copy / ~72k concat = 수십 ms~1초급, 300초 벽과 무관. (2) 이미 최적화된 클래스의 잔재: adb39cbba(#4851) \\\"own-link 1024-bucket def-index + sym-name cache — >90min→106s\\\"가 진짜 O(n^2) 항(95k relocs × 이름 2회 재구성 = 190k + def_names 선형 스캔)을 제거했고, 프리스캔은 그 190k의 ~1.9%. (3) 대조군이 결…"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "verify:diff-path:parse_elf_x86_obj /",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a1ed2a7b4cd7e20f0",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783976166445,
      "queuedAt": 1783976166442,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The cited lines are real executed code (elf_x86_64.hexa:389…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976253596,
      "tokens": 47116,
      "toolCalls": 9,
      "durationMs": 87151,
      "resultPreview": "{\"refuted\":true,\"reason\":\"The cited lines are real executed code (elf_x86_64.hexa:3892/:3897/:3904/:3921 byte-at-a-time section pushes; :2084 concat twin), so the \\\"comment/dead code\\\" refutations do not apply — but the claim is refuted as the BOTTLENECK on three grounds. (1) The census data itself contradicts byte-proportional cost: aot_cc_select.hexa is 305 source lines but its closure is `use s…"
    },
    {
      "type": "workflow_agent",
      "index": 16,
      "label": "verify:diff-path:link_elf_x86_64_owns",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a7f1ea3f45014120f",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783976166445,
      "queuedAt": 1783976166442,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED as a bottleneck (the code facts are real, the causa…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783976215796,
      "tokens": 50368,
      "toolCalls": 4,
      "durationMs": 49351,
      "resultPreview": "{\"refuted\":true,\"reason\":\"REFUTED as a bottleneck (the code facts are real, the causality is not).\\n\\nWhat I verified by opening /Users/mini/dancinlab/hexa-lang/compiler/emit/elf_x86_64.hexa:\\n1. The cited lines ARE live code, not comments. :3080 `while gfk < len(def_names) { if def_names[gfk] == gname { … } }` (GOT FILL), :2285-2299 (GOT COLLECT, `got_syms` linear seen-scan per kind-9 reloc), :29…"
    },
    {
      "type": "workflow_agent",
      "index": 17,
      "label": "verify:codegen:archive_extract_fixp",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a6c4775cdf6ec5530",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783977292271,
      "queuedAt": 1783977292266,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Claim survives. (1) Lines 4387-4405 of /Users/mini/dancinla…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783977402199,
      "tokens": 64157,
      "toolCalls": 8,
      "durationMs": 109928,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Claim survives. (1) Lines 4387-4405 of /Users/mini/dancinlab/hexa-lang/compiler/emit/elf_x86_64.hexa are executable code, not comments; _aef_symname (:4243) is a char-by-char immutable-concat rebuild with zero memo and _aef_has (:4257) is a flat linear scan. (2) The path is live and own-link-only: compiler/main.hexa:1352 -> link_elf_x86_64_ownstart_ar (:3207) -> archive_…"
    },
    {
      "type": "workflow_agent",
      "index": 18,
      "label": "verify:codegen:archive_extract_fixp",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a88d7c713e79fb6e9",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783977292273,
      "queuedAt": 1783977292266,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The cited code at compiler/emit/elf_x86_64.hexa:4426-4444 i…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783977366207,
      "tokens": 49236,
      "toolCalls": 6,
      "durationMs": 73934,
      "resultPreview": "{\"refuted\":true,\"reason\":\"The cited code at compiler/emit/elf_x86_64.hexa:4426-4444 is real, live, executable code (the #4871 own-link floor-pull mirror), and _aef_symname/_aef_has really are O(L^2)-rebuild + linear-scan. But it is NOT the bottleneck, for three independent reasons. (1) The claim admits the term is \\\"constant in program size\\\" — it is a function of runtime.a (~25 pulled members x ~…"
    },
    {
      "type": "workflow_agent",
      "index": 19,
      "label": "verify:codegen:_aef_all_member_defs",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "ad5d4dec92a6ab98d",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783977292274,
      "queuedAt": 1783977292266,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The code facts check out (real code, not comments: :4277 _a…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783977368076,
      "tokens": 46938,
      "toolCalls": 6,
      "durationMs": 75802,
      "resultPreview": "{\"refuted\":true,\"reason\":\"The code facts check out (real code, not comments: :4277 _aef_all_member_defs is live, called at :3216; it does a genuine 2nd full parse_elf_x86_obj over all members that archive_extract_fixpoint already parsed at :3204-3208; _aef_has at :4257 is a real O(n) linear dedupe, and the existing _ldx_hash index at :1617 is not used there). But causality fails: the function's in…"
    },
    {
      "type": "workflow_agent",
      "index": 20,
      "label": "verify:codegen:_census_resid_und (c",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "afff4905b7ac3d6c6",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783977292275,
      "queuedAt": 1783977292266,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED as the bottleneck (the code itself is real, live, a…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783977388325,
      "tokens": 50984,
      "toolCalls": 8,
      "durationMs": 96050,
      "resultPreview": "{\"refuted\":true,\"reason\":\"REFUTED as the bottleneck (the code itself is real, live, and genuinely uncached — but it is not the wall).\\n\\nDECISIVE CONTROL: the claim's own headline figure, \\\"program obj (7638 LOC) = 19,727 relocs\\\", belongs to the program that PASSED. Flattening the import closures:\\n  - tool/aot_cc_select.hexa: 305 LOC + self/lexer (1465) + self/parser (5853) = ~7,623 flattened LO…"
    },
    {
      "type": "workflow_agent",
      "index": 21,
      "label": "verify:codegen:_symc_lookup call si",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a14584c9e01a0fa62",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783977292275,
      "queuedAt": 1783977292266,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED — 사이트는 실재하지만(주석 아님·복잡도 주장 자체는 …",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783977360773,
      "tokens": 54796,
      "toolCalls": 8,
      "durationMs": 68497,
      "resultPreview": "{\"refuted\":true,\"reason\":\"REFUTED — 사이트는 실재하지만(주석 아님·복잡도 주장 자체는 구조적으로 맞음), 관측된 벽의 경로가 아니다(dead on the measured path).\\n\\n1) **관측된 벽은 linux-x86_64 이다.** hx.hexa rc=124 (>300s)는 aiden(linux-x86_64)에서 own-emit+own-link default-ON 경로로 측정됐다. own-link default-flip 커밋 d08bb7c1b(#4902)/#4881 의 본문이 명시: \\\"hexa run **x86_64-linux** now uses own-emit + hexa_ld\\\". 그리고 78c419ec4(#4483)는 native `--emit=obj` 경로를 …"
    },
    {
      "type": "workflow_agent",
      "index": 22,
      "label": "design:algo",
      "phaseIndex": 4,
      "phaseTitle": "Design",
      "agentId": "a5a45aa68b7e6429e",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783977402247,
      "queuedAt": 1783977402242,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -rn \"fn cwd\\|\\\"PWD\\\"\\|getcwd\\|current_dir\" self/module…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783977793048,
      "tokens": 94847,
      "toolCalls": 25,
      "durationMs": 390800,
      "resultPreview": "## 결론 (한 줄)\n6건 중 **A(arena rewind)** 와 **C+D(module_loader mangle + rt_str_replace)** 두 개가 전체의 대부분이고, 둘 다 과거 선례(bsearch·label_bkt·pack_lir)와 **같은 클래스**다 — \"블록리스트/문자열을 매번 처음부터 다시 훑는다\". 나머지 3건(B/E/F)은 기존 repo 안에 **이미 정답 코드가 있고 한 곳만 안 고쳐졌다**(reference-match 그대로 복사).\n\n---\n\n## A. `hexa_arena_rewind` — O(B) → **O(1)** (exec wall의 ~36%)\n\n**site**: `self/rt/alloc.hexa:151-168` (네이티브 시드 · perf 38.7%) + 미러 …"
    },
    {
      "type": "workflow_agent",
      "index": 23,
      "label": "design:reuse",
      "phaseIndex": 4,
      "phaseTitle": "Design",
      "agentId": "a451352aa196e4a54",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783977402252,
      "queuedAt": 1783977402242,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /Users/mini/dancinlab/hexa-lang; grep -n \"strict_citatio…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783977735830,
      "tokens": 91593,
      "toolCalls": 25,
      "durationMs": 333578,
      "resultPreview": "## 결론 — 가설 **확인됨**. 누락 사이트는 정확히 1곳이다.\n\n`compiler/main.hexa:726`\n```hexa\nlet atlas = if emit_kind == \"obj\" { empty_atlas_index() } else { _load_atlas() }\n```\natlas-skip 최적화는 **`emit_kind == \"obj\"` 에만** 걸려 있다. 그런데 own-link 는 **`--emit=exec` 를 쓰는 유일한 경로**다.\n\n| 경로 | 호출 사이트 | emit | atlas |\n|---|---|---|---|\n| clang/ld 폴백 (구 default) | `self/main.hexa:3662`, `:4793`, `:3314` | `--emit=obj` | **skip** (…"
    },
    {
      "type": "workflow_agent",
      "index": 24,
      "label": "design:gate",
      "phaseIndex": 4,
      "phaseTitle": "Design",
      "agentId": "accbd2ba042ce6c90",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783977402253,
      "queuedAt": 1783977402242,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /private/tmp/claude-501/-Users-mini-dancinlab-hexa-lang/…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783978757646,
      "tokens": 114034,
      "toolCalls": 41,
      "durationMs": 1355393,
      "resultPreview": "## 산출: 컴파일-시간 예산 게이트 (aiden 실측 · RED 실증 완료)\n\n### 만든 것 (3 파일 · 모두 aiden 에서 실행 검증)\n| 경로 (scratchpad, 그대로 repo 에 커밋 가능) | repo 목적지 |\n|---|---|\n| `/private/tmp/claude-501/-Users-mini-dancinlab-hexa-lang/adbce6b7-54f3-4f01-a7fe-77d3f277b3f0/scratchpad/compile_time_budget_gate` | `tool/compile_time_budget_gate` (chmod +x) |\n| `…/scratchpad/compile_time_budget.tsv` | `tool/compile_time_budget.tsv` |\n| `……"
    },
    {
      "type": "workflow_agent",
      "index": 25,
      "label": "synthesize",
      "phaseIndex": 5,
      "phaseTitle": "Synthesize",
      "agentId": "a08d44faa6b608569",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783978757667,
      "queuedAt": 1783978757666,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "sed -n 4243,4262p compiler/emit/elf_x86_64.hexa; echo ----;…",
      "promptPreview": "# 문제: own-link 경로의 컴파일 시간 벽 (axis-① 의 마지막 미지수)\n\nrepo: /Users/mini/dancinlab/hexa-lang\n\n## 배경 — 이번 세션에 실측된 것\n\naxis-③(own-emit + own-link)은 linux-x86_64 에서 **이미 default-ON** 이다(#4882/#4902).\n그런데 기존 게이트가 컴파일하는 코퍼스는 21줄 이하 · `use` 0개짜리 장난감 5개였다\n(array.hexa 21줄 use=0 · loop.hexa 19줄 use=0). 실제 프로그램은 import-closure(flatten)를 먼저\n타는데, own-link 가 그런 프로그램에서 한 번도 측정된 적이 없었다.\n\n그래서 `HEXA_OWNLINK_STRICT=1`(fail…",
      "lastProgressAt": 1783978982843,
      "tokens": 72907,
      "toolCalls": 3,
      "durationMs": 225176,
      "resultPreview": "# own-link 컴파일-시간 벽 — 종합 판정\n\n## 0. 한 줄\n\n**own-link 이 느린 게 아니다.** 벽은 (a) `--emit=exec` 만 아틀라스를 상주시키는 **한 줄짜리 게이트 누락**(compiler/main.hexa:726) 과 (b) 그 상주가 노출시킨 **프론트엔드 공통의 `hexa_arena_rewind` O(B)** 두 개다. own-link 링크 단계 자체(~33s)는 **프로그램 크기와 무관한 상수**이고, 별개의 3순위 항목이다.\n\n---\n\n## 1. 벽의 정체 — 실측\n\n### 1-a. diff-path 렌즈 결론: **own-link 전용 벽이 아니다. 프론트엔드 공통이다.**\n\n결정적 A/B (aiden, `/tmp/olc` GitHub 새 clone @77b1b…"
    }
  ],
  "totalTokens": 1883004,
  "totalToolCalls": 495
}```
