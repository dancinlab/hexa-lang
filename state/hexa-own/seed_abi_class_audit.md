# 시드 ABI 클래스 전면 감사 — 워크플로우 wf_5c5b82ee-80b

생성 2026-07-14 · 41 에이전트 · census 3병렬 → 발견별 적대적 반증 → realloc 갭 3안 설계 → 통합 판정
확정 발견 33건 (전부 반증관을 통과) · 아래는 워크플로우 반환 원문(JSON)

```json
{
  "summary": "raw-libc pair-ABI 미스컴파일 클래스 전면 감사 + typed-leaf carrier 치환 설계 (hexa_ptr_realloc 갭)",
  "agentCount": 41,
  "logs": [
    "census 확정 발견: 33건"
  ],
  "result": {
    "confirmed_count": 33,
    "confirmed": [
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 53,
        "symbol": "extern fn malloc(n: int) -> int",
        "klass": "A-raw-libc",
        "evidence": "array_typed_leaf.hexa:53 declares raw libc malloc. Emitted seed self/native/array_typed_leaf_x86_64.s:50-54 — `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default = TAG_INT` / `call malloc` / `mov [rbp-160],rax # store tag L4` / `mov r15,rdx # hv: unbox user-call result payload`. arm64 mirror array_typed_leaf_arm64.s:41-44 (`movz x0,#0` tag / `movz x1,#16` payload / `bl _malloc` / `stp x0,x1,[sp,#64]`). malloc is C-ABI: size in rdi/x0, result in rax/x0.",
        "impact": "default-ON (see tool/stage_resolve_runtime_a finding). Every hexa_arr_i64_new() call: C malloc reads rdi=0 (the TAG) → malloc(0); the returned pointer in rax is stored into the TAG slot and DISCARDED; the seed takes the payload from rdx = caller-saved garbage. `a` is garbage → the immediately following __hx_ptr_store64(a,0,d) writes through it → SIGSEGV (identical mechanism to the measured array_new/calloc SIGSEGV that forced the #4932 revert).",
        "verdict": {
          "refuted": false,
          "reason": "반증 실패 — 주장이 모든 축에서 확인됨. (1) array_typed_leaf.hexa:53 은 실제 코드 `extern fn malloc(n: int) -> int` (주석 아님, 로컬 정의 아님; :54-56 에 realloc/write/exit 동반). (2) self/runtime.h 에는 hexa_ptr_alloc(:872)/hexa_ptr_free(:873)/hexa_exit(:846) 만 HexaVal-ABI carrier로 존재하고 `HexaVal malloc` 심(shim)은 runtime.h/runtime_core.c 어디에도 없음 → U 심볼 malloc 은 C-ABI libc 바디에 바인딩. hexa_bool 같은 백엔드 헬퍼도 아님. (3) 방출된 시드가 클래스-A 미스매치를 문자 그대로 보여줌: self/native/array_typed_leaf_x86_64.s `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default = TAG_INT` / `call malloc` / `mov [rbp-160],rax # store tag L4` / `mov r15,rdx # hv: unbox user-call result payload` → C malloc 은 rdi=0(태그)을 size 로 읽어 malloc(0), 진짜 포인터 rax 는 TAG 슬롯에 저장돼 버려지고 payload 는 죽은 rdx 쓰레기. arm64 미러(array_typed_leaf_arm64.s: movz x0,#0 / movz x1,#16 / bl _malloc / stp x0,x1)도 동형. 뒤따르는 __hx_ptr_store64(a,0,d) 가 쓰레기 포인터로 write → SIGSEGV (#4932 revert 를 부른 array_new/calloc 과 동일 메커니즘). (4) default-OFF 반증도 실패: tool/stage_resolve_runtime_a:574-576 의 헤더 주석은 \"Default-OFF byte-neutral (opt-in)\"라고 쓰여 있지만 실제 게이트는 명시적 opt-out 뿐 — `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0`. 미설정이면 x≠0 이라 통과해 3타깃 시드(:615-617)를 어셈블하고 HEXA_RT_ARRAY_I64_LEAF_NATIVE=1 을 export(:629) → runtime_core.c 가 4개 C 바디를 extern 처리. 함수는 :4502 에서 무조건 호출됨. 주석이 stale 이고 코드는 default-ON.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 65,
        "symbol": "malloc(16)  // header alloc in hexa_arr_i64_new",
        "klass": "A-raw-libc",
        "evidence": "array_typed_leaf.hexa:65 `let a = malloc(16)` → array_typed_leaf_x86_64.s:50-54. Args go out as the pair (rdi=tag=0, rsi=payload=16); result payload read from rdx.",
        "impact": "`a` (the HexaArrI64*) = rdx garbage. The 3 stores at .hexa:68-70 (data@0, len@8, cap@12) all write through it. Crash / heap corruption on the very first typed-i64 array construction.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — the finding survives all four refutation avenues. (1) array_typed_leaf.hexa:65 `let a = malloc(16)` is real code, not a comment. (2) `malloc` is declared at :53 as `extern fn malloc(n: int) -> int` — a raw libc extern; self/runtime.h contains ONLY hexa_ptr_alloc(:872)/hexa_ptr_free(:873)/hexa_exit(:846)/hexa_bool(:113), there is no HexaVal-ABI malloc carrier and no malloc shim in self/*.c|*.h, so the U binds to the C-ABI libc body. (3) Not a local symbol — the emitted seed self/native/array_typed_leaf_x86_64.s:50-54 shows the exact pair-ABI lowering the claim predicts: `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default=TAG_INT` / `call malloc` / `mov [rbp-160],rax # store tag` / `mov r15,rdx # hv: unbox user-call result payload`. libc reads rdi=0 as its size argument and returns the pointer in rax (stored as a TAG), while the payload is read from dead rdx → `a` = rdx garbage, and the 3 stores at .hexa:68-70 (data@0, len@8, cap@12) all write through it. A second identical site at .s:73 (malloc(bytes) data buffer), plus `call realloc` @:241 and `call write` @:350 in push. (4) NOT default-OFF: tool/stage_resolve_runtime_a:575 is opt-OUT only (`[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0`) and resolve_native_array_typed_leaf_seed is called unconditionally at :4502, with seeds present for all 3 targets — so this is live on the deployed path. Same class-A signature that broke PR #4930 (array_new/arr_zeros_leaf). Incidental note: `exit` actually lowers to `call hexa_exit` (.s:356, the carrier), but malloc/realloc/write do not. Caveat: this is a static audit (source + emitted asm + link resolution + gating), not a runtime repro — I did not execute the seed .o against a pair-ABI driver.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 67,
        "symbol": "malloc(bytes)  // data buffer alloc",
        "klass": "A-raw-libc",
        "evidence": "array_typed_leaf.hexa:67 `let d = malloc(bytes)` → array_typed_leaf_x86_64.s:70-75: `mov rsi,[rbp-72] # hv arg payload` / `mov rdi,[rbp-184] # tag L7 from tag-slot` / `call malloc` / `mov [rbp-192],rax` / `mov r10,rdx`. arm64: array_typed_leaf_arm64.s:56 `bl _malloc` after `ldp x0,x1,[sp,#112]` (x0=tag, x1=size).",
        "impact": "C malloc receives the TAG (0) as its size → malloc(0); the real buffer pointer (rax/x0) is dropped; `d` = rdx/x1 garbage. Stored as HexaArrI64.data → every later element store/load dereferences garbage.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. All four refutation avenues were tested against the real files and every one failed. (1) NOT a carrier: array_typed_leaf.hexa:53 declares `extern fn malloc(n: int) -> int` = raw libc; self/runtime.h contains NO malloc carrier — the only HexaVal-ABI carriers are hexa_ptr_alloc (runtime.h:872), hexa_ptr_free (:873), hexa_exit (:846), plus the backend's own hexa_bool (:113). malloc is not one of them, and hexa_ptr_realloc does not exist at all (the stated gap holds). (2) NOT default-OFF: tool/stage_resolve_runtime_a:575 reads `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` — the `:-x` default is never \"0\", so the guard is an EXPLICIT OPT-OUT and the path is default-ON; lines 614-630 select the per-target .s, assemble it to build/array_typed_leaf_native.o and `export HEXA_RT_ARRAY_I64_LEAF_NATIVE=1`, so the seed ships in runtime.a. (3) NOT a comment: line 67 is executable code `let d = malloc(bytes)`. (4) NOT a local symbol: it is extern and the seeds emit it as an undefined call. Positive confirmation from the emitted seeds: array_typed_leaf_x86_64.s emits `mov rsi,[rbp-72] # hv arg payload` / `mov rdi,[rbp-184] # tag L7 from tag-slot` / `call malloc` / `mov [rbp-192],rax # store tag L8` / `mov r10,rdx # hv: unbox user-call result payload` — C-ABI malloc(size_t) reads rdi, so it receives the TAG (0) => malloc(0); the real pointer in rax is filed into the tag slot and dropped, and `d` takes rdx (dead caller-clobbered garbage). array_typed_leaf_arm64.s:56 is the same defect under AAPCS64: `ldp x0,x1,[sp,#112]` (x0=tag=0, x1=size) then `bl _malloc` => malloc(0); the returned x0 is stored as the tag half and x1 (caller-saved garbage) becomes the payload. That garbage is written straight to HexaArrI64.data at line 68 (`__hx_ptr_store64(a, 0, d)`), so every later element store/load in push/box dereferences it — exactly the class-A shape that broke PR #4930 on all 3 targets. Aggravating detail: the .s-seed branch (stage_resolve_runtime_a:614-634) has NO ALLOWED_U gate — it only counts the 4 exported globals (line 621), whereas the own-obj branch (600-608) does check the U-floor via `_bad`. So the raw-libc malloc/realloc/write/exit U-syms enter the shipped archive unchecked on the default path.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 54,
        "symbol": "extern fn realloc(p: int, n: int) -> int",
        "klass": "A-raw-libc",
        "evidence": "array_typed_leaf.hexa:54 declares raw libc realloc; runtime.h has NO hexa_ptr_realloc carrier (grep over self/ compiler/ stdlib/ = 0 hits; only hexa_ptr_alloc @self/runtime.h:872 and hexa_ptr_free @873 exist).",
        "impact": "There is no HexaVal-ABI replacement available — the #4935 carrier-swap recipe does NOT cover this file as-is. A new carrier (e.g. hexa_ptr_realloc(HexaVal ptr, HexaVal newsize) -> HexaVal) must be added to runtime.c/runtime.h first, or push() must be rewritten as ptr_alloc + copy + ptr_free (which also needs a copy carrier that does not exist).",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED. stdlib/runtime/array_typed_leaf.hexa:54 is real code (`extern fn realloc(p: int, n: int) -> int`), not a comment, and is actually called at :84 inside hexa_arr_i64_push (`let nd = realloc(old, nbytes)`). It is not a local symbol — the file header (L15-16) explicitly says it calls libc realloc as a C-ABI extern resolved at the final link. No HexaVal-ABI carrier exists: grep for `hexa_ptr_realloc` over self/ compiler/ stdlib/ tool/ = 0 hits; only hexa_ptr_alloc (self/runtime.h:872) and hexa_ptr_free (self/runtime.h:873). The frozen seed proves the class-A miscompile directly — self/native/array_typed_leaf_x86_64.s:235-243 sets up the call in pair-ABI (rdi=TAG, rsi=payload, rdx=TAG, rcx=payload), then `call realloc`, then `mov r10, rdx  # hv: unbox user-call result payload` — i.e. C-ABI realloc would read rdi (a TAG) as the pointer and return in rax, while the seed reads the dead rdx as the result payload. arm64 mirrors it (array_typed_leaf_arm64.s:173 `bl _realloc`). It is also on the deployed path: tool/stage_resolve_runtime_a:4502 calls resolve_native_array_typed_leaf_seed unconditionally and its only guard (:575) is an explicit opt-OUT (=0); unset falls through, assembles the seed, exports HEXA_RT_ARRAY_I64_LEAF_NATIVE=1, and :1874 ars it into runtime.a with -DHEXA_RT_CORE_ARRAY_I64_LEAF_NATIVE=1 (the \"(default-OFF)\" comments at :573/:4501 contradict the code). Only nit: the finding's impact text claims a ptr_alloc+copy+ptr_free rewrite \"also needs a copy carrier that does not exist\" — the file already has __hx_ptr_load64/__hx_ptr_store64 leaf intrinsics, so a manual copy loop is expressible without a new copy carrier. That does not refute the finding; the gap (no hexa_ptr_realloc; #4935 recipe does not cover this file as-is) is real.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 84,
        "symbol": "realloc(old, nbytes)  // THE realloc site — exactly 1 per file",
        "klass": "A-raw-libc",
        "evidence": "array_typed_leaf.hexa:84 `let nd = realloc(old, nbytes)` inside the len>=cap grow branch. Emitted array_typed_leaf_x86_64.s:236-243 — `mov rsi,[rbp-120] # hv arg payload` (=old data ptr) / `mov rdi,[rbp-392] # tag L13` (=0) / `mov rcx,[rbp-136] # hv arg payload` (=nbytes) / `mov rdx,[rbp-408] # tag L15` (=0) / `call realloc` / `mov [rbp-416],rax # store tag L16` / `mov r10,rdx # unbox user-call result payload`. arm64: array_typed_leaf_arm64.s:173 `bl _realloc` with the same (tag,payload)x2 quadruple in x0..x3.",
        "impact": "C realloc(void* p, size_t n) reads rdi=0 and rsi=old-pointer-value → it executes realloc(NULL, <the old heap address as a byte count>) = a multi-terabyte malloc → returns NULL (or, on a 47-bit address, an insane request). The genuine result in rax is stored to a TAG slot and dropped; `nd` = rdx garbage. The OOM guard at .hexa:85 (`__hx_payload_eq(nd,0)`) therefore tests garbage — it either falsely fires (→ the broken write/exit path below) or falsely passes, after which .hexa:90 installs the garbage `nd` as HexaArrI64.data and .hexa:95 stores x through it. Both arms are wrong; the second silently corrupts the heap. Result usage: nd → store64(a,0,nd) (new data) and cap<-ncap.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf.hexa:84 `let nd = realloc(old, nbytes)` is real code inside the len>=cap grow branch (verified by line-numbered read), not a comment. (2) The symbol is declared at the same file's line 54 as `extern fn realloc(p: int, n: int) -> int` — a raw libc extern, NOT a HexaVal-ABI carrier, NOT a backend helper like hexa_bool, and NOT locally defined. The file's own header (L14-18) states the intent explicitly: \"The seed does NOT re-implement realloc natively — it CALLS libc realloc as an extern C-ABI symbol.\" (3) self/runtime.h has only hexa_ptr_alloc (L872) and hexa_ptr_free (L873) — there is NO hexa_ptr_realloc carrier, so the gap named in the brief is real. (4) The emitted seed confirms the pair-ABI/C-ABI mismatch verbatim: self/native/array_typed_leaf_x86_64.s ~L236-243 emits `mov rsi,[rbp-120] # hv arg payload` (old ptr) / `mov rdi,[rbp-392] # tag L13` (=0) / `mov rcx,[rbp-136] # hv arg payload` (nbytes) / `mov rdx,[rbp-408] # tag L15` / `call realloc` / `mov [rbp-416],rax # store tag L16` / `mov r10,rdx # unbox user-call result payload`. C realloc therefore reads rdi=NULL as ptr and rsi=the old heap address as the size → realloc(NULL, <address as byte count>); the genuine rax is parked in a TAG slot and immediately discarded (the next tag use is `mov r11,0 # tag L16 = TAG_INT (i64-local, fused)`), and nd is taken from dead rdx. arm64 (array_typed_leaf_arm64.s:173) mirrors it: `bl _realloc` with ldp x0,x1 (L13 tag/payload) + ldp x2,x3 (L15 tag/payload). (5) The default-OFF escape does not apply: tool/stage_resolve_runtime_a:4502 calls resolve_native_array_typed_leaf_seed unconditionally; that function (L575) returns early ONLY on an explicit `=0` opt-out and otherwise assembles the arch seed and `export HEXA_RT_ARRAY_I64_LEAF_NATIVE=1` (L629), which the consumption block (L1874) uses to extern the 4 i64 bodies out of runtime_core.c — so the defective seed is on the shipped path. The inline \"(default-OFF)\" comments are stale relative to the code. Ancillary same-class externs in the same file (outside the stated claim): malloc @L66/L68, write @L87, exit @L88 (exit has an available carrier hexa_exit at runtime.h:846). __hx_payload_eq's `bl _hexa_bool` on arm64 is the sanctioned backend helper, not a class-B boxed slow-call.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 55,
        "symbol": "extern fn write(fd,buf,n) + write(2, msg, 20) @:87",
        "klass": "A-raw-libc",
        "evidence": "array_typed_leaf.hexa:55/87. Emitted array_typed_leaf_x86_64.s:344-352 — `mov rsi,2 # hv arg payload` / `mov rdi,0 # tag` / `mov rcx,[rbp-184] # hv arg payload` (=msg ptr) / `mov rdx,[rbp-456] # tag L21` / `mov r9,20 # hv arg payload` / `mov r8,0 # tag` / `call write`. C write(int fd, const void* buf, size_t n) reads rdi/rsi/rdx → it actually executes write(fd=0, buf=(void*)2, n=0).",
        "impact": "The OOM diagnostic is a silent no-op (writes 0 bytes to fd 0, message never printed). Not a crash, but it means the OOM path is undiagnosable — and it is exactly the path the broken realloc guard can falsely take. Same site in the f64 twin.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — every refutation avenue fails. (1) Real code, not comments: array_typed_leaf.hexa:55 = `extern fn write(fd: int, buf: int, n: int) -> int`, :87 = `write(2, msg, 20)` inside the realloc==0 OOM guard. (2) Not a carrier / not a backend helper: self/runtime.h declares NO `hexa_write` HexaVal-ABI carrier — `write` appears only as `#include <unistd.h>` (:37) and a static C wrapper (:2324) that calls raw write(2). The seed's declared prototype is the raw C write(2) signature. Contrast hexa_exit (runtime.h:846) / hexa_ptr_alloc (:872) / hexa_ptr_free (:873), which ARE carriers — and notably aprime already lowers the seed's `exit(1)` to `call hexa_exit`, but does NOT intercept `write`. (3) Not a local symbol: it emits as U / `call write`; tool/stage_resolve_runtime_a:602 explicitly whitelists raw libc `write` in the U-floor keeplist {malloc,realloc,write,hexa_exit,hexa_int,hexa_throw}. (4) The pair-ABI/C-ABI mismatch is exactly as alleged — array_typed_leaf_x86_64.s:343-350 emits rdi=0(tag)/rsi=2(payload fd), rdx=tag L21(=0, traced from `mov r11,0` @335 -> [rbp-448] -> [rbp-456])/rcx=msg ptr, r8=0/r9=20, then `call write`. C write reads rdi/rsi/rdx, so it actually executes write(fd=0, buf=(void*)2, n=0); the msg pointer (rcx) and length (r9) are discarded. arm64 mirrors it (array_typed_leaf_arm64.s:246-251: x0=0,x1=2,x2/x3=L21,x4=0,x5=20, `bl _write`). (5) On the deployed path: resolve_native_array_typed_leaf_seed is called unconditionally at stage_resolve_runtime_a:4502 and is opt-OUT only (`[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0`), assembling the .s seed and exporting =1 by default — the \"default-OFF\" comments at :572/:4501 are stale. (6) The f64 twin carries the identical extern (array_typed_leaf_f64.hexa:32) and call site. Impact as stated is accurate and correctly modest: not a crash, but the OOM diagnostic is a silent no-op (0 bytes to fd 0) while exit(1) still fires via the real hexa_exit carrier — leaving the exact path the broken pair-ABI realloc return can falsely enter undiagnosable.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 57,
        "symbol": "extern fn hexa_int(x: int) -> HexaVal  → hexa_int(x) @:118",
        "klass": "A-raw-libc",
        "evidence": "self/runtime.h:112 `HexaVal hexa_int(int64_t n);` = C-ABI scalar param (n in rdi/x0). Emitted array_typed_leaf_x86_64.s:557-562 — `mov rsi,[rbp-152] # hv arg payload` (=the loaded element x) / `mov rdi,[rbp-304] # tag L17` (=0, TAG_INT) / `call hexa_int` / `mov [rbp-312],rax # store tag L18` / `mov r10,rdx # unbox payload`. arm64: array_typed_leaf_arm64.s:402 `bl _hexa_int`.",
        "evidence_note": "return side IS correct (HexaVal is a 16B two-eightbyte struct → rax=tag, rdx=payload — matches what the seed reads); only the ARG is misrouted.",
        "impact": "NOT a crash — a SILENT WRONG VALUE. C hexa_int reads rdi = the TAG (constant 0), never the element. hexa_arr_i64_box(v,i) therefore returns hexa_int(0) = INT 0 for EVERY element, regardless of the data. Worse: `hexa_int` is on the own-obj U-floor allowlist (tool/stage_resolve_runtime_a:601), so the nm gate blesses it. Fix = replace with the leaf __hx_make_val(0, x) (the f64 twin already does the leaf thing via __hx_bits_f64 and is correct).",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf.hexa:57 is real code — `extern fn hexa_int(x: int) -> HexaVal` in the raw-libc carrier block; used at :118 `return hexa_int(x)` (tail of hexa_arr_i64_box). (2) The symbol is genuinely C-ABI scalar, not a HexaVal-ABI carrier and not a codegen-controlled helper like hexa_bool: self/runtime.h:112 `HexaVal hexa_int(int64_t n);` and the body self/runtime_core.c:1599 `HexaVal hexa_int(int64_t n) { return HX_MAKE_INT(n); }` read n from rdi/x0. It is externally referenced (U), not locally defined. (3) The emitted seeds prove the pair-ABI mismatch: array_typed_leaf_x86_64.s:557-562 sets `rsi = payload (the loaded element x)` and `rdi = tag (materialized imm 0, TAG_INT)` before `call hexa_int`, i.e. the C body reads rdi = 0 and returns HX_MAKE_INT(0); the identical lowering appears for malloc at :50-52. arm64: array_typed_leaf_arm64.s:402 `ldp x0,x1,[sp,#272]; bl _hexa_int` (x0 = tag = 0). The 16-byte HexaVal return (rax:rdx / x0:x1) coincidentally matches pair-ABI, so it does NOT crash — it is a SILENT WRONG VALUE: hexa_arr_i64_box returns INT 0 for every element. (4) It is on the shipped default path: tool/stage_resolve_runtime_a:574-576 only returns early on an explicit opt-out (HEXA_RT_ARRAY_I64_LEAF_NATIVE=0), otherwise assembles the arch seed and exports =1; it is called unconditionally at :4502; commit 6e90e7eb8 \"flip(axis-② #3 PR-2): array i64 typed-leaf native DEFAULT-ON — 3-target .s seeds (#4918)\" confirms the flip (the \"(default-OFF)\" comments at :572/:4501 are stale). (5) The U-floor gate allowlist at :601 does contain `hexa_int`, so the nm gate blesses it, as claimed. Corroboration: :641 documents the f64 twin's floor as \"(NO hexa_int — box re-boxes via ...)\" — the f64 sibling already uses the leaf. No refutation ground (comment / local symbol / carrier / default-OFF) survives.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 58,
        "symbol": "extern fn hexa_throw(v: HexaVal) → hexa_throw(\"index out of bounds\") @:113,:114",
        "klass": "OK-carrier",
        "evidence": "self/runtime.h:847 `void hexa_throw(HexaVal err);` — HexaVal param = the {tag,payload} register pair, which is exactly what the pair-ABI call site emits (array_typed_leaf_x86_64.s:492, :514; arm64 .s:355, :372). Argument is a TAG_STR HexaVal literal, no scalar crossing.",
        "impact": "None — correct by construction. HexaVal-in / void-out carriers are the only extern shape these seeds get right today.",
        "verdict": {
          "refuted": false,
          "reason": "Finding CONFIRMED (it is a correct OK-carrier classification; I could not refute it). All four cited anchors verify exactly: (1) stdlib/runtime/array_typed_leaf.hexa:58 is real code `extern fn hexa_throw(v: HexaVal)` (not a comment — the comments are at :108/:109), and :113/:114 are real call sites `hexa_throw(\"index out of bounds\")`. (2) self/runtime.h:847 is `void hexa_throw(HexaVal err);` with the body at self/runtime_core.c:7784 `void hexa_throw(HexaVal err) {` — a HexaVal-ABI carrier, NOT raw libc (class A) and NOT a boxed slow-call (class B). (3) The one way this could have been wrong is SysV eightbyte classification: HexaVal = {HexaTag tag; union {int64_t i; double f; char* s; ...}} = 16B, and the union DOES contain a `double`. But the SysV merger rule (§3.2.3) makes INTEGER win over SSE, so eightbyte-1 classifies INTEGER, not xmm0 → the struct is passed in (rdi=tag, rsi=payload), which is precisely the pair-ABI register pair. No SSE/INTEGER crossing, no scalar crossing. (4) The emitted seeds match: array_typed_leaf_x86_64.s:492/:514 emit `mov rdi, 3` (TAG_STR=3 per runtime.h:42) + `lea rsi, [rip+.LCstr1]` before `call hexa_throw`; arm64 .s:355/:372 emit `x0=3, x1=&str` before `bl _hexa_throw` — correct AAPCS for a 16-byte non-HFA composite. Return is void, so the dead `mov [rbp-232], rax` / `mov r10, rdx` after the x86 call write into spill slot L8 which no successor block reads — harmless garbage. Adjacent (out of scope): the same extern block at :52-:56 does carry raw libc malloc/realloc/write/exit = genuine class A; hexa_throw is the clean member of that block.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 64,
        "symbol": "__hx_payload_lt / _ge / _eq / _mul / _add (leaf intrinsics) + codegen-injected hexa_bool",
        "klass": "OK-carrier",
        "evidence": "Full call census of all 6 emitted seeds: ZERO hexa_eq / hexa_mul / hexa_add_slow / hexa_cmp_ge. The only extra symbol is hexa_bool, and it is INJECTED BY CODEGEN, arm64-only, to box the leaf compare: array_typed_leaf_arm64.s:28-30 `cmp x1,x3 ; __hx_payload_lt: cmp payloads` / `cset x0,lt` / `bl _hexa_bool`. x86 inlines it (no call at all). self/runtime_core.c:1831 `HexaVal hexa_bool(int b) { return HX_MAKE_BOOL(b); }` — codegen puts the raw int in x0, C reads x0 → ABI-correct; return x0=tag/x1=payload matches the `stp x0,x1` that follows.",
        "impact": "CLASS B = CLEAN in both files. Every offset/compare goes through a __hx_payload_* leaf; no plain `int` operator survives. hexa_bool is the sanctioned backend helper (harmless). NOTE the target divergence it proves though: hexa_bool is a real `bl` on arm64 and nothing on x86 — the exact asymmetry that lets an x86-only U-symbol check show clean while arm64 links differently.",
        "verdict": {
          "refuted": false,
          "reason": "Claim VERIFIED, not refuted. (1) stdlib/runtime/array_typed_leaf.hexa:64 is real code inside pub fn hexa_arr_i64_new (opens :62): `if __hx_payload_lt(c, 1) { c = 1 }` — not a comment. (2) hexa_bool is a genuine backend helper with a raw-int C ABI it controls itself: self/runtime.h:113 `HexaVal hexa_bool(int b);`, body self/runtime_core.c:1831 `HexaVal hexa_bool(int b) { return HX_MAKE_BOOL(b); }` (the finding's :1831 is right; the background note's :2016 is stale). It is on the sanctioned ALLOWED_U list. (3) Full call census of all six emitted seeds (self/native/array_typed_leaf{,_f64}_{x86_64,arm64,arm64-linux}.s) shows targets = malloc, realloc, write, hexa_exit, hexa_int, hexa_throw (+ hexa_bool on the two arm64 variants only). ZERO hexa_eq / hexa_mul / hexa_add_slow / hexa_cmp_ge anywhere — no boxed slow-call survives. (4) The asserted target divergence is literally in the seed text: array_typed_leaf_arm64.s:26-29 `cmp x1, x3 ; __hx_payload_lt: cmp payloads` / `cset x0, lt` / `bl _hexa_bool ; __hx_payload_lt: box bool`, while array_typed_leaf_x86_64.s:36-38 inlines it (`cmp r10, r11` / `setl al` / `movzx r13, al`) with no call — so an x86-only U-symbol check would indeed miss the arm64 hexa_bool U. Every offset/compare uses a __hx_payload_* leaf; CLASS B is clean in both files. (The raw malloc/realloc/write/exit externs in the same file remain the separate, still-live CLASS A defect — but that is outside this finding's claim.)",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf_f64.hexa",
        "line": 30,
        "symbol": "extern fn malloc → malloc(16) @:41, malloc(bytes) @:43",
        "klass": "A-raw-libc",
        "evidence": "Byte-identical emission to the i64 twin: self/native/array_typed_leaf_f64_x86_64.s:50-54 (`mov rsi,16` / `mov rdi,0` / `call malloc` / `mov [rbp-160],rax` / `mov r15,rdx`) and :71-75 for the data buffer. arm64: array_typed_leaf_f64_arm64.s:43, :56 (`movz x0,#0` tag / `bl _malloc` / `stp x0,x1`).",
        "impact": "default-ON. hexa_arr_f64_new() → malloc(0) with the tag as the size, real pointer dropped from rax/x0, header pointer = rdx/x1 garbage → SIGSEGV on the first store (or a silently bogus len=0 array on darwin-arm64, exactly as measured for arr_zeros_leaf).",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED as class A-raw-libc. All four refutation avenues fail. (1) Real code, not comments: array_typed_leaf_f64.hexa:30 = `extern fn malloc(n: int) -> int`, :41 = `let a = malloc(16)`, :43 = `let d = malloc(bytes)`. (2) Not a carrier / not a backend helper: self/runtime.h declares ONLY hexa_bool(:113), hexa_exit(:846), hexa_ptr_alloc(:872), hexa_ptr_free(:873) — there is no malloc carrier, and no hexa_ptr_realloc at all (the gap named in the brief is real). (3) Not a local symbol: the seed .s emits only `.globl hexa_arr_f64_{new,push_bits,len,box}`; malloc is undefined and binds to the libc C-ABI body at link. (4) NOT default-OFF — this was the strongest refutation avenue and it failed. tool/stage_resolve_runtime_a:1885 reads ${HEXA_RT_ARRAY_F64_LEAF_NATIVE:-0} under a comment saying \"Default-OFF byte-neutral\", which superficially reads as a refutation, but that is a CONSUMPTION read, not the default: the resolver resolve_native_array_f64_leaf_seed at :764-765 runs first — `[ \"${HEXA_RT_ARRAY_F64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` (explicit opt-out ONLY; unset yields \"x\" != \"0\" so it does not return) then `export HEXA_RT_ARRAY_F64_LEAF_NATIVE=1` when the seed .o exists and OWNOBJ is off. So unset => exported 1 => the seed .o is ar'd into runtime.a with -DHEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE=1. CHANGELOG.jsonl #4922 independently states \"own-native default-ON ... DEPLOYED\". The stale \"Default-OFF\" comment is exactly the `:-0` vs `:-auto` trap from #4591.\n\nEmission matches the cited evidence byte-for-byte. x86_64 hexa_arr_f64_new: `mov rsi,16` (payload) / `mov rdi,0` (tag default = TAG_INT) / `call malloc` / `mov [rbp-160],rax` (real pointer stored into the TAG slot) / `mov r15,rdx` (payload read from rdx = dead garbage); the data-buffer call repeats it (`mov rdi,[rbp-184]` tag / `call malloc` / `mov r10,rdx`). arm64 mirrors it: `movz x0,#0` (tag) / `movz x1,#16` / `bl _malloc` / `stp x0,x1` — payload = x1 garbage. C-ABI libc reads rdi/x0 as the size argument, so both mallocs receive size 0 and both real pointers (rax/x0) are discarded.\n\nImpact stands, with one precision: malloc(0) returns a valid minimal pointer rather than faulting immediately, so the SIGSEGV arises from the header pointer being rdx/x1 garbage at the first __hx_ptr_store64 (or a silently bogus array on darwin-arm64) — exactly the measured arr_zeros_leaf behavior.\n\nCorroboration that the existing gate is blind to this: tool/regen_array_typed_leaf_f64_native_s.sh:43 sets ALLOWED_U=\"malloc realloc write hexa_exit hexa_throw\" — the U-floor guard explicitly whitelists raw libc, i.e. it was authored to permit the very class-A signature it should reject. Note realloc (:31, called at :61) and write/exit (:32-33) are the same defect class in the same file, and fixing realloc is blocked on the missing hexa_ptr_realloc carrier.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf_f64.hexa",
        "line": 61,
        "symbol": "realloc(old, nbytes)  // the f64 realloc site — 1 site, in push_bits grow branch",
        "klass": "A-raw-libc",
        "evidence": "array_typed_leaf_f64.hexa:31 (extern) + :61 (`let nd = realloc(old, nbytes)`). Emitted array_typed_leaf_f64_x86_64.s:236-243 — identical register layout to the i64 seed: rdi=tag(0), rsi=old data ptr, rdx=tag(0), rcx=nbytes; result taken from rdx. arm64: array_typed_leaf_f64_arm64.s:173 `bl _realloc`.",
        "impact": "C realloc(p=0, n=<old heap address>) → NULL/absurd request; the true rax result is discarded into the tag slot; `nd` = garbage. .hexa:62 NULL guard tests garbage; .hexa:67 installs the garbage as HexaArrF64.data; .hexa:72 stores the double's bits through it. Same crash/corruption as i64. ★ Same missing-carrier gap: hexa_ptr_realloc does not exist.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — all four refutation avenues fail.\n\n(1) NOT a carrier: array_typed_leaf_f64.hexa:31 is `extern fn realloc(p: int, n: int) -> int` = raw libc. self/runtime.h declares ONLY hexa_ptr_alloc (:872), hexa_ptr_free (:873), hexa_exit (:846). `hexa_ptr_realloc` does not exist anywhere in self/, compiler/, stdlib/, or tool/ — the missing-carrier gap is real, exactly as alleged.\n\n(2) NOT a comment: :61 `let nd = realloc(old, nbytes)` is live code inside the push_bits grow branch (guarded by `if __hx_payload_ge(len, cap)`), feeding :62 NULL guard -> :67 `__hx_ptr_store64(a, 0, nd)` (installs into HexaArrF64.data) -> :72 stores the double's bits through it.\n\n(3) NOT a local symbol: the file's only fn defs are the four hexa_arr_f64_{new,push_bits,len,box} dispatchers. Emit is `call realloc` (x86, external) / `bl _realloc` (arm64:173).\n\n(4) NOT default-OFF — and this makes it WORSE than claimed. tool/stage_resolve_runtime_a:642 comment says \"Default-OFF\" but the code at :764 is opt-out-only: `[ \"${HEXA_RT_ARRAY_F64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0`. Unset expands to \"x\" != \"0\", so it falls through, assembles the arch seed, and exports HEXA_RT_ARRAY_F64_LEAF_NATIVE=1. Invoked unconditionally at :4504; seeds exist for all 3 targets. The comment is STALE — the seed is DEFAULT-ON and on the shipped path.\n\nEmitted asm independently confirms the class-A pair-ABI/C-ABI mismatch (array_typed_leaf_f64_x86_64.s:236-243): rdi=tag(0), rsi=old data ptr, rdx=tag(0), rcx=nbytes, `call realloc`, then `mov [rbp-416], rax` (the TRUE C return discarded into the tag slot) and `mov r10, rdx  # hv: unbox user-call result payload` (nd read from the never-written rdx). C realloc therefore sees realloc(NULL, <old heap address>) and its result is thrown away. Identical to the i64 seed that broke PR #4930 on all 3 targets.\n\nAggravating: the resolver's own U-floor check whitelists \" malloc realloc write hexa_exit hexa_throw \" as allowed undefs, so the existing ALLOWED_U gate SANCTIONS this raw-libc floor instead of catching it.\n\nFix requires minting a new hexa_ptr_realloc(HexaVal ptr, HexaVal size) -> HexaVal carrier in self/runtime.h (does not currently exist), unlike array_new/arr_zeros_leaf which could reuse the existing alloc/free carriers.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 57,
        "symbol": "hexa_int",
        "klass": "A-raw-libc",
        "evidence": "★ NEW — not in the known list. `extern fn hexa_int(x: int) -> HexaVal` (array_typed_leaf.hexa:57) is declared as a plain extern, so aprime lowers the call at the generic pair-ABI. But the callee is a C-ABI SCALAR fn: self/runtime_core.c:1599 `HexaVal hexa_int(int64_t n) { return HX_MAKE_INT(n); }` — n is read from a SINGLE reg (rdi/x0). Emitted proof, both targets: array_typed_leaf_x86_64.s:557-562 `mov rsi,[rbp-152] # hv arg payload` / `mov rdi,[rbp-304] # tag L17 from tag-slot` / `call hexa_int`; array_typed_leaf_arm64.s:401-402 `ldp x0,x1,[sp,#272]` / `bl _hexa_int`. So hexa_int receives rdi/x0 = the TAG SLOT (= TAG_INT = 0), and the actual element value sitting in rsi/x1 is discarded. This is the identical mechanism as calloc/malloc — a C-ABI body invoked through the pair-ABI — only the callee is a C-runtime scalar fn rather than libc. DISCRIMINATOR that proves it: in the CORE seeds hexa_int appears ONLY on arm64 and is backend-emitted with a CONTROLLED C-ABI (runtime_hi_native.s:26-28 `bl _hexa_len` / `sxtw x0,w0` / `bl _hexa_int ; ret int: box → HexaVal` — x0 = the VALUE, correct). In array_typed_leaf hexa_int appears on BOTH x86 AND arm64, because it came from a source-level `extern fn`, not the backend ret-box table. Presence of hexa_int in an x86_64 seed's U-list is the signature of this bug.",
        "impact": "DEFAULT-ON (see the stage_resolve_runtime_a finding). `hexa_arr_i64_box` — the accessor that boxes a packed-i64 element into a HexaVal — returns HexaVal{TAG_INT, 0} for EVERY element, on all 3 targets. Does NOT crash: silently wrong answer (every packed-i64 array read yields 0). The f64 twin escapes this one (it has no `extern fn hexa_int`; its ALLOWED_U lacks it) — which is why only the i64 seed carries it. FIX shape = same as the #4935 carrier substitution: a HexaVal-ABI box carrier (or use the __hx_make_val leaf, as the f64 seed already does).",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — I could not refute it; every listed refutation avenue is closed by direct reading.\n\n(1) NOT a comment: stdlib/runtime/array_typed_leaf.hexa:57 is live code `extern fn hexa_int(x: int) -> HexaVal`, in the extern block alongside malloc/realloc/write/exit/hexa_throw. Sole call site = line 118 `return hexa_int(x)` in hexa_arr_i64_box.\n\n(2) NOT a HexaVal-ABI carrier and NOT a backend-controlled helper: self/runtime.h:112 declares `HexaVal hexa_int(int64_t n);` with the body `HexaVal hexa_int(int64_t n) { return HX_MAKE_INT(n); }` (runtime_core.c, #else arm of the HEXA_RT_SELFEMIT||HEXA_RT_CORE_LEAF_NATIVE guard) — a single scalar int64_t in ONE register (rdi/x0). It has the same shape as hexa_bool, but hexa_bool is sanctioned only because CODEGEN emits that call itself with a controlled C-ABI; here the call originates from a source-level `extern fn`, so it is lowered on the generic pair-ABI.\n\n(3) NOT a local symbol / NOT redefined by a pair-ABI native seed: grep for `hexa_int:` / `.globl _?hexa_int` across self/native/ and self/rt/ returns EMPTY — the C body is the only definition. It is a true U extern in both seeds (both .s headers list it under \"External U-floor\").\n\n(4) NOT default-OFF: tool/stage_resolve_runtime_a:575 — `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` = explicit opt-out only, i.e. DEFAULT-ON.\n\nDECISIVE EMITTED PROOF: self/native/array_typed_leaf_x86_64.s ~551-560 materializes the tag as an immediate and passes it as arg0 — `mov r11, 0 # materialize tag imm 0` … `mov rsi, [rbp-152] # hv arg payload` (the real element value) / `mov rdi, [rbp-304] # tag L17 from tag-slot` (= 0) / `call hexa_int`. The C-ABI body reads n from rdi = 0; the actual value in rsi is discarded. arm64 identical: array_typed_leaf_arm64.s:401-402 `ldp x0, x1, [sp, #272]` / `bl _hexa_int` → x0 = tag = 0. So hexa_arr_i64_box returns HexaVal{TAG_INT, 0} for every element on all 3 targets — no crash, silently wrong (every packed-i64 boxed read yields 0). The f64 twin has no `extern fn hexa_int` and escapes, as claimed.\n\nEXTRA (not in the claim): the U-floor gate does not catch this, it SANCTIONS it — tool/stage_resolve_runtime_a:601 hardcodes the allowlist `\" malloc realloc write hexa_exit hexa_int hexa_throw \"`, so hexa_int passes the own-obj U-floor check by construction.\n\nONLY QUIBBLE (taxonomy, not substance): labeling it \"class A RAW-LIBC\" is a misnomer — hexa_int is a hexa C-runtime boxing primitive, not libc. The claim itself concedes this (\"the callee is a C-runtime scalar fn rather than libc\"). The mechanism (C-ABI scalar body invoked through the pair-ABI; arg0 receives the TAG) is identical to the calloc/malloc failure mode, so the defect stands; only the class label is loose.",
          "confidence": "high"
        }
      },
      {
        "file": "self/runtime_core.c",
        "line": 2530,
        "symbol": "hexa_arr_i64_new / hexa_arr_i64_push / hexa_arr_i64_len / hexa_arr_i64_box",
        "klass": "OTHER",
        "evidence": "★ NEW — the MIRROR of class A (C-ABI caller → pair-ABI callee, i.e. the arrow reversed). Under the seed guard, runtime_core.c:2530-2533 declares the seed's 4 entry points with MIXED C-ABI signatures: `extern HexaVal hexa_arr_i64_new(int cap);` `extern HexaVal hexa_arr_i64_push(HexaVal v, int64_t x);` `extern int hexa_arr_i64_len(HexaVal v);` `extern HexaVal hexa_arr_i64_box(HexaVal v, int64_t i);` — and NO __hxseed_/__asm__/__hxw_ bridge is built. But the seed source (array_typed_leaf.hexa:62/76/102/110 — plain `pub fn`, no C-ABI attribute) emits PURE pair-ABI. Emitted proof: (a) array_typed_leaf_x86_64.s:17-31 `hexa_arr_i64_new:` … `mov [rbp-128],rdi # store tag L0` / `mov rbx,rsi # ingress param payload` → reads cap from rsi, C caller puts it in rdi. (b) :136-150 `hexa_arr_i64_push:` … `mov [rbp-296],rdx # store tag L1` / `mov r12,rcx # ingress param payload` → reads x from rcx; the C decl puts x in rdx. (c) :425-437 `hexa_arr_i64_len` egress `mov rdx,r14 # hv arg payload` / `mov rax,[rbp-80] # tag L3` → returns len in rdx and tag(0) in rax, while `int hexa_arr_i64_len(HexaVal)` reads eax. The CODEGEN callers agree with the C decl, not the seed: compiler/codegen/x86_64_linux.hexa:3195 comment `hexa_arr_i64_push (rdi:rsi=descriptor, rdx=payload word)` and :3217-3222 emit `_x86_hv_payload(arg,…,\"rdx\")` + `tag→rdi` + `mov rsi,dst` + `call hexa_arr_i64_push` — rcx is NEVER set. CONTRAST (the correct precedent): the arena seed hit exactly this and built an explicit bridge — runtime_core.c:1601-1670 `extern HexaVal __hxseed_arena_alloc(HexaVal n) __asm__(\"hexa_arena_alloc\")` + __hxw_ wrappers, documented as F-M6-ROUTEB-ABI-ROOTCAUSE. array_typed_leaf never got that bridge.",
        "impact": "DEFAULT-ON and worse than the seed→libc break: BOTH boundaries of this seed are mis-ABI'd. Live consequences on all 3 targets — hexa_arr_i64_len ALWAYS returns 0 (caller reads eax = the tag); hexa_arr_i64_push stores garbage from an uninitialized rcx for every element of a packed-i64 array literal (codegen's packed path _pack_array_enabled() is DEFAULT-ON, x86_64_linux.hexa:1759-1762 `env(\"HEXA_PACK_ARRAY\") != \"0\"`); hexa_arr_i64_new takes cap from an unset rsi; hexa_arr_i64_box takes the index from an unset rcx (on top of the hexa_int bug). Silent corruption, not a crash. Fix = a __hxseed_/__hxw_ HexaVal bridge exactly as the arena has, OR re-declare the 4 as all-HexaVal, OR mark them C-ABI (Route C _is_cabi).",
        "verdict": {
          "refuted": false,
          "reason": "반증 실패 — 주장의 모든 요소를 코드에서 직접 확인했고, 제시된 네 가지 반증 사유 중 어느 것도 성립하지 않는다.\n\n(1) 주석 아님: runtime_core.c:2530-2533은 실제 코드이며, 진짜 SSOT는 생성기 self/runtime_core_emit.hexa:2764-2768로, 정확히 `extern HexaVal hexa_arr_i64_new(int cap);` / `extern HexaVal hexa_arr_i64_push(HexaVal v, int64_t x);` / `extern int hexa_arr_i64_len(HexaVal v);` / `extern HexaVal hexa_arr_i64_box(HexaVal v, int64_t i);` 를 방출한다. (self/runtime_core.c는 untracked 생성물 — git ls-files = 0.)\n\n(2) HexaVal carrier/백엔드 헬퍼 아님: 스칼라 int/int64_t/int 반환이 섞인 순수 C-ABI 시그니처다. 로컬 심볼도 아님 — extern이며 seed .o가 runtime.a에 ar되어 해소된다.\n\n(3) 시드는 순수 pair-ABI를 방출 — .s에서 직접 확인: hexa_arr_i64_new은 `mov [rbp-128],rdi # store tag L0` / `mov rbx,rsi # ingress param payload` (cap을 rsi에서 읽음; C 호출자는 rdi/edi). hexa_arr_i64_push는 `mov [rbp-296],rdx # store tag L1` / `mov r12,rcx # ingress param payload` (x를 rcx에서 읽음; C decl은 rdx). hexa_arr_i64_len egress는 `mov rdx,r14` / `mov rax,[rbp-80] # tag L3` (len을 rdx로, tag(0)을 rax로 반환; int 반환은 eax를 읽음). 소스 array_typed_leaf.hexa:62/76/102/110은 전부 plain `pub fn` — C-ABI 속성 없음.\n\n(4) 브리지 부재 확인: `__hxseed_arr` / `__hxw_arr` / `__asm__(\"hexa_arr` grep → 0 hit. 대조 선례(아레나 시드의 F-M6-ROUTEB-ABI-ROOTCAUSE 브리지, runtime_core.c ~1601)는 실재하며 정확히 이 실패 모드를 문서화한다.\n\n(5) 호출자는 C-ABI 준수: x86_64_linux.hexa:3195 주석 `(rdi:rsi=descriptor, rdx=payload word)` + :3217-3222이 `_x86_hv_payload(arg,…,\"rdx\")` + tag→rdi + `mov rsi,dst`를 방출 — rcx는 결코 세팅되지 않는다. 런타임 내부 C 호출자도 존재(`hexa_arr_i64_push(v, HX_IS_INT(item) ? HX_INT(item) : 0)`).\n\n(6) default-OFF 반증 시도 실패 — DEFAULT-ON 확정: stage_resolve_runtime_a:4502가 resolve_native_array_typed_leaf_seed를 무조건 호출; :575는 `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` — opt-OUT 전용이라 unset이면 진행하여 시드를 어셈블하고 =1을 export; :1875가 `-DHEXA_RT_CORE_ARRAY_I64_LEAF_NATIVE=1`을 넘기고 .o를 runtime.a에 ar하여 C 바디를 드롭한다. _pack_array_enabled()(:1759-1762)도 DEFAULT-ON. 3타깃 모두 시드 .s 존재. (참고: :1872의 \"Default-OFF byte-neutral\" 주석은 :575의 opt-out 의미론에 비추어 stale/오기다.)\n\n즉 이 시드는 양쪽 경계가 모두 mis-ABI된 클래스 A의 거울(C-ABI 호출자 → pair-ABI 콜리)이며, 배포 기본 경로에 있다.\n\n유보 사항(정직한 잔여): 정적 증거대로라면 default 경로의 모든 packed-i64 리터럴 원소가 쓰레기를 저장해야 하는데 CI가 그만큼 시끄럽게 붉지 않다는 긴장이 남는다. 이는 위 7개 확인점 중 어느 것도 반증하지 못하지만, 부모가 default runtime.a로 packed-i64 리터럴의 len/원소를 출력하는 런타임 스모크를 돌리면 CONFIRMED-static → CONFIRMED-live로 확정할 수 있다(가장 값싼 다음 단계).",
          "confidence": "high"
        }
      },
      {
        "file": "self/native/array_typed_leaf_f64_x86_64.s",
        "line": 1,
        "symbol": "hexa_arr_f64_new / hexa_arr_f64_push_bits / hexa_arr_f64_len / hexa_arr_f64_box",
        "klass": "OTHER",
        "evidence": "★ NEW — the f64 twin carries the IDENTICAL mirror break. C decls (mirror census over runtime_core.c): `extern HexaVal hexa_arr_f64_new(int cap);` `HexaVal hexa_arr_f64_push_bits(HexaVal v, int64_t bits)` `extern HexaVal hexa_arr_f64_box(HexaVal v, int64_t i);` — C-ABI. Seed emits pair-ABI: array_typed_leaf_f64_x86_64.s push_bits ingress `mov [rbp-296],rdx # store tag L1` / `mov r12,rcx # ingress param payload` (reads bits from rcx; caller puts them in rdx), and hexa_arr_f64_len egress `mov rdx,r14 # hv arg payload` / `mov rax,[rbp-80] # tag L3` / `ret` (returns len in rdx; C reads eax → always 0). Same 4 .globl symbols, same shape. The f64 seed does NOT carry the hexa_int defect (no `extern fn hexa_int` — it boxes via the __hx_make_val leaf), so it is the mirror break + raw libc only.",
        "impact": "DEFAULT-ON via resolve_native_array_f64_leaf_seed (stage_resolve_runtime_a:764-765, opt-out `:-x`; invoked at :4504; the .s exists for all 3 targets). Packed-f64 array len → always 0; push_bits stores garbage. Same fix shape as the i64 twin.",
        "verdict": {
          "confidence": "high",
          "refuted": false,
          "reason": "CONFIRMED, not refuted. (1) self/native/array_typed_leaf_f64_x86_64.s carries 4 real .globl bodies (new :14/17, push_bits :133/136, len :394/397, box :438/441) — code, not comments — and every one is pair-ABI: new reads cap payload from rsi (`mov [rbp-128],rdi # store tag L0` / `mov rbx,rsi # ingress param payload`); push_bits reads bits from rcx (`mov [rbp-296],rdx # store tag L1` / `mov r12,rcx`); box reads the index from rcx (`mov [rbp-176],rdx` / `mov r12,rcx`); len returns len in rdx with tag(0) in rax (`mov rdx,r14 # hv arg payload` / `mov rax,[rbp-80] # tag L3` / `ret`). (2) The C side is C-ABI. The checked-in self/runtime_core.c is STALE (contains no HEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE); the SSOT is self/runtime_core_emit.hexa:3061, whose guard-ON arm emits `extern HexaVal hexa_arr_f64_new(int cap); extern int hexa_arr_f64_len(HexaVal v); extern HexaVal hexa_arr_f64_box(HexaVal v, int64_t i); extern HexaVal hexa_arr_f64_push_bits(HexaVal v, int64_t xbits);` + the C shim `hexa_arr_f64_push(HexaVal,double)`. Callers are C-ABI: self/codegen.hexa:4782 emits `hexa_arr_f64_new(` and :10715 emits `hexa_arr_f64_box(` into gen2 C text; the native mint site (compiler/codegen/x86_64_linux.hexa:3203, ABI doc runtime_core.c:2672) calls push_bits with rdi:rsi=descriptor, rdx=bits. So C passes cap in edi / index+bits in rdx while the seed reads rsi/rcx → garbage; len is read from eax=tag=0. (3) DEFAULT-ON confirmed: tool/stage_resolve_runtime_a:763-765 `resolve_native_array_f64_leaf_seed()` opts out only on explicit =0, assembles the .s (present for all 3 targets), exports HEXA_RT_ARRAY_F64_LEAF_NATIVE=1, ar's the .o + -D's the guard (:1885-1888); invoked at :4504. The ':642 Default-OFF' text is a stale doc comment. None of the four refutation routes apply (not a HexaVal carrier/backend helper, not default-OFF, not a comment, not a local symbol). Two honest caveats that do NOT refute: hexa_arr_f64_len has no live C call site in-tree (decl-only → 'len always 0' is latent, not live), and the seed also carries raw libc (`call malloc`, realloc/write/exit), so it is class A PLUS the mirror break — which the claim itself acknowledges."
        }
      },
      {
        "file": "tool/stage_resolve_runtime_a",
        "line": 575,
        "symbol": "resolve_native_array_typed_leaf_seed (gate default)",
        "klass": "OTHER",
        "evidence": "★ The gate's own comment contradicts its code — the convergence-4591 gate-default-mismatch class. Header comment (:572-573) claims \"Default-OFF byte-neutral (opt-in HEXA_RT_ARRAY_I64_LEAF / HEXA_RT_OWNOBJ)\". The CODE is opt-OUT and auto-ON: :575 `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0   # explicit opt-out` (only a literal 0 disables), then it falls through, assembles the arch-matched .s (:626 `$CC -c \"$s_tmp\" -o build/array_typed_leaf_native.o`) and `export HEXA_RT_ARRAY_I64_LEAF_NATIVE=1` (:629). The consumption block then pairs correctly: :1873-1874 `if [ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-0}\" = \"1\" ] && [ -f build/array_typed_leaf_native.o ]` → `-DHEXA_RT_CORE_ARRAY_I64_LEAF_NATIVE=1` + ar's the .o into runtime.a. The resolver IS invoked (:4502; f64 twin :4504). All 3 target .s files exist. Same opt-out shape for f64 (:764) and arr_zeros (:708).",
        "impact": "This is what makes the three findings above LIVE rather than latent. The seeds are consumed on the default ship path (both halves of the gate move together), so runtime_core.c externs its C bodies away and the mis-ABI'd seed answers every call. Anyone reading only the comment would conclude these are default-OFF/byte-neutral and safe.",
        "verdict": {
          "refuted": false,
          "reason": "Read the file directly; the claim is confirmed on every axis. tool/stage_resolve_runtime_a:575 is REAL executable code (the trailing `# explicit opt-out` is only an annotation): `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0`. The `:-x` default means an UNSET var yields `x`, which is != \"0\", so the guard does NOT return — it falls through. Only a literal `0` disables. This is opt-OUT / auto-ON, directly contradicting the header comment at :572-573 (\"Default-OFF byte-neutral (opt-in HEXA_RT_ARRAY_I64_LEAF / HEXA_RT_OWNOBJ)\") — the convergence-4591 gate-default-mismatch class (class OTHER), exactly as alleged.\n\nFall-through path verified: :626-627 stages the arch-matched seed to build/array_typed_leaf_seed.s, :628 `$CC -c \"$s_tmp\" -o build/array_typed_leaf_native.o` (the finding said :626 — a cosmetic off-by-two; the assemble is real), :629 `export HEXA_RT_ARRAY_I64_LEAF_NATIVE=1`. The consumption block pairs correctly at :1874 `if [ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-0}\" = \"1\" ] && [ -f build/array_typed_leaf_native.o ]` → `-DHEXA_RT_CORE_ARRAY_I64_LEAF_NATIVE=1` + adds the .o to extra_obj (ar'd into runtime.a), so runtime_core.c externs its C bodies away and the seed answers every call. Both halves of the gate move together.\n\nEvery offered refutation ground FAILS:\n- \"cited line is a comment\" — NO, :575 is code.\n- \"seed is default-OFF, not on the ship path\" — NO. The resolver is invoked unconditionally at :4502 (f64 twin :4504, zeros :4506, arr-new :4508); all three seeds exist and are git-TRACKED (git ls-files confirms self/native/array_typed_leaf_{x86_64,arm64,arm64-linux}.s); tool/stage_resolve_runtime_a is referenced by install.sh (consumer ship path). Same opt-out shape for f64 (:764) and arr_zeros (:708).\n- \"symbol is actually a HexaVal carrier / backend helper\" — N/A; this finding is about the gate default, not a symbol. And the underlying seed source stdlib/runtime/array_typed_leaf.hexa:53-58 does declare the class-A raw libc externs (`extern fn malloc/realloc/write/exit`), so the auto-ON gate is exactly what makes those LIVE rather than latent.\n- \"local symbol\" — N/A.\n\nBonus corroboration (strengthens, not refutes): the gate's own U-floor allowlist at :601 sanctions \" malloc realloc write hexa_exit hexa_int hexa_throw \" — i.e. the guard itself blesses the raw libc symbols, so it would not catch the class-A pair-ABI breach on the own-obj arm either.",
          "confidence": "high"
        }
      },
      {
        "file": "tool/regen_array_typed_leaf_native_s.sh",
        "line": 43,
        "symbol": "ALLOWED_U (U-floor gate)",
        "klass": "OTHER",
        "evidence": "★ The U-floor gate STRUCTURALLY cannot catch class A for these seeds — it whitelists the defect. :43 `ALLOWED_U=\"malloc realloc write hexa_exit hexa_int hexa_throw\"`, with :41-42 explicitly framing it as intentional (\"UNLIKE map-query (ZERO libc), this seed is libc-BEARING: malloc/realloc/write are the whole point\"). Two separate holes: (1) raw libc is sanctioned outright; (2) `hexa_int` is listed as if it were a carrier — it is NOT (it is a C-ABI SCALAR fn, runtime_core.c:1599), so the gate green-lights the silent tag-boxing bug. Siblings: tool/regen_arr_zeros_leaf_native_s.sh:41 `ALLOWED_U=\"malloc calloc write exit hexa_exit\"`; tool/regen_array_new_leaf_native_s.sh:41 `ALLOWED_U=\"calloc hexa_exit malloc write exit\"`; tool/regen_array_typed_leaf_f64_native_s.sh:43 `ALLOWED_U=\"malloc realloc write hexa_exit hexa_throw\"`. CONTRAST — the one gate that is correct: tool/regen_map_query_native_s.sh:38-39 \"the carrier-resolved externs this seed is ALLOWED to leave undefined\" with a carrier-only list (hexa_array_new hexa_map_new rt_map_*) and the comment \"ZERO libc\". The same permissive list is duplicated in the own-obj path at stage_resolve_runtime_a:598 (`printf '%s\\n' \" malloc realloc write hexa_exit hexa_int hexa_throw \"`), so both the .s-seed and own-obj lanes pass.",
        "impact": "The gate that is supposed to be the tripwire for class A is the reason class A shipped. Any hardening must (a) drop raw libc from these ALLOWED_U lists in favour of the hexa_ptr_alloc/hexa_ptr_free carriers, (b) reclassify hexa_int OUT of the carrier keeplist, and (c) close the noted gap — there is still NO hexa_ptr_realloc carrier in self/runtime.h (only hexa_ptr_alloc:872 / hexa_ptr_free:873 / hexa_exit:846), which is the blocker for array_typed_leaf's realloc grow path.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED (class OTHER — gate/policy defect). (1) tool/regen_array_typed_leaf_native_s.sh:43 is real code, not a comment: ALLOWED_U=\"malloc realloc write hexa_exit hexa_int hexa_throw\", enforced as the U-floor gate at :84 (nm U-symbols not in ALLOWED_U → \"U-floor breach\" exit 1). Raw libc malloc/realloc/write are whitelisted outright, so the gate structurally cannot trip on class A for this seed. (2) hexa_int is NOT a HexaVal carrier and NOT a backend-controlled helper on this path: self/runtime.h:112 `HexaVal hexa_int(int64_t n)` (C-ABI scalar arg), definition in self/runtime_core.c `HexaVal hexa_int(int64_t n) { return HX_MAKE_INT(n); }`. The seed declares `extern fn hexa_int(x: int) -> HexaVal` (stdlib/runtime/array_typed_leaf.hexa:57), so aprime lowers it pair-ABI — the SHIPPED seed proves the bug is already live: self/native/array_typed_leaf_x86_64.s:557-560 does `mov rsi,[payload] / mov rdi,[tag] / call hexa_int`, so the C body reads rdi = TAG (0) and every box returns HX_MAKE_INT(0). The hexa_bool exemption does not transfer: hexa_bool is emitted by the backend itself (compiler/codegen/arm64_darwin.hexa:2413, payload already in x0), whereas hexa_int here is reached through a source-level extern fn call lowered pair-ABI. (3) Not default-OFF: tool/stage_resolve_runtime_a:575 is opt-OUT only (`:-x` = \"0\" → return), and the .s-seed lane at :614-631 assembles the seed and exports HEXA_RT_ARRAY_I64_LEAF_NATIVE=1 unconditionally — the \"Default-OFF\" header comment at :572-573 is stale w.r.t. its own code. (4) The permissive list is duplicated in the own-obj lane at tool/stage_resolve_runtime_a:601 (finding said :598 — 3-line drift, immaterial). (5) Siblings verified verbatim: regen_array_typed_leaf_f64_native_s.sh:43, regen_arr_zeros_leaf_native_s.sh:41, regen_array_new_leaf_native_s.sh:41; the correct contrast gate regen_map_query_native_s.sh:39 is carrier-only. (6) The blocker is real: grep over the tree finds hexa_exit(runtime.h:846)/hexa_ptr_alloc(:872)/hexa_ptr_free(:873) and NO hexa_ptr_realloc anywhere, so array_typed_leaf's realloc grow path has no carrier to migrate to. No refutation vector survives.",
          "confidence": "high"
        }
      },
      {
        "file": "self/native/array_typed_leaf_x86_64.s",
        "line": 52,
        "symbol": "malloc / realloc / write",
        "klass": "A-raw-libc",
        "evidence": "KNOWN (confirmed, listed for completeness). Textbook class A, exactly as described. malloc — :50-54 `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default = TAG_INT` / `call malloc` / `mov r15,rdx # hv: unbox user-call result payload` → C malloc reads rdi = the TAG = 0 ⇒ malloc(0); the returned pointer in rax is DISCARDED and the items ptr is taken from a dead rdx. realloc — :235-243 rdi=tag, rsi=payload(ptr), rdx=tag, rcx=payload(size) ⇒ C realloc(ptr=rdi=tag, size=rsi=the old ptr) ⇒ wholly wrong, result again read from rdx. write — :343-352 rdi=0, rsi=2(fd), rdx=tag, rcx=buf, r8=0, r9=20 ⇒ C write(fd=rdi=0, buf=rsi=2, count=rdx=tag) ⇒ writes from address 2 ⇒ EFAULT, so the OOM diagnostic never prints. Mirrored on arm64/arm64-linux. Source: stdlib/runtime/array_typed_leaf.hexa:53-56.",
        "impact": "DEFAULT-ON. Same family that broke PR #4930 on all 3 targets. Fix = the #4935 carrier substitution (hexa_ptr_alloc / hexa_ptr_free / hexa_exit) — but realloc has NO carrier yet (the hexa_ptr_realloc gap), so this seed cannot be fully converted until that carrier lands.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — every refutation path checked and closed. (1) Real code, not comments: self/native/array_typed_leaf_x86_64.s:52 `call malloc`, :241 `call realloc`, :350 `call write` are real instructions with exactly the claimed pair-ABI register layout — malloc gets `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default = TAG_INT`, then `mov [rbp-160],rax # store tag L4` (the real returned pointer is filed as a TAG) and `mov r15,rdx # hv: unbox user-call result payload` (the items ptr comes from dead rdx). C malloc reads rdi=0 as size ⇒ malloc(0). realloc: rdi=tag, rsi=payload(old ptr), rdx=tag, rcx=payload(size) ⇒ C realloc(ptr=tag, size=old_ptr), result again from rdx. write: rdi=0, rsi=2, rdx=tag, rcx=buf ⇒ C write(fd=0, buf=2, count=tag) ⇒ writes from address 2 ⇒ EFAULT, so the OOM diagnostic never prints. (2) Not local symbols: the only defined/.globl symbols in the file are hexa_arr_i64_{new,push,len,box} (+ _hexa_cap_manifest/_hexa_abi_stamp); malloc/realloc/write are U. (3) Not HexaVal-ABI carriers nor backend helpers like hexa_bool: stdlib/runtime/array_typed_leaf.hexa:52-55 declares them as raw libc (`extern fn malloc(n: int) -> int`, `realloc(p,n)`, `write(fd,buf,n)`), and self/runtime.h has NO malloc/realloc/write carrier — only hexa_exit (:846), hexa_ptr_alloc (:872), hexa_ptr_free (:873). Corroborating: the same seed already emits `call hexa_exit` (properly carrier'd) while `write` stays raw, so the two classes are distinguishable in-file. (4) Not default-OFF (this was the strongest candidate refutation and it fails): tool/stage_resolve_runtime_a:4502 invokes resolve_native_array_typed_leaf_seed UNCONDITIONALLY; the function body (:575) contains only an explicit opt-OUT check (`[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0`), so an unset env proceeds, assembles the seed and `export HEXA_RT_ARRAY_I64_LEAF_NATIVE=1` (:629); the consumption block (:1874) then ar's array_typed_leaf_native.o into runtime.a, and it runs later (build_runtime_a_from_source at :4583/:4611). The inline \"(default-OFF)\" comments at :4501 and in the :572-573 header block are STALE and contradicted by the code. (5) arm64 mirroring confirmed: array_typed_leaf_arm64.s and array_typed_leaf_arm64-linux.s each carry 4 raw sites (bl _malloc/bl malloc :43,:56; bl realloc :173; bl write). Carrier gap confirmed: hexa_ptr_realloc is absent from self/runtime.h, so the #4935 substitution cannot fully convert this seed until that carrier lands (write also lacks a carrier, though its OOM-diagnostic path could route through hexa_throw, which the seed already calls). Only inaccuracy is a trivial off-by-one in the cited source range (actual externs at array_typed_leaf.hexa:52-55, not :53-56) — immaterial to the substance.",
          "confidence": "high"
        }
      },
      {
        "file": "self/native/array_typed_leaf_f64_x86_64.s",
        "line": 1,
        "symbol": "malloc / realloc / write",
        "klass": "A-raw-libc",
        "evidence": "KNOWN (confirmed). Identical raw-libc U-floor to the i64 twin: census of `call`-targets not defined in-file = {malloc, realloc, write, hexa_exit, hexa_throw} (+hexa_bool on arm64). Source externs at stdlib/runtime/array_typed_leaf_f64.hexa; gate ALLOWED_U at tool/regen_array_typed_leaf_f64_native_s.sh:43.",
        "impact": "DEFAULT-ON (stage_resolve_runtime_a:764, opt-out). Same pair-ABI-vs-C-ABI corruption as the i64 twin, stacked on top of the mirror break reported above.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. (1) Call-target census of self/native/array_typed_leaf_f64_x86_64.s: undefined-in-file call targets = {malloc x2, realloc, write, hexa_exit, hexa_throw}; the file defines only the 4 .globl hexa_arr_f64_{new,push_bits,len,box}, so malloc/realloc/write are true U symbols, not local labels. (2) They are emitted with hexa pair-ABI, e.g. lines 50-54: `mov rsi,16 # hv arg payload / mov rdi,0 # tag default=TAG_INT / call malloc / mov [rbp-160],rax (stored as TAG) / mov r15,rdx # hv: unbox user-call result payload`. C-ABI malloc reads rdi=0 as its size and returns in rax; the seed reads the pointer from dead rdx. realloc identical (payloads in rsi/rcx, tags in rdi/rdx, result payload from rdx). Exactly the class-A corruption. (3) Source externs at stdlib/runtime/array_typed_leaf_f64.hexa:29-32 are raw libc (`extern fn malloc/realloc/write/exit`), NOT HexaVal-ABI carriers. (4) self/runtime.h has hexa_ptr_alloc(872)/hexa_ptr_free(873)/hexa_exit(846) only — no hexa_ptr_realloc carrier exists, confirming the stated gap. (5) The gate whitelists the defect: tool/regen_array_typed_leaf_f64_native_s.sh:43 ALLOWED_U=\"malloc realloc write hexa_exit hexa_throw\". (6) DEFAULT-ON confirmed: tool/stage_resolve_runtime_a:763-765 `resolve_native_array_f64_leaf_seed() { [ \"${HEXA_RT_ARRAY_F64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0 # explicit opt-out` — unset means proceed/export=1, and it is invoked unconditionally at line 4504 (the \"# Default-OFF.\" comment at line 642 is stale prose, contradicted by the code). None of the refutation escape hatches hold: the symbols are not carriers/backend helpers, the seed is not default-OFF, the calls are not local, and although the finding anchors at :1 (header comment), the real `call malloc/realloc/write` sites are in the body (52, 73, 241).",
          "confidence": "high"
        }
      },
      {
        "file": "self/native/arr_zeros_leaf_x86_64.s",
        "line": 1,
        "symbol": "calloc / malloc / write",
        "klass": "A-raw-libc",
        "evidence": "KNOWN (confirmed present in main at HEAD e891995ed). Census of undefined call-targets = {calloc, malloc, write, hexa_exit} (+hexa_bool on arm64). NOTE the partial state: hexa_exit has ALREADY been converted to the carrier, but calloc/malloc/write have NOT — so the #4935 fix is visible-but-incomplete in the tree I audited (main; the completed version lives in .worktrees/arrnew-fix, commits 3ebd44bfa/9e8e58450/d3d10f719, not yet on main). Gate still sanctions it: tool/regen_arr_zeros_leaf_native_s.sh:41 `ALLOWED_U=\"malloc calloc write exit hexa_exit\"`.",
        "impact": "DEFAULT-ON (stage_resolve_runtime_a:708, opt-out `:-x`). This is the measured exit-9 / silent-len=0 seed from the briefing. Confirms the fix has not landed on main yet.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED at main HEAD e891995ed. self/native/arr_zeros_leaf_x86_64.s has exactly 4 undefined call-targets: calloc (lines 34, 361), malloc (129, 456), write (180, 507), hexa_exit (186, 513). Only .globl syms are hexa_arr_zeros_leaf{,_int}; all other labels are local .L*_bb* — so calloc/malloc/write are true externs, not local symbols, and the call sites are real code (the :1 anchor is the header comment, but the cited census is stated explicitly and is accurate). They are lowered pair-ABI: at :30-36 `mov rsi,1 # hv arg payload` / `mov rdi,0 # tag default=TAG_INT` / `call calloc` / `mov [rbp-328],rax # store tag` / `mov r12,rdx # hv: unbox user-call result payload` — rdi carries the TAG, the rax pointer is stored as a tag, and the payload is read from the dead rdx. C-ABI calloc would see nmemb=0,size=1. Same shape at the malloc and write sites. Not carriers and not backend helpers: self/runtime.h defines hexa_ptr_alloc(:872), hexa_ptr_free(:873), hexa_exit(:846), hexa_bool(:113) but NO HexaVal calloc/malloc/write (and no hexa_ptr_realloc at all — the gap the briefing flags). hexa_exit is indeed already the carrier, exactly the partial/incomplete state the finding describes. Deployment: NOT default-OFF — tool/stage_resolve_runtime_a:708 is `[ \"${HEXA_RT_ARRAY_ZEROS_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` (:-x = opt-out), resolve_native_array_zeros_leaf_seed is invoked unconditionally at :4506, :743 selects this seed for linux-x86_64, :1891 links the .o (the :706 comment saying \"Default-OFF\" is stale). Gate sanctions it at tool/regen_arr_zeros_leaf_native_s.sh:41 ALLOWED_U=\"malloc calloc write exit hexa_exit\". No refutation ground applies.",
          "confidence": "high"
        }
      },
      {
        "file": "self/native/runtime_hi_x86_64.s",
        "line": 1,
        "symbol": "ALL other seed families (12 families / 60 of 69 .s files)",
        "klass": "OK-carrier",
        "evidence": "★ THE CENSUS NEGATIVE — this is the headline answer. I extracted every `call X` / `bl X` target not defined in-file across all 69 self/native/*.s, and separately verified there are ZERO tail-call (`jmp`/`b`) and ZERO address-taken (`lea [rip+…]`/`adrp`) external refs, so the call/bl set is the COMPLETE external surface. Result: raw libc (calloc/malloc/realloc/free/write/exit) appears in EXACTLY 3 seed families — arr_zeros_leaf, array_typed_leaf, array_typed_leaf_f64 — the ones already known. ZERO raw libc anywhere else. The other 12 families (runtime_hi/rt_hi_native, array_core, map_core, intern_core, num_core, valop_core, fs_core, num_float_core, float_parse_exact, float_parse_hexinfnan, regex_rt, alloc_syscall, hxlcl_*) call ONLY: (a) genuine HexaVal-ABI carriers — hexa_eq/mul/sub/div/mod/add_slow/cmp_*/index_get/index_set/array_new/array_push/map_new/map_get/map_set/str_byte_at/str_substring/str_join/to_string/type_of/byte_len/bits_to_float/float_to_bits/chr_byte/bytes_to_str_raw/regex_search/arr_poly_*/to_int/throw — all verified in self/runtime.h as HexaVal-in/HexaVal-out, and the backend emits the mixed ones correctly from its per-symbol signature table (e.g. hexa_map_set's `const char*` key: regex_rt_x86_64.s:7918-7923 correctly lays out rdi=tag,rsi=payload,rdx=&.LCstr,rcx=tag,r8=payload); (b) backend-controlled helpers with a KNOWN signature — hexa_bool (sanctioned), hexa_truthy (rdi=tag,rsi=payload → result read from rax, correct), hexa_len (`mov r15,rax # hv: unbox call result (rax)` — backend knows it returns int64 in rax, NOT rdx), hexa_int as ret-box (`sxtw x0,w0` / `bl _hexa_int ; ret int: box`); (c) hexa_arena_alloc — a native→native pair-ABI edge (defined by the alloc_syscall seed at alloc_syscall_x86_64.s:4757, ingress `mov [rbp-360],rdi # store tag L0` / `mov rbx,rsi # ingress param payload` = pair-ABI, matching the caller), documented as intentional at stdlib/runtime/array_core.hexa:55-63 and ar'd into the same runtime.a; (d) hxlcl_malloc — correctly lowered C-ABI via the Route C whitelist `_is_cabi` (compiler/codegen/x86_64_linux.hexa:2258+).",
        "impact": "ANSWER TO THE BRIEF: NO — class A does not hide anywhere else. The blast radius of raw-libc-via-pair-ABI is confined to the 3 array-leaf seeds already on the board. The core/hot seeds (runtime_hi, array_core, map_core, intern_core, num_core, valop_core, fs_core, num_float_core, float_parse_*, regex_rt, alloc_syscall, hxlcl_*) are clean. What I DID find instead are two NEW defects inside those same 3 known seeds (the hexa_int scalar-extern, and the C→seed mirror break) plus the gate hole that let them through.",
        "verdict": {
          "refuted": false,
          "reason": "I independently reproduced the census and it confirms the claim. (1) Extracting every `call`/`bl` target not defined in-file across all 69 self/native/*.s: raw libc (calloc/malloc/realloc/write) appears in EXACTLY 3 families — arr_zeros_leaf, array_typed_leaf, array_typed_leaf_f64 — and nowhere else. (2) The call/bl set really is the complete external surface: I verified ZERO external tail-calls (jmp/b), ZERO address-taken refs (lea [rip+…]/adrp), and ZERO .quad/.xword/.long data relocations to external symbols. (3) I checked each remaining external symbol's true signature in self/runtime.h and confirmed the mixed-ABI ones are emitted correctly by the backend's per-symbol table: hexa_map_get(HexaVal, const char*) at regex_rt_x86_64.s:890-893 lays rdi=tag/rsi=payload/rdx=&.LCstr and reads the result payload from rdx; hexa_arr_poly_get(HexaVal, int64_t) matches the backend's own lowering at compiler/codegen/x86_64_linux.hexa:3541-3546 (pair + raw idx in rdx); `int64_t hexa_len(HexaVal)` call sites read rax (`mov r15, rax # hv: unbox call result (rax)`), not rdx; `HexaVal hexa_int(int64_t)` in the clean families occurs ONLY in arm64 seeds and ONLY as the backend ret-box idiom (`bl hexa_len ; sxtw x0,w0 ; bl hexa_int # ret int: box`), i.e. scalar-in as required; hexa_arena_alloc is an intentional native→native pair-ABI edge that runtime_core.c:1639/1650 explicitly declares as `extern HexaVal __hxseed_arena_alloc(HexaVal n) __asm__(\"hexa_arena_alloc\")` with __hxw_ bridges. So class A is genuinely confined to the 3 known array-leaf seeds. Two non-refuting caveats for the parent: (a) the hexa_arena_alloc edge is sound only because HEXA_RT_ALLOC_NATIVE defaults to 1 (tool/build_aprime.sh:369 `${HEXA_RT_ALLOC_NATIVE:-1}`) which drops the C `void* hexa_arena_alloc(size_t)` body — with HEXA_RT_ALLOC_NATIVE=0 the always-linked array_core seed's pair call would bind to a C-ABI arena body (a latent class-A shape in that opt-out config, which tool/measure_descriptor_discriminator.sh:40 actually builds); (b) unrelated latent bug: arm64 narrows hexa_len's int64_t return via `sxtw x0, w0` despite runtime.h:271 declaring int64 specifically to avoid ≥4GB truncation. The cited anchor line (runtime_hi_x86_64.s:1) is a file-header comment, but it is an anchor for a census-negative, not a code-level assertion, so it does not undermine the substance.",
          "confidence": "high"
        }
      },
      {
        "file": "self/native/intern_core_arm64.s",
        "line": 1,
        "symbol": "hexa_bool / hexa_truthy (the x86-vs-arm64 U delta)",
        "klass": "OK-carrier",
        "evidence": "★ CLASS-B CENSUS NEGATIVE. I diffed the undefined-symbol set x86_64 vs arm64 for every family, since class B is defined by exactly that asymmetry (x86 folds inline, arm64 emits a real bl). The delta across ALL 22 families is EXACTLY {hexa_bool, hexa_truthy, hexa_int-as-ret-box} and nothing else. Concretely: intern_core / map_core / num_core — x86 U-set is EMPTY, arm64 = {hexa_bool, hexa_truthy}; valop_core + hxlcl_strchr/strncmp/strstr/strtoll — x86 EMPTY, arm64 = {hexa_bool}; fs_core — arm64 adds only hexa_truthy; array_core / alloc_syscall / regex_rt / runtime_hi / num_float_core / float_parse_* — arm64 adds only {hexa_bool} or {hexa_bool, hexa_int}. All three of those are backend-EMITTED helpers whose ABI the codegen controls itself (hexa_bool is explicitly sanctioned; hexa_truthy verified rdi=tag/rsi=payload→rax; hexa_int-as-ret-box verified at runtime_hi_native.s:26-28). CRUCIALLY: there is NO seed anywhere in which hexa_eq / hexa_mul / hexa_add_slow / hexa_cmp_* appears on arm64 but NOT on x86 — the seeds that call those call them on BOTH targets, i.e. they are real HexaVal-ABI runtime ops in non-leaf seeds that are ar'd into runtime.a where they resolve, not the source-level `a == 0` boxed slow-call that class B describes.",
        "impact": "ANSWER: the class-B pattern (a plain source operator silently becoming an arm64-only boxed slow-call, passing x86 CI and breaking the darwin link) does NOT recur in any other seed family. The only arm64-only symbols are the sanctioned backend helpers. Useful corollary for the gate: the presence of hexa_int in an X86_64 seed's U-list is the fingerprint of the array_typed_leaf bug — in a healthy seed hexa_int is arm64-only (ret-box). That single asymmetry check is a cheap tripwire.",
        "verdict": {
          "refuted": false,
          "reason": "The finding is CORRECT: hexa_bool/hexa_truthy in self/native/intern_core_arm64.s are OK-carrier (sanctioned backend-emitted helpers), not a class-A or class-B defect. Verified at register level, not by trusting the census.\n\nSIGNATURES (self/runtime.h:113,140): `HexaVal hexa_bool(int b)` (def runtime_core.c:1831) and `int hexa_truthy(HexaVal v)` (def runtime_core.c:8264). Both are defined in runtime_core.c and resolve out of runtime.a. hexa_bool is explicitly sanctioned by the stated ALLOWED_U policy.\n\nNOT CLASS A. Class A's failure mode is the caller reading the result payload from the dead second return register (rdx/x1) after a C-ABI call that only writes rax/x0. The arm64 seed does the exact opposite at every site (lines 55/58, 68/71, 190/193, 227/230, 306/311, 331/336):\n    cmp x1, x3 ; cset x0, eq        <- __hx_payload_eq leaf, INLINED\n    bl _hexa_bool                    ; int arg in x0                (correct C-ABI)\n    stp x0, x1, [sp,#112]            ; HexaVal ret read from x0:x1  (16B struct -> AAPCS correct)\n    ldp x0, x1, [sp,#112]            ; HexaVal arg in x0:x1         (correct)\n    bl _hexa_truthy\n    uxtw x0, w0 ; cbz x0             ; int ret read from w0 ONLY — x1 NEVER touched (correct)\nEvery register matches the true C signature. No dead-register payload read anywhere.\n\nNOT CLASS B. The x86-vs-arm64 asymmetry is real and structurally resembles class B (x86 folds cmp/sete/movzx/test fully inline with ZERO outbound calls; arm64 emits real bl). But class B is defined by a source-level plain operator silently becoming a boxed slow-call. The source stdlib/runtime/intern_core.hexa (lines 52,53,80,84,89,91) uses the SAFE leaves throughout — __hx_payload_eq / __hx_payload_lt — and those leaves ARE inlined as cmp/cset on both targets. hexa_bool/hexa_truthy are merely the backend's own box/unbox of the leaf's bool result for the branch condition. grep count for calloc|malloc|realloc|free|hexa_eq|hexa_mul|hexa_add_slow|hexa_cmp_ in the arm64 seed = 0.\n\nONLY NIT (cosmetic, not substantive): the cited anchor intern_core_arm64.s:1 is a COMMENT (the frozen-bootstrap-seed file header), not a code site. That is one of the listed refutation criteria, but here it functions as a file-level census anchor rather than a fabricated code citation — the substantive claim is verified at the real lines. Given the bias-to-refuted instruction I weighed this, but the finding's actual assertion (these symbols are OK-carrier) is confirmed on every axis: correct ABI, defined+resolvable, safe leaf source, zero forbidden symbols.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf.hexa",
        "line": 53,
        "symbol": "extern fn malloc / realloc / write / exit",
        "klass": "A-raw-libc",
        "evidence": "array_typed_leaf.hexa:53-56 declares `extern fn malloc(n: int) -> int` / `realloc(p,n)` / `write(fd,buf,n)` / `exit(code)`. The frozen seeds prove the calls survive to native: `self/native/array_typed_leaf_{x86_64,arm64,arm64-linux}.s` each emit `call/bl malloc, realloc, write` (raw C-ABI symbol names, no carrier). aprime lowers these as pair-ABI calls, so the (tag,payload) pair lands in (rdi,rsi)/(x0,x1) and the result payload is read from rdx/x1 — but the linker binds them to the C-ABI libc bodies that return in rax/x0 only.",
        "impact": "DEFAULT-ON, live on main. stage_resolve_runtime_a:575 gates on `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` — i.e. only an explicit 0 disables it; the seed .s exists for all 3 targets, so the native bodies replace the C ones on every build. Same defect shape as PR #4930 (array_new/calloc → SIGSEGV 3/3 targets, reverted #4932). realloc-grow is the hot path: hexa_arr_i64_push past cap → realloc returns the new base in rax while the seed reads rdx garbage → wild pointer store. Silently-wrong-then-crash, not a clean fail.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — could not refute on any of the four available grounds.\n\n(1) NOT a carrier: no hexa-side definition of malloc/realloc/write exists in self/*.c or self/*.h; self/runtime.h declares only hexa_ptr_alloc (:872) and hexa_ptr_free (:873) — hexa_ptr_realloc genuinely does not exist, confirming the stated gap. (The co-declared hexa_int/hexa_throw/hexa_bool ARE legitimate HexaVal-ABI symbols; malloc/realloc/write are not.)\n\n(2) NOT default-OFF: tool/stage_resolve_runtime_a:575 is `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` — the `:-x` default means only an explicit \"0\" opts out. Seeds are present for all 3 targets (:615-617), so the native bodies replace the C ones on every build. DEFAULT-ON, live on main.\n\n(3) NOT a comment: stdlib/runtime/array_typed_leaf.hexa:53-56 are live `extern fn` decls, called at :65 (malloc), :84 (realloc), :87-88 (write/exit).\n\n(4) NOT local: all three frozen seeds emit raw C-ABI `call/bl malloc, realloc, write` (self/native/array_typed_leaf_{x86_64,arm64,arm64-linux}.s).\n\nDirect evidence of the pair-ABI/C-ABI collision in self/native/array_typed_leaf_x86_64.s:\n- hexa_arr_i64_new :50-55 — `mov rsi,16` (size) / `mov rdi,0` (TAG) → libc sees malloc(0); `mov [rbp-160], rax` stores the REAL pointer into a *tag* slot; `mov r15, rdx  # hv: unbox user-call result payload` reads dead rdx garbage as the HexaArrI64* and then stores through it.\n- hexa_arr_i64_push realloc-grow :236-243 — rdi=tag, rsi=old ptr, rdx=tag, rcx=nbytes. C-ABI realloc(void*,size_t) therefore reads realloc(NULL, <old-pointer-value-as-size>); the byte count never reaches libc. New base returns in rax while the seed reads rdx → wild pointer store. Worse than the claim alleged.\n\nSmoking gun: the U-floor gate meant to catch class A (stage_resolve_runtime_a:601) explicitly ALLOWLISTS the raw libc symbols — `printf '%s\\n' \" malloc realloc write hexa_exit hexa_int hexa_throw \"` — so the guard blesses the defect instead of blocking it.\n\nONE CORRECTION (does not refute): `exit` is NOT class-A here. Despite the `extern fn exit` decl at :56, all 3 seeds lower it to `call/bl hexa_exit`, the sanctioned carrier. The class-A set is malloc / realloc / write (3 symbols), not 4.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_typed_leaf_f64.hexa",
        "line": 30,
        "symbol": "extern fn malloc / realloc / write / exit",
        "klass": "A-raw-libc",
        "evidence": "array_typed_leaf_f64.hexa:30-33 — identical raw-libc extern block (malloc, realloc, write, exit; no hexa_int). Seeds self/native/array_typed_leaf_f64_{x86_64,arm64,arm64-linux}.s all emit `call/bl malloc, realloc, write`.",
        "impact": "DEFAULT-ON (stage_resolve_runtime_a:764, same `:-x` = 0 polarity). f64 twin of the i64 defect — hexa_arr_f64_new/push_bits mint and grow through the same broken pair→C-ABI seam. Live latent on main since #4922.",
        "verdict": {
          "refuted": false,
          "reason": "Confirmed on origin/main, not refuted. (1) stdlib/runtime/array_typed_leaf_f64.hexa:30-33 is live code (not comment): `extern fn malloc/realloc/write/exit` with int params — raw libc C-ABI names, no hexa_int. (2) self/runtime.h has only hexa_exit(846), hexa_ptr_alloc(872), hexa_ptr_free(873) — no hexa_malloc/hexa_realloc/hexa_write carriers and notably NO hexa_ptr_realloc, so these bind to real libc bodies; they are not HexaVal-ABI carriers, not backend helpers (hexa_bool-class), and not locally defined. (3) All three seeds emit the class-A seam: array_typed_leaf_f64_x86_64.s:52 does `mov rsi,16 / mov rdi,0 / call malloc / mov r15,rdx` — libc reads rdi(=TAG_INT 0) as the size and returns in rax, while the seed takes the pointer from the dead rdx. arm64 (.s:43) and arm64-linux (.s:43) mirror it (x0=tag,x1=payload; result payload read from clobbered x1). Same pattern at realloc (x86:241/arm:173) and write (x86:350/arm:251). (4) DEFAULT-ON confirmed: tool/stage_resolve_runtime_a:764 `[ \"${HEXA_RT_ARRAY_F64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` — the `:-x` polarity means unset falls through, the seed is assembled (:806-812), HEXA_RT_ARRAY_F64_LEAF_NATIVE=1 is exported, and the 4 f64 bodies are externed out of runtime_core.c (:1885); the resolver is called unconditionally at :4504. The `Default-OFF` comment at :642 is stale prose — the identical polarity that broke array_new in #4930. The OWN-OBJ arm's U-floor check (:783) even explicitly allowlists malloc/realloc/write, so no gate catches it. Live latent on main since #4922, exactly as claimed.",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/arr_zeros_leaf.hexa",
        "line": 25,
        "symbol": "extern fn malloc / calloc / write / exit",
        "klass": "A-raw-libc",
        "evidence": "arr_zeros_leaf.hexa:25-28 raw externs. self/native/arr_zeros_leaf_{x86_64,arm64,arm64-linux}.s emit `call/bl calloc, malloc, write`. This is the exact seed the task background measured: linux-x86_64 → exit 9 (its own OOM guard mistakes the rdx garbage for NULL), darwin-arm64 → silent len=0 bogus array.",
        "impact": "DEFAULT-ON and MERGED on main (f810ddeb9 / #4927 flipped it 3-target). Not covered by the #4930 revert. Unless #4935 (in flight, not yet on origin/main — HEAD of origin/main is e891995ed) lands, main ships a runtime whose boxed-zeros constructors are ABI-broken on all 3 targets.",
        "verdict": {
          "refuted": false,
          "reason": "The finding stands. (1) Lines 25-28 of stdlib/runtime/arr_zeros_leaf.hexa on origin/main (e891995ed) are real code, not comments: `extern fn malloc/calloc/write/exit`. The file's own header comment states \"U-floor = { malloc, calloc, write, exit }\". (2) They are NOT HexaVal-ABI carriers: self/runtime.h declares no HexaVal malloc/calloc/write; it declares the carriers only for the other family (hexa_ptr_alloc:872, hexa_ptr_free:873, hexa_exit:846) and itself calls calloc/malloc as genuine C-ABI libc (line 908 `calloc(1, sizeof(HexaClo))`). The seed never references hexa_ptr_alloc. (3) They are NOT local symbols: all three seeds emit external calls — self/native/arr_zeros_leaf_x86_64.s `call calloc/malloc/write`, arm64.s `bl _calloc/_malloc/_write`, arm64-linux.s `bl calloc/malloc/write`. (4) It is NOT default-OFF: tool/stage_resolve_runtime_a:708 gates opt-out-only — `[ \"${HEXA_RT_ARRAY_ZEROS_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` — default `x` != `0`, so it falls through and exports HEXA_RT_ARRAY_ZEROS_LEAF_NATIVE=1, ar'ing the seed .o into runtime.a and dropping the C bodies via -DHEXA_RT_CORE_ARRAY_ZEROS_LEAF_NATIVE (line 1891-1894). The \"Default-OFF\" comments at lines 706/1872 are stale PR-1 text that the flip commit f810ddeb9 (#4927, \"own-native default-ON (3-target .s seed)\") did not update. (5) It is NOT covered by the revert: aab1956cd (#4932) reverted only #4930, which was unit #6 hexa_array_new; the last commit touching arr_zeros_leaf on main is the #4927 flip itself. One minor correction to the claim, not a refutation: `exit` actually lowers to `call hexa_exit` (the HexaVal carrier) in all 3 seeds, so the true raw-libc floor is {calloc, malloc, write}, not 4 symbols — but both allocators supplying the descriptor and the items[] run are raw C-ABI, which is exactly class A. Files: /Users/mini/dancinlab/hexa-lang/stdlib/runtime/arr_zeros_leaf.hexa, /Users/mini/dancinlab/hexa-lang/self/runtime.h, /Users/mini/dancinlab/hexa-lang/tool/stage_resolve_runtime_a, /Users/mini/dancinlab/hexa-lang/self/native/arr_zeros_leaf_{x86_64,arm64,arm64-linux}.s",
          "confidence": "high"
        }
      },
      {
        "file": "stdlib/runtime/array_new_leaf.hexa",
        "line": 23,
        "symbol": "extern fn calloc",
        "klass": "A-raw-libc",
        "evidence": "array_new_leaf.hexa:23 `extern fn calloc(cnt: int, size: int) -> int` still present in the tree. The .s seeds were reverted (#4932 — `git ls-files self/native/` shows NO array_new_leaf_*.s), so the SSOT source is still defective but currently inert.",
        "impact": "Inert TODAY only because the seeds are absent — stage_resolve_runtime_a:687 falls back to the C body when no seed file exists. Any re-run of tool/regen_array_new_leaf_native_s.sh re-mints the same broken seed and re-arms the SIGSEGV, because nothing in the regen gate rejects calloc (see regen_array_new_leaf_native_s.sh:41).",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — every refutation ground was checked and none holds. (1) NOT a comment: array_new_leaf.hexa:23 is genuine code `extern fn calloc(cnt: int, size: int) -> int`, called at line 27 `let a = calloc(1, 32)` (line 21 is a separate comment that merely mentions calloc in the U-floor note). (2) NOT a HexaVal carrier / backend helper: grep of self/runtime.h returns hexa_bool:113, hexa_exit:846, hexa_ptr_alloc:872, hexa_ptr_free:873 and ZERO calloc declaration — calloc binds to the C-ABI libc body, which is the class-A trap exactly. (3) NOT a local symbol: the file is 29 lines and defines only hexa_array_new. (4) The \"seed absent / default-OFF\" ground is factually true (`git ls-files self/native/` shows no array_new_leaf_*.s; stage_resolve_runtime_a:687 is verbatim the cited C-body fallback) but it does not refute the finding — the finding itself states \"still defective but currently inert\", so the mitigation IS the claim. The re-arm impact is corroborated verbatim: tool/regen_array_new_leaf_native_s.sh:41 is `ALLOWED_U=\"calloc hexa_exit malloc write exit\"`, so the U-floor gate (lines 80-82) explicitly whitelists calloc and cannot reject it. The finding is if anything UNDERSTATED: a second arming route exists that needs no .s seed at all — stage_resolve_runtime_a:657-668 (OWN-OBJ) compiles the SSOT .hexa directly via `hexat --backend=native --emit=obj` when HEXA_RT_OWNOBJ=1 or HEXA_RT_OWNOBJ_ARRNEW=1, and its inline U-floor allowlist (line 668) is the same \" calloc hexa_exit malloc write exit \" string. Sibling arr_zeros_leaf.hexa:25-28 still carries raw malloc/calloc/write/exit as well, i.e. the #4935 carrier fix has not landed in this tree.",
          "confidence": "high"
        }
      },
      {
        "file": "tool/regen_arr_zeros_leaf_native_s.sh",
        "line": 41,
        "symbol": "ALLOWED_U=\"malloc calloc write exit hexa_exit\"",
        "klass": "OTHER",
        "evidence": "The U-floor allowlist LITERALLY WHITELISTS the class-A symbols. Comment at :39-40 even sanctions it — \"calloc is the only new floor entrant vs the arr-i64/f64 leaves (sanctioned)\". The check at :79-82 then confirms every U ∈ ALLOWED_U and prints \"U-floor clean\".",
        "impact": "This is gate hole #1 and the primary reason the class shipped. The gate was designed to permit raw libc — it can never catch class A. It also mis-states the contract: the allowlist conflates 'symbol the C body also left undefined' (true) with 'symbol this seed may CALL' (false — the C body calls it with C-ABI, the seed calls it with pair-ABI).",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED. tool/regen_arr_zeros_leaf_native_s.sh:41 is live code (not a comment): ALLOWED_U=\"malloc calloc write exit hexa_exit\". Of those five, only hexa_exit is a real HexaVal-ABI carrier — self/runtime.h declares hexa_exit(HexaVal) (:846), hexa_ptr_alloc(HexaVal) (:872), hexa_ptr_free(HexaVal,HexaVal) (:873), and has NO HexaVal declaration for malloc/calloc/write/exit. So the U-floor allowlist literally whitelists the class-A raw-libc symbols by name; the comment at :39-40 sanctions calloc explicitly, and the check at :79-82 grep-matches every nm U symbol against that string and prints \"U-floor clean\" (:83). The seed genuinely calls them with pair-ABI: self/native/arr_zeros_leaf_x86_64.s has `call calloc` (:34,:361), `call malloc` (:129,:456), `call write` (:180,:507) — while `exit` correctly lowered to `call hexa_exit`. The SSOT stdlib/runtime/arr_zeros_leaf.hexa:25-28 still declares `extern fn malloc/calloc/write/exit` on origin/main. Not default-OFF either: tool/stage_resolve_runtime_a:709/730/756 auto-export HEXA_RT_ARRAY_ZEROS_LEAF_NATIVE=1 once the seed .o exists (:706's \"Default-OFF\" note is conditioned on no seed existing) and :1891-94 ars it into runtime.a with -DHEXA_RT_CORE_ARRAY_ZEROS_LEAF_NATIVE=1. None of the four refutation grounds apply (not a carrier, not a backend helper, not OFF-path, not a comment, not a local symbol). The gate as designed can never catch class A for this seed, and the OTHER classification (gate/policy hole, not a class-A call site itself) is right.",
          "confidence": "high"
        }
      },
      {
        "file": "tool/regen_array_new_leaf_native_s.sh",
        "line": 41,
        "symbol": "ALLOWED_U=\"calloc hexa_exit malloc write exit\"",
        "klass": "OTHER",
        "evidence": "Same whitelist-the-bug pattern. This is the gate that green-lit PR #4930, the one that broke all 3 targets.",
        "impact": "Gate hole. calloc/malloc/write/exit explicitly allowed → the exact symbol that caused the 3-target SIGSEGV passed 'U-floor clean'.",
        "verdict": {
          "refuted": false,
          "reason": "반증 실패 — 발견은 정확하다. 4가지 반증 경로를 모두 확인했고 전부 불성립.\n\n1) 주석 아님: tool/regen_array_new_leaf_native_s.sh:41 은 실제 코드 `ALLOWED_U=\"calloc hexa_exit malloc write exit\"` (주석은 39-40행). 이 값은 79-83행에서 실제로 강제된다 — nm의 U 심볼 중 ALLOWED_U에 없는 것만 \"U-floor breach\"로 exit 1, 있는 것은 \"U-floor clean\" 출력.\n\n2) carrier/백엔드-헬퍼 아님: self/runtime.h에서 HexaVal-ABI carrier는 정확히 hexa_exit(:846), hexa_ptr_alloc(:872), hexa_ptr_free(:873) 뿐. calloc/malloc/write/exit는 carrier로 존재하지 않는 raw libc. 화이트리스트 5개 중 hexa_exit만 정당하고 나머지 4개는 클래스 A 그 자체.\n\n3) 로컬 심볼 아님: stdlib/runtime/array_new_leaf.hexa:23 `extern fn calloc(cnt: int, size: int) -> int`, :27 `let a = calloc(1, 32)` — raw libc extern 선언 + 호출.\n\n4) default-OFF 면책 아님: git log(HEAD e891995ed)가 인과를 확증 — f1531dd2d(#4929 guard-OFF 시드) → d00cc84d8(#4930 default-ON 플립) → aab1956cd(#4932 revert). 현재 OFF인 것은 \"배포경로에 없어서\"가 아니라 \"플립이 3타깃을 깨서 되돌려졌기 때문\". 게이트가 바로 그 플립을 통과시킨 주체다. FIX #4935는 이 트리에 없으므로 구멍은 살아있다.\n\n체계적 패턴 확인: 형제 스크립트도 동일 — regen_arr_zeros_leaf_native_s.sh:41 \"malloc calloc write exit hexa_exit\", regen_array_typed_leaf_native_s.sh:43 \"malloc realloc write hexa_exit hexa_int hexa_throw\", regen_array_typed_leaf_f64_native_s.sh:43 \"malloc realloc write hexa_exit hexa_throw\".",
          "confidence": "high"
        }
      },
      {
        "file": "tool/regen_array_typed_leaf_native_s.sh",
        "line": 43,
        "symbol": "ALLOWED_U=\"malloc realloc write hexa_exit hexa_int hexa_throw\"",
        "klass": "OTHER",
        "evidence": "Comment :40-42 makes the wrong call explicit: \"UNLIKE map-query (ZERO libc), this seed is libc-BEARING: malloc/realloc/write are the whole point (the realloc-grow the wall said 'stays C')\". The 'net floor unchanged' reasoning is an nm-count argument, blind to ABI.",
        "impact": "Gate hole for the i64 leaf. Also note hexa_bool is NOT in this list — see the hexa_bool finding: the check demonstrably never ran on the arm64 targets.",
        "verdict": {
          "refuted": false,
          "reason": "Finding survives all four refutation routes. (1) tool/regen_array_typed_leaf_native_s.sh:43 is real code, not a comment — ALLOWED_U is consumed by the U-floor gate at :84, so malloc/realloc/write are actively whitelisted as permitted undefined externs. (2) The symbols are raw libc, not carriers: self/runtime.h defines carriers only for hexa_int (:112), hexa_exit (:846), hexa_throw (:847) — there is NO hexa_ptr_realloc, and malloc/realloc/write bind to C-ABI libc. stdlib/runtime/array_typed_leaf.hexa:53-56 declares `extern fn malloc/realloc/write/exit` (f64 twin :30-33 identical). (3) The class-A break is directly visible in the SHIPPED frozen seed: self/native/array_typed_leaf_x86_64.s:50-54 emits `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag` / `call malloc` / `mov r15, rdx # hv: unbox user-call result payload` — libc malloc reads rdi (the TAG=0) as its size argument and returns the pointer in rax, while the seed reads the payload from dead rdx. Same at :241 (call realloc -> mov r10,rdx) and :350 (call write -> mov r10,rdx); arm64 mirrors it (array_typed_leaf_arm64.s:43,56,173,251). Real carrier calls in the same file (call hexa_exit/hexa_throw/hexa_int) are correctly pair-ABI, proving the contrast. (4) Not default-OFF: tool/stage_resolve_runtime_a:575-576 makes it default-ON (only an explicit HEXA_RT_ARRAY_I64_LEAF_NATIVE=0 opts out; when build/array_typed_leaf_native.o is present it exports =1), and :1874-1877 ars the .o into runtime.a with -DHEXA_RT_CORE_ARRAY_I64_LEAF_NATIVE=1, dropping the correct C bodies. The ALLOWED_U line is therefore a genuine gate hole sanctioning the exact class-A ABI mismatch that broke #4930.",
          "confidence": "high"
        }
      },
      {
        "file": "tool/regen_array_typed_leaf_f64_native_s.sh",
        "line": 43,
        "symbol": "ALLOWED_U=\"malloc realloc write hexa_exit hexa_throw\"",
        "klass": "OTHER",
        "evidence": "Same as the i64 twin.",
        "impact": "Gate hole for the f64 leaf.",
        "verdict": {
          "confidence": "high",
          "reason": "CONFIRMED — not refuted. All four refutation avenues are closed by direct reading. (1) tool/regen_array_typed_leaf_f64_native_s.sh:43 is real code, not a comment: ALLOWED_U=\"malloc realloc write hexa_exit hexa_throw\", consumed at ~line 83-85 as the nm U-floor assertion whitelist. (2) The symbols are not all carriers: self/runtime.h:846 (HexaVal hexa_exit(HexaVal)) and :847 (void hexa_throw(HexaVal)) ARE legit HexaVal-ABI carriers, but malloc/realloc/write have NO HexaVal signature anywhere in runtime.h — they are raw libc, exactly the forbidden class-A set. hexa_ptr_realloc genuinely does not exist (runtime.h:872/873 only has hexa_ptr_alloc/hexa_ptr_free), so no carrier was available. (3) The seed is default-ON, not default-OFF: tool/stage_resolve_runtime_a:764 `[ \"${HEXA_RT_ARRAY_F64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0  # explicit opt-out` — the default (x) falls through, assembles the seed, exports =1 (line 811-812) and passes -DHEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE=1 (line 1886), dropping the C bodies. It is on the deploy path. (4) Not local symbols: stdlib/runtime/array_typed_leaf_f64.hexa:30-32 declares `extern fn malloc/realloc/write`, and the FROZEN DEPLOYED seeds actually call them — call-target extraction from self/native/array_typed_leaf_f64_x86_64.s and _arm64.s yields malloc, realloc, write (alongside carriers hexa_exit/hexa_throw/hexa_bool). Live pair-ABI call sites binding to C-ABI libc bodies. BONUS: the same whitelist is duplicated inline at tool/stage_resolve_runtime_a:784 (printf '%s\\n' \" malloc realloc write hexa_exit hexa_throw \"), so the own-obj staging path carries the identical hole — two sites, not one. Only quibble: the class LABEL. The line is the whitelist that blesses class-A externs, not itself an extern call site, so \"OTHER\" (gate-policy category) is defensible rather than a direct class-A instance; the substantive claim (gate hole for the f64 leaf, same as the i64 twin) holds on every axis.",
          "refuted": false
        }
      },
      {
        "file": "tool/regen_array_typed_leaf_native_s.sh",
        "line": 88,
        "symbol": "WARN: cross-assemble check skipped (no matching toolchain)",
        "klass": "OTHER",
        "evidence": "The ENTIRE U-floor check lives inside `if $CC $cc_extra -c \"$s\" -o \"$o\" 2>/dev/null; then` (:76). The else arm (:88-89) prints WARN and the seed is still emitted+kept, exit 0. cc_extra is set ONLY when the host is Darwin (:72-75: `case ... x86_64-linux-gnu) [ \"$(uname -s)\" = Darwin ] && cc_extra=\"-target x86_64-linux-gnu\"`). Identical structure at regen_arr_zeros:85, regen_array_new:85, regen_array_typed_leaf_f64:88, regen_map_query:86.",
        "impact": "THE ESCAPE HATCH. Regen runs on the pool (linux — script header says 'POOL ONLY'). On a Linux x86_64 host: (a) x86_64-linux-gnu → cc_extra empty, host clang assembles → check RUNS; (b) arm64-linux-gnu → cc_extra empty (host is not Darwin) → clang -c of aarch64 asm with an x86 default target FAILS → check SKIPPED; (c) arm64-apple-darwin → Mach-O asm on Linux clang FAILS → check SKIPPED. So 2 of 3 targets are never U-floor-checked on the machine where regen is actually run. This is exactly the 'x86 clean, arm64 undefined slips through' pathway the background describes.",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — could not refute. All four refutation criteria fail to apply, and every load-bearing claim checks out against the real code.\n\n(1) :88-89 is real executable code, not a comment — the `else` arm of `if $CC $cc_extra -c \"$s\" -o \"$o\" 2>/dev/null; then` at :76. It prints WARN, then :89 still prints the success-shaped \"→ $out (globl emitted)\" line. The seed was already written to $out at :68 and is kept. No `exit 1`; emit_one returns 0 and the script proceeds to `[regen_arr_i64] done.`\n\n(2) The ENTIRE U-floor assertion is inside that success arm: nm → awk U-symbols → diff vs ALLOWED_U → `exit 1` (:83-85). If the assemble fails, nothing is asserted.\n\n(3) :72-75 confirmed — `cc_extra` is set ONLY under `[ \"$(uname -s)\" = Darwin ]`, and the `case` has NO arm whatsoever for `arm64-apple-darwin`. On a Linux x86_64 pool host: x86_64-linux-gnu → cc_extra empty, host clang assembles → check RUNS; arm64-linux-gnu → cc_extra empty, aarch64 asm on an x86-default clang → FAILS → skipped; arm64-apple-darwin → Mach-O asm on Linux clang → FAILS → skipped. 1 of 3 checked, 2 of 3 silently waved through. (On a Darwin host all 3 are checked — native + two -target cross — so the hatch is host-polarity-dependent, exactly the \"x86 clean / arm64 slips\" shape.)\n\n(4) Empirical mirror-proof on this arm64-darwin host: `clang -c` of self/native/array_typed_leaf_x86_64.s with NO -target → `error: unknown directive .intel_syntax noprefix` → if-arm fails → WARN path taken. With `-target x86_64-linux-gnu` it assembles and nm yields the U-floor {hexa_exit, hexa_int, hexa_throw, malloc, realloc, write}. The check is functional — it just never runs without a matching toolchain.\n\n(5) NO independent gate exists to compensate. `ALLOWED_U` appears in exactly 5 files repo-wide — the 5 regen scripts themselves (regen_array_typed_leaf, _f64, arr_zeros, array_new, map_query — all structurally identical at :85-90/:88). tool/stage_resolve_runtime_a, the consumer, does ZERO U-floor checking on the .s-seed path (:625-631: grep -vE '^// ' → `$CC -c` → link → done); only its own-obj path (:599-605) has a U gate. So the regen script's check is the SOLE U-floor guard on the frozen .s seeds, and it is the one that silently self-disables.\n\n(6) Script header :18 says \"POOL ONLY (aprime emit is a heavy build)\"; per CLAUDE.md the pool (ghost/aiden/summer) is Linux and there is explicitly \"no arm64 self-hosted host\". So the skip fires on the machine where regen is actually run.\n\nClassification OTHER is correct: this is not a symbol/ABI defect (class A/B) but a gate-structure defect — the guard that would have caught class A/B on arm64 is itself bypassed. Note separately (not this finding): this seed's ALLOWED_U sanctions raw libc `malloc realloc write`, which is itself the class-A set — but the escape-hatch claim stands independently.",
          "confidence": "high"
        }
      },
      {
        "file": "tool/regen_array_typed_leaf_native_s.sh",
        "line": 68,
        "symbol": "} > \"$out\"  (seed written BEFORE the sanity check)",
        "klass": "OTHER",
        "evidence": "The seed file is written to self/native/*.s at :57-68; the cross-assemble + U-floor assertion only starts at :69-76 and the breach path exits 1 at :85. Same ordering in all 5 regen scripts (arr_zeros :65 vs :82, array_new :65 vs :82, f64 :68 vs :85).",
        "impact": "Even when the U-floor gate DOES fire, the poisoned .s is already on disk in the worktree. `exit 1` does not remove it. A subsequent `git add self/native/` (or a regen-all loop where a later target succeeds) commits the rejected seed. The gate is advisory-by-construction against the filesystem.",
        "verdict": {
          "refuted": false,
          "reason": "Read tool/regen_array_typed_leaf_native_s.sh directly: line 68 is real code (`} > \"$out\"`), closing the brace group that writes the seed header + sed-rewritten asm into the TRACKED path self/native/array_typed_leaf_{x86_64,arm64,arm64-linux}.s. The cross-assemble + T-count + U-floor assertion only begins at :69, and the breach path is `exit 1` at :85 — strictly after the write. The only trap (:34) is `rm -rf \"$TMP\"`, and there is no cp/mv/git-checkout/restore of $out anywhere in the script, so a U-floor-rejected seed is left mutated in the worktree; `exit 1` does not undo it. The earlier .globl checks (:50-56) correctly bail BEFORE the write, which makes the post-write placement of the U-floor gate an ordering defect rather than an intentional pattern. Same ordering confirmed in all siblings exactly as cited (arr_zeros 65 vs 82, array_new 65 vs 82, f64 68 vs 85; array_core 76). None of the enumerated refutation avenues apply: the finding makes no symbol-level claim (so carrier/hexa_bool, default-OFF, comment-line, local-symbol are all moot). Classification OTHER is correct — it is neither class A (raw libc extern) nor class B (boxed slow-call). Severity caveat (does not refute): the gate does print ERROR and exit 1, so harm requires a caller that ignores the exit code and then `git add self/native/`; it is a real but process-level/robustness defect, not a silent miscompile.",
          "confidence": "high"
        }
      },
      {
        "file": "tool/stage_resolve_runtime_a",
        "line": 614,
        "symbol": ".s-seed consumption path — NO U-floor check whatsoever",
        "klass": "OTHER",
        "evidence": "For all four array families the frozen-.s path performs exactly two checks: a `.globl` COUNT (`:621` i64, `:688` array_new, `:748` arr_zeros, `:804` f64) and `$CC -c` assembling (`:628`, `:695`, `:755`, `:811`). There is no nm, no U-floor, no allowlist. The nm-based `_bad` U-floor gate exists ONLY inside the own-obj branch (`:600-607`, `:667-674`, `:727-734`, `:783-790`), which is entered only when HEXA_RT_OWNOBJ/HEXA_RT_OWNOBJ_ARR* != 0 (`:589`, `:657`, `:717`, `:773`) — default 0, i.e. never in a normal build.",
        "impact": "GATE HOLE #2, and this is the path that actually SHIPS. The deployed default consumes the committed .s seed, and that consumption path has zero symbol-floor policy. Even if you fixed every ALLOWED_U in tool/regen_*, a hand-edited or stale .s seed would still be assembled and linked with no check. Any future class-A seed enters production through here.",
        "verdict": {
          "refuted": false,
          "reason": "Confirmed on the code, not refuted. (1) tool/stage_resolve_runtime_a:614 is real code (`case \"$tgt\" in`), not a comment. (2) The frozen-.s consumption path for all four array families performs exactly two checks — a `.globl` COUNT grep (:621 i64, :688 array_new, :748 arr_zeros, :804 f64) and `$CC -c` assembling (:628, :695, :755, :811) — then ar's the resulting .o straight into runtime.a (:1874-1877). No nm, no U-floor, no allowlist on that path. (3) The nm-based `_bad` U-floor gate exists ONLY inside the own-obj branch (:600-607, :667-674, :727-734, :783-790), entered only when HEXA_RT_OWNOBJ/HEXA_RT_OWNOBJ_ARR* != 0 (:589, :657, :717, :773) — default 0, so never in a normal build. (4) NOT default-OFF: the resolvers are invoked unconditionally at :4502-4508, and stage_resolve_runtime_a is run by the shipping build (tool/build_aprime.sh:126, tool/build_selfhost.sh:186). The opt-out is `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` — default `x`, so it proceeds; on a present seed it assembles and exports the flag =1. The \"(default-OFF)\" comments mean \"byte-neutral until a .s exists\" — and 3 of 4 seed families ARE committed/tracked (array_typed_leaf_*.s, array_typed_leaf_f64_*.s, arr_zeros_leaf_*.s across all 3 targets); only array_new_leaf_*.s is absent (the #4932 revert). (5) Those committed seeds carry precisely the class-A pattern: call targets in array_typed_leaf_x86_64.s = {hexa_exit hexa_int hexa_throw malloc realloc write}, f64 = {hexa_exit hexa_throw malloc realloc write}, arr_zeros_leaf = {calloc hexa_exit malloc write}. (6) No compensating gate exists: ALLOWED_U is present only in the five tool/regen_*_native_s.sh generators (generation-time, e.g. regen_array_typed_leaf_native_s.sh:43,82-85); grep over .github/workflows/ for ALLOWED_U|U-floor returns empty; no lint scans self/native/*.s. The two symbol-level refutation outs (HexaVal carrier / file-local symbol) do not apply — this is a missing-gate claim, not a symbol claim, and OTHER is the correct class.",
          "confidence": "high"
        }
      },
      {
        "file": "tool/stage_resolve_runtime_a",
        "line": 601,
        "symbol": "inline allowlist \" malloc realloc write hexa_exit hexa_int hexa_throw \"",
        "klass": "OTHER",
        "evidence": "The own-obj U-floor allowlists are hardcoded inline (duplicating, and free to drift from, the regen ALLOWED_U): :601 (i64), :668 `\" calloc hexa_exit malloc write exit \"` (array_new), :728 `\" malloc calloc write exit hexa_exit \"` (arr_zeros), :784 `\" malloc realloc write hexa_exit hexa_throw \"` (f64).",
        "impact": "The one path that DOES check U also whitelists raw libc. So both gates independently sanction class A. Additionally there is no single ALLOWED_U SSOT — 4 inline copies in stage + 5 more in tool/regen_* = 9 places to keep in sync.",
        "verdict": {
          "refuted": false,
          "reason": "반증 실패 — 주장은 실측으로 전부 확인됨. (1) stage_resolve_runtime_a:601은 주석이 아니라 own-obj U-floor 게이트의 실제 코드이며 문자열 \" malloc realloc write hexa_exit hexa_int hexa_throw \"가 그대로 존재(:668/:728/:784 나머지 3개 inline 사본도 인용 그대로 일치). (2) 그 allowlist의 malloc/realloc/write/calloc/exit는 self/runtime.h에 캐리어 선언이 전혀 없는 raw libc C-ABI 심볼 = 클래스 A 그 자체이고, hexa_bool 같은 백엔드 헬퍼도 아니다(runtime.h에 있는 것은 hexa_int:112 · hexa_exit:846 · hexa_throw:847 뿐). (3) default-OFF 반증도 성립 안 함: 동일 문자열이 배포 .s를 생산하는 regen_array_typed_leaf_native_s.sh:43 ALLOWED_U에도 그대로 있고, 배포된 시드가 실제로 raw libc를 호출한다(array_typed_leaf_x86_64.s:52/73 call malloc, :241 call realloc, :350 call write; array_typed_leaf_arm64.s:43/56 bl _malloc, :173 bl _realloc). (4) 로컬 심볼 반증도 실패: array_typed_leaf.hexa:53-58의 extern fn 선언 = 외부 U. (5) SSOT 부재도 사실: inline 4곳 + ALLOWED_U 정의 5곳(regen_array_typed_leaf, _f64, array_new, arr_zeros, map_query) = 9곳 중복. 분류상 시드 소스의 raw extern(클래스 A)도 boxed slow-call(클래스 B)도 아닌 게이트/SSOT 위생 결함이므로 OTHER가 타당.",
          "confidence": "high"
        }
      }
    ],
    "synth": "darwin-arm64에서 **실측**했다. 배포된 `~/.hx/bin/build/runtime.a`(Jul 14 00:16) + 배포된 `~/.hx/bin/self/runtime.h` 프로토타입으로 C 드라이버를 링크했다. 하니스: `/private/tmp/claude-501/-Users-mini-dancinlab-hexa-lang/adbce6b7-54f3-4f01-a7fe-77d3f277b3f0/scratchpad/abi/drv{,2,4,5,6,7}.c`\n\n---\n\n# 0. 헤드라인 — realloc 갭은 **2차 문제**다. 1차는 아무도 못 본 **스칼라 ABI 미러 브레이크**다\n\n세 에이전트 모두 \"시드→libc egress(클래스 A)\"만 봤다. 실측해보니 **시드→호출자 ingress/return 도 깨져 있다.** 캐리어 치환(#4935 레시피)만으로는 i64/f64 시드가 **안 고쳐진다.**\n\n**실측 A — 같은 아카이브 안의 A/B 대조 (이게 결정적이다):**\n\n| 아카이브 멤버 | 그 멤버의 U-floor (nm 실측) | C 프로토타입대로 호출한 결과 |\n|---|---|---|\n| `array_new_leaf_native.o` | `hexa_bool hexa_exit **hexa_ptr_alloc**` (#4935 fixed) | `hexa_array_new()` → **OK** tag=5 len=0 |\n| `arr_zeros_leaf_native.o` | `hexa_bool hexa_exit **hexa_ptr_alloc**` (#4935 fixed) | `hexa_arr_zeros_leaf(hexa_int(4))` → **OK** tag=5 len=4 |\n| `array_typed_leaf_native.o` | `hexa_bool hexa_exit **hexa_int hexa_throw malloc realloc write**` | `hexa_arr_i64_new(3)` → **SIGSEGV 139 (3/3)** |\n| `array_typed_leaf_f64_native.o` | 동일 | `hexa_arr_f64_new(3)` → **SIGSEGV 139** |\n\n→ #4935 캐리어 레시피는 **작동한다**(zeros/array_new가 살아있는 증거). i64/f64만 안 고쳐졌다.\n\n**실측 B — malloc과 무관하게 미러가 깨져 있음을 격리 (drv4/drv5):**\nC에서 손으로 **완전히 유효한** `HexaArrI64{data,len=3,cap=4}`를 만들어 넘겼다. 시드 malloc은 한 번도 안 탄다.\n\n```\nhexa_arr_i64_len(v)  -> 0        (진짜 len = 3)          ← RETURN 미러 브레이크\nhexa_arr_i64_box(v,1) -> \"index out of bounds\" (exit 1)  ← PARAM 미러 브레이크\n```\n\n근인 (frozen .s 바이트 + codegen 소스, 둘 다 읽음):\n- 시드 ingress는 **모든 파라미터를 (tag,payload) 쌍**으로 읽는다 — `array_typed_leaf_x86_64.s:hexa_arr_i64_new` → `mov [rbp-128], rdi # store tag L0` / `mov rbx, rsi # ingress param payload`. `cap: int` 인데 **rsi에서 읽는다.**\n- 반면 C 선언은 **C-ABI 스칼라**다 — `self/runtime_core_emit.hexa:1126-1129`:\n  ```c\n  HexaVal hexa_arr_i64_new(int cap);           // cap → rdi 단독\n  HexaVal hexa_arr_i64_push(HexaVal v, int64_t x);  // x → rdx 단독\n  int     hexa_arr_i64_len(HexaVal v);         // 결과 → rax 단독\n  HexaVal hexa_arr_i64_box(HexaVal v, int64_t i);   // i → rdx 단독\n  ```\n- 네이티브 백엔드도 **똑같이 C-ABI로** 부른다 — `compiler/codegen/x86_64_linux.hexa:3235-3239` 주석 그대로: *\"hexa_arr_i64_new(int cap): cap in rdi (single C-ABI int reg); returns HexaVal pair rax:rdx\"*.\n- 시드는 `len`을 **rdx에 넣고 rax엔 태그(0)** 를 넣는다(`array_typed_leaf_x86_64.s` len 에필로그: `mov rdx, r14 # hv arg payload` / `mov rax, [rbp-80] # tag L3`). C 호출자는 eax를 읽는다 → **항상 0**.\n\n**이 클래스를 트리는 이미 알고 있고 해법도 갖고 있다:** `self/runtime_core_emit.hexa:1789-1860`의 `__hxseed_*` / `__hxw_*` 브리지 (arena 10심볼). 주석이 이 버그를 문자 그대로 서술한다 — *\"HexaVal is {tag@0, payload@8}, so the SysV 16-byte struct ABI passes/returns in EXACTLY those rdi:rsi / rax:rdx pairs. Alias each seed symbol to its true HexaVal signature… and wrap it in a `__hxw_` static-inline\"*. **array_core는 이 브리지를 썼고, array_typed_leaf는 안 썼다.** zeros/array_new는 인자가 전부 HexaVal(또는 0-arg)이라 미러가 애초에 없어서 캐리어만으로 고쳐진 것이다.\n\n---\n\n# 1. 클래스 잔존 census 최종본\n\n## 1-A. 클래스 A (raw libc egress) — **3개 시드, 전부 default-ON, 실측 확인**\n\n| 파일 | 심볼 | 배포 | 증상 |\n|---|---|---|---|\n| `stdlib/runtime/array_typed_leaf.hexa:53-57` | `malloc realloc write exit hexa_int` | **default-ON** (`stage_resolve_runtime_a:575` `:-x`≠0, 3타깃 .s 전부 tracked, `:4502`에서 무조건 호출, `install.sh:648`이 실행) | **SIGSEGV 3/3** (실측) |\n| `stdlib/runtime/array_typed_leaf_f64.hexa:30-34` | `malloc realloc write exit` | **default-ON** (`:764`) | **SIGSEGV** (실측) |\n| `stdlib/runtime/arr_zeros_leaf.hexa:25` | `malloc calloc write exit` | default-ON (`:707`) — **origin/main엔 아직 깨진 채** (`git show origin/main:self/native/arr_zeros_leaf_arm64.s` → `bl _calloc`) | #4935가 고침 (실측 PASS) |\n| `stdlib/runtime/array_new_leaf.hexa:23` | `calloc` | main엔 .s 부재 → inert. **regen 스크립트가 다시 만들면 재무장** | #4935가 고침 (실측 PASS) |\n\n`hexa_int`은 **클래스 A가 맞다** — `self/runtime.h:112 HexaVal hexa_int(int64_t n)` = **스칼라 C 인자**. 시드가 pair-ABI로 부르면 rdi=태그(0)를 읽는다 → `hexa_arr_i64_box`는 **모든 원소를 INT 0으로** 박싱한다. `hexa_bool`은 무해가 맞다(코드젠이 직접 방출, `arm64_darwin.hexa:_emit_bool_box`가 payload를 x0에 먼저 넣음) — 단 **시드 소스가 `hexa_bool(x)`을 직접 쓰면 같은 함정에 빠진다.**\n\n## 1-B. **★ 신규: 클래스 A가 array_new/zeros 말고 \"다른 곳에도\" 있다 — 3번째 사이트**\n\n세 에이전트 모두 \"다른 시드 패밀리는 clean\"이라고 답했다. **틀렸다.**\n\n`self/runtime_core_emit.hexa:2714-2715` (C 쪽):\n```c\n#ifdef HEXA_RT_ARRAY_ARENA_NATIVE\n    extern int64_t rt_array_arena_alloc_items_native(int64_t n);   // ← C-ABI 스칼라\n    return (HexaVal*)(uintptr_t)rt_array_arena_alloc_items_native((int64_t)n);\n```\n시드 쪽 `stdlib/runtime/array_core.hexa:76-79`:\n```hexa\npub fn rt_array_arena_alloc_items_native(n: int) -> int {\n    let bytes = n * 16        // ← 평문 `*` = 클래스 B (boxed hexa_mul)\n    return hexa_arena_alloc(bytes)\n}\n```\n**실측 (drv6, 배포 runtime.a):**\n```\nrt_array_arena_alloc_items_native(4)  →  \"cannot multiply non-numeric operand (tag 4 * tag 0)\"  exit 1\n```\nn=4가 **태그**로 들어가 TAG_VOID(4)가 되고, 평문 `*`가 boxed `hexa_mul`을 불러 throw한다. **클래스 A + 클래스 B가 한 함수에 동시에.** 게이트 `HEXA_RT_ARRAY_ARENA_NATIVE`는 배포 빌드에서 **켜져 있다**(`nm runtime.o` → `U _rt_array_arena_alloc_items_native`).\n\n**단, 현재 LIVE는 아니다:** 호출부가 `hexa_array_arena_on() && __hexa_val_mark_top > 0` 뒤에 있고, 내가 만든 사용자 프로그램들은 `mark_top==0`이라 도달하지 못했다(t1/t2 정상 실행). **잠복 지뢰**로 분류 — val-arena mark를 켜는 무엇이든 이걸 하드 throw로 바꾼다. 이 파일은 census가 \"clean/hot\"으로 분류한 바로 그 시드다.\n\n## 1-C. 클래스 B — i64/f64 시드는 clean, **array_core는 아니다**\n\n`array_typed_leaf{,_f64}.hexa`의 모든 오프셋/비교는 `__hx_payload_*` 리프다 (확인). 반면 `array_core.hexa:77`의 `n * 16`은 평문 연산자다 → 위 실측이 그 boxed 콜을 잡았다.\n\n## 1-D. \"터지나 조용히 틀리나\"\n\n- `hexa_arr_{i64,f64}_new` → **SIGSEGV** (실측)\n- `hexa_arr_i64_len` → **조용히 0** (실측) — 크래시 없음, 모든 packed 배열 길이가 0\n- `hexa_arr_i64_box` → **잘못된 throw** \"index out of bounds\" (실측)\n- `hexa_arr_i64_box`의 `hexa_int` → 그 앞이 먼저 터져서 도달 못 했지만 정적으로 확실: 모든 원소 → INT 0\n- `rt_array_arena_alloc_items_native` → **throw** (도달 시)\n\n## 1-E. **실측으로 반증된 것 — \"지금 main이 유저에게 터지고 있다\"는 아직 미증명**\n\n배포 컴파일러(v0.574.1)로 `let xs=[10,20,30]; print(xs[1])`, 루프-합, `HEXA_NATIVE_ARR=1` 세 형태를 빌드해봤다 — **생성된 C에 `hexa_arr_i64_*` 호출이 하나도 안 나온다** (전부 boxed `hexa_array_new/push/index_get`). 즉 **심볼은 확실히 깨졌지만, 기본 emit이 그걸 부르는 shape를 나는 못 만들었다.** \n- gen2 autofire (`self/codegen.hexa:14705 _native_arr_autofire_ok`) = 비-escape 증명 시 **env 없이 발화**하도록 되어 있는데 발화 안 함.\n- 네이티브 백엔드 (`_pack_array_enabled()` = `env(\"HEXA_PACK_ARRAY\") != \"0\"` = **DEFAULT-ON**, `x86_64_linux.hexa:1759-1762`)가 유력 발화 경로인데 darwin에서 빌드 2분 타임아웃(#4483 doomed cross-compile)으로 확인 실패.\n- **`alloc-copy-free` 에이전트의 \"HEXA_NATIVE_ARR default-OFF라 호출자 0 = 휴면\"은 정확하지 않다** — `_native_arr_autofire_ok`는 env 없이도 발화하도록 설계돼 있고 `_pack_array_enabled`는 별개의 default-ON 플래그다. 그러나 **내 실측에서도 발화하지 않았다**. 이 항목은 **미해결**이며 §5에 남긴다.\n\n---\n\n# 2. 게이트 구멍 최종본\n\n| # | 구멍 | 좌표 | 지금 막나? |\n|---|---|---|---|\n| 1 | **ALLOWED_U가 raw libc를 명시 허용** — 설계상 클래스 A를 절대 못 잡는다 | `tool/regen_array_typed_leaf_native_s.sh:43` = `\"malloc realloc write hexa_exit hexa_int hexa_throw\"` · `_f64:43` · `regen_arr_zeros_leaf:41` · `regen_array_new_leaf:41` | ❌ |\n| 2 | **`hexa_int`가 carrier keeplist에 들어있음** — 클래스 A를 게이트가 축복 | 위 + `stage_resolve_runtime_a:601` 인라인 allowlist | ❌ |\n| 3 | **.s 시드 소비 경로엔 U-floor 검사가 아예 없다** — 실제로 SHIP되는 경로 | `stage_resolve_runtime_a:614-628` (`$CC -c \"$s_tmp\"` 직행) | ❌ |\n| 4 | **크로스어셈블 스킵 = x86-only 그린** | `regen_*.sh:88` `WARN: cross-assemble check skipped` — 리눅스 풀에서 arm64 2타깃은 **검사가 아예 안 돈다** | ❌ |\n| 5 | **거절된 .s가 이미 디스크에 있다** — `} > \"$out\"` 이 sanity check **앞에서** 실행 | `regen_*.sh:68` | ❌ |\n| 6 | **ABI 미러 검사가 어디에도 없다** — U-floor가 clean해도 §0의 미러 브레이크는 통과 | 전무 | ❌ |\n| 7 | **행동 스모크 0** — regen도 stage도 시드를 **한 번도 실행하지 않는다** | 전무 | ❌ |\n| 8 | ALLOWED_U SSOT 부재 — 9곳 중복 | regen ×5 + stage 인라인 ×4 | ❌ |\n| 9 | 주석이 거짓 — `array_typed_leaf.hexa:15-17` *\"the seed … CALLS libc realloc as an extern **C-ABI symbol**\"* · `stage:570,4502` *\"Default-OFF byte-neutral\"* · `runtime_core_emit:2700` *\"native→native edge needs no C bridge\"* (호출부는 **C다**) | — | ❌ |\n\n**요약: 이 클래스를 막는 게이트는 현재 0개다.** #6·#7이 진짜 구멍이다 — ALLOWED_U를 아무리 조여도 미러 브레이크는 U-floor에 아무 흔적도 안 남긴다(모든 심볼이 정상 carrier로 보인다).\n\n---\n\n# 3. realloc 갭 결정\n\n## 3-0. 먼저, **route-c는 실측 반증됐다**\n\n```\nnm -A ~/.hx/bin/build/runtime.a | grep hxlcl_\n  runtime.o: ... t _hxlcl_malloc     ← 소문자 t = STB_LOCAL (static)\n  runtime.o: ... t _hxlcl_realloc\n  runtime.o: ... t _hxlcl_calloc\n  runtime.o: ... t _hxlcl_write\n```\n배포 아카이브에 **shim TU 멤버 자체가 없고**, `hxlcl_*`는 전부 **file-local static**이다. 시드가 `extern fn hxlcl_realloc`을 선언하면 **undefined symbol → 링크 실패**(혹은 memory의 STB_LOCAL floor-pull 불가시성 버그). route-c 에이전트의 핵심 전제 — *\"runtime.a의 shim 멤버가 그 심볼들을 전역 C-ABI 심볼로 이미 export한다\"* — **FALSE**. 추가로 `HEXA_CABI_HXLCL` default-OFF env 의존 = frozen .s에 보이지 않는 불변식. **탈락.**\n\n## 3-1. **★ carrier-add(hexa_ptr_realloc)는 명세대로 하면 새 크래시를 만든다 — 실측**\n\n```\ndrv7: hexa_ptr_alloc(64) 두 번  →  p1=10960c018  p2=10960c058  delta=64\n```\n**`hexa_ptr_alloc`은 링크에서 ARENA bump 바디가 이긴다** (`self/rt/alloc.hexa:183` `return hexa_arena_alloc(size)`; `alloc_syscall_native.o`에 `T _hexa_ptr_alloc` — `runtime.o`의 malloc-backed `T _hexa_ptr_alloc`과 **중복 정의**인데 arena가 이겼다). 그리고 `self/rt/alloc.hexa:187 hexa_ptr_free → return 0` = **완전 NO-OP**.\n\n한편 carrier-add 에이전트가 권고한 바디 `realloc((void*)p, nsz)`는 배포 `runtime.c:2876`의 `#define realloc(p,n) hxlcl_realloc(...)`으로 확장되고, `hxlcl_realloc`(runtime.c:1224)은\n```c\nsize_t old_n = *(size_t *)((char *)p - HXLCL_HDR_BYTES);   // p-16 에서 헤더를 읽는다\n```\n**arena bump 포인터엔 그 16B 헤더가 없다** → `old_n` = 쓰레기 → `copy_n = min(n, 쓰레기)` → **와일드 memcpy → SIGSEGV/SIGABRT.** memory `zeroc #29 realloc darwin SIGABRT`(\"shim-only -D vs runtime_core bare free\")와 **정확히 같은 축의 새 버그**를 만든다. 살리려면 `runtime.c`와 `self/rt/alloc.hexa` **양쪽에** 바디를 넣어야 하고(= arena 쪽은 결국 alloc+copy), `hexa_ptr_alloc`의 중복 정의 위험도 그대로 상속한다.\n\n## 3-2. **alloc-copy-free의 성능 반론도 실측 반증됐다**\n\nalloc-copy-free 에이전트는 *\"glibc realloc의 in-place/mremap 0-복사를 포기한다 → 바이트당 60-100× 느리다\"* 라고 했다. **레퍼런스가 glibc realloc이 아니다.** C 바디가 실제로 부르는 것은 `hxlcl_realloc`(배포 runtime.c:1224-1239):\n```c\nstatic void *hxlcl_realloc(void *p, size_t n) {\n    if (!p) return hxlcl_malloc(n);\n    void *np = hxlcl_malloc(n);                        // ★ 항상 새로 할당\n    size_t copy_n = ...;\n    for (size_t i = 0; i < copy_n; i++) d[i] = s[i];   // ★ 바이트 1개씩 복사\n    return np;                                          // ★ 옛 버퍼 free 안 함\n}\n```\n**in-place 없음. mremap 없음. 바이트 루프. 회수 없음.** 즉 시드의 alloc+**워드**-copy+free-noop은 C 바디의 **reference-match**이고, 8B 워드 복사이므로 **오히려 빠르다.** arena 잔류 우려도 무의미하다 — C 바디가 이미 절대 회수하지 않는다.\n\n## 3-3. 4축 채점\n\n| 축 | carrier-add | **alloc-copy-free** | route-c |\n|---|---|---|---|\n| ① 완성도 | △ grow는 닫지만 **arena 바디 없이는 새 크래시**(실측). 살리려면 바디 2개 + decl + native-rt 3-리스트 | **◎ 새 심볼 0, 새 floor 진입 0, C 바디(hxlcl_realloc) 의미론 1:1 일치** | ✗ 링크 실패 (실측) |\n| ② 단순 | △ 5곳 배선, 심볼 1개 추가, 중복정의 리스크 상속 | **◎ 시드 2파일만. runtime/decl/native-rt 무변경** | △ env + ALLOWED_U 재개방 |\n| ③ 안전 | ✗→△ `p-16` 헤더 부재로 와일드 memcpy (실측 근거) | **◎ #4935가 3타깃 실측 통과시킨 캐리어 3개만 사용. 앨로케이터 분기 0** (리스크: 새 루프의 클래스 B — 게이트로 막음) | ✗ raw-libc 이름을 정책에 되돌림 |\n| ④ 표준 | △ carrier canon엔 맞으나 \"bump/no-free 위에 realloc\"은 의미론적 거짓말 + 같은 이름 두 앨로케이터 | **◎ 트리의 canon 그대로: libc egress = HexaVal carrier(#4935). 레퍼런스(hxlcl_realloc)가 하는 일을 그대로 함** | ✗ polarity 반전 |\n\n## 3-4. **권고 = alloc-copy-free (Option A)** — ①+④ 기준\n\n**단, 이것만으로는 안 고쳐진다.** §0의 미러 브레이크가 남는다. 최종안은 **2-파트**다:\n\n- **파트 1 (ABI 미러)** — 트리에 이미 있는 arena 패턴(`runtime_core_emit.hexa:1818-1860`) 이식: 시드 export를 `hxseed_arr_i64_*`로 rename → pair-ABI · runtime_core.c가 **얇은 C-ABI 래퍼**로 공개 이름 `hexa_arr_i64_*`를 유지 (박싱/언박싱). **codegen 0줄 변경 → byteeq 중립.**\n- **파트 2 (libc egress)** — `malloc/realloc/write/exit/hexa_int` → `hexa_ptr_alloc` + copy-loop + `hexa_ptr_free` + `hexa_exit` + `__hx_make_val(0,x)`.\n\n---\n\n# 4. 구현-ready 스펙\n\n## 4-1. 파트 1 — ABI 미러 브리지\n\n**`self/runtime_core_emit.hexa`** (guard-ON 팔, 현행 `:2765-2768` extern 선언 블록을 교체):\n```c\n/* 시드는 pair-ABI(HexaVal)만 방출한다 — arena(:1818)와 같은 __hxseed_ 별칭 */\n#if defined(__APPLE__)\nextern HexaVal __hxseed_arr_i64_new (HexaVal cap)            __asm__(\"_hxseed_arr_i64_new\");\nextern HexaVal __hxseed_arr_i64_push(HexaVal v, HexaVal x)   __asm__(\"_hxseed_arr_i64_push\");\nextern HexaVal __hxseed_arr_i64_len (HexaVal v)              __asm__(\"_hxseed_arr_i64_len\");\nextern HexaVal __hxseed_arr_i64_box (HexaVal v, HexaVal i)   __asm__(\"_hxseed_arr_i64_box\");\n#else\n... (언더스코어 없는 판 — :1829 블록 그대로 미러)\n#endif\n/* 공개 C-ABI 표면은 불변 → 네이티브/gen2 호출자 모두 무변경 = byteeq 중립 */\nHexaVal hexa_arr_i64_new (int cap)                { return __hxseed_arr_i64_new(hexa_int((int64_t)cap)); }\nHexaVal hexa_arr_i64_push(HexaVal v, int64_t x)   { return __hxseed_arr_i64_push(v, hexa_int(x)); }\nint     hexa_arr_i64_len (HexaVal v)              { return (int)HX_INT(__hxseed_arr_i64_len(v)); }\nHexaVal hexa_arr_i64_box (HexaVal v, int64_t i)   { return __hxseed_arr_i64_box(v, hexa_int(i)); }\n```\nf64 미러: `:3062-3066` 동일 (`push_bits`도 `hxseed_arr_f64_push_bits(HexaVal, HexaVal)`).\n⚠️ `__asm__(...)` 문자열은 Mach-O에서 **리터럴**이다 — `__APPLE__` 분기 필수(`:1806-1815` 교훈).\n\n**시드 시그니처** (`stdlib/runtime/array_typed_leaf.hexa:62/76/102/110`, `_f64:38/53/79/86`) — 이름 rename + 전 인자/반환 HexaVal화:\n```hexa\npub fn hxseed_arr_i64_new(cap: HexaVal) -> HexaVal\npub fn hxseed_arr_i64_push(v: HexaVal, x: HexaVal) -> HexaVal\npub fn hxseed_arr_i64_len(v: HexaVal) -> HexaVal\npub fn hxseed_arr_i64_box(v: HexaVal, i: HexaVal) -> HexaVal\n```\n(본문은 이미 전부 `__hx_payload_*` 리프라 payload 추출은 그대로 동작한다.)\n\n**심볼 계약 갱신**: `regen_array_typed_leaf_native_s.sh:37` `SYMS=` · `stage_resolve_runtime_a:614`의 `.globl` 카운트 grep · `:592`의 `_kg=` (own-obj keep-global) — 4곳 전부 새 이름으로.\n\n## 4-2. 파트 2 — libc egress (Option A)\n\n**`stdlib/runtime/array_typed_leaf.hexa:53-58` → 교체:**\n```hexa\nextern fn hexa_ptr_alloc(size: HexaVal) -> HexaVal     // self/runtime.h:872\nextern fn hexa_ptr_free(ptr: HexaVal, size: HexaVal) -> HexaVal  // :873\nextern fn hexa_exit(code: HexaVal)                     // :846\nextern fn hexa_throw(v: HexaVal)                       // :847\n```\n**사이트별:**\n- `:65` `malloc(16)` → `hexa_ptr_alloc(16)` — **HexaArrI64 16B는 data@0/len@8/cap@12를 전부 명시 write하므로 zeroing 불필요** (array_new의 4워드 zero와 다른 점)\n- `:67` `malloc(bytes)` → `hexa_ptr_alloc(bytes)`\n- `:84-89` realloc-grow → **alloc + word-copy + free**:\n```hexa\nlet ncap   = __hx_payload_mul(cap, 2)\nlet old    = __hx_ptr_load64(a, 0)\nlet obytes = __hx_payload_mul(cap, 8)\nlet nbytes = __hx_payload_mul(ncap, 8)\nlet nd     = hexa_ptr_alloc(nbytes)\nif __hx_payload_eq(nd, 0) { hexa_exit(1) }        // ★ 평문 `nd == 0` 절대 금지 (클래스 B)\nlet mut i  = 0\nlet mut go = __hx_payload_lt(i, len)              // len 워드만 (grow 시점 len==cap)\nwhile go {\n    let off = __hx_payload_mul(i, 8)\n    __hx_ptr_store64(nd, off, __hx_ptr_load64(old, off))\n    i  = __hx_payload_add(i, 1)\n    go = __hx_payload_lt(i, len)\n}\nhexa_ptr_free(old, obytes)\n__hx_ptr_store64(a, 0, nd)\n__hx_ptr_store32(a, 12, ncap)\n```\n  (`write(2,msg,20)` DROP — 현재 `write(0,(void*)2,0)`으로 **아무것도 출력 안 한다**. `exit(1)`은 태그를 넘겨 **exit 0 = 성공**으로 나간다. 둘 다 `hexa_exit(1)`로 대체 = zeros/array_new가 이미 취한 parity delta)\n- `:118` `hexa_int(x)` → **`__hx_make_val(0, x)`** (TAG_INT=0, 순수 리프, 콜 없음)\n\n**`stdlib/runtime/array_typed_leaf_f64.hexa`**: `:30-34` extern 블록(`hexa_int` 없음), `:41,:43` malloc, `:61-66` grow — 동일. `hexa_arr_f64_box`는 이미 `__hx_bits_f64` 리프 → 무변경.\n\n**`self/rt/alloc.hexa` 무변경** — arena `hexa_ptr_alloc`(bump) + `hexa_ptr_free`(no-op)는 C 바디의 hxlcl_realloc(alloc+copy+no-free)과 **의미론이 같다**. ★ 이게 Option A가 앨로케이터 분기를 못 만드는 이유다.\n\n## 4-3. 게이트 (구멍 1~7 전부)\n\n`tool/regen_array_typed_leaf_native_s.sh:43` / `_f64:43`:\n```bash\nALLOWED_U=\"hexa_ptr_alloc hexa_ptr_free hexa_exit hexa_throw hexa_bool\"\nDENY_U=\"malloc calloc realloc free write exit hexa_int hexa_eq hexa_mul hexa_add_slow hexa_cmp_ge\"\n```\n1. **DENY_U를 툴체인 무관하게 항상 실행** — `.s` 텍스트를 직접 스캔(`grep -E '^\\s*(call|bl)\\s+_?(malloc|calloc|...)'`). `:88`의 cross-assemble 스킵 경로에서도 반드시 돌 것 (구멍 #4).\n2. **sanity check를 `$out` 쓰기 전으로** — `:68`의 `} > \"$out\"` 앞에 검사 (구멍 #5).\n3. **`stage_resolve_runtime_a:614`의 .s 소비 경로에 동일 U-floor + DENY 검사 추가** (구멍 #3 — 실제로 SHIP되는 경로).\n4. **★ ABI 미러 린트 (구멍 #6)**: 시드의 `pub fn` 시그니처가 **전 인자·반환 HexaVal**이 아니면 regen FAIL. `grep -nE '^pub fn .*\\((.*: *(int|float|bool|string)|.*\\) *-> *(int|float|bool|string))'` → hit = FAIL.\n5. **★ 행동 스모크 (구멍 #7)** — 아래.\n\n## 4-4. regen + 검증\n\n```bash\n# 풀(summer/aiden)에서 — mini는 git/gh 전용\ntool/regen_array_typed_leaf_native_s.sh all\ntool/regen_array_typed_leaf_f64_native_s.sh all\n# → self/native/array_typed_leaf{,_f64}_{x86_64,arm64,arm64-linux}.s  6개, ONE commit\n```\n⚠️ **반드시 6개를 한 커밋에** — 클래스 B는 x86에서 인라인 접히고 arm64에서만 `bl hexa_eq`가 뜬다(실측 前例). x86-only 그린 = false green.\n\n**드라이버 스모크 (신규, `test/native_build/array_typed_leaf_abi_smoke.c`) — 무엇을 단언해야 하는가:**\n\n| 단언 | **구 시드 (현행)** | **신 시드 (기대)** |\n|---|---|---|\n| `hexa_arr_i64_new(3)` 반환 | **SIGSEGV 139** (실측) | exit 0, `tag==5`, `arr_ptr!=NULL` |\n| `hexa_arr_i64_len(손수-만든 len=3)` | **0** (실측) | **3** |\n| `hexa_arr_i64_box(손수-만든 v, 1)` | **throw \"index out of bounds\"** exit 1 (실측) | `tag==0(INT)`, `payload==8` |\n| 32회 push (cap 1→32, grow 5회) | SIGSEGV | `len==32` && `box(v,k)==k` ∀k |\n| `hexa_arr_f64_new/push_bits/len/box` | SIGSEGV | 비트-정확 왕복 |\n| nm U-floor | `malloc realloc write hexa_int` 포함 | `{hexa_ptr_alloc, hexa_ptr_free, hexa_exit, hexa_throw, hexa_bool}` **만** |\n\n→ **이 스모크는 반드시 `runtime.h`의 공개 C 프로토타입으로 호출해야 한다** (pair-ABI 드라이버로 부르면 미러 브레이크를 놓친다 — 그게 #4918이 그린이었던 이유다). 3타깃 CI에서 실행.\n\n## 4-5. byteeq 리스크\n\n- **codegen 0줄** → 모든 프로그램의 emit 바이트 불변 → **gen3≡gen4 유지.**\n- runtime.o에 얇은 C-ABI 래퍼 4+4개 추가 → runtime.o 바이트 변경 (byteeq 대상 아님). \"runtime.c→0\"에서 ~16줄 후퇴 — **정직한 트레이드오프**(바디는 네이티브, 래퍼만 C).\n- 시드 .s 6개 churn → byteeq 3타깃 + faithful-nobaseline 필수.\n- nm-UND libc floor: 시드당 `malloc/realloc/write/exit` **-4** (`write`/`exit`는 `nobaseline-gate.yml:446` 제외 목록에 없으므로 **게이트 숫자도 실제로 내려간다**). 신규 심볼 **0개**.\n\n---\n\n# 5. 모르는 것 · 다음에 볼 곳\n\n1. **★ 최대 미해결 — \"지금 main이 유저에게 터지고 있나?\"** 심볼은 확실히 깨졌다(SIGSEGV/0/throw, 실측). 그런데 **배포 컴파일러의 기본 emit이 그 심볼을 부르는 shape를 나는 못 만들었다** (3 형태 시도, 전부 boxed emit). 다음: 풀(리눅스 x86_64)에서 **네이티브 백엔드**(`_pack_array_enabled()` DEFAULT-ON, `x86_64_linux.hexa:1759`)로 `let xs=[1,2,3]` 프로그램을 빌드 → `objdump | grep 'call hexa_arr_i64'` → 실행. **여기서 발화하면 main은 SHIP-BROKEN이고 즉시 hotfix다.** darwin에선 native 빌드가 2분 타임아웃(#4483)이라 확인 불가였다.\n2. **`hexa_ptr_alloc` 중복 정의** — `runtime.o`(malloc-backed)와 `alloc_syscall_native.o`(arena)가 **둘 다 `T`로 정의**한다. darwin에선 arena가 이겼다(실측 delta=64). **링크 순서에 의존하는 미정의 상태**이며, 3타깃 전부에서 같은 쪽이 이기는지 미확인. 다음: 3타깃 `nm -A build/runtime.a | grep ' T _\\?hexa_ptr_alloc'` + drv7 bump 테스트.\n3. **`rt_array_arena_alloc_items_native` 지뢰의 도달 조건** — `__hexa_val_mark_top > 0`이 언제 참이 되는지 못 밝혔다(`HEXA_VAL_ARENA=1`로도 유저 프로그램에선 0). 컴파일러 자신(self-host)의 실행에서 참이 될 수 있다. 다음: `runtime_core_emit.hexa`에서 `__hexa_val_mark_top` push 사이트를 역추적 + `HEXA_RT_ARRAY_ARENA_NATIVE=1` 빌드로 self-transpile.\n4. **나머지 시드 패밀리 미러 전수조사 미완** — 나는 `stdlib/runtime/*.hexa`의 `pub fn` 시그니처를 스캔해 스칼라 파라미터 보유 시드를 3개(array_core, array_typed_leaf ×2) 찾았고 그 중 array_core만 추가 검증했다. `map_query.hexa`, `intern_core`, `fs_core`, `num_core`, `valop_core` 등은 **C 쪽 선언과 짝지어 확인하지 않았다**. §4-3의 \"ABI 미러 린트\"를 먼저 붙이고 **전 시드에 돌리는 것**이 이 클래스의 다음 라운드다.\n5. **MULTIOBJ shim TU** — 배포 아카이브엔 shim 멤버가 없었지만 `HEXA_RT_MULTIOBJ` 빌드는 `hxlcl_*`를 전역으로 export할 수 있다(`self/runtime_core_hxlcl_shim_emit.hexa`). route-c의 전제가 **그 구성에서는** 성립할 여지가 있다. 다음: 3타깃 MULTIOBJ 빌드 후 `nm build/runtime.a | grep -E ' T (hxlcl_realloc|hxlcl_malloc)'`.\n6. **성능 미측정** — Option A의 워드-copy가 `hxlcl_realloc`의 바이트-loop보다 빠를 것이라는 건 코드 읽기 기반 추론이다. flip 전에 summer에서 push N=1e3/1e5/1e7 3-way(현행 C 바디 / Option A 시드) 마이크로벤치.\n\n**수정한 파일: 없음 (읽기 전용).**"
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Census"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Design"
    },
    {
      "type": "workflow_phase",
      "index": 4,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "census:typed-leaf",
      "phaseIndex": 1,
      "phaseTitle": "Census",
      "agentId": "a53468d49325798b3",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783954800456,
      "queuedAt": 1783954800445,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "감사 결과 요약 (읽기 전용 · 코드 수정 없음). CLASS A (raw l…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955084689,
      "tokens": 81714,
      "toolCalls": 15,
      "durationMs": 284233,
      "resultPreview": "{\"findings\":[{\"file\":\"stdlib/runtime/array_typed_leaf.hexa\",\"line\":53,\"symbol\":\"extern fn malloc(n: int) -> int\",\"klass\":\"A-raw-libc\",\"evidence\":\"array_typed_leaf.hexa:53 declares raw libc malloc. Emitted seed self/native/array_typed_leaf_x86_64.s:50-54 — `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default = TAG_INT` / `call malloc` / `mov [rbp-160],rax # store tag L4` / `mov r15,rdx # hv: un…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "census:other-seeds",
      "phaseIndex": 1,
      "phaseTitle": "Census",
      "agentId": "a407803d9c2cb88d8",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783954800456,
      "queuedAt": 1783954800445,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "## 결론 요약 **브리핑 질문 — \"array_new/zeros/typed-leaf 말…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955688688,
      "tokens": 150108,
      "toolCalls": 60,
      "durationMs": 888232,
      "resultPreview": "{\"findings\":[{\"file\":\"stdlib/runtime/array_typed_leaf.hexa\",\"line\":57,\"symbol\":\"hexa_int\",\"klass\":\"A-raw-libc\",\"evidence\":\"★ NEW — not in the known list. `extern fn hexa_int(x: int) -> HexaVal` (array_typed_leaf.hexa:57) is declared as a plain extern, so aprime lowers the call at the generic pair-ABI. But the callee is a C-ABI SCALAR fn: self/runtime_core.c:1599 `HexaVal hexa_int(int64_t n) { retu…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "census:gate-holes",
      "phaseIndex": 1,
      "phaseTitle": "Census",
      "agentId": "a36829a5482b55dc3",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783954800456,
      "queuedAt": 1783954800445,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "전수 census 완료. 핵심 판정 3줄 요약: 1. **게이트가 버그를 …",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955115927,
      "tokens": 93709,
      "toolCalls": 17,
      "durationMs": 315470,
      "resultPreview": "{\"findings\":[{\"file\":\"stdlib/runtime/array_typed_leaf.hexa\",\"line\":53,\"symbol\":\"extern fn malloc / realloc / write / exit\",\"klass\":\"A-raw-libc\",\"evidence\":\"array_typed_leaf.hexa:53-56 declares `extern fn malloc(n: int) -> int` / `realloc(p,n)` / `write(fd,buf,n)` / `exit(code)`. The frozen seeds prove the calls survive to native: `self/native/array_typed_leaf_{x86_64,arm64,arm64-linux}.s` each emi…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "verify:array_typed_leaf.hexa:extern fn malloc(n: int) -> int",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ae36f5633e32b375a",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955084711,
      "queuedAt": 1783955084697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "반증 실패 — 주장이 모든 축에서 확인됨. (1) array_typed_leaf…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955153043,
      "tokens": 51330,
      "toolCalls": 7,
      "durationMs": 68332,
      "resultPreview": "{\"refuted\":false,\"reason\":\"반증 실패 — 주장이 모든 축에서 확인됨. (1) array_typed_leaf.hexa:53 은 실제 코드 `extern fn malloc(n: int) -> int` (주석 아님, 로컬 정의 아님; :54-56 에 realloc/write/exit 동반). (2) self/runtime.h 에는 hexa_ptr_alloc(:872)/hexa_ptr_free(:873)/hexa_exit(:846) 만 HexaVal-ABI carrier로 존재하고 `HexaVal malloc` 심(shim)은 runtime.h/runtime_core.c 어디에도 없음 → U 심볼 malloc 은 C-ABI libc 바디에 바인딩. hexa_bool 같은 백엔드 헬퍼도 아님. …"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "verify:array_typed_leaf.hexa:malloc(16) // header alloc in hexa_arr_i64_new",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "afb29c4b79013fe0e",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955084712,
      "queuedAt": 1783955084697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — the finding survives all four refutation avenue…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955161227,
      "tokens": 49314,
      "toolCalls": 9,
      "durationMs": 76515,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — the finding survives all four refutation avenues. (1) array_typed_leaf.hexa:65 `let a = malloc(16)` is real code, not a comment. (2) `malloc` is declared at :53 as `extern fn malloc(n: int) -> int` — a raw libc extern; self/runtime.h contains ONLY hexa_ptr_alloc(:872)/hexa_ptr_free(:873)/hexa_exit(:846)/hexa_bool(:113), there is no HexaVal-ABI malloc carrier …"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "verify:array_typed_leaf.hexa:malloc(bytes) // data buffer alloc",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a7594ea70588d31da",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955084712,
      "queuedAt": 1783955084697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. All four refutation avenues were te…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955183447,
      "tokens": 66670,
      "toolCalls": 11,
      "durationMs": 98735,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. All four refutation avenues were tested against the real files and every one failed. (1) NOT a carrier: array_typed_leaf.hexa:53 declares `extern fn malloc(n: int) -> int` = raw libc; self/runtime.h contains NO malloc carrier — the only HexaVal-ABI carriers are hexa_ptr_alloc (runtime.h:872), hexa_ptr_free (:873), hexa_exit (:846), plus the backen…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:array_typed_leaf.hexa:extern fn realloc(p: int, n: int) -> int",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "aaa22a0338a1ecb64",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955084712,
      "queuedAt": 1783955084697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED. stdlib/runtime/array_typed_leaf.hexa:54 is real …",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955187808,
      "tokens": 56929,
      "toolCalls": 15,
      "durationMs": 103095,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED. stdlib/runtime/array_typed_leaf.hexa:54 is real code (`extern fn realloc(p: int, n: int) -> int`), not a comment, and is actually called at :84 inside hexa_arr_i64_push (`let nd = realloc(old, nbytes)`). It is not a local symbol — the file header (L15-16) explicitly says it calls libc realloc as a C-ABI extern resolved at the final link. No HexaVal-ABI carrier…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:array_typed_leaf.hexa:realloc(old, nbytes) // THE realloc site — exactly 1 per file",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ab59ff2e166c79eb8",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955084712,
      "queuedAt": 1783955084697,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955181039,
      "tokens": 58395,
      "toolCalls": 11,
      "durationMs": 96327,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf.hexa:84 `let nd = realloc(old, nbytes)` is real code inside the len>=cap grow branch (verified by line-numbered read), not a comment. (2) The symbol is declared at the same file's line 54 as `extern fn realloc(p: int, n: int) -> int` — a raw libc extern, NOT a HexaVal-ABI carrier, NOT a backend helper like hexa_…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "verify:array_typed_leaf.hexa:extern fn write(fd,buf,n) + write(2, msg, 20) @:87",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a08b5ed599405d024",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955084712,
      "queuedAt": 1783955084698,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — every refutation avenue fails. (1) Real code, n…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955180242,
      "tokens": 54124,
      "toolCalls": 10,
      "durationMs": 95530,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — every refutation avenue fails. (1) Real code, not comments: array_typed_leaf.hexa:55 = `extern fn write(fd: int, buf: int, n: int) -> int`, :87 = `write(2, msg, 20)` inside the realloc==0 OOM guard. (2) Not a carrier / not a backend helper: self/runtime.h declares NO `hexa_write` HexaVal-ABI carrier — `write` appears only as `#include <unistd.h>` (:37) and a …"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "verify:array_typed_leaf.hexa:extern fn hexa_int(x: int) -> HexaVal → hexa_int(x) @:118",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a0462599a402f20ff",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955115928,
      "queuedAt": 1783955084698,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955239577,
      "tokens": 60405,
      "toolCalls": 13,
      "durationMs": 123649,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf.hexa:57 is real code — `extern fn hexa_int(x: int) -> HexaVal` in the raw-libc carrier block; used at :118 `return hexa_int(x)` (tail of hexa_arr_i64_box). (2) The symbol is genuinely C-ABI scalar, not a HexaVal-ABI carrier and not a codegen-controlled helper like hexa_bool: self/runtime.h:112 `HexaVal hexa_int(…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "verify:array_typed_leaf.hexa:extern fn exit(code: int) → exit(1) @:88",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a58317f48d1b0f9e6",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955153044,
      "queuedAt": 1783955084698,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The class-A defect report on `extern fn exit` @ stdlib/runt…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955225448,
      "tokens": 47642,
      "toolCalls": 8,
      "durationMs": 72403,
      "resultPreview": "{\"refuted\":true,\"reason\":\"The class-A defect report on `extern fn exit` @ stdlib/runtime/array_typed_leaf.hexa:56 / `exit(1)` @ :88 is WRONG — the backend launders it into the HexaVal carrier before it ever reaches the linker. Verified end-to-end, read-only:\\n\\n(1) DECL IS REAL CODE, not a comment: array_typed_leaf.hexa:56 `extern fn exit(code: int)`, called at :88 inside the OOM guard of hexa_arr…"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "verify:array_typed_leaf.hexa:extern fn hexa_throw(v: HexaVal) → hexa_throw(\"index out of bounds\") @:113,:114",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a40c8776dcbec3259",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955161227,
      "queuedAt": 1783955084698,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Finding CONFIRMED (it is a correct OK-carrier classificatio…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955253240,
      "tokens": 50107,
      "toolCalls": 10,
      "durationMs": 92012,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Finding CONFIRMED (it is a correct OK-carrier classification; I could not refute it). All four cited anchors verify exactly: (1) stdlib/runtime/array_typed_leaf.hexa:58 is real code `extern fn hexa_throw(v: HexaVal)` (not a comment — the comments are at :108/:109), and :113/:114 are real call sites `hexa_throw(\\\"index out of bounds\\\")`. (2) self/runtime.h:847 is `void he…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "verify:array_typed_leaf.hexa:__hx_payload_lt / _ge / _eq / _mul / _add (leaf intrinsics) + codegen-injected hexa_bool",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a92eb414c3174eff1",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955180243,
      "queuedAt": 1783955084698,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Claim VERIFIED, not refuted. (1) stdlib/runtime/array_typed…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955244231,
      "tokens": 49462,
      "toolCalls": 7,
      "durationMs": 63987,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Claim VERIFIED, not refuted. (1) stdlib/runtime/array_typed_leaf.hexa:64 is real code inside pub fn hexa_arr_i64_new (opens :62): `if __hx_payload_lt(c, 1) { c = 1 }` — not a comment. (2) hexa_bool is a genuine backend helper with a raw-int C ABI it controls itself: self/runtime.h:113 `HexaVal hexa_bool(int b);`, body self/runtime_core.c:1831 `HexaVal hexa_bool(int b) { …"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "verify:array_typed_leaf_f64.hexa:extern fn malloc → malloc(16) @:41, malloc(bytes) @:43",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a320f7608b33efe9f",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955181040,
      "queuedAt": 1783955084698,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED as class A-raw-libc. All four refutation avenues …",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955309747,
      "tokens": 58835,
      "toolCalls": 14,
      "durationMs": 128707,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED as class A-raw-libc. All four refutation avenues fail. (1) Real code, not comments: array_typed_leaf_f64.hexa:30 = `extern fn malloc(n: int) -> int`, :41 = `let a = malloc(16)`, :43 = `let d = malloc(bytes)`. (2) Not a carrier / not a backend helper: self/runtime.h declares ONLY hexa_bool(:113), hexa_exit(:846), hexa_ptr_alloc(:872), hexa_ptr_free(:873) — there…"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "verify:array_typed_leaf_f64.hexa:realloc(old, nbytes) // the f64 realloc site — 1 site, in push_bits grow branch",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a6cef5318a7630b45",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955183448,
      "queuedAt": 1783955084698,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — all four refutation avenues fail. (1) NOT a car…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955266539,
      "tokens": 62602,
      "toolCalls": 10,
      "durationMs": 83091,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — all four refutation avenues fail.\\n\\n(1) NOT a carrier: array_typed_leaf_f64.hexa:31 is `extern fn realloc(p: int, n: int) -> int` = raw libc. self/runtime.h declares ONLY hexa_ptr_alloc (:872), hexa_ptr_free (:873), hexa_exit (:846). `hexa_ptr_realloc` does not exist anywhere in self/, compiler/, stdlib/, or tool/ — the missing-carrier gap is real, exactly a…"
    },
    {
      "type": "workflow_agent",
      "index": 16,
      "label": "verify:array_typed_leaf.hexa:extern fn malloc / realloc / write / exit",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a2c31009b682499e9",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955187808,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — could not refute on any of the four available g…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955273141,
      "tokens": 58282,
      "toolCalls": 10,
      "durationMs": 85332,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — could not refute on any of the four available grounds.\\n\\n(1) NOT a carrier: no hexa-side definition of malloc/realloc/write exists in self/*.c or self/*.h; self/runtime.h declares only hexa_ptr_alloc (:872) and hexa_ptr_free (:873) — hexa_ptr_realloc genuinely does not exist, confirming the stated gap. (The co-declared hexa_int/hexa_throw/hexa_bool ARE legit…"
    },
    {
      "type": "workflow_agent",
      "index": 17,
      "label": "verify:array_typed_leaf_f64.hexa:extern fn malloc / realloc / write / exit",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "acc31c47315c8c2a4",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955225449,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed on origin/main, not refuted. (1) stdlib/runtime/a…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955304781,
      "tokens": 53000,
      "toolCalls": 11,
      "durationMs": 79331,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Confirmed on origin/main, not refuted. (1) stdlib/runtime/array_typed_leaf_f64.hexa:30-33 is live code (not comment): `extern fn malloc/realloc/write/exit` with int params — raw libc C-ABI names, no hexa_int. (2) self/runtime.h has only hexa_exit(846), hexa_ptr_alloc(872), hexa_ptr_free(873) — no hexa_malloc/hexa_realloc/hexa_write carriers and notably NO hexa_ptr_reallo…"
    },
    {
      "type": "workflow_agent",
      "index": 18,
      "label": "verify:arr_zeros_leaf.hexa:extern fn malloc / calloc / write / exit",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "aead3af0f1fc3772b",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955239578,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The finding stands. (1) Lines 25-28 of stdlib/runtime/arr_z…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955331381,
      "tokens": 52299,
      "toolCalls": 11,
      "durationMs": 91803,
      "resultPreview": "{\"refuted\":false,\"reason\":\"The finding stands. (1) Lines 25-28 of stdlib/runtime/arr_zeros_leaf.hexa on origin/main (e891995ed) are real code, not comments: `extern fn malloc/calloc/write/exit`. The file's own header comment states \\\"U-floor = { malloc, calloc, write, exit }\\\". (2) They are NOT HexaVal-ABI carriers: self/runtime.h declares no HexaVal malloc/calloc/write; it declares the carriers o…"
    },
    {
      "type": "workflow_agent",
      "index": 19,
      "label": "verify:array_new_leaf.hexa:extern fn calloc",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a628377e806035f1a",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955244231,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — every refutation ground was checked and none ho…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955338473,
      "tokens": 68243,
      "toolCalls": 10,
      "durationMs": 94242,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — every refutation ground was checked and none holds. (1) NOT a comment: array_new_leaf.hexa:23 is genuine code `extern fn calloc(cnt: int, size: int) -> int`, called at line 27 `let a = calloc(1, 32)` (line 21 is a separate comment that merely mentions calloc in the U-floor note). (2) NOT a HexaVal carrier / backend helper: grep of self/runtime.h returns hexa_…"
    },
    {
      "type": "workflow_agent",
      "index": 20,
      "label": "verify:regen_arr_zeros_leaf_native_s.sh:ALLOWED_U=\"malloc calloc write exit hexa_exit\"",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a70eed7f546e31ba5",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955253242,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED. tool/regen_arr_zeros_leaf_native_s.sh:41 is live…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955326043,
      "tokens": 50833,
      "toolCalls": 7,
      "durationMs": 72801,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED. tool/regen_arr_zeros_leaf_native_s.sh:41 is live code (not a comment): ALLOWED_U=\\\"malloc calloc write exit hexa_exit\\\". Of those five, only hexa_exit is a real HexaVal-ABI carrier — self/runtime.h declares hexa_exit(HexaVal) (:846), hexa_ptr_alloc(HexaVal) (:872), hexa_ptr_free(HexaVal,HexaVal) (:873), and has NO HexaVal declaration for malloc/calloc/write/ex…"
    },
    {
      "type": "workflow_agent",
      "index": 21,
      "label": "verify:regen_array_new_leaf_native_s.sh:ALLOWED_U=\"calloc hexa_exit malloc write exit\"",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ab13d1fc16106dd5c",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955266539,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "반증 실패 — 발견은 정확하다. 4가지 반증 경로를 모두 확인했…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955372339,
      "tokens": 52983,
      "toolCalls": 11,
      "durationMs": 105800,
      "resultPreview": "{\"refuted\":false,\"reason\":\"반증 실패 — 발견은 정확하다. 4가지 반증 경로를 모두 확인했고 전부 불성립.\\n\\n1) 주석 아님: tool/regen_array_new_leaf_native_s.sh:41 은 실제 코드 `ALLOWED_U=\\\"calloc hexa_exit malloc write exit\\\"` (주석은 39-40행). 이 값은 79-83행에서 실제로 강제된다 — nm의 U 심볼 중 ALLOWED_U에 없는 것만 \\\"U-floor breach\\\"로 exit 1, 있는 것은 \\\"U-floor clean\\\" 출력.\\n\\n2) carrier/백엔드-헬퍼 아님: self/runtime.h에서 HexaVal-ABI carrier는 정확히 hexa_exit(:846), hexa_ptr…"
    },
    {
      "type": "workflow_agent",
      "index": 22,
      "label": "verify:regen_array_typed_leaf_native_s.sh:ALLOWED_U=\"malloc realloc write hexa_exit hexa_int hexa_throw\"",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ab69d77fdc6cceb26",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955273141,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Finding survives all four refutation routes. (1) tool/regen…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955350691,
      "tokens": 50663,
      "toolCalls": 9,
      "durationMs": 77550,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Finding survives all four refutation routes. (1) tool/regen_array_typed_leaf_native_s.sh:43 is real code, not a comment — ALLOWED_U is consumed by the U-floor gate at :84, so malloc/realloc/write are actively whitelisted as permitted undefined externs. (2) The symbols are raw libc, not carriers: self/runtime.h defines carriers only for hexa_int (:112), hexa_exit (:846), …"
    },
    {
      "type": "workflow_agent",
      "index": 23,
      "label": "verify:regen_array_typed_leaf_f64_native_s.sh:ALLOWED_U=\"malloc realloc write hexa_exit hexa_throw\"",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ad547e29d60dbb164",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955304782,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "high",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955384322,
      "tokens": 52225,
      "toolCalls": 8,
      "durationMs": 79540,
      "resultPreview": "{\"confidence\":\"high\",\"reason\":\"CONFIRMED — not refuted. All four refutation avenues are closed by direct reading. (1) tool/regen_array_typed_leaf_f64_native_s.sh:43 is real code, not a comment: ALLOWED_U=\\\"malloc realloc write hexa_exit hexa_throw\\\", consumed at ~line 83-85 as the nm U-floor assertion whitelist. (2) The symbols are not all carriers: self/runtime.h:846 (HexaVal hexa_exit(HexaVal)) …"
    },
    {
      "type": "workflow_agent",
      "index": 24,
      "label": "verify:regen_array_typed_leaf_native_s.sh:WARN: cross-assemble check skipped (no matching toolchain)",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a52af9a1dd4dd9da9",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955309748,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — could not refute. All four refutation criteria …",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955437758,
      "tokens": 67300,
      "toolCalls": 11,
      "durationMs": 128010,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — could not refute. All four refutation criteria fail to apply, and every load-bearing claim checks out against the real code.\\n\\n(1) :88-89 is real executable code, not a comment — the `else` arm of `if $CC $cc_extra -c \\\"$s\\\" -o \\\"$o\\\" 2>/dev/null; then` at :76. It prints WARN, then :89 still prints the success-shaped \\\"→ $out (globl emitted)\\\" line. The seed…"
    },
    {
      "type": "workflow_agent",
      "index": 25,
      "label": "verify:regen_array_typed_leaf_native_s.sh:} > \"$out\" (seed written BEFORE the sanity check)",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "adb39aa49a70a660b",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955326044,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Read tool/regen_array_typed_leaf_native_s.sh directly: line…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955379042,
      "tokens": 47774,
      "toolCalls": 5,
      "durationMs": 52998,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Read tool/regen_array_typed_leaf_native_s.sh directly: line 68 is real code (`} > \\\"$out\\\"`), closing the brace group that writes the seed header + sed-rewritten asm into the TRACKED path self/native/array_typed_leaf_{x86_64,arm64,arm64-linux}.s. The cross-assemble + T-count + U-floor assertion only begins at :69, and the breach path is `exit 1` at :85 — strictly after t…"
    },
    {
      "type": "workflow_agent",
      "index": 26,
      "label": "verify:stage_resolve_runtime_a:.s-seed consumption path — NO U-floor check whatsoever",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a617c7a7e1b6d40ed",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955331381,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed on the code, not refuted. (1) tool/stage_resolve_…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955445183,
      "tokens": 72848,
      "toolCalls": 11,
      "durationMs": 113802,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Confirmed on the code, not refuted. (1) tool/stage_resolve_runtime_a:614 is real code (`case \\\"$tgt\\\" in`), not a comment. (2) The frozen-.s consumption path for all four array families performs exactly two checks — a `.globl` COUNT grep (:621 i64, :688 array_new, :748 arr_zeros, :804 f64) and `$CC -c` assembling (:628, :695, :755, :811) — then ar's the resulting .o stra…"
    },
    {
      "type": "workflow_agent",
      "index": 27,
      "label": "verify:stage_resolve_runtime_a:inline allowlist \" malloc realloc write hexa_exit hexa_int hexa_throw \"",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a29610ba5b30ccf21",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955338473,
      "queuedAt": 1783955115935,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "반증 실패 — 주장은 실측으로 전부 확인됨. (1) stage_resolve_…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955426227,
      "tokens": 56171,
      "toolCalls": 6,
      "durationMs": 87754,
      "resultPreview": "{\"refuted\":false,\"reason\":\"반증 실패 — 주장은 실측으로 전부 확인됨. (1) stage_resolve_runtime_a:601은 주석이 아니라 own-obj U-floor 게이트의 실제 코드이며 문자열 \\\" malloc realloc write hexa_exit hexa_int hexa_throw \\\"가 그대로 존재(:668/:728/:784 나머지 3개 inline 사본도 인용 그대로 일치). (2) 그 allowlist의 malloc/realloc/write/calloc/exit는 self/runtime.h에 캐리어 선언이 전혀 없는 raw libc C-ABI 심볼 = 클래스 A 그 자체이고, hexa_bool 같은 백엔드 헬퍼도 아니다(runtime.h에 있는 것은 hexa_in…"
    },
    {
      "type": "workflow_agent",
      "index": 28,
      "label": "verify:array_typed_leaf.hexa:hexa_int",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "afef69dec549978df",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955688690,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — I could not refute it; every listed refutation …",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955813057,
      "tokens": 57047,
      "toolCalls": 10,
      "durationMs": 124367,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — I could not refute it; every listed refutation avenue is closed by direct reading.\\n\\n(1) NOT a comment: stdlib/runtime/array_typed_leaf.hexa:57 is live code `extern fn hexa_int(x: int) -> HexaVal`, in the extern block alongside malloc/realloc/write/exit/hexa_throw. Sole call site = line 118 `return hexa_int(x)` in hexa_arr_i64_box.\\n\\n(2) NOT a HexaVal-ABI c…"
    },
    {
      "type": "workflow_agent",
      "index": 29,
      "label": "verify:runtime_core.c:hexa_arr_i64_new / hexa_arr_i64_push / hexa_arr_i64_len / hexa_arr_i64_box",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ad16550905dd2251c",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955688691,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "반증 실패 — 주장의 모든 요소를 코드에서 직접 확인했고, 제시…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955898346,
      "tokens": 73173,
      "toolCalls": 17,
      "durationMs": 209655,
      "resultPreview": "{\"refuted\":false,\"reason\":\"반증 실패 — 주장의 모든 요소를 코드에서 직접 확인했고, 제시된 네 가지 반증 사유 중 어느 것도 성립하지 않는다.\\n\\n(1) 주석 아님: runtime_core.c:2530-2533은 실제 코드이며, 진짜 SSOT는 생성기 self/runtime_core_emit.hexa:2764-2768로, 정확히 `extern HexaVal hexa_arr_i64_new(int cap);` / `extern HexaVal hexa_arr_i64_push(HexaVal v, int64_t x);` / `extern int hexa_arr_i64_len(HexaVal v);` / `extern HexaVal hexa_arr_i64_box(HexaVal v, int64_t…"
    },
    {
      "type": "workflow_agent",
      "index": 30,
      "label": "verify:array_typed_leaf_f64_x86_64.s:hexa_arr_f64_new / hexa_arr_f64_push_bits / hexa_arr_f64_len / hexa_arr_f64_box",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "af3bdd0b1532ba815",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955688691,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "high",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955912538,
      "tokens": 79421,
      "toolCalls": 22,
      "durationMs": 223847,
      "resultPreview": "{\"confidence\":\"high\",\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. (1) self/native/array_typed_leaf_f64_x86_64.s carries 4 real .globl bodies (new :14/17, push_bits :133/136, len :394/397, box :438/441) — code, not comments — and every one is pair-ABI: new reads cap payload from rsi (`mov [rbp-128],rdi # store tag L0` / `mov rbx,rsi # ingress param payload`); push_bits reads bits from rcx (`mo…"
    },
    {
      "type": "workflow_agent",
      "index": 31,
      "label": "verify:stage_resolve_runtime_a:resolve_native_array_typed_leaf_seed (gate default)",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a7d8aaf30942a89a4",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955688691,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Read the file directly; the claim is confirmed on every axi…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955755875,
      "tokens": 55083,
      "toolCalls": 6,
      "durationMs": 67184,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Read the file directly; the claim is confirmed on every axis. tool/stage_resolve_runtime_a:575 is REAL executable code (the trailing `# explicit opt-out` is only an annotation): `[ \\\"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\\\" = \\\"0\\\" ] && return 0`. The `:-x` default means an UNSET var yields `x`, which is != \\\"0\\\", so the guard does NOT return — it falls through. Only a lite…"
    },
    {
      "type": "workflow_agent",
      "index": 32,
      "label": "verify:regen_array_typed_leaf_native_s.sh:ALLOWED_U (U-floor gate)",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a73f8de35153c63d3",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955688691,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED (class OTHER — gate/policy defect). (1) tool/rege…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955818113,
      "tokens": 59182,
      "toolCalls": 14,
      "durationMs": 129422,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED (class OTHER — gate/policy defect). (1) tool/regen_array_typed_leaf_native_s.sh:43 is real code, not a comment: ALLOWED_U=\\\"malloc realloc write hexa_exit hexa_int hexa_throw\\\", enforced as the U-floor gate at :84 (nm U-symbols not in ALLOWED_U → \\\"U-floor breach\\\" exit 1). Raw libc malloc/realloc/write are whitelisted outright, so the gate structurally cannot …"
    },
    {
      "type": "workflow_agent",
      "index": 33,
      "label": "verify:array_typed_leaf_x86_64.s:malloc / realloc / write",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ab9469e5d06d4a2c3",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955688691,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — every refutation path checked and closed. (1) R…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955820702,
      "tokens": 55046,
      "toolCalls": 13,
      "durationMs": 132011,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — every refutation path checked and closed. (1) Real code, not comments: self/native/array_typed_leaf_x86_64.s:52 `call malloc`, :241 `call realloc`, :350 `call write` are real instructions with exactly the claimed pair-ABI register layout — malloc gets `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default = TAG_INT`, then `mov [rbp-160],rax # store tag L4` …"
    },
    {
      "type": "workflow_agent",
      "index": 34,
      "label": "verify:array_typed_leaf_f64_x86_64.s:malloc / realloc / write",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a2013981b6696c9d1",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955688691,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. (1) Call-target census of self/nati…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955763049,
      "tokens": 54332,
      "toolCalls": 10,
      "durationMs": 74358,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. (1) Call-target census of self/native/array_typed_leaf_f64_x86_64.s: undefined-in-file call targets = {malloc x2, realloc, write, hexa_exit, hexa_throw}; the file defines only the 4 .globl hexa_arr_f64_{new,push_bits,len,box}, so malloc/realloc/write are true U symbols, not local labels. (2) They are emitted with hexa pair-ABI, e.g. lines 50-54: `…"
    },
    {
      "type": "workflow_agent",
      "index": 35,
      "label": "verify:arr_zeros_leaf_x86_64.s:calloc / malloc / write",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a0611c2fd8b27766e",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955688692,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED at main HEAD e891995ed. self/native/arr_zeros_lea…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955770772,
      "tokens": 51830,
      "toolCalls": 11,
      "durationMs": 82080,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED at main HEAD e891995ed. self/native/arr_zeros_leaf_x86_64.s has exactly 4 undefined call-targets: calloc (lines 34, 361), malloc (129, 456), write (180, 507), hexa_exit (186, 513). Only .globl syms are hexa_arr_zeros_leaf{,_int}; all other labels are local .L*_bb* — so calloc/malloc/write are true externs, not local symbols, and the call sites are real code (th…"
    },
    {
      "type": "workflow_agent",
      "index": 36,
      "label": "verify:runtime_hi_x86_64.s:ALL other seed families (12 families / 60 of 69 .s files)",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ac5ebdfa23d7f1d11",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955755876,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "I independently reproduced the census and it confirms the c…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955966998,
      "tokens": 69373,
      "toolCalls": 16,
      "durationMs": 211122,
      "resultPreview": "{\"refuted\":false,\"reason\":\"I independently reproduced the census and it confirms the claim. (1) Extracting every `call`/`bl` target not defined in-file across all 69 self/native/*.s: raw libc (calloc/malloc/realloc/write) appears in EXACTLY 3 families — arr_zeros_leaf, array_typed_leaf, array_typed_leaf_f64 — and nowhere else. (2) The call/bl set really is the complete external surface: I verified…"
    },
    {
      "type": "workflow_agent",
      "index": 37,
      "label": "verify:intern_core_arm64.s:hexa_bool / hexa_truthy (the x86-vs-arm64 U delta)",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a08ae0d6b83d3aebf",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955763049,
      "queuedAt": 1783955688690,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The finding is CORRECT: hexa_bool/hexa_truthy in self/nativ…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783955861608,
      "tokens": 52582,
      "toolCalls": 9,
      "durationMs": 98559,
      "resultPreview": "{\"refuted\":false,\"reason\":\"The finding is CORRECT: hexa_bool/hexa_truthy in self/native/intern_core_arm64.s are OK-carrier (sanctioned backend-emitted helpers), not a class-A or class-B defect. Verified at register level, not by trusting the census.\\n\\nSIGNATURES (self/runtime.h:113,140): `HexaVal hexa_bool(int b)` (def runtime_core.c:1831) and `int hexa_truthy(HexaVal v)` (def runtime_core.c:8264…"
    },
    {
      "type": "workflow_agent",
      "index": 38,
      "label": "design:carrier-add",
      "phaseIndex": 3,
      "phaseTitle": "Design",
      "agentId": "a0a6923b8252d859b",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955967019,
      "queuedAt": 1783955967018,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -rln \"stage_resolve_runtime_a\" . 2>/dev/null | grep -v…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783956384277,
      "tokens": 107208,
      "toolCalls": 33,
      "durationMs": 417257,
      "resultPreview": "## VERDICT\n\nThe `hexa_ptr_realloc` carrier is the right design and is cheap — but the audit turned up **two things the brief did not have**, one of which changes the fix's scope.\n\n---\n\n## 1. Ground truth: I read the frozen seed, not the source\n\n`self/native/array_typed_leaf_arm64.s` is committed. The defect is visible in the emitted bytes.\n\n**The realloc call — worse than \"wrong registers\":**\n```a…"
    },
    {
      "type": "workflow_agent",
      "index": 39,
      "label": "design:alloc-copy-free",
      "phaseIndex": 3,
      "phaseTitle": "Design",
      "agentId": "a6cbb8677494f6b35",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955967020,
      "queuedAt": 1783955967018,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "python3 - <<'EOF' src=open('/Users/mini/dancinlab/hexa-lang…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783956407172,
      "tokens": 112221,
      "toolCalls": 39,
      "durationMs": 440152,
      "resultPreview": "## 1. 감사 결과 — 잔존 결함은 realloc **하나가 아니다** (읽기만 함, 수정 없음)\n\n두 typed-leaf 시드의 U-floor 6개 중 **4개가 클래스 A**다. realloc만 보면 안 된다.\n\n| extern | C 실제 시그니처 | 판정 |\n|---|---|---|\n| `malloc(n)` | `void* malloc(size_t)` C-ABI | **클래스 A** — `array_typed_leaf.hexa:65,67` / `_f64.hexa:41,43` |\n| `realloc(p,n)` | C-ABI | **클래스 A** — `:84` / `:61` |\n| `write(2,msg,20)` + `exit(1)` | C-ABI | **클래스 A** — `:86-88` / `:63-…"
    },
    {
      "type": "workflow_agent",
      "index": 40,
      "label": "design:route-c",
      "phaseIndex": 3,
      "phaseTitle": "Design",
      "agentId": "a813c979d557afc58",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783955967020,
      "queuedAt": 1783955967018,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /Users/mini/dancinlab/hexa-lang; grep -rn \"hxlcl_write(\"…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783956395289,
      "tokens": 115200,
      "toolCalls": 32,
      "durationMs": 428269,
      "resultPreview": "## 결론 먼저\n\n**`calloc/malloc/realloc`를 `_is_cabi`에 넣을 필요가 없다.** 이미 들어 있다 — `hxlcl_` 이름으로. 그리고 runtime.a의 shim 멤버가 그 심볼들을 **전역 C-ABI 심볼로 이미 export**한다. 즉 Route C는 \"설계할 대상\"이 아니라 **이미 배선이 끝나 있고 시드가 안 쓰고 있을 뿐**이다. 필요한 변경은 컴파일러 0줄, 시드 2줄 + regen 스크립트 env 1줄이다.\n\n---\n\n## 1. `_is_cabi` 배선 실측 (읽기 전용)\n\n정의: `compiler/codegen/x86_64_linux.hexa:2258` `pub fn _is_cabi(fname: string) -> bool`\n게이트: `:2259` `if env(…"
    },
    {
      "type": "workflow_agent",
      "index": 41,
      "label": "synthesize",
      "phaseIndex": 4,
      "phaseTitle": "Synthesize",
      "agentId": "a84ebb9d91eb84c6c",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783956407176,
      "queuedAt": 1783956407174,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /private/tmp/claude-501/-Users-mini-dancinlab-hexa-lang/…",
      "promptPreview": "# 배경 (2026-07-13 실측 · 추측 아님)\n\nhexa-lang의 self/native/*.s 시드는 aprime native emit이 만든 frozen bootstrap 시드다.\naprime는 시드의 **모든 호출을 hexa pair-ABI로 낮춘다**: 인자는 (tag,payload) 레지스터 쌍을 타고,\n호출 결과의 payload는 **둘째 반환 레지스터(rdx / x1)** 에서 읽힌다.\n\n## 확정된 결함 클래스 A — raw libc extern\n시드 소스가 `extern fn calloc/malloc/realloc/free/write/exit` 를 선언하면, 그 U 심볼은 링크에서\n**C-ABI libc 바디**에 묶인다. C-ABI는 (rdi,rsi)를 자기 스칼라로 읽고 결과를 **…",
      "lastProgressAt": 1783957512500,
      "tokens": 178146,
      "toolCalls": 73,
      "durationMs": 1105324,
      "resultPreview": "darwin-arm64에서 **실측**했다. 배포된 `~/.hx/bin/build/runtime.a`(Jul 14 00:16) + 배포된 `~/.hx/bin/self/runtime.h` 프로토타입으로 C 드라이버를 링크했다. 하니스: `/private/tmp/claude-501/-Users-mini-dancinlab-hexa-lang/adbce6b7-54f3-4f01-a7fe-77d3f277b3f0/scratchpad/abi/drv{,2,4,5,6,7}.c`\n\n---\n\n# 0. 헤드라인 — realloc 갭은 **2차 문제**다. 1차는 아무도 못 본 **스칼라 ABI 미러 브레이크**다\n\n세 에이전트 모두 \"시드→libc egress(클래스 A)\"만 봤다. 실측해보니 **시드→호출자 ingress/retu…"
    }
  ],
  "totalTokens": 2793811,
  "totalToolCalls": 632
}```
