# 프론티어 5레인 잔여 전수조사 (2026-07-17 · workflow w2wa9vz2y)

> 5레인(L1 own-emit·L2 native-serve·L3 runtime.c→0·L4 unboxing·L5 borrowck) 전 잔여를 census+blocker분류. 다음 세션/aiden 세션의 실행 보드. 좌표는 census 시점 origin/main 근처 — 구현 전 origin/main 재검증 필수.

## 최고레버리지
Dispatch ONE aiden from-scratch linux-x86_64 build session (reboot the D-state-hung host first) that BATCHES the four top measurements onto the same expensive from-source rebuild, led by the #4966 own-link default-ON A/B attribution. Rationale: (1) #4966 is an already-OPEN PR with all four demotion root causes merged-closed (#4954/56/59) and RUN-parity smoke 4× GREEN — only the attribution measurement remains, and it crosses the HARDEST DONE criterion (③ no-clang + no-binutils, default-ON on x86_64) the moment fail=5/timeout=2 is shown inherited-from-main rather than flip-introduced. (2) The same rebuilt tree immediately yields the axis-① HEXA_BUILD_NATIVE_STRICT corpus census + native_c_crash_census retire (unlocks the terminal hexa_cc.c DELETE, criterion ②) and the boxop/boxarg/calltype numbers (unblocks L4's next unbox lever + re-baselines the 8.6× north-star) — three DONE-line advances off one build. The single most impactful atomic action is the #4966 A/B; the highest-leverage session is that A/B plus the STRICT census riding the same build.

## LOCAL-NOW (mini 구현가능 · byteeq-neutral)
1. [L4 · HIGHEST LOCAL] static-types 147→0 TP/FP triage — no build. Read the 82 dirty files (family-clustered: self/tui=15, self/ml=12, stdlib/qforge=10) and classify each of the 147 HEXA_STATIC_TYPES emissions TP(fix corpus code)/FP(fix checker rung) per l5... l4_static_types_corpus_census.md:131-136 (list at :46-129). TP fixes + FP checker-rung fixes are both local PRs; only the residual RE-MEASURE (static-types-corpus.yml) needs aiden. Gates the flagless-default static-type check.
2. [L3] strtod residual-UND kill — edit 3 reference-retention sites (no build): runtime_emit_full.hexa:5388 hexa_float(strtod(...))→__hexa_num_parse_float; runtime_emit_full.hexa:14592 float-print round-trip strtod(buf,NULL)==f→native parse (precedent runtime_core_emit.hexa:7979); runtime_core_emit.hexa:2128 junk-fallback hxlcl_atof(cs)→native 0.0 under HEXA_RT_STRTOD_TAIL_NATIVE. Then aiden re-runs #4651 parity corpus + nm-witness.
3. [L2] run-path STRICT-census parity — mirror the build census onto the run seam: add HEXA_RUN_NATIVE_STRICT=1 fail-closed guard at cmd_run_user_direct clang-fallback drops (self/main.hexa ~:4890/4915/4918), default-OFF byte-neutral, same shape as the build guard at :3760. Makes `hexa run` axis-① fallback measurable like `hexa build` now is.
4. [L4] x86 SSE-ABI whitelist wiring — for each f64 runtime prim a future unbox lever routes through, add its fname to compiler/codegen/x86_64_linux.hexa:2560-2566 so float params classify to xmm0/xmm1 (mirror existing hxlcl_atof/fmod/fp_addmul entries). Unblocks f64 scalar unboxing on x86; aiden byteeq-validates.
5. [L3] trivial raw-svc / termios leaves — author native bodies (local): _exit→SYS_exit_group, abort→SYS_tgkill(SIGABRT), cfmakeraw→pure termios field-clear (musl 1:1), ptsname_r→TIOCGPTN ioctl. Add as default-OFF shim #ifndef arms; aiden byteeq/flip. Attack-plan R4/R5.
6. [L3] __stack_chk_fail residual — add -fno-stack-protector to the runtime.c/runtime_core.c compile in stage_resolve_runtime_a (mirrors the -D_FORTIFY_SOURCE=0 #4361 precedent). Local; aiden byteeq confirms neutral.
7. [L2] cross-target rung-2b distribution (Fable design DONE) — wire arch-tagged runtime-<target>.a asset upload into each release.yml job + install.sh opt-in pull + hook _resolve_target_runtime_a (self/main.hexa:3763-3785 HEXA_BUILD_NATIVE_CROSS block is implemented but INERT without it). Larger local edit; then aiden verifies a host `--target=x86_64-linux` build links own-start static (clang-0).

★ #3 run-path STRICT census = 구현완료 PR #4998 (2026-07-17). 나머지는 origin/main 좌표 재검증 후 구현.

## AIDEN-MEASURE (랭크 · 빌드/측정)
- [RANK-1 · crosses axis-③ clang-0+binutils-0 default on x86_64] #4966 own-link default-ON A/B attribution — on PR #4966 head vs origin/main HEAD (5c23e6017), `bash tool/release_build` then `./hexa run tool/stdlib_selftest_aggregate.hexa --ci-gate --strict` on BOTH trees with ~/.hexa-cache cleared. Confirm fail=5/timeout=2 is IDENTICAL on main (=inherited→retune/exclude gate, flip merges under release-integrity) vs flip-introduced (=own-link still corrupts a path). Same A/B for faithful-nobaseline. The demotion RCA (#4952) root causes are all merged-closed (#4954/#4956/#4959); RUN-parity smoke is 4× GREEN; stdlib-selftest now COMPLETES (pass=87 fail=5) not SIGSEGVs — corruption is fixed, this is the last gate.
- [RANK-2 · unlocks the hexa_cc.c DELETE, axis-① criterion ②] axis-① fallback-rate census — reboot the D-state-hung host (6 zombies 20h+) to clear I/O, then in ONE from-source build pass: (a) `hexa build` the .hexa corpus with HEXA_BUILD_NATIVE_STRICT=1 HEXA_RUN_NATIVE_TRACE=1, count FATAL vs total (0 = delete-precondition met; self/main.hexa:3754-3762); (b) re-run tool/native_c_crash_census to retire the stale '13' count. Rate 0 on 3-target fires the terminal delete of self/native/hexa_cc.c (1.85MB) + resolver marker :1639 + `hexa cc --regen` machinery.
- [RANK-3 · unblocks L4 next unbox lever + re-baselines 8.6× north-star] boxop/boxarg/calltype census numbers — after #4995 merges (HEXA_BOXARG_CENSUS producer instrument, OPEN/MERGEABLE), from-source compile the self-corpus with `HEXA_BOXOP_CENSUS=1 HEXA_CALLTYPE_CENSUS=1 HEXA_BOXARG_CENSUS=1 <rebuild> 2>census.log`, tally stderr lines by site-class + provably-int flag. measure-first discipline (lang-model.md §2.1) forbids authoring the call-arg unbox lever (_x86_hv_box_arg, 46 sites) without this number.
- [RANK-4 · converts L5 R3-R6 escape-widening into a shippable default-ON flip] leak-lane FP census — build ./hexa from source (ELF-magic+rc==0 gate, HEXA_PREBUILT_RUNTIME unset per l5_census_v2_verdict.md:78), compile aprime_cc self-compile closure + self/main.hexa under HEXA_BORROWCK_LEAK=1, diff leak-diag firings vs OFF. Report FP=0 (→ then byteeq-real 3-target + faithful + summary → flip _bck_leak_lane default-ON) or the offending hir_to_mir.hexa escape-KILL sites. #4984/89/91/92 all merged; this is the only remaining DONE-line move on the sub-axis.
- [RANK-5 · biggest single L3 UND drop, ~19] FILE*/STDIO_LINK flip — run state/hexa-own/l3_nmU_filestar_runbook.md exactly on aiden: `HEXA_RT_MULTIOBJ=1 HEXA_RT_STDIO_NATIVE=1 HEXA_RT_STDIO_LINK=1 bash tool/release_build`, nm -u diff vs OFF archive for the fopen/fread/fwrite/fclose/fseek/fdopen/fileno cluster, fuel_abort fake-fd smoke, then a separate default-ON flip PR gated on byteeq 3-target + install smoke. Mechanism landed #4894 (stage_resolve_runtime_a:66-67,4272,4303).
- [RANK-6 · the ONE lane catching a REAL live bug] HX3060 handle-lane UAF flip — compile real corpus under HEXA_BORROWCK_HANDLE=1, verify FP=0 AND farr_* codegen bind-registry mapping wired (bind-hexa-2 #4949: gate without codegen map worsened to SIGBUS), then byteeq 3-target → flip. Code hir_to_mir.hexa:867-894,3352-3367.
- [RANK-7] own-link real-corpus fallback = 0/8 full census — `CENSUS_TIMEOUT=300 bash tool/ownlink_strict_census.sh` to completion over the 8-program real corpus (#4965 fixed the qforge ccsd_rhf #7 truncation that dropped the last 2 tallies), confirm 0/8 clang-fallback, all 8 tallied.
- [RANK-8 · L3 flip cluster, each -1 to -3 UND] merge #4996 strftime (default-OFF) then flip PRs for: strftime (HEXA_RT_NATIVE_STRFTIME), SYSLEAF mmap/munmap/waitpid (#4944 HEXA_RT_NATIVE_SYSLEAF, -3), fork (#4982 HEXA_RT_NATIVE_FORK, -1), signal (HEXA_RT_NATIVE_SIGNAL + routec_signal_native_verify.sh), strtoll (bake hxlcl_strtoll_*.s seeds via tool/regen_hxlcl_strtoll_native_s.sh, routec_strtoll_native_verify.sh, -2), gmtime_r (pairs with strftime). Each = build+nm-drop+byteeq 3-target.
- [RANK-9] x86_64 encoder ENCODE-MISS==0 re-confirm — `bash scripts/scratch/rt_native/byteeq_x86.sh` then `grep -c 'ENCODE-MISS (x86_64):' /tmp/emit3.log`==0 AND `cmp /tmp/g3x.o /tmp/g4x.o` byte-identical. If 0, wire the assertion into byteeq_x86.sh tail as a standing gate; then link+run the emitted exec vs C-transpile gen for the C1/C2 correctness rung (bytes invisible to stderr census).
- [RANK-10] own-link advisory→required gate promotion — accumulate soak-green across N consecutive PR-CI runs (`gh run list --workflow nobaseline-gate.yml`) for the 4 advisory jobs (ownlink-determinism :1000, corpus-parity :1044, behavioral-smoke :1137, cfallback-zero :~1170), THEN add to selfhost-gates-required.yml (currently ZERO ownlink refs). Never wire an unmeasured job into the required gate.
- [RANK-11] L4 codegen-quality-probe re-baseline — re-run the 9-kernel broadbench grid (#4892, state/codegen-quality-probe-verdict.md) on current main with HEXA_UNBOX_* levers ON, to re-measure the CODEGEN-BOXED-SCALAR-SLOWPATH ratio vs gcc-O2 (stale 8.6× geomean was pre-lever). Quantifies whether the call-arg lever is worth authoring.
- [RANK-12] L5 B4 place-projection census — implement full field place-projection per l5_place_projection_design.md (28KB, ready), re-run B4 census; if the 3 field-disjoint FPs (#4909 NO-GO) go to 0, flip B4 warn→error with byteeq 3-target.

## CLOUD (arm64/darwin)
- [L1 · axis-③ parity leg, scoped OUT of x86_64 flip] arm64-linux own ELF linker LINK half — ENTIRELY MISSING (compile half exists: elf_arm64.hexa serialize_elf_arm64 :202-203, main.hexa:1041-1059). Author the aarch64 twin of each x86_64 own-start piece (structural mirror of elf_x86_64.hexa: link_ownstart :1849→_ownstart_ar :3208→serialize_elf_exec :3510→parse_elf/ar; no serialize_elf_exec_arm64 / arm64 _start / aarch64 reloc-at-vaddr / link_elf_arm64_ownstart exist; hexa_ld.hexa hardcodes EM_X86_64=0x3E at :504/:1063/:1392). Authoring is LOCAL; VERIFICATION needs a cloud arm64-linux pod (no arm64 self-hosted host). Multi-PR — do NOT block the x86_64 flip. state/hexa-own/axis3_arm64_ownlink_campaign.md.
- [L1+L2 · darwin default-flip] #4966 selfhost-native-build darwin-arm64 RED attribution (inherited vs flip) on cloud macos-15, then wire --linker=hexa (HEXA_LD Mach-O, tool/hexa_ld.hexa) into darwin leg-B opt-in-first (self/main.hexa ~:3603-3620) and flip HEXA_NATIVE_DARWIN default-ON via opt-in→byteeq→smoke. selfhost-native-build-gate.yml:81. Low urgency (darwin leg is itself opt-in).
- [L2 · darwin native-serve] build the corpus with HEXA_NATIVE_DARWIN=1 (body implemented, self/main.hexa:3694+ _bn_dar: aprime --emit=obj arm64-apple-darwin + ld64 -lSystem -syslibroot + codesign), verify Mach-O byte-eq vs the clang path + shipping smoke GREEN, then drop the `&& env_var("HEXA_NATIVE_DARWIN")=="1"` clause at self/main.hexa:3689. runtime.a already staged by install.sh; recipe proven on P1 corpus (axis3-darwin-route-design.md).

## MEASURED-TERMINAL (증명된 벽)
- [L1] self-emit .o memory wall = 19.87 GB peak RSS — the HEXA_SELFEMIT_PACK_OUT lineage-narrowing 6.8 GB target is FALSIFIED at full-self-emit scale (RunPod x86_64, 124GB, clang-18; verdict AMBER, l1_axis3_r7_verify.md 4-signal table). R7 delivered correctness-GREEN (self.o=6,869,920 B valid ELF REL) but NO memory win. NOT a default-flip blocker (correctness green, memory is a build-host RSS concern). Reopen only with a different carrier strategy (map-struct×5 arena rearch), NOT PACK_OUT.
- [L3] SANCTIONED-IRREDUCIBLE nm-UND floor — do NOT re-derive: net-FFI (socket/connect/bind), exec-family (execve/posix_spawn), CUDA opt-in (separate runtime.cuda.a, byteeq-neutral), dl* (S1-relocated out of canonical runtime.a, closed #4753), and the already-DROPPED default-ON set {strtod, environ/atexit, malloc/free/calloc, strcmp, fgets/glob/qsort/regex} closed #4449/50/52/535/651/656/747/602/679. Terminal by governance (native-canonical-default: external deps opt-in-flag-only) + measurement. axis2_post_flip6_floor_census.md. NOTE nm-UND-∅ is explicitly NOT the DONE line — TRACK-S substrate elimination is (see user_decision).
- [L5] R7 escape-kind widening — NO candidate. Escape kinds exhausted (R3 mutation/store, R4 literal construction, R5 closure capture, R6 _bck_active soundness gate); Fable-confirmed no R7 (ing 32bf1d39c). Reopen only if a NEW escape shape surfaces in a real leak the FP census finds. Do not merge a filler escape rung.
- [L5] Heap reclamation free=NO-OP — the deliberate GC-free arena design (letregion bump-alloc default-ON), so no malloc/free UAF exists to check beyond the HX3060 handle lane. Frame-arena reclaim measured 2-rung terminal (only stack-alloc #4697 wins; STREAM_RECLAIM banned; 22.7GB device-terminal — project_hexa_arena_reclaim_measured_terminal). GC-free safety = arena scoping + HX3060, not a malloc/free UAF checker.
- [L2] x86_64 HexaVal pair-carry SILENT miscompile — CLOSED, do not re-derive (#4764/4774/4780/4788, ARCHITECTURE.json). Live-crash-census substituted by #4843 PR-CI (byteeq 3-target + install smoke), also closed.

## USER-DECISION (owner 판단)
- [L3 · THE actual axis-② DONE line, biggest commitment] TRACK-S substrate elimination — runtime.c + runtime_core.c are STILL clang-compiled generated C (stage_resolve_runtime_a:4272/4303, self/main.hexa:1676); the literal CLAUDE.md 'no runtime.c' criterion ②. ~1090 distinct fn / ~32.7kLOC own-emit port (67% HexaVal machinery portable, 21% raw-syscall, 6.6% vendor-glue, 5.1% C-primitive-with-escape). Needs an OWNER CALL to commit the aiden-heavy multi-PR RFC061 TRACK-S ladder (FRAG-KILL fragments → runtime_core.c 17-cluster drain HEXA_ZEROC_RT_CORE_* → m3 all-or-nothing TU-drop → retire hxlcl_shim TU + runtime_core_sysheaders.h). rfc061-axis2-runtime-zero-attack-plan.md §4. This is the real DONE gate, not the nm-UND floor.
- [L3 · Fable, prerequisite for TRACK-S vendor-glue port] __hx_cabi_call indirect C-ABI call primitive — the ONLY still-absent codegen primitive (repo grep = 0 hits, confirmed). Fable design task: spec an additive name-prefix hook (same frozen-safe class as __hx_fn_addr — no new keyword, no parser change, frozen 151c52c8 untouched) lowering an indirect call through a resolved fn-ptr with C-ABI arg classification on x86_64+arm64. Deliver implementation-ready spec; then aiden builds+byteeq. Gates the 72-fn vendor-glue class + runtime_ffi_dyn.c→.hexa.
- [L4 · Fable, gates 3-target f64-unbox flip] arm64 fp-ABI param classifier — the x86 SSE-ABI whitelist (x86_64_linux.hexa:2560-2566) has NO arm64 equivalent, so any f64-unbox lever is x86-only, blocking a 3-target default-ON flip (release-integrity requires all-3-target GREEN). Fable design: an arm64 classifier routing float/double params to d0/d1 per AAPCS64. Standing cross-target ABI wall until designed (project_hexa_rfc061_hxlcl_crosstarget_abi_wall — pair-ABI FALSIFIED, residual arm64 fp-ABI).
- [L5 · Fable strategy fork, the real L5 north-star] borrowck new-detection-power = 0 on shipping corpus (census-v2 #4943 authoritative, l5_census_v2_verdict.md:19-63) — engine PROVEN real (rustc E0502/0499/0503/0384 fixtures fire RED) but corpus gives it no fuel: 0 lines @own/&mut adoption + free=NO-OP. Verdict: 'next rung is NOT turning on more checker code.' Owner must choose L5 north-star: corpus @own/&mut migration campaign (self/+stdlib/, gives loan/move's 19 lanes real inputs) vs GC-free-reclaim. Do NOT merge more opt-in checker lanes as filler.
- [L1 · Fable ordering, endgame] __errno_location→own TLS cell + backtrace×2 self-symtab reader — cannot drop until ALL syscall-leaf callers are native (shared errno epilogue target, attack-plan marks '종반'); backtrace×2 needs an own DWARF/unwind reader whose budget may be a measured wall (sanction as permanent floor if over-budget). Fable ordering/scope task, not actionable until strtoll/signal/SYSLEAF/fork flips land.
