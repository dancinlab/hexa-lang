# typed-leaf ABI/레이아웃 감사 — 워크플로우 wf_ba079006-120

생성 2026-07-14 · 30 에이전트 · 브리지 정답지 추출 → i64/f64 ABI 경계 전수 census → 경계별 적대적 반증 → 설계 3안 → 통합
확정 23건 · 반증된 전제 4건 · 신규 확정 = 클래스 C(디스크립터 레이아웃)

```json
{
  "summary": "typed-leaf(i64/f64) 스칼라-ABI 미스매치 — __hxw_ 브리지 reference-match + .s-seed U-floor 게이트 공백 설계",
  "agentCount": 30,
  "logs": [
    "확정 미스매치: 23건"
  ],
  "result": {
    "confirmed_count": 23,
    "confirmed": [
      {
        "symbol": "hexa_arr_i64_new / cap",
        "kind": "ingress-param",
        "c_abi": "self/runtime_core_emit.hexa:1126 `HexaVal hexa_arr_i64_new(int cap);` (extern arm :2802). 네이티브 호출자도 동일 — compiler/codegen/x86_64_linux.hexa:3235-3239 `// cap in rdi (single C-ABI int reg)` + `mov rdi, <cap>` / `call hexa_arr_i64_new`",
        "seed_abi": "self/native/array_typed_leaf_x86_64.s:28-29 `mov [rbp-128], rdi # store tag L0` / `mov rbx, rsi # ingress param payload` — cap payload 를 rsi 에서 읽음. arm64: array_typed_leaf_arm64.s:21 `stp x0, x1, [sp, #0] ; ingress param 0` → :23-28 `ldp x0,x1,[sp,#0]` … `cmp x1, x3` (payload=x1)",
        "mismatch": true,
        "evidence": "호출자는 cap 을 rdi/x0(스칼라) 에 넣는데 시드는 rdi/x0 를 TAG 로 버리고 payload 를 rsi/x1(미초기화) 에서 읽는다. cap<1 clamp 가 쓰레기값에 걸리고 malloc 바이트수가 난수화된다.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "The mismatch is real and reproducible from source. C-ABI side: self/runtime_core_emit.hexa:1126 declares `HexaVal hexa_arr_i64_new(int cap);` (public runtime.h), extern arm at :2802, and the #else C body uses the same scalar `int cap` — so cap travels in rdi/w0 alone. The native backend calls it identically: compiler/codegen/x86_64_linux.hexa:3235-3241 emits `mov rdi, <imm cap>` + `call hexa_arr_i64_new` with rsi NEVER written (the adjacent push site does explicitly set the rdi:rsi pair, proving the contrast is deliberate, not an oversight in my reading). Seed side: self/native/array_typed_leaf_x86_64.s:28-29, inside the real `hexa_arr_i64_new:` body (label :16), does `mov [rbp-128], rdi # store tag L0` / `mov rbx, rsi # ingress param payload` — it discards rdi as a TAG and reads the cap payload from uninitialized rsi, then runs the `cap<1` clamp (`__hx_payload_lt`) on that garbage and feeds cap*8 to `call malloc`. arm64 mirrors it: array_typed_leaf_arm64.s:19-23 `stp x0,x1,[sp,#0] ; ingress param 0` → `cmp x1, x3`, payload from uninitialized x1, then :47 `mul x1,x1,#8` + `bl _malloc` (byte count in x1 while malloc's C-ABI arg reg x0 carries the tag 0). All five refutation avenues fail: (1) the prototype is truly scalar int, not HexaVal; (2) no pair-calling branch exists in codegen for this symbol; (3) the cited asm is inside the function body, not a comment or a neighbor; (4) no __hxw_/__hxseed_ bridge wraps it — the bridge block at runtime_core_emit.hexa:1817-1836 aliases only the 10 arena/blk symbols, and grep for hxw_/hxseed_ has zero hits on arr_i64/arr_f64; (5) it IS exported and staged — `.globl hexa_arr_i64_new` / `.globl _hexa_arr_i64_new`, and tool/stage_resolve_runtime_a:574-631 assembles the seed, ar's it into runtime.a and sets HEXA_RT_ARRAY_I64_LEAF_NATIVE=1, which switches runtime_core.c to the extern arm so the seed becomes the sole definition. The same defect class also holds for hexa_arr_i64_len (payload returned in rdx while the `int` C-ABI caller reads eax), matching the isolated `len -> 0` measurement.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_i64_new → malloc(16)",
        "kind": "outgoing-call",
        "c_abi": "raw libc `void* malloc(size_t)` — n in rdi/x0, 반환 ptr in rax/x0 단독. 시드 선언 stdlib/runtime/array_typed_leaf.hexa:53 `extern fn malloc(n: int) -> int`",
        "seed_abi": "x86_64.s:50-54 `mov rsi, 16 # hv arg payload` / `mov rdi, 0 # tag default = TAG_INT` / `call malloc` / `mov [rbp-160], rax # store tag L4` / `mov r15, rdx # hv: unbox user-call result payload`. arm64.s:41-45 `movz x0,#0 ; TAG_INT` / `movz x1,#16` / `bl _malloc` / `stp x0,x1,[sp,#64]` (payload=x1)",
        "mismatch": true,
        "evidence": "클래스 A. 인자: malloc 은 rdi/x0=0 을 읽어 malloc(0). 반환: 진짜 ptr 은 rax/x0 인데 시드는 rdx/x1(죽은 레지스터) 을 payload 로 채택 → 디스크립터 포인터가 쓰레기. carrier(hexa_heap_alloc) 치환 대상.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "Directly verified in the repo; every cited line matches. stdlib/runtime/array_typed_leaf.hexa:53 declares `extern fn malloc(n: int) -> int` (raw libc egress, no __hxw_/__hxseed_ bridge in the file — grep 0 hits). self/native/array_typed_leaf_x86_64.s:50-54 emits `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default = TAG_INT` / `call malloc` / `mov [rbp-160],rax # store tag L4` / `mov r15,rdx # hv: unbox user-call result payload` — so libc malloc reads rdi=0 (the TAG) => malloc(0), the real pointer in rax is filed as the tag, and the payload is taken from rdx (dead after a C-ABI call). arm64.s:41-46 mirrors it: `movz x0,#0 ; TAG_INT` / `movz x1,#16` / `bl _malloc` / `stp x0,x1,[sp,#64]` (payload=x1=garbage), and that garbage x1 is then used as the store address (`add x1,x1,x3` / `str x5,[x1]` for __hx_ptr_store64). The second malloc(bytes) call has the identical shape. Refutation avenues all closed: (a) the C prototype is raw libc `void* malloc(size_t)`, not HexaVal; (b) no codegen special-case — `grep \"\\\"malloc\\\"\" compiler/codegen/*.hexa` = 0 hits; the x86_64_linux.hexa:3235-3239 special-casing concerns hexa_arr_i64_new callers, not the seed's outgoing libc calls; (c) no hexa-side `malloc` definition/interposition in self/native/*.s, and the Linux-glibc `#define malloc(n) hxlcl_malloc(...)` rename (self/runtime_core_emit.hexa:386-398) is a C-preprocessor shim that cannot rewrite the pre-emitted .s relocation — and hxlcl_malloc is itself C-ABI scalar, so the mismatch holds either way; (d) the symbol is live (.globl hexa_arr_i64_new, ELF .hidden / Mach-O .private_extern = still statically linked); (e) the quoted text is real instruction text inside hexa_arr_i64_new (label .L6cf9_hexa_arr_i64_new_bb2), not a comment. Both the argument and the return are wrong => class-A confirmed; carrier (hexa_heap_alloc) substitution is the correct fix for this site, though it is necessary-not-sufficient (the class-B ingress/return mirror breakage is independent).",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_i64_new → malloc(cap*8)",
        "kind": "outgoing-call",
        "c_abi": "동일 raw libc malloc C-ABI (rdi/x0 인자, rax/x0 반환)",
        "seed_abi": "x86_64.s:70-75 `mov rsi, [rbp-72]` / `mov rdi, [rbp-184] # tag L7` / `call malloc` / `mov r10, rdx # unbox user-call result payload`. arm64.s:55-58 `ldp x0,x1,[sp,#112]` / `bl _malloc` / `stp x0,x1,[sp,#128]`",
        "mismatch": true,
        "evidence": "클래스 A, 두 번째 사이트(데이터 버퍼). new() 안에 malloc 이 2개다 — carrier 치환 시 둘 다 바꿔야 한다.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. Both malloc call-sites inside hexa_arr_i64_new are genuine C-ABI/pair-ABI mismatches, verified by reading the seeds directly. self/native/array_typed_leaf_x86_64.s: site 1 (16B descriptor) at L50-54 does `mov rsi,16 / mov rdi,0 / call malloc / mov [rbp-160],rax (rax kept as TAG) / mov r15,rdx (payload from rdx)`; site 2 (cap*8 data buffer) at L70-75 does `mov rsi,[rbp-72] (=cap*8) / mov rdi,[rbp-184] (tag L7 = imm 0) / call malloc / mov r10,rdx`. arm64 (L55-58) mirrors it: `ldp x0,x1,[sp,#112]` puts tag in x0 and size in x1, then `bl _malloc`, then `stp x0,x1,[sp,#128]`. So libc malloc receives size=0 (the tag) in rdi/x0 at BOTH sites, and its rax/x0 return is discarded while dead rdx/x1 garbage becomes the \"pointer\" — the very next __hx_ptr_store64 writes through it, an exact mechanism for the measured SIGSEGV 139. Every refutation avenue is closed: (1) the declaration is `extern fn malloc(n: int) -> int` (stdlib/runtime/array_typed_leaf.hexa:53) and the callee is real libc `void* malloc(size_t)` — not a HexaVal pair; (2) no codegen special-case — `grep '\"malloc\"' compiler/codegen/*.hexa` = 0 hits, and the .s is the frozen emitted artifact anyway; (3) no pair-ABI `malloc` definition exists anywhere in self/ — runtime.c's `#define malloc(n) hxlcl_malloc(...)` is a C-preprocessor rename confined to that TU and cannot rewrite the `malloc` relocation already in the seed .o; no --wrap/--defsym in tool/ or compiler/; (4) the `__hxw_` bridge is NOT used by this seed (0 grep hits); (5) it is live — `.globl hexa_arr_i64_new` plus tool/stage_resolve_runtime_a:576 auto-sets HEXA_RT_ARRAY_I64_LEAF_NATIVE=1 whenever build/array_typed_leaf_native.o exists. The seed's own header (array_typed_leaf.hexa:29-34) asserts the falsified belief that plain-int params ride the C-ABI reg/rax. The claim's \"2 mallocs, both must be swapped\" is right; site 1 allocates the escaping descriptor, so it needs hexa_heap_alloc (not the arena).",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_i64_push / x (int64_t)",
        "kind": "ingress-param",
        "c_abi": "`int64_t x` → rdx 단독 (x86_64_linux.hexa:3246-3252 `// x (RAW element payload) → rdx` + `_x86_hv_payload(s.args[pi], …, \"rdx\", opts)`) / arm64: x2 단독 (arm64_darwin.hexa:2559 `raw int x in x2` + :2576 `mov x2, x10`)",
        "seed_abi": "x86_64.s:149-150 `mov [rbp-296], rdx # store tag L1` / `mov r12, rcx # ingress param payload` → x 를 rcx 에서 읽음. arm64.s:108 `stp x2, x3, [sp, #16] ; ingress param 1` → :207-209 `ldp x4, x5, [sp,#16]` / `str x5, [x1]` → 저장하는 원소는 x5 = x3",
        "mismatch": true,
        "evidence": "★ 핵심 결함. 호출자가 넣은 x(rdx/x2) 는 시드의 TAG 슬롯으로 흘러가 버려지고, 실제 저장 원소는 rcx/x3(미초기화 쓰레기). malloc 을 carrier 로 바꿔도 push 는 계속 쓰레기를 저장한다.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. (1) Prototype is a C-ABI scalar: `HexaVal hexa_arr_i64_push(HexaVal v, int64_t x)` at self/runtime_core_emit.hexa:1127 & :2821 and self/runtime.h:279 -> v in rdi:rsi, x in rdx alone. (2) The native backend calls it the same C-ABI way: compiler/codegen/x86_64_linux.hexa:3246-3252 (`// x (RAW element payload) -> rdx` + `_x86_hv_payload(s.args[pi], st, rm, \"rdx\", opts)`, rcx never written) and compiler/codegen/arm64_darwin.hexa:2559/2576 (`raw int x in x2` + `mov x2, x10`, x3 never written). (3) The seed reads it as a pair: stdlib/runtime/array_typed_leaf.hexa:76 declares `pub fn hexa_arr_i64_push(v: HexaVal, x: int)` so aprime lowers x as (tag,payload). self/native/array_typed_leaf_x86_64.s:136 label -> :150-151 `mov [rbp-296], rdx # store tag L1` / `mov r12, rcx # ingress param payload`; the element store at the __hx_ptr_store64 site is `mov rsi, r12` / `mov qword ptr [r10], rsi` -> it stores rcx (garbage), never rdx. self/native/array_typed_leaf_arm64.s:102 label -> :108 `stp x2, x3, [sp, #16]` -> :207-209 `ldp x4, x5, [sp,#16]` / `str x5, [x1]` where x5 = the sp+24 half = x3, not x2 (same in array_typed_leaf_arm64-linux.s). (4) Refutation avenues all fail: prototype is NOT HexaVal; there is NO pair-calling special case in codegen; the quoted asm is real instructions under the real `hexa_arr_i64_push` label (param0 v IS correctly a pair rdi:rsi / x0:x1, proving the pair-lowering, and param1 is the broken one); NO `__hxw_`/`__hxseed_` bridge covers arr symbols (the bridge block at runtime_core_emit.hexa:1817-1860 aliases exactly 10 arena/blk symbols, zero arr entries, and a bridge would only help C callers anyway since codegen `call`s the bare symbol); the symbol IS exported (.globl + .hidden/.private_extern in all 3 seeds) and its .o overrides the C body in runtime.a, with scalar-ABI callers on both paths (self/codegen.hexa:4756 `hexa_arr_i64_push(__nia, (int64_t)HX_INT(...))`, self/runtime_core.c:2714). Net: the caller's x (rdx/x2) lands in the seed's dead TAG slot and the stored element comes from rcx/x3 = uninitialized garbage — independent of the malloc/realloc egress class, matching the measured isolated repro (len->0, box->index out of bounds with a hand-built valid descriptor).",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_i64_push → realloc",
        "kind": "outgoing-call",
        "c_abi": "raw libc `void* realloc(void* p, size_t n)` — p→rdi/x0, n→rsi/x1, 반환 rax/x0. 시드 선언 array_typed_leaf.hexa:54 `extern fn realloc(p: int, n: int) -> int`",
        "seed_abi": "x86_64.s:235-243 `mov rsi, [rbp-120] # hv arg payload`(old ptr) / `mov rdi, [rbp-392] # tag` / `mov rcx, [rbp-136] # hv arg payload`(nbytes) / `mov rdx, [rbp-408] # tag` / `call realloc` / `mov r10, rdx # unbox user-call result payload`. arm64.s:171-175 `ldp x0,x1,[sp,#208]`(old) / `ldp x2,x3,[sp,#240]`(nbytes) / `bl _realloc` / `stp x0,x1,[sp,#256]`",
        "mismatch": true,
        "evidence": "클래스 A. libc 가 보는 것: realloc(rdi=0 → NULL, rsi=old_ptr → size). 즉 old 포인터가 SIZE 로 해석되어 거대 malloc 시도, nbytes 는 rcx/x3 로 사라짐. 반환도 rdx/x1 에서 읽어 쓰레기. → hexa_heap_realloc carrier 대상.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf.hexa:54 declares `extern fn realloc(p: int, n: int) -> int` and calls it at L84 in the grow branch. (2) self/native/array_typed_leaf_x86_64.s:235-243 emits the hexa pair-ABI exactly as alleged: `mov rsi,[rbp-120]` (old ptr payload) / `mov rdi,[rbp-392]` (tag, materialized 0 at L215) / `mov rcx,[rbp-136]` (nbytes payload) / `mov rdx,[rbp-408]` (tag) / `call realloc` / `mov [rbp-416],rax # store tag L16` / `mov r10,rdx # unbox user-call result payload`. So SysV libc sees realloc(NULL, old_ptr-as-size), nbytes is stranded in rcx, the true returned pointer lands in rax and is filed as the TAG, and the payload is read from clobbered rdx — the subsequent __hx_payload_eq(nd,0) OOM check then tests garbage. (3) arm64.s:171-175 identical: `ldp x0,x1,[sp,#208]` (tag,old) / `ldp x2,x3,[sp,#240]` (tag,nbytes) / `bl _realloc` / `stp x0,x1,[sp,#256]` — AAPCS64 realloc(x0=NULL, x1=old_ptr as size), nbytes stranded in x3, result tag=true-ptr / payload=garbage. All five refutation avenues closed: C prototype is `HexaVal hexa_arr_i64_push(HexaVal v, int64_t x)` (self/runtime.h:279) but the CALLEE is libc realloc with a fixed C-ABI; no codegen branch pair-calls bare `realloc` (x86_64_linux.hexa hits are all hxlcl_realloc Route-C leaf whitelist, a different symbol); no __hxw_/__hxseed_ bridge touches it (grep: zero bridge refs in array_typed_leaf.hexa, and the .s emits a literal `call realloc`/`bl _realloc`); the `#define realloc(p,n) hxlcl_realloc(...)` in self/runtime_core_sysheaders.h:162 is a C-preprocessor macro that cannot rewrite an assembly symbol reference, and nm shows _hxlcl_realloc is STB_LOCAL (`t`) hence link-invisible; and `pub fn hexa_arr_i64_push` is one of the 4 exported global T symbols on the hot push path. Scope note (not a refutation): the currently-built build/runtime.a is the C variant (runtime.o defines T _hexa_arr_i64_push), so the leaf-native seed is default-OFF today — this is a latent defect that fires on the flip, which is precisely what PR #4930 attempted and #4932 reverted. hexa_heap_realloc carrier is the correct fix.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_i64_push → write (OOM path)",
        "kind": "outgoing-call",
        "c_abi": "raw libc `ssize_t write(int fd, const void* buf, size_t n)` — fd→rdi/x0, buf→rsi/x1, n→rdx/x2. 시드 선언 array_typed_leaf.hexa:55",
        "seed_abi": "x86_64.s:343-352 `mov rsi, 2 # hv arg payload`(fd) / `mov rdi, 0 # tag` / `mov rcx, [rbp-184]`(msg) / `mov rdx, [rbp-456] # tag` / `mov r9, 20` / `mov r8, 0` / `call write`. arm64.s:246-251 `movz x0,#0`/`movz x1,#2`/`ldp x2,x3,[sp,#336]`/`movz x4,#0`/`movz x5,#20`/`bl _write`",
        "mismatch": true,
        "evidence": "클래스 A. libc 는 write(fd=0, buf=2, n=tag=0) 로 읽는다 — 3개 인자 전부 한 칸씩 밀림. OOM 메시지가 절대 안 나가고 fd 0 에 쓰기 시도. (n=0 이라 즉사는 안 하지만 완전한 무의미 호출.)",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "Verified directly in the tree, all refutation avenues fail. (1) stdlib/runtime/array_typed_leaf.hexa:55 declares raw libc `extern fn write(fd: int, buf: int, n: int) -> int` — no carrier. (2) The cited asm is real code inside the right function: self/native/array_typed_leaf_x86_64.s:336-350 under label `.L6cf9_hexa_arr_i64_push_bb3` emits `mov rsi,2 # hv arg payload` / `mov rdi,0 # tag` / `mov rcx,[rbp-184]` (msg) / `mov rdx,[rbp-456] # tag L21` / `mov r9,20` / `mov r8,0` / `call write` — i.e. (rdi,rsi)=(tag,fd), (rdx,rcx)=(tag,msg), (r8,r9)=(tag,20) pair-ABI. C-ABI libc write therefore reads fd=rdi=0, buf=rsi=2, n=rdx=msg-tag=0 → write(0,2,0). arm64 is identical (array_typed_leaf_arm64.s:246-251 `movz x0,#0`/`movz x1,#2`/`ldp x2,x3,[sp,#336]`/`movz x4,#0`/`movz x5,#20`/`bl _write`; arm64-linux.s:251 same). (3) No codegen pair-ABI/C-ABI special case for `write`: the C-ABI whitelist in compiler/codegen/x86_64_linux.hexa (~2340-2356) only matches `hxlcl_*` symbols (hxlcl_write etc.), and the emitted asm empirically proves no special-casing. (4) No `__hxw_`/`__hxseed_` bridge covers it — those bridges (self/runtime_core_emit.hexa:1817-1854) only wrap the 10 arena/blk symbols. (5) The symbol genuinely binds to libc: `nm -u build/array_typed_leaf_native.o` shows `_write` (with _malloc/_realloc), and `nm build/runtime.a` shows `U _write` in 3 members with ZERO `T` definition anywhere in the runtime — so it resolves to libc's C-ABI body at the final link. (6) hexa_arr_i64_push is an exported global T symbol, so the code is live (the write itself sits on the cold realloc-failure branch). Note incidentally that the adjacent `exit(1)` WAS routed to the pair-ABI carrier `hexa_exit`, while `write` was left as a raw libc U — exactly the class-A egress hole. Severity is low at runtime (write(0,2,0) is a harmless no-op, then hexa_exit(1) terminates), so this is not the SIGSEGV root cause, but the ABI mismatch claim as stated is correct: the OOM message never reaches stderr and the three args are each shifted one slot.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_i64_len / return",
        "kind": "return",
        "c_abi": "`int` (32-bit 스칼라) → eax/rax 단독 / w0 단독",
        "seed_abi": "x86_64.s:427-428 `mov rdx, r14 # hv arg payload` / `mov rax, [rbp-80] # tag L3 from tag-slot` (tag=TAG_INT=0). arm64.s:309 `ldp x0, x1, [sp, #48]` → x0=tag(0), x1=len",
        "mismatch": true,
        "evidence": "★ 격리 재현 `len(v) -> 0` 의 정확한 근인. 시드는 len 을 rdx/x1 에 넣고 rax/x0 에는 TAG_INT=0 을 넣는다 → C 호출자는 eax/w0 를 읽으므로 **항상 0**. malloc 과 완전 무관 — carrier 치환으로 절대 안 고쳐지는 리턴-미스매치.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "Claim CONFIRMED by direct file read; every refutation avenue checked and closed. (1) The C prototype really is a 32-bit scalar: self/runtime_core_emit.hexa:1128 emits `int     hexa_arr_i64_len(HexaVal v);` into public runtime.h and :2804 emits `extern int hexa_arr_i64_len(HexaVal v);` for the native-leaf arm (mirrored at self/runtime_core.c:1070/:2607) — NOT HexaVal, so the caller reads eax/w0. (2) The seed returns a pair: self/native/array_typed_leaf_x86_64.s:427-428 is exactly `mov rdx, r14 # hv arg payload` / `mov rax, [rbp - 80] # tag L3 from tag-slot`, and [rbp-80] was just filled with literal 0 at :425-426 (`mov r11, 0` / `mov [rbp-80], r11`) → rax = TAG_INT = 0, len in rdx. arm64 mirror self/native/array_typed_leaf_arm64.s:308-309 `stp x0,x1,[sp,#48]` (x0=0 from `movz x0,#0`) then `ldp x0,x1,[sp,#48]` → w0 = 0, len in x1. A C caller reading eax/w0 gets 0 unconditionally — the exact isolated repro (len(v)->0). (3) NOT a __hxw_ bridge case: the __hxseed_/__hxw_ alias table at runtime_core_emit.hexa:1817-1855 covers ONLY the 10 arena symbols (arena_on/alloc/head/cur/env_lo/env_hi, __blk_next/cap/used/data); hexa_arr_i64_* appears nowhere in it and is declared raw C-ABI at :2804. (4) NOT a codegen-pair-call case: `grep arr_i64_len compiler/` = ZERO hits; the native backend only calls new/push/box (x86_64_linux.hexa:3235-3239), whose own comment reaffirms the C-ABI convention (\"cap in rdi (single C-ABI int reg)\"). No branch calls len as a pair. (5) NOT harmless/unexported: `.globl hexa_arr_i64_len` (x86) / `.globl`+`.private_extern` (darwin), and tool/stage_resolve_runtime_a:576 sets HEXA_RT_ARRAY_I64_LEAF_NATIVE=1 by default whenever build/array_typed_leaf_native.o exists — that is precisely the #if arm that replaces the correct C body with the `extern int` decl bound to the pair-ABI seed, so the mismatch ships. (6) The quoted asm is real code, not a comment, and belongs to hexa_arr_i64_len (label at x86:397 / arm64:287; body ends at the ret right before `.globl hexa_arr_i64_box`). Precision notes (do not weaken the claim): the INGRESS of len is correct (`mov [rbp-56], rdi` / `mov rbx, rsi` = the proper SysV 16-byte pair for HexaVal v) — the defect is purely the RETURN, exactly as asserted; and the source stdlib/runtime/array_typed_leaf.hexa:101-102 comment literally says \"Returns a plain C int (rax)\" while aprime lowers `-> int` to the tag:payload pair, i.e. the comment states the contract the emitted code violates. Only non-refuting nuance: no compiler-emitted code currently calls hexa_arr_i64_len, so today's live blast radius is the public runtime.h surface (hand-written/bench/user C) plus its box/push siblings sharing the same seed object — but the return-ABI mismatch itself is real, shipped, and provably un-fixable by carrier substitution.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_i64_box / i (int64_t)",
        "kind": "ingress-param",
        "c_abi": "`int64_t i` → rdx 단독 (x86_64_linux.hexa:3557 `_x86_hv_payload(s.args[1], st, rm, \"rdx\", opts) // idx (raw int64)` + :3561 `call hexa_arr_i64_box`)",
        "seed_abi": "x86_64.s:454-455 `mov [rbp-176], rdx # store tag L1` / `mov r12, rcx # ingress param payload` → i 를 rcx 에서 읽음. arm64.s:322 `stp x2, x3, [sp, #16]` → :341/:359 `ldp x0,x1,[sp,#16]` 뒤 `cmp x1, x3` (payload=x3)",
        "mismatch": true,
        "evidence": "★ 격리 재현 `box(v,1) -> index out of bounds` 의 근인. 진짜 i(rdx/x2) 는 TAG 슬롯으로 버려지고 bounds 검사는 rcx/x3 의 쓰레기값에 대해 수행된다 → i<0 또는 i>=len 이 무작위로 성립.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "Claim CONFIRMED — all five refutation avenues fail on direct file inspection. (1) The C prototype is a real scalar, not a HexaVal: self/runtime.h:280 `HexaVal hexa_arr_i64_box(HexaVal v, int64_t i);` (mirrored by the emitter at self/runtime_core_emit.hexa:1129 and the leaf-native `extern` at :2768), so SysV puts v in rdi:rsi and i in rdx ALONE. (2) No pair-calling special branch exists: both call sites in compiler/codegen/x86_64_linux.hexa (:3560→:3561 index get-OOB, :3697→:3698 index_set-OOB) use `_x86_hv_payload(s.args[1], st, rm, \"rdx\", opts)  // idx (raw int64)` — a single payload register, no tag register set for arg1. (3) The cited asm is real code inside the right function: self/native/array_typed_leaf_x86_64.s:438-455 `.globl hexa_arr_i64_box` … `mov [rbp-176], rdx # store tag L1` / `mov r12, rcx # ingress param payload`, and bb0 then does `mov r10, r12` + `cmp r10, 0` (__hx_payload_lt) — so the true i (rdx) is discarded into a tag slot and the `i<0` / `i>=len` bounds checks run on rcx garbage. arm64 mirrors it: array_typed_leaf_arm64.s:316-322 `_hexa_arr_i64_box:` … `stp x2, x3, [sp, #16] ; ingress param 1`, then `ldp x0,x1,[sp,#16]` + `cmp x1, x3` — payload taken from old x3 while AAPCS64 passes i in x2 alone. (4) No __hxw_/__hxseed_ bridge: grep of stdlib/runtime/array_typed_leaf.hexa returns 0 hits (only raw-libc externs malloc/realloc/write/exit + hexa_int/hexa_throw); the seed source at :110 is `pub fn hexa_arr_i64_box(v: HexaVal, i: int) -> HexaVal`, and `i: int` is exactly what aprime lowers to a (tag,payload) pair. (5) It IS exported: `.globl hexa_arr_i64_box` (.hidden) on x86, `.globl _hexa_arr_i64_box` (.private_extern) on arm64 — hidden linkage still binds inside the final image. This is a sufficient root cause for the isolated repro `box(v,1) -> index out of bounds` with a hand-built valid descriptor, independent of malloc. Scope caveat (not a refutation): the seed body only wins when HEXA_RT_SELFEMIT / HEXA_RT_CORE_ARRAY_TYPED_LEAF_NATIVE / HEXA_RT_CORE_ARRAY_I64_LEAF_NATIVE is defined (else the correct C body at runtime_core_emit.hexa:2847 compiles), i.e. exactly the leaf-native config whose member is in the shipped runtime.a. The same mismatch class extends to hexa_arr_i64_push (scalar `int64_t x` → rdx at codegen :3248) and hexa_arr_i64_len (scalar `int` return expected in rax, seed returns payload in rdx/x1).",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_i64_box → hexa_int",
        "kind": "outgoing-call",
        "c_abi": "self/runtime.h:112 `HexaVal hexa_int(int64_t n);` — n → **rdi/x0 단독(C-ABI 스칼라)**, 반환 HexaVal rax:rdx/x0:x1. (본체는 hand-emit C-ABI 시퀀스이거나 runtime_core_emit.hexa:1777 `HexaVal hexa_int(int64_t n) { return HX_MAKE_INT(n); }` — 어느 쪽이든 첫 GP 인자를 읽는다)",
        "seed_abi": "시드 선언 array_typed_leaf.hexa:57 `extern fn hexa_int(x: int) -> HexaVal` → 유저 extern 이므로 pair 로 낮춰짐. x86_64.s:557-562 `mov rsi, [rbp-152] # hv arg payload`(x) / `mov rdi, [rbp-304] # tag L17`(=0) / `call hexa_int` / `mov r10, rdx`. arm64.s:401-403 `ldp x0, x1, [sp, #272]` / `bl _hexa_int`",
        "mismatch": true,
        "evidence": "★★ 새 발견 — libc 가 아닌 RUNTIME 심볼에서도 클래스 B 가 터진다. hexa_int 는 C-ABI 스칼라를 받는데 시드는 rdi/x0 에 TAG(=0) 를, rsi/x1 에 값을 넣는다 → hexa_int 는 항상 n=0 을 받아 **box() 가 인덱스 통과해도 언제나 0 을 리턴**. hexa_bool/hexa_throw/hexa_exit 는 무해하지만 hexa_int 는 아니다 — 'runtime 심볼이면 안전' 이라는 가정은 FALSE.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "Attempted refutation on all five avenues; every one fails, and the claim is confirmed by direct file inspection.\n\n(1) C prototype is genuinely a C-ABI SCALAR, not HexaVal: self/runtime.h:112 `HexaVal hexa_int(int64_t n);`; body self/runtime_core_emit.hexa:1778 `HexaVal hexa_int(int64_t n) { return HX_MAKE_INT(n); }`, and :1776 the `extern` under HEXA_RT_SELFEMIT/HEXA_RT_CORE_LEAF_NATIVE whose self-emitted body is the sibling of the documented `hexa_float: fmov x1,d0 / mov w0,#1 / ret` — i.e. arg read from the FIRST GP reg. Both build configs read n from rdi/x0.\n\n(2) No pair-ABI special-case in codegen — the opposite. The sole `\"hexa_int\"` hit under compiler/codegen/ is arm64_darwin.hexa:2371 inside `_emit_int_box`, which CORROBORATES the C-ABI contract: the inline form is `mov x1, x0 (payload -> x1)` / `movz x0, #0 (TAG_INT)`, so payload ARRIVES in x0 and the tag LEAVES in x0; the legacy `bl hexa_int` fallback is emitted with the raw value already in x0. The only name-keyed C-ABI whitelist, `_is_cabi` (x86_64_linux.hexa:2258), is default-OFF (requires HEXA_CABI_HXLCL=1) and lists ONLY hxlcl_* symbols — hexa_int is absent.\n\n(3) The cited asm is real instructions inside hexa_arr_i64_box, not a comment or another fn. array_typed_leaf_x86_64.s: `.globl hexa_arr_i64_box` @438, body @441, and the @549-562 window is its tail: `mov r11, 0 # materialize tag imm 0` -> ... -> `[rbp-304]`; then `mov rsi, [rbp-152]` / `mov rsi, rsi # hv arg payload` / `mov rdi, [rbp-304] # tag L17 from tag-slot` (= 0) / `call hexa_int`. arm64.s @393-403 mirrors it: `movz x0, #0 ; __hx_ptr_load64: TAG_INT` -> `stp/ldp x0,x1,[sp,#272]` -> `bl _hexa_int`, i.e. x0=TAG=0, x1=value. Both targets put the TAG in the C-ABI first-GP slot. (The RETURN side is coincidentally correct — `mov [rbp-312],rax` tag + `mov r10,rdx` payload = the rax:rdx 16B struct return — so ingress-only is broken, exactly as the claim states.)\n\n(4) The __hxw_/__hxseed_ bridge does NOT wrap hexa_int. runtime_core_emit.hexa ~1815-1860 aliases exactly the 10 arena symbols (__hxseed_arena_on/alloc/head/cur/env_lo/env_hi + __hxseed_blk_next/cap/used/data). hexa_int is not among them — it is instead CONSUMED by those wrappers (`__hxw_arena_alloc(...) { ... __hxseed_arena_alloc(hexa_int((int64_t)n)) ... }`), which is independent proof that hexa_int is C-ABI-scalar-callable from C. `grep '__hxw_|__hxseed_' stdlib/runtime/array_typed_leaf.hexa` = 0 hits.\n\n(5) It is exported and live: `pub fn hexa_arr_i64_box(v: HexaVal, i: int) -> HexaVal` at stdlib/runtime/array_typed_leaf.hexa:111-120, ending in `return hexa_int(x)` (:119), fed by the seed's user extern `extern fn hexa_int(x: int) -> HexaVal` (:57) — hence pair-lowered. `.globl hexa_arr_i64_box` present in the .s.\n\nCONSEQUENCE: hexa_int always receives n = 0 (the TAG_INT constant that the seed places in rdi/x0), so hexa_arr_i64_box returns boxed 0 for every in-bounds index. This is a genuine class-B (scalar-ABI ingress) mismatch on a RUNTIME (non-libc) symbol, so the \"runtime symbols are safe\" assumption is FALSE — hexa_bool/hexa_throw/hexa_exit being harmless does not generalize to hexa_int.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_new / cap",
        "kind": "ingress-param",
        "c_abi": "self/runtime_core_emit.hexa:1134/:3099 `HexaVal hexa_arr_f64_new(int cap);` — cap → rdi/x0 스칼라 (codegen x86_64_linux.hexa:3202 `hexa_arr_f64_new_esc` 미러도 동일 `mov rdi, ecap`)",
        "seed_abi": "self/native/array_typed_leaf_f64_x86_64.s:28-29 `mov [rbp-128], rdi # store tag L0` / `mov rbx, rsi # ingress param payload`. array_typed_leaf_f64_arm64.s:21 `stp x0, x1, [sp, #0] ; ingress param 0`",
        "mismatch": true,
        "evidence": "i64 new 와 바이트-형태 동일한 미스매치 (f64 시드는 stdlib/runtime/array_typed_leaf_f64.hexa:36 `pub fn hexa_arr_f64_new(cap: int)` — i64 의 정확한 미러).",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "Claim CONFIRMED — not refuted. (1) The C prototype really is a scalar: self/runtime_core_emit.hexa:1134 `HexaVal hexa_arr_f64_new(int cap);`, repeated as `extern` in BOTH seed-active arms (:3062 under HEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE, :3068 under HEXA_RT_SELFEMIT||TYPED_LEAF) and in self/runtime.h:281 — so cap travels in edi/w0 alone. (2) The seed body really reads the payload register: self/native/array_typed_leaf_f64_x86_64.s:14 `.globl hexa_arr_f64_new` (a real export, not a comment) with body :28-29 `mov [rbp-128], rdi # store tag L0` / `mov rbx, rsi # ingress param payload`, then `__hx_payload_lt` compares rbx; arm64 twin array_typed_leaf_f64_arm64.s:21 `stp x0,x1,[sp,#0]` then `cmp x1, x3`. Caller sets only rdi/w0; seed consumes rsi/x1 = garbage. (3) No __hxw_ bridge covers it — the bridge block (runtime_core_emit.hexa:1817-1854) wraps exactly the 10 arena symbols; grep of __hxw_/__hxseed_ in stdlib/runtime/array_typed_leaf_f64.hexa = 0 hits. (4) No pair-ABI special case in codegen — compiler/codegen/x86_64_linux.hexa:3202 calls hexa_arr_f64_new_esc, a DIFFERENT C-only symbol that the seed does not define (grep -c _esc in the seed = 0). (5) Not dead code — tool/stage_resolve_runtime_a:4504 invokes resolve_native_array_f64_leaf_seed unconditionally and its only gate (:764) is an explicit opt-out `[ \"${HEXA_RT_ARRAY_F64_LEAF_NATIVE:-x}\" = \"0\" ]`, so unset ⇒ the .o is assembled and ar'd into runtime.a (the ':642 Default-OFF' comment is stale); a real C-ABI caller exists at self/codegen.hexa:4782 which emits `hexa_arr_f64_new(N)` directly into gen2 C. Minor correction to the claim's evidence (does not change the verdict): the `hexa_arr_f64_new_esc` mirror at x86_64_linux.hexa:3202 is NOT itself mismatched — _esc is C-only and seed-absent; the genuinely broken caller is the gen2 C emit site self/codegen.hexa:4782 plus runtime_core.c-internal uses.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_new → malloc ×2",
        "kind": "outgoing-call",
        "c_abi": "raw libc malloc C-ABI (rdi/x0 인자, rax/x0 반환). 시드 선언 array_typed_leaf_f64.hexa:30 `extern fn malloc(n: int) -> int`",
        "seed_abi": "f64_x86_64.s:52-54 `call malloc` / `mov r15, rdx # unbox user-call result payload`; :73-75 두 번째 사이트. f64_arm64.s:43 / :56 `bl _malloc`",
        "mismatch": true,
        "evidence": "클래스 A, i64 와 동일한 2 사이트. carrier 치환은 i64/f64 두 시드 모두에 필요.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. Read the files directly. stdlib/runtime/array_typed_leaf_f64.hexa:30 declares raw `extern fn malloc(n: int) -> int` with no carrier and no __hxw_/__hxseed_ bridge (grep: 0 hits in that file, unlike array_core.hexa). self/native/array_typed_leaf_f64_x86_64.s:50-54 emits `mov rsi,16 / mov rdi,0 / call malloc / mov [rbp-160],rax (store tag L4) / mov r15,rdx (unbox user-call result payload)` and the second site at :71-75 does the same for the cap*8 data buffer — i.e. the size travels in rsi (payload reg) while C-ABI malloc reads rdi=0 → malloc(0), and the real returned pointer in rax is stored into the TAG half while the value payload is scavenged from the dead rdx. arm64 mirrors it: :41-43 `movz x0,#0 (tag) / movz x1,#16 (payload) / bl _malloc`, second site :55-56 — libc reads x0=0, and stp x0,x1 puts the pointer in the tag slot with garbage x1 as payload. Every refutation route is closed: (a) the C prototype is scalar `HexaVal hexa_arr_f64_new(int cap)` (self/runtime.h:281, self/runtime_core.c:1054) — that is the separate class-B ingress bug and does not make the outgoing malloc call sound; (b) no hexa-side `malloc` symbol exists to win the link (`grep 'globl.*malloc' self/native/*.s` → 0 hits), so it binds to real libc C-ABI; (c) the quoted asm is executable body of hexa_arr_f64_new (bb2 of __L1161_hexa_arr_f64_new), not a comment or another function; (d) the symbol is exported (.globl _hexa_arr_f64_new, array_typed_leaf_f64_arm64.s:13) and live (self/codegen.hexa:4782 emits calls to it; tool/regen_rtcore_array-typed-leaf_native_o.sh:53 nm-gates it). The i64 twin shows the identical 2-site pattern (array_typed_leaf_x86_64.s:52,73 · array_typed_leaf_arm64.s:43,56), so carrier substitution is indeed needed in both seeds.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_push_bits / xbits (int64_t)",
        "kind": "ingress-param",
        "c_abi": "`int64_t bits` → rdx 단독 (x86_64_linux.hexa:3203 `push_sym = \"hexa_arr_f64_push_bits\"` + :3219 `_x86_hv_payload(s.args[ei], …, \"rdx\", opts)`) / arm64 x2",
        "seed_abi": "f64_x86_64.s:149-150 `mov [rbp-296], rdx # store tag L1` / `mov r12, rcx # ingress param payload`. f64_arm64.s:108 `stp x2, x3, [sp, #16] ; ingress param 1`",
        "mismatch": true,
        "evidence": "i64 push 의 x 와 동형 — 더블의 비트가 TAG 슬롯으로 사라지고 rcx/x3 쓰레기가 원소로 저장된다 (stdlib/runtime/array_typed_leaf_f64.hexa:53 `pub fn hexa_arr_f64_push_bits(v: HexaVal, xbits: int)`).",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "Claim CONFIRMED — refutation failed on every avenue. (1) Callee: self/native/array_typed_leaf_f64_x86_64.s:136-150 is the genuine `hexa_arr_f64_push_bits` body (label :136, `.loc 1 53 0` → array_typed_leaf_f64.hexa:53). Its prologue reads param1 as a (tag,payload) PAIR: `mov [rbp-296], rdx # store tag L1` / `mov r12, rcx # ingress param payload`. r12 is the sole non-prologue/epilogue use in the body — line 292 `mov rsi, r12 # hv payload` feeds line 294 `mov qword ptr [r10], rsi # __hx_ptr_store64: *(addr) = val`, i.e. rcx IS the stored element. arm64 mirror array_typed_leaf_f64_arm64.s:108 `stp x2, x3, [sp,#16] ; ingress param 1` — same pair read. (2) Callers pass a SCALAR in rdx/x2 only: codegen compiler/codegen/x86_64_linux.hexa:3219 `_x86_hv_payload(s.args[ei], st, rm, \"rdx\", opts)` (comment :3196-3199 explicitly: \"rdi:rsi=descriptor, rdx=payload word\", all-GPR), and it never writes rcx; C shim self/runtime_core_emit.hexa:3065-3066 declares `extern HexaVal hexa_arr_f64_push_bits(HexaVal v, int64_t xbits);` and calls it from `hexa_arr_f64_push(HexaVal v, double x)` — SysV lands the 16B HexaVal in rdi:rsi and int64_t in rdx (same at :2925/:2927). So the double's bits land in the seed's TAG slot [rbp-296] and caller-garbage rcx becomes the element. Refutation avenues all dead: prototype is int64_t not HexaVal; no special pair-call branch (identical scalar path to hexa_arr_i64_push); asm cited is real code not a comment; the __hxseed_/__hxw_ bridge block (runtime_core_emit.hexa:1817-1858) covers only the 10 ARENA symbols — zero arr_f64 coverage; symbol IS exported (.globl x86:133 / .private_extern arm64:100) and tool/stage_resolve_runtime_a:804 gates seed adoption on exactly those 4 .globl hexa_arr_f64_* symbols then sets HEXA_RT_ARRAY_F64_LEAF_NATIVE=1 (:765,:812), so it ships and is live on both the C-shim and HEXA_PACK_ESCAPING codegen paths. Corroboration: the i64 twin's prologue (array_typed_leaf_x86_64.s:147-150) is shape-identical (`mov [rbp-296], rdx` / `mov r12, rcx`) and is already measured-SIGSEGV, confirming the isomorphism the claim rests on.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_push_bits → realloc / write / hexa_exit",
        "kind": "outgoing-call",
        "c_abi": "realloc·write = raw libc C-ABI (array_typed_leaf_f64.hexa:31-32) · hexa_exit = HexaVal(runtime.h:846)",
        "seed_abi": "f64_x86_64.s:241-243 `call realloc` / `mov r10, rdx`; :350-352 `call write`; :356-358 `call hexa_exit`. f64_arm64.s:173 `bl _realloc` / :251 `bl _write` / :255 `bl _hexa_exit`",
        "mismatch": true,
        "evidence": "realloc·write = 클래스 A (i64 와 완전 동형: 인자 한 칸 밀림 + 반환을 rdx/x1 에서 수취). hexa_exit 만 무해. f64 시드의 U-floor 도 i64 와 동일하게 malloc/realloc/write 3종.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "Claim CONFIRMED, not refuted. (1) Prototypes are raw libc C-ABI scalars: stdlib/runtime/array_typed_leaf_f64.hexa:31-32 = `extern fn realloc(p: int, n: int) -> int` and `extern fn write(fd: int, buf: int, n: int) -> int`; the seed's own header (array_typed_leaf_f64_x86_64.s:5-6) declares the U-floor \"malloc realloc write hexa_exit hexa_throw (libc-bearing)\". (2) The cited asm is genuine executable code inside hexa_arr_f64_push_bits (x86 body :136-:382): at :236-241 the seed loads rdi=tag / rsi=payload(old ptr) / rdx=tag / rcx=payload(nbytes) then `call realloc` — so libc reads rdi(=tag 0) as the pointer and rsi(=old data pointer) as the size; :243 `mov r10, rdx # hv: unbox user-call result payload` discards the real rax return and takes dead rdx. At :344-350 `write` gets rdi=0(tag)/rsi=2(fd)/rdx=tag/rcx=str/r8=tag/r9=20 — same one-slot shift. arm64 mirror confirmed at the exact cited lines: :173 `bl _realloc`, :251 `bl _write`, :255 `bl _hexa_exit`, args in x0:x1 / x2:x3 pairs, result taken from x1. (3) hexa_exit IS pair-correct — self/runtime.h:846 = `HexaVal hexa_exit(HexaVal code)` — matching the claim's own carve-out. (4) No __hxw_/__hxseed_ bridge covers them: the bridge block at self/runtime_core_emit.hexa:1817-1854 aliases only the 10 arena symbols; grep for __hxseed_realloc/__hxw_realloc/__hxseed_write/__hxw_write is empty repo-wide. (5) No codegen pair-ABI special branch: x86_64_linux.hexa realloc hits are only hxlcl_realloc commentary (:2300, :2462); :5110's pair note is about hexa_arr_*_push. (6) The `#define realloc(p,n) hxlcl_realloc(...)` macros (runtime_core.c:371 etc.) are C-preprocessor text and cannot rewrite a literal `call realloc` in a .s seed — and hxlcl_realloc is itself C-ABI, so no alias path rescues it. (7) The function is .globl and is the seed's declared nm contract (reached via the emitter's xmm0->GP C shim), so it is live. Every enumerated refutation path is closed.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_len / return",
        "kind": "return",
        "c_abi": "self/runtime_core_emit.hexa:1136/:3100 `int hexa_arr_f64_len(HexaVal v);` — eax/w0 단독",
        "seed_abi": "f64_x86_64.s:427-428 `mov rdx, r14 # hv arg payload` / `mov rax, [rbp-80] # tag L3` (tag=0). f64_arm64.s: _hexa_arr_f64_len 말미 `ldp x0, x1, [sp, #48]` (x0=tag=0)",
        "mismatch": true,
        "evidence": "i64 len 과 동일 — C 호출자는 언제나 0 을 읽는다. carrier 치환으로 안 고쳐지는 두 번째 리턴-미스매치.",
        "target": "i64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — I could not refute it. Every stated refutation avenue was checked against the files and each one fails.\n\n(1) C prototype is genuinely scalar `int`, not HexaVal. /Users/mini/dancinlab/hexa-lang/self/runtime_core_emit.hexa:1136 emits into the PUBLIC runtime.h: `int     hexa_arr_f64_len(HexaVal v);`. And :3063 / :3070 emit `extern int hexa_arr_f64_len(HexaVal v);` — these are exactly the decls used under `#if defined(HEXA_RT_SELFEMIT) || defined(HEXA_RT_CORE_ARRAY_TYPED_LEAF_NATIVE)` (mirrored at self/runtime_core.c:1056 / :2759), i.e. the arm that is live precisely when the native seed replaces the C body. C-ABI: result in eax/w0.\n\n(2) The cited asm is real, is inside the right function, and is not a comment. /Users/mini/dancinlab/hexa-lang/self/native/array_typed_leaf_f64_x86_64.s — symbol opens at :394-397 (`.globl hexa_arr_f64_len`), next symbol `.globl hexa_arr_f64_box` at :439. Inside that body:\n  425: `mov r11, 0 # materialize tag imm 0`\n  426: `mov [rbp - 80], r11 # store tag L3`\n  427: `mov rdx, r14 # hv arg payload`\n  428: `mov rax, [rbp - 80] # tag L3 from tag-slot`\n→ rax = 0 (tag), rdx = the loaded len. A C caller reading eax gets 0 unconditionally.\nSame on both arm64 seeds: array_typed_leaf_f64_arm64.s:287-309 and array_typed_leaf_f64_arm64-linux.s:287-309 end with `ldp x0, x1, [sp, #48]` where x0 was just stored as `movz x0, #0 // __hx_ptr_load32: TAG_INT` — w0 = 0, len is in x1.\n\n(3) No __hxw_ bridge covers it. The bridge block (runtime_core_emit.hexa:1817-1858) aliases exactly 10 symbols — arena_on/alloc/head/cur/env_lo/env_hi + __blk_next/cap/used/data. Zero `hexa_arr_*` entries. The bridge's own comment (:1794-1800) describes precisely this bug class but was never applied to the typed-leaf family.\n\n(4) No codegen branch calls it pair-wise. `grep -rn \"arr_f64_len\\|arr_i64_len\" compiler/` = 0 hits — the native backend never emits a call to it at all; the only arr-family calls it emits are hexa_arr_i64_new / *_new_esc, and its own comment at compiler/codegen/x86_64_linux.hexa:3235-3239 explicitly states the C-ABI convention (\"cap in rdi (single C-ABI int reg)\"). There is no special-case pair-call site to rescue it.\n\n(5) It is exported, not dead-code-harmless. `.globl hexa_arr_f64_len` (+ `.hidden` / `.private_extern`, which suppress *dynamic* export but keep it a link-visible global the C runtime binds to), and the prototype is emitted into the public runtime.h wall-piercing surface (runtime_core_emit.hexa:1133-1137, whose comment says it is \"public in runtime.h so codegen-emitted user.c can build/read/box a native HexaArrF64 ... across the runtime.o C-ABI wall\").\n\nCorroboration: the f64 return sequence is byte-for-byte identical to its measured-broken i64 twin — array_typed_leaf_x86_64.s:424-428 and array_typed_leaf_f64_x86_64.s:424-428 are the same five instructions. The i64 twin was measured returning 0 for a hand-built len=3 array. The f64 mirror is the same emitter output from the same source shape (stdlib/runtime/array_typed_leaf_f64.hexa:79-82, `pub fn hexa_arr_f64_len(v: HexaVal) -> int` — hexa `int` = boxed HexaVal → pair return).\n\nAlso worth flagging: stdlib/runtime/array_typed_leaf.hexa:101 carries the comment \"Returns a plain C int (rax)\" — the emitted asm falsifies that comment outright (rax holds the tag, 0).\n\nOnly genuine caveat (does not refute the ABI claim): there is no in-tree caller of hexa_arr_f64_len today (grep across compiler/, self/, stdlib/ finds only decl + def), so the defect is currently latent rather than actively firing — but the symbol ships exported in runtime.a (auto-enabled by tool/stage_resolve_runtime_a:765 whenever build/array_typed_leaf_f64_native.o exists) with a public runtime.h prototype that mis-describes its actual ABI. Any caller honoring that prototype reads 0.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_new",
        "kind": "ingress-param",
        "c_abi": "`HexaVal hexa_arr_f64_new(int cap);` — self/runtime_core_emit.hexa:1134 (runtime.h surface) + self/runtime_core_emit.hexa:3099 (`extern HexaVal hexa_arr_f64_new(int cap);` under the F64-leaf arm) + self/runtime.h:281. C-ABI scalar int → SysV: cap in edi/rdi (single INTEGER reg). AAPCS64: cap in w0. Callers: gen2 C emitter self/codegen.hexa:4782 (`\"({ HexaVal __nfa = hexa_arr_f64_new(\" + to_string(_nfi) + \"); \"`).",
        "seed_abi": "PAIR. self/native/array_typed_leaf_f64_x86_64.s:28-29 → `mov [rbp - 128], rdi # store tag L0` / `mov rbx, rsi # ingress param payload` — the seed reads cap's VALUE from rsi and treats rdi (which actually holds cap) as the tag. arm64: self/native/array_typed_leaf_f64_arm64.s:21 `stp x0, x1, [sp, #0] ; ingress param 0` → payload = x1 (uninitialized), tag = x0 (= the real cap).",
        "mismatch": true,
        "evidence": "x86: caller puts cap in rdi; seed's `c` = rbx = rsi = caller-scratch garbage. Then `__hx_payload_lt(c,1)` (:36-38 setl) clamps garbage → `malloc(c*8)` with a garbage byte count. arm64 identical (x1 garbage). Same shape as the i64 twin already measured SIGSEGV.",
        "target": "f64",
        "verdict": {
          "confidence": "high",
          "reason": "NOT REFUTED — the mismatch is real and every refutation avenue is closed by direct file inspection.\n\n(1) Prototype is genuinely C-ABI scalar: self/runtime.h:281 `HexaVal hexa_arr_f64_new(int cap);`, self/runtime_core_emit.hexa:1134 (runtime.h surface) and :3099/:3105 (`extern HexaVal hexa_arr_f64_new(int cap);` under both the F64-leaf arm and the SELFEMIT/TYPED arm). SysV → cap in edi/rdi (single INTEGER reg); AAPCS64 → w0. Not a HexaVal param.\n\n(2) Seed reads a PAIR. self/native/array_typed_leaf_f64_x86_64.s:14 `.globl hexa_arr_f64_new`, :17 label, :28-29 `mov [rbp - 128], rdi # store tag L0` / `mov rbx, rsi # ingress param payload` — inside the body right after the prologue, not a comment, not another function. So `c` = rbx = rsi = caller-scratch garbage, while rdi (the real cap) is discarded as a tag. Then `__hx_payload_lt(c,1)` clamps garbage and `malloc(c*8)` gets a garbage byte count. arm64 identical: array_typed_leaf_f64_arm64.s:21 `stp x0, x1, [sp, #0] ; ingress param 0` → the following `cmp x1, x3` in __hx_payload_lt compares x1 (uninitialized) while the real cap sits in x0.\n\n(3) No pair-call branch in codegen for this symbol. compiler/codegen/x86_64_linux.hexa:3202 names only the _esc variants (hexa_arr_f64_new_esc); the non-esc i64 twin at :3235-3239 is emitted explicitly as C-ABI scalar (`mov rdi, pcap` / `call hexa_arr_i64_new`). The gen2 C emitter caller self/codegen.hexa:4782 emits a plain C call with an int literal.\n\n(4) No __hxw_ bridge covers it. The bridge at self/runtime_core_emit.hexa:1817-1854 is arena-only (10 syms: arena_on/alloc/head/cur/env_lo/env_hi + blk_next/cap/used/data). Zero arr_* coverage; no __asm__ alias for any arr symbol.\n\n(5) It IS exported and shipped: `.globl hexa_arr_f64_new` / `.globl _hexa_arr_f64_new`, and tool/stage_resolve_runtime_a:806-820 selects the .s seed per target, assembles it, exports HEXA_RT_ARRAY_F64_LEAF_NATIVE=1, and ar's it into runtime.a — with no U-floor check on the .s-seed branch (the `_bad` scan exists only in the OWNOBJ arm at :791).\n\nCorroborating beyond the claim: stdlib/runtime/array_typed_leaf_f64.hexa:30-32 declares `extern fn malloc/realloc/write` (class-A raw-libc egress is present too — the x86 emit shows `mov rsi, 16` / `mov rdi, 0` / `call malloc` with the result read from rdx), and the return side is broken the same way: hexa_arr_f64_len puts the value in rdx and the tag in rax (`mov rdx, r14` / `mov rax, [rbp-80]` / `ret`), so a C caller reading eax gets the tag — exactly the i64 behavior already measured as `len -> 0` / SIGSEGV 139.\n\nKey files: /Users/mini/dancinlab/hexa-lang/self/native/array_typed_leaf_f64_x86_64.s:28-29, /Users/mini/dancinlab/hexa-lang/self/native/array_typed_leaf_f64_arm64.s:21, /Users/mini/dancinlab/hexa-lang/self/runtime.h:281, /Users/mini/dancinlab/hexa-lang/self/runtime_core_emit.hexa:1134,3099, /Users/mini/dancinlab/hexa-lang/self/codegen.hexa:4782, /Users/mini/dancinlab/hexa-lang/compiler/codegen/x86_64_linux.hexa:3235-3239, /Users/mini/dancinlab/hexa-lang/tool/stage_resolve_runtime_a:806-820.",
          "refuted": false
        }
      },
      {
        "symbol": "hexa_arr_f64_new",
        "kind": "return",
        "c_abi": "★ POINTEE LAYOUT. The returned payload must point at a C `HexaArrF64` = `typedef struct HexaArrF64 { double* data; int64_t len; int64_t cap; } HexaArrF64;` — self/runtime_core_emit.hexa:1381-1385 → ptr@0, **len@8 (int64_t)**, **cap@16 (int64_t)**, **sizeof = 24**. Corroborated by self/runtime.h:82-86 (`HexaArr { HexaVal* items; int64_t len; int64_t cap; } /* 64-bit len/cap … MUST match runtime_core.c */`) and by the f32 twin's comment self/runtime_core_emit.hexa:1390 (\"the SAME 24-byte (ptr@0, len@8, cap@16) shape\").",
        "seed_abi": "The seed mints a 16-byte descriptor with 32-bit len/cap: stdlib/runtime/array_typed_leaf_f64.hexa:22-23 header claims `HexaArrF64 { double* data @0(8B); int len @8(i32); int cap @12(i32) } sizeof 16` and the body does `let a = malloc(16)` (:41), `__hx_ptr_store32(a, 8, 0)` (:45), `__hx_ptr_store32(a, 12, c)` (:46). Emitted: x86_64.s:50 `mov rsi, 16 # hv arg payload` (the malloc size) and the store32s at :96-110 (`mov dword ptr [r10], esi # __hx_ptr_store32`); arm64.s:42 `movz x1, #16`, :228-229 `str w5, [x1] ; __hx_ptr_store32`.",
        "mismatch": true,
        "evidence": "STRUCT-LAYOUT class, independent of registers. (a) malloc(16) under-allocates a 24-byte C struct → any C `->cap` touch (@16) is an out-of-bounds heap access. (b) The seed writes cap as an i32 at +12, i.e. INTO THE HIGH HALF of C's int64 len@8 → every C reader (`hexa_arr_poly_len` :3016 `((HexaArrF64*)v.arr_ptr)->len`, `hexa_arr_poly_get` :3028-3031, `hexa_arr_poly_set` :3070-3073, and the generic `hexa_len`/HX_ARR_LEN path) sees len = (cap<<32)|len — e.g. len=0,cap=4 reads as 17179869184. (c) Conversely the seed reading a C-minted descriptor (`hexa_arr_f64_new_esc`, self/runtime_core_emit.hexa:2944-2954, which codegen calls at compiler/codegen/x86_64_linux.hexa:3202/3208) gets cap = load32(a,12) = high half of C's int64 len = 0 → `len >= cap` always true → realloc-grow on EVERY push (through the broken realloc egress below). The seed's own `LAYOUT (runtime.h HexaArrI64)` citation is stale: `grep -n 'HexaArr' self/runtime.h` shows runtime.h has NO HexaArrI64/HexaArrF64 at all, and the only HexaArr there is int64_t len/cap.",
        "target": "f64",
        "verdict": {
          "refuted": false,
          "reason": "Could not refute — the STRUCT-LAYOUT mismatch is confirmed at source on every cited line. (1) C pointee is 24B/int64: self/runtime_core_emit.hexa:1381-1385 emits `typedef struct HexaArrF64 { double* data; int64_t len; int64_t cap; }` (len@8, cap@16), corroborated by self/runtime.h:82 (HexaArr with int64_t len/cap) and the f32 twin comment at :1390 (\"the SAME 24-byte (ptr@0, len@8, cap@16) shape\"). runtime.h has NO HexaArrI64/HexaArrF64 at all, so the seed's `LAYOUT (runtime.h HexaArrI64)` citation is indeed stale. (2) The seed mints 16B with i32 len@8/cap@12: stdlib/runtime/array_typed_leaf_f64.hexa:41-46 (`malloc(16)`, `__hx_ptr_store32(a,8,0)`, `__hx_ptr_store32(a,12,c)`); the emitted asm confirms it is inside hexa_arr_f64_new, not a comment or another fn (x86 `.L1161_hexa_arr_f64_new_bb2: mov rsi,16 / call malloc`, store32 at :98-110; arm64 `movz x1,#16` + `str w5,[x1]`). (3) It ships and displaces the C bodies: tool/stage_resolve_runtime_a:1885-1888 sets -DHEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE=1 and ars the seed .o; under that guard (runtime_core_emit.hexa:3061-3065) new/len/box/push_bits become extern while `hexa_arr_f64_new_esc` (:2908-2917) REMAINS a C body minting the 24B int64 descriptor and push_bits is externed to the seed (:2924-2930) — so the seed reads cap=load32(a,12)= high half of C's int64 len = 0, giving len>=cap on EVERY push (claim (c) is a live path). (4) Reader corruption: HX_ARR_LEN(v) = ((v).arr_ptr->len) at :1469 is HexaArr's int64@8, so a seed-minted descriptor reads as (cap<<32)|len (claim (b)). All offered refutation grounds fail: the C-prototype/pair-ABI question is irrelevant (this is a pointee-struct claim, not a register claim); no codegen branch can change struct field offsets; the __hxseed_/__hxw_ bridges fix the rdi:rsi/rax:rdx pair ABI only, not sizeof/offsets, and grep confirms array_typed_leaf*.hexa never uses them; the symbols ARE exported (T hexa_arr_f64_{new,push_bits,len,box}) and ar'd into runtime.a. ONE CORRECTION that does not change the verdict: hexa_arr_poly_len/get/set branch on TAG_ARRAY_F64 but the seed's new mints plain TAG_ARRAY (__hx_make_val(5,a)), so those specific poly arms don't fire on seed-minted descriptors — they fall through to hexa_len/HX_ARR_LEN which reads the same int64@8 and corrupts identically. Likewise claim (a)'s out-of-bounds `->cap` touch @16 is not clearly reached in flag-ON mode (the C readers of ->cap are externed away), so (a) is the weakest leg; legs (b) and (c) hold at source and are sufficient.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_push_bits",
        "kind": "ingress-param",
        "c_abi": "param 1 `int64_t xbits` — scalar INTEGER → next free INTEGER reg after v consumed rdi:rsi = **rdx** (AAPCS64: **x2**). Two callers, both put it there: (1) the C shim `HexaVal hexa_arr_f64_push(HexaVal v, double x) { union { double d; long long i; } u; u.d = x; return hexa_arr_f64_push_bits(v, u.i); }` — self/runtime_core_emit.hexa:3103; (2) the native backend mint loop — compiler/codegen/x86_64_linux.hexa:3218-3222 `_x86_hv_payload(s.args[ei], st, rm, \"rdx\", opts)` + `mov rsi, dst` + `call hexa_arr_f64_push_bits`, with the comment at :3216-3217 spelling it out: \"hexa_arr_f64_push_bits(HexaVal v, int64_t bits). Both v → rdi:rsi, payload word → rdx (all-GPR).\"",
        "seed_abi": "PAIR. x86_64.s:149-150 → `mov [rbp - 296], rdx # store tag L1` / `mov r12, rcx # ingress param payload` — the seed takes rdx as xbits' TAG and reads the VALUE from **rcx**. arm64.s:108 → `stp x2, x3, [sp, #16] ; ingress param 1` → tag=x2, value=**x3**.",
        "mismatch": true,
        "evidence": "★ THE core class-B defect for f64. The element bits arrive in rdx/x2 and the seed stores rcx/x3 (caller-scratch, never set) into data[len] → every pushed double is garbage. Both callers (C shim AND the native mint site) are affected; note the codegen call site is emitted assembly, so a cpp `#define`/`__hxw_` bridge can NEVER cover it.",
        "target": "f64",
        "verdict": {
          "refuted": false,
          "reason": "Confirmed by direct file inspection. (1) Seed: self/native/array_typed_leaf_f64_x86_64.s:136 defines hexa_arr_f64_push_bits; :147-150 read param0 as pair (tag=rdi, payload=rsi) and param1 as pair (tag=rdx, VALUE=rcx → r12). r12 is never rewritten (only push@141 / mov@150 / pop@329) and is consumed at :292 `mov rsi, r12` → :294 `mov qword ptr [r10], rsi # __hx_ptr_store64`, i.e. data[len] = rcx (caller-scratch). arm64 mirror: array_typed_leaf_f64_arm64.s:108 `stp x2, x3, [sp,#16] ; ingress param 1`, reloaded :207 `ldp x4,x5,[sp,#16]` and stored :209 `str x5,[x1] ; __hx_ptr_store64` → data[len] = x3. Same in array_typed_leaf_f64_arm64-linux.s. (2) Both callers use the C-ABI scalar slot: the C shim self/runtime_core_emit.hexa:3065-3066 (inside `#if defined(HEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE)`) declares `extern HexaVal hexa_arr_f64_push_bits(HexaVal v, int64_t xbits)` and calls it with bits → rdx/x2; the native mint loop compiler/codegen/x86_64_linux.hexa:3216-3222 emits `_x86_hv_payload(..., \"rdx\", ...)` + rdi=tag + rsi=descriptor + `call hexa_arr_f64_push_bits`, with the comment at :3216-3217 stating \"payload word → rdx (all-GPR)\" — no tag register is emitted for the bits arg and there is no per-symbol pair-ABI special case anywhere in codegen. (3) Every refutation avenue fails: the prototype is int64_t (not HexaVal); stdlib/runtime/array_typed_leaf_f64.hexa:53 declares `xbits: int` which aprime lowers to a pair; there is NO __hxw_/__hxseed_ bridge for the typed-leaf symbols (`grep -rn \"__hxw_arr\\|__hxseed_arr\" self/` = 0 hits; the bridge at self/runtime_core_emit.hexa:1817-1854 covers only the 10 arena/blk symbols); the symbol IS exported (.globl/.private_extern) and is in the runtime.a keygroup at tool/stage_resolve_runtime_a:775; the quoted asm lines are real instructions, not comments, and the cited line numbers (x86 149-150, arm64 108) match exactly. Net: v transits correctly (16B struct pair == rdi:rsi/x0:x1) but the element bits arrive in rdx/x2 while the seed reads rcx/x3, so every pushed double is garbage. Because one of the two callers is backend-emitted assembly, a cpp #define/__hxw_ static-inline bridge can never cover it.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_push_bits",
        "kind": "ingress-param",
        "c_abi": "param 0's POINTEE — C guarantees v.arr_ptr → HexaArrF64 {double* data@0; int64_t len@8; int64_t cap@16} (self/runtime_core_emit.hexa:1381-1385). C-minted descriptors (hexa_arr_f64_new_esc :2944-2950 `a->len = 0; a->cap = cap;`) obey it.",
        "seed_abi": "stdlib/runtime/array_typed_leaf_f64.hexa:55-56 `let len = __hx_ptr_load32(a, 8)` / `let cap = __hx_ptr_load32(a, 12)`; :68 `__hx_ptr_store32(a, 12, ncap)`; :74 `__hx_ptr_store32(a, 8, nlen)`. Emitted x86_64.s:158-166 (`mov r10d, dword ptr [r10] # __hx_ptr_load32`) and :313-317 (`mov dword ptr [r10], esi # __hx_ptr_store32`); arm64.s:306 `ldr w1, [x1] ; __hx_ptr_load32`, :229 `str w5, [x1] ; __hx_ptr_store32`.",
        "mismatch": true,
        "evidence": "Same STRUCT-LAYOUT class as the new() pointee. On a C-minted (esc) descriptor: cap read from +12 = high half of int64 len = 0 → `len>=cap` always → realloc on every push; and `store32(a,12,ncap)` clobbers the high 32 bits of C's int64 len → C's subsequent `->len` becomes ncap<<32|len.",
        "target": "f64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED. (1) C's HexaArrF64 is emitted UNCONDITIONALLY at self/runtime_core_emit.hexa:1381-1385 as {double* data@0; int64_t len@8; int64_t cap@16} (24B) — I grepped: it is the only HexaArrF64 typedef, no guarded alternate layout exists. (2) stdlib/runtime/array_typed_leaf_f64.hexa models it as 16B with i32 len@8/cap@12: header comment says \"sizeof 16\", new() does malloc(16), and push_bits does __hx_ptr_load32(a,8)/__hx_ptr_load32(a,12), __hx_ptr_store32(a,12,ncap), __hx_ptr_store32(a,8,nlen). (3) The emitted seed asm really is 32-bit at those offsets — self/native/array_typed_leaf_f64_x86_64.s:163-175 \"mov r10d, dword ptr [r10] # __hx_ptr_load32\" and :316-317 \"mov dword ptr [r10], esi # __hx_ptr_store32\", inside the push_bits body (not comments, not another fn). (4) The cross-flow is emitted by the native backend itself: compiler/codegen/x86_64_linux.hexa:3202-3203 picks new_sym=\"hexa_arr_f64_new_esc\" and push_sym=\"hexa_arr_f64_push_bits\" for emk==103. hexa_arr_f64_new_esc is NEVER externed away by any guard (stays the C body, ~:2907-2917, \"a->len = 0; a->cap = cap;\" on the 24B struct) while hexa_arr_f64_push_bits IS replaced by the seed under HEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE (:3061-3066). So codegen mints a C 24B descriptor and feeds it to the seed's 16B reader. Consequence is worse than claimed: cap=load32(a,12)= high half of int64 len = 0 ALWAYS, so len>=cap fires on the FIRST push, ncap=cap*2=0, nbytes=0, realloc(old,0) -> glibc frees+returns NULL -> seed's OOM arm write(2)+exit(1); where realloc returns non-NULL, store64(data,off,bits) writes into a 0-byte block (heap corruption), and store32(a,12,ncap) corrupts the high half of C's int64 len exactly as claimed. All listed refutation avenues are closed: param 0's C proto IS a HexaVal (rdi:rsi pair ingress of the descriptor is fine — the defect is the POINTEE, orthogonal to the class-B register issue); no codegen branch special-cases the layout; the cited asm is the right function; grep shows no __hxw_/__hxseed_ bridge wrapping these symbols; and default-OFF guard status makes it latent, not false — it is exactly the flip-blocker. Minor cite nit (immaterial): new_esc is at ~:2907-2917, not :2944-2950 (that region is the f32 mirror); the quoted code text is verbatim correct.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_len",
        "kind": "return",
        "c_abi": "★ `int` — a C-ABI SCALAR. SysV: value in **eax** (rax), rdx is dead. AAPCS64: value in **w0**.",
        "seed_abi": "PAIR. x86_64.s:427-428 → `mov rdx, r14 # hv arg payload` / `mov rax, [rbp - 80] # tag L3 from tag-slot` — the LENGTH goes to rdx and rax gets the TAG (0 = TAG_INT, materialized at :425 `mov r11, 0`). arm64.s:307-309 → `movz x0, #0 ; __hx_ptr_load32: TAG_INT` / `stp x0, x1, [sp, #48]` / `ldp x0, x1, [sp, #48]` → x0 = tag = 0, x1 = len.",
        "mismatch": true,
        "evidence": "★ Any C caller reads eax/w0 = the TAG = 0 → hexa_arr_f64_len ALWAYS RETURNS 0, exactly reproducing the measured `hexa_arr_i64_len(v) -> 0` (true len 3) in the isolated darwin-arm64 repro. Register-mismatch on the RETURN, not the params — a carrier swap cannot touch it.",
        "target": "f64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. (1) The C prototype really is a scalar int: self/runtime_core_emit.hexa:1136 emits `int hexa_arr_f64_len(HexaVal v);` into runtime.h and BOTH native arms (:3063 under HEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE, :3070 under HEXA_RT_SELFEMIT||TYPED_LEAF_NATIVE) emit `extern int hexa_arr_f64_len(HexaVal v);` — so a C caller reads eax/w0 and the correct C body (:3094) is dropped. (2) The seed really returns a PAIR at exactly the cited lines: self/native/array_typed_leaf_f64_x86_64.s:427-428 `mov rdx, r14 # hv arg payload` / `mov rax, [rbp - 80] # tag L3 from tag-slot`, where [rbp-80] was just filled by `mov r11, 0 # materialize tag imm 0` → eax = TAG_INT = 0; self/native/array_typed_leaf_f64_arm64.s:307-309 `movz x0, #0 ; __hx_ptr_load32: TAG_INT` / `stp x0,x1,[sp,#48]` / `ldp x0,x1,[sp,#48]` → x0 = tag = 0, x1 = len. These lines are inside the real `_hexa_arr_f64_len` body (.globl at :284, body 287-310), not a comment and not a neighboring function. (3) No __hxw_ bridge covers it — the bridge block at runtime_core_emit.hexa:1817-1858 wraps ONLY the 10 arena symbols (arena_on/alloc/head/cur/env_lo/env_hi + __blk_next/cap/used/data); zero arr_* entries. (4) No pair-calling codegen special-case exists: `grep -rn \"arr_f64_len|arr_i64_len\" compiler/` returns ZERO hits (codegen only emits new/new_esc/push/push_bits/box, and x86_64_linux.hexa:3235 states the C-ABI scalar convention). (5) It is exported (.globl / .private_extern) and the seed path is default-ON — tool/stage_resolve_runtime_a:764 uses `[ \"${HEXA_RT_ARRAY_F64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0`, i.e. opt-OUT only, so the pair-returning seed replaces the correct C body in the shipped runtime.a. The hexa source (stdlib/runtime/array_typed_leaf_f64.hexa:79 `-> int`; the i64 twin's comment at array_typed_leaf.hexa:101 even asserts \"Returns a plain C int (rax)\") shows the intent the aprime emit falsifies, and the measured i64 repro (len -> 0 for true len 3) is the identical emit shape. Only nuance: no in-tree C call site currently invokes hexa_arr_f64_len (decl-only), so the defect is latent-but-real — any C caller of the declared prototype reads the TAG (0). A carrier swap cannot fix a return-register mismatch.",
          "confidence": "high"
        }
      },
      {
        "symbol": "hexa_arr_f64_box",
        "kind": "ingress-param",
        "c_abi": "★ param 1 `int64_t i` — C-ABI SCALAR → **rdx** (AAPCS64 **x2**). Real caller: the gen2 C emitter self/codegen.hexa:10715 → `\"hexa_arr_f64_box(\" + gen2_expr(node.left) + \", (int64_t)HX_INT(\" + gen2_expr(node.right) + \"))\"`.",
        "seed_abi": "PAIR. x86_64.s:454-455 → `mov [rbp - 176], rdx # store tag L1` / `mov r12, rcx # ingress param payload` → index VALUE read from **rcx**. arm64.s:322 `stp x2, x3, [sp, #16] ; ingress param 1` → index read from **x3**.",
        "mismatch": true,
        "evidence": "The index is read from an unwritten scratch register → the bounds checks at array_typed_leaf_f64.hexa:89-90 (`__hx_payload_lt(i,0)` / `__hx_payload_ge(i,len)`; emitted x86_64.s:483-492 → `call hexa_throw`) fire on garbage, or the offset `__hx_payload_mul(i,8)` (x86_64.s:534 `imul r10, r11`) addresses arbitrary memory. This is the mechanism behind the measured `hexa_arr_i64_box(v,1) -> index out of bounds`.",
        "target": "f64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — all five refutation routes fail on inspection.\n\n(1) C prototype is NOT HexaVal: self/runtime.h:283 and the SSOT self/runtime_core_emit.hexa:3101/3108 both declare `HexaVal hexa_arr_f64_box(HexaVal v, int64_t i);` — param 1 is a C-ABI scalar → rdx (x86_64 SysV) / x2 (AAPCS64), since `v` consumes rdi:rsi / x0:x1 as a 16B struct.\n\n(2) The cited asm is real code, not a comment, and the line numbers are exact. self/native/array_typed_leaf_f64_x86_64.s: body `.globl hexa_arr_f64_box` :438, label :441, and ingress :452-455 →\n    mov [rbp - 168], rdi   # store tag L0\n    mov rbx, rsi           # ingress param payload   (v.payload — correct)\n    mov [rbp - 176], rdx   # store tag L1            (the REAL index lands in the TAG slot)\n    mov r12, rcx           # ingress param payload   (index VALUE read from rcx — never written by a 2-arg C call)\nr12 is then the index at every consumer: `cmp r10, r11 # __hx_payload_lt` (:483 block → `call hexa_throw` :486), `__hx_payload_ge` (:497 block → `call hexa_throw`), and `imul r10, r11 # __hx_payload_mul` (:534). arm64 mirror: array_typed_leaf_f64_arm64.s:316 `_hexa_arr_f64_box:` / :322 `stp x2, x3, [sp, #16] ; ingress param 1`; the later `ldp x0, x1, [sp, #16]` yields x1 = old x3 = garbage, consumed by `cmp x1, x3` (__hx_payload_lt/ge) and `mul x1, x1, x3` (__hx_payload_mul).\n\n(3) No native-backend pair-ABI special case: `grep arr_f64_box compiler/` returns ZERO call sites — the native backend only ever emits `hexa_arr_i64_box` (x86_64_linux.hexa:3561/3698, packed-OOB slow path). The sole real caller is the gen2 C emitter, self/codegen.hexa:10715 → `\"hexa_arr_f64_box(\" + gen2_expr(node.left) + \", (int64_t)HX_INT(\" + gen2_expr(node.right) + \"))\"` — plain C-ABI.\n\n(4) The __hxw_ bridge does NOT wrap it: runtime_core_emit.hexa:1817-1854 aliases exactly the 10 arena symbols (arena_on/alloc/head/cur/env_lo/env_hi, blk_next/cap/used/data). `grep '__hxw_.*box|__hxseed_.*box'` over self/ → empty. Same gap that leaves array_typed_leaf broken while array_core is fine.\n\n(5) It IS exported and default-ON: `.globl hexa_arr_f64_box` (x86) / `.globl _hexa_arr_f64_box` + `.private_extern` (darwin) in all 3 targets. tool/stage_resolve_runtime_a:763-764 `resolve_native_array_f64_leaf_seed()` opts out only on an EXPLICIT `HEXA_RT_ARRAY_F64_LEAF_NATIVE=0`; it is invoked unconditionally at :4504, sets the guard at :812, and :1885-1888 ar's build/array_typed_leaf_f64_native.o into runtime.a while -D'ing the guard that swaps runtime_core.c's C body out for the `extern` C-ABI decl. The `# Default-OFF.` text at :642 is a stale doc comment contradicted by the code below it.\n\nRoot cause corroboration: stdlib/runtime/array_typed_leaf_f64.hexa:86 declares `pub fn hexa_arr_f64_box(v: HexaVal, i: int) -> HexaVal` — aprime lowers BOTH params to (tag,payload) register pairs, so param 1's value is read from rcx/x3 while the C caller placed it in rdx/x2. The index is therefore read from an unwritten scratch register, exactly as claimed, which is the mechanism behind the measured `hexa_arr_i64_box(v,1) -> index out of bounds` on its i64 twin (identical shape) and the SIGSEGV 139 on array_typed_leaf_f64_native.o.\n\nHonest non-refuting caveats: (a) the seed additionally carries class-A raw-libc egress (`call malloc`/realloc/write), which the claim itself acknowledges — so this file is class A PLUS the class-B mirror break; (b) on darwin-arm64 the __hx_payload_lt/ge lower to `bl _hexa_bool` (a backend helper, ABI-controlled by codegen) rather than an inline cmp, but the garbage x3 is compared before that call, so the defect is target-independent.\n\nKey paths: /Users/mini/dancinlab/hexa-lang/self/native/array_typed_leaf_f64_x86_64.s:441-534 · /Users/mini/dancinlab/hexa-lang/self/native/array_typed_leaf_f64_arm64.s:316-400 · /Users/mini/dancinlab/hexa-lang/self/runtime.h:283 · /Users/mini/dancinlab/hexa-lang/self/runtime_core_emit.hexa:3101 · /Users/mini/dancinlab/hexa-lang/self/codegen.hexa:10715 · /Users/mini/dancinlab/hexa-lang/stdlib/runtime/array_typed_leaf_f64.hexa:86 · /Users/mini/dancinlab/hexa-lang/tool/stage_resolve_runtime_a:763,812,1885,4504",
          "confidence": "high"
        }
      },
      {
        "symbol": "malloc",
        "kind": "outgoing-call",
        "c_abi": "`void* malloc(size_t)` — RAW LIBC. size in rdi (x0); result pointer in rax (x0) ALONE; rdx/x1 is dead.",
        "seed_abi": "PAIR, 2 sites. Declared `extern fn malloc(n: int) -> int` at stdlib/runtime/array_typed_leaf_f64.hexa:30. Site 1 (descriptor, :41): x86_64.s:50-54 → `mov rsi, 16 # hv arg payload` / `mov rdi, 0 # tag default = TAG_INT` / `call malloc` / `mov [rbp - 160], rax # store tag L4` / `mov r15, rdx # hv: unbox user-call result payload`. Site 2 (data buffer, :43): x86_64.s:71-75 (same shape). arm64: :41-44 `movz x0, #0` / `movz x1, #16` / `bl _malloc` / `stp x0, x1, [sp, #64]`, and :56.",
        "mismatch": true,
        "evidence": "CLASS A confirmed at both sites: libc sees size = rdi = 0 (the TAG) → malloc(0); the returned pointer lands in rax and the seed throws it away as a \"tag\", taking rdx (dead) as the pointer. Fix = the hexa_heap_alloc pair-ABI carrier (PR #4936).",
        "target": "f64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf_f64.hexa:30 declares `extern fn malloc(n: int) -> int` — raw libc, whose true C-ABI is `void* malloc(size_t)` (size in rdi/x0, result in rax/x0 ALONE). (2) The emitted seed calls it PAIR: self/native/array_typed_leaf_f64_x86_64.s:50-54 (inside .L1161_hexa_arr_f64_new_bb2, real instructions) `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default = TAG_INT` / `call malloc` / `mov [rbp-160],rax # store tag L4` / `mov r15,rdx # hv: unbox user-call result payload` — libc reads rdi=0 (the TAG) so it mallocs 0 bytes, returns the pointer in rax which the seed files as a *tag*, and takes dead rdx as the pointer. Site 2 (:71-75, from source :43) is identical with rdi loaded from the tag-slot (0). arm64 twin array_typed_leaf_f64_arm64.s:41-44 and :54-56: `movz x0,#0` / `movz x1,#16` / `bl _malloc` / `stp x0,x1` — same inversion. (3) Every refutation avenue fails: no HexaVal prototype for malloc anywhere; codegen's only C-ABI call branch is `_is_cabi` (compiler/codegen/x86_64_linux.hexa:2258) which is a HEXA_CABI_HXLCL-gated, default-OFF whitelist of hxlcl_* symbols only — malloc is NOT in it, so the call lowers via the PAIR-MODEL branch (exactly what the .s shows); no `__hxw_`/`__hxseed_` bridge wraps malloc (those 10 bridges at self/runtime_core_emit.hexa:1817-1854 are arena-only, and are ingress C-to-seed bridges that cannot fix an egress call into libc); and the object IS shipped — tool/stage_resolve_runtime_a:763-817 assembles the .s seed, exports HEXA_RT_ARRAY_F64_LEAF_NATIVE=1 and drops the C bodies, with the .s branch checking only the 4-dispatcher nm count and NO U-floor scan (the `_bad` U-check exists only in the own-obj branch at :783), matching the seed header's own admission of a `malloc realloc write` U-floor (array_typed_leaf_f64_x86_64.s:5-6). Same defect also at realloc (:241) and write (:350) in the same seed. hexa_heap_alloc pair-ABI carrier (PR #4936) is the correct fix shape for this CLASS-A egress; it does not address the orthogonal CLASS-B ingress/return mirror.",
          "confidence": "high"
        }
      },
      {
        "symbol": "realloc",
        "kind": "outgoing-call",
        "c_abi": "`void* realloc(void* p, size_t n)` — RAW LIBC. p→rdi, n→rsi (x0,x1); result in rax (x0) alone.",
        "seed_abi": "PAIR. Declared at array_typed_leaf_f64.hexa:31; called at :61. x86_64.s:235-243 → `mov rsi, [rbp - 120]` / `mov rsi, rsi # hv arg payload` (= old ptr) / `mov rdi, [rbp - 392] # tag L13` / `mov rcx, [rbp - 136]` / `mov rcx, rcx # hv arg payload` (= nbytes) / `mov rdx, [rbp - 408] # tag L15` / `call realloc` / `mov r10, rdx # hv: unbox user-call result payload`. arm64.s:171-174 → `ldp x0, x1, [sp, #208]` (tag,oldptr) / `ldp x2, x3, [sp, #240]` (tag,nbytes) / `bl _realloc`.",
        "mismatch": true,
        "evidence": "CLASS A: libc gets realloc(p = rdi = TAG(0), n = rsi = the OLD POINTER) → realloc(NULL, huge) or SIGSEGV; the new pointer (rax) is discarded and rdx garbage is stored back into data@0 (array_typed_leaf_f64.hexa:67). Fix = hexa_heap_realloc carrier.",
        "target": "f64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — I could not refute it; every escape hatch is closed by direct file evidence. (1) stdlib/runtime/array_typed_leaf_f64.hexa:31 declares `extern fn realloc(p: int, n: int) -> int` — raw libc, not hxlcl_, not bridged; called :61, result stored to data@0 at :67. (2) The cited asm is real code, not a comment, and is exactly as quoted: self/native/array_typed_leaf_f64_x86_64.s:235-243 loads the OLD POINTER payload into rsi and the TAG (0) into rdi, nbytes payload into rcx and its tag into rdx, `call realloc`, then `mov [rbp-416], rax # store tag L16` (the true new pointer is filed as the TAG) and `mov r10, rdx # hv: unbox user-call result payload` (the value becomes clobbered rdx garbage). arm64 (:171-174) is the same shape: `ldp x0,x1,[sp,#208]` = (tag=0, oldptr), `ldp x2,x3,[sp,#240]` = (tag=0, nbytes), `bl _realloc`, then `stp x0,x1` → x0 (real ptr) into the tag slot, clobbered x1 into the payload. So libc sees realloc(p=NULL, n=oldptr) and the caller keeps garbage. (3) No codegen pair-ABI special-case exists: the C-ABI gate at compiler/codegen/x86_64_linux.hexa:2230+ is a name whitelist whose 69 entries are all `hxlcl_*` (including hxlcl_realloc) plus __errno_location — bare `realloc` is NOT in it, so it takes the default pair-box lowering, exactly as the emitted .s shows. (4) No __hxw_ bridge covers it — the bridge at self/runtime_core_emit.hexa:1817-1858 covers only the 10 arena symbols; the `#define realloc(p,n) hxlcl_realloc(...)` at :404 is a C preprocessor macro that cannot rewrite a relocation in an already-assembled seed .o. (5) No own definition of bare `realloc` exists anywhere in self/ or compiler/. (6) It is shipped and reachable: `nm build/array_typed_leaf_f64_native.o` → `U _realloc` / `U _malloc` / `U _write`, and tool/stage_resolve_runtime_a:798-813 (.s-seed branch) assembles this seed and sets HEXA_RT_ARRAY_F64_LEAF_NATIVE=1 with NO U-floor `_bad` check (only the own-obj branch at :783 has one); the grow branch fires on the 2nd push since cap starts at 1. Bonus: the OOM guard `__hx_payload_eq(nd, 0)` (:62) tests the garbage payload, not rax/x0, so the NULL check is inert as well. This is a textbook CLASS A raw-libc egress mismatch; hexa_heap_realloc carrier is the correct fix.",
          "confidence": "high"
        }
      },
      {
        "symbol": "write",
        "kind": "outgoing-call",
        "c_abi": "`ssize_t write(int fd, const void* buf, size_t n)` — RAW LIBC. fd→rdi, buf→rsi, n→rdx (x0,x1,x2).",
        "seed_abi": "PAIR (3 pairs). Declared at array_typed_leaf_f64.hexa:32; called at :64 (`write(2, msg, 20)`). x86_64.s:343-350 → `mov rsi, 2 # hv arg payload` / `mov rdi, 0 # tag default = TAG_INT` / `mov rcx, [rbp - 184]` / `mov rcx, rcx # hv arg payload` (= msg ptr) / `mov rdx, [rbp - 456] # tag L21` / `mov r9, 20 # hv arg payload` / `mov r8, 0 # tag` / `call write`. arm64.s:251 `bl _write` with the same (x0,x1)(x2,x3)(x4,x5) pair fan-out.",
        "mismatch": true,
        "evidence": "CLASS A: libc sees write(fd = rdi = 0(tag), buf = rsi = 2, n = rdx = tag) → EFAULT / write to stdin. Only reachable on the OOM path, but it is a live raw-libc U symbol in the shipped runtime.a.",
        "target": "f64",
        "verdict": {
          "refuted": false,
          "reason": "CONFIRMED — every refutation avenue is closed by direct file evidence. (1) Declaration is a raw libc extern with C-ABI scalar params: stdlib/runtime/array_typed_leaf_f64.hexa:32 `extern fn write(fd: int, buf: int, n: int) -> int`, called at :64 `write(2, msg, 20)` on the realloc-OOM arm (identical twin at array_typed_leaf.hexa:55/:87). (2) The cited asm is real executable code inside `hexa_arr_f64_push_bits`, not a comment: self/native/array_typed_leaf_f64_x86_64.s block `.L1161_hexa_arr_f64_push_bits_bb3` emits a 3-pair fan-out (rdi=tag,rsi=payload)(rdx=tag,rcx=payload)(r8=tag,r9=payload) then `call write`; arm64 (array_typed_leaf_f64_arm64.s:251) mirrors with (x0,x1)(x2,x3)(x4,x5) then `bl _write`. (3) No codegen special-case makes this pair-legal: the only C-ABI branch is `_is_cabi` at compiler/codegen/x86_64_linux.hexa:2258, hard-gated on HEXA_CABI_HXLCL==\"1\" (default OFF) and an exact-name whitelist of `hxlcl_*` symbols — `hxlcl_write` is whitelisted, plain `write` is NOT. So pair-lowering is the default and only behavior. (4) The `__hxw_`/`__hxseed_` bridge (self/runtime_core_emit.hexa:1817-1854) wraps exactly 10 arena symbols, nothing for write; and it is the C-caller→seed ingress direction anyway, not seed→libc egress. (5) The symbol genuinely ships: nm -u build/array_typed_leaf_f64_native.o → `_malloc _realloc _write` (+hexa_bool/exit/throw), and nm over build/runtime.a shows NO definition of write anywhere in the archive — only the undefined `_write`, so it binds to libc's C-ABI write. ONE CORRECTION to the claim's stated consequence (does not change the verdict): the actual regs at the call are rdi=0 (tag), rsi=2 (payload), rdx=0 (L21's tag slot, materialized by `mov r11, 0`). libc therefore sees write(0, (void*)2, 0) — a no-op write to stdin with n=0 that returns 0, NOT an EFAULT. The message is silently never printed, and the return is also misread (libc returns ssize_t in rax; the seed does `mov [rbp-464], rax # store tag` / `mov r10, rdx # unbox result payload`, reading dead rdx as the value), though the result is unused. So the harm is milder than 'EFAULT', but the ABI mismatch class (CLASS A raw-libc egress, pair-ABI call into a C-ABI libc body) is exactly as claimed and is a live U symbol in the shipped runtime.a.",
          "confidence": "high"
        }
      }
    ],
    "bridge": "## 결론 요약 (선행 전제 1건 반증됨)\n\n**⚠️ 태스크 전제 \"array_core.hexa 는 이 브리지를 쓴다\" 는 FALSE.** grep 히트는 **주석**이고, 그 주석은 정확히 반대를 말한다 — \"브리지 불필요\". 그리고 typed_leaf 가 브리지를 안 쓰는 건 **역사적 누락이 아니라 구조적 불가**다. 아래 전부 file:line.\n\n---\n\n## 1. 브리지의 정확한 idiom (복붙 가능 · `self/runtime_core_emit.hexa:1780-1858`)\n\n전체가 `#ifdef HEXA_RT_ALLOC_NATIVE` 안에 있다 (`:1780`, 닫힘 `:1990` 부근 `#endif // HEXA_RT_ALLOC_NATIVE`). 메커니즘은 **4-스텝**이고, 네가 물은 \"asm label? #define? static inline?\" 은 **셋 다 동시에**가 정답이다.\n\n**Step 1 — extern + `__asm__()` 심볼 alias (진짜 HexaVal 시그니처로 재선언).** `__APPLE__` 로 밑줄 분기 (`:1816` / `:1827`):\n\n```c\n// darwin (Mach-O: asm-label 은 LITERAL — clang 이 밑줄 안 붙여줌 → 손으로 하나 붙임)\nextern HexaVal __hxseed_arena_on(void)        __asm__(\"_hexa_arena_on\");\nextern HexaVal __hxseed_arena_alloc(HexaVal n) __asm__(\"_hexa_arena_alloc\");\nextern HexaVal __hxseed_blk_next(HexaVal b)   __asm__(\"___blk_next\");   // __blk → ___blk\n// linux (ELF: 밑줄 없음)\nextern HexaVal __hxseed_arena_on(void)        __asm__(\"hexa_arena_on\");\nextern HexaVal __hxseed_arena_alloc(HexaVal n) __asm__(\"hexa_arena_alloc\");\nextern HexaVal __hxseed_blk_next(HexaVal b)   __asm__(\"__blk_next\");\n```\n\n**Step 2 — `__hxw_` static-inline 래퍼 (HX_INT 추출 / hexa_int 박싱)** (`:1839-1848`):\n\n```c\nstatic inline int   __hxw_arena_on(void)        { return (int)HX_INT(__hxseed_arena_on()); }\nstatic inline void* __hxw_arena_alloc(size_t n) { return (void*)(uintptr_t)HX_INT(__hxseed_arena_alloc(hexa_int((int64_t)n))); }\nstatic inline long  __hxw_blk_next(long b)      { return (long)HX_INT(__hxseed_blk_next(hexa_int((int64_t)b))); }\n```\n\n**Step 3 — function-like `#define` 로 call-site 철자 리다이렉트** (`:1849-1858`) — 36개 runtime.c 호출부를 **텍스트 무변경**으로 둔다:\n\n```c\n#define hexa_arena_on()     __hxw_arena_on()\n#define hexa_arena_alloc(n) __hxw_arena_alloc(n)\n#define __blk_next(b)       __hxw_blk_next(b)\n```\n\n**Step 4 — 왜 래퍼가 별도 심볼(`__hxw_`)이어야 하나 (`:1795-1798`, 하드 함정).** 시드가 이미 plain 이름(`hexa_arena_cur`)을 **소유**한다. 래퍼를 같은 이름으로 두면 링크에서 래퍼가 시드를 shadow → `__asm__`-alias 한 extern 이 **래퍼 자신으로 되돌아 해석** → **무한 자기재귀** (실측: `hexa_arena_cur` 스택오버플로 SIGSEGV). 그리고 `__asm__(\"...\")` **문자열 안의 토큰은 cpp 가 매크로 확장하지 않으므로** (`:1801-1802`) `#define hexa_arena_alloc` 이 있어도 alias 는 진짜 시드에 계속 바인딩된다. 이 두 성질이 트릭 전체를 성립시킨다.\n\n**arena 10 심볼 표 (시드 export 명 → C 호출 철자 → 변환):**\n\n| 시드 export (ELF / Mach-O) | C 가 부르는 철자 | 브리지 변환 |\n|---|---|---|\n| `hexa_arena_on` / `_hexa_arena_on` | `hexa_arena_on()` | `HexaVal(void)` → `int` |\n| `hexa_arena_alloc` / `_…` | `hexa_arena_alloc(n)` | `size_t` → `hexa_int()` 박싱, ret → `void*` |\n| `hexa_arena_head` / `_…` | `hexa_arena_head()` | → `long` |\n| `hexa_arena_cur` / `_…` | `hexa_arena_cur()` | → `long` |\n| `hexa_arena_env_lo` / `_…` | `hexa_arena_env_lo()` | → `long` |\n| `hexa_arena_env_hi` / `_…` | `hexa_arena_env_hi()` | → `long` |\n| `__blk_next` / `___blk_next` | `__blk_next(b)` | `long` → 박싱, ret → `long` |\n| `__blk_cap` / `___blk_cap` | `__blk_cap(b)` | 동일 |\n| `__blk_used` / `___blk_used` | `__blk_used(b)` | 동일 |\n| `__blk_data` / `___blk_data` | `__blk_data(b)` | 동일 |\n\n`hexa_arena_mark/rewind/reset` 은 브리지 **밖**이다 (`:1803-1805`) — 이미 C-ABI 와 일치 (`HexaArenaMark{block,used}` ≡ `HexaVal{tag,payload}`, reset 은 `void(void)`) → plain 선언.\n\n---\n\n## 2. array_core.hexa 의 소비 패턴 — **브리지를 안 쓴다** (쓸 필요가 없다)\n\n`stdlib/runtime/array_core.hexa:54-63` 주석이 명시:\n\n> \"…so the call uses the matching HexaVal 2-register ABI (arg {tag,payload} in rdi:rsi, raw-ptr return in rdx) — **no C bridge needed for this native→native edge** (the `__hxseed_arena_alloc` C bridge in runtime_core_emit.hexa exists **only for C-side callers**).\"\n\n시드 소스 쪽 선언/호출은 그냥 평범한 pair-ABI extern 이다 (`array_core.hexa:64`, `:76-79`):\n\n```hexa\nextern fn hexa_arena_alloc(n: int) -> int          // :64\npub fn rt_array_arena_alloc_items_native(n: int) -> int {\n    let bytes = n * 16                  // n * sizeof(HexaVal)\n    return hexa_arena_alloc(bytes)\n}\n```\n\n→ **aprime 가 caller·callee 양쪽을 컴파일** = 양쪽 다 pair-ABI = 자동 일치. 브리지는 오직 `runtime.c` 텍스트가 부를 때만 필요.\n\n---\n\n## 3. ★ typed_leaf 가 브리지를 못 쓰는 이유 — **구조적 불가 (caller 2종)**\n\narena 와 typed_leaf 의 결정적 차이는 **호출자 집합**이다.\n\n**arena 심볼의 호출자 = C 뿐.** 실측: 코드젠은 arena 심볼을 **단 한 번도 직접 emit 하지 않는다** —\n`grep 'op_label(\"hexa_arena_alloc\")\\|op_label(\"hexa_arena_on\")\\|_op_label(\"__blk' compiler/codegen/*.hexa` → **0 히트**. 그래서 cpp `#define` 이 호출자 전체를 100% 커버한다. 브리지가 **완결**된다.\n\n**typed_leaf 심볼의 호출자 = C + 코드젠(어셈블리).** 코드젠이 C-ABI 로 `call` 을 **직접 방출**한다:\n\n- `compiler/codegen/x86_64_linux.hexa:3235-3239` — `mov rdi, <cap>` / `call hexa_arr_i64_new` (주석: \"cap in rdi (single C-ABI int reg); returns HexaVal pair rax:rdx\")\n- `:3246-3252` — `hexa_arr_i64_push`: `v → rdi:rsi`, `x → rdx`\n- `:3561` — `call hexa_arr_i64_box` (packed OOB)\n- `compiler/codegen/arm64_darwin.hexa:2558-2578` — `hexa_arr_i64_new_esc(int cap) → cap in w0` / `bl hexa_arr_i64_push`\n\n**cpp `#define` 은 코드젠이 뱉은 어셈블리 `call` 에 도달할 수 없다.** 그래서 브리지는 typed_leaf 에서 **원리적으로 반쪽짜리**다. 역사적 누락이 아니다.\n\n**그리고 시드 ingress 는 실제로 pair 다 (4/4 확인, `self/native/array_typed_leaf_x86_64.s`):**\n\n| fn | 시드가 읽는 곳 | C/코드젠이 넣는 곳 | 결과 |\n|---|---|---|---|\n| `new(cap)` | `mov [rbp-128], rdi # store tag L0` / `mov rbx, rsi # ingress param payload` | `cap → rdi` | payload 를 **rsi(미초기화)** 에서 읽음 |\n| `push(v, x)` | `rdi:rsi = v` ✅ / `[rbp-296]←rdx (tag L1)` / `mov r12, rcx # ingress param payload` | `x → rdx` | x 를 **rcx** 에서 읽음 |\n| `box(v, i)` | `rdi:rsi = v` ✅ / `mov r12, rcx` | `i → rdx` | i 를 **rcx** 에서 읽음 |\n| `len(v)` | `rdi:rsi = v` ✅ | — | 리턴: `mov rdx, r14 # hv arg payload` / `mov rax, [rbp-80] # tag` → C 는 **eax=태그=0** 을 읽음 |\n\n즉 `v: HexaVal` 파라미터만 우연히 맞고 (**HexaVal 자체가 pair 니까**), **스칼라 파라미터·스칼라 리턴이 전부 어긋난다**. 이게 격리 재현의 `len→0` · `box→OOB` 를 정확히 설명한다. `array_new_leaf` / `arr_zeros_leaf` 가 carrier 치환만으로 고쳐진 이유도 동일 — 인자가 0-arg 이거나 전부 HexaVal 이라 **미스매치할 스칼라가 애초에 없다**.\n\n시드가 raw libc 를 끄는 것도 확인: `stdlib/runtime/array_typed_leaf.hexa:53-56` = `extern fn malloc/realloc/write/exit` (클래스 A 잔존). 하지만 클래스 B 는 그것과 **독립**이다.\n\nC 바디는 `self/runtime_core_emit.hexa:2763` 의 `#if defined(HEXA_RT_SELFEMIT) || defined(HEXA_RT_CORE_ARRAY_TYPED_LEAF_NATIVE) || defined(HEXA_RT_CORE_ARRAY_I64_LEAF_NATIVE)` 로 `extern` 선언(`:2765-2768`, **C-ABI 스칼라**)으로 갈아치워지고, `tool/stage_resolve_runtime_a:576` 이 `HEXA_RT_ARRAY_I64_LEAF_NATIVE=1` 을 **default-ON** 으로 export 한다 → 깨진 시드가 기본 경로다.\n\n---\n\n## 4. 따라서 진짜 fix 는 브리지가 아니라 **양쪽 정렬**\n\n두 갈래 중 하나여야 하고, 브리지 단독은 **답이 아니다**:\n\n- **(A) 호출자를 pair-ABI 로 정렬**: C 쪽은 `__hxseed_`/`__hxw_` idiom 그대로 복제(가능) + **코드젠 emit 을 pair-ABI 로 수정**(`x86_64_linux.hexa:3235/3246/3561`, `arm64_darwin.hexa:2558/2568/2578`) — `cap` 을 `rdi=TAG_INT, rsi=cap` 으로, `push` 의 `x` 를 `rdx=TAG_INT, rcx=x` 로, `len` 리턴을 `rdx` 에서 수취.\n- **(B) 시드를 C-ABI 로 방출**: `hexa_int`/`hexa_float` 처럼 **손으로 emit** 하거나(`runtime_core_emit.hexa:1769-1771` 의 hand-emitted ctors 가 정확히 C-ABI 로 작성돼 있음 — `fmov x1,d0 / mov w0,#1 / ret`), aprime 에 C-ABI export 모드를 준다. 이게 **RFC061 Route C (codegen C-ABI)** 와 같은 축이다.\n\n브리지 정답지는 **(A)의 C-절반만** 제공한다. 코드젠 절반은 repo 어디에도 선례가 없다 — **모른다: aprime 가 C-ABI 심볼을 export 하는 기존 모드가 있는지**. 다음에 볼 곳 = `compiler/` 의 `--keep-global` / `--isolate` / Route C `hxlcl_*` cross-target emit 경로 (`tool/stage_resolve_runtime_a:590-608` 의 own-obj 분기가 같은 시드를 `--emit=obj` 로 뽑으므로, 거기 ABI 가 pair 인지 C 인지가 갈림길).",
    "synth": "# 1. 미스매치 최종본 — typed-leaf i64/f64\n\n세 개의 **독립** 결함 클래스가 겹쳐 있다. 클래스 B만 고쳐도, 클래스 A만 고쳐도(=carrier 치환) SIGSEGV는 안 사라진다.\n\n## 반증된 전제 (먼저)\n\n| 전제 | 판정 | 증거 |\n|---|---|---|\n| \"array_core.hexa 는 `__hxseed_`/`__hxw_` 브리지를 쓴다\" | **FALSE** | `stdlib/runtime/array_core.hexa:54-63` 주석이 정반대를 말한다 — \"no C bridge needed for this native→native edge (the `__hxseed_arena_alloc` C bridge … exists **only for C-side callers**)\". `:64` 는 그냥 `extern fn hexa_arena_alloc(n: int) -> int` = pair↔pair. grep 히트는 **주석**이었다. |\n| \"typed_leaf 가 브리지를 안 쓰는 건 역사적 누락\" | **FALSE (구조적 불가)** | 브리지의 `#define` 은 `self/runtime_core.c` 텍스트만 덮는다. typed-leaf 심볼의 호출자는 **3종**: ① runtime_core.c ② gen2가 뱉는 user.c (`self/codegen.hexa:4753/4756/4782/10712/10715`) ③ **네이티브 백엔드가 뱉는 어셈블리 `call`** (`compiler/codegen/x86_64_linux.hexa:3239/3252/3561/3698/5130`, `arm64_darwin.hexa:2578`). cpp는 ②③에 도달 못 한다. arena 심볼은 코드젠이 **한 번도 emit 하지 않아서**(`grep 'op_label(\"hexa_arena_' compiler/codegen/*.hexa` → 0) 브리지가 완결된 것이다. |\n| \"runtime_core.c 안에 C 호출자가 수십 개\" | **FALSE** | 시드 심볼의 실제 C 호출자는 **2개**: `runtime_core_emit.hexa:3007`(`hexa_arr_poly_push` → `hexa_arr_i64_push`) · `:3066`(`hexa_arr_f64_push` double 시shim → `hexa_arr_f64_push_bits`). |\n| \"`len→0` 이 프로덕션 SIGSEGV의 근인\" | **FALSE (데드 심볼)** | `git grep hexa_arr_i64_len` 실측 → 선언·정의·regen SYMS 목록·시드뿐, **실호출자 0**. `self/runtime.h:278-283` 에는 `len` 프로토타입이 **아예 없다**(new/push/box만). len 수정은 위생이지 fix가 아니다. |\n\n## ★ 신규 확정 — **클래스 C (디스크립터 레이아웃)**. 이게 SIGSEGV의 진짜 근인이고 ABI 수정으로는 원리적으로 못 고친다.\n\nC 진실 (`self/runtime_core_emit.hexa:1366-1370`, F64 쌍둥이 `:1381-1385`, F32 `:1390` 주석이 \"the SAME 24-byte (ptr@0, len@8, cap@16) shape\" 로 layout-match invariant 를 못 박음):\n```c\ntypedef struct HexaArrI64 { int64_t* data; int64_t len; int64_t cap; } HexaArrI64;  // sizeof=24\n```\n64-bit 로 넓힌 커밋 = **`2acf4a7f7` (#3352)**. 시드는 그보다 **한참 뒤**인 `14c00fcb8` (#4917)인데 **16B/i32 옛 레이아웃**으로 작성됐다:\n\n- `stdlib/runtime/array_typed_leaf.hexa:36-39` 주석 = **stale** (\"{ data @0; int len @8(i32); int cap @12(i32) } sizeof 16\")\n- `:65` `malloc(16)` · `:69` `store32(a,8,0)` · `:70` `store32(a,12,c)` · `:78-79` `load32(a,8)/load32(a,12)` · `:91` `store32(a,12,ncap)` · `:97` `store32(a,8,nlen)` · `:104`/`:112` `load32(a,8)`\n- f64 쌍둥이 동일: `array_typed_leaf_f64.hexa:22` (stale 주석) · `:41` `malloc(16)` · `:45-46` · `:55-56` · `:68` · `:74`\n\n**정답지가 같은 리포에 있다** — `stdlib/runtime/arr_zeros_leaf.hexa:61-63`: `__hx_ptr_store64(a,0,items)` / `store64(a,8,n)` / `store64(a,16,n)` (+ `calloc(1,32)`). zeros/array_new 이 carrier 치환만으로 산 이유는 \"스칼라 미러가 없어서\"만이 아니라 **레이아웃도 이미 맞았기 때문**이다.\n\n귀결 (전부 실측 코드 인용):\n\n| 방향 | 결과 |\n|---|---|\n| 시드-mint → 코드젠 read | `_x86_emit_arrpk_get` 이 `mov r11, [r10+8]` = **64-bit** len 로드 (`x86_64_linux.hexa:1999`, 레이아웃 주석 `:1985` \"data*@0, len@8, cap@16\") → len = `(cap<<32)\\|len` = 거대값 → bounds check 통과 → **OOB 로드 SIGSEGV** |\n| C-mint(`hexa_arr_i64_new_esc` `:2893`, 정상 24B) → 시드 push | 시드가 cap 을 +12(=len의 상위 32비트)에서 읽음 → cap=0 → `ncap=0` → `realloc(old,0)`=free → 직후 `data[len]` 저장 → **UAF** |\n| 시드-mint → C `hexa_arr_i64_push`(`:2835`) | `a->cap *= 2` 가 16B 블록의 **+16 에 8바이트 쓰기** → 힙 메타 파손 |\n\n## 클래스 B (ABI) — 확정 미스매치 표 (전부 `.s` + 프로토타입 대조)\n\n시드는 aprime lowering 상 **모든 파라미터를 (tag,payload) GP 쌍**으로 읽고 **결과 payload 를 rdx/x1** 에 넣는다. `HexaVal` 은 16B `{tag@0,payload@8}` 라서 `HexaVal f(HexaVal…)` **일 때만** SysV/AAPCS64 by-value 규약과 우연히 일치한다. 스칼라가 하나라도 끼면 깨진다.\n\n| 심볼 | 경계 | C-ABI (프로토타입/코드젠) | 시드 (.s) | 결과 |\n|---|---|---|---|---|\n| `hexa_arr_i64_new` | ingress `cap` | `HexaVal(int cap)` `runtime_core_emit.hexa:1126`·`:2765` / 코드젠 `x86_64_linux.hexa:3235-3239` `mov rdi, cap` (주석: \"cap in rdi (single C-ABI int reg)\") | `array_typed_leaf_x86_64.s:28-29` `mov [rbp-128], rdi # store tag L0` / `mov rbx, rsi # ingress param payload` | cap 을 **rsi(미초기화)** 에서 읽음 |\n| `hexa_arr_i64_push` | ingress `x` | `HexaVal(HexaVal, int64_t)` `:1127`·`:2766` / `x86_64_linux.hexa:3246-3252` `_x86_hv_payload(…,\"rdx\")` · arm64 `arm64_darwin.hexa:2576` `mov x2, x10` | `.s:149-150` `[rbp-296]←rdx (tag L1)` / `mov r12, rcx` · arm64 `stp x2,x3,[sp,#16]` | x 를 **rcx / x3** 에서 읽음 |\n| `hexa_arr_i64_box` | ingress `i` | `HexaVal(HexaVal,int64_t)` `:1129` / `x86_64_linux.hexa:3557` `…\"rdx\" // idx (raw int64)` + `:3561 call` (+`:3698` index_set) | `.s:454-455` `mov r12, rcx` | i 를 **rcx** 에서 읽음 |\n| `hexa_arr_i64_len` | return | `int(HexaVal)` `:1128` → eax/w0 | `.s:427-428` `mov rdx, r14 # payload` / `mov rax, [rbp-80] # tag` | C는 eax=**tag=0** 을 읽음 (단, 실호출자 0 = 데드) |\n| `hexa_arr_f64_new/len/box` | 동일 | `:1134-1136`·`:3062-3064` | `array_typed_leaf_f64_*.s` 동형 | 동일 |\n| `hexa_arr_f64_push_bits` | ingress `bits` | `HexaVal(HexaVal,int64_t)` `:2925`·`:3065` / `x86_64_linux.hexa:3216-3222` (\"Both v → rdi:rsi, payload word → rdx\") | `f64_x86_64.s:149-150` `mov r12, rcx` | bits 를 **rcx** 에서 |\n| **`hexa_int`** (egress) | outgoing | `HexaVal hexa_int(int64_t n)` `self/runtime.h:112` = **C-ABI 스칼라** | `array_typed_leaf.hexa:57` `extern fn hexa_int` → pair 로 낮춰짐 (`.s:557-562` rdi=tag(0), rsi=payload) | `hexa_int` 은 rdi만 읽음 → **항상 `hexa_int(0)`** — 그런데 sanctioned allow-list(`stage_resolve_runtime_a:601`, `regen_…_native_s.sh:43`)에 **들어 있어 이름 게이트로는 영원히 안 잡힌다** |\n\n## 클래스 A (raw libc egress) — 잔존\n\n`array_typed_leaf.hexa:53-56` `extern fn malloc/realloc/write/exit`, `_f64.hexa:30-32` 동일. 시드는 pair 로 6개 인자 레지스터까지 채우고 C-ABI libc 바디를 부른다. 반환 포인터는 rax 단독인데 시드는 rdx 를 읽는다 (`.s:52-54` `call malloc` / `mov r15, rdx`).\n\n## ⚠️ 이건 잠복이 아니라 **shipped default**\n\n- `stage_resolve_runtime_a:574` = `[ \"${HEXA_RT_ARRAY_I64_LEAF_NATIVE:-x}\" = \"0\" ] && return 0` → **opt-out 만 존재**. 시드 `.s` 가 in-tree(`self/native/array_typed_leaf_{arm64,arm64-linux,x86_64}.s` 9개 파일 전부 tracked)면 조립하고 `:628` 이 flag=1 export.\n- 코드젠 packed 경로도 `_pack_array_enabled()` = `env(\"HEXA_PACK_ARRAY\") != \"0\"` → **DEFAULT-ON** (`x86_64_linux.hexa:1759-1763`).\n→ **깨진 시드 × 깨진 레이아웃이 현재 기본 경로다.** release-integrity 순증 작업이다.\n\n---\n\n# 2. 결정 — 3안 채점\n\n| 안 | ① 완성도 | ② 단순 | ③ 안전 | ④ 표준(reference-match·폴라리티) |\n|---|---|---|---|---|\n| **bridge-wrap** (`#define`+`static inline`) | ✗ **반쪽** — user.c(`codegen.hexa:4753…`)·코드젠 asm(`x86_64_linux.hexa:3239…`) 미도달. 브리지를 `runtime.h` 로 옮기면 leaf `-D` 를 user.c 컴파일까지 스레딩해야 하고 불일치=**조용한 ABI 파손** | ○ | ✗ 새 -D 커플링 = 새 결함면 | ○ idiom 재사용이지만 **적용 불가능한 곳에 재사용** |\n| **proto-to-HexaVal** (C 프로토타입을 전부 HexaVal로) | ○ 전 호출자 커버 | ✗ 편집 6파일 · 코드젠 8사이트 · `restore_frozen_seeds:1263-1315`(4번째 C 사본 + `:1318` grep 가드 문자열) · `rtcore_array-typed-leaf_emit.hexa`(5번째 사본) 락스텝 | ✗✗ **flag-day**: 심볼명 그대로 → 새 컴파일러 × 낡은 `~/.hx/lib/runtime.a` = 링크 성공 + **조용한 SIGSEGV** (#4930/#4932 재현 등급) | ✗ **폴라리티 역행** — CLAUDE.md \"cross-target native-emit = **codegen C-ABI** (Route C)\" 와 정확히 반대 축. 대외 C 표면 전체에 `hexa_int()` 박싱 강제 |\n| **★ thunk (rename + C-ABI 외부링키지 래퍼)** | **○ 3종 호출자 전부** — 정식 이름을 C가 소유하므로 user.c·gen2·코드젠 asm·runtime_core.c 가 전부 C-ABI thunk 에 착지 | **○ 코드젠 편집 0 · -D 스레딩 0 · frozen-seed 편집 0** | **○ rename → 낡은 runtime.a = undefined symbol 로 시끄럽게 죽음** (silent SIGSEGV 불가) | **○** `__hxseed_`/`__hxw_` idiom 을 그대로 승격(static-inline→외부링키지). C-ABI 폴라리티 유지 = Route C 축 보존 |\n\n## 추천 = **thunk 안** (① 완성도 + ④ 표준 기준)\n\nreference-match 규율상 \"이미 검증된 idiom 재사용\"은 **브리지의 *메커니즘*(Step1 extern-alias + Step2 HexaVal↔스칼라 변환 래퍼)을 재사용**한다는 뜻이지, **Step3 `#define`** 까지 재사용한다는 뜻이 아니다. `#define` 은 arena 가 \"호출자=runtime_core.c 뿐\"이라는 **특수 조건**에서만 완결됐다 (`runtime_core_emit.hexa:1798-1799` \"the 36 runtime.c call sites\"). typed-leaf 는 그 조건이 깨진다. thunk 는 같은 idiom을 조건이 다른 곳에 **올바르게** 적용한 것이다.\n\n추가 보너스: rename 하면 시드가 정식 이름을 더는 소유하지 않으므로 `runtime_core_emit.hexa:1795-1798` 의 **무한 자기재귀 함정**(래퍼가 시드를 shadow → `__asm__` alias 가 래퍼로 되돌아 해석 → SIGSEGV 스택오버플로)이 **구조적으로 소멸**하고, `__asm__(\"…\")` 밑줄 분기(`:1806-1809` — \"plainly-declared symbols … clang adds the Mach-O underscore automatically\")도 **불필요**해진다. 즉 브리지보다 **더 단순하다**.\n\n**단, 어느 안을 택하든 클래스 C(레이아웃)와 클래스 A(carrier)는 필수다. thunk 단독은 SIGSEGV 를 못 고친다.**\n\n---\n\n# 3. 구현-ready 스펙 (thunk 안)\n\n## E1 — 시드 소스 `stdlib/runtime/array_typed_leaf.hexa` (rename + 클래스 C + 클래스 A)\n\n| line | 현재 | 편집 |\n|---|---|---|\n| `:36-39` | stale LAYOUT 주석 (16B / i32) | `{ int64_t* data @0; int64_t len @8; int64_t cap @16 } sizeof 24` + \"#3352(2acf4a7f7)에서 i64로 넓어졌다. **load32/store32 금지**\" 로 정정 |\n| `:53-56` | `extern fn malloc/realloc/write/exit` | `extern fn hexa_heap_alloc(n: int) -> int` / `extern fn hexa_heap_realloc(p: int, n: int) -> int` (#4936 carrier). `write`+`exit` OOM 경로는 **`hexa_throw(\"OOM in arr_i64_push\")` 로 교체**(carrier 0 추가·U-floor 순감). ⚠️ descriptor 는 **heap** — `hexa_ptr_alloc` 은 이름과 달리 ARENA(`alloc_syscall_native.o` 가 링크에서 이겨 `hexa_arena_alloc` 위임)이고 descriptor 는 프레임을 탈출한다 |\n| `:57` | `extern fn hexa_int(x: int) -> HexaVal` | **삭제**. `:118` `return hexa_int(x)` → `return __hx_make_val(0, x)` (TAG_INT=0). f64 쌍둥이가 이미 이 idiom (`stage_resolve_runtime_a:766-767`: \"NO hexa_int — box re-boxes via the `__hx_bits_f64` leaf, not a carrier\") |\n| `:62 / :76 / :102 / :110` | `pub fn hexa_arr_i64_{new,push,len,box}` | → `hexa_arr_i64_{new,push,len,box}_seed` |\n| `:65` | `malloc(16)` | `hexa_heap_alloc(24)` |\n| `:69` | `__hx_ptr_store32(a, 8, 0)` | `__hx_ptr_store64(a, 8, 0)` |\n| `:70` | `__hx_ptr_store32(a, 12, c)` | `__hx_ptr_store64(a, 16, c)` |\n| `:78 / :79` | `load32(a,8)` / `load32(a,12)` | `load64(a,8)` / `load64(a,**16**)` |\n| `:84` | `realloc(old, nbytes)` | `hexa_heap_realloc(old, nbytes)` |\n| `:91` | `store32(a,12,ncap)` | `store64(a,**16**,ncap)` |\n| `:97` | `store32(a,8,nlen)` | `store64(a,8,nlen)` |\n| `:104 / :112` | `load32(a,8)` | `load64(a,8)` |\n\n**`stdlib/runtime/array_typed_leaf_f64.hexa` 동형**: `:22`(주석) · `:30-32`(carrier) · `:41`(`malloc(16)`→`hexa_heap_alloc(24)`) · `:45-46`(store32→store64 @8/@16) · `:55-56`(load32→load64 @8/@16) · `:61`(realloc→carrier) · `:64`(write/exit→hexa_throw) · `:68`(store32(a,12)→store64(a,16)) · `:74`(store32(a,8)→store64) · `:79`(len) · box — 그리고 `pub fn` 4개 → `hexa_arr_f64_{new,push_bits,len,box}_seed`.\n\n## E2 — `self/runtime_core_emit.hexa:2765-2768` (i64 leaf-ON arm) 을 통째 교체\n\n```c\n// SEED-ABI (class B thunk · reference-match = the Route-B __hxseed_/__hxw_ bridge at\n// runtime_core_emit.hexa:1780-1858, promoted from static-inline to EXTERNAL linkage).\n// aprime lowers EVERY seed param/return to a {tag,payload} GP pair (rdi:rsi / rax:rdx ·\n// x0:x1). HexaVal is {tag@0,payload@8}, so a HexaVal(HexaVal…) signature IS that pair\n// ABI — but `int cap` / `int64_t x` / an `int` return is NOT (cap lands in rdi = the\n// seed's TAG slot; len comes back in rdx while C reads rax). A cpp #define bridge CANNOT\n// fix this: the seed's callers are (1) this file, (2) gen2-emitted user.c\n// (self/codegen.hexa:4753/4756/4782/10712/10715), (3) the native backend's raw asm `call`\n// (compiler/codegen/x86_64_linux.hexa:3239/3252/3561/3698/5130 · arm64_darwin.hexa:2578)\n// — cpp reaches neither (2) nor (3). So the SEED exports *_seed with an all-HexaVal\n// signature and C owns the canonical C-ABI names as real thunks, which all three caller\n// classes land on. The rename also kills the bridge's shadow/self-recursion trap\n// (:1795-1798) and needs no __asm__ asm-label (plain decls get the Mach-O underscore\n// automatically, :1806-1809). Renaming is ALSO the flag-day guard: an OLD runtime.a\n// fails LOUDLY with an undefined symbol instead of silently mis-passing registers\n// (the #4930 → #4932 3-target revert mode).\nextern HexaVal hexa_arr_i64_new_seed (HexaVal cap);\nextern HexaVal hexa_arr_i64_push_seed(HexaVal v, HexaVal x);\nextern HexaVal hexa_arr_i64_len_seed (HexaVal v);\nextern HexaVal hexa_arr_i64_box_seed (HexaVal v, HexaVal i);\nHexaVal hexa_arr_i64_new (int cap)              { return hexa_arr_i64_new_seed(hexa_int((int64_t)cap)); }\nHexaVal hexa_arr_i64_push(HexaVal v, int64_t x) { return hexa_arr_i64_push_seed(v, hexa_int(x)); }\nint     hexa_arr_i64_len (HexaVal v)            { return (int)HX_INT(hexa_arr_i64_len_seed(v)); }\nHexaVal hexa_arr_i64_box (HexaVal v, int64_t i) { return hexa_arr_i64_box_seed(v, hexa_int(i)); }\n```\n\n## E3 — `self/runtime_core_emit.hexa:3062-3066` (f64 leaf-ON arm) 교체\n\n```c\nextern HexaVal hexa_arr_f64_new_seed      (HexaVal cap);\nextern HexaVal hexa_arr_f64_push_bits_seed(HexaVal v, HexaVal bits);\nextern HexaVal hexa_arr_f64_len_seed      (HexaVal v);\nextern HexaVal hexa_arr_f64_box_seed      (HexaVal v, HexaVal i);\nHexaVal hexa_arr_f64_new (int cap)                      { return hexa_arr_f64_new_seed(hexa_int((int64_t)cap)); }\nHexaVal hexa_arr_f64_push_bits(HexaVal v, int64_t bits) { return hexa_arr_f64_push_bits_seed(v, hexa_int(bits)); }\nint     hexa_arr_f64_len (HexaVal v)                    { return (int)HX_INT(hexa_arr_f64_len_seed(v)); }\nHexaVal hexa_arr_f64_box (HexaVal v, int64_t i)         { return hexa_arr_f64_box_seed(v, hexa_int(i)); }\nHexaVal hexa_arr_f64_push(HexaVal v, double x) { union { double d; long long i; } u; u.d = x; return hexa_arr_f64_push_bits(v, u.i); }  // 기존 :3066 유지 (double→bits SSE-ABI 벽)\n```\n\n## E4 — **건드리면 안 되는 곳** (thunk 안의 핵심 이점)\n\n- `self/runtime_core_emit.hexa:1126-1129 / 1134-1137` (runtime.h 서피스) — **무변경**. C-ABI 가 정식으로 유지된다.\n- `self/runtime.h:278-283` — **무변경**.\n- `compiler/codegen/x86_64_linux.hexa` · `arm64_darwin.hexa` — **무변경** (8 emit 사이트 그대로).\n- `self/codegen.hexa` (gen2 emit 문자열 5곳) — **무변경**.\n- `tool/restore_frozen_seeds:1263-1321` (4번째 C 사본 + `:1318` grep 가드) — **무변경** (그 바디들이 정식 C-ABI 이름을 소유하는 게 그대로 옳다). ⚠️ 단 §6 미확인 1건 참조.\n\n## E5 — 이름 목록 락스텝 (rename 부수 편집, 전부 문자열 치환)\n\n- `tool/stage_resolve_runtime_a:591` `_kg=\"hexa_arr_i64_new,…\"` → `…_seed` ×4\n- `:601` own-obj `_bad` allow-list → carrier-only prefix 로 교체 (§4)\n- `:621` `.globl` 카운트 정규식 `_?hexa_arr_i64_(new|push|len|box)` → `…_seed`\n- `:775` f64 `_kg` · 그 분기의 `.globl` 정규식 → `…_seed`\n- `tool/regen_array_typed_leaf_native_s.sh:38` `SYMS=` · `tool/regen_array_typed_leaf_f64_native_s.sh:38` → `…_seed`\n- 두 regen 의 `:43` `ALLOWED_U=\"malloc realloc write hexa_exit hexa_int hexa_throw\"` → **폐기**, carrier-only prefix 검사로 교체 (§4-E4)\n- `.s` 시드 9개 재생성 (regen 스크립트가 산출) — 손으로 `.globl` 만 sed 하지 말 것 (레이아웃/carrier 가 바디를 바꾸므로 진짜 재생성 필요)\n\n## 대가 (정직하게)\n\n- 호출당 thunk 1단 + `hexa_int()` 박싱 — `push`/`box` 는 배열 리터럴 **원소당 1콜** = 최핫패스. typed-leaf 의 존재 이유가 \"packed=빠른 경로\"이므로 **flip 전 실측 필수** (지금은 SIGSEGV라 측정 대상 자체가 없다).\n- zero-c 회계: C 4줄 × 2 = sanctioned wrapper 8줄 잔존 (arena 브리지 10 심볼과 동급).\n\n---\n\n# 4. 게이트\n\n## (a) `.s-seed` U-floor 공백 — 게이트 코드는 이미 있고 **hoist 가 안 됐다**\n\n`_rn_seed_und_gate` (`tool/stage_resolve_runtime_a:1812-1815`) 가 정확히 그 검사다:\n```sh\nnm -u \"$1\" | awk '{print $NF}' | grep -vE '^_?(hexa_|hxlcl_|__hx_)' | tr '\\n' ' '\n```\n문제는 **스코프**: `build_runtime_a_from_source()` (`:1802`) **안에 중첩 정의**돼 있는데, 18개 resolver 는 `:4493-4562` 에서 실행되고 `build_runtime_a_from_source` 호출은 `:4583` 이다 → resolver 들은 이 함수를 **호출할 수조차 없다**. hxlcl_* 분기들만 같은 함수 안이라 보호된다.\n\n- **GATE-A/E1** — `_rn_seed_und_gate` 정의를 `:1812` 에서 **top-level(:282 뒤, 첫 resolver 정의 직전)** 로 hoist. 안쪽 참조는 그대로 보인다.\n- **GATE-A/E2** — `:4562`(`resolve_cuda_runtime_seed` 직후) 에 **중앙 스윕 1곳**: `for o in build/*_native.o` → `_rn_seed_und_gate` → 비어있지 않으면 `rm -f \"$o\"`(= coherent revert: `:1846~:2071` 의 모든 `-DHEXA_RT_*_NATIVE` 가 `[ -f build/<x>.o ]` 가드라 .o 소멸 = -D 소멸 = C 바디 복귀) + 경고. 중앙 스윕이어야 하는 이유: 18개 분기 전부 `[ -f build/X.o ] && export …; return 0` 형태의 **cached-.o early-return**(`:285 :347 :420 :501 :576 :649 :709 :765 :833 :909 :964 :1091 :1163 :1239 :1315 :1401 :1472 :1564`)이 있어서 per-branch 게이트만으로는 캐시 경로가 새 나간다.\n- ⚠️ **`build/alloc_syscall_native.o` 는 rm 금지** — `resolve_native_alloc_syscall_seed` (`:1562`) 는 `HEXA_RT_ALLOC_NATIVE:-1` = **default-ON** 이고 C arena `#else` 가 emitter 에서 **DROP** 됐다 (C fallback 없음). 여기서 트립하면 `rm` 이 아니라 **FATAL(exit 1)**. (현재는 clean.)\n- `nm` 부재 호스트: `return 0`(=clean) 로 조용히 무력화된다 → 최소 WARN. 진짜 방어는 (b).\n- **GATE-A/E4** — regen 스크립트 3종의 `ALLOWED_U` 폐기. 이걸 안 하면 다음 regen 이 같은 깨진 시드를 다시 굽고 스테이지 게이트가 매번 떨어뜨려 zero-c 축이 조용히 되감긴다.\n\n**실측 근거 (22 시드 × 2 타깃, in-tree `.s` 를 clang/clang -target x86_64-linux-gnu 로 조립 → `nm -u`)**: carrier-only prefix 규칙 위반 = **정확히 3개** — `arr_zeros_leaf`{malloc,calloc,write} · `array_typed_leaf`{malloc,realloc,write} · `array_typed_leaf_f64`{malloc,realloc,write}. 나머지 19개(array_core·alloc_syscall·map_core·map_query·intern·fs·str·num·num_float·float_exact·hexinfnan·regex·valop·runtime_hi·hxlcl ×8)는 전부 `hexa_*`/`hxlcl_*` 만. **false-positive 0, 2타깃 동일** → 게이트를 켤 근거는 끝났다.\n\n## (b) 클래스 B 게이트 — **가능하다. asm 파싱도, 툴체인도 필요 없다.**\n\naprime lowering 은 **무조건적**이다 (시드 asm 마커가 파라미터당 정확히 1쌍: x86 `# store tag L{i}` + `# ingress param payload`, arm64 `stp x{2i}, x{2i+1} ; ingress param {i}`). 즉 **asm 은 언제나 pair 다 — 스캔해봐야 새 정보가 없다.** 미스매치를 만드는 유일한 자유도는 **C 쪽 프로토타입**이다.\n\n> **GATE-B 불변식**: aprime-emit 시드가 export 하는 심볼의 C 프로토타입은 **`HexaVal f(HexaVal, …)` / `HexaVal f(void)`** — 반환·모든 파라미터가 HexaVal — 이어야 한다.\n> **GATE-B 참조측**: 시드 소스의 모든 `extern fn X` 의 X 는 **PAIR**(다른 aprime 시드) ∪ **CABI-HEXAVAL**(all-HexaVal C 프로토타입) 이어야 한다.\n\n전부 **git-tracked 텍스트**로 판정된다: `self/native/*.s` 의 `.globl` 집합 × `stdlib/runtime/*.hexa` 의 `extern fn` × `self/runtime_core_emit.hexa` 의 프로토타입 문자열. 그리고 이미 **사실상의 관례**다 — `rt_intern_find_native`(`:4143`, cap/h 같은 논리적 int 까지 전부 박싱) · `rt_array_get/set_native`(`:3451-3452`) · `rt_str_eq_native`(`:6842`) · `rt_parse_int/float_native`(`:2200`/`:2125`) · `hexa_array_new`(`:1122`) · `hexa_arr_zeros_leaf{,_int}`(`:9427`) — **전 시드 export 집합에서 위반자는 정확히 그 8개**다. 규칙이 실측으로 자기검증된다.\n\n이 게이트는 U-floor 를 **포함**하고(libc = 프로토타입 없음 → 위반), **`hexa_int`(CABI-SCALAR)** 도 잡는다 — 이름 allow-list 로는 영원히 못 잡던 놈이다. 백엔드 intrinsic(`hexa_bool`/`hexa_truthy`/`hexa_eq`/`hexa_mul`…)은 `extern fn` 으로 선언되지 않으므로 **면제 목록을 손으로 관리할 필요가 없다** (실측: `valop_core_arm64.s:39-40` `cset x0,eq` → `bl _hexa_bool` = 백엔드가 ABI 통제).\n\n**thunk 안을 채택하면 GATE-B 가 1줄로 축약된다**: \"시드 `.globl` 은 반드시 `_seed` 접미 + 그 C 프로토타입은 all-HexaVal\".\n\n## (c) GATE-C (레이아웃) — 싸고 강력\n\ndescriptor 필드가 전부 int64 이므로: **`stdlib/runtime/array_*.hexa` 에서 `__hx_ptr_(load|store)32` 사용 = FATAL** (grep 1줄). 이번 클래스 C 를 정확히 잡고, `arr_zeros_leaf.hexa:61-63` 정답 idiom 은 통과한다.\n\n**구현 위치 = Stage-0 lint(`tool/seed_abi_lint.hexa`) · `.githooks/pre-commit` BLOCK + CI 필수 게이트.** 스테이지에서 못 하는 이유: `self/runtime.h`/`runtime_core.c` 는 gitignored 생성물이고 resolver 는 `regen_runtime_core_from_emitter`(`:1803`) **이전**에 돈다 → 헤더를 오라클로 쓸 수 없다. GATE-A 는 stale `.o`/드리프트용 **런타임 안전망**으로 남긴다 (2중).\n\n---\n\n# 5. 검증\n\n## ⚠️ 격리 스모크의 함정 (이번 세션 교훈)\n\ncarrier 를 C 스텁으로 대체한 드라이버는 **arena/heap 결함을 못 잡는다** (`hexa_ptr_alloc` 은 이름이 carrier 지만 실체는 ARENA — `alloc_syscall_native.o` 가 링크에서 이겨 `hexa_arena_alloc` 으로 위임하고, descriptor 를 거기 두면 frame rewind 후 len/cap 이 조용히 덮인다). **스모크는 반드시 실제 `runtime.a` 를 링크**해야 한다. 그리고 클래스 C 는 **격리 드라이버가 손으로 24B 디스크립터를 만들면 안 보인다** — 시드가 mint 한 걸 C가 읽고, C가 mint 한 걸 시드가 쓰는 **교차** 케이스를 반드시 넣어라.\n\n## 드라이버가 단언해야 할 것\n\n| # | 케이스 | 신 시드 기대 | 구 시드 (현재) |\n|---|---|---|---|\n| 1 | `v = hexa_arr_i64_new(4)` | tag=5, `*(int64*)(p+8)==0`, `*(int64*)(p+16)==4`, **malloc_size(p) ≥ 24** | cap 이 +12(len 상위)에, +16 은 힙 밖 |\n| 2 | push ×3 → `hexa_arr_i64_len(v)` | **3** | **0** (rax=tag) |\n| 3 | `hexa_arr_i64_box(v,1)` | 두번째 원소 | **\"index out of bounds\" throw** |\n| 4 | **grow**: `new(1)` + push ×9 | len=9, 원소 0..8 전부 보존 | realloc(old, 0)=free → UAF |\n| 5 | **교차 A**: C-mint `hexa_arr_i64_new_esc(4)` (`runtime_core_emit.hexa:2893`, 정상 24B) → **시드** push ×3 → C read len | 3 | cap 을 +12 에서 읽어 0 → free-then-write |\n| 6 | **교차 B**: 시드-mint → 코드젠 packed get (`arrpk_get`, len@8 을 **64-bit** 로 읽음) | in-range OK / OOB throw | len=`(cap<<32)\\|len` = 거대 → bounds 통과 → **OOB SIGSEGV 139** |\n| 7 | f64 전부 동형 + `hexa_arr_f64_push(v, 1.5)` (double shim `:3066` 경유) | 1.5 왕복 | 동일 파손 |\n| 8 | `nm build/array_typed_leaf_native.o \\| grep U` | carrier(`hexa_`/`hxlcl_`/`__hx_`) **만** | `malloc realloc write hexa_int` |\n| 9 | `nm` 정식 이름 | 시드에 `hexa_arr_i64_new` **부재**(=`_seed` 만), `runtime_core.o` 에 T | 시드가 정식 이름 소유 |\n\n## 로컬 재현 (풀 호스트 · 환경오염 방지 필수)\n\n```sh\n# ⚠️ 이번 세션에 3번 오진했다 — stale 산출물을 코드 결함으로 착각\nenv -u HEXA_PREBUILT_RUNTIME sh -c '\n  cd <worktree> &&\n  rm -f self/runtime.c self/runtime_core.c self/runtime_hi_gen.c build/*_native.o build/runtime.a &&\n  TARGET=linux-x86_64 bash tool/stage_resolve_runtime_a 2>&1 | tee /tmp/stage.log\n'\ngrep -E \"ARR-I64|ARR-F64|SEED-ABI\" /tmp/stage.log     # 어느 경로로 들어갔는지 확인\nnm -u build/array_typed_leaf_native.o | grep -vE '^\\s*U _?(hexa_|hxlcl_|__hx_)'   # 비어야 함\nnm    build/runtime.o | grep -E ' T _?hexa_arr_i64_new$'                          # thunk 소유 확인 (§6 중복 체크)\n```\n빌드/측정은 **pool(ghost·aiden·summer)** 에서. mini 는 git/gh 전용.\n\n## byteeq 리스크\n\n- **byte-neutral 아님 · RECONVERGE-flip.** 게이트는 `cc-prc2-fix.o == cc-gen3.o == cc-gen4.o` 재수렴이지 origin 대비 바이트동일이 아니다 (`selfhost-byteeq-gate.yml:8-9`; `runtime_core_emit.hexa:2873-2876` 이 같은 선례를 문서화: \"runtime.o bytes change → fixpoint RECONVERGES to a new (still-identical) fixpoint … byteeq gate + ship smoke stay GREEN\").\n- thunk 안은 **gen2 emit 문자열을 안 바꾸므로 `self/native/hexa_cc.c` 재생성이 없다** (proto-to-HexaVal 안과의 결정적 차이). 흔들리는 건 `runtime_core.o`/`runtime.a` 바이트뿐.\n- **머지 폴리시**: 시드 4+4 를 **default-OFF 로 되돌려서 머지**(`HEXA_RT_ARRAY_I64_LEAF_NATIVE`/`F64`/`ZEROS` 를 opt-in 으로 — 현재 `:574`/`:763`/`:707` 은 opt-out 만 있어 사실상 ON) → 3-target byteeq + `install.sh` 컨슈머 스모크 GREEN → flip. #4930 이 \"전 CI GREEN 인데 3타깃 전부 깨져 #4932 revert\" 된 지점이 정확히 여기다.\n\n---\n\n# 6. 모르는 것 + 다음에 볼 곳\n\n1. **`tool/restore_frozen_seeds:1263-1315` 가 frozen `self/runtime.c` 에 주입하는 8개 C 바디가 leaf-ON 에서 `runtime_core.c` 의 thunk 와 중복정의로 충돌하는가.** 오늘 leaf-ON 에서 시드가 정식 이름을 export 하는데 링크가 되고 있다 → runtime.c 의 사본이 `runtime.a` 에 안 들어가거나 다른 메커니즘이 있다는 뜻. thunk 는 leaf-ON 에서 **새로** C 정의를 만드므로 여기가 갈릴 수 있다. **확인 = `nm build/runtime.o build/runtime_core.o | grep hexa_arr_i64_new` 1회.** (실패해도 **시끄러운** duplicate-symbol 링크 에러 → 안전.)\n2. **`self/native/rtcore_array-typed-leaf_emit.hexa` (5번째 C 사본, `:140-190` 에 8개 바디 전문 + `tool/regen_rtcore_array-typed-leaf_native_o.sh` 소비)** — 이 라우트가 실제 릴리스 경로에 쓰이는지. 쓰인다면 프로토타입 락스텝 대상.\n3. **own-obj(`--emit=obj --isolate`, `stage_resolve_runtime_a:597`) 경로의 ABI 가 pair 인가 C 인가** — thunk 안에서는 **어느 쪽이든 무해**(둘 다 `_seed` 이름으로 export 되고 C 가 정식 이름을 소유)지만, aprime 에 **C-ABI export 모드**가 있다면 rename 없이 시드를 C-ABI 로 뽑는 더 싼 길(Route C 축)이 열린다. 확인 = `objdump -d build/array_typed_leaf_native.o` 의 `hexa_arr_i64_new_seed` 프롤로그가 `rdi` 를 tag 로 읽는지.\n4. **arm64-linux 코드젠 백엔드가 별도 파일인지** (`compiler/codegen/` 에 arm64_linux 가 있는지 안 봤다). thunk 안은 codegen 무변경이라 영향 없지만, 대안 (B)를 재검토할 때 필요.\n5. **thunk 의 push/box 핫패스 비용** — 미측정. flip 전 `tool/unshadow_native_arr_bench.hexa` (자체 `static int hexa_arr_i64_len` 사본을 emit 하므로 측정 대상이 바뀐다 — 갱신 필요) 로 실측.\n6. **regex_rt 시드가 `hexa_map_get/set`(`HexaVal(HexaVal, const char*)` = CABI-SCALAR)을 `extern fn` 으로 pair 호출하는가** — 그렇다면 동일 클래스-B 결함(default-OFF 라 잠복). 확인 = `stdlib/runtime/regex_rt.hexa` 의 `extern fn` 목록 + `self/native/regex_rt_arm64.s` 의 `bl _hexa_map_get` 앞 `ldp` 유무.\n7. **f32 쌍둥이**(`HexaArrF32`, `runtime_core_emit.hexa:1390` / `hexa_arr_f32_push_bits` `:2970`) — 시드가 **없어서** 지금은 C-only(안전). 나중에 시드화하면 동일 3-클래스 점검 필수."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Reference"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Census"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Verify"
    },
    {
      "type": "workflow_phase",
      "index": 4,
      "title": "Design"
    },
    {
      "type": "workflow_phase",
      "index": 5,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "reference:bridge",
      "phaseIndex": 1,
      "phaseTitle": "Reference",
      "agentId": "aae60e5dda7994623",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960152965,
      "queuedAt": 1783960152964,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -rn \"op_label(\\\"hexa_arena_alloc\\\")\\|op_label(\\\"hexa_a…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960311303,
      "tokens": 66245,
      "toolCalls": 15,
      "durationMs": 158338,
      "resultPreview": "## 결론 요약 (선행 전제 1건 반증됨)\n\n**⚠️ 태스크 전제 \"array_core.hexa 는 이 브리지를 쓴다\" 는 FALSE.** grep 히트는 **주석**이고, 그 주석은 정확히 반대를 말한다 — \"브리지 불필요\". 그리고 typed_leaf 가 브리지를 안 쓰는 건 **역사적 누락이 아니라 구조적 불가**다. 아래 전부 file:line.\n\n---\n\n## 1. 브리지의 정확한 idiom (복붙 가능 · `self/runtime_core_emit.hexa:1780-1858`)\n\n전체가 `#ifdef HEXA_RT_ALLOC_NATIVE` 안에 있다 (`:1780`, 닫힘 `:1990` 부근 `#endif // HEXA_RT_ALLOC_NATIVE`). 메커니즘은 **4-스텝**이고, 네가 물은 …"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "census:i64",
      "phaseIndex": 2,
      "phaseTitle": "Census",
      "agentId": "a02d20052c495bf8b",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960311321,
      "queuedAt": 1783960311306,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "## 판정 요약 — 경계 28개 중 **미스매치 14개** | 함수 | ingr…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960682084,
      "tokens": 117600,
      "toolCalls": 24,
      "durationMs": 370763,
      "resultPreview": "{\"boundaries\":[{\"symbol\":\"hexa_arr_i64_new / cap\",\"kind\":\"ingress-param\",\"c_abi\":\"self/runtime_core_emit.hexa:1126 `HexaVal hexa_arr_i64_new(int cap);` (extern arm :2802). 네이티브 호출자도 동일 — compiler/codegen/x86_64_linux.hexa:3235-3239 `// cap in rdi (single C-ABI int reg)` + `mov rdi, <cap>` / `call hexa_arr_i64_new`\",\"seed_abi\":\"self/native/array_typed_leaf_x86_64.s:28-29 `mov [rbp-128], rdi # store…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "census:f64",
      "phaseIndex": 2,
      "phaseTitle": "Census",
      "agentId": "a9f0cdb924540ec38",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960311321,
      "queuedAt": 1783960311306,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "## 판정: f64 시드는 클래스 A(egress) + 클래스 B(스칼라 ingr…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960784020,
      "tokens": 131892,
      "toolCalls": 40,
      "durationMs": 472699,
      "resultPreview": "{\"boundaries\":[{\"symbol\":\"hexa_arr_f64_new\",\"kind\":\"ingress-param\",\"c_abi\":\"`HexaVal hexa_arr_f64_new(int cap);` — self/runtime_core_emit.hexa:1134 (runtime.h surface) + self/runtime_core_emit.hexa:3099 (`extern HexaVal hexa_arr_f64_new(int cap);` under the F64-leaf arm) + self/runtime.h:281. C-ABI scalar int → SysV: cap in edi/rdi (single INTEGER reg). AAPCS64: cap in w0. Callers: gen2 C emitter …"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "verify:i64:hexa_arr_i64_new / cap:ingress-param",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "addade05a43da14b1",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960682099,
      "queuedAt": 1783960682087,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The mismatch is real and reproducible from source. C-ABI si…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960766207,
      "tokens": 55516,
      "toolCalls": 8,
      "durationMs": 84108,
      "resultPreview": "{\"refuted\":false,\"reason\":\"The mismatch is real and reproducible from source. C-ABI side: self/runtime_core_emit.hexa:1126 declares `HexaVal hexa_arr_i64_new(int cap);` (public runtime.h), extern arm at :2802, and the #else C body uses the same scalar `int cap` — so cap travels in rdi/w0 alone. The native backend calls it identically: compiler/codegen/x86_64_linux.hexa:3235-3241 emits `mov rdi, <i…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "verify:i64:hexa_arr_i64_new → malloc(16):outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a856c1b2c4dc603b3",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960682099,
      "queuedAt": 1783960682087,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Directly verified in the repo; every cited line matches. st…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960762173,
      "tokens": 53967,
      "toolCalls": 9,
      "durationMs": 80074,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Directly verified in the repo; every cited line matches. stdlib/runtime/array_typed_leaf.hexa:53 declares `extern fn malloc(n: int) -> int` (raw libc egress, no __hxw_/__hxseed_ bridge in the file — grep 0 hits). self/native/array_typed_leaf_x86_64.s:50-54 emits `mov rsi,16 # hv arg payload` / `mov rdi,0 # tag default = TAG_INT` / `call malloc` / `mov [rbp-160],rax # sto…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "verify:i64:hexa_arr_i64_new → malloc(cap*8):outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a452de38bb59ec2cb",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960682099,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. Both malloc call-sites inside hexa_…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960801681,
      "tokens": 57209,
      "toolCalls": 8,
      "durationMs": 119581,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. Both malloc call-sites inside hexa_arr_i64_new are genuine C-ABI/pair-ABI mismatches, verified by reading the seeds directly. self/native/array_typed_leaf_x86_64.s: site 1 (16B descriptor) at L50-54 does `mov rsi,16 / mov rdi,0 / call malloc / mov [rbp-160],rax (rax kept as TAG) / mov r15,rdx (payload from rdx)`; site 2 (cap*8 data buffer) at L70-…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:i64:hexa_arr_i64_push / x (int64_t):ingress-param",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a8ba3185b816b595a",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960682100,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. (1) Prototype is a C-ABI scalar: `H…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960780273,
      "tokens": 59031,
      "toolCalls": 10,
      "durationMs": 98173,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. (1) Prototype is a C-ABI scalar: `HexaVal hexa_arr_i64_push(HexaVal v, int64_t x)` at self/runtime_core_emit.hexa:1127 & :2821 and self/runtime.h:279 -> v in rdi:rsi, x in rdx alone. (2) The native backend calls it the same C-ABI way: compiler/codegen/x86_64_linux.hexa:3246-3252 (`// x (RAW element payload) -> rdx` + `_x86_hv_payload(s.args[pi], s…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:i64:hexa_arr_i64_push → realloc:outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a14f17231b4d7ab06",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960682100,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960792281,
      "tokens": 54707,
      "toolCalls": 13,
      "durationMs": 110181,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf.hexa:54 declares `extern fn realloc(p: int, n: int) -> int` and calls it at L84 in the grow branch. (2) self/native/array_typed_leaf_x86_64.s:235-243 emits the hexa pair-ABI exactly as alleged: `mov rsi,[rbp-120]` (old ptr payload) / `mov rdi,[rbp-392]` (tag, materialized 0 at L215) / `mov rcx,[rbp-136]` (nbytes…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "verify:i64:hexa_arr_i64_push → write (OOM path):outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "aaaee2f65147c2375",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960682100,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified directly in the tree, all refutation avenues fail.…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960777869,
      "tokens": 54459,
      "toolCalls": 10,
      "durationMs": 95769,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Verified directly in the tree, all refutation avenues fail. (1) stdlib/runtime/array_typed_leaf.hexa:55 declares raw libc `extern fn write(fd: int, buf: int, n: int) -> int` — no carrier. (2) The cited asm is real code inside the right function: self/native/array_typed_leaf_x86_64.s:336-350 under label `.L6cf9_hexa_arr_i64_push_bb3` emits `mov rsi,2 # hv arg payload` / `…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "verify:i64:hexa_arr_i64_len / return:return",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a956e5cbf4dbb0e20",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960682100,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Claim CONFIRMED by direct file read; every refutation avenu…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960778902,
      "tokens": 63018,
      "toolCalls": 10,
      "durationMs": 96802,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Claim CONFIRMED by direct file read; every refutation avenue checked and closed. (1) The C prototype really is a 32-bit scalar: self/runtime_core_emit.hexa:1128 emits `int     hexa_arr_i64_len(HexaVal v);` into public runtime.h and :2804 emits `extern int hexa_arr_i64_len(HexaVal v);` for the native-leaf arm (mirrored at self/runtime_core.c:1070/:2607) — NOT HexaVal, so …"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "verify:i64:hexa_arr_i64_box / i (int64_t):ingress-param",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "ac00dff6426d289e9",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960762174,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Claim CONFIRMED — all five refutation avenues fail on direc…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960880230,
      "tokens": 56935,
      "toolCalls": 12,
      "durationMs": 118055,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Claim CONFIRMED — all five refutation avenues fail on direct file inspection. (1) The C prototype is a real scalar, not a HexaVal: self/runtime.h:280 `HexaVal hexa_arr_i64_box(HexaVal v, int64_t i);` (mirrored by the emitter at self/runtime_core_emit.hexa:1129 and the leaf-native `extern` at :2768), so SysV puts v in rdi:rsi and i in rdx ALONE. (2) No pair-calling specia…"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "verify:i64:hexa_arr_i64_box → hexa_int:outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "ac3fc2e7848ceb84e",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960766208,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Attempted refutation on all five avenues; every one fails, …",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960900358,
      "tokens": 60690,
      "toolCalls": 13,
      "durationMs": 134149,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Attempted refutation on all five avenues; every one fails, and the claim is confirmed by direct file inspection.\\n\\n(1) C prototype is genuinely a C-ABI SCALAR, not HexaVal: self/runtime.h:112 `HexaVal hexa_int(int64_t n);`; body self/runtime_core_emit.hexa:1778 `HexaVal hexa_int(int64_t n) { return HX_MAKE_INT(n); }`, and :1776 the `extern` under HEXA_RT_SELFEMIT/HEXA_R…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "verify:i64:hexa_arr_f64_new / cap:ingress-param",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a16df640681d31ead",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960777869,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Claim CONFIRMED — not refuted. (1) The C prototype really i…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960899442,
      "tokens": 70826,
      "toolCalls": 14,
      "durationMs": 121573,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Claim CONFIRMED — not refuted. (1) The C prototype really is a scalar: self/runtime_core_emit.hexa:1134 `HexaVal hexa_arr_f64_new(int cap);`, repeated as `extern` in BOTH seed-active arms (:3062 under HEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE, :3068 under HEXA_RT_SELFEMIT||TYPED_LEAF) and in self/runtime.h:281 — so cap travels in edi/w0 alone. (2) The seed body really reads the…"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "verify:i64:hexa_arr_f64_new → malloc ×2:outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a0a29de485a42e461",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960778903,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. Read the files directly. stdlib/run…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960849753,
      "tokens": 51903,
      "toolCalls": 6,
      "durationMs": 70850,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. Read the files directly. stdlib/runtime/array_typed_leaf_f64.hexa:30 declares raw `extern fn malloc(n: int) -> int` with no carrier and no __hxw_/__hxseed_ bridge (grep: 0 hits in that file, unlike array_core.hexa). self/native/array_typed_leaf_f64_x86_64.s:50-54 emits `mov rsi,16 / mov rdi,0 / call malloc / mov [rbp-160],rax (store tag L4) / mov …"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "verify:i64:hexa_arr_f64_push_bits / xbits (int64_t):ingress-param",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a6a90a66539bad5cc",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960780274,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Claim CONFIRMED — refutation failed on every avenue. (1) Ca…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960903169,
      "tokens": 63245,
      "toolCalls": 10,
      "durationMs": 122895,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Claim CONFIRMED — refutation failed on every avenue. (1) Callee: self/native/array_typed_leaf_f64_x86_64.s:136-150 is the genuine `hexa_arr_f64_push_bits` body (label :136, `.loc 1 53 0` → array_typed_leaf_f64.hexa:53). Its prologue reads param1 as a (tag,payload) PAIR: `mov [rbp-296], rdx # store tag L1` / `mov r12, rcx # ingress param payload`. r12 is the sole non-prol…"
    },
    {
      "type": "workflow_agent",
      "index": 16,
      "label": "verify:i64:hexa_arr_f64_push_bits → realloc / write / hexa_exit:outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a3dd94536c4f7965d",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960784021,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Claim CONFIRMED, not refuted. (1) Prototypes are raw libc C…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960872117,
      "tokens": 56180,
      "toolCalls": 10,
      "durationMs": 88096,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Claim CONFIRMED, not refuted. (1) Prototypes are raw libc C-ABI scalars: stdlib/runtime/array_typed_leaf_f64.hexa:31-32 = `extern fn realloc(p: int, n: int) -> int` and `extern fn write(fd: int, buf: int, n: int) -> int`; the seed's own header (array_typed_leaf_f64_x86_64.s:5-6) declares the U-floor \\\"malloc realloc write hexa_exit hexa_throw (libc-bearing)\\\". (2) The ci…"
    },
    {
      "type": "workflow_agent",
      "index": 17,
      "label": "verify:i64:hexa_arr_f64_len / return:return",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "abed8669243363c89",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960792285,
      "queuedAt": 1783960682088,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — I could not refute it. Every stated refutation …",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960926461,
      "tokens": 65853,
      "toolCalls": 17,
      "durationMs": 134176,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — I could not refute it. Every stated refutation avenue was checked against the files and each one fails.\\n\\n(1) C prototype is genuinely scalar `int`, not HexaVal. /Users/mini/dancinlab/hexa-lang/self/runtime_core_emit.hexa:1136 emits into the PUBLIC runtime.h: `int     hexa_arr_f64_len(HexaVal v);`. And :3063 / :3070 emit `extern int hexa_arr_f64_len(HexaVal …"
    },
    {
      "type": "workflow_agent",
      "index": 18,
      "label": "verify:f64:hexa_arr_f64_new:ingress-param",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a8c16a809904b27b8",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960801682,
      "queuedAt": 1783960784027,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "high",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960908127,
      "tokens": 60805,
      "toolCalls": 11,
      "durationMs": 106445,
      "resultPreview": "{\"confidence\":\"high\",\"reason\":\"NOT REFUTED — the mismatch is real and every refutation avenue is closed by direct file inspection.\\n\\n(1) Prototype is genuinely C-ABI scalar: self/runtime.h:281 `HexaVal hexa_arr_f64_new(int cap);`, self/runtime_core_emit.hexa:1134 (runtime.h surface) and :3099/:3105 (`extern HexaVal hexa_arr_f64_new(int cap);` under both the F64-leaf arm and the SELFEMIT/TYPED arm…"
    },
    {
      "type": "workflow_agent",
      "index": 19,
      "label": "verify:f64:hexa_arr_f64_new:return",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a8635832267fff7f1",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960849754,
      "queuedAt": 1783960784027,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Could not refute — the STRUCT-LAYOUT mismatch is confirmed …",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960984003,
      "tokens": 65829,
      "toolCalls": 13,
      "durationMs": 134248,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Could not refute — the STRUCT-LAYOUT mismatch is confirmed at source on every cited line. (1) C pointee is 24B/int64: self/runtime_core_emit.hexa:1381-1385 emits `typedef struct HexaArrF64 { double* data; int64_t len; int64_t cap; }` (len@8, cap@16), corroborated by self/runtime.h:82 (HexaArr with int64_t len/cap) and the f32 twin comment at :1390 (\\\"the SAME 24-byte (pt…"
    },
    {
      "type": "workflow_agent",
      "index": 20,
      "label": "verify:f64:hexa_arr_f64_push_bits:ingress-param",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "afd96b433942a49c6",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960872119,
      "queuedAt": 1783960784027,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed by direct file inspection. (1) Seed: self/native/…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783960984494,
      "tokens": 61537,
      "toolCalls": 10,
      "durationMs": 112375,
      "resultPreview": "{\"refuted\":false,\"reason\":\"Confirmed by direct file inspection. (1) Seed: self/native/array_typed_leaf_f64_x86_64.s:136 defines hexa_arr_f64_push_bits; :147-150 read param0 as pair (tag=rdi, payload=rsi) and param1 as pair (tag=rdx, VALUE=rcx → r12). r12 is never rewritten (only push@141 / mov@150 / pop@329) and is consumed at :292 `mov rsi, r12` → :294 `mov qword ptr [r10], rsi # __hx_ptr_store64…"
    },
    {
      "type": "workflow_agent",
      "index": 21,
      "label": "verify:f64:hexa_arr_f64_push_bits:ingress-param",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a1377fbcdb2387aef",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960880232,
      "queuedAt": 1783960784027,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED. (1) C's HexaArrF64 is emitted UNCONDITIONALLY at…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783961014691,
      "tokens": 61853,
      "toolCalls": 11,
      "durationMs": 134459,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED. (1) C's HexaArrF64 is emitted UNCONDITIONALLY at self/runtime_core_emit.hexa:1381-1385 as {double* data@0; int64_t len@8; int64_t cap@16} (24B) — I grepped: it is the only HexaArrF64 typedef, no guarded alternate layout exists. (2) stdlib/runtime/array_typed_leaf_f64.hexa models it as 16B with i32 len@8/cap@12: header comment says \\\"sizeof 16\\\", new() does mal…"
    },
    {
      "type": "workflow_agent",
      "index": 22,
      "label": "verify:f64:hexa_arr_f64_len:return",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a21fc539f6e2b9ca4",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960899444,
      "queuedAt": 1783960784027,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. (1) The C prototype really is a sca…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783961036528,
      "tokens": 66645,
      "toolCalls": 11,
      "durationMs": 137082,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. (1) The C prototype really is a scalar int: self/runtime_core_emit.hexa:1136 emits `int hexa_arr_f64_len(HexaVal v);` into runtime.h and BOTH native arms (:3063 under HEXA_RT_CORE_ARRAY_F64_LEAF_NATIVE, :3070 under HEXA_RT_SELFEMIT||TYPED_LEAF_NATIVE) emit `extern int hexa_arr_f64_len(HexaVal v);` — so a C caller reads eax/w0 and the correct C bod…"
    },
    {
      "type": "workflow_agent",
      "index": 23,
      "label": "verify:f64:hexa_arr_f64_box:ingress-param",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a0797f66aeb23e478",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960900358,
      "queuedAt": 1783960784027,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — all five refutation routes fail on inspection. …",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783961011258,
      "tokens": 78248,
      "toolCalls": 10,
      "durationMs": 110900,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — all five refutation routes fail on inspection.\\n\\n(1) C prototype is NOT HexaVal: self/runtime.h:283 and the SSOT self/runtime_core_emit.hexa:3101/3108 both declare `HexaVal hexa_arr_f64_box(HexaVal v, int64_t i);` — param 1 is a C-ABI scalar → rdx (x86_64 SysV) / x2 (AAPCS64), since `v` consumes rdi:rsi / x0:x1 as a 16B struct.\\n\\n(2) The cited asm is real c…"
    },
    {
      "type": "workflow_agent",
      "index": 24,
      "label": "verify:f64:malloc:outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a47dc558309d2bc30",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960903169,
      "queuedAt": 1783960784027,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783961002078,
      "tokens": 58827,
      "toolCalls": 10,
      "durationMs": 98909,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED, not refuted. (1) stdlib/runtime/array_typed_leaf_f64.hexa:30 declares `extern fn malloc(n: int) -> int` — raw libc, whose true C-ABI is `void* malloc(size_t)` (size in rdi/x0, result in rax/x0 ALONE). (2) The emitted seed calls it PAIR: self/native/array_typed_leaf_f64_x86_64.s:50-54 (inside .L1161_hexa_arr_f64_new_bb2, real instructions) `mov rsi,16 # hv arg …"
    },
    {
      "type": "workflow_agent",
      "index": 25,
      "label": "verify:f64:realloc:outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a746861ec744e8198",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960908127,
      "queuedAt": 1783960784027,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — I could not refute it; every escape hatch is cl…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783961020884,
      "tokens": 65483,
      "toolCalls": 13,
      "durationMs": 112756,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — I could not refute it; every escape hatch is closed by direct file evidence. (1) stdlib/runtime/array_typed_leaf_f64.hexa:31 declares `extern fn realloc(p: int, n: int) -> int` — raw libc, not hxlcl_, not bridged; called :61, result stored to data@0 at :67. (2) The cited asm is real code, not a comment, and is exactly as quoted: self/native/array_typed_leaf_f…"
    },
    {
      "type": "workflow_agent",
      "index": 26,
      "label": "verify:f64:write:outgoing-call",
      "phaseIndex": 3,
      "phaseTitle": "Verify",
      "agentId": "a6a908f1f28b9d71e",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783960926462,
      "queuedAt": 1783960784027,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED — every refutation avenue is closed by direct fil…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783961034700,
      "tokens": 58334,
      "toolCalls": 11,
      "durationMs": 108238,
      "resultPreview": "{\"refuted\":false,\"reason\":\"CONFIRMED — every refutation avenue is closed by direct file evidence. (1) Declaration is a raw libc extern with C-ABI scalar params: stdlib/runtime/array_typed_leaf_f64.hexa:32 `extern fn write(fd: int, buf: int, n: int) -> int`, called at :64 `write(2, msg, 20)` on the realloc-OOM arm (identical twin at array_typed_leaf.hexa:55/:87). (2) The cited asm is real executabl…"
    },
    {
      "type": "workflow_agent",
      "index": 27,
      "label": "design:bridge-wrap",
      "phaseIndex": 4,
      "phaseTitle": "Design",
      "agentId": "a9f3ef01ed1f0243b",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783961036535,
      "queuedAt": 1783961036532,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /Users/mini/dancinlab/hexa-lang && grep -rn 'hexa_arr_i6…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783961377187,
      "tokens": 101216,
      "toolCalls": 21,
      "durationMs": 340652,
      "resultPreview": "## 결론 (선(先) 판정)\n\n**__hxw_ 브리지를 typed-leaf 에 적용하는 안은 정답이 아니다.** 두 개의 독립적인 killer 가 있고, 둘 다 실측/코드 인용으로 확정된다.\n\n1. **커버리지 구조 결함** — 브리지(`#define`)가 사는 파일은 `self/runtime_core.c` **뿐**이다. typed-leaf 심볼의 진짜 호출자 2종(코드젠이 뱉는 **어셈블리 `call`**, 그리고 코드젠이 뱉는 **user.c**)에 cpp 는 도달하지 못한다. arena 는 호출자가 runtime_core.c **하나뿐**이라서 브리지가 완결된 것이다.\n2. **★ 결함 클래스 C (디스크립터 레이아웃) — 지금까지 아무도 안 잡은 진짜 SIGSEGV 근인.** ABI 브리지로는 **…"
    },
    {
      "type": "workflow_agent",
      "index": 28,
      "label": "design:proto-to-hexaval",
      "phaseIndex": 4,
      "phaseTitle": "Design",
      "agentId": "a3b967ed1d70f2ec2",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783961036537,
      "queuedAt": 1783961036533,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /Users/mini/dancinlab/hexa-lang && git grep -n 'HEXA_RT_…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783961435073,
      "tokens": 112401,
      "toolCalls": 18,
      "durationMs": 398536,
      "resultPreview": "## 판정 요약\n\n**전제 정정 1건:** \"C 호출자(runtime_core.c 안의 **수십 사이트**)\"는 **틀렸다**. 실측 C 호출 사이트는 **runtime_core.c 안 5개** + **gen2 C 이미터 문자열 7개**뿐이다. 진짜 비용은 C 호출자가 아니라 **① 네이티브 코드젠 emit 6+2 사이트 ② 컴파일러↔runtime.a 간 flag-day ABI 단절**이다.\n\n**핵심 발견 (가장 중요):** 이 안은 **`.s` 시드를 한 바이트도 안 건드린다.** 시드는 이미 pair 다 — C 프로토타입을 HexaVal로 바꾸는 순간 배포된 asm이 **저절로 맞는다**. 시드 `.hexa` 소스(`cap: int`)도 그대로 둔다(aprime가 payload를 unbox해서 raw …"
    },
    {
      "type": "workflow_agent",
      "index": 29,
      "label": "design:gate-gap",
      "phaseIndex": 4,
      "phaseTitle": "Design",
      "agentId": "a7eabab5a4c8338c6",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783961036537,
      "queuedAt": 1783961036533,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /Users/mini/dancinlab/hexa-lang && grep -n '\\-DHEXA_RT_[…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783961561045,
      "tokens": 139232,
      "toolCalls": 23,
      "durationMs": 524508,
      "resultPreview": "## 0. 결론 먼저 — 전제 2건이 실측으로 교정됐다\n\n**(a) `.s-seed` 경로에 U-floor 검사가 \"아예 없다\"는 정확하지만, 게이트 코드는 이미 repo 에 있다.** `_rn_seed_und_gate` (`tool/stage_resolve_runtime_a:1812-1815`) 가 정확히 그 검사다:\n\n```sh\n_rn_seed_und_gate() {\n    command -v nm >/dev/null 2>&1 || return 0\n    nm -u \"$1\" 2>/dev/null | awk '{print $NF}' | grep -vE '^_?(hexa_|hxlcl_|__hx_)' | tr '\\n' ' '\n}\n```\n\n문제는 **스코프**다. 이 함수는 `build_runtime_a_fro…"
    },
    {
      "type": "workflow_agent",
      "index": 30,
      "label": "synthesize",
      "phaseIndex": 5,
      "phaseTitle": "Synthesize",
      "agentId": "a0ad1809ecec55811",
      "model": "claude-opus-4-8[1m]",
      "state": "done",
      "startedAt": 1783961561050,
      "queuedAt": 1783961561047,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /Users/mini/dancinlab/hexa-lang && grep -rn \"fn _pack_ar…",
      "promptPreview": "# 확정 사실 (전부 실측 · 2026-07-13~14)\n\n## substrate\nself/native/*.s 시드는 aprime native emit 산물이다. aprime 는 시드의 **모든 호출을 hexa pair-ABI 로 낮춘다**:\n인자는 (tag,payload) 레지스터 쌍(rdi:rsi / x0:x1)을 타고, 결과의 payload 는 **둘째 반환 레지스터(rdx/x1)** 에서 읽힌다.\nHexaVal = {tag@0, payload@8} 16B 구조체이고, SysV/AAPCS64 의 16B by-value 규약이 정확히 그 pair 레지스터와 일치한다.\n\n## 이미 확정된 결함 클래스 A (raw libc egress) — 부분 해결됨\n시드가 `extern fn malloc/calloc/r…",
      "lastProgressAt": 1783962019851,
      "tokens": 132052,
      "toolCalls": 19,
      "durationMs": 458801,
      "resultPreview": "# 1. 미스매치 최종본 — typed-leaf i64/f64\n\n세 개의 **독립** 결함 클래스가 겹쳐 있다. 클래스 B만 고쳐도, 클래스 A만 고쳐도(=carrier 치환) SIGSEGV는 안 사라진다.\n\n## 반증된 전제 (먼저)\n\n| 전제 | 판정 | 증거 |\n|---|---|---|\n| \"array_core.hexa 는 `__hxseed_`/`__hxw_` 브리지를 쓴다\" | **FALSE** | `stdlib/runtime/array_core.hexa:54-63` 주석이 정반대를 말한다 — \"no C bridge needed for this native→native edge (the `__hxseed_arena_alloc` C bridge … exists **only for C-side calle…"
    }
  ],
  "totalTokens": 2201738,
  "totalToolCalls": 410
}```
