{
  "ref_sha": "fa9815944",
  "tracks": [
    {
      "track": "axis-③ round-3 track A — kill the darwin exec-path `as` fork (route --emit=exec through the native obj writer, then default-flip darwin exec)",
      "ready": true,
      "risk_class": "bit-changing",
      "edits_apply": true,
      "confidence": "medium",
      "corrections": [
        "EDIT F1 comment-banner verbatim: the actual line compiler/main.hexa:1080 is `// ── Assemble (system `as`) ` followed by 35 trailing box-drawing chars (U+2500 · 37 total incl. the 2 leading). The spec's rendered dash-run appears shorter (~30). The old_string must reproduce the file banner byte-exact, OR anchor only on `write_file(asm_path, asm_text)` (unique at :1078 — :1058/:1063 use out_path) and drop the comment from the match. Medium-confidence because I cannot count the spec's exact dashes from my side.",
        "EDIT F2 line-label off by 2: spec cites :1106-1109 but the quoted old block actually begins at :1104 (`write_file(out_path + \".compile_failed\",`) and runs through :1109. The quoted text itself matches verbatim; only the numeric label is imprecise.",
        "GATE wording 'encode miss emits SILENTLY-EMPTY bytes' is imprecise: encode_arm64_insn eprintln's `ENCODE-MISS: ...` (macho_arm64.hexa:713/:726/:1052) THEN return 0 — a miss is logged to stderr (not silent) but still emits a 0-word and does not abort. The ENCODE-MISS census-grep gate is therefore the correct mechanism; only the 'silently' phrasing is wrong.",
        "Verified TRUE (no correction, for the record): HEAD==fa9815944; all 7 code old-texts (A anchor :803/:804, B :977-986, C :997-1006, D :1019-1028, E :1038-1040, F2 :1104-1109, G :508-512) match verbatim; serialize/serialize_elf_arm64/serialize_elf_x86_64 all return [Int] (A type correct); exec path really forks `as` (default backend=system, obj-native branches all require emit_kind==\"obj\"); link at :1134ff links obj_path; --linker=hexa honored at :1164 (opt-in, ld64/xcrun forked :1135-1140 by default); risk_class bit-changing correct; --stream carve-out accurate; EDIT G darwin-only scope correct (x86_64 flip :529, arm64-linux :516 stay obj-only)."
      ],
      "edits": [
        "ANSWER to the design question: it is BOTH — a small default-flip alone is insufficient. The exec path needs restructuring because the three native obj branches (main.hexa:977/:997/:1019) write out_path + exit(0); exec must instead serialize to obj_path and CONTINUE to link, skipping emit_asm + the `as` fork. Structure: 6 byte-NEUTRAL plumbing edits (opt-in via --backend=native, dormant by default since the flip blocks only set native for obj) + 1 bit-CHANGING default-flip that actually kills `as` on darwin exec.",
        "EDIT A (carrier decl, byte-neutral) — compiler/main.hexa:803. AFTER the line `let mut asm_text = \"\"` and BEFORE `if stream_mode && target == \"arm64-apple-darwin\" {`, insert:\n```\n// axis-③ round-3 track A — native-object carrier for the --emit=exec path.\n// When the native backend serializes the object for an exec build (in the\n// obj-emit branches below), the bytes land here + native_obj_ready is set, so\n// the link section writes them straight to obj_path and skips emit_asm + the\n// system `as` fork — the exec-path sibling of the --emit=obj native branches\n// (which write out_path + exit). Dormant unless backend_kind==\"native\" on an\n// exec build, so byte-neutral for every default invocation.\nlet mut native_obj_bytes: [Int] = []\nlet mut native_obj_ready = false\n```\n(Type [Int] matches serialize / serialize_elf_* return + write_bytes arg; precedent: main.hexa:671 `let mut diags: [Diagnostic] = []`.)",
        "EDIT B (darwin obj branch +exec, byte-neutral) — compiler/main.hexa:977-986. Replace old:\n```\n    if emit_kind == \"obj\" && backend_kind == \"native\" && target == \"arm64-apple-darwin\" {\n        let obj_data = pack_lir(lmodule)\n        let obj_bytes = serialize(obj_data)\n        write_bytes(out_path, obj_bytes)\n        if verbose {\n            eprintln(\"hexa-compiler: emitted Mach-O .o via native backend (\"\n                     + to_string(len(obj_bytes)) + \" bytes) to \" + out_path)\n        }\n        exit(0)\n    }\n```\nwith:\n```\n    if (emit_kind == \"obj\" || emit_kind == \"exec\") && backend_kind == \"native\" && target == \"arm64-apple-darwin\" {\n        let obj_data = pack_lir(lmodule)\n        let obj_bytes = serialize(obj_data)\n        if emit_kind == \"obj\" {\n            write_bytes(out_path, obj_bytes)\n            if verbose {\n                eprintln(\"hexa-compiler: emitted Mach-O .o via native backend (\"\n                         + to_string(len(obj_bytes)) + \" bytes) to \" + out_path)\n            }\n            exit(0)\n        } else {\n            native_obj_bytes = obj_bytes\n            native_obj_ready = true\n            if verbose {\n                eprintln(\"hexa-compiler: serialized Mach-O .o via native backend for exec link (\"\n                         + to_string(len(obj_bytes)) + \" bytes)\")\n            }\n        }\n    }\n```",
        "EDIT C (arm64-linux obj branch +exec, byte-neutral) — compiler/main.hexa:997-1006. Same shape as EDIT B: condition `emit_kind==\"obj\"` → `(emit_kind==\"obj\" || emit_kind==\"exec\")`; keep `let elf_obj = pack_lir_arm64_elf(lmodule)` + `let elf_bytes = serialize_elf_arm64(elf_obj)`; wrap the tail `if emit_kind==\"obj\" { write_bytes(out_path, elf_bytes); verbose; exit(0) } else { native_obj_bytes = elf_bytes; native_obj_ready = true; verbose }`. (ELF AArch64 label in the verbose strings.)",
        "EDIT D (x86_64 obj branch +exec, byte-neutral) — compiler/main.hexa:1019-1028. Same shape: condition +exec; keep `let elf_obj = pack_lir_x86_64(lmodule)` + `let elf_bytes = serialize_elf_x86_64(elf_obj)`; wrap tail identically (ELF x86-64 label).",
        "EDIT E (emit_asm guard, byte-neutral) — compiler/main.hexa:1038-1040. Replace `} else {\\n        asm_text = emit_asm(lmodule)\\n    }` with `} else if !native_obj_ready {\\n        asm_text = emit_asm(lmodule)\\n    }`. When native_obj_ready, asm_text stays \"\" and emit_asm is skipped (nvptx branch untouched — native_obj_ready is never set for nvptx).",
        "EDIT F1 (link section open guard, byte-neutral) — compiler/main.hexa:1078. Replace old:\n```\nwrite_file(asm_path, asm_text)\n\n// ── Assemble (system `as`) ──────────────────────────────\n```\nwith:\n```\nif native_obj_ready {\n    // axis-③ round-3 track A — object already serialized by the native backend\n    // for this exec build; write it straight to obj_path and skip write-asm +\n    // the `as` fork. Kills the last exec-path `as` execve on native targets.\n    write_bytes(obj_path, native_obj_bytes)\n    if verbose {\n        eprintln(\"hexa-compiler: wrote native object (\"\n            + to_string(len(native_obj_bytes)) + \" bytes) to \" + obj_path\n            + \"; skipped `as`\")\n    }\n} else {\nwrite_file(asm_path, asm_text)\n\n// ── Assemble (system `as`) ──────────────────────────────\n```\n(hexa is brace-delimited, not indentation-sensitive, so the enclosed `as` block keeps its body verbatim.)",
        "EDIT F2 (link section close guard, byte-neutral) — compiler/main.hexa:1106-1109. Replace old:\n```\n    write_file(out_path + \".compile_failed\",\n        \"stage=as\\ncmd=\" + as_cmd + \"\\n\" + as_out + \"\\n\")\n    exit(2)\n}\n\nif emit_kind == \"obj\" {\n```\nwith:\n```\n    write_file(out_path + \".compile_failed\",\n        \"stage=as\\ncmd=\" + as_cmd + \"\\n\" + as_out + \"\\n\")\n    exit(2)\n}\n}\n\nif emit_kind == \"obj\" {\n```\nThis closes the `else` opened in F1. For native exec, emit_kind==\"exec\" so the `if emit_kind==\"obj\"` cp+exit block (:1109) is skipped and control proceeds to the existing link step (:1134ff), which links obj_path — now our native object.",
        "EDIT G (darwin exec default-flip, BIT-CHANGING — the actual kill) — compiler/main.hexa:508-512. Replace old:\n```\nif !backend_kind_user_set\n    && target == \"arm64-apple-darwin\"\n    && emit_kind == \"obj\" {\n    backend_kind = \"native\"\n}\n```\nwith:\n```\nif !backend_kind_user_set\n    && target == \"arm64-apple-darwin\"\n    && (emit_kind == \"obj\" || emit_kind == \"exec\") {\n    backend_kind = \"native\"\n}\n```\nThis makes darwin exec default to the native writer → EDIT B fires → native_obj_ready → EDIT F1 skips `as`. Scope this round to darwin only; x86_64-linux (:529) + arm64-linux (:516) exec flips stay OFF (their plumbing from EDITs C/D is present but dormant, opt-in via --backend=native) and land in follow-up rounds after their own byteeq measurement.",
        "CARVE-OUT (note, not an edit): the --stream path (arm64-darwin, opt-in) produces asm_text without an lmodule, so native_obj_ready stays false and --stream exec still forks `as` after EDIT G. Not a regression (still builds correctly); native-exec+stream is a follow-up round."
      ],
      "gate": [
        "byteeq 3-target GREEN: darwin-arm64 is the bit-changing leg (exec binary object shifts from as-produced to native-encoder-produced); x86_64-linux + arm64-linux exec are UNTOUCHED-default (their flip blocks stay OFF) so must stay bit-identical. Plumbing EDITs A-F must be byteeq-neutral on all 3 targets BEFORE EDIT G lands (verify: apply A-F only, run byteeq — must be 3/3 identical since backend_kind is 'system' for exec by default).",
        "strace/execve gate: on a darwin exec build with EDIT G on, 0 `as` execve. macOS has no strace — use a PATH-shim counter (prepend a fake `as` that touches a sentinel file + exec-fails; assert the sentinel is never created), or `sudo dtruss -f -t execve hexa build /tmp/s.hexa 2>&1 | grep -c '/as'` == 0. Baseline (EDIT G off / --backend=system) must show `as` execve >= 1 to prove the kill is real.",
        "ENCODE-MISS==0 self-emit census on darwin: `bash tool/build_native_darwin_arm64 --self-emit` (or the darwin equivalent) — a native-writer encode miss emits SILENTLY-EMPTY bytes (macho_arm64.hexa returns 0 on miss), so MISS>0 = corrupt exec text, not a warning. Hard gate; if >0 the round output is the ordered encoder-gap list and EDIT G waits.",
        "behavior parity: OFF-vs-ON (backend=system vs native) darwin exec binaries over a corpus — stdout + rc identical (binary BYTES differ by design). Plus gen3≡gen4 self-host byteeq WITHIN the new native exec path (leg-B darwin build routes exec through the native writer).",
        "shipping smoke + install.sh consumer smoke GREEN on darwin (release-integrity: `hexa build`/`run` on the user-facing path must not break). Build/measure on the pool (aiden/summer for linux legs, a darwin host for the darwin exec gate); mini is git/gh/read only."
      ],
      "wall": "After this round darwin exec = native object + external link only (`ld64`+`xcrun` still forked at :1135-1140; `--linker=hexa` darwin path landed #4677 is the opt-in kill for THAT, next to default). Remaining exec-path `as` forks: (1) x86_64-linux + arm64-linux exec — plumbing present (EDITs C/D) but flip dormant; each is a scoped bit-changing default-flip gated on its own target byteeq, mirroring EDIT G — next rounds. (2) --stream darwin exec (carve-out above) — needs the streaming path to serialize an object instead of asm_text. (3) the deeper wall stays the external LINK leaf: default-wiring `--linker=hexa` for darwin (#4677 opt-in → default) and the ELF `hexa_ld` (x86_64/arm64-linux static ET_EXEC), which is coupled to axis-② own-start (crt1.o/-lc drop). DONE-③ for the darwin CPU toolchain = this round's `as`-kill + the `ld64`/xcrun-kill via hexa_ld default; the dynamic-link (CUDA/dlopen/net) leaf remains the named sanctioned-or-emitter candidate."
    },
    {
      "track": "axis-③ round-3 track B — make `hexa build` (self/main.hexa cmd_build) reach hexa_ld on darwin-arm64 via an opt-in HEXA_BUILD_LINKER=hexa forward: native --emit=obj (default-ON Mach-O writer, no `as`) + link through tool/hexa_ld.hexa (#4677 --linker=hexa path), mirroring the linux leg-B block. Byte-neutral: new block runs only when HEXA_BUILD_LINKER=hexa (default-OFF); any failure falls through to the existing C-transpile path.",
      "ready": false,
      "risk_class": "byte-neutral",
      "edits_apply": false,
      "confidence": "high",
      "corrections": [
        "HEAD confirmed fa9815944. All cited anchors match VERBATIM: EDIT 1 site (self/main.hexa resolve_native_cc closes at line 2665 '}', blank 2666, '// S5: fail-loud...' 2667, 'fn die_no_native_cc()' 2668; home_cc block 2659-2665 verbatim; resolve_hexa_ld absent = no collision). EDIT 2 site (3576 '        }', 3577 '    }', 3578 '    // Rider (axis ① measurement)...' verbatim; leg-B opens 3541; emit-cmd mirrors 3550). All helper fns exist (basename_stem 2355, install_dir_from_argv0 2507, file_exists/env/eprintln used; env(\"HEXA_LANG_INTERP\") real at 3474; tr -d idiom at 3546 verbatim). cmd_build(src,out,c_only,target,shared) 3014, __actual_src 3152 — scope vars valid.",
        "TRUE mechanism claims: native --emit=obj on darwin forks ZERO `as` (default-native flip compiler/main.hexa:508-512 → direct Mach-O write 977-985). cmd_build DOES bypass --linker=hexa (existing native path links via host_cc()/clang at 3503; --linker=hexa lives only in compiler/main.hexa:1164-1183). resolve_prebuilt_runtime returns a .a ARCHIVE (1691-1693) which hexa_ld cannot parse (parses .o only, header line 17) — wall claim TRUE. codesign in-tool 2372-2400, --lc-main _main default 43, F-P1-RUNEQ runtime-eq 31-36 — TRUE.",
        "DEFECT A (FALSE CLAIM — answers adversarial 'log gaps or silently emit?' = SILENTLY EMIT): the spec's fall-through-safety gate ('a program referencing unresolved runtime symbols MUST FAIL the hexa_ld link and fall through') is FALSE. hexa_ld hard-fails ONLY on an undefined ENTRY symbol (return 3, tool/hexa_ld.hexa:934-936). Every other unresolved symbol (BRANCH26 func refs 939-998, GOT_LOAD data refs 988-1029, patched 1386-1439) is BLINDLY appended to import_names and emitted as a libSystem.B.dylib ordinal-1 import with NO existence check, then codesigned + written with rc 0. The '// unresolved = link error' at line 473 is an aspirational RFC checklist comment, NOT the implemented Phase-H behavior. Consequence: a runtime-symbol-referencing program with no $HEXA_LD_RUNTIME_O returns rc 0 → EDIT 2's `test -x '_bltmp'`==yes → block prints 'OK: built ... no clang, no ld64', moves the binary to out, returns \"\" (NO fall-through) → ships a binary that crashes at launch (dyld symbol-not-found). This falsifies both the gate and the EDIT-2 comment 'ANY emit/link failure falls through (delegate intact)'.",
        "DEFECT B (EDIT 2 shell-quoting bug): `let _bllnk = \"'\" + _blld + \"' -o ...\"` single-quote-wraps the linker command, but resolve_hexa_ld branch (3) returns multi-word `hexa run '<inst>/tool/hexa_ld.hexa'` and branch (1) $HEXA_LD may be `hexa run tool/hexa_ld.hexa`. Wrapping a multi-word command in single quotes collapses it into one argv[0] with embedded spaces → 'command not found'. The cited reference compiler/main.hexa:1179 deliberately leaves _hexa_ld UNQUOTED for exactly this. Only branch (2) (bare `command -v hexa_ld` binary, single token) survives — and typical boxes have no standalone hexa_ld binary, so the common resolution (branches 1/3) is broken. FIX: mirror line 1179 — leave _blld unquoted (ld_cmd = _blld + \" -o ...\").",
        "MINOR (not blocking): hexa_ld arg parser (2446) accepts only .o-suffixed inputs + -o/--lc-main/--static; $HEXA_LD_RUNTIME_O must end in .o — satisfied by name, no change needed. NOTE (c)'s detect_default_target() reference is explanatory/non-load-bearing (EDIT passes --target=arm64-apple-darwin explicitly); the load-bearing 508-512→977 chain is verified TRUE.",
        "VERDICT: risk_class 'byte-neutral' is CORRECT for the DEFAULT path (block gated behind HEXA_BUILD_LINKER==\"hexa\", unset by default) and cited sites are all verbatim-correct, BUT edits_apply=false because (A) the fall-through-safety claim is materially FALSE against the real hexa_ld (silent-emit ships crashing binaries on the opt-in path — a release-integrity hazard) and (B) EDIT 2 has a real shell-quoting defect that breaks the common resolve_hexa_ld branches. Both must be fixed before applying."
      ],
      "edits": [
        "EDIT 1 — NEW HELPER fn resolve_hexa_ld() in self/main.hexa. Insert immediately AFTER resolve_native_cc()'s closing brace (line ~2666), BEFORE the '// S5: fail-loud when HEXA_BACKEND=native...' comment + fn die_no_native_cc() at 2667. Anchor (unique): the block 'let home_cc = exec(...) ... return \"\" }' followed by '// S5: fail-loud...' / 'fn die_no_native_cc() {'. Body mirrors compiler/main.hexa:1177 ($HEXA_LD else hexa_ld) plus leg-B resolver idioms: (1) let e = env(\"HEXA_LD\"); if len(e)>0 return e. (2) let p = exec(\"command -v hexa_ld 2>/dev/null | tr -d '\\\\n'\")  -- COPY the exact `tr -d '\\\\n'` idiom verbatim from self/main.hexa:3546; if len(p)>0 return p. (3) let inst = install_dir_from_argv0(); if len(inst)>0 && file_exists(inst + \"/tool/hexa_ld.hexa\") { let hx = env(\"HEXA_LANG_INTERP\"); let mut hxc = \"hexa\"; if len(hx)>0 { hxc = hx }; return hxc + \" run '\" + inst + \"/tool/hexa_ld.hexa'\" }. (4) return \"\". Empty return => caller skips the block and falls through to the C path (opt-in default-OFF, so a box without hexa_ld is unaffected).",
        "EDIT 2 — NEW OPT-IN BLOCK in cmd_build (self/main.hexa). Insert AFTER the linux leg-B block's closing (line 3577, the '    }' that closes the `if env_var(\"HEXA_BUILD_NATIVE\")==\"1\" && ...Linux...` block) and BEFORE line 3578 '    // Rider (axis ① measurement): resolve/bootstrap hexat HERE'. Anchor (unique): the two lines '        }\\n    }' at 3576-3577 immediately followed by '    // Rider (axis ① measurement): resolve/bootstrap hexat HERE (at its sole use below), NOT at'. New block (darwin-arm64 sibling of leg-B, LINK routed through hexa_ld):\n\n    // ── DONE-criterion axis ③ round-3 track B: `hexa build` darwin hexa_ld link (no clang, no ld64) ──\n    // HEXA_BUILD_LINKER=hexa (default-OFF, byte-neutral): on darwin-arm64, emit the object with the\n    // native Mach-O writer (aprime --emit=obj --target=arm64-apple-darwin, default-ON native flip →\n    // ZERO `as`) and link it via the in-tree tool/hexa_ld.hexa (#4677 --linker=hexa path) instead of\n    // forking ld64+xcrun. ANY emit/link failure falls through to the C path below (delegate intact;\n    // trace under HEXA_RUN_NATIVE_TRACE=1). darwin-arm64 only; refuses shared/--c-only/--target.\n    // HONEST SCOPE: hexa_ld parses Mach-O .o (not .a) and resolves libSystem leaves + co-linked .o\n    // symbols; default corpus = hexa_ld F-P1-RUNEQ {exit-42, fib} (runtime-symbol-light). A native\n    // single-object runtime .o may be co-linked via $HEXA_LD_RUNTIME_O; full runtime.a slurp = next round.\n    if env_var(\"HEXA_BUILD_LINKER\") == \"hexa\" && exec(\"uname -sm 2>/dev/null | tr -d ' \\\\n\\\\t'\") == \"Darwinarm64\" && shared != \"1\" && c_only != \"1\" && len(target) == 0 && len(__actual_src) > 0 {\n        let _blcc = resolve_native_cc()\n        let _blld = resolve_hexa_ld()\n        if len(_blcc) > 0 && len(_blld) > 0 {\n            let _ = exec(\"mkdir -p build/artifacts 2>/dev/null\")\n            let _blobj = \"build/artifacts/\" + basename_stem(out) + \".blobj.o\"\n            let _ble = exec(\"'\" + _blcc + \"' _drv.hexa --emit=obj --target=arm64-apple-darwin -o '\" + _blobj + \"' '\" + __actual_src + \"' 2>&1\")\n            let _blobj_ok = exec(\"test -s '\" + _blobj + \"' && printf yes || printf no\")\n            if _blobj_ok == \"yes\" {\n                let mut _blrt_arg = \"\"\n                let _blrt = env(\"HEXA_LD_RUNTIME_O\")\n                if len(_blrt) > 0 && file_exists(_blrt) { _blrt_arg = \" '\" + _blrt + \"'\" }\n                let _bltmp = exec(\"printf '%s.bltmp.%s' '\" + out + \"' \\\"$$\\\"\")\n                let _bllnk = \"'\" + _blld + \"' -o '\" + _bltmp + \"' '\" + _blobj + \"'\" + _blrt_arg + \" --lc-main _main 2>&1\"\n                let _bllr = exec(_bllnk)\n                let _blbin_ok = exec(\"test -x '\" + _bltmp + \"' && printf yes || printf no\")\n                if _blbin_ok == \"yes\" {\n                    let _ = exec(\"mv -f '\" + _bltmp + \"' '\" + out + \"' 2>/dev/null\")\n                    let _ = exec(\"rm -f '\" + _blobj + \"' 2>/dev/null\")\n                    if env_var(\"HEXA_RUN_NATIVE_TRACE\") == \"1\" { eprintln(\"[build-linker] clang-free link: aprime --emit=obj + hexa_ld → \" + out) }\n                    println(\"OK: built \" + out + \" (native backend, hexa_ld link — no clang, no ld64)\")\n                    return \"\"\n                } else if env_var(\"HEXA_RUN_NATIVE_TRACE\") == \"1\" { eprintln(\"[build-linker] hexa_ld link failed → C fallback: \" + _bllr) }\n            } else if env_var(\"HEXA_RUN_NATIVE_TRACE\") == \"1\" { eprintln(\"[build-linker] native-emit failed → C fallback: \" + _ble) }\n            let _ = exec(\"rm -f '\" + _blobj + \"' 2>/dev/null\")\n        }\n    }\n\nNOTES: (a) hexa_ld ad-hoc codesigns in-tool (tool/hexa_ld.hexa:2372-2403) so NO codesign_if_macos call is needed here. (b) The atomic .bltmp→out mv + `test -x` mirrors leg-B (3562-3568). (c) On darwin, detect_default_target()=arm64-apple-darwin so the explicit --target=arm64-apple-darwin is belt-and-suspenders that guarantees the native macho obj flip (compiler/main.hexa:508-512 → :977). (d) No default-path bytes change: the block is unreachable unless HEXA_BUILD_LINKER=hexa is set."
      ],
      "gate": [
        "byteeq 3-target NEUTRAL: with HEXA_BUILD_LINKER unset (default), cmd_build takes the identical existing path — no artifact bytes change on any of darwin-arm64 / x86_64-linux / arm64-linux. Verify by diffing default-build outputs pre/post-edit (must be bit-identical).",
        "darwin hexa_ld link measurement (ghost = darwin-arm64 selfhost host, or a darwin pod): build a smoke (exit-42) with `HEXA_BUILD_LINKER=hexa HEXA_RUN_NATIVE_TRACE=1 hexa build s.hexa -o s`, tracing execve with `sudo dtruss -f -t execve` (darwin's strace equiv). GATE: 0 execve of clang / ld / ld64 in the LINK step (aprime --emit=obj is native → 0 `as` too; hexa_ld is the only link tool). Binary runs, rc==42, and the trace shows the [build-linker] clang-free line.",
        "behavior parity: HEXA_BUILD_LINKER=hexa binary vs default (clang-linked) binary over the hexa_ld F-P1-RUNEQ corpus {exit-42, fib} — identical stdout + rc (Mach-O bytes may differ; runtime-equivalence is the contract per tool/hexa_ld.hexa header).",
        "fall-through safety: a program that references unresolved runtime symbols (no $HEXA_LD_RUNTIME_O) must FAIL the hexa_ld link and transparently fall through to a working C-path build (exit 0, correct binary) — the delegate stays intact.",
        "merge is measurement-gated: land only after byteeq-3-target GREEN + the darwin 0-clang/0-ld64 trace + install.sh consumer smoke; opt-in stays default-OFF until then."
      ],
      "wall": "Wall for THIS round = hexa_ld's runtime-symbol reach. hexa_ld links Mach-O .o only (not .a) and its `--lc-main _main` recipe resolves libSystem leaves + co-linked .o symbols — so the general corpus (programs referencing runtime.c hexa_* symbols) links cleanly ONLY when a native single-object runtime .o is co-linked (exposed now via $HEXA_LD_RUNTIME_O); the default smoke is the proven F-P1-RUNEQ {exit-42, fib} runtime-light corpus. resolve_prebuilt_runtime() returns a runtime.a ARCHIVE, which hexa_ld cannot parse today. NEXT ROUND (named): either (a) native-emit the darwin runtime as a single Mach-O .o and auto-resolve it here (drop the env escape), or (b) teach tool/hexa_ld.hexa to slurp runtime.a members — whichever lands makes the darwin hexa_ld path cover the full corpus, closing axis-③ for darwin-arm64 (obj+link both own-emitted, zero clang/as/ld64). The dynamic-link leaves (CUDA/dlopen/net) remain the deeper terminal track, unchanged by this round."
    },
    {
      "track": "axis-③ round-3 track C — hexa_ld darwin corpus parity/gap census (measurement + gap-log). Deliverable = a POOL/ghost(darwin-arm64)-only harness under test/native_build/ that builds a feature-spread corpus with --linker=hexa vs --linker=system, compares run stdout+rc, and enumerates the ordered round-4 encoder/fixup worklist; plus a gap-log edit to hexa_ld.hexa because it currently emits wrong bytes on an unhandled reloc SILENTLY (no log).",
      "ready": true,
      "risk_class": "test-infra",
      "edits_apply": true,
      "confidence": "high",
      "corrections": [
        "Non-blocking (model fidelity): regex_parity_run.sh only refuses *mini* via `case $(hostname)` (would still run on linux aiden/summer); File 1's stronger 'refuse unless darwin-arm64' is a correct deliberate tightening for a Mach-O-arm64 track, not a deviation error.",
        "Non-blocking (GATE-C impl detail): `otool -rv` emits reloc type NAMES (ARM64_RELOC_UNSIGNED, _BRANCH26, _PAGE21, _PAGEOFF12, _GOT_LOAD_*, _SUBTRACTOR, _POINTER_TO_GOT, _TLVP_LOAD_*) not the numeric kinds {0,2,3,4,5,6}/{1,7,8,9} the gate references — the harness must include a name->number map.",
        "Non-blocking (p10/GATE-C tension): of p10's three offered probers, only the TLS/TLVP (kind8/9) or difference-of-symbols/jump-table (kind1 SUBTRACTOR) variants satisfy GATE-C's required 'at least one of {1,7,8,9}'; the third option (non-libSystem-dylib call) emits kind5/6 GOT + BRANCH26 and would NOT satisfy GATE-C, so the author must pick the TLVP or SUBTRACTOR variant."
      ],
      "edits": [
        "HARNESS FILE 1 (new): test/native_build/hexa_ld_corpus_run.sh — driver, modeled 1:1 on regex_parity_run.sh. Host guard: refuse unless darwin-arm64 (ghost); refuse mini/linux (`case $(hostname)`); require `command -v hexa`, `otool`, `nm`. HEXA_LD default = \"hexa run $WT/tool/hexa_ld.hexa\" so the edited (gap-logging) source is exercised with NO separate hexa rebuild. WORK=$(mktemp -d); trap rm.",
        "HARNESS FILE 2 (new dir): test/native_build/hexa_ld_corpus/ with 10 .hexa programs, each a source-feature lever that induces a distinct reloc/symbol spread (mirrors regex corpus = features→behaviors): p01_write_hello (single println → the PROVEN `bl _write` baseline / known-GREEN anchor) · p02_multi_print (several distinct literals → many __cstring PAGE21/PAGEOFF12 kind3/4 + repeated runtime BRANCH26 kind2 import-dedup) · p03_int_fmt (println of computed ints → to_string runtime BRANCH26 + arith in __text) · p04_global_array (module-level array-of-strings iterated → kind0 UNSIGNED pointer-init → chained-rebase path) · p05_float_math (sqrt/abs + println → libm-family refs, still libSystem ord-1 on darwin, float regs) · p06_mut_global (`let mut` global mutated in loop → __bss COMMON sect=255) · p07_fn_calls (mutually-recursive user fns → internal BRANCH26 that MUST resolve internally, not become imports) · p08_big_cstring (large multiline string / many literals → adrp page-delta stress across a 4KB boundary) · p09_struct_copy (struct/array value copy → compiler-synth memcpy/memset small-copy) · p10_gap_prober (DELIBERATE gap: TLS/thread-local access → kind8/9 TLVP, OR a difference-of-two-symbols/jump-table → kind1 SUBTRACTOR, OR a non-libSystem-dylib call — the allowlisted-expected-gap program).",
        "HARNESS FILE 3 (new): test/native_build/hexa_ld_gap_allowlist.txt — known-today gap programs (starts with p10 + any program that MIS-LINKs on first measurement), one `progname reason` per line; the gate-3c reference (mirrors regex_intentional_divergence.txt). Output artifact hexa_ld_gap_worklist.txt is written to $WORK (gitignored/scratch), NOT committed.",
        "DRIVER PIPELINE (in file 1): (0) host+tool guard. (1) per program P: oracle build `hexa build P -o out_sys --linker=system --keep-temp` and candidate `HEXA_LD=... hexa build P -o out_hexa --linker=hexa --keep-temp`; capture build rc + FULL stderr (gap-log WARNs land here). (2) reloc census: `otool -rv` on the retained intermediate .o → reloc_coverage.txt recording which ARM64_RELOC_* kinds each P exercises. (3) run both binaries; capture stdout+rc (detect SIGSEGV/SIGKILL via rc>=128). (4) classify each P: SYS-FAIL (oracle build/run failed → corpus bug, hard `exit 2`) · LINK-FAIL (--linker=hexa produced no binary / hexa_ld rc!=0) · MIS-LINK (hexa binary produced but stdout!=sys OR rc!=sys OR crashed) · GREEN (identical stdout+rc). (5) emit ordered worklist: LINK-FAIL first (linker can't emit at all), then MIS-LINK grouped by the triggering reloc-kind/symbol → the round-4 encoder/fixup priority list.",
        "PASS/FAIL GATE (in file 1, `set -euo pipefail`, FAIL accumulator like regex): GATE-A parity — every program NOT in the allowlist MUST be GREEN; any non-allowlisted LINK-FAIL/MIS-LINK = RED (uncharted regression). GATE-B (gate-3c, stale-allowlist guard) — every allowlisted program MUST actually diverge (LINK-FAIL or MIS-LINK); an allowlisted entry that turns GREEN = RED (retire it from the allowlist). GATE-C coverage — reloc_coverage.txt must collectively show kinds {0,2,3,4,5,6} present AND at least one of {1,7,8,9} (from p10), else the corpus doesn't exercise the claimed spread = RED. GATE-D silent-miscompile — if any program is MIS-LINK but hexa_ld emitted ZERO gap-log WARN on its stderr, FLAG 'SILENT MISCOMPILE' RED (this is the exact finding the gap-log edit below closes; after the edit lands, every unhandled-reloc MIS-LINK must carry a WARN). Exit 0 iff A&B&C&D pass; worklist always written even on all-GREEN (census is the deliverable).",
        "GAP-LOG EDIT A (tool/hexa_ld.hexa:1488) — declare a counter before the reloc-apply loop. old: `    let mut pro = 0` → new: `    let mut unhandled_relocs = 0\\n    let mut pro = 0`. (`to_string`/`eprintln` already in-scope, used at :258.)",
        "GAP-LOG EDIT B (tool/hexa_ld.hexa:1672) — the `}` that closes `else if pr.kind == 5 || pr.kind == 6 {` (the 3rd of the three consecutive `}` before `pri = pri + 1` at :1673) → append an else arm catching every reloc kind NOT handled here and NOT handled by the separate BRANCH26/UNSIGNED passes: `} else if pr.kind != 0 && pr.kind != 2 { eprintln(\"hexa_ld: WARN unhandled reloc kind=\" + to_string(pr.kind) + \" sym='\" + pr.sym_name + \"' obj#\" + to_string(pro) + \" off=\" + to_string(pr.offset) + \" -> bytes left UNPATCHED (miscompile)\"); unhandled_relocs = unhandled_relocs + 1 }`. (Excludes kind0=DATA-rebase pass :1700+ and kind2=BRANCH26 stubs pass :1388+; kind10 already folded at parse :218.)",
        "GAP-LOG EDIT C (tool/hexa_ld.hexa:1561) — the `}` closing `if tgt_addr >= 0 {` inside the kind3/4 block → append `else { eprintln(\"hexa_ld: WARN kind\" + to_string(pr.kind) + \" target '\" + pr.sym_name + \"' UNRESOLVED (no internal slot; extern non-GOT ref unsupported) -> UNPATCHED\"); unhandled_relocs = unhandled_relocs + 1 }` so an unresolved internal/extern PAGE21/PAGEOFF12 target stops being a silent skip.",
        "GAP-LOG EDIT D (tool/hexa_ld.hexa:1676) — after the `}` closing `while pro < len(objs)` (the reloc-apply loop), insert a summary: `    if unhandled_relocs > 0 {\\n        eprintln(\"hexa_ld: ADVISORY \" + to_string(unhandled_relocs) + \" unhandled reloc(s) — output may be MISCOMPILED (see WARNs above)\")\\n    }`. (Advisory, not fatal: keeps hexa_ld emitting so the harness can still diff bytes and enumerate the gap; the WARNs make the miscompile visible to GATE-D. A future round may promote to hard-fail once the encoder is complete.)"
      ],
      "gate": [
        "darwin-arm64 (ghost) ONLY — refuse mini + linux (aiden/summer are linux; this is a Mach-O-arm64 track). Needs a full local hexa build present on PATH.",
        "Harness merge is docs/test-infra (gate-exempt per tool/CLAUDE.md 'docs-only tooling changes are gate-exempt'), but the gap-log edit touches tool/hexa_ld.hexa: verify it is not L0-lockdown (`/lockdown check tool/hexa_ld.hexa`); if L0, the same commit must append CHANGELOG.jsonl.",
        "The gap-log is stderr-only and byte-neutral to every emitted output binary AND only on the --linker=hexa default-OFF path — so no byteeq 3-target gate is triggered by it; still run selfhost_gates_summary once to confirm the darwin byteeq path is untouched.",
        "GATE-D self-check: after the gap-log lands, re-run the harness and confirm every MIS-LINK program now carries a matching WARN (no silent miscompile remains)."
      ],
      "wall": "n/a"
    }
  ]
}