{
  "ref_sha": "75739333f",
  "tracks": [
    {
      "track": "Axis-③ round-3 track B unblock: teach tool/hexa_ld.hexa to ingest a static ar(1) archive (.a) so `hexa build` can route through hexa_ld and consume runtime.a instead of feeding only individual Mach-O .o files.",
      "ready": true,
      "risk_class": "byte-neutral",
      "plan_holds": true,
      "confidence": "high",
      "corrections": [
        "Line-number nit: `return ParsedObj {` starts at :449 (closes ~:459), not :450-461 as cited; SITE-1 body is :256-460, not :256-461. Non-blocking.",
        "SITE-3 cites ':2446 input_objs.push(a)' but :2446 is the `else if ...\".o\"` CONDITION line; the actual push is :2447. The 'add an else if for .a' instruction still lands on the correct site.",
        "The linker signature at :597 names params `entry`/`_is_static`, not `lc_main_sym`/`is_static` as the scope's descriptive comment quotes; the signature shape `(objs: [ParsedObj], ...)` and N-object behavior are exactly as claimed."
      ],
      "current_state": [
        "tool/hexa_ld.hexa:254 — `pub fn parse_macho_obj(path: string) -> ParsedObj` slurps the WHOLE file with `read_file_bytes(path)` (:255) and parses everything file-offset-relative from 0: magic check `0xfeedfacf` at :262, cputype ARM64 `0x0100000c` at :267, LC pass :311-369, section/symtab capture, returns ParsedObj at :450-461. All reads are `_r_u32(buf, off)` with buf-relative offsets — there is no `base` parameter, so it can only parse a standalone .o that begins at byte 0.",
        "tool/hexa_ld.hexa:2409 main() — arg loop :2428-2454 accepts a token ONLY if it ends in `.o` (:2446 `input_objs.push(a)`); anything else (incl. `.a`) falls to the else at :2449-2452 → prints \"unknown arg\" + exit 1. Ingest loop :2465-2470 calls `parse_macho_obj` per path; link at :2471 `link_macho_arm64(parsed, out, lc_main, is_static)`.",
        "tool/hexa_ld.hexa:597 `link_macho_arm64(objs: [ParsedObj], ...)` already handles N objects: 5-slot section model (SLOT_TEXT..SLOT_BSS, N_SLOTS=5 :616-621), per-obj slot-base concat :670-705, Pass A defined-symbol union across all objs :1127, Pass B undefined-extern de-dup by name :1163-1171, inter-module BL/PAGE relocs :893+. So once archive members become ParsedObj entries, the existing linker consumes them unchanged.",
        "read_file_bytes is a builtin returning [Int] (used repo-wide: stdlib/safetensors.hexa:415, ckpt/verifier.hexa:73, etc.).",
        "grep for `!<arch>` / `SYMDEF` / `0x21` / `arch` across hexa_ld.hexa: NONE — zero archive-format handling exists today. This is the exact wall the round-3 verify hit."
      ],
      "round1": [
        "LEVER: pull-all-members ar(1) ingest. ar/BSD format is trivial (fixed 60-byte ASCII member headers), so this is ONE round, not a multi-round track.",
        "SITE 1 (byte-neutral refactor): split parse_macho_obj at :255 into `parse_macho_from_bytes(buf: [Int], label: string) -> ParsedObj` (body :256-461 verbatim, all offsets already buf-relative) + keep `parse_macho_obj(path)` as a 2-line wrapper (read_file_bytes → parse_macho_from_bytes). Existing .o output must stay byte-identical.",
        "SITE 2 (new): add `parse_archive(path) -> [ParsedObj]`: read_file_bytes; verify 8-byte magic `!<arch>\\n` (0x21,0x3c,0x61,0x72,0x63,0x68,0x3e,0x0a); walk members — each header is 60 bytes: size = ASCII-decimal at member+48 (10 bytes); data starts at member+60; handle BSD extended names `#1/<N>` (name field starts \"#1/\", advance data start by N, reduce effective size by N); slice member data into a fresh [Int]; if first 4 bytes == 0xfeedfacf → parse_macho_from_bytes and push (this magic-check elegantly SKIPS the __.SYMDEF symbol table + // string-table members without name parsing); advance to next header at data_start+size rounded up to even (2-byte pad).",
        "SITE 3 (main :2446): add an `else if` for tokens ending in `.a` → `parse_archive(a)` and append all returned ParsedObj into `parsed`; keep the `.o` branch and the ingest/link tail unchanged.",
        "MEASUREMENT: on the pool (aiden/summer), build two trivial .o (a.o entry calls into b.o), `ar rcs two.a a.o b.o`, then `hexa run tool/hexa_ld.hexa --static -o out two.a`; PASS = (a) `out` runs and prints the expected value, AND (b) `out` is byte-identical to `hexa_ld --static -o out a.o b.o` (the .o-direct link) — proves the archive path threads the same ParsedObj set. Then smoke the real target: link the actual runtime.a and confirm a working binary (that binary-produces-output check is the track-B unblock signal; wiring `hexa build` to route through it is round-2)."
      ],
      "gate": [
        "byteeq: existing .o-only link output must be byte-identical after the SITE-1 refactor (the refactor is pure extraction; verify gen3≡gen4 self-host fixpoint + a .o-link byte-diff both stay GREEN).",
        "2-member synthetic .a: hexa_ld output byte-identical to the equivalent .o-direct link AND runs with correct output.",
        "real runtime.a: hexa_ld consumes it and the linked binary runs (round-1 exit criterion; NOT yet `hexa build` rewiring).",
        "build/measure on pool only (mini = read/git/gh) per repo governance."
      ],
      "wall": "\"Beyond round-1: pull-ALL-members is correct but not true archive semantics — if runtime.a has multiple members DEFINING the same symbol (common in archives) or weak/common symbols, the linker's Pass A defined-union (:1127) has no documented duplicate-definition dedup/tie-break, so it may double-emit or mis-resolve. True symbol-table-driven pull-on-demand (parse __.SYMDEF, pull only members that resolve an outstanding undefined) is a deferred round. Also unknown until measured: whether every section type runtime.a members carry maps into the fixed 5-slot model (:616-621) — a member with an unmodeled section is a real wall. And GNU-ar long-name (`/NNN` → `//` string table) is unhandled by the magic-check-only filter, but macOS `ar`/`libtool` emit BSD format so this is not on the darwin path.\"",
      "reachable": "\"Round-1 (2-member synthetic .a links byte-eq to .o-direct + output runs) is reachable in a single round — ar/BSD format is a fixed 60-byte ASCII header the existing parse_macho_from_bytes consumes unchanged, and link_macho_arm64 is already N-object. The real runtime.a linking is the honest round-1 stretch goal and probably surfaces the duplicate-symbol / unmodeled-section wall above; that wall, if hit, is a measured round-2, not a substrate limit. `hexa build` DX rewiring stays round-2/track-B-proper regardless.\""
    },
    {
      "track": "axis-③ R2 — hexa_ld ELF static-exec linking: replace binutils `ld`+crt1.o on the x86_64-linux `--emit=exec`/`--linker=hexa` path with an in-tree ELF ET_EXEC writer, coupled to axis-② own-`_start` (default-ON) so no crt1.o/-lc/-dynamic-linker is needed. This is the sibling of the darwin `--linker=hexa` wiring that landed at #4677.",
      "ready": true,
      "risk_class": "bit-changing",
      "plan_holds": true,
      "confidence": "high",
      "corrections": [
        "MINOR (off-by/imprecise line, non-blocking): scope cites self/main.hexa:4551 as the leg-B own-start site, but :4551 is the native-run host-gate `if !file_exists(tmpbin) && _run_native_on`. The actual own-start default-ON flip on the native/leg-B link is self/main.hexa:4608 (`_zc_own = ... env_var(\"HEXA_ZEROC_OWN_START\") != \"0\"`), and the C-transpile leg is :1422. The cited line is in the correct block but not the precise flip line. The substrate claim (own-start default-ON) is nonetheless TRUE and confirmed at build_selfhost.sh:163 (`${HEXA_ZEROC_OWN_START:-1}`).",
        "MINOR (off-by-one): build_elf64_dyn_with_dynamic_reloc is at compiler/link/hexa_ld.hexa:1171, not :1170.",
        "MINOR (omission): tool/hexa_ld.hexa main() :2409 also parses a `--static` flag (sets is_static, threaded to link_macho_arm64) in addition to the cited -o/--lc-main/.o inputs. Does not affect the round-1 ELF-dispatch plan.",
        "MINOR (range precision): link_elf_x86_64's PC32/PLT32 reloc patch writes are at :824-829 (formula comment :815-823); scope's ':818-833' is a loose superset but correct in substance.",
        "MINOR (block bounds): the darwin `if linker_kind==\"hexa\"` arm is :1213-1227 (scope said :1215-1227); the enclosing block :1212-1231 and x86 warn-fallback :1228-1230 (warn at :1229) are exact."
      ],
      "current_state": [
        "TWO complementary ELF-link scaffolds exist, NEITHER driver-wired. (1) compiler/link/hexa_ld.hexa::link_elf(obj_path,out_path) at :1475 — reads a REAL on-disk ELF64 .o (is_elf64_le :315, find_sections :376, find_symbol_value :432), resolves entry `_start` then `main` fallback (:1497-1502), emits STATIC ET_EXEC via build_elf64_exec :545 / _build_elf64_image :481 (single PT_LOAD R+X, _LOAD_ADDR 0x400000 :469, _TEXT_VADDR 0x401000 :470). It is TEXT-ONLY and applies ZERO relocations (header comment :142/:151: 'Relocation processing beyond the trivial case' + '.rodata/.bss/.data — v1.x collapses everything to .text').",
        "(2) compiler/emit/elf_x86_64.hexa::link_elf_x86_64(objs,out_path,entry) at :736 — takes IN-MEMORY ElfX86Obj structs (not on-disk), concats text, DOES apply intra-text R_X86_64_PC32/PLT32 relocs (S+A-P, :818-833), emits via serialize_elf_exec_x86_64 :1115. BUT requires entry symbol at text offset 0 (returns rc=4 otherwise, :843-849) and its serializer emits a bare text-only PT_LOAD with NO .rodata/.data/.bss segment.",
        "tool/hexa_ld.hexa is the PRODUCTION CLI binary ($HEXA_LD) but is Mach-O-arm64-ONLY: main() at :2409 parses argv (-o / --lc-main / .o inputs) and calls ONLY parse_macho_obj:254 + link_macho_arm64:597; it hard-rejects non-Mach-O (grep: 2 ELF hits total). The ELF link logic lives in the SEPARATE compiler/link/hexa_ld.hexa module, which has NO main()/argv entry (it is a library, not a CLI).",
        "Driver wiring: compiler/main.hexa `if linker_kind == \"hexa\"` block (:1212-1231) is wired ONLY for arm64-apple-darwin (sets ld_cmd=$HEXA_LD ... --lc-main _main, #4677); the x86_64-linux arm just warns + falls back to system `ld` (:1229). The system x86_64 recipe (:1189-1195) forks binutils `ld` + /usr/lib/x86_64-linux-gnu/crt1.o + -lc + -dynamic-linker /lib64/ld-linux-x86-64.so.2.",
        "Axis-② own-`_start` (HEXA_ZEROC_OWN_START) is ALREADY default-ON on the Linux leg-B link (self/main.hexa:4551, build_selfhost.sh:163, opt-out=0 per done_axis23_next_rounds_plans.md:56/:104) — so a crt1.o-free / -lc-free static ELF exec is already achievable substrate-wise; only the linker itself is missing.",
        "The native x86_64 ELF .o EMITTER already lands a real ET_REL .o (pack_lir_x86_64 + serialize_elf_x86_64, #4676 default-flip), and tool/selfhost_crossemit_smoke.sh already builds three fixtures — module (B) 'exit7' TEXT-ONLY/zero-reloc, and module (C) 'start' write(1,str,3)+exit(7) needing R_X86_64_PC32 into .rodata — but links them with binutils `ld`/qemu, NOT hexa_ld."
      ],
      "round1": [
        "Lever: WIRE the existing link_elf scaffold end-to-end for a SINGLE-.o, ZERO-RELOC, text-only static exec — this is pure plumbing, NO new linker algorithm. It maps exactly onto link_elf's current capability (read .o -> find _start/main -> build_elf64_exec text-only PT_LOAD). Do NOT attempt hello-world-with-a-string in round-1: that needs .rodata + a text->rodata PC32 reloc, which link_elf's text-only collapse cannot do (that is round-2).",
        "Exact gate program = the crossemit-smoke module-(B) 'exit7' fixture: a freestanding `_start` (mov eax,60; mov edi,7; syscall) — zero relocations, zero .rodata, single .o. Under axis-② own-`_start` (default-ON) it needs no crt1.o/-lc/-dynamic-linker.",
        "Edit site A (CLI entry): give tool/hexa_ld.hexa's main() (:2409) an ELF-magic dispatch — read magic of input_objs[0]; if is_elf64_le, run the ELF static-exec path instead of parse_macho_obj/link_macho_arm64. Since the ELF logic (link_elf/build_elf64_exec/find_sections/is_elf64_le, ~120 LOC self-contained) lives in the sibling compiler/link/hexa_ld.hexa, either import it or port those ~120 lines into tool/hexa_ld.hexa (the two files share the name; a shared module is cleaner but the port is bounded).",
        "Edit site B (driver): in compiler/main.hexa's `if linker_kind == \"hexa\"` block, add an `else if target == \"x86_64-linux-gnu\"` arm mirroring the darwin #4677 shape (:1215-1227) — set ld_cmd = $HEXA_LD -o OUT OBJ.o (ELF auto-detected by magic), replacing the warn-fallback at :1229. Ship default-OFF/opt-in (--linker=hexa), exactly like darwin, so byteeq stays neutral until a later flip.",
        "Measurement (on aiden/summer, linux-x86_64 — mini is git/gh only): build module-B via the native emitter (#4676 toggle ON) to a real ELF .o, link it with `hexa build --linker=hexa` (0 `ld` fork), then run: assert `echo $?`==7; `readelf -l OUT` shows PT_LOAD only (NO PT_INTERP); strace -f -e trace=execve shows 0 `as` AND 0 `ld` execve. Capture the trace + readelf as the pass artifact."
      ],
      "gate": [
        "A native x86_64 ELF .o (crossemit module-B exit7: text-only, zero-reloc, single-obj) links to a RUNNING static ET_EXEC via hexa_ld with 0 binutils invocations: strace execve count of both `as` and `ld` == 0, process exit code == 7.",
        "readelf -l on the produced binary shows a single PT_LOAD (R+X) and NO PT_INTERP / no DT_NEEDED — proving crt1.o-free / dynamic-linker-free static output, consistent with axis-② own-`_start` default-ON.",
        "--linker=hexa remains opt-in/default-OFF (mirrors darwin #4677); byteeq 3-target stays GREEN on the untouched default path, so the merge is byte-neutral (the flip to default-ON is a separate later round gated on the multi-section+reloc rounds landing)."
      ],
      "wall": "[\"IN-TRACK (round-2) wall, NOT substrate: real programs (and crossemit module-C write+string) need .rodata + a text->rodata R_X86_64_PC32 reloc. link_elf's text-only collapse (compiler/link/hexa_ld.hexa:142/:151) and serialize_elf_exec_x86_64's single-segment layout are the wall — round-2 must add a multi-section PT_LOAD serializer (.text/.rodata/.data/.bss with distinct vaddrs) + apply the 3 reloc kinds. The pieces already exist split across the two scaffolds (link_elf's on-disk reader + link_elf_x86_64's PC32/PLT32 reloc engine + serialize_elf_arm64's multi-section template); round-2/3 MERGES them. Multi-obj (program.o + runtime.a members) with R_X86_64_PLT32 call resolution is round-3.\", \"SUBSTRATE wall (the genuine deep leaf): DYNAMIC ELF linking — PT_INTERP + DT_NEEDED + PLT/GOT + .dynsym/.dynstr/.hash + glibc symbol versioning — required only by the libcuda.so / dlopen / networking variants. compiler/link/hexa_ld.hexa already carries an ET_DYN scaffold (build_elf64_dyn_with_dynamic_reloc :1170, GOT-family classifier :806) but it is explicitly 'NOT YET LOADABLE' for the consumer path (G7-C/D punted). This mirrors the axis-② sanctioned-floor: dlopen/dlsym (nobaseline-gate.yml S1 HARD GATE) + net-FFI + CUDA opt-in are the permanent irreducible leaves — they gate ONLY the dynamic/plugin/CUDA variants, never the base static `hexa` binary.\"]",
      "reachable": "A full STATIC ELF x86_64 linker is REACHABLE as a bounded multi-round track (no measured substrate wall): round-1 wires link_elf for exit-N text-only; round-2 adds multi-section PT_LOAD + R_X86_64_PC32/R_X86_64_64 (rodata/data refs); round-3 adds R_X86_64_PLT32 multi-obj resolution (program.o + runtime.a) — all three reloc formulas (S+A-P / S+A / L+A-P) and the section-layout template already exist in scaffold across the two files, coupled to axis-②'s already-default-ON own-`_start` (no crt1.o/-lc/-dynamic-linker). The base static binary reaches zero-external-tool on all 3 targets. The one genuinely deep leaf is DYNAMIC ELF (PT_INTERP/DT_NEEDED/PLT/GOT/.dynsym/symbol-versioning), needed only for libcuda.so/dlopen/networking — big engineering volume, not proven infeasible (an ET_DYN scaffold already exists), but it is the honest terminal risk and, like the axis-② sanctioned floor, may become the named sanctioned exception rather than a full own dynamic-linker."
    },
    {
      "track": "axis-① HX1102/HX1103 native-emit codegen-gap drain — round 2: implement literal_bool match-arm pattern lowering (the single dominant HX1102 punt) so hexa build's native path + hexa run leg-B stop aborting-to-C-transpile on boolean `match` arms.",
      "ready": true,
      "risk_class": "byte-neutral",
      "plan_holds": true,
      "confidence": "high",
      "corrections": [
        "Immaterial (non-blocking): the WALL section cites the char-expr _const_str_op arm at hir_to_mir.hexa:3204, but the actual literal_char `return _value(ctx, _const_str_op(e.text))` is at :3200 (within the 3193-3200 block). This is a speculative next-round reference, not a round-1 edit site, so it does not affect the plan."
      ],
      "current_state": [
        "compiler/lower/hir_to_mir.hexa:5502-5738 — the match-arm pattern dispatch. Handled shapes: wildcard(:5553), ident(:5564), enum_path(:5575), literal_int(:5624), literal_string(:5655), struct_pattern(:5687). The trailing else(:5702-5738) emits HX1102 via _emit_hx1102(pat.kind,...) at :5707 for EVERY other pattern shape, then does a defensive operand-== recovery whose MIR is never emitted (see next).",
        "compiler/main.hexa:849-861 — HX1102 is drained from _lr_diag and, being Severity::Error (compiler/diag/catalog.hexa:160-162), triggers `exit(1)` before codegen unless --ignore-errors. So an HX1102 in any fn ABORTS the native compile → the hexa build/run driver falls back to the self/native/hexa_cc.c C-transpile delegate. The else-branch's recovery body at :5708-5737 is dead-in-practice.",
        "Match patterns are lowered by ast_to_hir.hexa::_lower_pattern_inner(:1720-1725) → for non-struct patterns just _lower_expr, so pat.kind is the ordinary expr kind: a boolean arm `true`/`false` becomes pat.kind==\"literal_bool\", pat.text==\"true\"|\"false\"; char→\"literal_char\", float→\"literal_float\".",
        "RESIDUAL HX1102 shapes (fire the punt): literal_bool, literal_char, literal_float, range/range_inclusive, or-/tuple-patterns. Real-code frequency (grep compiler+stdlib+self): literal_bool = 14 genuine match arms (all in self/test_*.hexa e.g. test_match_enum.hexa:75-76, test_parser_full.hexa:548-549), literal_char = 0, literal_float = 0. → literal_bool is the sole HX1102 shape that actually fires on real code.",
        "HX1103 default fall-through at hir_to_mir.hexa:6247-6249 (`if !_is_known_hexpr_kind(k) { _emit_hx1103(k,...) }`). The dispatch (branches :3042-6242) + known set (:2963-2992) cover every kind ast_to_hir emits: `for` is desugared to `while` in ast_to_hir.hexa:2330 and ranges are consumed there or become binop-kind. array_lit is fully handled at :6023 but merely absent from the advisory _is_known list (not a punt). → HX1103 has NO active real-code trigger today; its named targets (growable-array methods, for/yield desugars) are never emitted. It is a defensive sink, not the round-2 lever.",
        "Helpers confirmed present: _const_bool_op(v:i64) at :98, _const_int_op at :79, mirroring the literal_int arm at :5624-5654 which is the exact template."
      ],
      "round1": [
        "Add ONE `else if pat.kind == \"literal_bool\"` branch to the match-arm dispatch chain in compiler/lower/hir_to_mir.hexa, inserted after the literal_string branch (ends :5686 `ctx2 = ctx3`) and before `} else if pat.kind == \"struct_pattern\"` (:5687).",
        "Body mirrors the literal_int branch (:5624-5654) exactly, substituting the operand constructor: `let mut v: i64 = 0; if pat.text == \"true\" { v = 1 }` then STMT_BINOP `==` of scrut_op vs `_const_bool_op(v)` into a fresh `lit_eq` local, then STMT_BR_COND (args=[cmp_dst], target_block=arm_id, target_block_else=next_id), then _add_edge to arm_id and next_id. No new helpers, no signature changes.",
        "Net effect: a boolean `match` arm now lowers to a native tag-equality branch instead of emitting HX1102 → no Error → no exit(1) abort → the native source→own-IR→native path completes without the C-transpile fallback."
      ],
      "gate": [
        "byteeq 3-target GREEN (x86_64-linux · arm64-linux · darwin-arm64) — the new else-if is only reachable for literal_bool patterns, which today ABORT the native compile via HX1102, so every existing pattern kind keeps byte-identical MIR (no baseline exists to violate for bool arms).",
        "Native-path fallback-count drop: compile a bool-match fixture (e.g. self/test_match_enum.hexa) through the native build path and confirm HX1102 emissions for its bool arms drop from N→0 and the compile no longer exit(1)-aborts to C-transpile; repo-wide the 14 self/test_*.hexa bool-arm sites go 14→0 HX1102.",
        "shipping smoke: hexa build/run of a bool-match program produces a correct native binary (arm→body on true, else→next), not empty/wrong MIR."
      ],
      "wall": "\"After literal_bool: the remaining HX1102 shapes (literal_char via _const_str_op like the char-expr arm at :3204; literal_float; range/or/tuple patterns) have ZERO real-code frequency today — they are speculative, not next-round drivers. HX1103 has no active trigger at all (all ast_to_hir kinds are dispatched; for-desugar + array_lit already handled). So this round exhausts the *measured* HX1102 punt on real code; the honest next lever after this is not more pattern kinds but confirming whether any OTHER Error-severity lower diagnostic (HX1101 unbound, HX1104 closure) is what actually forces C-fallback on the self-host corpus.\"",
      "reachable": "\"The census + edit are fully doable on mini (read-only). The gate is NOT reachable on mini: byteeq 3-target + the native-path fallback-count measurement must run on the pool (aiden/summer/ghost) per CLAUDE.md (mini = git/gh/read/write only, no heavy build). The lowering itself is small and self-contained; the only substrate wall is that verification requires a pool build or a PR-CI run.\""
    }
  ]
}