{
  "ref_sha": "30b3b199b",
  "items": [
    {
      "item": "hexa_ld ELF x86_64 round-1: link a single native x86_64 .o into a running static ET_EXEC (exit-code-only) via the in-tree ELF linker, replacing binutils ld — coupled to axis-② own-start (14-byte _start, no crt1.o/-lc/-dynamic-linker).",
      "ready": true,
      "risk_class": "byte-neutral",
      "holds": true,
      "byte_arithmetic_ok": true,
      "confidence": "high",
      "corrections": [],
      "edits": [
        "EDIT A — compiler/emit/elf_x86_64.hexa — INSERT a new pub fn link_elf_x86_64_ownstart between the close of link_elf_x86_64 (line 851 `}`) and the `serialize_elf_x86_64 — P2 cycle 1` comment (line 853). Unique anchor.\n\nBEFORE (verbatim, lines 848-855):\n    let bytes = serialize_elf_exec_x86_64(text_bytes)\n    write_bytes(out_path, bytes)\n    return 0\n}\n\n// serialize_elf_x86_64 — P2 cycle 1: empty-object byte stream.\n// Layout: header → .text → .symtab → .strtab → .shstrtab → section\n// header table (5 entries: NULL, .text, .symtab, .strtab, .shstrtab).\n\nAFTER (verbatim):\n    let bytes = serialize_elf_exec_x86_64(text_bytes)\n    write_bytes(out_path, bytes)\n    return 0\n}\n\n// ────────────────────────────────────────────────\n// link_elf_x86_64_ownstart — axis-② own-start ELF static-exec link\n// ────────────────────────────────────────────────\n//\n// Round-1 (exit-code-only) own-start wrapper over link_elf_x86_64's\n// body. Prepends a 14-byte `_start` stub at text offset 0 so the ET_EXEC\n// needs NO crt1.o / -lc / -dynamic-linker: the kernel enters at _start,\n// which calls `main`, forwards main's return (EAX) into EDI and issues\n// the exit syscall directly.\n//\n// _start (14 bytes = 5+2+5+2):\n//   E8 00 00 00 00   call  main          ; rel32 patched below (offs 1..4)\n//   89 C7            mov   edi, eax       ; exit code = main() ret\n//   B8 3C 00 00 00   mov   eax, 60        ; __NR_exit\n//   0F 05            syscall\n//\n// Every obj symbol.value and reloc.offset is shifted by len(stub)=14\n// because the text concat begins with `base = len(text_bytes) = 14` (the\n// stub occupies [0,14)). PC-relative reloc deltas (S+A−P) are invariant\n// under the shared +14 shift, so the reloc loop is byte-identical to\n// link_elf_x86_64. Only the intra-stub `call main` rel32 is resolved\n// here: rel32 = (main final offset) − (next-insn addr = 5); for main at\n// obj-local value 0 that is 14 − 5 = 9.\n//\n// Text-only (round-1): serialize_elf_exec_x86_64 emits a single PT_LOAD;\n// only programs with ZERO data refs link correctly (see round-2 wall).\npub fn link_elf_x86_64_ownstart(objs: [ElfX86Obj], out_path: string, main_name: string) -> Int {\n    // 14-byte own-start stub at text offset 0.\n    let mut text_bytes: [Int] = []\n    text_bytes.push(0xE8); text_bytes.push(0x00); text_bytes.push(0x00)\n    text_bytes.push(0x00); text_bytes.push(0x00)          // call rel32 (patched)\n    text_bytes.push(0x89); text_bytes.push(0xC7)          // mov edi, eax\n    text_bytes.push(0xB8); text_bytes.push(0x3C); text_bytes.push(0x00)\n    text_bytes.push(0x00); text_bytes.push(0x00)          // mov eax, 60 (__NR_exit)\n    text_bytes.push(0x0F); text_bytes.push(0x05)          // syscall\n    // len(text_bytes) is now 14 — the +14 shift applied to every obj below.\n\n    let mut main_off = -1\n    let mut def_names:   [string] = []\n    let mut def_offsets: [Int]    = []\n    let mut obj_bases:   [Int]    = []\n\n    let mut oi = 0\n    while oi < len(objs) {\n        let obj = objs[oi]\n        let base = len(text_bytes)        // >= 14 — the own-start shift\n        obj_bases.push(base)\n        let mut di = 0\n        while di < len(obj.symbols) {\n            let s = obj.symbols[di]\n            let mut name = \"\"\n            let mut k = 0\n            while true {\n                let b = obj.strtab[s.name_offset + k] & 0xff\n                if b == 0 { break }\n                name = name + from_char_code(b)\n                k = k + 1\n            }\n            if s.section != 0 {\n                def_names.push(name)\n                def_offsets.push(base + s.value)\n                if main_off < 0 && name == main_name {\n                    main_off = base + s.value\n                }\n            }\n            di = di + 1\n        }\n        let mut ti = 0\n        while ti < len(obj.text) {\n            text_bytes.push(obj.text[ti] & 0xff)\n            ti = ti + 1\n        }\n        oi = oi + 1\n    }\n    if main_off < 0 {\n        eprintln(\"hexa_ld(elf,ownstart): entry '\" + main_name + \"' not defined in any obj\")\n        return 3\n    }\n\n    // Patch the `call main` rel32 (stub bytes 1..4). Next-insn addr = 5.\n    let call_rel = main_off - 5\n    text_bytes[1] = call_rel & 0xff\n    text_bytes[2] = (call_rel >> 8) & 0xff\n    text_bytes[3] = (call_rel >> 16) & 0xff\n    text_bytes[4] = (call_rel >> 24) & 0xff\n\n    // Resolve inter/intra-obj PLT32/PC32 relocs. The shared +14 shift\n    // cancels in S+A−P, so this is identical to link_elf_x86_64.\n    let mut ro = 0\n    while ro < len(objs) {\n        let obj = objs[ro]\n        let base = obj_bases[ro]\n        let mut ri = 0\n        while ri < len(obj.relocs) {\n            let r = obj.relocs[ri]\n            let sym = obj.symbols[r.sym_idx]\n            let mut name = \"\"\n            let mut k = 0\n            while true {\n                let b = obj.strtab[sym.name_offset + k] & 0xff\n                if b == 0 { break }\n                name = name + from_char_code(b)\n                k = k + 1\n            }\n            let mut found_idx = -1\n            let mut sk = 0\n            while sk < len(def_names) {\n                if def_names[sk] == name { found_idx = sk; break }\n                sk = sk + 1\n            }\n            if found_idx >= 0 {\n                let tgt_off = def_offsets[found_idx]\n                let patch_off = base + r.offset\n                let delta = tgt_off + r.addend - patch_off\n                text_bytes[patch_off + 0] = delta & 0xff\n                text_bytes[patch_off + 1] = (delta >> 8) & 0xff\n                text_bytes[patch_off + 2] = (delta >> 16) & 0xff\n                text_bytes[patch_off + 3] = (delta >> 24) & 0xff\n            }\n            ri = ri + 1\n        }\n        ro = ro + 1\n    }\n\n    // Entry is the stub at text offset 0 (serialize_elf_exec_x86_64 sets\n    // e_entry = vaddr_base + code_off where code_off is the first text byte).\n    let bytes = serialize_elf_exec_x86_64(text_bytes)\n    write_bytes(out_path, bytes)\n    return 0\n}\n\n// serialize_elf_x86_64 — P2 cycle 1: empty-object byte stream.\n// Layout: header → .text → .symtab → .strtab → .shstrtab → section\n// header table (5 entries: NULL, .text, .symtab, .strtab, .shstrtab).",
        "EDIT B1 — compiler/main.hexa — add module-level holder vars for the captured native ElfX86Obj immediately AFTER the native_obj_ready declaration (line 812). Unique anchor.\n\nBEFORE (verbatim, lines 811-813):\nlet mut native_obj_bytes: [Int] = []\nlet mut native_obj_ready = false\nif stream_mode && target == \"arm64-apple-darwin\" {\n\nAFTER (verbatim):\nlet mut native_obj_bytes: [Int] = []\nlet mut native_obj_ready = false\n// axis-② own-start ELF static-exec link (opt-in --linker=hexa on\n// x86_64-linux): capture the native ElfX86Obj so the hexa-linker dispatch\n// below can link it in-process with a synthesized 14-byte _start — no\n// crt1.o / -lc / -dynamic-linker. A 0-or-1 element holder array avoids\n// needing a struct zero-value; it stays empty (dormant, byte-neutral) for\n// every non-native / non-hexa-linker build.\nlet mut elf_obj_x86_holder: [ElfX86Obj] = []\nlet mut elf_obj_x86_ready = false\nif stream_mode && target == \"arm64-apple-darwin\" {",
        "EDIT B2 — compiler/main.hexa — in the native x86-64 exec branch, capture the ElfX86Obj into the holder when serializing for the exec link. Anchor is the exec `else` sub-branch at lines 1056-1058.\n\nBEFORE (verbatim, lines 1056-1058):\n        } else {\n            native_obj_bytes = elf_bytes\n            native_obj_ready = true\n\nAFTER (verbatim):\n        } else {\n            native_obj_bytes = elf_bytes\n            native_obj_ready = true\n            elf_obj_x86_holder.push(elf_obj)\n            elf_obj_x86_ready = true",
        "EDIT B3 — compiler/main.hexa — wire the in-process ELF own-start link into the linker_kind==\"hexa\" dispatch, BEFORE the arm64-apple-darwin Mach-O arm (mirrors the Mach-O tool/hexa_ld.hexa dispatch pattern). Converts the arm64 `if` into an `else if`. Anchor lines 1212-1213.\n\nBEFORE (verbatim, lines 1212-1213):\nif linker_kind == \"hexa\" {\n    if target == \"arm64-apple-darwin\" {\n\nAFTER (verbatim):\nif linker_kind == \"hexa\" {\n    if target == \"x86_64-linux-gnu\" && elf_obj_x86_ready {\n        // axis-② own-start ELF static-exec link (round-1, opt-in): link the\n        // native ElfX86Obj in-process via the in-tree ELF linker with a\n        // synthesized 14-byte _start — NO crt1.o / -lc / -dynamic-linker and\n        // ZERO `ld`/`as` execve. Exits before the shared system-ld exec\n        // below. Round-1 = exit-code-only (serialize_elf_exec_x86_64 is\n        // text-only; a print(\\\"..\\\")-with-string data ref is the round-2 wall).\n        let rc = link_elf_x86_64_ownstart(elf_obj_x86_holder, out_path, \"main\")\n        if rc != 0 {\n            eprintln(\"hexa-compiler: hexa_ld(elf) own-start link failed rc=\" + to_string(rc))\n            exit(2)\n        }\n        if verbose {\n            eprintln(\"hexa-compiler: linked static ELF ET_EXEC via in-tree hexa_ld \"\n                     + \"(own-start _start, no crt1.o/-lc/-dynamic-linker) to \" + out_path)\n        }\n        exit(0)\n    } else if target == \"arm64-apple-darwin\" {"
      ],
      "gate": [
        "Native x86_64 .o (pack_lir_x86_64 output) links to a running static ET_EXEC via in-tree link_elf_x86_64_ownstart, with 0 binutils `ld` AND 0 `as` execve (strace -f -qq -e trace=execve shows zero execve of `as` or `ld`), no crt1.o / no -lc / no -dynamic-linker — coupled to the synthesized 14-byte own `_start` (axis-② own-start on the native-emit path).",
        "Concretely GREEN on pool aiden/summer with hexa built >= 30b3b199b (NOT the stale installed ~/.hx binary): `fn main()->int{return 42}` -> `hexa build --backend=native --linker=hexa --target=x86_64-linux-gnu t.hexa -o t` -> (1) `file t` == ELF 64-bit LSB executable, statically linked; (2) `./t; echo $?` == 42 on real x86_64-linux; (3) strace shows zero as/ld forks.",
        "byteeq / release-integrity: the DEFAULT shipping path (no --linker=hexa, backend=system) is byte-identical — the new fn has zero production callers on that path and the holder stays empty; run the 3-target byteeq (x86_64-linux · arm64-linux · darwin-arm64) gen3≡gen4 fixpoint to confirm untouched.",
        "FALSIFIER to check FIRST (round-1b trigger): confirm `return 42` from main emits NO builtin CALL (no hexa_add/box) so text is self-contained with 0 UNDEF externs — objdump/nm the pack_lir_x86_64 .o for undefined symbols. If any UNDEF exists, narrow the round-1 target to a body the encoder fully self-contains, and runtime-.o multi-obj linking (own-emitted runtime as an ELF .o) becomes an explicit round-1b dependency before this gate can go green.",
        "14-byte-stub verification (the load-bearing correction): objdump -d the produced ET_EXEC and confirm _start disassembles to exactly E8 <rel32> / 89 C7 / B8 3C 00 00 00 / 0F 05 (14 bytes), the call rel32 == main_final_offset − 5 (== 9 when main is at obj-local value 0), and e_entry == 0x400000 + 120 (code_off). A +13 shift would place main at the 0x05 byte of syscall and skew every reloc by one — reject if the stub is not 14 bytes."
      ],
      "wall": "Round-1 is exit-code-only (zero data refs). WALL / named round-2 = hello-world WITH a string: serialize_elf_exec_x86_64 (elf_x86_64.hexa:1115) is text-only — a single PT_LOAD, no .rodata/.data/.bss segment — and link_elf_x86_64's PC32 data-reloc math (elf_x86_64.hexa:814-829, mirrored in link_elf_x86_64_ownstart) computes S as a flat text offset (base + s.value), a single-segment assumption, so any print(\\\"..\\\") lea rip->.rodata resolves to garbage. Round-2 = multi-segment static-exec layout: add .rodata/.data/.bss PT_LOAD segments to the exec serializer at distinct page-aligned vaddrs + fix PC32 relocs to compute S against each section's real segment vaddr (not a flat text offset). Round-1b sub-dependency if `main` calls any runtime builtin (FALSIFIER above): own-emit the native runtime as an ELF .o and multi-obj-link it (UNDEF externs -> real defs), which also pulls in the emitted-C own-start (HEXA_ZEROC_OWN_START/_hx_start_c) coupling. Deep wall (only gates CUDA/dlopen/net variants, not the base static binary) = dynamic ELF output (PT_INTERP, DT_NEEDED, PLT/GOT, symbol versioning)."
    },
    {
      "item": "Scope the POOL measurement harness for hexa_ld ELF x86_64 round-1: a native x86_64 .o (own backend) links to a RUNNING static ET_EXEC via the in-tree ELF own-start linker with 0 binutils. Two NEW test-infra files under test/native_build/ (regex_parity_run.sh idiom), pool-only (aiden/summer, x86_64-linux; mini refuses). CORRECTION to task framing: the round-1 ELF link surface is the IN-DRIVER `hexa build --backend=native --linker=hexa` path over compiler/emit/elf_x86_64.hexa, NOT `hexa run tool/hexa_ld.hexa` — tool/hexa_ld.hexa is Mach-O arm64 ONLY (verified: 0 elf/x86_64/link_elf refs in it). Own-start stub = 14 bytes (E8 00000000[5] + 89 C7[2] + B8 3C000000[5] + 0F 05[2]); shift +14; call-main rel32 = main_value-5.",
      "ready": true,
      "risk_class": "test-infra",
      "holds": true,
      "byte_arithmetic_ok": true,
      "confidence": "high",
      "corrections": [],
      "edits": [
        "NEW FILE test/native_build/elf_x86_ownstart_exit.hexa (the trivial exit-code program; round-1 target is ZERO data refs + zero runtime-builtin CALL so .text is self-contained, 0 UND externs):\n----\n// test/native_build/elf_x86_ownstart_exit.hexa\n// HEXA-OWN axis-3 . ELF x86_64 round-1 -- trivial exit-code program.\n// Compiled to a native x86_64 ELF .o via the OWN backend (pack_lir_x86_64 +\n// serialize_elf_x86_64, compiler/main.hexa native ELF-x86 branch #4676), then\n// linked to a static ET_EXEC via the in-tree ELF own-start linker\n// (link_elf_x86_64_ownstart, compiler/emit/elf_x86_64.hexa) with 0 binutils.\n// Round-1 = ZERO data refs + zero runtime-builtin CALL so .text is\n// self-contained (0 UND externs); the own 14-byte _start forwards main's\n// return as the exit code. Expected: ./elf_x86_ownstart_exit -> rc 42.\nfn main() -> int {\n    return 42\n}",
        "NEW FILE test/native_build/elf_x86_ownstart_run.sh (pool runner, chmod +x; follows regex_parity_run.sh idiom):\n----\n#!/usr/bin/env bash\n# test/native_build/elf_x86_ownstart_run.sh\n# HEXA-OWN axis-3 . ELF x86_64 round-1 measurement harness.\n# GATE: a native x86_64 .o (own backend) links to a RUNNING static ET_EXEC via\n# the in-tree ELF own-start linker with 0 binutils (as/ld). POOL ONLY\n# (aiden/summer -- x86_64-linux; NOT mini = git/gh only). Needs hexa built at\n# >= 30b3b199b PLUS EDIT A (link_elf_x86_64_ownstart, 14-byte _start stub in\n# compiler/emit/elf_x86_64.hexa) + EDIT B (main.hexa x86_64-linux --linker=hexa\n# arm before the arm64-darwin one @1212). Until A+B land, STEP-2 falls back to\n# system ld (main.hexa:1189) and the strace gate goes RED -- that RED is the\n# pre-implementation baseline this harness pins.\nset -euo pipefail\nHERE=\"$(cd \"$(dirname \"$0\")\" && pwd)\"\nHEXA=\"${HEXA:-hexa}\"\nTGT=\"x86_64-linux-gnu\"\nWORK=\"$(mktemp -d)\"; trap 'rm -rf \"$WORK\"' EXIT\n\ncase \"$(hostname)\" in\n  *mini*) echo \"REFUSE: mini is git/gh only -- run on aiden/summer (x86_64-linux)\"; exit 2 ;;\nesac\n[ \"$(uname -m)\" = \"x86_64\" ] || { echo \"REFUSE: round-1 is x86_64-linux only (host=$(uname -m))\"; exit 2; }\ncommand -v \"$HEXA\" >/dev/null || { echo \"REFUSE: no hexa on PATH (pool build host required)\"; exit 2; }\nfor t in readelf file strace nm; do command -v \"$t\" >/dev/null || { echo \"REFUSE: missing $t\"; exit 2; }; done\n\nSRC=\"$HERE/elf_x86_ownstart_exit.hexa\"; OBJ=\"$WORK/t.o\"; EXE=\"$WORK/t\"\n\n# -- STEP 1: native x86_64 ELF .o via the OWN backend (exists today, #4676) --\n# --backend=native --emit=obj skips the host `as` fork; writes ELF REL bytes.\n\"$HEXA\" build --backend=native --emit=obj --target=\"$TGT\" \"$SRC\" -o \"$OBJ\"\nfile \"$OBJ\" | grep -q 'ELF 64-bit.*x86-64.*relocatable' || { echo \"FAIL(1): .o not ELF x86-64 REL\"; file \"$OBJ\"; exit 1; }\nreadelf -h \"$OBJ\" | grep -qE 'Type:[[:space:]]+REL' || { echo \"FAIL(1): obj e_type != ET_REL\"; exit 1; }\n# FALSIFIER PRE-CHECK (spec gate point 4): round-1 requires a self-contained\n# .text = 0 UNDEF externs. If `return 42` pulls a runtime builtin (hexa_box/\n# hexa_add), round-1 narrows and runtime-.o multi-obj link becomes round-1b.\nUND=\"$(nm -u \"$OBJ\" 2>/dev/null | grep -vE '^[[:space:]]*$' | wc -l | tr -d ' ')\"\nif [ \"$UND\" != \"0\" ]; then\n  echo \"STOP(1b): .o carries $UND UNDEF extern(s) -- not self-contained; round-1b runtime-obj dependency:\"; nm -u \"$OBJ\"; exit 3\nfi\necho \"PASS(1): self-contained ELF x86-64 .o, 0 UND externs\"\n\n# -- STEP 2: link to static ET_EXEC via the in-tree ELF own-start linker --\n# --linker=hexa routes the exec link through link_elf_x86_64_ownstart:\n# synthesize the 14-byte own _start at text offset 0, shift every symbol.value\n# + reloc.offset by +14, resolve `call main` rel32 to main_value-5, then\n# serialize_elf_exec_x86_64. strace-wrap the whole build+link (feeds STEP 4).\nstrace -f -qq -e trace=execve -o \"$WORK/strace.txt\" \\\n  \"$HEXA\" build --backend=native --linker=hexa --emit=exec --target=\"$TGT\" \"$SRC\" -o \"$EXE\"\n[ -x \"$EXE\" ] || { echo \"FAIL(2): no executable produced\"; exit 1; }\n\n# -- STEP 4: prove 0 binutils (zero as/ld fork in the build+link) --\nif grep -E 'execve\\(\"[^\"]*/(as|ld)\"' \"$WORK/strace.txt\" >/dev/null; then\n  echo \"FAIL(4): binutils forked (as/ld execve seen):\"; grep -E 'execve\\(\"[^\"]*/(as|ld)\"' \"$WORK/strace.txt\"; exit 1\nfi\necho \"PASS(4): 0 binutils -- no as/ld execve in build+link\"\n\n# -- STEP 5: ET_EXEC static assertion --\nfile \"$EXE\" | grep -q 'statically linked' || { echo \"FAIL(5): not statically linked\"; file \"$EXE\"; exit 1; }\nreadelf -h \"$EXE\" | grep -qE 'Type:[[:space:]]+EXEC' || { echo \"FAIL(5): e_type != ET_EXEC\"; readelf -h \"$EXE\" | grep Type; exit 1; }\nif readelf -l \"$EXE\" | grep -qE 'INTERP|DYNAMIC'; then echo \"FAIL(5): has PT_INTERP/PT_DYNAMIC -- not a bare static exec\"; exit 1; fi\necho \"PASS(5): ET_EXEC, statically linked, no PT_INTERP/PT_DYNAMIC\"\n\n# -- STEP 3: run + assert exit code --\nset +e; \"$EXE\"; RC=$?; set -e\n[ \"$RC\" = 42 ] || { echo \"FAIL(3): exit code $RC != 42\"; exit 1; }\necho \"PASS(3): ./t exited 42\"\n\necho \"ELF x86_64 round-1 GATE GREEN -- own .o -> in-tree own-start ELF link -> running static ET_EXEC(42), 0 binutils\"",
        "RECIPE STEP 0 (host): run ONLY on aiden or summer (x86_64-linux) with a freshly-built hexa at >= 30b3b199b on PATH (NOT a stale ~/.hx installed binary; `hexa --version` + confirm the build sha). mini refuses (git/gh only). Build hexa on the pool per build_selfhost / install.sh, do NOT use a stale-pool hexat.",
        "RECIPE STEP 1 (own .o, exists today #4676): `hexa build --backend=native --emit=obj --target=x86_64-linux-gnu elf_x86_ownstart_exit.hexa -o t.o` (equivalently `hexa run compiler/main.hexa --backend=native --emit=obj --target=x86_64-linux-gnu ... `). This hits main.hexa's native ELF-x86 branch (pack_lir_x86_64 + serialize_elf_x86_64), NO `as` fork. Assert `file t.o`=ELF x86-64 relocatable, `readelf -h`=Type REL, and the FALSIFIER pre-check `nm -u t.o`=0 UND (self-contained). UND>0 => STOP: round-1b runtime-obj multi-obj-link dependency, record symbols.",
        "RECIPE STEP 2 (in-driver ELF own-start link; needs EDIT A+B): `hexa build --backend=native --linker=hexa --emit=exec --target=x86_64-linux-gnu elf_x86_ownstart_exit.hexa -o t`. Routes through link_elf_x86_64_ownstart (14-byte _start @ offset 0, shift +14, call-main rel32=main_value-5, serialize_elf_exec_x86_64). Wrap the whole invocation in `strace -f -qq -e trace=execve -o strace.txt` to feed STEP 4.",
        "RECIPE STEP 3 (run + assert exit): `./t; echo $?` must equal 42 (main's return forwarded by _start's `mov edi,eax` before `mov eax,60; syscall`).",
        "RECIPE STEP 4 (prove 0 binutils): `grep -E 'execve\\(\"[^\"]*/(as|ld)\"' strace.txt` must be EMPTY (zero as/ld fork in the entire build+link). Belt-and-suspenders which-ld-absent variant: re-run STEP 2 with PATH stripped of binutils (`PATH=/nonexistent-binutils:$PATH` after removing /usr/bin/ld,as from PATH dirs) and require the build still succeeds. Also assert the emitted binary has no PT_INTERP (below).",
        "RECIPE STEP 5 (ET_EXEC static assertion): `file t`=`ELF 64-bit LSB executable, x86-64, statically linked`; `readelf -h t` Type=EXEC (serialize_elf_exec_x86_64 stamps e_type=ET_EXEC, e_entry=0x400000+120); `readelf -l t` = single PT_LOAD, NO PT_INTERP, NO PT_DYNAMIC.",
        "PLACEMENT: both files go in test/native_build/ (same dir as regex_parity_run.sh, the regex corpus runner idiom). The runner is pool-only and self-refuses on mini/non-x86_64. Do NOT reuse compiler/test/macho_p0_corpus/run_F_P2_*.hexa — those ssh to ubu-2 and drive the ELF primitives with hand-built synthetic ElfX86Obj byte arrays, NOT the real pack_lir_x86_64 output from a compiled .hexa program; this harness exercises the full own-backend -> own-linker driver path."
      ],
      "gate": [
        "Native x86_64 .o from the own backend (pack_lir_x86_64 + serialize_elf_x86_64, `hexa build --backend=native --emit=obj --target=x86_64-linux-gnu`) is ELF ET_REL x86-64 with 0 UNDEF externs (self-contained .text for the exit-code program).",
        "That .o links to a static ET_EXEC via in-tree link_elf_x86_64_ownstart (`--linker=hexa --emit=exec`) with a synthesized 14-byte own _start at text offset 0 (no crt1.o / no -lc / no dynamic-linker).",
        "The linked binary runs on real x86_64-linux and `./t; echo $?` == 42.",
        "strace -f -e trace=execve over the whole build+link shows ZERO execve of any `.../as` or `.../ld` (0 binutils), corroborated by a which-ld-absent PATH-stripped re-run that still succeeds.",
        "file/readelf assert ET_EXEC + statically linked + single PT_LOAD, no PT_INTERP and no PT_DYNAMIC."
      ],
      "wall": "The harness GATE cannot go GREEN until EDIT A (link_elf_x86_64_ownstart with the 14-byte _start stub, shift +14, call-main rel32=main_value-5, in compiler/emit/elf_x86_64.hexa) + EDIT B (x86_64-linux-gnu arm in the main.hexa:1212 linker_kind==\\\"hexa\\\" block, before the arm64-darwin one) land — today x86_64-linux-gnu --linker=hexa warns and falls back to system ld (main.hexa:1189 crt1.o -lc), so STEP-2/4 are RED (that RED is the pre-impl baseline the harness pins). Round-1b sub-dependency: if the FALSIFIER pre-check finds STEP-1 UND>0 (return 42 pulls a runtime builtin), the target narrows and the native runtime must be own-emitted as an ELF .o + multi-obj-linked (real defs for the UND externs), pulling in the emitted-C own-start coupling. Named round-2 = hello-world WITH a string: serialize_elf_exec_x86_64 is text-only and link_elf_x86_64's PC32 data-reloc math (elf_x86_64.hexa:814-829) uses a single-segment text-offset assumption, so any print lea rip->.rodata resolves to garbage — round-2 adds .rodata/.data/.bss PT_LOAD segments at page-aligned vaddrs + fixes PC32 against each section's real segment vaddr. Deep wall = dynamic ELF (PT_INTERP/DT_NEEDED/PLT-GOT), only gating CUDA/dlopen/net variants, not the base static binary."
    }
  ]
}