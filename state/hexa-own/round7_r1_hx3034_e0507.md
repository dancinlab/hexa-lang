# round7 R1 — HX3034 E0507 move-out-of-borrowed-content (HEXA-OWN L5-E9)

Branch: `fix/hexa-own-r7-r1-hx3034-e0507` (off origin/main `0657b2204`).
Spec: `state/hexa-own/round7_dual_lane_batch_plan.md` §3 R1.

## What
New opt-in borrowck diagnostic **HX3034** (Rust **E0507** — cannot move out of borrowed content).
Fires when an `@own` call argument is a field access `r.f` whose base `r` is a LIVE `&`/`&mut`
reference binding: moving content out from behind a reference is forbidden.

## Files changed
- `compiler/lower/hir_to_mir.hexa`
  - (a) emitter `_bck_emit_move_out_of_borrow(name,field,origin,borrow_mut,borrow_line,sp)` — cloned
    from `_bck_emit_move_while_borrowed`; shared same-site dedup (`_bck_e_lines`/`_bck_e_cols`) +
    STRICT Error re-band. Inserted after `_bck_emit_move_while_borrowed` (post :994).
  - check branch — sibling of the call-arm ident branch (after it closes, ~:2651). Gates:
    `_bck_active && !_bk_is_method && kind=="field" && len(children)>0 && children[0].kind=="ident"`,
    then `_bck_own_param(callee_text, i-1) && _mir_lookup_global<0` and
    `_bck_find_ref(base) >= 0 && len(_bck_ref_origins[ri]) > 0` (LIVE loan). MUTABILITY-BLIND
    (`&`/`&mut` both fire). Report-only (no `_bck_note_move`).
- `compiler/diag/catalog.hexa` — HX3034 complete DiagSpec block inserted between HX3033 and HX4001.
  Severity::Warning · stage "S3" · `fix_it_kind: FixItKind::None`. Parity **78/78**
  (`grep -c 'DiagSpec {' == grep -c 'fix_it_kind:'`).
- `compiler/check/borrowck_test.hexa` — `_build_mob_module(binding, ref_base)` / `_run_mob(...)` +
  4 cases wired after the mut-method probes:
  1. `hz_move_out_shared_borrow` (shared, r) → HX3034 ×1
  2. `hz_move_out_mut_borrow` (mut, r) → HX3034 ×1, HX3027 ×0 (standalone — no preemption)
  3. `fp_move_out_plain_copy` (`let r = a`, r) → 0 (non-ref binding)
  4. `fp_move_out_owner_base` (`g(a.f)`, a) → 0 (owner-direct base)
  Each: stray-diag == 0 · OFF sweep 0 · STRICT error-band == want · `len(mm.funcs) == 2`.

## Boundaries (conservative)
single-ident base only (nested `r.a.b` silent) · same-fn loan · killed loan silent (`origin==""`) ·
global base skipped · corpus adoption 0 → fires on ZERO real source (HX3029 posture).

## byteeq neutrality
All paths gated on `_bck_active` (default OFF); flag-OFF never reaches the branch/emitter. ON is
report-only (diag push only, MIR unchanged). corpus.yml census regex UNCHANGED (borrowck codes are
not in it).

## Verify
summer, 3-mode `hexa run compiler/check/borrowck_test.hexa`: OFF / `HEXA_BORROWCK=1` /
`HEXA_BORROWCK_STRICT=1`.
