{
  "ref_sha": "c62ea8d38",
  "items": [
    {
      "item": "EXACT link-and-run measurement harness for the ELF x86_64 own-start linker (rounds 1+2), pool-only (aiden/summer x86_64-linux), REAL-verifying that a native own-backend .o links via --linker=hexa into a running static ET_EXEC with 0 binutils.",
      "ready": false,
      "risk_class": "test-infra",
      "holds": false,
      "confidence": "high",
      "corrections": [
        "DEFECT 1 (CERTAIN, reproduced): run.sh hello_str stdout gate FALSE-FAILs even on a correct round-2 run. run_case is called `run_case hello_str ... 0 $'hello\\n'`, so exp_out (line 64 `exp_out=\"$4\"`) retains the trailing newline (6 bytes hello\\n). But line 133 `got=\"$(cat \"$out\")\"` uses command substitution, which strips ALL trailing newlines -> got=\"hello\" (5 bytes). The compare at line 134 `[ \"$got\" = \"$exp_out\" ]` is then `[ hello = hello<NL> ]` -> NEVER equal -> STEP6-run-stdout FAILs -> TALLY shows a failure -> `GATE RED` even when the executable emits exactly \"hello\\n\". The round-2 data-segment proof (the harness's central deliverable) can therefore never report GREEN. Fix: strip the newline from both sides (e.g. `exp_out=\"${4%$'\\n'}\"`) or byte-compare against the file (e.g. `printf '%s' \"$exp_out\" | cmp -s - \"$out\"`).",
        "DEFECT 2 (PLAUSIBLE): run.sh STEP1 line 73 `file \"$obj\" | grep -qE 'ELF 64-bit.*x86-64.*relocatable'` has a word-order bug. GNU file prints the ELF *type* before the *machine*: `ELF 64-bit LSB relocatable, x86-64, version 1 (SYSV), not stripped` -> 'relocatable' appears BEFORE 'x86-64'. The regex requires 'x86-64' then 'relocatable' (in that order), which cannot match -> the `file` half of the `&&` at line 73-74 is false -> STEP1 false-FAILs for BOTH programs at the very first gate. The `readelf -h ... Type: REL` half is the authoritative check. Fix: reorder to `'ELF 64-bit.*relocatable.*x86-64'` (or `.*(x86-64.*relocatable|relocatable.*x86-64)`), or demote the `file` check to informational exactly like STEP5 does for the exec. (Could not exec `file` on a real x86-64 .o from this darwin host; verdict from GNU file's canonical type-before-machine ordering.)",
        "NON-BLOCKING (stale comment, not a functional defect): main.hexa:1228-1229 dispatch comment says 'Round-1 = exit-code-only (serialize_elf_exec_x86_64 is text-only; a print(\"..\")-with-string data ref is the round-2 wall)'. This is STALE — the merged link_elf_x86_64_ownstart body (elf_x86_64.hexa:987 has_data, :1061-1065) already branches to serialize_elf_exec_x86_64_2seg when data is present, so round-2 IS wired and hello_str will link correctly. The comment misleads a reader but does not affect behavior; the harness's claim that hello_str exercises the 2-PT_LOAD path is correct."
      ],
      "files_or_edits_or_findings": [
        "/Users/mini/dancinlab/hexa-lang/.worktrees/wf-elf3/test/native_build/elf_ownstart/exit_code.hexa — COMPLETE CONTENTS:\n// test/native_build/elf_ownstart/exit_code.hexa\n//\n// HEXA-OWN axis-③ · ELF x86_64 own-start linker ROUND-1 target (exit-code-only).\n//\n// Compiled to a native x86_64 ELF .o via the OWN backend (pack_lir_x86_64 +\n// serialize_elf_x86_64, compiler/main.hexa native ELF-x86 branch), then linked\n// to a static ET_EXEC via the in-tree ELF own-start linker\n// (link_elf_x86_64_ownstart, compiler/emit/elf_x86_64.hexa) with 0 binutils.\n//\n// ROUND-1 invariant proven by this file: ZERO data refs + zero runtime-builtin\n// CALL, so `.text` is fully self-contained (0 UNDEF externs). The synthesized\n// 14-byte own `_start` at text offset 0 (call main; mov edi,eax; mov eax,60;\n// syscall) forwards main's return (EAX) as the process exit code — no crt1.o,\n// no -lc, no -dynamic-linker.\n//\n// Expected end-to-end:  ./exit_code ; echo $?   ->   42\nfn main() -> int {\n    return 42\n}",
        "/Users/mini/dancinlab/hexa-lang/.worktrees/wf-elf3/test/native_build/elf_ownstart/hello_str.hexa — COMPLETE CONTENTS (prints a string via raw write(2), NOT println — println would pull a runtime UND extern = round-1b, not the round-2 data-segment gate):\nfn main() -> int {\n    __hx_syscall6(1, 1, __hx_str_ptr(\"hello\\n\"), 6, 0, 0, 0)\n    return 0\n}\n// __NR_write=1, fd=1, buf=&.LCstr0(\"hello\\n\") via self-contained RIP-relative lea+R_X86_64_PC32 into .rodata (x86_64_linux.hexa:2831), count=6. Self-contained (0 UND), exercises round-2 second RW PT_LOAD + true-vaddr reloc. Full 40-line header comment in the written file.",
        "/Users/mini/dancinlab/hexa-lang/.worktrees/wf-elf3/test/native_build/elf_ownstart/run.sh — COMPLETE, chmod +x, bash -n clean. Pool-only tally harness (refuses on mini/non-linux/non-x86_64; requires readelf/file/strace/nm + hexa>=c62ea8d38). Per program (exit_code expect rc=42 stdout empty; hello_str expect rc=0 stdout 'hello\\n'): STEP1 own-backend `hexa build --backend=native --emit=obj --target=x86_64-linux-gnu` -> assert file/readelf ELF x86-64 ET_REL; STEP2 FALSIFIER `nm -u`==0 UNDEF (else STOP=round-1b, not a false FAIL); STEP3 `hexa build --backend=native --linker=hexa --emit=exec --target=x86_64-linux-gnu` under `strace -f -qq -e trace=execve -o log`; STEP4 assert 0 binutils via regex execve(\\\"[^\\\"]*/(as|ld|ld\\.[A-Za-z0-9]+|gcc|g++|cc|clang|clang++|collect2)\\\"); STEP5 readelf -h Type=EXEC + readelf -l has NO INTERP/DYNAMIC (file(1) statically-linked is informational since exec is section-header-less); STEP6 run + assert exit code and exact stdout. Prints per-gate PASS/FAIL trace + final 'TALLY: N passed, M failed' and GATE GREEN/RED (exit 0/1).",
        "FINDING (load-bearing): print()/println() will NOT link-and-run under rounds 1+2 alone — it lowers to a CALL of a runtime print/write symbol = UNDEF extern in the .o (round-1b multi-obj-link dependency). Verified in compiler/codegen/x86_64_linux.hexa that raw-syscall intrinsics __hx_syscall6 + __hx_str_ptr are implemented and emit an inline `syscall` (no runtime symbol), and that a bare string literal materializes as a self-contained RIP-relative lea into .rodata with R_X86_64_PC32 (line 2831). Hence hello_str uses raw write(2) to stay self-contained (0 UND) while genuinely exercising the round-2 .rodata data segment + PC32 reloc + serialize_elf_exec_x86_64_2seg two-PT_LOAD path.",
        "FINDING: main.hexa dispatch confirmed — the x86_64-linux --linker=hexa own-start arm (calls link_elf_x86_64_ownstart(elf_obj_x86_holder, out_path, \"main\")) fires only when backend_kind==native && target==x86_64-linux-gnu && elf_obj_x86_ready (set in the native exec branch ~line 1063). The harness passes --backend=native --linker=hexa --emit=exec --target=x86_64-linux-gnu, matching that gate. Program entry MUST be `fn main()` (linker resolves symbol name \"main\") — both test programs use fn main; NOT the top-level-statement smoke_exit_42.hexa idiom."
      ],
      "gate": [
        "exit_code.hexa: own-backend `--emit=obj` yields ELF x86-64 ET_REL with `nm -u`==0 UNDEF; `--linker=hexa --emit=exec` produces an ET_EXEC (readelf Type=EXEC, no PT_INTERP, no PT_DYNAMIC); ./exit_code exits 42; strace over the whole build+link shows 0 execve of any /as or /ld (nor cc/gcc/clang/collect2).",
        "hello_str.hexa: own-backend `--emit=obj` yields ELF x86-64 ET_REL with `nm -u`==0 UNDEF (self-contained via raw syscall, no runtime print symbol); `--linker=hexa --emit=exec` produces a 2-PT_LOAD static ET_EXEC (readelf Type=EXEC, no PT_INTERP/PT_DYNAMIC); running it writes exactly 'hello\\n' to stdout and exits 0; 0 binutils execve.",
        "run.sh prints a per-gate PASS/FAIL trace and a final tally; exits 0 only when both programs pass all gates (round-1 exit + round-2 data segment), else 1.",
        "Environment gate: pool-only — refuses on mini / non-linux / non-x86_64 / missing tools; requires a freshly-built hexa >= c62ea8d38 on PATH (stale ~/.hx falls back to system ld and makes STEP3/4 RED = the pre-round-1/2 baseline)."
      ],
      "wall": "This harness cannot be executed on mini (git/gh only; self-refuses) — it must run on aiden/summer x86_64-linux with hexa built >= c62ea8d38, so it is authored-and-staged but NOT yet green-measured here. Named next walls it pins: (1) if STEP2 `nm -u`>0 for either program, that program hit the round-1b runtime-.o multi-obj-link dependency (a runtime builtin leaked into .text) — the harness STOPs on it rather than false-FAILing, and round-1b (own-emit the runtime as an ELF .o + multi-obj link) becomes the prerequisite. (2) hello_str is the round-2 data-segment proof via a self-contained raw write(2); the literal `println(\\\"hello\\\")` form is deliberately NOT used because it pulls a runtime print/write UND (round-1b), which is a different gate. (3) round-3 walls (per round-2 spec): .bss symbol vaddr mapping, multiple-data-object colliding-local-name resolution, data->data absolute relocs, and dynamic ELF (PT_INTERP/GOT/PLT) — none exercised by these two static self-contained programs. Also: `file(1)` may not print 'statically linked' on a section-header-less exec (e_shoff=0), so readelf is the authoritative static assertion (file is informational)."
    },
    {
      "item": "Adversarial re-review of round-2 data-segment ELF code (serialize_elf_exec_x86_64_2seg + link_elf_x86_64_ownstart data path) at c62ea8d38 for a real correctness bug that would crash/mis-run a linked binary.",
      "ready": true,
      "risk_class": "review",
      "holds": true,
      "confidence": "high",
      "corrections": [
        "HEAD confirmed == c62ea8d38 (git rev-parse). All 6 dismissed suspicions re-verified against the actual code and are correctly NO-BUG; the LATENT multi-obj STB_LOCAL name-collision finding is real-but-unreachable, premise confirmed: main.hexa:1067 pushes exactly one ElfX86Obj to elf_obj_x86_holder (pack_lir_x86_64(lmodule) at :1055 is whole-program->one-obj), so link_elf_x86_64_ownstart always receives length-1. No reachable bug was missed and no finding is a false-positive.",
        "Minor citation/file mislabel (non-substantive): the review prose attributes several sites to 'codegen/x86_64_linux.hexa' but they actually live in compiler/emit/elf_x86_64.hexa -- data_offsets.push(cur_off+3) is emit/elf_x86_64.hexa:1739 (not codegen:1739); the PC32 data-reloc addend -4 is emit/elf_x86_64.hexa:2013; rodata_sec=3 / data_sec=has_rodata?4:3 is emit/elf_x86_64.hexa:1815-1816. The pack_lir_x86_64 packer+serializer are in emit/, not codegen/. The '.LCstr'+to_string(n) naming citation at codegen/x86_64_linux.hexa:5794 IS correct.",
        "End-to-end hello_str.hexa trace confirmed: test is __hx_syscall6(1,1,__hx_str_ptr(\"hello\\n\"),6,0,0,0), self-contained (0 UNDEF externs, inline 0F05 syscall), exercising exactly the PC32-into-.rodata reloc the round-2 second RW PT_LOAD resolves. 7-byte lea rip-rel with disp32 at +3 (emit:1734-1740), addend -4, linker delta = s_vaddr - 4 - p_vaddr(disp32) = target - next_insn_pc = correct RIP displacement.",
        "Concur with the review's WALL: rounds 1+2 have NEVER been link-and-run measured; the real next gate is an actual --linker=hexa link + RUN of the ET_EXEC on aiden/summer x86_64-linux (assert stdout=='hello\\n', exit 0). The flat by-name LOCAL-scope reloc resolver (elf_x86_64.hexa:1016-1021) must be per-obj-qualified before round-1b multi-obj runtime-.o linking lands, else silent data-cross miscompile."
      ],
      "files_or_edits_or_findings": [
        "REACHABLE PATH CLEAN: The round-2 data path is correct for the only reachable program shape (single object; main.hexa:1055 compiles the whole program to one LModule -> one ElfX86Obj, so elf_obj_x86_holder always has length 1). End-to-end trace of test/native_build/elf_ownstart/hello_str.hexa (write(1,&\".LCstr0\",6)) resolves correctly: .LCstr0 (section=3) -> data-seg def_off=0, s_vaddr=data_vaddr_base, PC32 delta=s_vaddr-4-p_vaddr = correct RIP disp into .rodata; call main rel32 = 14-5 = 9.",
        "Suspicion (1) p_vaddr/p_offset congruence: NO BUG. Linker (compiler/emit/elf_x86_64.hexa:990-994) and serializer (:1408-1414) compute identical data_file_off = text_end_file + ((0x1000-(text_end_file&0xfff))&0xfff) and data_vaddr=0x400000+data_file_off. Uniform 0x400000+file_off map => p_vaddr-p_offset=0x400000 (page multiple) for ANY text size; page-round makes seg0 rounded-page end == seg1 start, no overlap even when text_end already page-aligned.",
        "Suspicion (2) data symbol vs wrong segment base: NO BUG. s_vaddr=data_vaddr_base+def_off (:1028), data_bytes serialized at file offset data_file_off (:1465-1469), so data_bytes[j] sits at data_vaddr_base+j. def_off computed per-obj via ro_base/da_base into the concatenated blob correctly.",
        "Suspicion (3) section-classification edge (>=4=.data, ==3=.rodata at :955-959): NO BUG. Codegen hardcodes the SAME indices it stamps into symbols (:1815-1816 rodata_sec=3, data_sec = has_rodata?4:3). The only case a .data symbol carries section 3 is when there is no rodata, and there ro_base==da_base so ro_base+value==da_base+value. Consistent in every has_rodata/has_data combination; the nrelocs-dependent serializer index (:1117) does not affect symbol.section because codegen hardcodes 3/4.",
        "Suspicion (4) p_filesz vs p_memsz for .bss: NO BUG. seg1 p_filesz=data_size, p_memsz=data_size+bss_size (:1456-1457). Kernel zero-fills [filesz,memsz). Note: obj.bss_size is never set by this codegen (stays 0), so bss is not actually exercised.",
        "Suspicion (5) e_entry/e_phoff/e_phnum at phnum=2: NO BUG. e_entry=0x400000+176 (stub at text off 0 -> file off 176), e_phoff=64, e_phnum=2 (:1429/1430/1435). Entry lands on the 14-byte own-start stub; call main rel32=main_off-5 correct.",
        "Suspicion (6) R_X86_64_64 byte width: NO BUG. kind==1 writes 8 bytes (:1037-1044), PC32/PLT32 writes 4 (:1048-1051). Codegen currently emits NO kind=1 reloc (only PC32/PLT32), so that arm is dead code but still correct. Data PC32: data_offsets.push(cur_off+3) (:1739) points at disp32 of 7-byte lea, addend -4 (:2013) => end-to-end RIP math correct.",
        "LATENT (PLAUSIBLE, NOT reachable today) — flat by-name reloc resolution ignores STB_LOCAL scope: link_elf_x86_64_ownstart resolves each reloc by a global first-match name scan over def_names (compiler/emit/elf_x86_64.hexa:1016-1021), disregarding that round-2 data symbols .LCstr<N> and g<id> are per-module STB_LOCAL with module-local numbering (codegen/x86_64_linux.hexa:5794 '.LCstr'+to_string(n); globals 'g'+id). Concrete failing shape (once multi-obj linking / round-1b runtime-.o link lands): two objects A and B each defining a local .LCstr0 (or g0); B's 'lea rN,[rip+.LCstr0]' PC32 reloc first-matches A's .LCstr0 -> B prints A's string / reads A's global -> silent wrong output. UNREACHABLE at c62ea8d38 because main.hexa:1055 always passes exactly one obj, so I cannot exhibit a currently-crashing program; flagged because round-1's own comments name multi-obj runtime-.o link as the next gate, at which point this becomes a live miscompile. Fix: qualify local .LCstr/g names per-obj (or scope the name search to the reloc's own object for LOCAL-bind symbols)."
      ],
      "gate": [
        "review-only: no build/link/run executed (mini is git/gh/read-only per CLAUDE.md; x86_64-linux link+run is aiden/summer)",
        "byteeq not re-run (round-2 already passed byteeq per task; this pass is source-level adversarial review)",
        "the LATENT finding needs a multi-obj link+run on x86_64-linux to demonstrate; not reproducible under current single-obj dispatch"
      ],
      "wall": "Reachable single-object round-2 data path is correct on all 6 suspicion classes (verified end-to-end incl. hello_str.hexa). NEXT ROUND (real gate, still unmeasured): actually link hello_str.o/exit_code.o via --linker=hexa on aiden/summer x86_64-linux and RUN the ET_EXEC (assert stdout=='hello\\\\n' and exit 0) — rounds 1+2 have NEVER been link-and-run measured. Before round-1b multi-obj runtime-.o linking lands, fix the flat-name LOCAL-scope reloc resolver (per-obj-qualify .LCstr/g names or scope local-symbol resolution to the reloc's own obj), else the round-1b multi-obj link is a silent data-cross miscompile."
    },
    {
      "item": "ELF own-start linker round-3 spec: MULTI-OBJECT DATA (per-obj local-symbol scoping). Of {.bss-only, multi-obj-data, GOT/dynamic}, multi-obj-data is the smallest tractable round WITH A REAL CONSUMER. Rationale grounded in the codegen: pack_lir_x86_64 hardcodes bss_size=0 and puts every global in .data as a zeroed 16B slot, so NO real program emits .bss (case a is a codegen-gated filler with 0 consumers) and NO program emits a data->data R_X86_64_64 (case c unreachable); GOT/PLT/PT_INTERP is the sanctioned CUDA/dlopen/net floor (deferred). Multi-obj-data is on the critical path: the round-2 END-TO-END gate form (b) print(\\\"hello\\\") itself REQUIRES linking the own-emitted runtime .o beside the user .o, and any multi-module program hits it. The defect: link_elf_x86_64_ownstart resolves relocs through a name-keyed FIRST-match global def_names map; each obj emits LOCAL symbols .LCstr0/g0/... from 0, so a second data-carrying obj's reloc against ITS .LCstr0 cross-resolves to the first obj's copy (wrong data). Fix = per-obj local scoping.",
      "ready": true,
      "risk_class": "byte-neutral",
      "holds": true,
      "confidence": "high",
      "corrections": [
        "HEAD confirmed == c62ea8d38. OLD-text anchor (elf_x86_64.hexa lines 1016-1029) matches verbatim incl. 12-space indent.",
        "NEW-code section dispatch (==1 text / >=4 data / else rodata) is provably identical to the existing def_off computation in the setup loop (lines 940-959), including the no-rodata edge case where obj_rodata_base[ro]==obj_data_base[ro]; all cited symbols (sym.section/.value from struct lines 78-80, obj_bases/obj_rodata_base/obj_data_base populated lines 908-926, base at line 1003) are in scope. Semantic equivalence and byte-neutrality (single-obj + zero-reloc) both hold.",
        "Bug is REAL and correctly diagnosed: g<id> data globals (line 1855) and .LCstrN (line 1833) are STB_LOCAL with per-obj restarting names, yet all section!=0 syms are pushed into the shared def_names map (lines 942-959), so a 2nd obj's reloc against its own .LCstr0 first-matches the 1st obj's copy. Fix's section!=0-first routing correctly resolves defined-here locals against their own blob and reserves the unique-name map for SHN_UNDEF GLOBAL fn refs only.",
        "Dispatch is real: --linker=hexa/--emit/--backend/--target parsed (main.hexa 437-450); link_elf_x86_64_ownstart(elf_obj_x86_holder,out_path,\"main\") at 1230; elf_obj_x86_holder is a genuine 0-or-1 holder pushed once at 1067 — so GATE-3 multi-obj print correctly needs the flagged main.hexa co-requisite, cleanly separated from the byte-neutral linker EDIT (GATE-1 falsifier drives the fn directly, needs no main.hexa change).",
        "Reachability findings confirmed against codegen: pack_lir_x86_64 hardcodes bss_size:0 (line 1804) and emits zero-init globals as 16B file-present .data slots (lines 1843-1860); no static initializer emits an address into .data (patch sites are .text-only). .bss-only and data->data are genuinely codegen-unreachable filler; multi-obj-data is the only real-program-reachable round.",
        "MINOR non-substantive imprecisions (do not affect the fix or mechanism): (a) inline comment main.hexa:1228-1229 still labels the path 'Round-1 = exit-code-only, print-with-string is the round-2 wall' — stale now that round-2 data is merged and dispatched at line 1230. (b) GATE-0(a) 'Entry 0x4000b0-120...' prefix is garbled but its operative assertion =0x400078 is correct (0x400000 + code_off=120). (c) the item text loosely calls the round-2 e2e gate form 'print(\"hello\")' while GATE-0(b) actually uses the single-obj return \"ABC\"[0]; both statements are individually true (single-obj rodata ref vs multi-obj runtime-.o print), just phrased ambiguously."
      ],
      "files_or_edits_or_findings": [
        "TARGET: compiler/emit/elf_x86_64.hexa :: link_elf_x86_64_ownstart reloc-resolution loop (lines 1016-1053 at c62ea8d38).",
        "EDIT (single, self-contained, byte-neutral on gated paths). Replace the name-keyed first-match resolver so a reloc whose OWN referenced symbol is DEFINED in this obj (section != 0) resolves directly against THIS obj's blobs; only SHN_UNDEF externs fall to the cross-obj name map (where GLOBAL fn/data names are unique). obj_bases[ro] (base), obj_rodata_base[ro], obj_data_base[ro] are already populated in scope.\n\nOLD (verbatim anchor, lines 1016-1029):\n            let mut found_idx = -1\n            let mut sk = 0\n            while sk < len(def_names) {\n                if def_names[sk] == name { found_idx = sk; break }\n                sk = sk + 1\n            }\n            if found_idx >= 0 {\n                // Target symbol's virtual address (text seg vs data seg).\n                let mut s_vaddr = 0\n                if def_seg[found_idx] == 0 {\n                    s_vaddr = vaddr_base + code_off + def_off[found_idx]\n                } else {\n                    s_vaddr = data_vaddr_base + def_off[found_idx]\n                }\n\nNEW (verbatim; the trailing `if resolved {` re-opens the block whose body/patch code lines 1030-1053 stay UNCHANGED, incl. its closing brace):\n            // Per-obj local scoping (round-3 multi-obj-data). If the reloc's\n            // OWN referenced symbol is DEFINED here (section != 0), resolve it\n            // directly against THIS obj's blobs -- colliding LOCAL names\n            // (.LCstr0/g0 repeat once per obj) must never cross-resolve to\n            // another obj's copy. Only a SHN_UNDEF (extern GLOBAL) falls to the\n            // cross-obj name map, where global fn/data names are unique.\n            let mut s_vaddr = 0\n            let mut resolved = false\n            if sym.section != 0 {\n                resolved = true\n                if sym.section == 1 {\n                    s_vaddr = vaddr_base + code_off + base + sym.value\n                } else if sym.section >= 4 {\n                    s_vaddr = data_vaddr_base + obj_data_base[ro] + sym.value\n                } else {\n                    s_vaddr = data_vaddr_base + obj_rodata_base[ro] + sym.value\n                }\n            } else {\n                let mut found_idx = -1\n                let mut sk = 0\n                while sk < len(def_names) {\n                    if def_names[sk] == name { found_idx = sk; break }\n                    sk = sk + 1\n                }\n                if found_idx >= 0 {\n                    resolved = true\n                    if def_seg[found_idx] == 0 {\n                        s_vaddr = vaddr_base + code_off + def_off[found_idx]\n                    } else {\n                        s_vaddr = data_vaddr_base + def_off[found_idx]\n                    }\n                }\n            }\n            if resolved {",
        "BYTE-NEUTRALITY PROOF (why gated byteeq is safe): (1) round-1 exit-only program has 0 relocs -> loop body never runs -> byte-identical. (2) single-obj data program: first obj -> obj_rodata_base[ro]=obj_data_base[ro]=0, so new .rodata s_vaddr = data_vaddr_base + 0 + sym.value == old data_vaddr_base + def_off(=ro_base+s.value=s.value); new .text s_vaddr = vaddr_base+code_off+base+sym.value == old vaddr_base+code_off+def_off(=base+s.value); intra-obj CALL (section==1) identical. Only >=2-obj behavior changes -- that path had NO prior consumer/test, so this is a pure bug-fix, not a regression.",
        "CO-REQUISITE (precise plan, NOT a linker edit): to exercise multi-obj from `hexa build`, main.hexa must collect >1 ElfX86Obj. Today elf_obj_x86_holder (main.hexa ~line 813, dispatch ~1222) is a 0-or-1 holder passing ONE obj to link_elf_x86_64_ownstart. The falsifier below needs NO main.hexa change (drives the linker fn directly with two hand-built objs); the print(\"hello\")-via-runtime-.o form is the follow-on that needs main.hexa to also emit+append the runtime object.",
        "REACHABILITY FINDINGS: .bss-only (round-2 wall a) = UNREACHABLE today (pack_lir_x86_64 line 1804 bss_size:0; zero-init globals go to .data as 16B file-present zeros, lines 1843-1860) -> needs a codegen change first, 0 real consumers = filler. data->data absolute R_X86_64_64 (wall c) = UNREACHABLE (no static initializer emits an address into .data). GOT/PLT/PT_INTERP/DT_NEEDED (wall d) = sanctioned CUDA/dlopen/net floor, deferred. Multi-obj-data (wall b) = the ONLY item a real program (print/hello via runtime .o, or any multi-module build) hits."
      ],
      "gate": [
        "GATE-0 (MANDATORY PREREQUISITE -- rounds 1+2 were NEVER link-and-run): first prove the EXISTING single-obj path on aiden/summer at >= c62ea8d38 (NOT stale ~/.hx). (a) round-1 exit-only `fn main()->int{return 42}` via `hexa build --backend=native --linker=hexa --emit=exec --target=x86_64-linux-gnu` -> run, echo $? == 42; readelf -l == 1 PT_LOAD (R E), Entry 0x4000b0-120... =0x400078. (b) round-2 single-obj data `return \"ABC\"[0]` -> exit 65; readelf -l == EXACTLY 2 PT_LOAD (seg0 R E off0 vaddr0x400000; seg1 RW, (Off&0xfff)==(VirtAddr&0xfff)==0, VirtAddr >= seg0 VirtAddr+MemSiz); NO PT_INTERP/PT_DYNAMIC. strace -f -e trace=execve over build+link shows ZERO execve of any .../as or .../ld. This gate depends on NO round-3 code and MUST be green before trusting any 2-obj result.",
        "GATE-1 (round-3 falsifier, needs no main.hexa change): a .hexa test driver builds TWO ElfX86Obj via a minimal hand-constructor -- obj0 .rodata .LCstr0 = byte 0x01 with global fn main that leas its own .LCstr0 and loads *ptr into eax then calls global fn add1 (defined in obj1); obj1 .rodata .LCstr0 = byte 0x02 with global fn add1 that leas ITS own .LCstr0, loads *ptr, adds to eax, ret. Call link_elf_x86_64_ownstart([obj0,obj1], out, \"main\"); run. CORRECT (fix present) -> exit 3 (1+2). BUG (name-collision, both resolve to obj0's .LCstr0) -> exit 2. Exit==3 is the pass; capture $? on real x86_64-linux (aiden/summer).",
        "GATE-2 (release integrity / byteeq): the DEFAULT shipping path (backend=system, no --linker=hexa) is untouched. On --linker=hexa: round-1 exit-only AND round-2 single-obj-data binaries are BYTE-IDENTICAL pre/post EDIT (byte-diff the two exec files) -- proves the resolver restructure is byte-neutral on every path with <=1 obj. Then the 3-target byteeq gen3===gen4 fixpoint (x86_64-linux, arm64-linux, darwin-arm64) stays GREEN. Build+measure on pool (mini = git/gh only).",
        "GATE-3 (end-to-end, 0 binutils): once main.hexa multi-obj collection lands (co-requisite), a print(\"hello\")-class program links user .o + own-emitted runtime .o via --linker=hexa and prints `hello`; strace shows ZERO .../as or .../ld execve."
      ],
      "wall": "Round-4 = DATA->DATA absolute relocs (R_X86_64_64 whose PATCH SITE lives in the data segment -- e.g. a global statically initialized to another symbol's address; round-2/3 only patch text-resident R_X86_64_64, a data-segment patch site must be applied into data_bytes BEFORE serialize_elf_exec_x86_64_2seg), co-itemed with .BSS SYMBOL PLACEMENT (compute def_off/vaddr for section==bss_sec symbols in the p_memsz-reserved region above .data). BOTH are currently UNREACHABLE from codegen -- round-4 is gated on a prior pack_lir_x86_64 change (route zero-init globals to .bss / emit address-initialized static globals), so round-4's real first move is that codegen emission, then the linker placement. Round-5+ endgame = DYNAMIC ELF (PT_INTERP/DT_NEEDED/GOT/PLT/symbol-versioning) for the sanctioned CUDA/dlopen/net floor only -- never the base static self-host binary."
    }
  ]
}