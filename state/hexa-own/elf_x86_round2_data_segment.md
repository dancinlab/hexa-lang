{
  "ref_sha": "e9ec0b4f1",
  "item": "hexa_ld ELF x86_64 round-2: add a SECOND RW PT_LOAD data segment (.rodata ++ .data, +.bss reserved in p_memsz) so a program with string/const-literal data refs (e.g. print(\"hello\") -> lea rip->.LCstr0 via R_X86_64_PC32) links into a running static ET_EXEC. Two surgical edits to compiler/emit/elf_x86_64.hexa: (1) new serialize_elf_exec_x86_64_2seg(text,data,bss_size) two-PT_LOAD exec serializer; (2) extend link_elf_x86_64_ownstart IN PLACE to concat each obj's rodata/data after text, compute each symbol's final VADDR (text seg vs data seg), and resolve relocs in true vaddr space (PC32/PLT32 32-bit PC-relative + R_X86_64_64 64-bit absolute). No main.hexa change needed: round-1 EDIT-B3 already calls link_elf_x86_64_ownstart, which now auto-handles data. Still coupled to the 14-byte own-start (no crt1.o/-lc/-dynamic-linker). Zero-data programs stay byte-identical to round-1 (single PT_LOAD via the untouched serialize_elf_exec_x86_64; text->text deltas are vaddr-base invariant).",
  "ready": true,
  "risk_class": "byte-neutral",
  "holds": true,
  "vaddr_arithmetic_ok": true,
  "confidence": "high",
  "corrections": [
    "HEAD confirmed e9ec0b4f1; both EDIT-1 (lines 877-982) and EDIT-2 (lines 1289-1296) BEFORE old-text match the live file verbatim. Struct fields rodata/data/bss_size at lines 64-66, reloc constants R_X86_64_64=1/PC32=2/PLT32=4 at lines 48-51, section-numbering scheme at lines 1032-1042 all match the spec's assumptions.",
    "VADDR MATH VERIFIED (all 4 sub-checks pass). (a) Congruence: seg0 p_vaddr=0x400000, p_offset=0 -> both 0 mod 0x1000. seg1 p_vaddr=0x400000+data_file_off, p_offset=data_file_off, difference 0x400000 is a page multiple, AND data_file_off is page-aligned (round-up), so both are 0 mod 0x1000. Holds. (b) Data-symbol vaddr = 0x400000+data_file_off+def_off; the byte at data_bytes[def_off] lands at file offset data_file_off+def_off under the uniform vaddr=0x400000+fileoff map. Linker's data_vaddr_base and serializer's data_vaddr both compute 0x400000+data_file_off with identical code_off=176/phnum=2/len(text). Match. (c) PC32/PLT32 delta = S_vaddr+addend-P_vaddr; text->text reduces to def_off+addend-patch_off (vaddr_base+code_off cancels) = round-1 formula exactly. R_X86_64_64 writes 8-byte S_vaddr+addend. Correct. (d) e_entry = 0x400000+176 = 0x4000b0 points at text offset 0 = the 14-byte _start stub. Correct.",
    "NON-BLOCKING NOTE (nlocal): the ElfX86Obj.nlocal field is NOT referenced by the linker edits. This is correct, not a defect -- nlocal only feeds .symtab sh_info in serialize_elf_x86_64, and the exec output emits no symtab. The spec only claims to use rodata/data/bss_size; it makes no false claim about nlocal.",
    "NON-BLOCKING NOTE (per-obj section ambiguity, already folded into round-3 wall b): the section>=4=.data / ==3=.rodata classification assumes THAT obj has relocs (nrelocs>0, so .rela.text occupies index 2 and shifts data sections up by 1). A data-carrying obj with ZERO relocs would have .data at section 3 (misclassified as rodata). This is harmless for round-2 because an unreferenced data symbol's def_off is never consumed (resolution only runs for actual relocs), and the print('hello') form-(b) test's string lives in the main obj which always has the lea reloc. Correctly deferred as part of round-3 multi-obj-data.",
    "NON-BLOCKING NOTE (kind==1 on no-data path): round-1's reloc loop wrote 4 bytes for ALL relocs including R_X86_64_64; round-2 correctly branches kind==1 to 8-byte absolute. The gated byteeq test (return 42) has zero relocs so it is byte-identical. A no-data program CONTAINING an R_X86_64_64 reloc would differ from round-1 -- but that is a latent round-1 bug-fix, not a regression, and is outside the exit-only byteeq gate.",
    "MINOR gate-wording nit: if text_end_file is an exact page multiple, data_file_off==text_end_file so seg1.VirtAddr == seg0.VirtAddr+MemSiz exactly (adjacent, not strictly greater). The kernel accepts adjacent non-overlapping segments, so the readelf gate's 'VirtAddr > seg0 VirtAddr+MemSiz' should read '>=' for that rare exact-aligned edge; no correctness impact.",
    "MECHANICAL caveat (could not byte-verify): the EDIT-2 separator lines use box-drawing dashes; the live file uses 65 U+2500 dashes per separator (lines 1234/1295 confirmed = 65). The spec's rendered separators must be exactly 65 dashes for the Edit anchor to apply cleanly. Content of the code lines matches verbatim; only the dash-count of the cosmetic separator could not be counted from the rendered spec text."
  ],
  "edits": [
    "EDIT 1 (data-segment linker) — compiler/emit/elf_x86_64.hexa — REPLACE the whole link_elf_x86_64_ownstart function + its trailing 2-line 'Text-only (round-1)' comment (lines 877-982, from '// Text-only (round-1): serialize_elf_exec_x86_64 emits a single PT_LOAD;' through the function's closing '}' just above the 'serialize_elf_x86_64 — P2 cycle 1' comment). Leave the stub doc-comment 854-876 intact (the +14 shift math is unchanged). Unique anchor: the 'pub fn link_elf_x86_64_ownstart' signature.\n\nVADDR MATH (the load-bearing part):\n  vaddr_base = 0x400000 ; phnum = has_data ? 2 : 1 ; code_off = 64 + 56*phnum  (=120 for exit-only, =176 for a data program)\n  text seg (R-X): p_offset=0  p_vaddr=0x400000  p_filesz=p_memsz = code_off + len(text)\n  text symbol vaddr = vaddr_base + code_off + text_off\n  data_file_off = round_up_page(code_off + len(text)) = e + ((0x1000 - (e & 0xfff)) & 0xfff), e = code_off+len(text)\n  data seg (RW): p_offset=data_file_off  p_vaddr=0x400000+data_file_off  p_filesz=len(data)  p_memsz=len(data)+bss\n  data symbol vaddr = (vaddr_base + data_file_off) + data_blob_off\n  => whole file is a uniform vaddr = 0x400000 + file_off map, split at a page boundary, so p_vaddr === p_offset (mod 0x1000) holds trivially (both ===0 at the split) and the R-X / RW pages never overlap (data_file_off is the next page >= text end).\n  reloc (patch site always in .text): P_vaddr = vaddr_base + code_off + (base + r.offset) ; PC32/PLT32 -> delta = S_vaddr + addend - P_vaddr (4 bytes) ; R_X86_64_64 -> S_vaddr + addend (8 bytes). For text->text this reduces to the round-1 text-offset delta (vaddr_base+code_off cancels) => byte-identical.\n\nBEFORE (verbatim, lines 877-982):\n// Text-only (round-1): serialize_elf_exec_x86_64 emits a single PT_LOAD;\n// only programs with ZERO data refs link correctly (see round-2 wall).\npub fn link_elf_x86_64_ownstart(objs: [ElfX86Obj], out_path: string, main_name: string) -> Int {\n    // 14-byte own-start stub at text offset 0.\n    let mut text_bytes: [Int] = []\n    text_bytes.push(0xE8); text_bytes.push(0x00); text_bytes.push(0x00)\n    text_bytes.push(0x00); text_bytes.push(0x00)          // call rel32 (patched)\n    text_bytes.push(0x89); text_bytes.push(0xC7)          // mov edi, eax\n    text_bytes.push(0xB8); text_bytes.push(0x3C); text_bytes.push(0x00)\n    text_bytes.push(0x00); text_bytes.push(0x00)          // mov eax, 60 (__NR_exit)\n    text_bytes.push(0x0F); text_bytes.push(0x05)          // syscall\n    // len(text_bytes) is now 14 — the +14 shift applied to every obj below.\n\n    let mut main_off = -1\n    let mut def_names:   [string] = []\n    let mut def_offsets: [Int]    = []\n    let mut obj_bases:   [Int]    = []\n\n    let mut oi = 0\n    while oi < len(objs) {\n        let obj = objs[oi]\n        let base = len(text_bytes)        // >= 14 — the own-start shift\n        obj_bases.push(base)\n        let mut di = 0\n        while di < len(obj.symbols) {\n            let s = obj.symbols[di]\n            let mut name = \"\"\n            let mut k = 0\n            while true {\n                let b = obj.strtab[s.name_offset + k] & 0xff\n                if b == 0 { break }\n                name = name + from_char_code(b)\n                k = k + 1\n            }\n            if s.section != 0 {\n                def_names.push(name)\n                def_offsets.push(base + s.value)\n                if main_off < 0 && name == main_name {\n                    main_off = base + s.value\n                }\n            }\n            di = di + 1\n        }\n        let mut ti = 0\n        while ti < len(obj.text) {\n            text_bytes.push(obj.text[ti] & 0xff)\n            ti = ti + 1\n        }\n        oi = oi + 1\n    }\n    if main_off < 0 {\n        eprintln(\"hexa_ld(elf,ownstart): entry '\" + main_name + \"' not defined in any obj\")\n        return 3\n    }\n\n    // Patch the `call main` rel32 (stub bytes 1..4). Next-insn addr = 5.\n    let call_rel = main_off - 5\n    text_bytes[1] = call_rel & 0xff\n    text_bytes[2] = (call_rel >> 8) & 0xff\n    text_bytes[3] = (call_rel >> 16) & 0xff\n    text_bytes[4] = (call_rel >> 24) & 0xff\n\n    // Resolve inter/intra-obj PLT32/PC32 relocs. The shared +14 shift\n    // cancels in S+A−P, so this is identical to link_elf_x86_64.\n    let mut ro = 0\n    while ro < len(objs) {\n        let obj = objs[ro]\n        let base = obj_bases[ro]\n        let mut ri = 0\n        while ri < len(obj.relocs) {\n            let r = obj.relocs[ri]\n            let sym = obj.symbols[r.sym_idx]\n            let mut name = \"\"\n            let mut k = 0\n            while true {\n                let b = obj.strtab[sym.name_offset + k] & 0xff\n                if b == 0 { break }\n                name = name + from_char_code(b)\n                k = k + 1\n            }\n            let mut found_idx = -1\n            let mut sk = 0\n            while sk < len(def_names) {\n                if def_names[sk] == name { found_idx = sk; break }\n                sk = sk + 1\n            }\n            if found_idx >= 0 {\n                let tgt_off = def_offsets[found_idx]\n                let patch_off = base + r.offset\n                let delta = tgt_off + r.addend - patch_off\n                text_bytes[patch_off + 0] = delta & 0xff\n                text_bytes[patch_off + 1] = (delta >> 8) & 0xff\n                text_bytes[patch_off + 2] = (delta >> 16) & 0xff\n                text_bytes[patch_off + 3] = (delta >> 24) & 0xff\n            }\n            ri = ri + 1\n        }\n        ro = ro + 1\n    }\n\n    // Entry is the stub at text offset 0 (serialize_elf_exec_x86_64 sets\n    // e_entry = vaddr_base + code_off where code_off is the first text byte).\n    let bytes = serialize_elf_exec_x86_64(text_bytes)\n    write_bytes(out_path, bytes)\n    return 0\n}\n\nAFTER (verbatim):\n// Round-2 (data-segment): extends the round-1 text-only path with a SECOND\n// RW PT_LOAD carrying .rodata ++ .data (+ .bss reserved in p_memsz), so a\n// program with string/const-literal data refs (e.g. print(\"hello\") -> lea\n// rip->.LCstr0, an R_X86_64_PC32) links into a running static ET_EXEC.\n// Byte-identical to round-1 when NO obj carries data (single PT_LOAD via the\n// untouched serialize_elf_exec_x86_64; text->text deltas are vaddr-base\n// invariant). Data present -> serialize_elf_exec_x86_64_2seg (two PT_LOADs).\npub fn link_elf_x86_64_ownstart(objs: [ElfX86Obj], out_path: string, main_name: string) -> Int {\n    // 14-byte own-start stub at text offset 0.\n    let mut text_bytes: [Int] = []\n    text_bytes.push(0xE8); text_bytes.push(0x00); text_bytes.push(0x00)\n    text_bytes.push(0x00); text_bytes.push(0x00)          // call rel32 (patched)\n    text_bytes.push(0x89); text_bytes.push(0xC7)          // mov edi, eax\n    text_bytes.push(0xB8); text_bytes.push(0x3C); text_bytes.push(0x00)\n    text_bytes.push(0x00); text_bytes.push(0x00)          // mov eax, 60 (__NR_exit)\n    text_bytes.push(0x0F); text_bytes.push(0x05)          // syscall\n    // len(text_bytes) is now 14 — the +14 shift applied to every obj below.\n\n    // Concatenated data blob (.rodata ++ .data per obj) for the RW segment.\n    let mut data_bytes: [Int] = []\n    let mut total_bss  = 0\n\n    let mut main_off = -1\n    // Defined-symbol map. def_seg = 0 for a .text def, 1 for a data-blob def;\n    // def_off is the byte offset INSIDE that segment's blob (text offset for\n    // text, data-blob offset for data). Converted to a vaddr AFTER the layout\n    // constants (code_off / data_file_off) are known below.\n    let mut def_names:   [string] = []\n    let mut def_seg:     [Int]    = []\n    let mut def_off:     [Int]    = []\n\n    let mut obj_bases:       [Int] = []   // .text base per obj\n    let mut obj_rodata_base: [Int] = []   // .rodata start in data_bytes per obj\n    let mut obj_data_base:   [Int] = []   // .data   start in data_bytes per obj\n\n    let mut oi = 0\n    while oi < len(objs) {\n        let obj = objs[oi]\n        let base = len(text_bytes)        // >= 14 — the own-start shift\n        obj_bases.push(base)\n\n        // Append this obj's data blob: .rodata then .data (bss reserved later).\n        let ro_base = len(data_bytes)\n        obj_rodata_base.push(ro_base)\n        let mut rri = 0\n        while rri < len(obj.rodata) { data_bytes.push(obj.rodata[rri] & 0xff); rri = rri + 1 }\n        let da_base = len(data_bytes)\n        obj_data_base.push(da_base)\n        let mut ddi = 0\n        while ddi < len(obj.data) { data_bytes.push(obj.data[ddi] & 0xff); ddi = ddi + 1 }\n        total_bss = total_bss + obj.bss_size\n\n        let mut di = 0\n        while di < len(obj.symbols) {\n            let s = obj.symbols[di]\n            let mut name = \"\"\n            let mut k = 0\n            while true {\n                let b = obj.strtab[s.name_offset + k] & 0xff\n                if b == 0 { break }\n                name = name + from_char_code(b)\n                k = k + 1\n            }\n            if s.section == 1 {\n                // .text def -> segment 0, text offset.\n                def_names.push(name)\n                def_seg.push(0)\n                def_off.push(base + s.value)\n                if main_off < 0 && name == main_name {\n                    main_off = base + s.value\n                }\n            } else if s.section != 0 {\n                // Data def. section==4 (data_sec when .rodata present) -> .data\n                // pool; otherwise (section==3) -> .rodata pool. When the obj has\n                // no .rodata its .data syms carry section==3 and ro_base==da_base,\n                // so the .rodata branch still lands at the right blob offset.\n                def_names.push(name)\n                def_seg.push(1)\n                if s.section >= 4 {\n                    def_off.push(da_base + s.value)\n                } else {\n                    def_off.push(ro_base + s.value)\n                }\n            }\n            di = di + 1\n        }\n        let mut ti = 0\n        while ti < len(obj.text) {\n            text_bytes.push(obj.text[ti] & 0xff)\n            ti = ti + 1\n        }\n        oi = oi + 1\n    }\n    if main_off < 0 {\n        eprintln(\"hexa_ld(elf,ownstart): entry '\" + main_name + \"' not defined in any obj\")\n        return 3\n    }\n\n    // Patch the `call main` rel32 (stub bytes 1..4). Next-insn addr = 5.\n    let call_rel = main_off - 5\n    text_bytes[1] = call_rel & 0xff\n    text_bytes[2] = (call_rel >> 8) & 0xff\n    text_bytes[3] = (call_rel >> 16) & 0xff\n    text_bytes[4] = (call_rel >> 24) & 0xff\n\n    // Layout constants — MUST match serialize_elf_exec_x86_64{,_2seg}.\n    // No data blob -> single PT_LOAD (phnum=1, code_off=120): byte-identical\n    // to round-1. Data present -> two PT_LOADs (phnum=2, code_off=176: R-X\n    // text + RW rodata/data at a page-aligned vaddr above text).\n    let vaddr_base = 0x400000\n    let has_data   = (len(data_bytes) > 0) || (total_bss > 0)\n    let phnum      = if has_data { 2 } else { 1 }\n    let code_off   = 64 + 56 * phnum\n    let text_end_file = code_off + len(text_bytes)\n    // Page-align the data segment's file offset (=> p_vaddr === p_offset mod\n    // 0x1000 holds and the R-X/RW pages never overlap). Same idiom as the\n    // serializer's rodata_pad round-up.\n    let data_file_off   = text_end_file + ((0x1000 - (text_end_file & 0xfff)) & 0xfff)\n    let data_vaddr_base = vaddr_base + data_file_off\n\n    // Resolve relocs in TRUE vaddr space. text->text reduces to the round-1\n    // text-offset delta (vaddr_base+code_off cancels) => no-data path is\n    // byte-identical; text->data uses the data segment's distinct vaddr.\n    let mut ro = 0\n    while ro < len(objs) {\n        let obj = objs[ro]\n        let base = obj_bases[ro]\n        let mut ri = 0\n        while ri < len(obj.relocs) {\n            let r = obj.relocs[ri]\n            let sym = obj.symbols[r.sym_idx]\n            let mut name = \"\"\n            let mut k = 0\n            while true {\n                let b = obj.strtab[sym.name_offset + k] & 0xff\n                if b == 0 { break }\n                name = name + from_char_code(b)\n                k = k + 1\n            }\n            let mut found_idx = -1\n            let mut sk = 0\n            while sk < len(def_names) {\n                if def_names[sk] == name { found_idx = sk; break }\n                sk = sk + 1\n            }\n            if found_idx >= 0 {\n                // Target symbol's virtual address (text seg vs data seg).\n                let mut s_vaddr = 0\n                if def_seg[found_idx] == 0 {\n                    s_vaddr = vaddr_base + code_off + def_off[found_idx]\n                } else {\n                    s_vaddr = data_vaddr_base + def_off[found_idx]\n                }\n                // Patch site is always in .text (CALL imm32 or lea disp32).\n                let patch_off = base + r.offset\n                let p_vaddr = vaddr_base + code_off + patch_off\n                if r.kind == 1 {\n                    // R_X86_64_64 — absolute 64-bit (patch site in .text).\n                    // Data-resident absolute relocs (data->data) are round-3.\n                    let av = s_vaddr + r.addend\n                    text_bytes[patch_off + 0] = av & 0xff\n                    text_bytes[patch_off + 1] = (av >> 8) & 0xff\n                    text_bytes[patch_off + 2] = (av >> 16) & 0xff\n                    text_bytes[patch_off + 3] = (av >> 24) & 0xff\n                    text_bytes[patch_off + 4] = (av >> 32) & 0xff\n                    text_bytes[patch_off + 5] = (av >> 40) & 0xff\n                    text_bytes[patch_off + 6] = (av >> 48) & 0xff\n                    text_bytes[patch_off + 7] = (av >> 56) & 0xff\n                } else {\n                    // R_X86_64_PC32 (2) / R_X86_64_PLT32 (4) — 32-bit PC-relative.\n                    let delta = s_vaddr + r.addend - p_vaddr\n                    text_bytes[patch_off + 0] = delta & 0xff\n                    text_bytes[patch_off + 1] = (delta >> 8) & 0xff\n                    text_bytes[patch_off + 2] = (delta >> 16) & 0xff\n                    text_bytes[patch_off + 3] = (delta >> 24) & 0xff\n                }\n            }\n            ri = ri + 1\n        }\n        ro = ro + 1\n    }\n\n    // Serialize. No data -> single-PT_LOAD round-1 serializer (byte-identical);\n    // data present -> two-segment serializer (R-X text + RW rodata/data).\n    if has_data {\n        write_bytes(out_path, serialize_elf_exec_x86_64_2seg(text_bytes, data_bytes, total_bss))\n    } else {\n        write_bytes(out_path, serialize_elf_exec_x86_64(text_bytes))\n    }\n    return 0\n}",
    "EDIT 2 (two-segment exec serializer) — compiler/emit/elf_x86_64.hexa — INSERT a new pub fn serialize_elf_exec_x86_64_2seg immediately AFTER the close of serialize_elf_exec_x86_64 (line 1293 '}') and BEFORE the 'LIR walker' comment block (line 1295). Unique anchor: the '// LIR walker — `LModule` -> byte stream (RFC 063 § P2, cycle 29)' comment.\n\nBEFORE (verbatim, lines 1289-1296):\n    let mut i = 0\n    while i < text_size { out.push(text[i] & 0xff); i = i + 1 }\n\n    return out\n}\n\n// ──────────────────────────────────────────────────────────────\n// LIR walker — `LModule` → byte stream (RFC 063 § P2, cycle 29)\n\nAFTER (verbatim):\n    let mut i = 0\n    while i < text_size { out.push(text[i] & 0xff); i = i + 1 }\n\n    return out\n}\n\n// serialize_elf_exec_x86_64_2seg — round-2 two-segment static exec.\n// ─────────────────────────────────────────────────────────────\n// Static ELF ET_EXEC for Linux x86_64 with TWO PT_LOAD segments:\n//   seg0 (R-X): ELF header + 2 phdrs + .text          at vaddr 0x400000\n//   seg1 (RW):  .rodata ++ .data (+ .bss in p_memsz)   at a page-aligned\n//               vaddr above the text segment.\n// The whole file stays a uniform vaddr = 0x400000 + file_off map, split at a\n// page boundary between the R-X and RW segments, so each segment satisfies\n// p_vaddr === p_offset (mod 0x1000) and their pages never overlap. No dynamic\n// linker, no symtab. .bss is zero-filled by the kernel (p_memsz > p_filesz).\n//\n// Layout (data program):\n//   ELF64 header  64 B   e_type=ET_EXEC e_entry=0x400000+176\n//   PT_LOAD[0]    56 B   R-X  p_offset=0            p_vaddr=0x400000\n//   PT_LOAD[1]    56 B   RW   p_offset=data_file_off p_vaddr=0x400000+data_file_off\n//   .text          N B   at file offset 176 (=64+56*2)\n//   (zero pad to the next page)\n//   .rodata/.data  D B   at file offset data_file_off\npub fn serialize_elf_exec_x86_64_2seg(text: [Int], data: [Int], bss_size: Int) -> [Int] {\n    let out: [Int] = []\n    let text_size = len(text)\n    let data_size = len(data)\n\n    let elf_header_size = 64\n    let phdr_size = 56\n    let phnum = 2\n    let code_off = elf_header_size + phdr_size * phnum     // 176\n    let vaddr_base = 0x400000\n    let entry_vaddr = vaddr_base + code_off\n    let text_end_file = code_off + text_size\n    // Page-align the data segment's file offset (=> vaddr congruence holds).\n    let data_file_off = text_end_file + ((0x1000 - (text_end_file & 0xfff)) & 0xfff)\n    let data_vaddr = vaddr_base + data_file_off\n    let text_filesz = code_off + text_size                 // seg0 file+mem size\n    let data_memsz  = data_size + bss_size                 // seg1 mem (incl .bss)\n\n    // ── ELF header ──\n    out.push(ELF_EI_MAG0); out.push(ELF_EI_MAG1)\n    out.push(ELF_EI_MAG2); out.push(ELF_EI_MAG3)\n    out.push(ELF_ELFCLASS64)\n    out.push(ELF_ELFDATA2LSB)\n    out.push(ELF_EV_CURRENT)\n    out.push(ELF_ELFOSABI_NONE)\n    _ew_zero(out, 8)\n    _ew_u16(out, ELF_ET_EXEC)\n    _ew_u16(out, ELF_EM_X86_64)\n    _ew_u32(out, ELF_EV_CURRENT)\n    _ew_u64(out, entry_vaddr)\n    _ew_u64(out, elf_header_size)           // e_phoff\n    _ew_u64(out, 0)                         // e_shoff\n    _ew_u32(out, 0)                         // e_flags\n    _ew_u16(out, elf_header_size)\n    _ew_u16(out, phdr_size)\n    _ew_u16(out, phnum)\n    _ew_u16(out, 0)                         // e_shentsize\n    _ew_u16(out, 0)                         // e_shnum\n    _ew_u16(out, 0)                         // e_shstrndx\n\n    // ── PT_LOAD[0] — R-X text ──\n    _ew_u32(out, 1)                         // p_type = PT_LOAD\n    _ew_u32(out, 5)                         // p_flags = PF_R | PF_X\n    _ew_u64(out, 0)                         // p_offset\n    _ew_u64(out, vaddr_base)                // p_vaddr\n    _ew_u64(out, vaddr_base)                // p_paddr\n    _ew_u64(out, text_filesz)               // p_filesz\n    _ew_u64(out, text_filesz)               // p_memsz\n    _ew_u64(out, 0x1000)                    // p_align\n\n    // ── PT_LOAD[1] — RW rodata/data (+ .bss in memsz) ──\n    _ew_u32(out, 1)                         // p_type = PT_LOAD\n    _ew_u32(out, 6)                         // p_flags = PF_R | PF_W\n    _ew_u64(out, data_file_off)             // p_offset\n    _ew_u64(out, data_vaddr)                // p_vaddr\n    _ew_u64(out, data_vaddr)                // p_paddr\n    _ew_u64(out, data_size)                 // p_filesz\n    _ew_u64(out, data_memsz)                // p_memsz  (includes .bss)\n    _ew_u64(out, 0x1000)                    // p_align\n\n    // ── .text at file offset code_off (176) ──\n    let mut i = 0\n    while i < text_size { out.push(text[i] & 0xff); i = i + 1 }\n\n    // ── zero pad from text end up to the page-aligned data file offset ──\n    _ew_zero(out, data_file_off - text_end_file)\n\n    // ── .rodata/.data at file offset data_file_off ──\n    let mut d = 0\n    while d < data_size { out.push(data[d] & 0xff); d = d + 1 }\n\n    return out\n}\n\n// ──────────────────────────────────────────────────────────────\n// LIR walker — `LModule` → byte stream (RFC 063 § P2, cycle 29)"
  ],
  "gate": [
    "STRUCTURAL (rock-solid, always verifiable) — build hexa on pool aiden/summer at >= e9ec0b4f1 (NOT stale ~/.hx), compile a data-carrying program via `hexa build --backend=native --linker=hexa --emit=exec --target=x86_64-linux-gnu p.hexa -o p`. `readelf -l p` shows EXACTLY 2 PT_LOAD: seg0 flags 'R E' (PF_R|PF_X) Offset 0x0 VirtAddr 0x400000; seg1 flags 'RW ' (PF_R|PF_W) with (Offset & 0xfff)==(VirtAddr & 0xfff)==0 (page-aligned, congruent) and VirtAddr > seg0's VirtAddr+MemSiz (no page overlap). `readelf -h p` Type=EXEC, Entry=0x4000b0 (0x400000+176). `readelf -l p` has NO PT_INTERP and NO PT_DYNAMIC.",
    "RELOC CORRECTNESS (the round-2 deliverable) — `objdump -d p`: the string-literal RIP-relative reference (`lea reg,[rip+disp32]` or `mov`) in main resolves so that (address_of_next_insn + disp32) == data_vaddr + pool_off, i.e. it points into the RW data segment (0x400000+data_file_off + the .LCstrN byte offset), NOT into .text. Cross-check disp32 == (data_vaddr_base + ro_base + s.value) - (P_vaddr + 4).",
    "END-TO-END RUN (0 binutils) — a print(\\\"hello\\\")-class program runs on real x86_64-linux and emits the correct bytes; `strace -f -qq -e trace=execve` over the whole build+link shows ZERO execve of any .../as or .../ld. Two admissible forms: (a) fully self-contained data-read program with 0 runtime UNDEF (e.g. a raw write(2)-syscall hello, or `return \\\"ABC\\\"[0]` -> exit 65) — this alone proves the data-segment vaddr is correct end-to-end with NO round-1b dependency; (b) the literal print(\\\"hello\\\") whose runtime print/write symbol is supplied by multi-obj-linking the own-emitted runtime .o (round-1b co-requisite for the print symbol — the linker already iterates N objs and resolves it) prints 'hello'.",
    "RELEASE INTEGRITY / byteeq — the DEFAULT shipping path (no --linker=hexa, backend=system) is untouched. On the --linker=hexa path, a ZERO-data program (round-1 exit-only, e.g. `fn main()->int{return 42}`) is byte-IDENTICAL to round-1: has_data=false -> phnum=1, code_off=120, single PT_LOAD via the untouched serialize_elf_exec_x86_64, and every text->text reloc delta is unchanged (vaddr_base+code_off cancels). Confirm with a byte-diff of a round-1 exit binary pre/post EDIT, plus the 3-target byteeq gen3===gen4 fixpoint."
  ],
  "wall": "Round-3 = (a) .bss symbol vaddr mapping: bss-resident symbols (section==bss_sec) are not yet placed — total_bss is reserved in p_memsz but no def_off/vaddr is computed for a .bss symbol, so a program referencing an uninitialized global resolves wrong (round-2 test files carry bss_size=0). (b) MULTIPLE DATA OBJECTS: colliding LOCAL data names across objs (every obj emits `.LCstr0`, `g0`, ...) — the name-based def map returns the FIRST match, and there is no string/const dedup or duplicate-global-def merge; correct multi-obj data linking needs (obj_index, name) keyed resolution or per-obj local scoping. (c) DATA->DATA absolute relocs: R_X86_64_64 whose PATCH SITE lives in the data segment (e.g. a global initialized to another symbol's address) — round-2 only patches text-resident R_X86_64_64; a data-segment patch site needs the reloc applied into data_bytes before serialization. (d) DYNAMIC ELF output (PT_INTERP / DT_NEEDED / GOT / PLT / symbol versioning) — only gates the CUDA/dlopen/net variants, not the base static binary."
}