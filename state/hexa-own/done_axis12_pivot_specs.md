{
  "ref_sha": "ccd544c12",
  "items": [
    {
      "item": "axis-① HX1102 residual completion: natively lower literal_char + literal_float match arms in compiler/lower/hir_to_mir.hexa (the #4687 literal_bool follow-ons)",
      "ready": true,
      "risk_class": "byte-neutral",
      "holds": true,
      "confidence": "high",
      "corrections": [
        "Git HEAD confirmed == ccd544c12. Anchor verified verbatim at hir_to_mir.hexa:5726-5729; the full 4-line old_string is unique even though `} else if pat.kind == \"struct_pattern\" {` alone appears twice (5729 test-switch, 5891 binding-switch) — the 5891 occurrence is NOT preceded by the three `_add_edge`/`ctx2 = ctx3` lines, so the Edit matches only at 5726-5729.",
        "Both constructors exist as claimed: _const_float_op_with_text at line 127, _const_str_op at line 155. to_float builtin used at value-lowering literal_float (3183). Value-lowering already handles literal_char (3204, _const_str_op/TAG_STR) and literal_float (3183). literal_int (5624-5654) and literal_string (5655-5686) arms confirmed as clone templates. Family is real: literal_char/literal_float currently fall to the else emitting _emit_hx1102 (5749).",
        "MINOR (justification-only, does not affect the edit): the plan calls lines 2964/2967 a 'runtime.hexa-side match kind gate' — they are actually in the SAME file compiler/lower/hir_to_mir.hexa inside fn _is_known_hexpr_kind (the value-expr known-kinds fall-through set), not a runtime.hexa file and not a match/pattern gate. The substance (both kinds whitelisted, value-lowering handles them) is nonetheless correct.",
        "MINOR (comment-only, not load-bearing): the new literal_char arm comment cites self/codegen_c2.hexa:3247, which is not present in the tree (git ls-files + find both empty — emitted/gitignored hexat C artifact). It faithfully mirrors the identical citation already in the existing literal_char value-lowering comment (3198-3199), so it is documentation-consistent, not a defect."
      ],
      "edits_or_plan": [
        "FILE: compiler/lower/hir_to_mir.hexa (single Edit; one insertion point). Insert the two new arms immediately AFTER the literal_bool arm ends and BEFORE the struct_pattern arm begins. The literal_bool arm ends at line 5728 with `ctx2 = ctx3`; the struct_pattern arm begins at line 5729 with `} else if pat.kind == \"struct_pattern\" {`.",
        "ANCHOR (unique — the `ctx2 = ctx3` that immediately precedes the sole `struct_pattern` occurrence; the trailing struct_pattern line disambiguates from the identical literal_int/literal_string/literal_bool arm tails):\n\nold_string:\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, arm_id)\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, next_id)\n                ctx2 = ctx3\n            } else if pat.kind == \"struct_pattern\" {",
        "new_string (the anchor's three closing lines are preserved verbatim at the TOP, then the two new arms, then the struct_pattern line preserved verbatim at the BOTTOM):\n\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, arm_id)\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, next_id)\n                ctx2 = ctx3\n            } else if pat.kind == \"literal_char\" {\n                // HEXA-OWN axis-① — char-literal pattern `match c { 'a' -> … }`.\n                // Byte-for-byte clone of the literal_string arm above; char\n                // literals are TAG_STR (single-char string) so the operand\n                // constructor is _const_str_op(pat.text) — mirrors the value-\n                // lowering path (_const_str_op(e.text) at the literal_char\n                // ExprKind; self/codegen_c2.hexa:3247 \"char literals must also\n                // be TAG_STR for hexa_eq\"). Previously fell to the HX1102 `else`\n                // — a fatal S2 abort forcing a C-transpile punt even though the\n                // emitted compare is identical. Byte-neutral (programs without\n                // char-arms never reach here; the C-transpile path never runs\n                // hir_to_mir). 0 usage in the #4687 census — completes the\n                // literal-pattern residual set (no more HX1102 punts).\n                let mut ctx3 = _fresh_local(ctx2, pat.typ, \"lit_eq\")\n                let cmp_dst = _lr_locals[len(_lr_locals) - 1]\n                let mut cmp_args: [Operand] = []\n                cmp_args.push(scrut_op)\n                cmp_args.push(_const_str_op(pat.text))\n                let cmp = Stmt {\n                    kind: STMT_BINOP,\n                    dst: cmp_dst,\n                    op: \"==\",\n                    args: cmp_args,\n                    target_block: -1,\n                    target_block_else: -1\n                }\n                ctx3 = _push_stmt(ctx3, cmp)\n                let mut br_args: [Operand] = []\n                br_args.push(_local_op(cmp_dst.id))\n                let br = Stmt {\n                    kind: STMT_BR_COND,\n                    dst: _miss_local(),\n                    op: \"\",\n                    args: br_args,\n                    target_block: arm_id,\n                    target_block_else: next_id\n                }\n                ctx3 = _push_stmt(ctx3, br)\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, arm_id)\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, next_id)\n                ctx2 = ctx3\n            } else if pat.kind == \"literal_float\" {\n                // HEXA-OWN axis-① — float-literal pattern `match x { 1.0 -> … }`.\n                // Byte-for-byte clone of the literal_int arm above with the\n                // operand constructor swapped to _const_float_op_with_text(\n                // to_float(pat.text), pat.text) — mirrors the value-lowering\n                // path (literal_float ExprKind). The source text rides through\n                // unparsed so both the parsed f64 and the original text are\n                // threaded (float-pool precision in the emitted .double). Prev-\n                // iously fell to the HX1102 `else` — a fatal S2 abort forcing a\n                // C-transpile punt even though the emitted compare is identical.\n                // Byte-neutral (programs without float-arms never reach here;\n                // the C-transpile path never runs hir_to_mir). 0 usage in the\n                // #4687 census — completes the literal-pattern residual set.\n                let mut ctx3 = _fresh_local(ctx2, pat.typ, \"lit_eq\")\n                let cmp_dst = _lr_locals[len(_lr_locals) - 1]\n                let mut cmp_args: [Operand] = []\n                cmp_args.push(scrut_op)\n                cmp_args.push(_const_float_op_with_text(to_float(pat.text), pat.text))\n                let cmp = Stmt {\n                    kind: STMT_BINOP,\n                    dst: cmp_dst,\n                    op: \"==\",\n                    args: cmp_args,\n                    target_block: -1,\n                    target_block_else: -1\n                }\n                ctx3 = _push_stmt(ctx3, cmp)\n                let mut br_args: [Operand] = []\n                br_args.push(_local_op(cmp_dst.id))\n                let br = Stmt {\n                    kind: STMT_BR_COND,\n                    dst: _miss_local(),\n                    op: \"\",\n                    args: br_args,\n                    target_block: arm_id,\n                    target_block_else: next_id\n                }\n                ctx3 = _push_stmt(ctx3, br)\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, arm_id)\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, next_id)\n                ctx2 = ctx3\n            } else if pat.kind == \"struct_pattern\" {",
        "CONSTRUCTOR CONFIRMATION (no new symbols; both already defined + used elsewhere in this file): `_const_str_op(s: string) -> Operand` at line 155; `_const_float_op_with_text(v: f64, text: string) -> Operand` at line 127. `to_float` builtin already used at the value-lowering literal_float site (line 3183). The two new arms are exact structural clones of the existing literal_int (5624-5654) / literal_string (5655-5686) arms — same STMT_BINOP `==` + STMT_BR_COND + 2×_add_edge shape — differing ONLY in the operand constructor pushed as cmp_args[1].",
        "NO OTHER EDIT NEEDED: the runtime.hexa-side match kind gate already whitelists both (line 2964 `if k == \"literal_float\" { return true }`, line 2967 `if k == \"literal_char\" { return true }`), and the value-lowering path (3193 literal_char, 3173 literal_float) already lowers these constructors — only the match-arm TEST dispatch was missing the arms, which this single edit closes. HX1102 `else` (5744-) and _emit_hx1102 are untouched."
      ],
      "gate": [
        "byteeq 3-target GREEN (x86_64-linux · arm64-linux · darwin-arm64) — bit-identical expected: no existing program hits these arms (0-usage census), so gen3≡gen4 blob is unchanged for all current sources",
        "native own-IR path lowers a `match` on a char scrutinee (`match c { 'a' -> …, _ -> … }`) and a float scrutinee (`match x { 1.0 -> …, _ -> … }`) WITHOUT emitting HX1102 and WITHOUT the S2 abort/C-transpile punt — new arms fire, STMT_BINOP `==` + STMT_BR_COND emitted identically to literal_int/literal_string",
        "shipping smoke: hexa build/run of an existing program is unaffected (the C-transpile path never runs hir_to_mir; new arms only reach on the native path when such an arm is present)"
      ],
      "wall": "Residual HX1102 literal-pattern set is COMPLETE after this (literal_int·literal_string·literal_bool·literal_char·literal_float all natively lowered). Next round for the HX1102 else is the non-literal residual shapes still punting there (e.g. range patterns / or-patterns / nested tuple-or-guard arms) — census those against the else's _emit_hx1102 hits; and the broader axis-① frontier stays the emitted hexa_cc.c C-transpile fallback elimination. axis-③ ELF/self-assemble remains pool-measurement-blocked."
    },
    {
      "item": "NEXT axis-② libc-UND family drop = FLIP-6 WALL-2 heap family (__libc_calloc + __libc_free). This is the LAST non-sanctioned reducible libc-UND leaf per the session's own verify-corrected wall in state/hexa-own/done_axis23_next_rounds_plans.md. The prior \"last leaf\" — non-shim strcmp callers — was ALREADY landed by this session (#4667 caller-drop gate + #4679 default-ON), so strcmp is no longer the target. All other tracked-WALL families are landed: strtod-tail (default-ON, PRs #4639/#4645/#4646 — NOT the fictitious #4651 that CLAUDE.md cites), own-start environ/atexit (FLIP-7 #4656/#4657), regex #4535, qsort #4452, glob #4449, fgets #4450, str-leaf/mem-leaf/strlen/isdigit/getenv, and realloc (F2 flip #4620/#4621). The remaining un-levered reducible seed-flip surface = 0 (SSOT line 45); calloc+free are the terminal reducible pair before the sanctioned floor.",
      "ready": false,
      "risk_class": "bit-changing",
      "holds": true,
      "confidence": "high",
      "corrections": [
        "VERIFIED — HEAD == ccd544c12. git log 53ee90270..HEAD confirms #4667 (strcmp caller-drop gate), #4679 (strcmp default-ON flip [DRAFT]), #4687 (literal_bool native-lower) all landed this session, so strcmp is no longer the target and FLIP-6 is genuinely next per the plans-doc wall's own ordering ('After strcmp-caller drop, the LAST non-sanctioned reducible leaf is FLIP-6').",
        "VERIFIED — every cited resolver line is EXACT at HEAD: tool/stage_resolve_runtime_a:2010 (HEXA_RT_NATIVE_FREE:-auto), :2016 (realloc-active forces _rnfr_mode=0), :2201 (HEXA_RT_NATIVE_CALLOC:-auto), :2598 (HEXA_RT_NATIVE_REALLOC:-auto) with :2600/:2601 dual -D to shim+rtcore, :1500 (sysheaders malloc-family rename). The free mechanism is confirmed verbatim by the resolver echo at :2602: 'reclaiming magic-guarded free serves (free no-op seed excluded)'. Shim emit sites :190/:227/:282 (#ifndef FREE/REALLOC/CALLOC) all exact. Seeds self/native/hxlcl_calloc_{x86_64,arm64-linux,arm64}.s + hxlcl_free_*.s all present.",
        "VERIFIED — not sanctioned-floor: SSOT §3:141 explicitly excludes __libc_calloc/__libc_free from the CRT floor ('atexit·environ·__libc_calloc·__libc_free 는 여기 아님 — FLIP-7 + campaign'); DAG B2 (§2:91,126) and §4 action-3 both name FLIP-6 (calloc/free/realloc default-ON) as PENDING, and §1:47 says 'FLIP-6 미실시'. So the flip is real, not-done, not-floor. Wall-section sanctioned set (dl* departed via S1 #4481, __libc_start_main M8 #3959, exec #4263 default-OFF, CUDA opt-in) all match SSOT §3.",
        "CORRECTION (overstated status): the STATUS NUANCE frames FLIP-6 as merely a 'VERIFY-AND-CONFIRM round … no mini-authorable byte-neutral code change remains'. But the plans-doc wall (line 43) and SSOT DAG B2 gate (§2:90-92) name a REAL blocker — the arm64/darwin fp-ABI Route-C else-arm at stage_resolve_runtime_a (:1930/:2084/:2131 ignore non-x86_64-linux). realloc-F2 only crossed this via the magic-guard heap-family split (#4620/#4621); whether calloc/free inherit that coverage is an open cross-target question, so it is a genuine bit-changing flip with an ABI risk, not a pure measurement pass. (The claim's own risk_class='bit-changing' + wall acknowledge this, making it an internal inconsistency rather than a false statement.)",
        "CORRECTION (conflation flagged by the session's own census-verify, plans-doc line 98): __libc_calloc/__libc_free are TRANSITIVE glibc-internal symbols, potentially reduced by removing libc CONSUMERS (regex #4535 / glob #4449 / qsort #4452 — already default-ON), NOT necessarily by flipping the hxlcl_calloc/hxlcl_free native gates. So the gate-section prediction 'nm-UND __libc_calloc AND __libc_free drop to 0 when the native gate is ON' is stated with more certainty than the session's own verification supports — the drop attribution is disputed between the SSOT DAG (FLIP-6 arena-port drops them, −3) and the census-verify (consumer campaign dissolves them). A measurement risk, not a clean error.",
        "MINOR — free-seed PR attribution: census-verify records free seeds as #4548/#4554 and calloc seed as #4556; the claim cites only #4556 ('baked on pool #4556') and thus under-attributes the free seeds. Non-load-bearing. (The claim correctly AVOIDS the stale #4242/#4244 attribution that census-verify flagged as wrong.)"
      ],
      "edits_or_plan": [
        "FAMILY: __libc_calloc + __libc_free (WALL-2 heap family, FLIP-6). 2 symbols, integer-ABI (no fp-ABI xmm concern), byteeq-safe (deterministic arena/magic-guard), perf-neutral (arena bump + magic-tag header). NOT sanctioned-floor (heap alloc is a portable algorithm; net-FFI/CRT/exec/dlopen are the irreducible floor).",
        "MECHANISM — all in-tree and realloc-F2-proven, NOT a fresh seed: (calloc) frozen-seed-consume via tri-state gate HEXA_RT_NATIVE_CALLOC default 'auto' at tool/stage_resolve_runtime_a:2201; seeds self/native/hxlcl_calloc_{x86_64,arm64-linux,arm64}.s baked on pool #4556 (.globl==1 isolated) -> $CC -c -> shim.o built with -DHEXA_RT_NATIVE_CALLOC -> shim #ifndef selects native body -> libc calloc dropped.",
        "(free) served by the realloc-F2 magic-guarded RECLAIMING body, NOT the no-op seed: HEXA_RT_NATIVE_FREE (:2010) is FORCED mode=0 whenever realloc is active (:2016) so the magic-guarded hxlcl_free reclaims family pointers and calls exact libc free only for foreign pointers. HEXA_RT_NATIVE_REALLOC default 'auto' (:2598) adds -DHEXA_RT_NATIVE_REALLOC to BOTH shim + runtime_core TUs (magic-tag base[8..16) header).",
        "CALLER-DROP PATTERN (the strcmp #4667 analogue): strcmp #4667 used '#define strcmp hxlcl_strcmp' to route non-shim callers; the heap family uses the malloc-family sysheaders rename in runtime_core_sysheaders.h (stage_resolve_runtime_a:1500) to route runtime_core.c's whole malloc/free/calloc/realloc family through the shim — this is what closed the darwin foreign-pointer SIGABRT (#4599/#4614 heap-family ABI split -> F2 magic-tag fix #4620/#4621, convergence hxlcl-realloc-arm64-s-1).",
        "SITES: tool/stage_resolve_runtime_a:2010 (FREE gate) / :2016 (realloc-couples-free-off) / :2201 (CALLOC seed-consume) / :2598 (REALLOC F2 dual -D) / :1500 (sysheaders malloc-family rename = caller-drop). self/runtime_core_hxlcl_shim_emit.hexa:190 (free #ifndef 3-arm body) / :227 (realloc) / :282 (calloc). Seeds: self/native/hxlcl_calloc_{x86_64,arm64-linux,arm64}.s (#4556).",
        "STATUS NUANCE: the resolver gates are already default-'auto' (graceful-ON) in-tree since #4548/#4556/#4620, so FLIP-6 is really a VERIFY-AND-CONFIRM round (capture the 3-target faithful nm-DROP + close any residual foreign-pointer 'free' UND) rather than a toggle edit. No mini-authorable byte-neutral code change remains — it is pool measurement."
      ],
      "gate": [
        "nm-UND __libc_calloc AND __libc_free drop to 0 on the canonical Linux runtime.a (faithful-nobaseline advisory dump, .github/workflows/nobaseline-gate.yml #4360) — the existence proof, with ZERO new libc UND introduced (native bodies add no UND: arena bump + magic-tag over hexa carriers).",
        "byteeq 3-target GREEN (x86_64-linux + arm64-linux + darwin-arm64); the merged mechanism is byte-neutral with gates OFF (archive byte-identical), so the DROP is the bit-changing flip that must reconverge gen3==gen4.",
        "faithful BUILD execution GREEN on darwin AND linux (the SIGABRT witness class from #4599/#4614) + a malloc->calloc->free->realloc round-trip smoke (foreign-vs-family magic-guard correctness).",
        "install.sh consumer smoke 3/3 — never promote on x86-only.",
        "pool-run only: aiden/summer (linux) + darwin host — mini is git/gh/read-only (SSOT confirms mini-immediate-landing = 0 for all reducible flips)."
      ],
      "wall": "After FLIP-6 (calloc+free) the canonical runtime.a nm-UND bottoms out at the SANCTIONED terminal set — no portable algorithm exists to remove these: (1) network/socket FFI (~11-15 syms: socket/bind/connect/accept/listen/recv*/send*/select/setsockopt/getsockopt/getaddrinfo/freeaddrinfo/gai_strerror + DNS resolver = vendor-FFI surface); (2) CRT bootstrap __libc_start_main (M8 irreducible seed #3959) + __errno_location/__stack_chk_*/_exit; (3) exec-family execve/execvp/posix_spawn (kernel-ABI leaf; Route-C native-emit #4263 exists default-OFF so technically flip-conditional but sanctioned absent a flip); (4) CUDA/vendor GPU __cudaRegisterFatBinary (opt-in HEXA_CUDA only, absent from default runtime.a). dl*×3 already structurally departed via S1 FFI-TU partition (#4481) — not a floor member. Literal `nm -u runtime.a` == emptyset is UNREACHABLE; the honest axis-② endpoint for 'no runtime.c' is the sanctioned-floor, distinct from the emitted-C-compile-step (axis-①) and clang-elimination (axis-③) walls which are substrate-walled (M8 + HexaVal-x86_64 pair-carry + setjmp jmp_buf + .s-assembly floor)."
    },
    {
      "item": "Scope the -fno-builtin-memcpy / -fno-builtin-memset flip readiness (compiler-synth native memcpy/memset body — the program-side perf lever, distinct from the already-landed axis-② runtime.a floor drop).",
      "ready": false,
      "risk_class": "bit-changing",
      "holds": true,
      "confidence": "high",
      "corrections": [
        "Non-blocking note: the fable design doc's own inline anchors (self/runtime_emit_full.hexa:395/651/698/725 and 2835-2853) are stale/line-drifted from its authoring commit; the task correctly uses the CURRENT verified anchors (409/673/720/747 and 2862/2877-2879), so the scope is more accurate than its cited source — no change needed.",
        "Minor precision: HEXA_RT_MEM_LIBC is not 'unimplemented' — it is an implemented runtime-side opt-OUT (runtime_core_sysheaders.h:123-140 + runtime_core_hxlcl_shim_emit.hexa #elif arms). The task's operative claim is nonetheless correct: it is runtime-only and there is NO program-side mem toggle."
      ],
      "edits_or_plan": [
        "CURRENT STATE — the fast native body ALREADY EXISTS (implemented, in-tree). #4366 overlapping-load/store size-dispatch bodies emitted at self/runtime_emit_full.hexa:673 (hxlcl_memcpy), :720 (hxlcl_memset), :409 (hxlcl_memcmp), :747 (hxlcl_memmove) — glibc/musl-style: n<=16 two overlapping word loads, n<=32 four-word, else byte loop; static __attribute__((noinline)).",
        "MEASUREMENT captured in-tree at self/runtime_emit_full.hexa:678: 'Measured 1.3-2.0x libc for n<=64 (summer clang-18, struct-copy regime), parity at large' — the naive-byte-loop perf-wall is FALSIFIED, matching CLAUDE.md.",
        "AXIS-② FLOOR DROP IS ALREADY DONE (not this task): the mem-leaf family (memcpy/memset/memmove/memcmp) already left the canonical linux runtime.a nm-UND floor via #4604, mechanism = static #4366 bodies + a textual override block '#define memcpy(d,s,n) hxlcl_memcpy(...)' at self/runtime_emit_full.hexa:2862/2877-2879, applied ONLY to the runtime.c TU. Confirmed state/zeroc-29-floor-to-zero-endgame-ssot-2026-07-03.md:31 (mem-leaf ✅ drop #4604 byteeq-3-target) and :40. Design rationale in state/hexa-own/memleaf_flip_design_fable.md.",
        "WHAT THIS TASK'S FLIP IS (the DISTINCT remaining lever): the SSOT explicitly calls it 'optional program-side -fno-builtin-memcpy (perf lever · NOT floor drop)' (endgame-ssot :45) and the fable design §4 names it the WAITING round: 'the final-binary / emitted-user-C floor: interposing the literal memcpy/memset symbols at final link + -fno-builtin on emitted-C compiles — that is the CLAUDE.md measured-viable next round, byte-changing for user binaries and perf-gated.' Today user programs' memcpy/memset (explicit + clang-synthesized from struct copies/array inits) still bind to LIBC — the #4366 bodies are static/TU-local to runtime.o and never reach the user TU; no #define block exists in the emitted user-C (self/build_c.hexa has none) and no external/interposing memcpy symbol exists (grep confirmed).",
        "EXACT FLIP SURFACE: self/main.hexa:3815 — the hexa build/run user-program compile line: compile = host_cc() + ' -O2 ' + omp_cflags() + _shared_cflags + os_clang_cflags() + _strict + cc_deep_nest_cflags() + '-I ...' + c_file + ' ' + _rt_input + ffi_dyn_objs(...) + ' -o ...' + os_clang_ldflags(). It carries NO -fno-builtin-mem*. The cflag helpers are os_clang_cflags() at self/main.hexa:1380 and cc_deep_nest_cflags() at :1368. (Existing -fno-builtin-memcpy/-memset usages — tool/stage_resolve_runtime_a:1214 _zeroc_nobuiltin, tool/build_native_linux_x86_64:64, tool/build_aprime.sh:808, tool/cross_build_* — are ALL runtime/self-host archive-build only; none touch the user-program line 3815.)",
        "REMAINING WORK to flip default-ON: (1) provide a non-static / interposing strong memcpy+memset symbol built from the #4366 dispatch body so user-program calls can bind to it (current bodies are static, invisible to the user TU) — either emit the dispatch body as strong 'memcpy'/'memset' at final link or add the #define override + external body into the emitted user-C via self/build_c.hexa; (2) add '-fno-builtin-memcpy -fno-builtin-memset' to the user compile at self/main.hexa:3815 (and the --target/cross variants nearby) so clang neither inline-expands to nor re-synthesizes the libc call, letting the interposed symbol win; (3) land behind an opt-in env toggle default-OFF first (byte-neutral) per HEXA-UNBOX/bit-changing discipline — no program-side mem toggle exists yet (HEXA_RT_MEM_LIBC is runtime-only, unimplemented for programs)."
      ],
      "gate": [
        "byteeq 3-target GREEN (x86_64-linux + arm64-linux + darwin) — bit-changing for user binaries, so gate is strict all-3-target, never x86-only",
        "real-workload perf ≥ libc: re-capture the small-copy 1.3-2.0x (n<=64 struct regime) + parity-at-large on the pool (summer/aiden), isolated micro not back-to-back, per measure-first discipline",
        "shipping/consumer smoke GREEN (install.sh exit42 + hello) + full faithful/self-host build execution on pool — per #4599 lesson byteeq+install-link prove NOTHING about runtime behavior, so a run that exercises the swapped user-program bodies is mandatory",
        "default-OFF byte-neutral merge first; flip default-ON only after all-3-target byteeq + measured perf GREEN"
      ],
      "wall": "Pool-measurement-blocked. The flip's perf gate (real-workload small-copy re-capture + full faithful build execution) requires the summer/aiden build+measure pool, which is the same wall that blocks the axis-③ ELF track this session pivoted away from (mini = git/gh/read only). The native body and axis-② floor drop are done; the program-side interpose symbol + -fno-builtin-on-user-compile + measured perf are unimplemented/unmeasured, so this is scoped-ready-to-implement, not ready-to-flip."
    }
  ]
}