{
  "ref_sha": "23130814b",
  "items": [
    {
      "item": "axis-① round-2 first step: natively lower ONE C-transpile-punting construct in compiler/lower/hir_to_mir.hexa (HX1102 match-pattern / HX1103 unknown-HExpr). Pick=literal_bool match pattern.",
      "ready": true,
      "risk_class": "byte-neutral",
      "holds": true,
      "confidence": "high",
      "corrections": [
        "Non-blocking precision: CORRECTNESS PIN claims the emitted STMT sequence is 'identical' to the HX1102 fallback, but the edit's _fresh_local label is \"lit_eq\" while the current fallback uses \"tag_eq\". This is a local temp debug name only — it does not affect .text bytes, byteeq, or semantics, and \"lit_eq\" is actually more correct (matches the literal_int arm). The compare stmt, operands, and branch topology are identical, so the safety argument stands.",
        "Non-blocking wording slip: the gate bullet says 'the 14 in-tree bool-arm files (self/test_match_*.hexa)'. 14 is the ARM count (grep ^\\s*(true|false)\\s*-> = 14), spread across 7 files: self/test_ir_match_enum_try.hexa, test_struct_patterns.hexa, test_match_advanced.hexa, test_match_enum.hexa, test_match_try_surface.hexa, test_type_checker_full.hexa, test_parser_full.hexa — not all are named test_match_*. Immaterial to the edit.",
        "Confirmed the ANCHOR is correctly a 2-line block precisely BECAUSE the 1-line `} else if pat.kind == \"struct_pattern\" {` is non-unique (it also appears at L5849, the binding dispatch). The full 2-line anchor with the comment `// Stage1 punch list C10 — struct-pattern. v0: take an` is unique to L5687-5688 (the TEST dispatch), so the Edit will target the correct site. No correction needed — noting the adversarial check passed."
      ],
      "findings_or_edits": [
        "CENSUS — HX1103 (unknown HExpr kind, default fall-through at hir_to_mir.hexa:6247 `if !_is_known_hexpr_kind(k)`): the full ExprKind catalog is ast_to_hir.hexa:256 `_expr_kind_tag` (28 kinds). Every one has a `_lower_hexpr` branch EXCEPT `for` — and `for` is desugared to `while` inside ast_to_hir (comment L52 'unique-id counter for for-desugar'; confirmed `grep '\"for\"' hir_to_mir.hexa` = 0 hits). Synthetic non-ExprKind carriers (`match_guard`, `field_lit`, `range`/`range_inclusive` binop-text) are all handled inline. NET: HX1103 is effectively UNREACHABLE for real programs — not a tractable target.",
        "CENSUS — HX1102 (unsupported match-pattern shape, the `else` branch at hir_to_mir.hexa:5702-5738 `_emit_hx1102(pat.kind, pat.span)`): match-arm test-dispatch (L5553-5738) handles exactly {wildcard, ident, enum_path, literal_int, literal_string, struct_pattern}. Match patterns are parsed by parser.hexa:1381 `parse_match_pattern`, which for a literal delegates to `parse_expr` → produces LiteralInt/LiteralFloat/LiteralString/LiteralChar/LiteralBool. RESIDUAL punting pattern kinds (parseable but NOT handled → hit HX1102): `literal_bool`, `literal_char`, `literal_float`.",
        "PUNT MECHANISM confirmed: HX1102 severity = Severity::Error (diag/catalog.hexa:160-166). main.hexa:849-861 drains `hir_to_mir_diags()` and, if `_has_errors`, prints 'aborting before codegen' + `exit(1)`. So a bool/char/float-literal match arm makes the native source→own-IR→native (leg-B) path ABORT, forcing the build onto the C-transpile delegate (hexa_cc.c / hexat). Lowering the shape natively removes the abort → axis-① fallback-count drops.",
        "PICK = `literal_bool` — most common + most tractable. Usage census across compiler/ stdlib/ self/: bool-literal arms (`^\\s*(true|false)\\s*->`) = 14 (e.g. self/test_match_enum.hexa:75-76, test_match_advanced.hexa:196, test_match_try_surface.hexa:65); char-literal arms = 0; float-literal arms = 0. literal_bool is the ONLY residual shape with real usage, and its lowering is a byte-for-byte clone of the existing literal_int arm (L5624-5654) with one operand-constructor swap.",
        "CORRECTNESS PIN: the constant MUST be `_const_bool_op(bv)` (kind=const_bool / TAG_BOOL, hir_to_mir.hexa:98-107), NOT `_const_int_op` — per the L90-97 note, `hexa_eq` returns false on a tag mismatch, so a TAG_INT `true` never compares equal to the TAG_BOOL scrutinee. This exactly reproduces what the current HX1102 fallback already lowers (`_lower_hexpr(pat)` → literal_bool expr branch L3188-3191 returns `_const_bool_op`), so the emitted MIR/STMT sequence is identical; the ONLY behavioral delta is that the HX1102 error (and its exit(1)) no longer fires.",
        "EXACT EDIT (compiler/lower/hir_to_mir.hexa) — insert a new else-if branch immediately BEFORE the struct_pattern TEST branch. ANCHOR (unique 2-line, at L5687-5688):\n            } else if pat.kind == \"struct_pattern\" {\n                // Stage1 punch list C10 — struct-pattern. v0: take an\nBECOMES:\n            } else if pat.kind == \"literal_bool\" {\n                // HEXA-OWN axis-① r2 — boolean-literal pattern `match b {\n                // true -> …, false -> … }`. Byte-for-byte clone of the\n                // literal_int arm (L5624) with the operand constructor swapped\n                // to _const_bool_op (TAG_BOOL) so hexa_eq tag-matches the bool\n                // scrutinee (see _const_bool_op note L90). Previously this shape\n                // fell to the HX1102 `else` below — a fatal S2 error that\n                // aborted the native own-IR path (main.hexa:854 exit(1)) and\n                // forced a C-transpile punt — even though the emitted compare is\n                // identical to the fallback. Lowering here drops the abort;\n                // byte-neutral (programs without bool-arms never reach this\n                // branch; the default C-transpile path never runs hir_to_mir).\n                let mut bv: i64 = 0\n                if pat.text == \"true\" { bv = 1 }\n                let mut ctx3 = _fresh_local(ctx2, pat.typ, \"lit_eq\")\n                let cmp_dst = _lr_locals[len(_lr_locals) - 1]\n                let mut cmp_args: [Operand] = []\n                cmp_args.push(scrut_op)\n                cmp_args.push(_const_bool_op(bv))\n                let cmp = Stmt {\n                    kind: STMT_BINOP,\n                    dst: cmp_dst,\n                    op: \"==\",\n                    args: cmp_args,\n                    target_block: -1,\n                    target_block_else: -1\n                }\n                ctx3 = _push_stmt(ctx3, cmp)\n                let mut br_args: [Operand] = []\n                br_args.push(_local_op(cmp_dst.id))\n                let br = Stmt {\n                    kind: STMT_BR_COND,\n                    dst: _miss_local(),\n                    op: \"\",\n                    args: br_args,\n                    target_block: arm_id,\n                    target_block_else: next_id\n                }\n                ctx3 = _push_stmt(ctx3, br)\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, arm_id)\n                ctx3 = _add_edge(ctx3, ctx3.cur_block, next_id)\n                ctx2 = ctx3\n            } else if pat.kind == \"struct_pattern\" {\n                // Stage1 punch list C10 — struct-pattern. v0: take an",
        "NO BINDING-SIDE EDIT NEEDED: a bool-literal pattern introduces no binders, so the arm-entry binding dispatch (L5744-5899) requires no new branch — it already no-ops for non-ident/enum_path/struct_pattern kinds. The guard-handling (L5907) and value-carry (L5928+) paths are pattern-kind-agnostic and unchanged. Optional hygiene (not required for function): the diag/catalog.hexa:165 HX1102 `explain` text lists the handled set — could append 'literal_bool' for accuracy, but it is prose-only and does not gate.",
        "OPTIONAL follow-on in same round if desired (both zero-usage today, so lower priority): `literal_char` arm — clone using `_const_str_op(pat.text)` (char literals are TAG_STR, per L3193-3204); `literal_float` arm — clone using `_const_float_op_with_text(to_float(pat.text), pat.text)` (per L3173-3183). Kept OUT of this step to keep the change minimal + to the single most-common shape."
      ],
      "gate": [
        "byteeq 3-target GREEN (x86_64-linux · arm64-linux · darwin-arm64): gen3≡gen4 fixpoint holds after the additive branch (change fires only on a previously-aborting input, so no prior byte output diverges).",
        "native-path lowers it: `hexa build/run` (native own-IR path, no --emit=c) on a probe `match b { true -> \"y\", false -> \"n\" }` compiles + runs to correct output WITHOUT the 'aborting before codegen' exit(1); pre-edit the same probe aborts with HX1102.",
        "fallback-count drops: HX1102 no longer emitted for bool-literal arms — grep the pool build log / lower-diag drain shows the HX1102 'literal_bool' occurrence count go to 0; the 14 in-tree bool-arm files (self/test_match_*.hexa) now lower natively instead of punting to hexat.",
        "existing self/test_match_enum.hexa + test_match_advanced.hexa + test_match_try_surface.hexa assertions ('match bool true'=='yes' etc.) still pass under the native backend.",
        "byte-neutrality spot-check: a compiler/stdlib source file with NO bool-literal match arm produces byte-identical MIR/object pre- vs post-edit (new branch never executes)."
      ],
      "wall": "After literal_bool lands, the residual HX1102 pattern shapes are literal_char + literal_float (both 0 in-tree usage today → next-round, low priority) and future not-yet-parsed shapes (ranges, or-patterns, tuple patterns — parser doesn't emit them yet, so not reachable). HX1103 is effectively unreachable (only `for`, which is desugared upstream) — so the axis-① residual-punt frontier after this step is the C-transpile-only constructs OUTSIDE the match/HExpr dispatch, not more pattern shapes. Next round should re-census which whole `.hexa` files still force the native path to abort (run the full native build over compiler/+stdlib and collect the remaining HX11xx/native-only-abort set) to pick the next-most-common punt."
    },
    {
      "item": "hexa_ld ELF x86_64 round-1 spec: link a native x86_64 .o into a running static ET_EXEC via the in-tree ELF linker (0 binutils ld/as), coupled to axis-② own-start (no crt1.o/-lc).",
      "ready": false,
      "risk_class": "byte-neutral",
      "holds": false,
      "confidence": "high",
      "corrections": [
        "EDIT A off-by-one (load-bearing miscompile): the own-start stub is 14 bytes, not 13 — E8 00 00 00 00 (5, call) + 89 C7 (2, mov edi,eax) + B8 3C 00 00 00 (5, mov eax,60) + 0F 05 (2, syscall) = 5+2+5+2 = 14. The spec's 'Prepend a 13-byte _start stub' and 'Shift every obj symbol.value and every reloc.offset by +13' are both wrong; the shift MUST equal len(stub)=14. Following +13 literally places main at text offset 13 (the second byte 0x05 of the syscall, since syscall occupies offsets 12-13) and skews every symbol/reloc by one -> miscompiled binary. Correct: 14-byte stub, shift +14, and 'call main' rel32 = main_post_shift_value(14) - 5 = 9. The rel32 formula '(main_post_shift_value - 5)' itself is correct once the shift constant is fixed to 14.",
        "All other spec claims verified TRUE against 23130814b: cited line numbers, EXISTS/MISSING/WALL mechanisms, dead-scaffold (zero production callers of link_elf_x86_64 + serialize_elf_exec_x86_64), entry_off==0 gate @842, PC32 single-segment reloc @814-829, text-only exec serializer @1115, and EDIT B anchors (@811/@1047/@1212/import@78) with the hexa-linker block correctly preceding the ld exec fork @1234. Only the EDIT A byte-count constant is defective.",
        "Minor (non-blocking) citation nit: EXISTS point (2) attributes pack_lir wiring to 'main.hexa:1048' but 1048 is serialize_elf_x86_64(elf_obj); pack_lir_x86_64(lmodule) is at 1047. Does not affect the mechanism."
      ],
      "findings_or_edits": [
        "EXISTS (emit primitives, all in compiler/emit/elf_x86_64.hexa): (1) x86_64 instruction encoder encode_x86_64_insn @:332 (MOV r32/r64-imm, SYSCALL, RET, CALL, JMP/Jcc, lea rip-rel _ex86_lea_rip_rel @:1213). (2) pack_lir_x86_64(lm) @:1487 — FULL LModule->ElfX86Obj: .text + .rodata(.LCstrN pool) + .data(16B g<id> HexaVal slots) + .bss, symbols, and BOTH reloc kinds: R_X86_64_PLT32 (CALL, addend -4, with intra-module pre-patch @:1652) and R_X86_64_PC32 (data lea, addend -4). ElfX86Obj/ElfRel/ElfSym structs @:53-84. (3) serialize_elf_x86_64(obj) @:856 — complete ET_REL .o (header+.text+.rela.text+.rodata+.data+.bss+.symtab+.strtab+.shstrtab, STN_UNDEF idx0, sh_info=nlocal+1). This is the ONLY link/emit fn already wired into the driver (main.hexa:1048).",
        "EXISTS (link/exec primitives, PROVEN ON REAL LINUX via test corpus but DEAD in the driver): (4) serialize_elf_exec_x86_64(text) @:1115 — minimal static ET_EXEC, single PT_LOAD (p_offset 0, p_vaddr 0x400000, PF_R|PF_X), e_entry=0x400000+code_off(120), TEXT-ONLY (drops rodata/data/bss). (5) link_elf_x86_64(objs,out_path,entry) @:736 — multi-obj: concat texts, build global def-symbol map, resolve entry, patch PLT32/PC32 relocs by S+A-P as pure text offsets, then serialize_elf_exec_x86_64. Both proven end-to-end: compiler/test/macho_p0_corpus/run_F_P2_LINUX_EXIT.hexa (single-obj exit(42) runs on ubu-2) and run_F_P2_MULTIOBJ_RUNEQ.hexa (2-obj PLT32 CALL reloc, entry '_main', runs). grep confirms link_elf_x86_64 + serialize_elf_exec_x86_64 have ZERO callers outside compiler/test/** — they are dead scaffold in production.",
        "MISSING #1 (the gating piece — driver wiring): compiler/main.hexa NEVER calls link_elf_x86_64 or serialize_elf_exec_x86_64. For target x86_64-linux-gnu the exec link ALWAYS forks binutils: main.hexa:1189-1194 builds `ld ... /usr/lib/x86_64-linux-gnu/crt1.o -lc -dynamic-linker /lib64/ld-linux-x86-64.so.2`. The `linker_kind==\"hexa\"` block @:1212 wires ONLY arm64-apple-darwin (Mach-O tool/hexa_ld.hexa); x86_64 hits the fallback warning @:1229 and forks system ld. So even with --backend=native (which already skips `as`: native_obj_ready path @:1114-1123 writes obj bytes, no as fork), the LINK still shells binutils ld. Wiring this in-process is round-1's core.",
        "MISSING #2 (own-start entry — the axis-② coupling): link_elf_x86_64 @:842 REQUIRES entry_off==0 (returns rc=4 otherwise) i.e. the entry fn must be text-offset 0. A normal compiled `fn main()->int` (a) is not guaranteed first in lm.funcs and (b) ends in `ret`, which without crt1.o's _start has no return address on the SysV initial stack -> crash. To link with NO crt1.o/-lc you must synthesize an own `_start` at offset 0 that calls main then does the exit syscall directly. This is exactly axis-②'s own-start applied to the native-emit path (the emitted-C floor's HEXA_ZEROC_OWN_START/_hx_start_c in tool/restore_frozen_seeds is the parallel C-runtime track, per state/hexa-own/flip7_environ_multiobj_design_fable.md; the native ELF path has no own-start yet).",
        "MISSING #3 (the WALL for hello-world-WITH-string — round-2, not round-1): serialize_elf_exec_x86_64 is text-only (no .rodata/.data/.bss PT_LOAD), and link_elf_x86_64's PC32 data-reloc math @:814-829 places the target at `base + s.value` treating a .rodata offset as a .text offset (single-segment assumption) — cross-segment vaddr is not modeled, so any print(\"..\") lea rip->rodata resolves to garbage. Therefore round-1's runnable target must have ZERO data refs (exit-code program), and multi-segment exec layout is the named next round.",
        "ROUND-1 EDIT PLAN (smallest runnable end-to-end = exit-code program, opt-in --backend=native --linker=hexa on x86_64-linux): EDIT A (compiler/emit/elf_x86_64.hexa): add `pub fn link_elf_x86_64_ownstart(objs, out_path, main_name) -> Int` — a thin own-start wrapper over link_elf_x86_64's body. Prepend a 13-byte `_start` stub at text offset 0: `E8 00 00 00 00`(call main, rel32 placeholder) + `89 C7`(mov edi,eax = forward main's return as exit code) + `B8 3C 00 00 00`(mov eax,60 sys_exit) + `0F 05`(syscall). Shift every obj symbol.value and every reloc.offset by +13 during concat, resolve the `call main` rel32 to (main_post_shift_value - 5) intra-object, force entry_off=0 (the stub), then serialize_elf_exec_x86_64(text). Keep the existing pure link_elf_x86_64 unchanged (the multi-obj test still uses it). Encode the stub via encode_x86_64_insn where possible to stay reference-consistent. EDIT B (compiler/main.hexa): in the native x86 exec branch @:1046-1064, also capture the ElfX86Obj (add module vars `let mut elf_obj_x86: ElfX86Obj` + `elf_obj_x86_ready=false` near native_obj_bytes @:811, set from pack_lir_x86_64(lmodule) @:1047). Then in the `linker_kind==\"hexa\"` block @:1212 add an x86_64-linux-gnu arm BEFORE the arm64-darwin one: `if target==\"x86_64-linux-gnu\" && elf_obj_x86_ready { let rc = link_elf_x86_64_ownstart([elf_obj_x86], out_path, \"main\"); if rc!=0 {exit(2)}; exit(0) }` — in-process link+write_bytes, exit(0) before ANY as/ld fork. elf_x86_64.hexa is already imported at main.hexa:78.",
        "ROUND-1 GATE / MEASUREMENT (pool aiden/summer, hexa built >=23130814b — not stale installed): `fn main()->int { return 42 }` -> `hexa build --backend=native --linker=hexa --target=x86_64-linux-gnu t.hexa -o t` -> (1) `file t` = ELF 64-bit LSB executable, statically linked; (2) `./t; echo $?` == 42 on real x86_64-linux; (3) `strace -f -qq -e trace=execve` shows ZERO execve of `as` OR `ld` (the killed-tool proof); (4) FALSIFIER to check first: confirm `return 42` from main emits NO builtin CALL (no hexa_add/box) so text is self-contained with 0 UNDEF externs — if it does, the round-1 target narrows to a body the encoder fully self-contains, and runtime-.o multi-obj linking becomes an explicit round-1b dependency. Default shipping path (no --linker=hexa) is byte-identical -> untouched."
      ],
      "gate": [
        "Native x86_64 .o (pack_lir_x86_64 output) links to a running static ET_EXEC via in-tree link_elf_x86_64_ownstart, with 0 binutils `ld` AND 0 `as` execve (strace-verified), no crt1.o / no -lc / no dynamic-linker — coupled to a synthesized own `_start` entry (axis-② own-start on the native-emit path).",
        "Concretely GREEN when: `hexa build --backend=native --linker=hexa --target=x86_64-linux-gnu` on `fn main()->int{return 42}` produces a statically-linked ELF ET_EXEC that runs on real x86_64-linux and returns 42, with strace showing zero as/ld forks."
      ],
      "wall": "Round-1 is exit-code-only (no data refs). WALL / named round-2 = hello-world WITH a string: serialize_elf_exec_x86_64 is text-only and link_elf_x86_64's PC32 data-reloc math (elf_x86_64.hexa:814-829) uses a single-segment (text-offset) assumption, so a print(\\\"..\\\") lea rip->.rodata resolves to garbage. Round-2 = multi-segment static-exec layout — add .rodata/.data/.bss PT_LOAD segments to the exec serializer at distinct page-aligned vaddrs + fix PC32 relocs to compute S against each section's real segment vaddr (not a flat text offset). Round-1b sub-dependency if `main` calls any runtime builtin: the native runtime must be own-emitted as an ELF .o and multi-obj-linked (UNDEF externs -> real defs), which also pulls in the emitted-C own-start (HEXA_ZEROC_OWN_START/_hx_start_c) coupling. Deep wall (unchanged, only gates CUDA/dlopen/net variants, not the base static binary) = dynamic ELF output (PT_INTERP, DT_NEEDED, PLT/GOT, symbol versioning)."
    },
    {
      "item": "Adversarial review of hexa_ld .a-archive ingest (#4684) at tool/hexa_ld.hexa",
      "ready": true,
      "risk_class": "review",
      "holds": true,
      "confidence": "high",
      "corrections": [],
      "findings_or_edits": [
        "[CONFIRMED · HIGH] parse_archive member-walk overshoots by name-length for every BSD extended-name (#1/N) member. tool/hexa_ld.hexa:528 computes `next = data_off + sz`, but data_off was already advanced by `nm` at line 514 (`data_off = data_off + nm`). For a #1/N member the true next header is `off + 60 + sz` (sz already INCLUDES the N name bytes), so `next = data_off + sz = off + 60 + nm + sz` overshoots by exactly `nm`. FAILING SHAPE: a macOS ar/libtool -static archive whose members carry #1/N long names — runtime.a qualifies (e.g. `runtime_cuda_host.o` = 18 chars > 16 → ar/libtool emits #1/ extended names for the whole archive). After the FIRST extended-name member, `off` lands `nm` bytes into the following member; the 60-byte header re-read at that bogus offset yields a garbage decimal `sz`, the `_r_u32` magic almost never equals 0xfeedfacf so the member is silently skipped, and `next = data_off + garbage_sz` desyncs further → most/all members after the first are DROPPED. Manifest result: link fails with a flood of 'Undefined symbols' (or, on an unlucky bogus magic match, exit(2)). Only single-member or short-name-only archives happen to work. FIX: `next = data_off + data_sz` (= off+60+nm + (sz-nm) = off+60+sz) or compute from the original header: `next = off + 60 + sz`.",
        "[PLAUSIBLE · LOW] data_sz underflow aborts the whole link instead of skipping. tool/hexa_ld.hexa:515 `data_sz = sz - nm` goes negative when nm > sz. The copy loop at 521 (`while m < data_sz && ...`) then produces an empty member, and parse_macho_from_bytes([]) hits n<32 at line 267 → exit(2), killing the entire link for one malformed member. This is reachable on a genuinely malformed archive, and is ALSO the likely crash mode once finding #1 desyncs the walk and feeds garbage nm/sz into a header that happens to pass the 0xfeedfacf magic check. A defensive `if data_sz < 0 || data_off + data_sz > n { advance-and-continue }` before the magic check would make ingest robust rather than fatal.",
        "[PLAUSIBLE · LOW / by-design-deferred] pull-all-members with no strong-def dedup. The main dispatch (2551-2556) pushes EVERY Mach-O member; link_macho_arm64 pushes each defined symbol into dsym_names/def_names unconditionally (896, 960) and every downstream resolver linear-scans first-match-with-break (984, 1035, 1090, 1209, 1574, 1666, 1800). If runtime.a defines a symbol in two members, both function bodies are emitted into slot0 and all references bind to copy-1 (copy-2 becomes dead weight) — bloat / non-canonical output size, NOT a miscompile, and no 'duplicate symbol' diagnostic is produced (only sect==255 commons are deduped at 921-935, strong+strong is silently accepted). The code comment at 476-478 already flags dedup + on-demand pull as a later round; noting it as a correctness-adjacent robustness gap, not a blocker.",
        "[NO FINDING · verified guarded] _r_u32 magic check at 518 is properly bounded by `data_off + 4 <= n`, so it never reads past buf end even when finding #1 pushes data_off toward EOF. And the walk cannot infinite-loop: `next = data_off + sz` with data_off >= off+60 and sz>=0 advances by >=60 every iteration even for sz==0, so `while off + 60 <= n` always terminates. parse_macho_from_bytes is byte-identical to parse_macho_obj for a standalone .o (parse_macho_obj just does read_file_bytes then delegates; body is offset-relative to buf) — no divergence."
      ],
      "gate": [
        "Add a multi-member .a corpus fixture built with macOS ar/libtool where >=1 member uses a BSD #1/N long name (>16-char member filename) and is followed by >=1 more Mach-O member; assert parse_archive recovers the FULL member count and every member's defined symbols, and that a full link over it resolves with zero undefined symbols (directly catches finding #1).",
        "Add a malformed-member fixture with an #1/N header where the name length nm exceeds the ar_size sz; assert parse_archive skips it gracefully and continues (no exit(2), remaining valid members still ingested) — catches finding #2.",
        "Add a duplicate-strong-def archive (same symbol defined in two members); assert deterministic first-def resolution, executable still links/runs, and (once dedup lands) that copy-2 body is not emitted — catches finding #3 and pins the pull-all behavior.",
        "Add a byte-parity assertion: link `foo.o` standalone vs link `ar rcs lib.a foo.o` (single member) and require identical executable output, pinning parse_macho_from_bytes == parse_macho_obj."
      ],
      "wall": "Next round: fix the member-walk arithmetic at hexa_ld.hexa:528 (`next = data_off + data_sz`, or `off + 60 + sz`), add the extended-name multi-member archive corpus above, and only then run the pool exercise. Follow-on rounds (already flagged in-code): graceful skip on data_sz<0 / out-of-range member, on-demand symbol-table (__.SYMDEF) member pull, and strong-def duplicate dedup + a duplicate-symbol diagnostic."
    }
  ]
}