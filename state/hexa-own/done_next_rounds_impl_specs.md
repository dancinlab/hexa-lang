{
  "ref_sha": "bdd81399f",
  "rounds": [
    {
      "round": "axis-③ round-2 — wire --linker=hexa (tool/hexa_ld.hexa) for darwin-arm64 exec link (opt-in, default-OFF)",
      "ready": true,
      "risk_class": "byte-neutral",
      "edits_apply": true,
      "confidence": "high",
      "corrections": [],
      "edits": [
        "compiler/main.hexa:164 — help text lockstep. OLD (verbatim):\n    println(\"  --linker=KIND          hexa | system     (default: system; hexa_ld not yet landed)\")\nNEW:\n    println(\"  --linker=KIND          hexa | system     (default: system; hexa=in-tree hexa_ld, arm64-darwin opt-in via $HEXA_LD)\")",
        "compiler/main.hexa:1151-1153 — replace the warn-stub with real hexa_ld dispatch for darwin-arm64. OLD (verbatim, exactly these 3 lines):\nif linker_kind == \"hexa\" {\n    eprintln(\"hexa-compiler: warning: --linker=hexa not yet implemented; falling back to system\")\n}\nNEW:\nif linker_kind == \"hexa\" {\n    if target == \"arm64-apple-darwin\" {\n        // axis-③ track-2 (opt-in, default-OFF): link via the in-tree Mach-O\n        // arm64 linker tool/hexa_ld.hexa instead of forking ld64 + xcrun.\n        // hexa_ld emits MH_EXECUTE + MH_PIE, LC_LOAD_DYLIB libSystem +\n        // chained fixups for extern refs, LC_MAIN(_main), then ad-hoc\n        // codesigns in-tool (codesign is a sanctioned OS-Gatekeeper leaf).\n        // Gate = 0 ld64/xcrun execve on a --linker=hexa build; behavior\n        // parity vs --linker=system over a corpus.\n        // Resolution: $HEXA_LD (explicit prefix; a prebuilt hexa_ld binary\n        // or \\\"hexa run tool/hexa_ld.hexa\\\") else `hexa_ld` on PATH. Overrides\n        // the system ld_cmd built above; the shared exec + `test -f OUT`\n        // success check below is unchanged (hexa_ld writes out_path on success).\n        let mut _hexa_ld = env(\"HEXA_LD\")\n        if len(_hexa_ld) == 0 { _hexa_ld = \"hexa_ld\" }\n        ld_cmd = _hexa_ld + \" -o \" + out_path + \" \" + obj_path + \" --lc-main _main 2>&1\"\n    } else {\n        eprintln(\"hexa-compiler: warning: --linker=hexa is only wired for arm64-apple-darwin (target=\" + target + \"); falling back to system ld\")\n    }\n}"
      ],
      "gate": [
        "Build compiler at bdd81399f on a darwin-arm64 host (ghost = darwin arm64 selfhost; mini forbidden for heavy build). Also build a hexa_ld binary once, or set HEXA_LD='hexa run tool/hexa_ld.hexa'.",
        "KILLED-TOOL measurement (the round's deliverable): sudo dtruss -f -t execve on `hexa run compiler/main.hexa --target=arm64-apple-darwin --emit=exec --linker=hexa -o /tmp/out /tmp/s.hexa`, filter execve args for ld64/xcrun -> MUST be 0 (SIP note: dtruss needs csrutil-relaxed or use an execve-logging PATH-shim / fs_usage as fallback). Baseline `--linker=system` run shows ld64+xcrun >=1.",
        "Behavior parity: OFF (--linker=system) vs ON (--linker=hexa) binaries over a small darwin corpus -> identical stdout + rc; /tmp/out actually runs.",
        "Release integrity: default `hexa build` / `--emit=exec` path is byte-identical (linker_kind defaults to system; new branch is dead unless --linker=hexa passed) -> byteeq 3-target GREEN + install.sh consumer smoke stay unchanged/GREEN; no default-path flip this round.",
        "CHANGELOG.jsonl entry same-change (main.hexa is a .hexa edit -> changelog gate); no-hardcode lint: `hexa_ld` is a bare binary name + HEXA_LD env, not a path/URL/host (advisory, grandfather if flagged)."
      ],
      "wall": "\"as-fork still present on the darwin exec path (compiler/main.hexa:1075-1080) — the --emit=exec auto-flip at :508-512 covers only emit_kind==obj, so obj is still as-produced; to reach true 0-external, couple next round with an exec-path native-obj-emit flip (extend the flip to emit_kind==exec or emit native Mach-O inline before link). NEXT: `hexa build` DX proper routes through self/main.hexa cmd_build, which on darwin uses --emit=asm + clang (self/main.hexa:3251-3256) and does NOT touch compiler/main.hexa's linker — forwarding --emit=exec + --linker=hexa (or a HEXA_LINKER env) from cmd_build is the follow-on to make `hexa build` (not just direct compiler invocation) reach hexa_ld. RESIDUAL: codesign remains an execve inside hexa_ld (sanctioned OS-Gatekeeper leaf, like the CUDA/net dynamic-link leaves). MATURITY WALL: hexa_ld is Phase-H inc4, run-verified only for `bl _write` (single libSystem import) — real programs referencing many libSystem symbols may hit fixup/encoder gaps; the corpus parity gate surfaces them and any gap becomes round-3's ordered worklist (failure mode is still a win).\""
    },
    {
      "round": "axis-② strcmp default-ON flip (#4667 follow-up) — route the standalone runtime_core.o range_field/map_get_field key-compare to native hxlcl_strcmp, coupled to the RT-NATIVE-STRCMP seed adoption, dropping the last `strcmp` nm-UND from the shipped runtime.a",
      "ready": true,
      "risk_class": "bit-changing",
      "edits_apply": true,
      "confidence": "high",
      "corrections": [
        "Non-blocking wording nit in EDIT 1's inserted comment: it states 'every other compare is the self-contained rt_strcmp', but in the emitted runtime_core.c the other key-compares are overwhelmingly hxlcl_strcmp (which in the standalone S3 runtime_core.o is an external 'U hxlcl_strcmp' resolved from the seed/shim member, not self-contained), and rt_strcmp only appears under a separate flag (emitter line 771-781, 4817/4828). The load-bearing claim that line 4256 is the ONLY direct libc strcmp( in runtime_core.c is TRUE (grep confirms a single hit), so this does not block the edit.",
        "Verified true (not a correction): all three OLD texts match verbatim at HEAD bdd81399f — EDIT1 L1503 (8-sp indent, unique), EDIT2 L1910-1911 (24-sp indent, inside the _rnsc_mode!=0 -> seed-adoption else branch), EDIT3 L3069 anchor unique. risk_class 'bit-changing' is correct given seed default=auto + baked ~49KB seeds. NO-EDIT anchors S2:3064/S4:3082/CUDA-caseB:3132-3133/single-TU:3152 and emitter gates runtime_core_emit.hexa:4253-4257 + runtime_emit.hexa:1049-1054 all confirmed present."
      ],
      "edits": [
        "EDIT 1 — tool/stage_resolve_runtime_a:1503 (declare the coupling variable in the same MULTIOBJ scope as _mo_shim_def/_mo_rtcore_def, so it is empty by default = byte-neutral). OLD (line 1503, verbatim):\n        local _mo_rtcore_def=\"\"\nNEW (append the declaration + rationale immediately after that line):\n        local _mo_rtcore_def=\"\"\n        # axis-② strcmp caller-drop (#4667 follow-up flip · default-ON, COUPLED to the\n        # RT-NATIVE-STRCMP seed adoption at ~L1910). -DHEXA_RT_RANGE_STRCMP_NATIVE routes the\n        # standalone runtime_core.o (S3) hexa_map_get_field range key-compare\n        # (self/runtime_core_emit.hexa:4256 — the ONLY direct strcmp( in that TU; every other\n        # compare is the self-contained rt_strcmp) from libc strcmp to native hxlcl_strcmp,\n        # dropping runtime_core.o's last `strcmp` nm-UND. SET only when the strcmp native seed\n        # is adopted (hxlcl_strcmp becomes the native word-scan, not the shim :464\n        # `return strcmp` libc delegate) so the pair moves together; empty (seed-fallback /\n        # SELFEMIT-smoke RT_NATIVE_STRCMP=0 / non-adopting target) => range gate OFF =>\n        # runtime_core.o byte-identical to today. convergence axis-2-strcmp-caller-drop-flip.\n        local _rng_strcmp_def=\"\"",
        "EDIT 2 — tool/stage_resolve_runtime_a:1911 (set the coupled range def at the exact point where the strcmp native seed is adopted; anchored on the unique $_rnsc_o member line). OLD (lines 1910-1911, verbatim, 24-space indent):\n                        _mo_shim_def=\"$_mo_shim_def -DHEXA_RT_NATIVE_STRCMP\"\n                        _mo_hxlcl_members=\"$_mo_hxlcl_members $_rnsc_o\"\nNEW (append the coupling line after the member add):\n                        _mo_shim_def=\"$_mo_shim_def -DHEXA_RT_NATIVE_STRCMP\"\n                        _mo_hxlcl_members=\"$_mo_hxlcl_members $_rnsc_o\"\n                        _rng_strcmp_def=\"-DHEXA_RT_RANGE_STRCMP_NATIVE\"  # axis-② flip: couple range_field caller-drop to strcmp-seed adoption (both ON => strcmp fully drops: runtime_core.o->hxlcl_strcmp + shim native seed member)",
        "EDIT 3 — tool/stage_resolve_runtime_a:3069 (emit $_rng_strcmp_def into the S3 standalone runtime_core.o compile — the ONLY TU that still carries a direct strcmp; expands to nothing when empty = byte-neutral OFF; anchored on the unique '$_mo_rtcore_def -I self -include self/runtime_core_sysheaders.h' fragment). OLD fragment (within line 3069):\n-DHEXA_ZEROC_DROP_RTCORE $_mo_rtcore_def -I self -include self/runtime_core_sysheaders.h -c self/runtime_core.c\nNEW fragment:\n-DHEXA_ZEROC_DROP_RTCORE $_mo_rtcore_def $_rng_strcmp_def -I self -include self/runtime_core_sysheaders.h -c self/runtime_core.c",
        "NO EDIT to S2 (:3064), S4 shim (:3082), CUDA-caseB (:3132), or single-TU (:3152): S2/single-TU amalgam already route range/map strcmp to hxlcl_strcmp via the runtime_emit_full.hexa:2863 `#define strcmp(a,b) hxlcl_strcmp(...)` (S2 drops the runtime_core.c include so it does not even contain hexa_map_get_field); the shim TU has no range_field; the CUDA-caseB archive REUSES the same build/runtime_core.o built at S3 (:3133 ar's build/runtime_core.o), so EDIT 3 covers CUDA. Adding the def elsewhere is a pure no-op.",
        "NO EDIT to the emitters (self/runtime_emit.hexa:1049 / self/runtime_core_emit.hexa:4253): the #4667 #ifdef HEXA_RT_RANGE_STRCMP_NATIVE gate blocks are already in place at bdd81399f (self/runtime_core_emit.hexa:4253-4257 ON=hxlcl_strcmp / OFF=strcmp; self/runtime_emit.hexa:1049-1054 hx_range_strcmp macro). This round only flips the -D emission ON; no emitter change.",
        "NO EDIT to the shim resolver default (self/runtime_core_hxlcl_shim_emit.hexa:451/464): CONFIRMED already default-active — tool/stage_resolve_runtime_a:1879 `_rnsc_mode=\"${HEXA_RT_NATIVE_STRCMP:-auto}\"` defaults to auto, all three seeds self/native/hxlcl_strcmp_{x86_64,arm64-linux,arm64}.s are baked (49KB each), so the auto path adds -DHEXA_RT_NATIVE_STRCMP to _mo_shim_def and the native seed member, dropping the shim's :464 `int hxlcl_strcmp(...) { return strcmp(a,b); }` in favor of the native word-scan. The coupling in EDIT 2 fires precisely in this branch."
      ],
      "gate": [
        "byteeq 3-target GREEN with the flip active: gen3≡gen4 per target on {x86_64-linux, arm64-linux, darwin-arm64} via release_build (HEXA_RT_MULTIOBJ=1, HEXA_RT_NATIVE_STRCMP=auto) — build on the pool (aiden/summer/ghost), mini is git/gh only",
        "faithful nm-DROP: `nm -u build/runtime.a | grep -w strcmp` is EMPTY on all 3 targets (runtime_core.o now shows `U hxlcl_strcmp` not `U strcmp`; shim member is the native seed) — verify against the nobaseline-gate.yml advisory nm-UND floor dump (#4360)",
        "in-stage gates must stay GREEN under the flip: S5 `ld -r` multidef=0 (hxlcl_strcmp stays single-provider — the seed member) + OWN-START nm-clean (atexit/environ/__dso_handle unaffected)",
        "install.sh consumer smoke GREEN on all 3 targets: `hexa build`/`run` hello + exit42 rc=42 links the fresh runtime.a (release-integrity: user-facing path unbroken)",
        "SELFEMIT smoke unchanged: nobaseline-gate.yml step-(2) pins HEXA_RT_NATIVE_STRCMP=0, so _rng_strcmp_def stays empty there and runtime_core.o is byte-identical to today (no perturbation of the pinned smoke)"
      ],
      "wall": "Runtime.a bytes change: the standalone runtime_core.o's hexa_map_get_field emits `hxlcl_strcmp(key,...)` instead of `strcmp(key,...)` (byte-different .o), so this is BIT-CHANGING and pool-gated, not applied on mini. NOT byte-neutral (unlike #4667 which shipped default-OFF). The drop only completes when BOTH halves are ON: (a) runtime_core.o range caller -> hxlcl_strcmp (this flip), and (b) shim hxlcl_strcmp is the native seed (already default-active via auto + baked seeds); the EDIT-2 coupling guarantees they move together, so there is never a partial state where runtime_core.o routes to a hxlcl_strcmp that itself calls strcmp. NEXT ROUNDS: (1) after strcmp fully drops on the ship path, extend the coupling to the SELFEMIT-native config (couple _rng_strcmp_def to -DHEXA_RT_SELFEMIT_STRCMP too) so strcmp also drops in the darwin SELFEMIT smoke — deferred, needs its own multidef re-check; (2) the byte-scan seed is byteeq-safe, but a word-at-a-time (HASZERO) upgrade is a SEPARATE post-flip round (SIMD/IFUNC dispatch = same-binary-different-code nondeterminism hazard, per the :1867 note); (3) name the next nm-UND libc family to drop from the runtime_core.o floor after strcmp."
    },
    {
      "round": "regex-off-replace-zerowidth-eos-crash-fix (follow-on to regex_parity_corpus §4)",
      "ready": true,
      "risk_class": "bit-changing",
      "edits_apply": true,
      "confidence": "high",
      "corrections": [],
      "edits": [
        "EDIT 1 — guard the zero-width copy against off==L (mirror rt_regex_replace regex_rt.hexa:341). File: self/runtime_emit_full.hexa lines 14020-14026.\n\nOLD (verbatim):\n    buf = buf + \"        if (m.rm_eo == m.rm_so) {\\n\"\n    buf = buf + \"            // Copy one char and advance.\\n\"\n    buf = buf + \"            if (op + 1 >= cap) break;\\n\"\n    buf = buf + \"            out_buf[op++] = s[off];\\n\"\n    buf = buf + \"            off += 1;\\n\"\n    buf = buf + \"            continue;\\n\"\n    buf = buf + \"        }\\n\"\n\nNEW:\n    buf = buf + \"        if (m.rm_eo == m.rm_so) {\\n\"\n    buf = buf + \"            // Zero-width match: copy one char and advance. Guard off==L (a\\n\"\n    buf = buf + \"            // zero-width match on the empty tail, e.g. $ / a* / a? / (a|)):\\n\"\n    buf = buf + \"            // s[L] is the NUL terminator; copying it is wrong and advancing to\\n\"\n    buf = buf + \"            // L+1 underflows the tail below. Mirrors rt_regex_replace (regex_rt.hexa:341).\\n\"\n    buf = buf + \"            if (off < L) {\\n\"\n    buf = buf + \"                if (op + 1 >= cap) break;\\n\"\n    buf = buf + \"                out_buf[op++] = s[off];\\n\"\n    buf = buf + \"            }\\n\"\n    buf = buf + \"            off += 1;\\n\"\n    buf = buf + \"            continue;\\n\"\n    buf = buf + \"        }\\n\"\n\n(Note: emitted-C indentation goes 8->12 spaces for the inner statements now nested under `if (off < L)`. Line 14035's `if buf.len() > 65536 { _flush_runtime(...) }` sits BELOW this block (between edits) and is untouched.)",
        "EDIT 2 — wrap the final-tail memcpy in `if (off <= L)` (mirror hexa_regex_split :13985 + rt_regex_replace regex_rt.hexa:352). File: self/runtime_emit_full.hexa lines 14042-14050.\n\nOLD (verbatim):\n    buf = buf + \"    // Tail.\\n\"\n    buf = buf + \"    size_t tail = L - off;\\n\"\n    buf = buf + \"    if (op + tail + 1 >= cap) {\\n\"\n    buf = buf + \"        cap = op + tail + 1;\\n\"\n    buf = buf + \"        char* nb = (char*)realloc(out_buf, cap);\\n\"\n    buf = buf + \"        if (nb) out_buf = nb;\\n\"\n    buf = buf + \"    }\\n\"\n    buf = buf + \"    hxlcl_memcpy(out_buf + op, s + off, tail);\\n\"\n    buf = buf + \"    op += tail;\\n\"\n\nNEW:\n    buf = buf + \"    // Tail after the last match. Guard off>L: a zero-width EOS advance leaves\\n\"\n    buf = buf + \"    // off==L+1, so (L - off) underflows size_t. Mirrors hexa_regex_split's off<=L\\n\"\n    buf = buf + \"    // guard above and rt_regex_replace (regex_rt.hexa:352).\\n\"\n    buf = buf + \"    if (off <= L) {\\n\"\n    buf = buf + \"        size_t tail = L - off;\\n\"\n    buf = buf + \"        if (op + tail + 1 >= cap) {\\n\"\n    buf = buf + \"            cap = op + tail + 1;\\n\"\n    buf = buf + \"            char* nb = (char*)realloc(out_buf, cap);\\n\"\n    buf = buf + \"            if (nb) out_buf = nb;\\n\"\n    buf = buf + \"        }\\n\"\n    buf = buf + \"        hxlcl_memcpy(out_buf + op, s + off, tail);\\n\"\n    buf = buf + \"        op += tail;\\n\"\n    buf = buf + \"    }\\n\"\n\n(The subsequent `out_buf[op] = '\\\\0';` at :14051 stays; when off>L the tail block is skipped, op is unchanged, so the NUL terminator lands correctly at the accumulated op.)"
      ],
      "gate": [
        "Regression test (new file, self/test_regex_replace_zerowidth.hexa): exercise the previously-crashing nullable patterns via the regex_replace builtin and assert they return correctly (matching the ON rt_regex_replace output) WITHOUT segfault: regex_replace(\"$\",\"abc\",\"X\")==\"abc\"; regex_replace(\"a*\",\"b\",\"X\")==\"b\"; regex_replace(\"a?\",\"\",\"X\")==\"\"; regex_replace(\"(a|)\",\"ab\",\"X\") non-crashing; plus a non-nullable control regex_replace(\"a\",\"banana\",\"X\")==\"bXnXnX\" to prove the normal path is unchanged. Run the test against the OFF build (HEXA_REGEX_NATIVE=0) — the crashing arm — and confirm exit 0, then against the ON build (HEXA_REGEX_NATIVE=1) and confirm identical outputs.",
        "Build the OFF-path runtime on the pool (aiden / summer / ghost — mini is git/gh only) and run the regression test: assert NO SIGSEGV (pre-fix segfaults on regex_replace(\"$\",\"abc\",\"X\")).",
        "byteeq 3-target GREEN — x86_64-linux, arm64-linux, darwin-arm64: this changes the emitted runtime.c OFF `#else` body -> runtime.a bytes, so run the full 3-target byte-identical gate on the pool (self-hosted runners), not one-target-green.",
        "gen3 == gen4 self-host fixpoint holds (the emitter self/runtime_emit_full.hexa is inside the compile closure; the string-literal change is deterministic so the fixpoint must still converge).",
        "install.sh consumer shipping smoke GREEN + `hexa build`/`run` path unaffected (release integrity > self-host progress; do not promote on x86-only).",
        "Verify via a github-hosted PR-CI run if the local SSH pool is down; promote (finalize Latest) only on 3/3 GREEN + install.sh smoke GREEN."
      ],
      "wall": "This is the named byte-changing follow-on the regex_parity_corpus_portmap_fable.md §4 deferred (\\\"the crash itself is a genuine byte-changing fix in the frozen #else arm -> its own gated follow-on round, NOT this corpus PR\\\"). Next round after this lands GREEN: (1) re-admit nullable patterns (a*, a?, $, (a|)) into the regex_replace corpus rows in regex_parity_corpus_portmap_fable.md §6 item 5 / class table — the OFF oracle (a verbatim port of this body) now matches ON, so replace becomes parity-testable for nullable inputs instead of excluded; (2) the OFF-path body remains a RUNTIME-PORT target for the eventual HEXA_REGEX_NATIVE default-ON flip (native rt_regex_replace already carries the correct guards) — the ON shim is the canonical path, this fix only closes the legacy #else crash so the two arms stay byte-parity-clean until the flip."
    }
  ]
}