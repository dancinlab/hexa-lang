# ATLAS — theorem-atlas system upgrade campaign

@goal: `/gap` 40-렌즈 스윕에서 발굴한 아틀라스 업그레이드 갭(무결성·비용·구조·증거·시간)을 origin/main에 체계적으로 클로즈. 메타 신호 = 두 뿌리(① verify_cli↔atlas_cli 미러 이중화, ② 등록 시 동결·재검증 없음) 우선 해소.

SSOT = `compiler/atlas/embedded.gen.hexa` (런타임 TEXT-parse). 등록 = `hexa atlas register`. 검증 = bin/hexa-atlas 단일 툴 빌드(~5s) + 기능 테스트. 랜딩 = 격리 worktree → PR(auto-merge 훅).

## 진행 (milestones)

- [x] R1: register fold dedup(kind,id)+skip + atomic temp-rename write (#948)
- [x] R1: cite_missing_count audit 커버리지 메트릭 — 8194/8650 미인용 확정 (#945)
- [x] R1: 표현 일관성 CI — embedded↔n6-export 노드수 동등 게이트 (#946)
- [x] R1: static_atlas() 프로세스-수명 파싱 캐시 — 파싱 9→1/proc (#950)
- [x] R2: drill `--rounds≥2` SIGSEGV triage+fix + C5 novelty-fixpoint(net-novel 포화) 착지 — compiler/drill/*
- [x] R2: audit domain-coverage 축 — `:: <domain>` 파싱 → domain×kind 매트릭스 + 0-cell 플래그 (#958)
- [x] R2: wigner_stabilizer_sn 미러 발산 fix — atlas_cli register 미러를 verify_cli 실제 소수 sieve와 일치(d>13) (#957)
- [x] R3: re-verify-🟢 부활 — `hexa atlas reverify [--limit N]` read-only drift 리포트(35노드 중 3 DRIFT 실측). 동결 등록 verdict in-process 재계산 (META-SIGNAL ② 첫 슬라이스)
- [x] R3: lookup on-disk offset 색인 — 단일 `lookup <id>`가 16k-노드 풀파싱 회피, 해당 노드 1줄만 seek+파싱. byte-identical(47-id 샘플 PASS) + unhealthy-index 풀파싱 폴백. ~3.9x user-CPU 가속 (offset_index.hexa)
- [x] R4: calc_dispatch.hexa SSOT 모듈 신설 — float 멤버십 술어(`calc_is_float_fn` 99-fn · `calc_is_zero_arg_float_fn` 8-fn) + ε(`calc_eps`=1e-9)를 단일 홈으로 호이스팅. verify_cli·atlas_cli 양쪽 `use` (META-SIGNAL ① 멤버십+ε 슬라이스). 양 바이너리 LINK PASS(핵심 위험 해소) · reverify byte-identical(36/32/3/1) · verify --expr·register 동작 불변. R3 실측 36-fn 멤버십 갭(99 vs 63) 제거 — float fn 추가는 이제 verify↔atlas 미러 동기 불필요, calc_dispatch 1곳만 편집
- [x] R5: drift 보정 — R3 reverify가 surface한 3노드(`allen_dynes_tc`·`mcmillan_tc`·`bcs_gap_ratio`)가 6 sig-fig 반올림 리터럴로 등록되어 full-precision 재계산과 ε 초과(|Δ|=4e-6~1.8e-4). stale 노드 제거 후 `register --from-verify`로 full-precision 재등록(IEEE-754 double, 17 sig-fig). dedup(kind,id)이 skip-on-dup이라 사전 삭제로 clean REPLACE 보장. reverify `numerical_seen=35 match=35 drift=0` 달성 · 노드수 불변(16101) · 3노드 lookup full-precision 확인 (META-SIGNAL ② drift→재등록 첫 적용)
- [x] R5: falsifier 구조화 accessor + cascade 무효화 query — additive·read-only (compiler/atlas/cascade.hexa 신설). hexa 구조체 리터럴이 누락 필드를 default 안 함(codegen `missing_field` 컴파일 에러 실측) → `AtlasNode` 필드 추가는 16101 embedded.gen 리터럴 전부 재작성 강제(데이터 파일 금지) → **스키마 변경 0**. 대신 (1) `node_falsifier`/`node_has_falsifier` = `raw`의 `falsifier:`/`= `/`=` 연속줄을 구조화 추출(필드 없이 타입드 surface), (2) `cascade_candidates` = 기존 edge 모델(`<-`depends_on·`|>`verified_by·`==`equivalents·`->`derives) 재사용해 falsified-id 인용 노드 read-only 리스트(tombstone/PR 無 — RFC-017 §5e 중량 머신은 RETIRED discover/cascade.hexa에 격리 유지). 16101 노드 byte-identical 로드 PASS(`n`·`sigma` 룩업 불변) · 실데이터 cascade(`n`→26 노드 인용) · cascade_test 19/19 PASS
- [x] R5: active-acquisition 랭킹(Q/🟡/🟠) — `hexa atlas stats --acquisition [--top=N] [--scope] [--format=json]` 신규 read-only 축. 동결-but-미검증/open 노드를 composite priority(tier+domain-gap+kind)로 정렬한 "다음 검증/획득 프론티어" 출력. 실측 rodata 프론티어 2152 = 🟠 1709(cite-bearing·무인용·무재계산) + 🟡 360(인용·미재계산) + Q 83. 검증 불변식 PASS(서브카운트 합=총, 🟡⊆cite_present, 🟠⊆cite_missing, verified[*] 6583 제외, priority 단조감소). audit.hexa 소유 + atlas_cli 1-블록 dispatch wire
- [x] R6: cascade CLI verb 노출 — `hexa atlas cascade <id> [--format=json]`. R5 #1026 라이브러리 surface(`cascade_candidates`/`cascade_to_text`)를 atlas_cli에 wire(`use compiler/atlas/cascade` + cmd_cascade + 1-블록 dispatch + help). 정적 아틀라스를 1회 enrich(edge 백필) → 동일 리스트로 id 존재 확인 + cascade 질의. READ-ONLY(tombstone/mutate 無). 엣지케이스: unknown id → `# unknown id` + exit 1 / 0-citer → "no cascade candidates" / no-arg → usage exit 2. 실측 `cascade n`→26 노드 인용(R5 측정 일치) · `--format=json` well-formed(python json.load PASS) · 0-citer(`xpoll-n-material`) · unknown id 클린 메시지. cascade.hexa 무수정
- [x] R6: 신규역량 `hexa atlas diff <A> <B>` — atlas 판 git diff. (kind,id) 키 비교로 removed(only-A)/added(only-B)/changed(raw differs)/same 리포트. embed↔.n6-export 자동감지(`raw: "` 마커 유무) · `--format=json` · READ-ONLY. 정규 `parse_atlas_string`+`_extract_raw_blocks` 재사용(파서 미복제). self-diff=빈 diff(same N) + 1노드-수정 정확 감지(removed/added/changed 각 1) 실측 · 실 embedded.gen(16101) self-diff=0/0/0 · 회귀 무영향 (compiler/atlas/atlas_diff.hexa 신설 + atlas_cli cmd_diff 1-블록)
- [x] R6: proof-chain export — `hexa atlas proof <id> [--depth=N] [--format=json]` (compiler/atlas/proof_chain.hexa 신설). cascade의 **INVERSE**: cascade가 "A가 falsified되면 누가(다운스트림) A를 인용?"이면 proof는 dual "A는 무엇(업스트림)에 의존?"을 노드의 SUPPORTING 엣지(`<-`depends_on·`|>`verified_by·`==`equivalents)를 transitively 따라 증거 기반 export. 풀스캔 無 — depend 프론티어 따라 per-node lookup만. BFS + visited-set(cycle/diamond-safe) + depth-cap(-1=무제한·0=루트만·N=N홉). 노드 미해석 엣지(`== phi * n`·`|> verify_primitives.py` = bare expr/스크립트)는 UNRESOLVED leaf로 보고(crash 無). 각 링크에 verdict tier(🟢 verified/🟡 graded-but-unverified/🟠 unresolved-or-gap) → 체인이 🔵/🟢에 ground-out하는지 가시화. 실측: `sigma`→`n`(depends_on, 🟢)·equiv는 UNRESOLVED · leaf `n`은 자기만(+|> 스크립트 leaf) · multi-hop `probe_steps_cpgd`→`template_count`/`round_seed`→`n_axis_cell`(depth 2) · **diamond dedup**: `sigma_n`(←sigma,n)에서 sigma도 ←n이라 n이 2경로 도달하나 visited-set이 정확히 1회만 방출(cycle-safe 입증) · `--depth=0`=루트만·`--depth=1`=직접 기반만 · `--format=json` jq-valid · unknown id → `# not a node` + exit 1. proof_chain.hexa 코드본문 189줄(g4 내) + atlas_cli 1-블록 dispatch(cascade·diff verb KEEP). embedded.gen.hexa·cascade.hexa·atlas_diff.hexa 무수정

- [x] R7: numerology 격리 tier — `hexa atlas stats --numerology [--scope=rodata|overlay|merged] [--format=json]` 신규 read-only 격리 축. g5 honesty 루브릭이 실검증(🔵/🟢)과 미지지 주장을 구분하는데, NUMEROLOGY 노드(유도/인용 없이 무관 상수간 ≈/비율-등식 우연일치 주장)는 엄밀 노드와 silent-mix되면 안 됨 → 가시적 quarantine. **보수적 4-신호 AND 시그니처**(false-positive 평판 비용 高): (1) 헤더 CLAIM BODY(`id = ` 분리자 strip 후)의 우연일치 SHAPE(`≈`·숫자 비율-등식 `a/b=…`·숫자 `=…=…` 체인) — 토론용 `=>` 엣지의 "coincidence" 언급은 제외(그들은 우연을 반증) · (2) numerolog/coincidence/수치일치/coincidence_not_proof 키워드(일반 유도공식엔 부재) · (3) 무검증([*] 아님) · (4) 무인용(`cite =`·`|>` 無, `_node_has_cite` 재사용). 실측 rodata 16101 스캔 → **정확히 2 quarantine**: `MILL-PX-A3-ym-beta0-rewriting`(σ(6)-sopfr(6)=12-5=7, COINCIDENCE_NOT_PROOF 자기태그 [7]) + `MILL-V3-T4-n6-numerical-coincidence-honest-miss`(12/5=σ(6)/sopfr(6) post-hoc MISS [7]) — 양쪽 진짜 우연일치·무유도·무인용. **false-positive 0**: `id = ` 분리자 strip이 `…-non-applicability = n=6 … 연결 없음`(정직한 link-DENIAL [10])을 정확히 제외(strip 전엔 오탐) · clay falsifier/robustness 노드는 헤더 SHAPE 부재로 제외. `--format=json` jq-valid · 기존 audit/acquisition 모드 불변(프론티어 2150·entries 16101). audit.hexa 소유(`NumerologyReport`/`NumItem` + `_nu_*` 헬퍼, `_extract_domain`/`_node_has_cite`/`_raw_has_verified` 재사용) + audit_rodata 래퍼 2 + atlas_cli 1-블록 dispatch+help(verb KEEP)

## deferred (다음 라운드)

calc_dispatch recompute-helper 풀 이동(R4 잔여: `_recompute_float`의 ~99 per-fn 계산 helper ~2200줄 — g4 200-LOC·link-risk로 별도 PR; atlas register는 이미 `hexa verify --expr` shell-out 위임이라 register-side 미러는 dead-code, reverify만 live subset) · numerology tier 후속(키워드 사전 확장 · domain×tier 격리 매트릭스 · overlay 신규등록 게이트) · reverify 확장(비-F kind · drift→자동 재등록 옵션) · acquisition 랭킹 후속(domain×tier 매트릭스 · 프론티어→drill seed 자동 공급) · proof-chain 후속(`->`derives-source 역방향 스캔 통합 · 미해석 leaf 휴리스틱 노드-매칭) · 신규역량(MCP/API·시맨틱검색·auto-edge 추론)

## 방법

격리 worktree off origin/main → 최소 additive 편집 → `hexa parse` → bin/hexa-atlas 빌드 → 기능 테스트 → green일 때만 PR. disjoint 파일만 병렬 fan-out (머지 충돌 0). pool-route 훅이 worktree 빌드 거부 시 STOP+보고.
