// regex_rt_x86_64.s — FROZEN BOOTSTRAP SEED (RT-NATIVE zero-c #29 — regex-rt).
// GENERATED: tool/regen_regex_rt_native_s.sh — aprime_cc _drv.hexa --emit=asm
//   --target=x86_64-linux-gnu -o regex_rt_x86_64.s regex_rt.hexa thompson.hexa backtrack.hexa, then a
//   .globl DEMOTION post-pass keeping ONLY the 6 rt_regex_* shim globals.
//   Provides the 6 rt_regex_* (match/match_full/search/findall/split/replace)
//   the emitted runtime.c hexa_regex_* bodies delegate to under
//   HEXA_REGEX_NATIVE, backed by a Thompson NFA + backtrack VM. Every other
//   symbol is demoted to local so the seed exports a 6-symbol contract only
//   (thompson/backtrack are public stdlib modules — undemoted globals would
//   collide at ld with a user program that also imports them).
//   These leaves are gen2-native-only (the hexat C-transpile bootstrap cannot
//   lower them), so the body enters the shipped runtime.a ONLY via this seed.
//   ABI: ELF, rt_regex_* no underscore. External: hexa string/value/array runtime (resolved within runtime.a).
//   Lets stage_resolve_runtime_a define HEXA_REGEX_NATIVE (opt-IN, default-OFF)
//   + ar this .o into runtime.a so the regcomp/regexec/regfree seams route
//   native, dropping those libc symbols from the nm-UND floor on flip.
# hexa-lang emit pass — target=x86_64-linux-gnu
# source: stdlib/runtime/regex_rt.hexa
.intel_syntax noprefix
.file 1 "stdlib/runtime/regex_rt.hexa"
.text
.hidden bt_needs_backtrack
    .p2align 4
bt_needs_backtrack:
    .loc 1 105 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 1280 # prologue: alloc spill frame
    mov [rbp - 672], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd_bt_needs_backtrack_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 680], rax # store tag L1
    mov r12, rdx # hv: unbox call result (rdx)
    mov r13, r12 # assign L2
    mov r11, [rbp - 680] # tag L1 from tag-slot
    mov [rbp - 688], r11 # store tag L2
    mov r14, 0 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 696], r11 # store tag L3
    jmp .Lb2dd_bt_needs_backtrack_bb1 # branch
.Lb2dd_bt_needs_backtrack_bb1:
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 688] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r15, rdx # binop <: capture bool payload
    mov [rbp - 704], rax # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb3 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb2 # jump -> then
.Lb2dd_bt_needs_backtrack_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 712], rax # store tag L5
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 56], r10 # spill L5 to slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 712] # tag L5 from tag-slot
    mov [rbp - 720], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 720] # tag L6 from tag-slot
    mov rcx, 92 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 728], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb5 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb4 # jump -> then
.Lb2dd_bt_needs_backtrack_bb3:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 1280 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_needs_backtrack_bb4:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 744], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 688] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 752], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb7 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb6 # jump -> then
.Lb2dd_bt_needs_backtrack_bb5:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 720] # tag L6 from tag-slot
    mov rcx, 91 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 832], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb14 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb13 # jump -> then
.Lb2dd_bt_needs_backtrack_bb6:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 768], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, [rbp - 112] # reload L12 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L12 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 776], rax # store tag L13
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 776] # tag L13 from tag-slot
    mov [rbp - 784], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 784] # tag L14 from tag-slot
    mov rcx, 49 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 792], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb9 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb8 # jump -> then
.Lb2dd_bt_needs_backtrack_bb7:
    mov r10, r14 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 824], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, [rbp - 168] # reload L19 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L19 = TAG_INT (i64-local, fused)
    mov [rbp - 696], r11 # store tag L3
    jmp .Lb2dd_bt_needs_backtrack_bb1 # branch
.Lb2dd_bt_needs_backtrack_bb8:
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 784] # tag L14 from tag-slot
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 808], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 808] # tag L17 from tag-slot
    mov [rbp - 800], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb10 # branch
.Lb2dd_bt_needs_backtrack_bb9:
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 792] # tag L15 from tag-slot
    mov [rbp - 800], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb10 # branch
.Lb2dd_bt_needs_backtrack_bb10:
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb12 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb11 # jump -> then
.Lb2dd_bt_needs_backtrack_bb11:
    mov rdx, 1 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 1280 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_needs_backtrack_bb12:
    jmp .Lb2dd_bt_needs_backtrack_bb7 # branch
.Lb2dd_bt_needs_backtrack_bb13:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 848], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L22 = TAG_INT (i64-local, fused)
    mov [rbp - 696], r11 # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 688] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 856], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb16 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb15 # jump -> then
.Lb2dd_bt_needs_backtrack_bb14:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 720] # tag L6 from tag-slot
    mov rcx, 40 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1048], rax # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    mov r10, [rbp - 392] # reload L47 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb37 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb36 # jump -> then
.Lb2dd_bt_needs_backtrack_bb15:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 872], rax # store tag L25
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 216], r10 # spill L25 to slot
    mov r10, [rbp - 216] # reload L25 from spill slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 872] # tag L25 from tag-slot
    mov rcx, 94 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 880], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 880] # tag L26 from tag-slot
    mov [rbp - 864], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb17 # branch
.Lb2dd_bt_needs_backtrack_bb16:
    mov r11, [rbp - 200] # reload L23 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 856] # tag L23 from tag-slot
    mov [rbp - 864], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb17 # branch
.Lb2dd_bt_needs_backtrack_bb17:
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb19 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb18 # jump -> then
.Lb2dd_bt_needs_backtrack_bb18:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 896], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L28 = TAG_INT (i64-local, fused)
    mov [rbp - 696], r11 # store tag L3
    jmp .Lb2dd_bt_needs_backtrack_bb19 # branch
.Lb2dd_bt_needs_backtrack_bb19:
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 688] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 904], rax # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r10, [rbp - 248] # reload L29 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb21 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb20 # jump -> then
.Lb2dd_bt_needs_backtrack_bb20:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 920], rax # store tag L31
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 264], r10 # spill L31 to slot
    mov r10, [rbp - 264] # reload L31 from spill slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 920] # tag L31 from tag-slot
    mov rcx, 93 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 928], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r11, [rbp - 272] # reload L32 from spill slot
    mov r10, r11 # assign L30
    mov r11, [rbp - 928] # tag L32 from tag-slot
    mov [rbp - 912], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb22 # branch
.Lb2dd_bt_needs_backtrack_bb21:
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L30
    mov r11, [rbp - 904] # tag L29 from tag-slot
    mov [rbp - 912], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb22 # branch
.Lb2dd_bt_needs_backtrack_bb22:
    mov r10, [rbp - 256] # reload L30 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb24 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb23 # jump -> then
.Lb2dd_bt_needs_backtrack_bb23:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 944], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L34 = TAG_INT (i64-local, fused)
    mov [rbp - 696], r11 # store tag L3
    jmp .Lb2dd_bt_needs_backtrack_bb24 # branch
.Lb2dd_bt_needs_backtrack_bb24:
    jmp .Lb2dd_bt_needs_backtrack_bb25 # branch
.Lb2dd_bt_needs_backtrack_bb25:
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 688] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 952], rax # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov r10, [rbp - 296] # reload L35 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb29 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb28 # jump -> then
.Lb2dd_bt_needs_backtrack_bb26:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 984], rax # store tag L39
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 328], r10 # spill L39 to slot
    mov r10, [rbp - 328] # reload L39 from spill slot
    mov rsi, [rbp - 328] # reload L39 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 984] # tag L39 from tag-slot
    mov rcx, 92 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 992], rax # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb32 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb31 # jump -> then
.Lb2dd_bt_needs_backtrack_bb27:
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 688] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 1024], rax # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov r10, [rbp - 368] # reload L44 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb35 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb34 # jump -> then
.Lb2dd_bt_needs_backtrack_bb28:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 968], rax # store tag L37
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    mov rsi, [rbp - 312] # reload L37 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 968] # tag L37 from tag-slot
    mov rcx, 93 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 976], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov r11, [rbp - 320] # reload L38 from spill slot
    mov r10, r11 # assign L36
    mov r11, [rbp - 976] # tag L38 from tag-slot
    mov [rbp - 960], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb30 # branch
.Lb2dd_bt_needs_backtrack_bb29:
    mov r11, [rbp - 296] # reload L35 from spill slot
    mov r10, r11 # assign L36
    mov r11, [rbp - 952] # tag L35 from tag-slot
    mov [rbp - 960], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb30 # branch
.Lb2dd_bt_needs_backtrack_bb30:
    mov r10, [rbp - 304] # reload L36 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb27 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb26 # jump -> then
.Lb2dd_bt_needs_backtrack_bb31:
    mov r10, r14 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1008], r11 # store tag L42
    mov [rbp - 352], r10 # spill L42 to slot
    mov r10, [rbp - 352] # reload L42 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L42 = TAG_INT (i64-local, fused)
    mov [rbp - 696], r11 # store tag L3
    jmp .Lb2dd_bt_needs_backtrack_bb33 # branch
.Lb2dd_bt_needs_backtrack_bb32:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1016], r11 # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov r10, [rbp - 360] # reload L43 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L43 = TAG_INT (i64-local, fused)
    mov [rbp - 696], r11 # store tag L3
    jmp .Lb2dd_bt_needs_backtrack_bb33 # branch
.Lb2dd_bt_needs_backtrack_bb33:
    jmp .Lb2dd_bt_needs_backtrack_bb25 # branch
.Lb2dd_bt_needs_backtrack_bb34:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1040], r11 # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    mov r10, [rbp - 384] # reload L46 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L46 = TAG_INT (i64-local, fused)
    mov [rbp - 696], r11 # store tag L3
    jmp .Lb2dd_bt_needs_backtrack_bb35 # branch
.Lb2dd_bt_needs_backtrack_bb35:
    jmp .Lb2dd_bt_needs_backtrack_bb1 # branch
.Lb2dd_bt_needs_backtrack_bb36:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1064], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov r10, [rbp - 408] # reload L49 from spill slot
    mov rsi, [rbp - 408] # reload L49 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L49 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 688] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 1072], rax # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    mov r11, [rbp - 416] # reload L50 from spill slot
    mov r10, r11 # assign L48
    mov r11, [rbp - 1072] # tag L50 from tag-slot
    mov [rbp - 1056], r11 # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb38 # branch
.Lb2dd_bt_needs_backtrack_bb37:
    mov r11, [rbp - 392] # reload L47 from spill slot
    mov r10, r11 # assign L48
    mov r11, [rbp - 1048] # tag L47 from tag-slot
    mov [rbp - 1056], r11 # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb38 # branch
.Lb2dd_bt_needs_backtrack_bb38:
    mov r10, [rbp - 400] # reload L48 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb40 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb39 # jump -> then
.Lb2dd_bt_needs_backtrack_bb39:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1088], r11 # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, [rbp - 432] # reload L52 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L52 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 1096], rax # store tag L53
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 440], r10 # spill L53 to slot
    mov r10, [rbp - 440] # reload L53 from spill slot
    mov rsi, [rbp - 440] # reload L53 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1096] # tag L53 from tag-slot
    mov rcx, 63 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1104], rax # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov r11, [rbp - 448] # reload L54 from spill slot
    mov r10, r11 # assign L51
    mov r11, [rbp - 1104] # tag L54 from tag-slot
    mov [rbp - 1080], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb41 # branch
.Lb2dd_bt_needs_backtrack_bb40:
    mov r11, [rbp - 400] # reload L48 from spill slot
    mov r10, r11 # assign L51
    mov r11, [rbp - 1056] # tag L48 from tag-slot
    mov [rbp - 1080], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb41 # branch
.Lb2dd_bt_needs_backtrack_bb41:
    mov r10, [rbp - 424] # reload L51 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb43 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb42 # jump -> then
.Lb2dd_bt_needs_backtrack_bb42:
    mov r10, r14 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1120], r11 # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    mov r10, [rbp - 464] # reload L56 from spill slot
    mov rsi, [rbp - 464] # reload L56 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L56 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 688] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 1128], rax # store tag L57
    mov [rbp - 472], r10 # spill L57 to slot
    mov r10, [rbp - 472] # reload L57 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb45 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb44 # jump -> then
.Lb2dd_bt_needs_backtrack_bb43:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 720] # tag L6 from tag-slot
    mov rcx, 123 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1216], rax # store tag L68
    mov [rbp - 560], r10 # spill L68 to slot
    mov r10, [rbp - 560] # reload L68 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb55 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb54 # jump -> then
.Lb2dd_bt_needs_backtrack_bb44:
    mov r10, r14 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1144], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, [rbp - 488] # reload L59 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L59 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 1152], rax # store tag L60
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 496], r10 # spill L60 to slot
    mov r11, [rbp - 496] # reload L60 from spill slot
    mov r10, r11 # assign L61
    mov r11, [rbp - 1152] # tag L60 from tag-slot
    mov [rbp - 1160], r11 # store tag L61
    mov [rbp - 504], r10 # spill L61 to slot
    mov r10, [rbp - 504] # reload L61 from spill slot
    mov rsi, [rbp - 504] # reload L61 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1160] # tag L61 from tag-slot
    mov rcx, 61 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1168], rax # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov r10, [rbp - 512] # reload L62 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb47 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb46 # jump -> then
.Lb2dd_bt_needs_backtrack_bb45:
    jmp .Lb2dd_bt_needs_backtrack_bb43 # branch
.Lb2dd_bt_needs_backtrack_bb46:
    mov r11, [rbp - 512] # reload L62 from spill slot
    mov r10, r11 # assign L63
    mov r11, [rbp - 1168] # tag L62 from tag-slot
    mov [rbp - 1176], r11 # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb48 # branch
.Lb2dd_bt_needs_backtrack_bb47:
    mov r10, [rbp - 504] # reload L61 from spill slot
    mov rsi, [rbp - 504] # reload L61 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1160] # tag L61 from tag-slot
    mov rcx, 33 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1184], rax # store tag L64
    mov [rbp - 528], r10 # spill L64 to slot
    mov r11, [rbp - 528] # reload L64 from spill slot
    mov r10, r11 # assign L63
    mov r11, [rbp - 1184] # tag L64 from tag-slot
    mov [rbp - 1176], r11 # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb48 # branch
.Lb2dd_bt_needs_backtrack_bb48:
    mov r10, [rbp - 520] # reload L63 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb50 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb49 # jump -> then
.Lb2dd_bt_needs_backtrack_bb49:
    mov r11, [rbp - 520] # reload L63 from spill slot
    mov r10, r11 # assign L65
    mov r11, [rbp - 1176] # tag L63 from tag-slot
    mov [rbp - 1192], r11 # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb51 # branch
.Lb2dd_bt_needs_backtrack_bb50:
    mov r10, [rbp - 504] # reload L61 from spill slot
    mov rsi, [rbp - 504] # reload L61 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1160] # tag L61 from tag-slot
    mov rcx, 60 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1200], rax # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    mov r11, [rbp - 544] # reload L66 from spill slot
    mov r10, r11 # assign L65
    mov r11, [rbp - 1200] # tag L66 from tag-slot
    mov [rbp - 1192], r11 # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb51 # branch
.Lb2dd_bt_needs_backtrack_bb51:
    mov r10, [rbp - 536] # reload L65 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb53 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb52 # jump -> then
.Lb2dd_bt_needs_backtrack_bb52:
    mov rdx, 1 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 1280 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_needs_backtrack_bb53:
    jmp .Lb2dd_bt_needs_backtrack_bb45 # branch
.Lb2dd_bt_needs_backtrack_bb54:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1232], r11 # store tag L70
    mov [rbp - 576], r10 # spill L70 to slot
    mov r10, [rbp - 576] # reload L70 from spill slot
    mov rsi, [rbp - 576] # reload L70 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L70 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 688] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 1240], rax # store tag L71
    mov [rbp - 584], r10 # spill L71 to slot
    mov r11, [rbp - 584] # reload L71 from spill slot
    mov r10, r11 # assign L69
    mov r11, [rbp - 1240] # tag L71 from tag-slot
    mov [rbp - 1224], r11 # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb56 # branch
.Lb2dd_bt_needs_backtrack_bb55:
    mov r11, [rbp - 560] # reload L68 from spill slot
    mov r10, r11 # assign L69
    mov r11, [rbp - 1216] # tag L68 from tag-slot
    mov [rbp - 1224], r11 # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb56 # branch
.Lb2dd_bt_needs_backtrack_bb56:
    mov r10, [rbp - 568] # reload L69 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb58 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb57 # jump -> then
.Lb2dd_bt_needs_backtrack_bb57:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1256], r11 # store tag L73
    mov [rbp - 600], r10 # spill L73 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, [rbp - 600] # reload L73 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L73 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 1264], rax # store tag L74
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 608], r10 # spill L74 to slot
    mov r10, [rbp - 608] # reload L74 from spill slot
    mov rsi, [rbp - 608] # reload L74 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1264] # tag L74 from tag-slot
    mov rcx, 48 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 1272], rax # store tag L75
    mov [rbp - 616], r10 # spill L75 to slot
    mov r11, [rbp - 616] # reload L75 from spill slot
    mov r10, r11 # assign L72
    mov r11, [rbp - 1272] # tag L75 from tag-slot
    mov [rbp - 1248], r11 # store tag L72
    mov [rbp - 592], r10 # spill L72 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb59 # branch
.Lb2dd_bt_needs_backtrack_bb58:
    mov r11, [rbp - 568] # reload L69 from spill slot
    mov r10, r11 # assign L72
    mov r11, [rbp - 1224] # tag L69 from tag-slot
    mov [rbp - 1248], r11 # store tag L72
    mov [rbp - 592], r10 # spill L72 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb59 # branch
.Lb2dd_bt_needs_backtrack_bb59:
    mov r10, [rbp - 592] # reload L72 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb61 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb60 # jump -> then
.Lb2dd_bt_needs_backtrack_bb60:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1288], r11 # store tag L77
    mov [rbp - 632], r10 # spill L77 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 672] # tag L0 from tag-slot
    mov rcx, [rbp - 632] # reload L77 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L77 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 1296], rax # store tag L78
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 640], r10 # spill L78 to slot
    mov r10, [rbp - 640] # reload L78 from spill slot
    mov rsi, [rbp - 640] # reload L78 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1296] # tag L78 from tag-slot
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 1304], rax # store tag L79
    mov [rbp - 648], r10 # spill L79 to slot
    mov r11, [rbp - 648] # reload L79 from spill slot
    mov r10, r11 # assign L76
    mov r11, [rbp - 1304] # tag L79 from tag-slot
    mov [rbp - 1280], r11 # store tag L76
    mov [rbp - 624], r10 # spill L76 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb62 # branch
.Lb2dd_bt_needs_backtrack_bb61:
    mov r11, [rbp - 592] # reload L72 from spill slot
    mov r10, r11 # assign L76
    mov r11, [rbp - 1248] # tag L72 from tag-slot
    mov [rbp - 1280], r11 # store tag L76
    mov [rbp - 624], r10 # spill L76 to slot
    jmp .Lb2dd_bt_needs_backtrack_bb62 # branch
.Lb2dd_bt_needs_backtrack_bb62:
    mov r10, [rbp - 624] # reload L76 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_needs_backtrack_bb64 # jump-if-zero -> else
    jmp .Lb2dd_bt_needs_backtrack_bb63 # jump -> then
.Lb2dd_bt_needs_backtrack_bb63:
    mov rdx, 1 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 1280 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_needs_backtrack_bb64:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1320], r11 # store tag L81
    mov [rbp - 664], r10 # spill L81 to slot
    mov r10, [rbp - 664] # reload L81 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L81 = TAG_INT (i64-local, fused)
    mov [rbp - 696], r11 # store tag L3
    jmp .Lb2dd_bt_needs_backtrack_bb1 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 1280 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_peek
    .p2align 4
_bt_peek:
    .loc 1 151 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 128 # prologue: alloc spill frame
    mov [rbp - 96], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_peek_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r12, rdx # field: capture result payload
    mov [rbp - 104], rax # store tag L1
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 104] # tag L1 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r13, rdx # index: capture element payload
    mov [rbp - 112], rax # store tag L2
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    call hexa_map_get # field: hexa_map_get
    mov r14, rdx # field: capture result payload
    mov [rbp - 120], rax # store tag L3
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 112] # tag L2 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, [rbp - 120] # tag L3 from tag-slot
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r15, rdx # binop >=: capture bool payload
    mov [rbp - 128], rax # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd__bt_peek_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_peek_bb1 # jump -> then
.Lb2dd__bt_peek_bb1:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 128 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_peek_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr2] # map key (const char*): &.LCstr2
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 144], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 152], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 152] # tag L7 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 160], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 144] # tag L6 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 160] # tag L8 from tag-slot
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 168], rax # store tag L9
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 88], r10 # spill L9 to slot
    mov rdx, [rbp - 88] # reload L9 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 168] # tag L9 from tag-slot
    add rsp, 128 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_adv
    .p2align 4
_bt_adv:
    .loc 1 156 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 48 # prologue: alloc spill frame
    mov [rbp - 56], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_adv_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 56] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r12, rdx # field: capture result payload
    mov [rbp - 64], rax # store tag L1
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 56] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 72], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 72] # tag L2 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r14, rdx # index: capture element payload
    mov [rbp - 80], rax # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 80] # tag L3 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r15, rdx # binop +: capture result payload
    mov [rbp - 88], rax # store tag L4
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 64] # tag L1 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, r15 # hv arg payload
    mov r8, 0 # tag L4 = TAG_INT (i64-local, fused)
    call hexa_index_set # index_set: hexa_index_set
    mov r12, rdx # index_set: store-back container payload
    mov [rbp - 64], rax # store tag L1
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 48 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_emit
    .p2align 4
_bt_emit:
    .loc 1 160 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 80 # prologue: alloc spill frame
    mov [rbp - 72], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 80], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__bt_emit_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 72] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 88], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 88] # tag L2 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 80] # tag L1 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 96], rax # store tag L3
    mov r14, rdx # hv: unbox call result (rdx)
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 72] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r15, rdx # field: capture result payload
    mov [rbp - 104], rax # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 104] # tag L4 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 112], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 112] # tag L5 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 120], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rdx, [rbp - 64] # reload L6 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L6 = TAG_INT (i64-local, fused)
    add rsp, 80 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_class_for_escape
    .p2align 4
_bt_class_for_escape:
    .loc 1 167 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 288 # prologue: alloc spill frame
    mov [rbp - 176], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_class_for_escape_bb0:
    cmp rbx, 100 # binop ==
    sete al # binop == → al
    movzx r12, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 184], r11 # store tag L1
    test r12, r12 # br_cond test
    jz .Lb2dd__bt_class_for_escape_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_for_escape_bb1 # jump -> then
.Lb2dd__bt_class_for_escape_bb1:
    call hexa_array_new # array_lit: new array
    mov r14, rdx # array_lit: capture new array payload
    mov [rbp - 200], rax # store tag L3
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 200] # tag L3 from tag-slot
    mov rsi, r14 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r14, rdx # array_lit: recapture array payload
    mov [rbp - 200], rax # store tag L3
    mov rcx, 48 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 200] # tag L3 from tag-slot
    mov rsi, r14 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r14, rdx # array_lit: recapture array payload
    mov [rbp - 200], rax # store tag L3
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 200] # tag L3 from tag-slot
    mov rsi, r14 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r14, rdx # array_lit: recapture array payload
    mov [rbp - 200], rax # store tag L3
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 200] # tag L3 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_class_for_escape_bb2:
    cmp rbx, 68 # binop ==
    sete al # binop == → al
    movzx r15, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 208], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd__bt_class_for_escape_bb4 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_for_escape_bb3 # jump -> then
.Lb2dd__bt_class_for_escape_bb3:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 224], rax # store tag L6
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 224] # tag L6 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 224], rax # store tag L6
    mov rcx, 48 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 224] # tag L6 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 224], rax # store tag L6
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 224] # tag L6 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 224], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rdx, [rbp - 64] # reload L6 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 224] # tag L6 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_class_for_escape_bb4:
    cmp rbx, 119 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 232], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_class_for_escape_bb6 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_for_escape_bb5 # jump -> then
.Lb2dd__bt_class_for_escape_bb5:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 48 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 65 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 90 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 4
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 97 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 5
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 122 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 6
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 95 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 7
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 95 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 8
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rdx, [rbp - 88] # reload L9 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 248] # tag L9 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_class_for_escape_bb6:
    cmp rbx, 87 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 256], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_class_for_escape_bb8 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_for_escape_bb7 # jump -> then
.Lb2dd__bt_class_for_escape_bb7:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 48 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 65 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 90 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 4
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 97 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 5
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 122 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 6
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 95 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 7
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 95 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 8
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rdx, [rbp - 112] # reload L12 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 272] # tag L12 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_class_for_escape_bb8:
    cmp rbx, 115 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 280], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_class_for_escape_bb10 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_for_escape_bb9 # jump -> then
.Lb2dd__bt_class_for_escape_bb9:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 13 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 32 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 32 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 4
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rdx, [rbp - 136] # reload L15 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 296] # tag L15 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_class_for_escape_bb10:
    cmp rbx, 83 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 304], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_class_for_escape_bb12 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_for_escape_bb11 # jump -> then
.Lb2dd__bt_class_for_escape_bb11:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 13 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 32 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 32 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 4
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rdx, [rbp - 160] # reload L18 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 320] # tag L18 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_class_for_escape_bb12:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 328], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov rdx, [rbp - 168] # reload L19 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 328] # tag L19 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_lit_for_escape
    .p2align 4
_bt_lit_for_escape:
    .loc 1 179 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 112 # prologue: alloc spill frame
    mov [rbp - 88], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_lit_for_escape_bb0:
    cmp rbx, 110 # binop ==
    sete al # binop == → al
    movzx r12, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 96], r11 # store tag L1
    test r12, r12 # br_cond test
    jz .Lb2dd__bt_lit_for_escape_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_lit_for_escape_bb1 # jump -> then
.Lb2dd__bt_lit_for_escape_bb1:
    mov rdx, 10 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_lit_for_escape_bb2:
    cmp rbx, 116 # binop ==
    sete al # binop == → al
    movzx r14, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 112], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd__bt_lit_for_escape_bb4 # jump-if-zero -> else
    jmp .Lb2dd__bt_lit_for_escape_bb3 # jump -> then
.Lb2dd__bt_lit_for_escape_bb3:
    mov rdx, 9 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_lit_for_escape_bb4:
    cmp rbx, 114 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 128], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_lit_for_escape_bb6 # jump-if-zero -> else
    jmp .Lb2dd__bt_lit_for_escape_bb5 # jump -> then
.Lb2dd__bt_lit_for_escape_bb5:
    mov rdx, 13 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_lit_for_escape_bb6:
    cmp rbx, 48 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 144], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_lit_for_escape_bb8 # jump-if-zero -> else
    jmp .Lb2dd__bt_lit_for_escape_bb7 # jump -> then
.Lb2dd__bt_lit_for_escape_bb7:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_lit_for_escape_bb8:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_push_class
    .p2align 4
_bt_push_class:
    .loc 1 187 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 256 # prologue: alloc spill frame
    mov [rbp - 160], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 168], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 176], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd__bt_push_class_bb0:
    call hexa_array_new # array_lit: new array
    mov r14, rdx # array_lit: capture new array payload
    mov [rbp - 184], rax # store tag L3
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 184] # tag L3 from tag-slot
    mov rsi, r14 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r14, rdx # array_lit: recapture array payload
    mov [rbp - 184], rax # store tag L3
    mov r15, r14 # assign L4
    mov r11, [rbp - 184] # tag L3 from tag-slot
    mov [rbp - 192], r11 # store tag L4
    mov r10, 0 # assign L5
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 200], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd__bt_push_class_bb1 # branch
.Lb2dd__bt_push_class_bb1:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 168] # tag L1 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 208], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 64] # reload L6 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 208] # tag L6 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 216], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_push_class_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_push_class_bb2 # jump -> then
.Lb2dd__bt_push_class_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 168] # tag L1 from tag-slot
    mov rdx, [rbp - 56] # reload L5 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 224], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 192] # tag L4 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 224] # tag L8 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 232], rax # store tag L9
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 240], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L5
    mov r11, 0 # tag L10 = TAG_INT (i64-local, fused)
    mov [rbp - 200], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd__bt_push_class_bb1 # branch
.Lb2dd__bt_push_class_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 160] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 248], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 248] # tag L11 from tag-slot
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 192] # tag L4 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 256], rax # store tag L12
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 160] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 264], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 264] # tag L13 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 272], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 272] # tag L14 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 280], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 288], rax # store tag L16
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L16
    mov rcx, [rbp - 136] # reload L15 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L15 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 288] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L16
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L16
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 160] # tag L0 from tag-slot
    mov rcx, [rbp - 144] # reload L16 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 288] # tag L16 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 296], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov rdx, [rbp - 152] # reload L17 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L17 = TAG_INT (i64-local, fused)
    add rsp, 256 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_parse_class
    .p2align 4
_bt_parse_class:
    .loc 1 196 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 1328 # prologue: alloc spill frame
    mov [rbp - 696], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_parse_class_bb0:
    call hexa_array_new # array_lit: new array
    mov r12, rdx # array_lit: capture new array payload
    mov [rbp - 704], rax # store tag L1
    mov r13, r12 # assign L2
    mov r11, [rbp - 704] # tag L1 from tag-slot
    mov [rbp - 712], r11 # store tag L2
    mov r14, 0 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 720], r11 # store tag L3
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 728], rax # store tag L4
    mov r15, rdx # hv: unbox user-call result payload
    cmp r15, 94 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 736], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb1 # jump -> then
.Lb2dd__bt_parse_class_bb1:
    mov r14, 1 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 720], r11 # store tag L3
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 752], rax # store tag L7
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 72], r10 # spill L7 to slot
    jmp .Lb2dd__bt_parse_class_bb2 # branch
.Lb2dd__bt_parse_class_bb2:
    mov r10, 1 # assign L8
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 760], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    jmp .Lb2dd__bt_parse_class_bb3 # branch
.Lb2dd__bt_parse_class_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 768], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 768] # tag L9 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 776], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 784], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 776] # tag L10 from tag-slot
    mov rcx, [rbp - 104] # reload L11 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 784] # tag L11 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 792], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb5 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb4 # jump -> then
.Lb2dd__bt_parse_class_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 800], rax # store tag L13
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov [rbp - 808], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, 93 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 816], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb7 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb6 # jump -> then
.Lb2dd__bt_parse_class_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1368], rax # store tag L84
    mov [rbp - 688], r10 # spill L84 to slot
    mov rsi, [rbp - 688] # reload L84 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1368] # tag L84 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1368], rax # store tag L84
    mov [rbp - 688], r10 # spill L84 to slot
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1328 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_class_bb6:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 760] # tag L8 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 832], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 832] # tag L17 from tag-slot
    mov [rbp - 824], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__bt_parse_class_bb8 # branch
.Lb2dd__bt_parse_class_bb7:
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 816] # tag L15 from tag-slot
    mov [rbp - 824], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__bt_parse_class_bb8 # branch
.Lb2dd__bt_parse_class_bb8:
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb10 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb9 # jump -> then
.Lb2dd__bt_parse_class_bb9:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 848], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 712] # tag L2 from tag-slot
    mov r9, r14 # hv arg payload
    mov r8, 0 # tag L3 = TAG_INT (i64-local, fused)
    call _bt_push_class # call _bt_push_class
    mov [rbp - 856], rax # store tag L20
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 176], r10 # spill L20 to slot
    mov rdx, [rbp - 176] # reload L20 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L20 = TAG_INT (i64-local, fused)
    add rsp, 1328 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_class_bb10:
    mov r10, 0 # assign L8
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 760], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, -1 # assign L21
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 864], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov r10, 0 # assign L22
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 872], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, 92 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 880], r11 # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb12 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb11 # jump -> then
.Lb2dd__bt_parse_class_bb11:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 896], rax # store tag L25
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 216], r10 # spill L25 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 904], rax # store tag L26
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 224], r10 # spill L26 to slot
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L27
    mov r11, 0 # tag L26 = TAG_INT (i64-local, fused)
    mov [rbp - 912], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 920], rax # store tag L28
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 240], r10 # spill L28 to slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L27 = TAG_INT (i64-local, fused)
    call _bt_class_for_escape # call _bt_class_for_escape
    mov [rbp - 928], rax # store tag L29
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 248], r10 # spill L29 to slot
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L30
    mov r11, [rbp - 928] # tag L29 from tag-slot
    mov [rbp - 936], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov rsi, [rbp - 256] # reload L30 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 936] # tag L30 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 944], r11 # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov r10, [rbp - 264] # reload L31 from spill slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 944] # tag L31 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_gt # binop >: tag-dispatch hexa_cmp_gt
    mov r10, rdx # binop >: capture bool payload
    mov [rbp - 952], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, [rbp - 272] # reload L32 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb14 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb13 # jump -> then
.Lb2dd__bt_parse_class_bb12:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1080], rax # store tag L48
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 400], r10 # spill L48 to slot
    mov r11, [rbp - 128] # reload L14 from spill slot
    mov r10, r11 # assign L21
    mov r11, 0 # tag L14 = TAG_INT (i64-local, fused)
    mov [rbp - 864], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    jmp .Lb2dd__bt_parse_class_bb22 # branch
.Lb2dd__bt_parse_class_bb13:
    mov r10, 1 # assign L34
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 968], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    jmp .Lb2dd__bt_parse_class_bb15 # branch
.Lb2dd__bt_parse_class_bb14:
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L27 = TAG_INT (i64-local, fused)
    call _bt_lit_for_escape # call _bt_lit_for_escape
    mov [rbp - 1048], rax # store tag L44
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 368], r10 # spill L44 to slot
    mov r11, [rbp - 368] # reload L44 from spill slot
    mov r10, r11 # assign L45
    mov r11, 0 # tag L44 = TAG_INT (i64-local, fused)
    mov [rbp - 1056], r11 # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov r10, [rbp - 376] # reload L45 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1064], r11 # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    mov r10, [rbp - 384] # reload L46 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb19 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb18 # jump -> then
.Lb2dd__bt_parse_class_bb15:
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 976], r11 # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov rsi, [rbp - 256] # reload L30 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 936] # tag L30 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 984], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov r10, [rbp - 296] # reload L35 from spill slot
    mov r11, [rbp - 304] # reload L36 from spill slot
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L35 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 304] # reload L36 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 984] # tag L36 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 992], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb17 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb16 # jump -> then
.Lb2dd__bt_parse_class_bb16:
    mov rsi, [rbp - 256] # reload L30 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 936] # tag L30 from tag-slot
    mov rdx, [rbp - 288] # reload L34 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 1000], rax # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 320] # reload L38 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1000] # tag L38 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1008], rax # store tag L39
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 328], r10 # spill L39 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1016], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov rsi, [rbp - 256] # reload L30 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 936] # tag L30 from tag-slot
    mov rdx, [rbp - 336] # reload L40 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 1024], rax # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 344] # reload L41 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1024] # tag L41 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1032], rax # store tag L42
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 352], r10 # spill L42 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1040], r11 # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov r11, [rbp - 360] # reload L43 from spill slot
    mov r10, r11 # assign L34
    mov r11, 0 # tag L43 = TAG_INT (i64-local, fused)
    mov [rbp - 968], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    jmp .Lb2dd__bt_parse_class_bb15 # branch
.Lb2dd__bt_parse_class_bb17:
    mov r10, 1 # assign L22
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 872], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    jmp .Lb2dd__bt_parse_class_bb21 # branch
.Lb2dd__bt_parse_class_bb18:
    mov r11, [rbp - 376] # reload L45 from spill slot
    mov r10, r11 # assign L21
    mov r11, 0 # tag L45 = TAG_INT (i64-local, fused)
    mov [rbp - 864], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    jmp .Lb2dd__bt_parse_class_bb20 # branch
.Lb2dd__bt_parse_class_bb19:
    mov r11, [rbp - 232] # reload L27 from spill slot
    mov r10, r11 # assign L21
    mov r11, 0 # tag L27 = TAG_INT (i64-local, fused)
    mov [rbp - 864], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    jmp .Lb2dd__bt_parse_class_bb20 # branch
.Lb2dd__bt_parse_class_bb20:
    jmp .Lb2dd__bt_parse_class_bb21 # branch
.Lb2dd__bt_parse_class_bb21:
    jmp .Lb2dd__bt_parse_class_bb22 # branch
.Lb2dd__bt_parse_class_bb22:
    mov rsi, [rbp - 192] # reload L22 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 872] # tag L22 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1088], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov r10, [rbp - 408] # reload L49 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb24 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb23 # jump -> then
.Lb2dd__bt_parse_class_bb23:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1104], rax # store tag L51
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 424], r10 # spill L51 to slot
    mov r10, [rbp - 424] # reload L51 from spill slot
    cmp r10, 45 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1112], r11 # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov r10, [rbp - 432] # reload L52 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb26 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb25 # jump -> then
.Lb2dd__bt_parse_class_bb24:
    jmp .Lb2dd__bt_parse_class_bb3 # branch
.Lb2dd__bt_parse_class_bb25:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1128], rax # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov rsi, [rbp - 448] # reload L54 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1128] # tag L54 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1136], rax # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    mov r10, [rbp - 456] # reload L55 from spill slot
    mov rsi, [rbp - 456] # reload L55 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1136] # tag L55 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 1144], rax # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1152], rax # store tag L57
    mov [rbp - 472], r10 # spill L57 to slot
    mov r10, [rbp - 464] # reload L56 from spill slot
    mov r11, [rbp - 472] # reload L57 from spill slot
    mov rsi, [rbp - 464] # reload L56 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L56 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 472] # reload L57 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1152] # tag L57 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 1160], rax # store tag L58
    mov [rbp - 480], r10 # spill L58 to slot
    mov r11, [rbp - 480] # reload L58 from spill slot
    mov r10, r11 # assign L53
    mov r11, [rbp - 1160] # tag L58 from tag-slot
    mov [rbp - 1120], r11 # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    jmp .Lb2dd__bt_parse_class_bb27 # branch
.Lb2dd__bt_parse_class_bb26:
    mov r11, [rbp - 432] # reload L52 from spill slot
    mov r10, r11 # assign L53
    mov r11, [rbp - 1112] # tag L52 from tag-slot
    mov [rbp - 1120], r11 # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    jmp .Lb2dd__bt_parse_class_bb27 # branch
.Lb2dd__bt_parse_class_bb27:
    mov r10, [rbp - 440] # reload L53 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb29 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb28 # jump -> then
.Lb2dd__bt_parse_class_bb28:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr2] # map key (const char*): &.LCstr2
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1176], rax # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1184], rax # store tag L61
    mov [rbp - 504], r10 # spill L61 to slot
    mov rsi, [rbp - 504] # reload L61 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1184] # tag L61 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1192], rax # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov r10, [rbp - 512] # reload L62 from spill slot
    mov rsi, [rbp - 512] # reload L62 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1192] # tag L62 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 1200], rax # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    mov rsi, [rbp - 496] # reload L60 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L60 from tag-slot
    mov rcx, [rbp - 520] # reload L63 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L63 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 1208], rax # store tag L64
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 528], r10 # spill L64 to slot
    mov r10, [rbp - 528] # reload L64 from spill slot
    mov rsi, [rbp - 528] # reload L64 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1208] # tag L64 from tag-slot
    mov rcx, 93 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1216], r11 # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    mov r11, [rbp - 536] # reload L65 from spill slot
    mov r10, r11 # assign L59
    mov r11, [rbp - 1216] # tag L65 from tag-slot
    mov [rbp - 1168], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    jmp .Lb2dd__bt_parse_class_bb30 # branch
.Lb2dd__bt_parse_class_bb29:
    mov r11, [rbp - 440] # reload L53 from spill slot
    mov r10, r11 # assign L59
    mov r11, [rbp - 1120] # tag L53 from tag-slot
    mov [rbp - 1168], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    jmp .Lb2dd__bt_parse_class_bb30 # branch
.Lb2dd__bt_parse_class_bb30:
    mov r10, [rbp - 488] # reload L59 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb32 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb31 # jump -> then
.Lb2dd__bt_parse_class_bb31:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1232], rax # store tag L67
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 552], r10 # spill L67 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1240], rax # store tag L68
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 560], r10 # spill L68 to slot
    mov r11, [rbp - 560] # reload L68 from spill slot
    mov r10, r11 # assign L69
    mov r11, 0 # tag L68 = TAG_INT (i64-local, fused)
    mov [rbp - 1248], r11 # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1256], rax # store tag L70
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 576], r10 # spill L70 to slot
    mov r10, [rbp - 568] # reload L69 from spill slot
    cmp r10, 92 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1264], r11 # store tag L71
    mov [rbp - 584], r10 # spill L71 to slot
    mov r10, [rbp - 584] # reload L71 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb34 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb33 # jump -> then
.Lb2dd__bt_parse_class_bb32:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 184] # reload L21 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L21 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1352], rax # store tag L82
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 672], r10 # spill L82 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 184] # reload L21 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L21 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1360], rax # store tag L83
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 680], r10 # spill L83 to slot
    mov r11, [rbp - 680] # reload L83 from spill slot
    mov r10, r11 # assign L66
    mov r11, [rbp - 1360] # tag L83 from tag-slot
    mov [rbp - 1224], r11 # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    jmp .Lb2dd__bt_parse_class_bb38 # branch
.Lb2dd__bt_parse_class_bb33:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1280], rax # store tag L73
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 600], r10 # spill L73 to slot
    mov r11, [rbp - 600] # reload L73 from spill slot
    mov r10, r11 # assign L74
    mov r11, 0 # tag L73 = TAG_INT (i64-local, fused)
    mov [rbp - 1288], r11 # store tag L74
    mov [rbp - 608], r10 # spill L74 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1296], rax # store tag L75
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 616], r10 # spill L75 to slot
    mov rsi, [rbp - 608] # reload L74 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L74 = TAG_INT (i64-local, fused)
    call _bt_lit_for_escape # call _bt_lit_for_escape
    mov [rbp - 1304], rax # store tag L76
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 624], r10 # spill L76 to slot
    mov r11, [rbp - 624] # reload L76 from spill slot
    mov r10, r11 # assign L77
    mov r11, 0 # tag L76 = TAG_INT (i64-local, fused)
    mov [rbp - 1312], r11 # store tag L77
    mov [rbp - 632], r10 # spill L77 to slot
    mov r10, [rbp - 632] # reload L77 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1320], r11 # store tag L78
    mov [rbp - 640], r10 # spill L78 to slot
    mov r10, [rbp - 640] # reload L78 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_class_bb36 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_class_bb35 # jump -> then
.Lb2dd__bt_parse_class_bb34:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 184] # reload L21 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L21 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1336], rax # store tag L80
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 656], r10 # spill L80 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 568] # reload L69 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L69 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1344], rax # store tag L81
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 664], r10 # spill L81 to slot
    mov r11, [rbp - 664] # reload L81 from spill slot
    mov r10, r11 # assign L66
    mov r11, [rbp - 1344] # tag L81 from tag-slot
    mov [rbp - 1224], r11 # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    jmp .Lb2dd__bt_parse_class_bb38 # branch
.Lb2dd__bt_parse_class_bb35:
    mov r11, [rbp - 632] # reload L77 from spill slot
    mov r10, r11 # assign L69
    mov r11, 0 # tag L77 = TAG_INT (i64-local, fused)
    mov [rbp - 1248], r11 # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    jmp .Lb2dd__bt_parse_class_bb37 # branch
.Lb2dd__bt_parse_class_bb36:
    mov r11, [rbp - 608] # reload L74 from spill slot
    mov r10, r11 # assign L69
    mov r11, 0 # tag L74 = TAG_INT (i64-local, fused)
    mov [rbp - 1248], r11 # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    jmp .Lb2dd__bt_parse_class_bb37 # branch
.Lb2dd__bt_parse_class_bb37:
    jmp .Lb2dd__bt_parse_class_bb34 # branch
.Lb2dd__bt_parse_class_bb38:
    jmp .Lb2dd__bt_parse_class_bb24 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 1328 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_parse_alt
    .p2align 4
_bt_parse_alt:
    .loc 1 248 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 128 # prologue: alloc spill frame
    mov [rbp - 96], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_parse_alt_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    call _bt_parse_concat # call _bt_parse_concat
    mov [rbp - 104], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov r13, r12 # assign L2
    mov r11, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov [rbp - 112], r11 # store tag L2
    jmp .Lb2dd__bt_parse_alt_bb1 # branch
.Lb2dd__bt_parse_alt_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 120], rax # store tag L3
    mov r14, rdx # hv: unbox user-call result payload
    cmp r14, 124 # binop ==
    sete al # binop == → al
    movzx r15, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 128], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd__bt_parse_alt_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_alt_bb2 # jump -> then
.Lb2dd__bt_parse_alt_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 136], rax # store tag L5
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    call _bt_parse_concat # call _bt_parse_concat
    mov [rbp - 144], rax # store tag L6
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 64], r10 # spill L6 to slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r10, r11 # assign L7
    mov r11, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rbp - 152], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 160], rax # store tag L8
    mov rcx, 7 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 160] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 160], rax # store tag L8
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 160] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 160], rax # store tag L8
    mov rcx, [rbp - 72] # reload L7 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L7 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 160] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 160], rax # store tag L8
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 160] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 160], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 160] # tag L8 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 168], rax # store tag L9
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rbp - 112], r11 # store tag L2
    jmp .Lb2dd__bt_parse_alt_bb1 # branch
.Lb2dd__bt_parse_alt_bb3:
    mov rdx, r13 # hv arg payload
    mov rax, 0 # tag L2 = TAG_INT (i64-local, fused)
    add rsp, 128 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_parse_concat
    .p2align 4
_bt_parse_concat:
    .loc 1 259 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 368 # prologue: alloc spill frame
    mov [rbp - 216], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_parse_concat_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 224], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov r13, r12 # assign L2
    mov r11, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov [rbp - 232], r11 # store tag L2
    cmp r13, -1 # binop ==
    sete al # binop == → al
    movzx r14, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 240], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd__bt_parse_concat_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_concat_bb1 # jump -> then
.Lb2dd__bt_parse_concat_bb1:
    mov r15, r14 # assign L4
    mov r11, [rbp - 240] # tag L3 from tag-slot
    mov [rbp - 248], r11 # store tag L4
    jmp .Lb2dd__bt_parse_concat_bb3 # branch
.Lb2dd__bt_parse_concat_bb2:
    cmp r13, 124 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 256], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r15, r10 # assign L4
    mov r11, [rbp - 256] # tag L5 from tag-slot
    mov [rbp - 248], r11 # store tag L4
    jmp .Lb2dd__bt_parse_concat_bb3 # branch
.Lb2dd__bt_parse_concat_bb3:
    test r15, r15 # br_cond test
    jz .Lb2dd__bt_parse_concat_bb5 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_concat_bb4 # jump -> then
.Lb2dd__bt_parse_concat_bb4:
    mov r10, r15 # assign L6
    mov r11, [rbp - 248] # tag L4 from tag-slot
    mov [rbp - 264], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    jmp .Lb2dd__bt_parse_concat_bb6 # branch
.Lb2dd__bt_parse_concat_bb5:
    cmp r13, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 272], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 272] # tag L7 from tag-slot
    mov [rbp - 264], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    jmp .Lb2dd__bt_parse_concat_bb6 # branch
.Lb2dd__bt_parse_concat_bb6:
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_concat_bb8 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_concat_bb7 # jump -> then
.Lb2dd__bt_parse_concat_bb7:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 288], rax # store tag L9
    mov rcx, 8 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L9
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L9
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L9
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    mov rcx, [rbp - 88] # reload L9 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 288] # tag L9 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 296], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov rdx, [rbp - 96] # reload L10 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L10 = TAG_INT (i64-local, fused)
    add rsp, 368 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_concat_bb8:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    call _bt_parse_repeat # call _bt_parse_repeat
    mov [rbp - 304], rax # store tag L11
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 104], r10 # spill L11 to slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, 0 # tag L11 = TAG_INT (i64-local, fused)
    mov [rbp - 312], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_parse_concat_bb9 # branch
.Lb2dd__bt_parse_concat_bb9:
    mov r10, 1 # materialize test cond to reg
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_concat_bb11 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_concat_bb10 # jump -> then
.Lb2dd__bt_parse_concat_bb10:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 320], rax # store tag L13
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov [rbp - 328], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, -1 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 336], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_concat_bb13 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_concat_bb12 # jump -> then
.Lb2dd__bt_parse_concat_bb11:
    mov rdx, [rbp - 112] # reload L12 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L12 = TAG_INT (i64-local, fused)
    add rsp, 368 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_concat_bb12:
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 336] # tag L15 from tag-slot
    mov [rbp - 344], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__bt_parse_concat_bb14 # branch
.Lb2dd__bt_parse_concat_bb13:
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, 124 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 352], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 352] # tag L17 from tag-slot
    mov [rbp - 344], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__bt_parse_concat_bb14 # branch
.Lb2dd__bt_parse_concat_bb14:
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_concat_bb16 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_concat_bb15 # jump -> then
.Lb2dd__bt_parse_concat_bb15:
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 344] # tag L16 from tag-slot
    mov [rbp - 360], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__bt_parse_concat_bb17 # branch
.Lb2dd__bt_parse_concat_bb16:
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 368], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 368] # tag L19 from tag-slot
    mov [rbp - 360], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__bt_parse_concat_bb17 # branch
.Lb2dd__bt_parse_concat_bb17:
    mov r10, [rbp - 160] # reload L18 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_concat_bb19 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_concat_bb18 # jump -> then
.Lb2dd__bt_parse_concat_bb18:
    jmp .Lb2dd__bt_parse_concat_bb11 # branch
.Lb2dd__bt_parse_concat_bb19:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    call _bt_parse_repeat # call _bt_parse_repeat
    mov [rbp - 384], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L22
    mov r11, 0 # tag L21 = TAG_INT (i64-local, fused)
    mov [rbp - 392], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 400], rax # store tag L23
    mov rcx, 6 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 400] # tag L23 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L23
    mov rcx, [rbp - 112] # reload L12 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L12 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 400] # tag L23 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L23
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L22 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 400] # tag L23 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L23
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 400] # tag L23 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    mov rcx, [rbp - 200] # reload L23 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 400] # tag L23 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 408], rax # store tag L24
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 208], r10 # spill L24 to slot
    mov r11, [rbp - 208] # reload L24 from spill slot
    mov r10, r11 # assign L12
    mov r11, 0 # tag L24 = TAG_INT (i64-local, fused)
    mov [rbp - 312], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_parse_concat_bb9 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 368 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _BT_MAX_REPEAT
    .p2align 4
_BT_MAX_REPEAT:
    .loc 1 275 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
.Lb2dd__BT_MAX_REPEAT_bb0:
    mov rdx, 100000 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_parse_repeat
    .p2align 4
_bt_parse_repeat:
    .loc 1 278 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 1072 # prologue: alloc spill frame
    mov [rbp - 568], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_parse_repeat_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_parse_atom # call _bt_parse_atom
    mov [rbp - 576], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov r13, r12 # assign L2
    mov r11, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov [rbp - 584], r11 # store tag L2
    jmp .Lb2dd__bt_parse_repeat_bb1 # branch
.Lb2dd__bt_parse_repeat_bb1:
    mov r10, 1 # materialize test cond to reg
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb2 # jump -> then
.Lb2dd__bt_parse_repeat_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 592], rax # store tag L3
    mov r14, rdx # hv: unbox user-call result payload
    mov r15, r14 # assign L4
    mov r11, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov [rbp - 600], r11 # store tag L4
    cmp r15, 42 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 608], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb5 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb4 # jump -> then
.Lb2dd__bt_parse_repeat_bb3:
    mov rdx, r13 # hv arg payload
    mov rax, 0 # tag L2 = TAG_INT (i64-local, fused)
    add rsp, 1072 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_repeat_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 624], rax # store tag L7
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 72], r10 # spill L7 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 632], rax # store tag L8
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 632] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 632], rax # store tag L8
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 632] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 632], rax # store tag L8
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 632] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 632], rax # store tag L8
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 632] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 632], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 632] # tag L8 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 640], rax # store tag L9
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rbp - 584], r11 # store tag L2
    jmp .Lb2dd__bt_parse_repeat_bb40 # branch
.Lb2dd__bt_parse_repeat_bb5:
    cmp r15, 43 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 648], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb7 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb6 # jump -> then
.Lb2dd__bt_parse_repeat_bb6:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 664], rax # store tag L12
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 112], r10 # spill L12 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 672], rax # store tag L13
    mov rcx, 4 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 672] # tag L13 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 672], rax # store tag L13
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 672] # tag L13 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 672], rax # store tag L13
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 672] # tag L13 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 672], rax # store tag L13
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 672] # tag L13 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 672], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 672] # tag L13 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 680], rax # store tag L14
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L14 = TAG_INT (i64-local, fused)
    mov [rbp - 584], r11 # store tag L2
    jmp .Lb2dd__bt_parse_repeat_bb39 # branch
.Lb2dd__bt_parse_repeat_bb7:
    cmp r15, 63 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 688], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb9 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb8 # jump -> then
.Lb2dd__bt_parse_repeat_bb8:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 704], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 712], rax # store tag L18
    mov rcx, 5 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 712] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 712], rax # store tag L18
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 712] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 712], rax # store tag L18
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 712] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 712], rax # store tag L18
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 712] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 712], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, [rbp - 160] # reload L18 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 712] # tag L18 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 720], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, [rbp - 168] # reload L19 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L19 = TAG_INT (i64-local, fused)
    mov [rbp - 584], r11 # store tag L2
    jmp .Lb2dd__bt_parse_repeat_bb38 # branch
.Lb2dd__bt_parse_repeat_bb9:
    cmp r15, 123 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 728], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb11 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb10 # jump -> then
.Lb2dd__bt_parse_repeat_bb10:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 744], rax # store tag L22
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, 0 # assign L23
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 752], r11 # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, 0 # assign L24
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 760], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__bt_parse_repeat_bb12 # branch
.Lb2dd__bt_parse_repeat_bb11:
    jmp .Lb2dd__bt_parse_repeat_bb3 # branch
.Lb2dd__bt_parse_repeat_bb12:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 768], rax # store tag L25
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 216], r10 # spill L25 to slot
    mov r10, [rbp - 216] # reload L25 from spill slot
    cmp r10, 48 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 776], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r10, [rbp - 224] # reload L26 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb16 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb15 # jump -> then
.Lb2dd__bt_parse_repeat_bb13:
    mov r10, [rbp - 200] # reload L23 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 10 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 808], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 816], rax # store tag L31
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 264], r10 # spill L31 to slot
    mov r10, [rbp - 264] # reload L31 from spill slot
    mov r10, r10 # binop lhs into dst
    sub r10, 48 # binop -
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 824], r11 # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, [rbp - 256] # reload L30 from spill slot
    mov r11, [rbp - 272] # reload L32 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, r11 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 832], r11 # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r11, [rbp - 280] # reload L33 from spill slot
    mov r10, r11 # assign L23
    mov r11, 0 # tag L33 = TAG_INT (i64-local, fused)
    mov [rbp - 752], r11 # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 840], rax # store tag L34
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 288], r10 # spill L34 to slot
    mov r10, 1 # assign L24
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 760], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    call _BT_MAX_REPEAT # call _BT_MAX_REPEAT
    mov [rbp - 848], rax # store tag L35
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 296], r10 # spill L35 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    mov r11, [rbp - 296] # reload L35 from spill slot
    cmp r10, r11 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 856], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov r10, [rbp - 304] # reload L36 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb19 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb18 # jump -> then
.Lb2dd__bt_parse_repeat_bb14:
    mov r11, [rbp - 200] # reload L23 from spill slot
    mov r10, r11 # assign L39
    mov r11, 0 # tag L23 = TAG_INT (i64-local, fused)
    mov [rbp - 880], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 888], rax # store tag L40
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 336], r10 # spill L40 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    cmp r10, 44 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 896], r11 # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r10, [rbp - 344] # reload L41 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb21 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb20 # jump -> then
.Lb2dd__bt_parse_repeat_bb15:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 792], rax # store tag L28
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 240], r10 # spill L28 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    cmp r10, 57 # binop <=
    setle al # binop <= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 800], r11 # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L27
    mov r11, [rbp - 800] # tag L29 from tag-slot
    mov [rbp - 784], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    jmp .Lb2dd__bt_parse_repeat_bb17 # branch
.Lb2dd__bt_parse_repeat_bb16:
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L27
    mov r11, [rbp - 776] # tag L26 from tag-slot
    mov [rbp - 784], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    jmp .Lb2dd__bt_parse_repeat_bb17 # branch
.Lb2dd__bt_parse_repeat_bb17:
    mov r10, [rbp - 232] # reload L27 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb14 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb13 # jump -> then
.Lb2dd__bt_parse_repeat_bb18:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 872], rax # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov rsi, [rbp - 320] # reload L38 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 872] # tag L38 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 872], rax # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1072 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_repeat_bb19:
    jmp .Lb2dd__bt_parse_repeat_bb12 # branch
.Lb2dd__bt_parse_repeat_bb20:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 912], rax # store tag L43
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 360], r10 # spill L43 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 920], rax # store tag L44
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 368], r10 # spill L44 to slot
    mov r10, [rbp - 368] # reload L44 from spill slot
    cmp r10, 125 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 928], r11 # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov r10, [rbp - 376] # reload L45 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb23 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb22 # jump -> then
.Lb2dd__bt_parse_repeat_bb21:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1056], rax # store tag L61
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 504], r10 # spill L61 to slot
    mov r10, [rbp - 504] # reload L61 from spill slot
    cmp r10, 125 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1064], r11 # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov r10, [rbp - 512] # reload L62 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb34 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb33 # jump -> then
.Lb2dd__bt_parse_repeat_bb22:
    mov r10, -1 # assign L39
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 880], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    jmp .Lb2dd__bt_parse_repeat_bb32 # branch
.Lb2dd__bt_parse_repeat_bb23:
    mov r10, 0 # assign L39
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 880], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    jmp .Lb2dd__bt_parse_repeat_bb24 # branch
.Lb2dd__bt_parse_repeat_bb24:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 944], rax # store tag L47
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 392], r10 # spill L47 to slot
    mov r10, [rbp - 392] # reload L47 from spill slot
    cmp r10, 48 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 952], r11 # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    mov r10, [rbp - 400] # reload L48 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb28 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb27 # jump -> then
.Lb2dd__bt_parse_repeat_bb25:
    mov r10, [rbp - 328] # reload L39 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 10 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 984], r11 # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 992], rax # store tag L53
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 440], r10 # spill L53 to slot
    mov r10, [rbp - 440] # reload L53 from spill slot
    mov r10, r10 # binop lhs into dst
    sub r10, 48 # binop -
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1000], r11 # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov r10, [rbp - 432] # reload L52 from spill slot
    mov r11, [rbp - 448] # reload L54 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, r11 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1008], r11 # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    mov r11, [rbp - 456] # reload L55 from spill slot
    mov r10, r11 # assign L39
    mov r11, 0 # tag L55 = TAG_INT (i64-local, fused)
    mov [rbp - 880], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1016], rax # store tag L56
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 464], r10 # spill L56 to slot
    call _BT_MAX_REPEAT # call _BT_MAX_REPEAT
    mov [rbp - 1024], rax # store tag L57
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 472], r10 # spill L57 to slot
    mov r10, [rbp - 328] # reload L39 from spill slot
    mov r11, [rbp - 472] # reload L57 from spill slot
    cmp r10, r11 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1032], r11 # store tag L58
    mov [rbp - 480], r10 # spill L58 to slot
    mov r10, [rbp - 480] # reload L58 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb31 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb30 # jump -> then
.Lb2dd__bt_parse_repeat_bb26:
    jmp .Lb2dd__bt_parse_repeat_bb32 # branch
.Lb2dd__bt_parse_repeat_bb27:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 968], rax # store tag L50
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 416], r10 # spill L50 to slot
    mov r10, [rbp - 416] # reload L50 from spill slot
    cmp r10, 57 # binop <=
    setle al # binop <= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 976], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    mov r11, [rbp - 424] # reload L51 from spill slot
    mov r10, r11 # assign L49
    mov r11, [rbp - 976] # tag L51 from tag-slot
    mov [rbp - 960], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    jmp .Lb2dd__bt_parse_repeat_bb29 # branch
.Lb2dd__bt_parse_repeat_bb28:
    mov r11, [rbp - 400] # reload L48 from spill slot
    mov r10, r11 # assign L49
    mov r11, [rbp - 952] # tag L48 from tag-slot
    mov [rbp - 960], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    jmp .Lb2dd__bt_parse_repeat_bb29 # branch
.Lb2dd__bt_parse_repeat_bb29:
    mov r10, [rbp - 408] # reload L49 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb26 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb25 # jump -> then
.Lb2dd__bt_parse_repeat_bb30:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1048], rax # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov rsi, [rbp - 496] # reload L60 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1048] # tag L60 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1048], rax # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1072 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_repeat_bb31:
    jmp .Lb2dd__bt_parse_repeat_bb24 # branch
.Lb2dd__bt_parse_repeat_bb32:
    jmp .Lb2dd__bt_parse_repeat_bb21 # branch
.Lb2dd__bt_parse_repeat_bb33:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1080], rax # store tag L64
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 528], r10 # spill L64 to slot
    jmp .Lb2dd__bt_parse_repeat_bb34 # branch
.Lb2dd__bt_parse_repeat_bb34:
    mov rsi, [rbp - 208] # reload L24 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 760] # tag L24 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1088], r11 # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    mov r10, [rbp - 536] # reload L65 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_repeat_bb36 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_repeat_bb35 # jump -> then
.Lb2dd__bt_parse_repeat_bb35:
    mov r10, 0 # assign L23
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 752], r11 # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    jmp .Lb2dd__bt_parse_repeat_bb36 # branch
.Lb2dd__bt_parse_repeat_bb36:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1104], rax # store tag L67
    mov rcx, 11 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1104] # tag L67 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1104], rax # store tag L67
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1104] # tag L67 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1104], rax # store tag L67
    mov rcx, [rbp - 200] # reload L23 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L23 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1104] # tag L67 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1104], rax # store tag L67
    mov rcx, [rbp - 328] # reload L39 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L39 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1104] # tag L67 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1104], rax # store tag L67
    mov [rbp - 552], r10 # spill L67 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, [rbp - 552] # reload L67 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1104] # tag L67 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 1112], rax # store tag L68
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 560], r10 # spill L68 to slot
    mov r10, [rbp - 560] # reload L68 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L68 = TAG_INT (i64-local, fused)
    mov [rbp - 584], r11 # store tag L2
    jmp .Lb2dd__bt_parse_repeat_bb37 # branch
.Lb2dd__bt_parse_repeat_bb37:
    jmp .Lb2dd__bt_parse_repeat_bb38 # branch
.Lb2dd__bt_parse_repeat_bb38:
    jmp .Lb2dd__bt_parse_repeat_bb39 # branch
.Lb2dd__bt_parse_repeat_bb39:
    jmp .Lb2dd__bt_parse_repeat_bb40 # branch
.Lb2dd__bt_parse_repeat_bb40:
    jmp .Lb2dd__bt_parse_repeat_bb1 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 1072 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_parse_atom
    .p2align 4
_bt_parse_atom:
    .loc 1 315 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 2304 # prologue: alloc spill frame
    mov [rbp - 1184], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_parse_atom_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1192], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov r13, r12 # assign L2
    mov r11, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov [rbp - 1200], r11 # store tag L2
    cmp r13, 40 # binop ==
    sete al # binop == → al
    movzx r14, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1208], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb1 # jump -> then
.Lb2dd__bt_parse_atom_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1224], rax # store tag L5
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1232], rax # store tag L6
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    cmp r10, 63 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1240], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb4 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb3 # jump -> then
.Lb2dd__bt_parse_atom_bb2:
    cmp r13, 91 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1912], r11 # store tag L91
    mov [rbp - 744], r10 # spill L91 to slot
    mov r10, [rbp - 744] # reload L91 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb39 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb38 # jump -> then
.Lb2dd__bt_parse_atom_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1256], rax # store tag L9
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1264], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, 0 # tag L10 = TAG_INT (i64-local, fused)
    mov [rbp - 1272], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    cmp r10, 58 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1280], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb6 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb5 # jump -> then
.Lb2dd__bt_parse_atom_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr6] # map key (const char*): &.LCstr6
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1784], rax # store tag L75
    mov [rbp - 616], r10 # spill L75 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr6] # map key (const char*): &.LCstr6
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1792], rax # store tag L76
    mov [rbp - 624], r10 # spill L76 to slot
    mov rsi, [rbp - 624] # reload L76 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1792] # tag L76 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1800], rax # store tag L77
    mov [rbp - 632], r10 # spill L77 to slot
    mov r10, [rbp - 632] # reload L77 from spill slot
    mov rsi, [rbp - 632] # reload L77 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1800] # tag L77 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 1808], rax # store tag L78
    mov [rbp - 640], r10 # spill L78 to slot
    mov rsi, [rbp - 616] # reload L75 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1784] # tag L75 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, [rbp - 640] # reload L78 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L78 = TAG_INT (i64-local, fused)
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1784], rax # store tag L75
    mov [rbp - 616], r10 # spill L75 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr6] # map key (const char*): &.LCstr6
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1816], rax # store tag L79
    mov [rbp - 648], r10 # spill L79 to slot
    mov rsi, [rbp - 648] # reload L79 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1816] # tag L79 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1824], rax # store tag L80
    mov [rbp - 656], r10 # spill L80 to slot
    mov r11, [rbp - 656] # reload L80 from spill slot
    mov r10, r11 # assign L81
    mov r11, [rbp - 1824] # tag L80 from tag-slot
    mov [rbp - 1832], r11 # store tag L81
    mov [rbp - 664], r10 # spill L81 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_parse_alt # call _bt_parse_alt
    mov [rbp - 1840], rax # store tag L82
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 672], r10 # spill L82 to slot
    mov r11, [rbp - 672] # reload L82 from spill slot
    mov r10, r11 # assign L83
    mov r11, 0 # tag L82 = TAG_INT (i64-local, fused)
    mov [rbp - 1848], r11 # store tag L83
    mov [rbp - 680], r10 # spill L83 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1856], rax # store tag L84
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 688], r10 # spill L84 to slot
    mov r10, [rbp - 688] # reload L84 from spill slot
    cmp r10, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1864], r11 # store tag L85
    mov [rbp - 696], r10 # spill L85 to slot
    mov r10, [rbp - 696] # reload L85 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb36 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb35 # jump -> then
.Lb2dd__bt_parse_atom_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1296], rax # store tag L14
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 128], r10 # spill L14 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_parse_alt # call _bt_parse_alt
    mov [rbp - 1304], rax # store tag L15
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L16
    mov r11, 0 # tag L15 = TAG_INT (i64-local, fused)
    mov [rbp - 1312], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1320], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    cmp r10, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1328], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov r10, [rbp - 160] # reload L18 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb8 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb7 # jump -> then
.Lb2dd__bt_parse_atom_bb6:
    mov r10, [rbp - 104] # reload L11 from spill slot
    cmp r10, 61 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1360], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb11 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb10 # jump -> then
.Lb2dd__bt_parse_atom_bb7:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1344], rax # store tag L20
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__bt_parse_atom_bb9 # branch
.Lb2dd__bt_parse_atom_bb8:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1352], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov rsi, [rbp - 184] # reload L21 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1352] # tag L21 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1352], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    jmp .Lb2dd__bt_parse_atom_bb9 # branch
.Lb2dd__bt_parse_atom_bb9:
    mov rdx, [rbp - 144] # reload L16 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L16 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb10:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1376], rax # store tag L24
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 208], r10 # spill L24 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_parse_alt # call _bt_parse_alt
    mov [rbp - 1384], rax # store tag L25
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 216], r10 # spill L25 to slot
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L26
    mov r11, 0 # tag L25 = TAG_INT (i64-local, fused)
    mov [rbp - 1392], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1400], rax # store tag L27
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, [rbp - 232] # reload L27 from spill slot
    cmp r10, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1408], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb13 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb12 # jump -> then
.Lb2dd__bt_parse_atom_bb11:
    mov r10, [rbp - 104] # reload L11 from spill slot
    cmp r10, 33 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1456], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb16 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb15 # jump -> then
.Lb2dd__bt_parse_atom_bb12:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1424], rax # store tag L30
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 256], r10 # spill L30 to slot
    jmp .Lb2dd__bt_parse_atom_bb14 # branch
.Lb2dd__bt_parse_atom_bb13:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1432], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1432] # tag L31 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1432], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    jmp .Lb2dd__bt_parse_atom_bb14 # branch
.Lb2dd__bt_parse_atom_bb14:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1440], rax # store tag L32
    mov rcx, 14 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1440] # tag L32 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1440], rax # store tag L32
    mov rcx, [rbp - 224] # reload L26 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L26 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1440] # tag L32 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1440], rax # store tag L32
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1440] # tag L32 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1440], rax # store tag L32
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1440] # tag L32 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1440], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 272] # reload L32 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1440] # tag L32 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 1448], rax # store tag L33
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 280], r10 # spill L33 to slot
    mov rdx, [rbp - 280] # reload L33 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L33 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb15:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1472], rax # store tag L36
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 304], r10 # spill L36 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_parse_alt # call _bt_parse_alt
    mov [rbp - 1480], rax # store tag L37
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 312], r10 # spill L37 to slot
    mov r11, [rbp - 312] # reload L37 from spill slot
    mov r10, r11 # assign L38
    mov r11, 0 # tag L37 = TAG_INT (i64-local, fused)
    mov [rbp - 1488], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1496], rax # store tag L39
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 328], r10 # spill L39 to slot
    mov r10, [rbp - 328] # reload L39 from spill slot
    cmp r10, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1504], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb18 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb17 # jump -> then
.Lb2dd__bt_parse_atom_bb16:
    mov r10, [rbp - 104] # reload L11 from spill slot
    cmp r10, 60 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1552], r11 # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    mov r10, [rbp - 384] # reload L46 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb21 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb20 # jump -> then
.Lb2dd__bt_parse_atom_bb17:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1520], rax # store tag L42
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 352], r10 # spill L42 to slot
    jmp .Lb2dd__bt_parse_atom_bb19 # branch
.Lb2dd__bt_parse_atom_bb18:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1528], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov rsi, [rbp - 360] # reload L43 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1528] # tag L43 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1528], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    jmp .Lb2dd__bt_parse_atom_bb19 # branch
.Lb2dd__bt_parse_atom_bb19:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1536], rax # store tag L44
    mov rcx, 14 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1536] # tag L44 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1536], rax # store tag L44
    mov rcx, [rbp - 320] # reload L38 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L38 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1536] # tag L44 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1536], rax # store tag L44
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1536] # tag L44 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1536], rax # store tag L44
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1536] # tag L44 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1536], rax # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 368] # reload L44 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1536] # tag L44 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 1544], rax # store tag L45
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 376], r10 # spill L45 to slot
    mov rdx, [rbp - 376] # reload L45 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L45 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb20:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1568], rax # store tag L48
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 400], r10 # spill L48 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1576], rax # store tag L49
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 408], r10 # spill L49 to slot
    mov r11, [rbp - 408] # reload L49 from spill slot
    mov r10, r11 # assign L50
    mov r11, 0 # tag L49 = TAG_INT (i64-local, fused)
    mov [rbp - 1584], r11 # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    mov r10, [rbp - 416] # reload L50 from spill slot
    cmp r10, 61 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1592], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    mov r10, [rbp - 424] # reload L51 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb23 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb22 # jump -> then
.Lb2dd__bt_parse_atom_bb21:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1776], rax # store tag L74
    mov [rbp - 608], r10 # spill L74 to slot
    mov rsi, [rbp - 608] # reload L74 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1776] # tag L74 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1776], rax # store tag L74
    mov [rbp - 608], r10 # spill L74 to slot
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb22:
    mov r11, [rbp - 424] # reload L51 from spill slot
    mov r10, r11 # assign L52
    mov r11, [rbp - 1592] # tag L51 from tag-slot
    mov [rbp - 1600], r11 # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    jmp .Lb2dd__bt_parse_atom_bb24 # branch
.Lb2dd__bt_parse_atom_bb23:
    mov r10, [rbp - 416] # reload L50 from spill slot
    cmp r10, 33 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1608], r11 # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    mov r11, [rbp - 440] # reload L53 from spill slot
    mov r10, r11 # assign L52
    mov r11, [rbp - 1608] # tag L53 from tag-slot
    mov [rbp - 1600], r11 # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    jmp .Lb2dd__bt_parse_atom_bb24 # branch
.Lb2dd__bt_parse_atom_bb24:
    mov r10, [rbp - 432] # reload L52 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb26 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb25 # jump -> then
.Lb2dd__bt_parse_atom_bb25:
    mov r10, [rbp - 416] # reload L50 from spill slot
    cmp r10, 33 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1624], r11 # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    mov r10, [rbp - 456] # reload L55 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb28 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb27 # jump -> then
.Lb2dd__bt_parse_atom_bb26:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1768], rax # store tag L73
    mov [rbp - 600], r10 # spill L73 to slot
    mov rsi, [rbp - 600] # reload L73 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1768] # tag L73 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1768], rax # store tag L73
    mov [rbp - 600], r10 # spill L73 to slot
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb27:
    mov r10, 1 # assign L56
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 1632], r11 # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    jmp .Lb2dd__bt_parse_atom_bb29 # branch
.Lb2dd__bt_parse_atom_bb28:
    mov r10, 0 # assign L56
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 1632], r11 # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    jmp .Lb2dd__bt_parse_atom_bb29 # branch
.Lb2dd__bt_parse_atom_bb29:
    mov r11, [rbp - 464] # reload L56 from spill slot
    mov r10, r11 # assign L57
    mov r11, 0 # tag L56 = TAG_INT (i64-local, fused)
    mov [rbp - 1640], r11 # store tag L57
    mov [rbp - 472], r10 # spill L57 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1648], rax # store tag L58
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 480], r10 # spill L58 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_parse_alt # call _bt_parse_alt
    mov [rbp - 1656], rax # store tag L59
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 488], r10 # spill L59 to slot
    mov r11, [rbp - 488] # reload L59 from spill slot
    mov r10, r11 # assign L60
    mov r11, 0 # tag L59 = TAG_INT (i64-local, fused)
    mov [rbp - 1664], r11 # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 1672], rax # store tag L61
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 504], r10 # spill L61 to slot
    mov r10, [rbp - 504] # reload L61 from spill slot
    cmp r10, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1680], r11 # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov r10, [rbp - 512] # reload L62 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb31 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb30 # jump -> then
.Lb2dd__bt_parse_atom_bb30:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1696], rax # store tag L64
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 528], r10 # spill L64 to slot
    jmp .Lb2dd__bt_parse_atom_bb32 # branch
.Lb2dd__bt_parse_atom_bb31:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1704], rax # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    mov rsi, [rbp - 536] # reload L65 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1704] # tag L65 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1704], rax # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    jmp .Lb2dd__bt_parse_atom_bb32 # branch
.Lb2dd__bt_parse_atom_bb32:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1712], rax # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    mov rsi, [rbp - 544] # reload L66 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1712] # tag L66 from tag-slot
    mov rcx, [rbp - 496] # reload L60 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L60 = TAG_INT (i64-local, fused)
    call _bt_maxwidth # call _bt_maxwidth
    mov [rbp - 1720], rax # store tag L67
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 552], r10 # spill L67 to slot
    mov r10, [rbp - 552] # reload L67 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1728], r11 # store tag L68
    mov [rbp - 560], r10 # spill L68 to slot
    mov r10, [rbp - 560] # reload L68 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb34 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb33 # jump -> then
.Lb2dd__bt_parse_atom_bb33:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1744], rax # store tag L70
    mov [rbp - 576], r10 # spill L70 to slot
    mov rsi, [rbp - 576] # reload L70 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1744] # tag L70 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1744], rax # store tag L70
    mov [rbp - 576], r10 # spill L70 to slot
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb34:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1752], rax # store tag L71
    mov rcx, 15 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1752] # tag L71 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1752], rax # store tag L71
    mov rcx, [rbp - 496] # reload L60 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L60 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1752] # tag L71 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1752], rax # store tag L71
    mov rcx, [rbp - 472] # reload L57 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L57 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1752] # tag L71 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1752], rax # store tag L71
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1752] # tag L71 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1752], rax # store tag L71
    mov [rbp - 584], r10 # spill L71 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 584] # reload L71 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1752] # tag L71 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 1760], rax # store tag L72
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 592], r10 # spill L72 to slot
    mov rdx, [rbp - 592] # reload L72 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L72 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb35:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1880], rax # store tag L87
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 712], r10 # spill L87 to slot
    jmp .Lb2dd__bt_parse_atom_bb37 # branch
.Lb2dd__bt_parse_atom_bb36:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1888], rax # store tag L88
    mov [rbp - 720], r10 # spill L88 to slot
    mov rsi, [rbp - 720] # reload L88 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1888] # tag L88 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1888], rax # store tag L88
    mov [rbp - 720], r10 # spill L88 to slot
    jmp .Lb2dd__bt_parse_atom_bb37 # branch
.Lb2dd__bt_parse_atom_bb37:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1896], rax # store tag L89
    mov rcx, 12 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1896] # tag L89 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1896], rax # store tag L89
    mov rcx, [rbp - 680] # reload L83 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L83 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1896] # tag L89 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1896], rax # store tag L89
    mov rcx, [rbp - 664] # reload L81 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1832] # tag L81 from tag-slot
    mov rdi, [rbp - 1896] # tag L89 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1896], rax # store tag L89
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1896] # tag L89 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1896], rax # store tag L89
    mov [rbp - 728], r10 # spill L89 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 728] # reload L89 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1896] # tag L89 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 1904], rax # store tag L90
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 736], r10 # spill L90 to slot
    mov rdx, [rbp - 736] # reload L90 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L90 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb38:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1928], rax # store tag L93
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 760], r10 # spill L93 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_parse_class # call _bt_parse_class
    mov [rbp - 1936], rax # store tag L94
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 768], r10 # spill L94 to slot
    mov rdx, [rbp - 768] # reload L94 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L94 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb39:
    cmp r13, 46 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1944], r11 # store tag L95
    mov [rbp - 776], r10 # spill L95 to slot
    mov r10, [rbp - 776] # reload L95 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb41 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb40 # jump -> then
.Lb2dd__bt_parse_atom_bb40:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 1960], rax # store tag L97
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 792], r10 # spill L97 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1968], rax # store tag L98
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1968] # tag L98 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1968], rax # store tag L98
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1968] # tag L98 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1968], rax # store tag L98
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1968] # tag L98 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1968], rax # store tag L98
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 1968] # tag L98 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1968], rax # store tag L98
    mov [rbp - 800], r10 # spill L98 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 800] # reload L98 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1968] # tag L98 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 1976], rax # store tag L99
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 808], r10 # spill L99 to slot
    mov rdx, [rbp - 808] # reload L99 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L99 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb41:
    cmp r13, 94 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1984], r11 # store tag L100
    mov [rbp - 816], r10 # spill L100 to slot
    mov r10, [rbp - 816] # reload L100 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb43 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb42 # jump -> then
.Lb2dd__bt_parse_atom_bb42:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 2000], rax # store tag L102
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 832], r10 # spill L102 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2008], rax # store tag L103
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2008] # tag L103 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2008], rax # store tag L103
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2008] # tag L103 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2008], rax # store tag L103
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2008] # tag L103 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2008], rax # store tag L103
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2008] # tag L103 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2008], rax # store tag L103
    mov [rbp - 840], r10 # spill L103 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 840] # reload L103 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2008] # tag L103 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 2016], rax # store tag L104
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 848], r10 # spill L104 to slot
    mov rdx, [rbp - 848] # reload L104 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L104 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb43:
    cmp r13, 36 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2024], r11 # store tag L105
    mov [rbp - 856], r10 # spill L105 to slot
    mov r10, [rbp - 856] # reload L105 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb45 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb44 # jump -> then
.Lb2dd__bt_parse_atom_bb44:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 2040], rax # store tag L107
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 872], r10 # spill L107 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2048], rax # store tag L108
    mov rcx, 10 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2048] # tag L108 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2048], rax # store tag L108
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2048] # tag L108 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2048], rax # store tag L108
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2048] # tag L108 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2048], rax # store tag L108
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2048] # tag L108 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2048], rax # store tag L108
    mov [rbp - 880], r10 # spill L108 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 880] # reload L108 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2048] # tag L108 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 2056], rax # store tag L109
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 888], r10 # spill L109 to slot
    mov rdx, [rbp - 888] # reload L109 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L109 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb45:
    cmp r13, 92 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2064], r11 # store tag L110
    mov [rbp - 896], r10 # spill L110 to slot
    mov r10, [rbp - 896] # reload L110 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb47 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb46 # jump -> then
.Lb2dd__bt_parse_atom_bb46:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 2080], rax # store tag L112
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 912], r10 # spill L112 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_peek # call _bt_peek
    mov [rbp - 2088], rax # store tag L113
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 920], r10 # spill L113 to slot
    mov r11, [rbp - 920] # reload L113 from spill slot
    mov r10, r11 # assign L114
    mov r11, 0 # tag L113 = TAG_INT (i64-local, fused)
    mov [rbp - 2096], r11 # store tag L114
    mov [rbp - 928], r10 # spill L114 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 2104], rax # store tag L115
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 936], r10 # spill L115 to slot
    mov r10, [rbp - 928] # reload L114 from spill slot
    cmp r10, 49 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2112], r11 # store tag L116
    mov [rbp - 944], r10 # spill L116 to slot
    mov r10, [rbp - 944] # reload L116 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb49 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb48 # jump -> then
.Lb2dd__bt_parse_atom_bb47:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    call _bt_adv # call _bt_adv
    mov [rbp - 2328], rax # store tag L143
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1160], r10 # spill L143 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2336], rax # store tag L144
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2336] # tag L144 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2336], rax # store tag L144
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2336] # tag L144 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2336], rax # store tag L144
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2336] # tag L144 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2336], rax # store tag L144
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2336] # tag L144 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2336], rax # store tag L144
    mov [rbp - 1168], r10 # spill L144 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 1168] # reload L144 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2336] # tag L144 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 2344], rax # store tag L145
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1176], r10 # spill L145 to slot
    mov rdx, [rbp - 1176] # reload L145 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L145 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb48:
    mov r10, [rbp - 928] # reload L114 from spill slot
    cmp r10, 57 # binop <=
    setle al # binop <= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2128], r11 # store tag L118
    mov [rbp - 960], r10 # spill L118 to slot
    mov r11, [rbp - 960] # reload L118 from spill slot
    mov r10, r11 # assign L117
    mov r11, [rbp - 2128] # tag L118 from tag-slot
    mov [rbp - 2120], r11 # store tag L117
    mov [rbp - 952], r10 # spill L117 to slot
    jmp .Lb2dd__bt_parse_atom_bb50 # branch
.Lb2dd__bt_parse_atom_bb49:
    mov r11, [rbp - 944] # reload L116 from spill slot
    mov r10, r11 # assign L117
    mov r11, [rbp - 2112] # tag L116 from tag-slot
    mov [rbp - 2120], r11 # store tag L117
    mov [rbp - 952], r10 # spill L117 to slot
    jmp .Lb2dd__bt_parse_atom_bb50 # branch
.Lb2dd__bt_parse_atom_bb50:
    mov r10, [rbp - 952] # reload L117 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb52 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb51 # jump -> then
.Lb2dd__bt_parse_atom_bb51:
    mov r10, [rbp - 928] # reload L114 from spill slot
    mov r10, r10 # binop lhs into dst
    sub r10, 48 # binop -
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2144], r11 # store tag L120
    mov [rbp - 976], r10 # spill L120 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2152], rax # store tag L121
    mov rcx, 13 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2152] # tag L121 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2152], rax # store tag L121
    mov rcx, [rbp - 976] # reload L120 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L120 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2152] # tag L121 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2152], rax # store tag L121
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2152] # tag L121 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2152], rax # store tag L121
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2152] # tag L121 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2152], rax # store tag L121
    mov [rbp - 984], r10 # spill L121 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 984] # reload L121 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2152] # tag L121 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 2160], rax # store tag L122
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 992], r10 # spill L122 to slot
    mov rdx, [rbp - 992] # reload L122 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L122 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb52:
    mov rsi, [rbp - 928] # reload L114 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L114 = TAG_INT (i64-local, fused)
    call _bt_class_for_escape # call _bt_class_for_escape
    mov [rbp - 2168], rax # store tag L123
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1000], r10 # spill L123 to slot
    mov r11, [rbp - 1000] # reload L123 from spill slot
    mov r10, r11 # assign L124
    mov r11, [rbp - 2168] # tag L123 from tag-slot
    mov [rbp - 2176], r11 # store tag L124
    mov [rbp - 1008], r10 # spill L124 to slot
    mov rsi, [rbp - 1008] # reload L124 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2176] # tag L124 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2184], r11 # store tag L125
    mov [rbp - 1016], r10 # spill L125 to slot
    mov r10, [rbp - 1016] # reload L125 from spill slot
    mov rsi, [rbp - 1016] # reload L125 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2184] # tag L125 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_gt # binop >: tag-dispatch hexa_cmp_gt
    mov r10, rdx # binop >: capture bool payload
    mov [rbp - 2192], rax # store tag L126
    mov [rbp - 1024], r10 # spill L126 to slot
    mov r10, [rbp - 1024] # reload L126 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb54 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb53 # jump -> then
.Lb2dd__bt_parse_atom_bb53:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 2208], rax # store tag L128
    mov [rbp - 1040], r10 # spill L128 to slot
    mov rsi, [rbp - 1040] # reload L128 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2208] # tag L128 from tag-slot
    mov rcx, [rbp - 1008] # reload L124 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2176] # tag L124 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 2216], rax # store tag L129
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 1048], r10 # spill L129 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 2224], rax # store tag L130
    mov [rbp - 1056], r10 # spill L130 to slot
    mov rsi, [rbp - 1056] # reload L130 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2224] # tag L130 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2232], r11 # store tag L131
    mov [rbp - 1064], r10 # spill L131 to slot
    mov r10, [rbp - 1064] # reload L131 from spill slot
    mov rsi, [rbp - 1064] # reload L131 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2232] # tag L131 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 2240], rax # store tag L132
    mov [rbp - 1072], r10 # spill L132 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2248], rax # store tag L133
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2248] # tag L133 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2248], rax # store tag L133
    mov rcx, [rbp - 1072] # reload L132 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L132 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2248] # tag L133 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2248], rax # store tag L133
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2248] # tag L133 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2248], rax # store tag L133
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2248] # tag L133 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2248], rax # store tag L133
    mov [rbp - 1080], r10 # spill L133 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 1080] # reload L133 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2248] # tag L133 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 2256], rax # store tag L134
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1088], r10 # spill L134 to slot
    mov rdx, [rbp - 1088] # reload L134 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L134 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb54:
    mov rsi, [rbp - 928] # reload L114 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L114 = TAG_INT (i64-local, fused)
    call _bt_lit_for_escape # call _bt_lit_for_escape
    mov [rbp - 2264], rax # store tag L135
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1096], r10 # spill L135 to slot
    mov r11, [rbp - 1096] # reload L135 from spill slot
    mov r10, r11 # assign L136
    mov r11, 0 # tag L135 = TAG_INT (i64-local, fused)
    mov [rbp - 2272], r11 # store tag L136
    mov [rbp - 1104], r10 # spill L136 to slot
    mov r10, [rbp - 1104] # reload L136 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2280], r11 # store tag L137
    mov [rbp - 1112], r10 # spill L137 to slot
    mov r10, [rbp - 1112] # reload L137 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_parse_atom_bb56 # jump-if-zero -> else
    jmp .Lb2dd__bt_parse_atom_bb55 # jump -> then
.Lb2dd__bt_parse_atom_bb55:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2296], rax # store tag L139
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2296] # tag L139 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2296], rax # store tag L139
    mov rcx, [rbp - 1104] # reload L136 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L136 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2296] # tag L139 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2296], rax # store tag L139
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2296] # tag L139 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2296], rax # store tag L139
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2296] # tag L139 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2296], rax # store tag L139
    mov [rbp - 1128], r10 # spill L139 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 1128] # reload L139 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2296] # tag L139 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 2304], rax # store tag L140
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1136], r10 # spill L140 to slot
    mov rdx, [rbp - 1136] # reload L140 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L140 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_parse_atom_bb56:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2312], rax # store tag L141
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2312] # tag L141 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2312], rax # store tag L141
    mov rcx, [rbp - 928] # reload L114 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L114 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2312] # tag L141 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2312], rax # store tag L141
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2312] # tag L141 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2312], rax # store tag L141
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 2312] # tag L141 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2312], rax # store tag L141
    mov [rbp - 1144], r10 # spill L141 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1184] # tag L0 from tag-slot
    mov rcx, [rbp - 1144] # reload L141 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2312] # tag L141 from tag-slot
    call _bt_emit # call _bt_emit
    mov [rbp - 2320], rax # store tag L142
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1152], r10 # spill L142 to slot
    mov rdx, [rbp - 1152] # reload L142 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L142 = TAG_INT (i64-local, fused)
    add rsp, 2304 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_width
    .p2align 4
_bt_width:
    .loc 1 387 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 1152 # prologue: alloc spill frame
    mov [rbp - 608], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 616], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__bt_width_bb0:
    cmp r12, 0 # binop <
    setl al # binop < → al
    movzx r13, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 624], r11 # store tag L2
    test r13, r13 # br_cond test
    jz .Lb2dd__bt_width_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb1 # jump -> then
.Lb2dd__bt_width_bb1:
    mov r14, r13 # assign L3
    mov r11, [rbp - 624] # tag L2 from tag-slot
    mov [rbp - 632], r11 # store tag L3
    jmp .Lb2dd__bt_width_bb3 # branch
.Lb2dd__bt_width_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 608] # tag L0 from tag-slot
    call hexa_len # call hexa_len
    mov r15, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 640], r11 # store tag L4
    mov rsi, r12 # hv arg payload
    mov rdi, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 640] # tag L4 from tag-slot
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 648], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r14, r10 # assign L3
    mov r11, [rbp - 648] # tag L5 from tag-slot
    mov [rbp - 632], r11 # store tag L3
    jmp .Lb2dd__bt_width_bb3 # branch
.Lb2dd__bt_width_bb3:
    test r14, r14 # br_cond test
    jz .Lb2dd__bt_width_bb5 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb4 # jump -> then
.Lb2dd__bt_width_bb4:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 608] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 664], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L8
    mov r11, [rbp - 664] # tag L7 from tag-slot
    mov [rbp - 672], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 680], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r10, r11 # assign L10
    mov r11, [rbp - 680] # tag L9 from tag-slot
    mov [rbp - 688], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 696], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb7 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb6 # jump -> then
.Lb2dd__bt_width_bb6:
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 696] # tag L11 from tag-slot
    mov [rbp - 704], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_width_bb8 # branch
.Lb2dd__bt_width_bb7:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 712], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 712] # tag L13 from tag-slot
    mov [rbp - 704], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_width_bb8 # branch
.Lb2dd__bt_width_bb8:
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb10 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb9 # jump -> then
.Lb2dd__bt_width_bb9:
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 704] # tag L12 from tag-slot
    mov [rbp - 720], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_width_bb11 # branch
.Lb2dd__bt_width_bb10:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 728], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 728] # tag L15 from tag-slot
    mov [rbp - 720], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_width_bb11 # branch
.Lb2dd__bt_width_bb11:
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb13 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb12 # jump -> then
.Lb2dd__bt_width_bb12:
    mov rdx, 1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb13:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 8 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 744], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb15 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb14 # jump -> then
.Lb2dd__bt_width_bb14:
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 744] # tag L17 from tag-slot
    mov [rbp - 752], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__bt_width_bb16 # branch
.Lb2dd__bt_width_bb15:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 760], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 760] # tag L19 from tag-slot
    mov [rbp - 752], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__bt_width_bb16 # branch
.Lb2dd__bt_width_bb16:
    mov r10, [rbp - 160] # reload L18 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb18 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb17 # jump -> then
.Lb2dd__bt_width_bb17:
    mov r11, [rbp - 160] # reload L18 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 752] # tag L18 from tag-slot
    mov [rbp - 768], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__bt_width_bb19 # branch
.Lb2dd__bt_width_bb18:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 10 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 776], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 776] # tag L21 from tag-slot
    mov [rbp - 768], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__bt_width_bb19 # branch
.Lb2dd__bt_width_bb19:
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb21 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb20 # jump -> then
.Lb2dd__bt_width_bb20:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb21:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 14 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 792], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb23 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb22 # jump -> then
.Lb2dd__bt_width_bb22:
    mov r11, [rbp - 200] # reload L23 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 792] # tag L23 from tag-slot
    mov [rbp - 800], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__bt_width_bb24 # branch
.Lb2dd__bt_width_bb23:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 15 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 808], rax # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 808] # tag L25 from tag-slot
    mov [rbp - 800], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__bt_width_bb24 # branch
.Lb2dd__bt_width_bb24:
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb26 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb25 # jump -> then
.Lb2dd__bt_width_bb25:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb26:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 12 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 824], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, [rbp - 232] # reload L27 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb28 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb27 # jump -> then
.Lb2dd__bt_width_bb27:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 840], rax # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 608] # tag L0 from tag-slot
    mov rcx, [rbp - 248] # reload L29 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 840] # tag L29 from tag-slot
    call _bt_width # call _bt_width
    mov [rbp - 848], rax # store tag L30
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 256], r10 # spill L30 to slot
    mov rdx, [rbp - 256] # reload L30 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L30 = TAG_INT (i64-local, fused)
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb28:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 6 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 856], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov r10, [rbp - 264] # reload L31 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb30 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb29 # jump -> then
.Lb2dd__bt_width_bb29:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 872], rax # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 608] # tag L0 from tag-slot
    mov rcx, [rbp - 280] # reload L33 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 872] # tag L33 from tag-slot
    call _bt_width # call _bt_width
    mov [rbp - 880], rax # store tag L34
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 288], r10 # spill L34 to slot
    mov r11, [rbp - 288] # reload L34 from spill slot
    mov r10, r11 # assign L35
    mov r11, 0 # tag L34 = TAG_INT (i64-local, fused)
    mov [rbp - 888], r11 # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 896], rax # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 608] # tag L0 from tag-slot
    mov rcx, [rbp - 304] # reload L36 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 896] # tag L36 from tag-slot
    call _bt_width # call _bt_width
    mov [rbp - 904], rax # store tag L37
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 312], r10 # spill L37 to slot
    mov r11, [rbp - 312] # reload L37 from spill slot
    mov r10, r11 # assign L38
    mov r11, 0 # tag L37 = TAG_INT (i64-local, fused)
    mov [rbp - 912], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov r10, [rbp - 296] # reload L35 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 920], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov r10, [rbp - 328] # reload L39 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb32 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb31 # jump -> then
.Lb2dd__bt_width_bb30:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 7 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 960], rax # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov r10, [rbp - 368] # reload L44 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb37 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb36 # jump -> then
.Lb2dd__bt_width_bb31:
    mov r11, [rbp - 328] # reload L39 from spill slot
    mov r10, r11 # assign L40
    mov r11, [rbp - 920] # tag L39 from tag-slot
    mov [rbp - 928], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    jmp .Lb2dd__bt_width_bb33 # branch
.Lb2dd__bt_width_bb32:
    mov r10, [rbp - 320] # reload L38 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 936], r11 # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r11, [rbp - 344] # reload L41 from spill slot
    mov r10, r11 # assign L40
    mov r11, [rbp - 936] # tag L41 from tag-slot
    mov [rbp - 928], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    jmp .Lb2dd__bt_width_bb33 # branch
.Lb2dd__bt_width_bb33:
    mov r10, [rbp - 336] # reload L40 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb35 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb34 # jump -> then
.Lb2dd__bt_width_bb34:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb35:
    mov r10, [rbp - 296] # reload L35 from spill slot
    mov r11, [rbp - 320] # reload L38 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, r11 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 952], r11 # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov rdx, [rbp - 360] # reload L43 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L43 = TAG_INT (i64-local, fused)
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb36:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 976], rax # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 608] # tag L0 from tag-slot
    mov rcx, [rbp - 384] # reload L46 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 976] # tag L46 from tag-slot
    call _bt_width # call _bt_width
    mov [rbp - 984], rax # store tag L47
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 392], r10 # spill L47 to slot
    mov r11, [rbp - 392] # reload L47 from spill slot
    mov r10, r11 # assign L48
    mov r11, 0 # tag L47 = TAG_INT (i64-local, fused)
    mov [rbp - 992], r11 # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1000], rax # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 608] # tag L0 from tag-slot
    mov rcx, [rbp - 408] # reload L49 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1000] # tag L49 from tag-slot
    call _bt_width # call _bt_width
    mov [rbp - 1008], rax # store tag L50
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 416], r10 # spill L50 to slot
    mov r11, [rbp - 416] # reload L50 from spill slot
    mov r10, r11 # assign L51
    mov r11, 0 # tag L50 = TAG_INT (i64-local, fused)
    mov [rbp - 1016], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    mov r10, [rbp - 400] # reload L48 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1024], r11 # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov r10, [rbp - 432] # reload L52 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb39 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb38 # jump -> then
.Lb2dd__bt_width_bb37:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L10 from tag-slot
    mov rcx, 11 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1072], rax # store tag L58
    mov [rbp - 480], r10 # spill L58 to slot
    mov r10, [rbp - 480] # reload L58 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb47 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb46 # jump -> then
.Lb2dd__bt_width_bb38:
    mov r11, [rbp - 432] # reload L52 from spill slot
    mov r10, r11 # assign L53
    mov r11, [rbp - 1024] # tag L52 from tag-slot
    mov [rbp - 1032], r11 # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    jmp .Lb2dd__bt_width_bb40 # branch
.Lb2dd__bt_width_bb39:
    mov r10, [rbp - 424] # reload L51 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1040], r11 # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov r11, [rbp - 448] # reload L54 from spill slot
    mov r10, r11 # assign L53
    mov r11, [rbp - 1040] # tag L54 from tag-slot
    mov [rbp - 1032], r11 # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    jmp .Lb2dd__bt_width_bb40 # branch
.Lb2dd__bt_width_bb40:
    mov r10, [rbp - 440] # reload L53 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb42 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb41 # jump -> then
.Lb2dd__bt_width_bb41:
    mov r11, [rbp - 440] # reload L53 from spill slot
    mov r10, r11 # assign L55
    mov r11, [rbp - 1032] # tag L53 from tag-slot
    mov [rbp - 1048], r11 # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    jmp .Lb2dd__bt_width_bb43 # branch
.Lb2dd__bt_width_bb42:
    mov r10, [rbp - 400] # reload L48 from spill slot
    mov r11, [rbp - 424] # reload L51 from spill slot
    mov rsi, [rbp - 400] # reload L48 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L48 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 424] # reload L51 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L51 = TAG_INT (i64-local, fused)
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1056], r11 # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    mov r11, [rbp - 464] # reload L56 from spill slot
    mov r10, r11 # assign L55
    mov r11, [rbp - 1056] # tag L56 from tag-slot
    mov [rbp - 1048], r11 # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    jmp .Lb2dd__bt_width_bb43 # branch
.Lb2dd__bt_width_bb43:
    mov r10, [rbp - 456] # reload L55 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb45 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb44 # jump -> then
.Lb2dd__bt_width_bb44:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb45:
    mov rdx, [rbp - 400] # reload L48 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L48 = TAG_INT (i64-local, fused)
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb46:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1088], rax # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov r10, [rbp - 496] # reload L60 from spill slot
    mov rsi, [rbp - 496] # reload L60 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1088] # tag L60 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 1096], rax # store tag L61
    mov [rbp - 504], r10 # spill L61 to slot
    mov r10, [rbp - 504] # reload L61 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb49 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb48 # jump -> then
.Lb2dd__bt_width_bb47:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb48:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1112], rax # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1120], rax # store tag L64
    mov [rbp - 528], r10 # spill L64 to slot
    mov r10, [rbp - 520] # reload L63 from spill slot
    mov r11, [rbp - 528] # reload L64 from spill slot
    mov rsi, [rbp - 520] # reload L63 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1112] # tag L63 from tag-slot
    mov rcx, [rbp - 528] # reload L64 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1120] # tag L64 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1128], rax # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    mov r11, [rbp - 536] # reload L65 from spill slot
    mov r10, r11 # assign L62
    mov r11, [rbp - 1128] # tag L65 from tag-slot
    mov [rbp - 1104], r11 # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    jmp .Lb2dd__bt_width_bb50 # branch
.Lb2dd__bt_width_bb49:
    mov r11, [rbp - 504] # reload L61 from spill slot
    mov r10, r11 # assign L62
    mov r11, [rbp - 1096] # tag L61 from tag-slot
    mov [rbp - 1104], r11 # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    jmp .Lb2dd__bt_width_bb50 # branch
.Lb2dd__bt_width_bb50:
    mov r10, [rbp - 512] # reload L62 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb52 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb51 # jump -> then
.Lb2dd__bt_width_bb51:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1144], rax # store tag L67
    mov [rbp - 552], r10 # spill L67 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 608] # tag L0 from tag-slot
    mov rcx, [rbp - 552] # reload L67 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1144] # tag L67 from tag-slot
    call _bt_width # call _bt_width
    mov [rbp - 1152], rax # store tag L68
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 560], r10 # spill L68 to slot
    mov r11, [rbp - 560] # reload L68 from spill slot
    mov r10, r11 # assign L69
    mov r11, 0 # tag L68 = TAG_INT (i64-local, fused)
    mov [rbp - 1160], r11 # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    mov r10, [rbp - 568] # reload L69 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1168], r11 # store tag L70
    mov [rbp - 576], r10 # spill L70 to slot
    mov r10, [rbp - 576] # reload L70 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_width_bb54 # jump-if-zero -> else
    jmp .Lb2dd__bt_width_bb53 # jump -> then
.Lb2dd__bt_width_bb52:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb53:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_width_bb54:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 672] # tag L8 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1184], rax # store tag L72
    mov [rbp - 592], r10 # spill L72 to slot
    mov r10, [rbp - 568] # reload L69 from spill slot
    mov r11, [rbp - 592] # reload L72 from spill slot
    mov rsi, [rbp - 568] # reload L69 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L69 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 592] # reload L72 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1184] # tag L72 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 1192], rax # store tag L73
    mov [rbp - 600], r10 # spill L73 to slot
    mov rdx, [rbp - 600] # reload L73 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L73 = TAG_INT (i64-local, fused)
    add rsp, 1152 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_minwidth
    .p2align 4
_bt_minwidth:
    .loc 1 422 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 992 # prologue: alloc spill frame
    mov [rbp - 528], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 536], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__bt_minwidth_bb0:
    cmp r12, 0 # binop <
    setl al # binop < → al
    movzx r13, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 544], r11 # store tag L2
    test r13, r13 # br_cond test
    jz .Lb2dd__bt_minwidth_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb1 # jump -> then
.Lb2dd__bt_minwidth_bb1:
    mov r14, r13 # assign L3
    mov r11, [rbp - 544] # tag L2 from tag-slot
    mov [rbp - 552], r11 # store tag L3
    jmp .Lb2dd__bt_minwidth_bb3 # branch
.Lb2dd__bt_minwidth_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 528] # tag L0 from tag-slot
    call hexa_len # call hexa_len
    mov r15, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 560], r11 # store tag L4
    mov rsi, r12 # hv arg payload
    mov rdi, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 560] # tag L4 from tag-slot
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 568], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r14, r10 # assign L3
    mov r11, [rbp - 568] # tag L5 from tag-slot
    mov [rbp - 552], r11 # store tag L3
    jmp .Lb2dd__bt_minwidth_bb3 # branch
.Lb2dd__bt_minwidth_bb3:
    test r14, r14 # br_cond test
    jz .Lb2dd__bt_minwidth_bb5 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb4 # jump -> then
.Lb2dd__bt_minwidth_bb4:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 528] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 584], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L8
    mov r11, [rbp - 584] # tag L7 from tag-slot
    mov [rbp - 592], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L8 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 600], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r10, r11 # assign L10
    mov r11, [rbp - 600] # tag L9 from tag-slot
    mov [rbp - 608], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 616], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb7 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb6 # jump -> then
.Lb2dd__bt_minwidth_bb6:
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 616] # tag L11 from tag-slot
    mov [rbp - 624], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_minwidth_bb8 # branch
.Lb2dd__bt_minwidth_bb7:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 632], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 632] # tag L13 from tag-slot
    mov [rbp - 624], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_minwidth_bb8 # branch
.Lb2dd__bt_minwidth_bb8:
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb10 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb9 # jump -> then
.Lb2dd__bt_minwidth_bb9:
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 624] # tag L12 from tag-slot
    mov [rbp - 640], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_minwidth_bb11 # branch
.Lb2dd__bt_minwidth_bb10:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 648], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 648] # tag L15 from tag-slot
    mov [rbp - 640], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_minwidth_bb11 # branch
.Lb2dd__bt_minwidth_bb11:
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb13 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb12 # jump -> then
.Lb2dd__bt_minwidth_bb12:
    mov rdx, 1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb13:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 8 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 664], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb15 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb14 # jump -> then
.Lb2dd__bt_minwidth_bb14:
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 664] # tag L17 from tag-slot
    mov [rbp - 672], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__bt_minwidth_bb16 # branch
.Lb2dd__bt_minwidth_bb15:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 680], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 680] # tag L19 from tag-slot
    mov [rbp - 672], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__bt_minwidth_bb16 # branch
.Lb2dd__bt_minwidth_bb16:
    mov r10, [rbp - 160] # reload L18 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb18 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb17 # jump -> then
.Lb2dd__bt_minwidth_bb17:
    mov r11, [rbp - 160] # reload L18 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 672] # tag L18 from tag-slot
    mov [rbp - 688], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__bt_minwidth_bb19 # branch
.Lb2dd__bt_minwidth_bb18:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 10 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 696], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 696] # tag L21 from tag-slot
    mov [rbp - 688], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__bt_minwidth_bb19 # branch
.Lb2dd__bt_minwidth_bb19:
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb21 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb20 # jump -> then
.Lb2dd__bt_minwidth_bb20:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb21:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 14 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 712], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb23 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb22 # jump -> then
.Lb2dd__bt_minwidth_bb22:
    mov r11, [rbp - 200] # reload L23 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 712] # tag L23 from tag-slot
    mov [rbp - 720], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__bt_minwidth_bb24 # branch
.Lb2dd__bt_minwidth_bb23:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 15 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 728], rax # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 728] # tag L25 from tag-slot
    mov [rbp - 720], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__bt_minwidth_bb24 # branch
.Lb2dd__bt_minwidth_bb24:
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb26 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb25 # jump -> then
.Lb2dd__bt_minwidth_bb25:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb26:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 13 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 744], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, [rbp - 232] # reload L27 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb28 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb27 # jump -> then
.Lb2dd__bt_minwidth_bb27:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb28:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 12 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 760], rax # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r10, [rbp - 248] # reload L29 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb30 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb29 # jump -> then
.Lb2dd__bt_minwidth_bb29:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 776], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 528] # tag L0 from tag-slot
    mov rcx, [rbp - 264] # reload L31 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 776] # tag L31 from tag-slot
    call _bt_minwidth # call _bt_minwidth
    mov [rbp - 784], rax # store tag L32
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 272], r10 # spill L32 to slot
    mov rdx, [rbp - 272] # reload L32 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L32 = TAG_INT (i64-local, fused)
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb30:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 6 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 792], rax # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r10, [rbp - 280] # reload L33 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb32 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb31 # jump -> then
.Lb2dd__bt_minwidth_bb31:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 808], rax # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 528] # tag L0 from tag-slot
    mov rcx, [rbp - 296] # reload L35 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 808] # tag L35 from tag-slot
    call _bt_minwidth # call _bt_minwidth
    mov [rbp - 816], rax # store tag L36
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 304], r10 # spill L36 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L8 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 824], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 528] # tag L0 from tag-slot
    mov rcx, [rbp - 312] # reload L37 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 824] # tag L37 from tag-slot
    call _bt_minwidth # call _bt_minwidth
    mov [rbp - 832], rax # store tag L38
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 320], r10 # spill L38 to slot
    mov r10, [rbp - 304] # reload L36 from spill slot
    mov r11, [rbp - 320] # reload L38 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, r11 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 840], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov rdx, [rbp - 328] # reload L39 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L39 = TAG_INT (i64-local, fused)
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb32:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 7 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 848], rax # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb34 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb33 # jump -> then
.Lb2dd__bt_minwidth_bb33:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 864], rax # store tag L42
    mov [rbp - 352], r10 # spill L42 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 528] # tag L0 from tag-slot
    mov rcx, [rbp - 352] # reload L42 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 864] # tag L42 from tag-slot
    call _bt_minwidth # call _bt_minwidth
    mov [rbp - 872], rax # store tag L43
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 360], r10 # spill L43 to slot
    mov r11, [rbp - 360] # reload L43 from spill slot
    mov r10, r11 # assign L44
    mov r11, 0 # tag L43 = TAG_INT (i64-local, fused)
    mov [rbp - 880], r11 # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L8 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 888], rax # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 528] # tag L0 from tag-slot
    mov rcx, [rbp - 376] # reload L45 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 888] # tag L45 from tag-slot
    call _bt_minwidth # call _bt_minwidth
    mov [rbp - 896], rax # store tag L46
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 384], r10 # spill L46 to slot
    mov r11, [rbp - 384] # reload L46 from spill slot
    mov r10, r11 # assign L47
    mov r11, 0 # tag L46 = TAG_INT (i64-local, fused)
    mov [rbp - 904], r11 # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    mov r10, [rbp - 368] # reload L44 from spill slot
    mov r11, [rbp - 392] # reload L47 from spill slot
    cmp r10, r11 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 912], r11 # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    mov r10, [rbp - 400] # reload L48 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb36 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb35 # jump -> then
.Lb2dd__bt_minwidth_bb34:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 4 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 928], rax # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    mov r10, [rbp - 416] # reload L50 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb38 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb37 # jump -> then
.Lb2dd__bt_minwidth_bb35:
    mov rdx, [rbp - 368] # reload L44 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L44 = TAG_INT (i64-local, fused)
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb36:
    mov rdx, [rbp - 392] # reload L47 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L47 = TAG_INT (i64-local, fused)
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb37:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 944], rax # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 528] # tag L0 from tag-slot
    mov rcx, [rbp - 432] # reload L52 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 944] # tag L52 from tag-slot
    call _bt_minwidth # call _bt_minwidth
    mov [rbp - 952], rax # store tag L53
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 440], r10 # spill L53 to slot
    mov rdx, [rbp - 440] # reload L53 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L53 = TAG_INT (i64-local, fused)
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb38:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 960], rax # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov r10, [rbp - 448] # reload L54 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb40 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb39 # jump -> then
.Lb2dd__bt_minwidth_bb39:
    mov r11, [rbp - 448] # reload L54 from spill slot
    mov r10, r11 # assign L55
    mov r11, [rbp - 960] # tag L54 from tag-slot
    mov [rbp - 968], r11 # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    jmp .Lb2dd__bt_minwidth_bb41 # branch
.Lb2dd__bt_minwidth_bb40:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 5 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 976], rax # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    mov r11, [rbp - 464] # reload L56 from spill slot
    mov r10, r11 # assign L55
    mov r11, [rbp - 976] # tag L56 from tag-slot
    mov [rbp - 968], r11 # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    jmp .Lb2dd__bt_minwidth_bb41 # branch
.Lb2dd__bt_minwidth_bb41:
    mov r10, [rbp - 456] # reload L55 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb43 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb42 # jump -> then
.Lb2dd__bt_minwidth_bb42:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb43:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L10 from tag-slot
    mov rcx, 11 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 992], rax # store tag L58
    mov [rbp - 480], r10 # spill L58 to slot
    mov r10, [rbp - 480] # reload L58 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_minwidth_bb45 # jump-if-zero -> else
    jmp .Lb2dd__bt_minwidth_bb44 # jump -> then
.Lb2dd__bt_minwidth_bb44:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1008], rax # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 528] # tag L0 from tag-slot
    mov rcx, [rbp - 496] # reload L60 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1008] # tag L60 from tag-slot
    call _bt_minwidth # call _bt_minwidth
    mov [rbp - 1016], rax # store tag L61
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 504], r10 # spill L61 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L8 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1024], rax # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov r10, [rbp - 504] # reload L61 from spill slot
    mov r11, [rbp - 512] # reload L62 from spill slot
    mov rsi, [rbp - 504] # reload L61 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L61 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 512] # reload L62 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1024] # tag L62 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 1032], rax # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    mov rdx, [rbp - 520] # reload L63 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L63 = TAG_INT (i64-local, fused)
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_minwidth_bb45:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 992 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_maxwidth
    .p2align 4
_bt_maxwidth:
    .loc 1 449 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 1184 # prologue: alloc spill frame
    mov [rbp - 624], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 632], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__bt_maxwidth_bb0:
    cmp r12, 0 # binop <
    setl al # binop < → al
    movzx r13, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 640], r11 # store tag L2
    test r13, r13 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb1 # jump -> then
.Lb2dd__bt_maxwidth_bb1:
    mov r14, r13 # assign L3
    mov r11, [rbp - 640] # tag L2 from tag-slot
    mov [rbp - 648], r11 # store tag L3
    jmp .Lb2dd__bt_maxwidth_bb3 # branch
.Lb2dd__bt_maxwidth_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 624] # tag L0 from tag-slot
    call hexa_len # call hexa_len
    mov r15, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 656], r11 # store tag L4
    mov rsi, r12 # hv arg payload
    mov rdi, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 656] # tag L4 from tag-slot
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 664], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r14, r10 # assign L3
    mov r11, [rbp - 664] # tag L5 from tag-slot
    mov [rbp - 648], r11 # store tag L3
    jmp .Lb2dd__bt_maxwidth_bb3 # branch
.Lb2dd__bt_maxwidth_bb3:
    test r14, r14 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb5 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb4 # jump -> then
.Lb2dd__bt_maxwidth_bb4:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 624] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 680], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L8
    mov r11, [rbp - 680] # tag L7 from tag-slot
    mov [rbp - 688], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 696], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r10, r11 # assign L10
    mov r11, [rbp - 696] # tag L9 from tag-slot
    mov [rbp - 704], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 712], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb7 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb6 # jump -> then
.Lb2dd__bt_maxwidth_bb6:
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 712] # tag L11 from tag-slot
    mov [rbp - 720], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_maxwidth_bb8 # branch
.Lb2dd__bt_maxwidth_bb7:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 728], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 728] # tag L13 from tag-slot
    mov [rbp - 720], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_maxwidth_bb8 # branch
.Lb2dd__bt_maxwidth_bb8:
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb10 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb9 # jump -> then
.Lb2dd__bt_maxwidth_bb9:
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 720] # tag L12 from tag-slot
    mov [rbp - 736], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_maxwidth_bb11 # branch
.Lb2dd__bt_maxwidth_bb10:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 744], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 744] # tag L15 from tag-slot
    mov [rbp - 736], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_maxwidth_bb11 # branch
.Lb2dd__bt_maxwidth_bb11:
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb13 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb12 # jump -> then
.Lb2dd__bt_maxwidth_bb12:
    mov rdx, 1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb13:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 8 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 760], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb15 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb14 # jump -> then
.Lb2dd__bt_maxwidth_bb14:
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 760] # tag L17 from tag-slot
    mov [rbp - 768], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__bt_maxwidth_bb16 # branch
.Lb2dd__bt_maxwidth_bb15:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 776], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 776] # tag L19 from tag-slot
    mov [rbp - 768], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__bt_maxwidth_bb16 # branch
.Lb2dd__bt_maxwidth_bb16:
    mov r10, [rbp - 160] # reload L18 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb18 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb17 # jump -> then
.Lb2dd__bt_maxwidth_bb17:
    mov r11, [rbp - 160] # reload L18 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 768] # tag L18 from tag-slot
    mov [rbp - 784], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__bt_maxwidth_bb19 # branch
.Lb2dd__bt_maxwidth_bb18:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 10 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 792], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 792] # tag L21 from tag-slot
    mov [rbp - 784], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__bt_maxwidth_bb19 # branch
.Lb2dd__bt_maxwidth_bb19:
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb21 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb20 # jump -> then
.Lb2dd__bt_maxwidth_bb20:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb21:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 14 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 808], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb23 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb22 # jump -> then
.Lb2dd__bt_maxwidth_bb22:
    mov r11, [rbp - 200] # reload L23 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 808] # tag L23 from tag-slot
    mov [rbp - 816], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__bt_maxwidth_bb24 # branch
.Lb2dd__bt_maxwidth_bb23:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 15 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 824], rax # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 824] # tag L25 from tag-slot
    mov [rbp - 816], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__bt_maxwidth_bb24 # branch
.Lb2dd__bt_maxwidth_bb24:
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb26 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb25 # jump -> then
.Lb2dd__bt_maxwidth_bb25:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb26:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 13 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 840], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, [rbp - 232] # reload L27 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb28 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb27 # jump -> then
.Lb2dd__bt_maxwidth_bb27:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb28:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 12 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 856], rax # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r10, [rbp - 248] # reload L29 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb30 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb29 # jump -> then
.Lb2dd__bt_maxwidth_bb29:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 872], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 624] # tag L0 from tag-slot
    mov rcx, [rbp - 264] # reload L31 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 872] # tag L31 from tag-slot
    call _bt_maxwidth # call _bt_maxwidth
    mov [rbp - 880], rax # store tag L32
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 272], r10 # spill L32 to slot
    mov rdx, [rbp - 272] # reload L32 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L32 = TAG_INT (i64-local, fused)
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb30:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 5 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 888], rax # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r10, [rbp - 280] # reload L33 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb32 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb31 # jump -> then
.Lb2dd__bt_maxwidth_bb31:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 904], rax # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 624] # tag L0 from tag-slot
    mov rcx, [rbp - 296] # reload L35 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 904] # tag L35 from tag-slot
    call _bt_maxwidth # call _bt_maxwidth
    mov [rbp - 912], rax # store tag L36
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 304], r10 # spill L36 to slot
    mov rdx, [rbp - 304] # reload L36 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L36 = TAG_INT (i64-local, fused)
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb32:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 6 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 920], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb34 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb33 # jump -> then
.Lb2dd__bt_maxwidth_bb33:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 936], rax # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 624] # tag L0 from tag-slot
    mov rcx, [rbp - 328] # reload L39 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 936] # tag L39 from tag-slot
    call _bt_maxwidth # call _bt_maxwidth
    mov [rbp - 944], rax # store tag L40
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 336], r10 # spill L40 to slot
    mov r11, [rbp - 336] # reload L40 from spill slot
    mov r10, r11 # assign L41
    mov r11, 0 # tag L40 = TAG_INT (i64-local, fused)
    mov [rbp - 952], r11 # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 960], rax # store tag L42
    mov [rbp - 352], r10 # spill L42 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 624] # tag L0 from tag-slot
    mov rcx, [rbp - 352] # reload L42 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 960] # tag L42 from tag-slot
    call _bt_maxwidth # call _bt_maxwidth
    mov [rbp - 968], rax # store tag L43
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 360], r10 # spill L43 to slot
    mov r11, [rbp - 360] # reload L43 from spill slot
    mov r10, r11 # assign L44
    mov r11, 0 # tag L43 = TAG_INT (i64-local, fused)
    mov [rbp - 976], r11 # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov r10, [rbp - 344] # reload L41 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 984], r11 # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov r10, [rbp - 376] # reload L45 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb36 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb35 # jump -> then
.Lb2dd__bt_maxwidth_bb34:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 7 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1024], rax # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    mov r10, [rbp - 416] # reload L50 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb41 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb40 # jump -> then
.Lb2dd__bt_maxwidth_bb35:
    mov r11, [rbp - 376] # reload L45 from spill slot
    mov r10, r11 # assign L46
    mov r11, [rbp - 984] # tag L45 from tag-slot
    mov [rbp - 992], r11 # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    jmp .Lb2dd__bt_maxwidth_bb37 # branch
.Lb2dd__bt_maxwidth_bb36:
    mov r10, [rbp - 368] # reload L44 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1000], r11 # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    mov r11, [rbp - 392] # reload L47 from spill slot
    mov r10, r11 # assign L46
    mov r11, [rbp - 1000] # tag L47 from tag-slot
    mov [rbp - 992], r11 # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    jmp .Lb2dd__bt_maxwidth_bb37 # branch
.Lb2dd__bt_maxwidth_bb37:
    mov r10, [rbp - 384] # reload L46 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb39 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb38 # jump -> then
.Lb2dd__bt_maxwidth_bb38:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb39:
    mov r10, [rbp - 344] # reload L41 from spill slot
    mov r11, [rbp - 368] # reload L44 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, r11 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1016], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov rdx, [rbp - 408] # reload L49 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L49 = TAG_INT (i64-local, fused)
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb40:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1040], rax # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 624] # tag L0 from tag-slot
    mov rcx, [rbp - 432] # reload L52 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1040] # tag L52 from tag-slot
    call _bt_maxwidth # call _bt_maxwidth
    mov [rbp - 1048], rax # store tag L53
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 440], r10 # spill L53 to slot
    mov r11, [rbp - 440] # reload L53 from spill slot
    mov r10, r11 # assign L54
    mov r11, 0 # tag L53 = TAG_INT (i64-local, fused)
    mov [rbp - 1056], r11 # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1064], rax # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 624] # tag L0 from tag-slot
    mov rcx, [rbp - 456] # reload L55 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1064] # tag L55 from tag-slot
    call _bt_maxwidth # call _bt_maxwidth
    mov [rbp - 1072], rax # store tag L56
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 464], r10 # spill L56 to slot
    mov r11, [rbp - 464] # reload L56 from spill slot
    mov r10, r11 # assign L57
    mov r11, 0 # tag L56 = TAG_INT (i64-local, fused)
    mov [rbp - 1080], r11 # store tag L57
    mov [rbp - 472], r10 # spill L57 to slot
    mov r10, [rbp - 448] # reload L54 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1088], r11 # store tag L58
    mov [rbp - 480], r10 # spill L58 to slot
    mov r10, [rbp - 480] # reload L58 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb43 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb42 # jump -> then
.Lb2dd__bt_maxwidth_bb41:
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L10 from tag-slot
    mov rcx, 11 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1136], rax # store tag L64
    mov [rbp - 528], r10 # spill L64 to slot
    mov r10, [rbp - 528] # reload L64 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb50 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb49 # jump -> then
.Lb2dd__bt_maxwidth_bb42:
    mov r11, [rbp - 480] # reload L58 from spill slot
    mov r10, r11 # assign L59
    mov r11, [rbp - 1088] # tag L58 from tag-slot
    mov [rbp - 1096], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    jmp .Lb2dd__bt_maxwidth_bb44 # branch
.Lb2dd__bt_maxwidth_bb43:
    mov r10, [rbp - 472] # reload L57 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1104], r11 # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov r11, [rbp - 496] # reload L60 from spill slot
    mov r10, r11 # assign L59
    mov r11, [rbp - 1104] # tag L60 from tag-slot
    mov [rbp - 1096], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    jmp .Lb2dd__bt_maxwidth_bb44 # branch
.Lb2dd__bt_maxwidth_bb44:
    mov r10, [rbp - 488] # reload L59 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb46 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb45 # jump -> then
.Lb2dd__bt_maxwidth_bb45:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb46:
    mov r10, [rbp - 448] # reload L54 from spill slot
    mov r11, [rbp - 472] # reload L57 from spill slot
    cmp r10, r11 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1120], r11 # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov r10, [rbp - 512] # reload L62 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb48 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb47 # jump -> then
.Lb2dd__bt_maxwidth_bb47:
    mov rdx, [rbp - 448] # reload L54 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L54 = TAG_INT (i64-local, fused)
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb48:
    mov rdx, [rbp - 472] # reload L57 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L57 = TAG_INT (i64-local, fused)
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb49:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1152], rax # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    mov r10, [rbp - 544] # reload L66 from spill slot
    mov rsi, [rbp - 544] # reload L66 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1152] # tag L66 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 1160], rax # store tag L67
    mov [rbp - 552], r10 # spill L67 to slot
    mov r10, [rbp - 552] # reload L67 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb52 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb51 # jump -> then
.Lb2dd__bt_maxwidth_bb50:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb51:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb52:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1176], rax # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 624] # tag L0 from tag-slot
    mov rcx, [rbp - 568] # reload L69 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1176] # tag L69 from tag-slot
    call _bt_maxwidth # call _bt_maxwidth
    mov [rbp - 1184], rax # store tag L70
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 576], r10 # spill L70 to slot
    mov r11, [rbp - 576] # reload L70 from spill slot
    mov r10, r11 # assign L71
    mov r11, 0 # tag L70 = TAG_INT (i64-local, fused)
    mov [rbp - 1192], r11 # store tag L71
    mov [rbp - 584], r10 # spill L71 to slot
    mov r10, [rbp - 584] # reload L71 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1200], r11 # store tag L72
    mov [rbp - 592], r10 # spill L72 to slot
    mov r10, [rbp - 592] # reload L72 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_maxwidth_bb54 # jump-if-zero -> else
    jmp .Lb2dd__bt_maxwidth_bb53 # jump -> then
.Lb2dd__bt_maxwidth_bb53:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_maxwidth_bb54:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L8 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1216], rax # store tag L74
    mov [rbp - 608], r10 # spill L74 to slot
    mov r10, [rbp - 584] # reload L71 from spill slot
    mov r11, [rbp - 608] # reload L74 from spill slot
    mov rsi, [rbp - 584] # reload L71 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L71 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 608] # reload L74 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1216] # tag L74 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 1224], rax # store tag L75
    mov [rbp - 616], r10 # spill L75 to slot
    mov rdx, [rbp - 616] # reload L75 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L75 = TAG_INT (i64-local, fused)
    add rsp, 1184 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden bt_empty
    .p2align 4
bt_empty:
    .loc 1 484 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 32 # prologue: alloc spill frame
.Lb2dd_bt_empty_bb0:
    call hexa_array_new # array_lit: new array
    mov rbx, rdx # array_lit: capture new array payload
    mov [rbp - 56], rax # store tag L0
    call hexa_array_new # array_lit: new array
    mov r12, rdx # array_lit: capture new array payload
    mov [rbp - 64], rax # store tag L1
    call hexa_array_new # array_lit: new array
    mov r13, rdx # array_lit: capture new array payload
    mov [rbp - 72], rax # store tag L2
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 72] # tag L2 from tag-slot
    mov rsi, r13 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r13, rdx # array_lit: recapture array payload
    mov [rbp - 72], rax # store tag L2
    call hexa_map_new # struct_lit: new map
    mov r14, rdx # struct_lit: capture new map payload
    mov [rbp - 80], rax # store tag L3
    mov r8, 0 # hv arg payload
    mov rcx, 2 # tag const_bool = TAG_BOOL
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    mov rsi, r14 # struct_lit: map payload
    mov rdi, [rbp - 80] # tag L3 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r14, rdx # struct_lit: recapture map payload
    mov [rbp - 80], rax # store tag L3
    mov r8, rbx # hv arg payload
    mov rcx, [rbp - 56] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    mov rsi, r14 # struct_lit: map payload
    mov rdi, [rbp - 80] # tag L3 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r14, rdx # struct_lit: recapture map payload
    mov [rbp - 80], rax # store tag L3
    mov r8, r12 # hv arg payload
    mov rcx, [rbp - 64] # tag L1 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    mov rsi, r14 # struct_lit: map payload
    mov rdi, [rbp - 80] # tag L3 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r14, rdx # struct_lit: recapture map payload
    mov [rbp - 80], rax # store tag L3
    mov r8, -1 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr8] # map key (const char*): &.LCstr8
    mov rsi, r14 # struct_lit: map payload
    mov rdi, [rbp - 80] # tag L3 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r14, rdx # struct_lit: recapture map payload
    mov [rbp - 80], rax # store tag L3
    mov r8, 0 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr9] # map key (const char*): &.LCstr9
    mov rsi, r14 # struct_lit: map payload
    mov rdi, [rbp - 80] # tag L3 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r14, rdx # struct_lit: recapture map payload
    mov [rbp - 80], rax # store tag L3
    mov r8, 0 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr10] # map key (const char*): &.LCstr10
    mov rsi, r14 # struct_lit: map payload
    mov rdi, [rbp - 80] # tag L3 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r14, rdx # struct_lit: recapture map payload
    mov [rbp - 80], rax # store tag L3
    mov r8, r13 # hv arg payload
    mov rcx, [rbp - 72] # tag L2 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    mov rsi, r14 # struct_lit: map payload
    mov rdi, [rbp - 80] # tag L3 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r14, rdx # struct_lit: recapture map payload
    mov [rbp - 80], rax # store tag L3
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 80] # tag L3 from tag-slot
    add rsp, 32 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden bt_compile
    .p2align 4
bt_compile:
    .loc 1 489 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 432 # prologue: alloc spill frame
    mov [rbp - 248], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 256], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_bt_compile_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 248] # tag L0 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 264], rax # store tag L2
    mov r13, rdx # hv: unbox call result (rdx)
    call hexa_array_new # array_lit: new array
    mov r14, rdx # array_lit: capture new array payload
    mov [rbp - 272], rax # store tag L3
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L3 from tag-slot
    mov rsi, r14 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r14, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L3
    call hexa_array_new # array_lit: new array
    mov r15, rdx # array_lit: capture new array payload
    mov [rbp - 280], rax # store tag L4
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 280] # tag L4 from tag-slot
    mov rsi, r15 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r15, rdx # array_lit: recapture array payload
    mov [rbp - 280], rax # store tag L4
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 288], rax # store tag L5
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L5 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 296], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 304], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    call hexa_map_new # struct_lit: new map
    mov r10, rdx # struct_lit: capture new map payload
    mov [rbp - 312], rax # store tag L8
    mov r8, rbx # hv arg payload
    mov rcx, [rbp - 248] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr2] # map key (const char*): &.LCstr2
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 312] # tag L8 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 312], rax # store tag L8
    mov r8, r13 # hv arg payload
    mov rcx, [rbp - 264] # tag L2 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 312] # tag L8 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 312], rax # store tag L8
    mov r8, r14 # hv arg payload
    mov rcx, [rbp - 272] # tag L3 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 312] # tag L8 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 312], rax # store tag L8
    mov r8, r15 # hv arg payload
    mov rcx, [rbp - 280] # tag L4 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 312] # tag L8 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 312], rax # store tag L8
    mov r8, [rbp - 56] # reload L5 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 288] # tag L5 from tag-slot
    lea rdx, [rip+.LCstr6] # map key (const char*): &.LCstr6
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 312] # tag L8 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 312], rax # store tag L8
    mov r8, [rbp - 64] # reload L6 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 296] # tag L6 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 312] # tag L8 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 312], rax # store tag L8
    mov r8, [rbp - 72] # reload L7 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 304] # tag L7 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 312] # tag L8 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 312], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L9
    mov r11, [rbp - 312] # tag L8 from tag-slot
    mov [rbp - 320], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 320] # tag L9 from tag-slot
    call _bt_parse_alt # call _bt_parse_alt
    mov [rbp - 328], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, 0 # tag L10 = TAG_INT (i64-local, fused)
    mov [rbp - 336], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 320] # tag L9 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 344], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 344] # tag L12 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 352], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 352] # tag L13 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 360], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_compile_bb2 # jump-if-zero -> else
    jmp .Lb2dd_bt_compile_bb1 # jump -> then
.Lb2dd_bt_compile_bb1:
    call bt_empty # call bt_empty
    mov [rbp - 376], rax # store tag L16
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 144], r10 # spill L16 to slot
    mov rdx, [rbp - 144] # reload L16 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 376] # tag L16 from tag-slot
    add rsp, 432 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_compile_bb2:
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 320] # tag L9 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 384], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 384] # tag L17 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 392], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 320] # tag L9 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 400], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, [rbp - 160] # reload L18 from spill slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 392] # tag L18 from tag-slot
    mov rcx, [rbp - 168] # reload L19 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 400] # tag L19 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 408], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_compile_bb4 # jump-if-zero -> else
    jmp .Lb2dd_bt_compile_bb3 # jump -> then
.Lb2dd_bt_compile_bb3:
    call bt_empty # call bt_empty
    mov [rbp - 424], rax # store tag L22
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 192], r10 # spill L22 to slot
    mov rdx, [rbp - 192] # reload L22 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 424] # tag L22 from tag-slot
    add rsp, 432 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_compile_bb4:
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 320] # tag L9 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 432], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 320] # tag L9 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 440], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 320] # tag L9 from tag-slot
    lea rdx, [rip+.LCstr6] # map key (const char*): &.LCstr6
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 448], rax # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 448] # tag L25 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 456], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 464], rax # store tag L27
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 464] # tag L27 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 464], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    call hexa_map_new # struct_lit: new map
    mov r10, rdx # struct_lit: capture new map payload
    mov [rbp - 472], rax # store tag L28
    mov r8, 1 # hv arg payload
    mov rcx, 2 # tag const_bool = TAG_BOOL
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 472] # tag L28 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 472], rax # store tag L28
    mov r8, [rbp - 200] # reload L23 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 432] # tag L23 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 472] # tag L28 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 472], rax # store tag L28
    mov r8, [rbp - 208] # reload L24 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 440] # tag L24 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 472] # tag L28 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 472], rax # store tag L28
    mov r8, [rbp - 104] # reload L11 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, 0 # tag L11 = TAG_INT (i64-local, fused)
    lea rdx, [rip+.LCstr8] # map key (const char*): &.LCstr8
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 472] # tag L28 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 472], rax # store tag L28
    mov r8, [rbp - 224] # reload L26 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 456] # tag L26 from tag-slot
    lea rdx, [rip+.LCstr9] # map key (const char*): &.LCstr9
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 472] # tag L28 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 472], rax # store tag L28
    mov r8, r12 # hv arg payload
    mov rcx, 0 # tag L1 = TAG_INT (i64-local, fused)
    lea rdx, [rip+.LCstr10] # map key (const char*): &.LCstr10
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 472] # tag L28 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 472], rax # store tag L28
    mov r8, [rbp - 232] # reload L27 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 464] # tag L27 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 472] # tag L28 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 472], rax # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov rdx, [rbp - 240] # reload L28 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 472] # tag L28 from tag-slot
    add rsp, 432 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_class_match
    .p2align 4
_bt_class_match:
    .loc 1 524 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 336 # prologue: alloc spill frame
    mov [rbp - 200], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 208], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 216], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd__bt_class_match_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r14, rdx # index: capture element payload
    mov [rbp - 224], rax # store tag L3
    mov r15, r14 # assign L4
    mov r11, [rbp - 224] # tag L3 from tag-slot
    mov [rbp - 232], r11 # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 232] # tag L4 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 240], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 240] # tag L5 from tag-slot
    mov [rbp - 248], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, 1 # assign L7
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 256], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, 0 # assign L8
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 264], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    jmp .Lb2dd__bt_class_match_bb1 # branch
.Lb2dd__bt_class_match_bb1:
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 272], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 232] # tag L4 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 280], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 96] # reload L10 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 280] # tag L10 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 288], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_class_match_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_match_bb2 # jump -> then
.Lb2dd__bt_class_match_bb2:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 232] # tag L4 from tag-slot
    mov rcx, [rbp - 72] # reload L7 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L7 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 296], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov rsi, r13 # hv arg payload
    mov rdi, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 112] # reload L12 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 296] # tag L12 from tag-slot
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 304], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_class_match_bb5 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_match_bb4 # jump -> then
.Lb2dd__bt_class_match_bb3:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 248] # tag L6 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 360], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_class_match_bb10 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_match_bb9 # jump -> then
.Lb2dd__bt_class_match_bb4:
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 320], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 232] # tag L4 from tag-slot
    mov rcx, [rbp - 136] # reload L15 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L15 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 328], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov rsi, r13 # hv arg payload
    mov rdi, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 144] # reload L16 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 328] # tag L16 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 336], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 336] # tag L17 from tag-slot
    mov [rbp - 312], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_class_match_bb6 # branch
.Lb2dd__bt_class_match_bb5:
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 304] # tag L13 from tag-slot
    mov [rbp - 312], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_class_match_bb6 # branch
.Lb2dd__bt_class_match_bb6:
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_class_match_bb8 # jump-if-zero -> else
    jmp .Lb2dd__bt_class_match_bb7 # jump -> then
.Lb2dd__bt_class_match_bb7:
    mov r10, 1 # assign L8
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 264], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    jmp .Lb2dd__bt_class_match_bb8 # branch
.Lb2dd__bt_class_match_bb8:
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 352], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L7
    mov r11, 0 # tag L19 = TAG_INT (i64-local, fused)
    mov [rbp - 256], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    jmp .Lb2dd__bt_class_match_bb1 # branch
.Lb2dd__bt_class_match_bb9:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 264] # tag L8 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 376], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rdx, [rbp - 192] # reload L22 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 376] # tag L22 from tag-slot
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_class_match_bb10:
    mov rdx, [rbp - 80] # reload L8 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 264] # tag L8 from tag-slot
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_fill
    .p2align 4
_bt_fill:
    .loc 1 538 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 96 # prologue: alloc spill frame
    mov [rbp - 80], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 88], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__bt_fill_bb0:
    call hexa_array_new # array_lit: new array
    mov r13, rdx # array_lit: capture new array payload
    mov [rbp - 96], rax # store tag L2
    mov r14, r13 # assign L3
    mov r11, [rbp - 96] # tag L2 from tag-slot
    mov [rbp - 104], r11 # store tag L3
    mov r15, 0 # assign L4
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 112], r11 # store tag L4
    jmp .Lb2dd__bt_fill_bb1 # branch
.Lb2dd__bt_fill_bb1:
    cmp r15, rbx # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 120], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_fill_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_fill_bb2 # jump -> then
.Lb2dd__bt_fill_bb2:
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 104] # tag L3 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 128], rax # store tag L6
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, r15 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 136], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov r15, r10 # assign L4
    mov r11, 0 # tag L7 = TAG_INT (i64-local, fused)
    mov [rbp - 112], r11 # store tag L4
    jmp .Lb2dd__bt_fill_bb1 # branch
.Lb2dd__bt_fill_bb3:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 104] # tag L3 from tag-slot
    add rsp, 96 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_copy
    .p2align 4
_bt_copy:
    .loc 1 546 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 112 # prologue: alloc spill frame
    mov [rbp - 88], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_copy_bb0:
    call hexa_array_new # array_lit: new array
    mov r12, rdx # array_lit: capture new array payload
    mov [rbp - 96], rax # store tag L1
    mov r13, r12 # assign L2
    mov r11, [rbp - 96] # tag L1 from tag-slot
    mov [rbp - 104], r11 # store tag L2
    mov r14, 0 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 112], r11 # store tag L3
    jmp .Lb2dd__bt_copy_bb1 # branch
.Lb2dd__bt_copy_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 88] # tag L0 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r15, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 120], r11 # store tag L4
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 120] # tag L4 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 128], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_copy_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_copy_bb2 # jump -> then
.Lb2dd__bt_copy_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 88] # tag L0 from tag-slot
    mov rdx, r14 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 136], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 104] # tag L2 from tag-slot
    mov rcx, [rbp - 64] # reload L6 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 136] # tag L6 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 144], rax # store tag L7
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 152], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, [rbp - 80] # reload L8 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L8 = TAG_INT (i64-local, fused)
    mov [rbp - 112], r11 # store tag L3
    jmp .Lb2dd__bt_copy_bb1 # branch
.Lb2dd__bt_copy_bb3:
    mov rdx, r13 # hv arg payload
    mov rax, [rbp - 104] # tag L2 from tag-slot
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_restore
    .p2align 4
_bt_restore:
    .loc 1 553 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 80 # prologue: alloc spill frame
    mov [rbp - 72], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 80], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__bt_restore_bb0:
    mov r13, 0 # assign L2
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 88], r11 # store tag L2
    jmp .Lb2dd__bt_restore_bb1 # branch
.Lb2dd__bt_restore_bb1:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 80] # tag L1 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r14, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 96], r11 # store tag L3
    mov rsi, r13 # hv arg payload
    mov rdi, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rcx, r14 # hv arg payload
    mov rdx, [rbp - 96] # tag L3 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r15, rdx # binop <: capture bool payload
    mov [rbp - 104], rax # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd__bt_restore_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_restore_bb2 # jump -> then
.Lb2dd__bt_restore_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 80] # tag L1 from tag-slot
    mov rdx, r13 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 112], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 72] # tag L0 from tag-slot
    mov rdx, r13 # hv payload
    mov r8, [rbp - 56] # reload L5 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 112] # tag L5 from tag-slot
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov rbx, rdx # index_set: store-back poly descriptor
    mov [rbp - 72], rax # store tag L0
    mov r10, r13 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 120], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rbp - 88], r11 # store tag L2
    jmp .Lb2dd__bt_restore_bb1 # branch
.Lb2dd__bt_restore_bb3:
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 80 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_cons
    .p2align 4
_bt_cons:
    .loc 1 559 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 128 # prologue: alloc spill frame
    mov [rbp - 96], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 104], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__bt_cons_bb0:
    call hexa_array_new # array_lit: new array
    mov r13, rdx # array_lit: capture new array payload
    mov [rbp - 112], rax # store tag L2
    mov rcx, rbx # hv arg payload
    mov rdx, 0 # tag L0 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 112] # tag L2 from tag-slot
    mov rsi, r13 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r13, rdx # array_lit: recapture array payload
    mov [rbp - 112], rax # store tag L2
    mov r14, r13 # assign L3
    mov r11, [rbp - 112] # tag L2 from tag-slot
    mov [rbp - 120], r11 # store tag L3
    mov r15, 0 # assign L4
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 128], r11 # store tag L4
    jmp .Lb2dd__bt_cons_bb1 # branch
.Lb2dd__bt_cons_bb1:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 104] # tag L1 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 136], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov rsi, r15 # hv arg payload
    mov rdi, 0 # tag L4 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 136] # tag L5 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 144], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_cons_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_cons_bb2 # jump -> then
.Lb2dd__bt_cons_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 104] # tag L1 from tag-slot
    mov rdx, r15 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 152], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 120] # tag L3 from tag-slot
    mov rcx, [rbp - 72] # reload L7 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 152] # tag L7 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 160], rax # store tag L8
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, r15 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 168], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r15, r10 # assign L4
    mov r11, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rbp - 128], r11 # store tag L4
    jmp .Lb2dd__bt_cons_bb1 # branch
.Lb2dd__bt_cons_bb3:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 120] # tag L3 from tag-slot
    add rsp, 128 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_tail
    .p2align 4
_bt_tail:
    .loc 1 567 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 112 # prologue: alloc spill frame
    mov [rbp - 88], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__bt_tail_bb0:
    call hexa_array_new # array_lit: new array
    mov r12, rdx # array_lit: capture new array payload
    mov [rbp - 96], rax # store tag L1
    mov r13, r12 # assign L2
    mov r11, [rbp - 96] # tag L1 from tag-slot
    mov [rbp - 104], r11 # store tag L2
    mov r14, 1 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 112], r11 # store tag L3
    jmp .Lb2dd__bt_tail_bb1 # branch
.Lb2dd__bt_tail_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 88] # tag L0 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r15, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 120], r11 # store tag L4
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 120] # tag L4 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 128], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_tail_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_tail_bb2 # jump -> then
.Lb2dd__bt_tail_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 88] # tag L0 from tag-slot
    mov rdx, r14 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 136], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 104] # tag L2 from tag-slot
    mov rcx, [rbp - 64] # reload L6 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 136] # tag L6 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 144], rax # store tag L7
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 152], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, [rbp - 80] # reload L8 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L8 = TAG_INT (i64-local, fused)
    mov [rbp - 112], r11 # store tag L3
    jmp .Lb2dd__bt_tail_bb1 # branch
.Lb2dd__bt_tail_bb3:
    mov rdx, r13 # hv arg payload
    mov rax, [rbp - 104] # tag L2 from tag-slot
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_marker
    .p2align 4
_bt_marker:
    .loc 1 575 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 80 # prologue: alloc spill frame
    mov [rbp - 72], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 80], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__bt_marker_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 72] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 88], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 88] # tag L2 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 80] # tag L1 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 96], rax # store tag L3
    mov r14, rdx # hv: unbox call result (rdx)
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 72] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r15, rdx # field: capture result payload
    mov [rbp - 104], rax # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 104] # tag L4 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 112], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 112] # tag L5 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 120], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rdx, [rbp - 64] # reload L6 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L6 = TAG_INT (i64-local, fused)
    add rsp, 80 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_star_view
    .p2align 4
_bt_star_view:
    .loc 1 581 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 336 # prologue: alloc spill frame
    mov [rbp - 200], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 208], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__bt_star_view_bb0:
    mov r13, 0 # assign L2
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 216], r11 # store tag L2
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r14, rdx # field: capture result payload
    mov [rbp - 224], rax # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 224] # tag L3 from tag-slot
    call hexa_len # call hexa_len
    mov r15, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 232], r11 # store tag L4
    mov r10, r15 # assign L5
    mov r11, [rbp - 232] # tag L4 from tag-slot
    mov [rbp - 240], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd__bt_star_view_bb1 # branch
.Lb2dd__bt_star_view_bb1:
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov rsi, r13 # hv arg payload
    mov rdi, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 240] # tag L5 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 248], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_star_view_bb3 # jump-if-zero -> else
    jmp .Lb2dd__bt_star_view_bb2 # jump -> then
.Lb2dd__bt_star_view_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 256], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 256] # tag L7 from tag-slot
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 264], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L9
    mov r11, [rbp - 264] # tag L8 from tag-slot
    mov [rbp - 272], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 272] # tag L9 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 280], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 280] # tag L10 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 288], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_star_view_bb5 # jump-if-zero -> else
    jmp .Lb2dd__bt_star_view_bb4 # jump -> then
.Lb2dd__bt_star_view_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 336], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 344], rax # store tag L18
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 344] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 344], rax # store tag L18
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 344] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 344], rax # store tag L18
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 344] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 344], rax # store tag L18
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 344] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 344], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 336] # tag L17 from tag-slot
    mov rcx, [rbp - 160] # reload L18 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 344] # tag L18 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 352], rax # store tag L19
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 360], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov rsi, [rbp - 176] # reload L20 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 360] # tag L20 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 368], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov r10, [rbp - 184] # reload L21 from spill slot
    mov rsi, [rbp - 184] # reload L21 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 368] # tag L21 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 376], rax # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rdx, [rbp - 192] # reload L22 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L22 = TAG_INT (i64-local, fused)
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_star_view_bb4:
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 272] # tag L9 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 304], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 304] # tag L13 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 312], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r11, [rbp - 128] # reload L14 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 312] # tag L14 from tag-slot
    mov [rbp - 296], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_star_view_bb6 # branch
.Lb2dd__bt_star_view_bb5:
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 288] # tag L11 from tag-slot
    mov [rbp - 296], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__bt_star_view_bb6 # branch
.Lb2dd__bt_star_view_bb6:
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_star_view_bb8 # jump-if-zero -> else
    jmp .Lb2dd__bt_star_view_bb7 # jump -> then
.Lb2dd__bt_star_view_bb7:
    mov rdx, r13 # hv arg payload
    mov rax, 0 # tag L2 = TAG_INT (i64-local, fused)
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_star_view_bb8:
    mov r10, r13 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 328], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L16 = TAG_INT (i64-local, fused)
    mov [rbp - 216], r11 # store tag L2
    jmp .Lb2dd__bt_star_view_bb1 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_run
    .p2align 4
_bt_run:
    .loc 1 593 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 4848 # prologue: alloc spill frame
    mov [rbp - 2456], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 2464], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 2472], r8 # store tag L2
    mov r13, r9 # ingress param payload
    mov r10, [rbp + 16] # ingress stack param 3 tag
    mov [rbp - 2480], r10 # store tag L3
    mov r10, [rbp + 24] # ingress stack param 3 payload
    mov r14, r10 # ingress stack param payload
    mov r10, [rbp + 32] # ingress stack param 4 tag
    mov [rbp - 2488], r10 # store tag L4
    mov r10, [rbp + 40] # ingress stack param 4 payload
    mov r15, r10 # ingress stack param payload
    mov r10, [rbp + 48] # ingress stack param 5 tag
    mov [rbp - 2496], r10 # store tag L5
    mov r10, [rbp + 56] # ingress stack param 5 payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp + 64] # ingress stack param 6 tag
    mov [rbp - 2504], r10 # store tag L6
    mov r10, [rbp + 72] # ingress stack param 6 payload
    mov [rbp - 64], r10 # spill L6 to slot
.Lb2dd__bt_run_bb0:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 2472] # tag L2 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 2512], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2512] # tag L7 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 2520], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 2472] # tag L2 from tag-slot
    mov rdx, 0 # hv payload
    mov r8, [rbp - 80] # reload L8 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 2520] # tag L8 from tag-slot
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r13, rdx # index_set: store-back poly descriptor
    mov [rbp - 2472], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 2472] # tag L2 from tag-slot
    mov rdx, 1 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 2528], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2528] # tag L9 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_gt # binop >: tag-dispatch hexa_cmp_gt
    mov r10, rdx # binop >: capture bool payload
    mov [rbp - 2536], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb1 # jump -> then
.Lb2dd__bt_run_bb1:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 2472] # tag L2 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 2552], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 2472] # tag L2 from tag-slot
    mov rdx, 1 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 2560], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2552] # tag L12 from tag-slot
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2560] # tag L13 from tag-slot
    call hexa_cmp_gt # binop >: tag-dispatch hexa_cmp_gt
    mov r10, rdx # binop >: capture bool payload
    mov [rbp - 2568], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r11, [rbp - 128] # reload L14 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 2568] # tag L14 from tag-slot
    mov [rbp - 2544], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    jmp .Lb2dd__bt_run_bb3 # branch
.Lb2dd__bt_run_bb2:
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 2536] # tag L10 from tag-slot
    mov [rbp - 2544], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    jmp .Lb2dd__bt_run_bb3 # branch
.Lb2dd__bt_run_bb3:
    mov r10, [rbp - 104] # reload L11 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb5 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb4 # jump -> then
.Lb2dd__bt_run_bb4:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 2472] # tag L2 from tag-slot
    mov rdx, 2 # hv payload
    mov r8, 1 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r13, rdx # index_set: store-back poly descriptor
    mov [rbp - 2472], rax # store tag L2
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb5:
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 2480] # tag L3 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2584], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2584] # tag L16 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2592], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb7 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb6 # jump -> then
.Lb2dd__bt_run_bb6:
    mov rdx, [rbp - 56] # reload L5 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L5 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb7:
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 2480] # tag L3 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 2608], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 2608] # tag L19 from tag-slot
    mov [rbp - 2616], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 2480] # tag L3 from tag-slot
    call _bt_tail # call _bt_tail
    mov [rbp - 2624], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L22
    mov r11, [rbp - 2624] # tag L21 from tag-slot
    mov [rbp - 2632], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 2640], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov rsi, [rbp - 200] # reload L23 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2640] # tag L23 from tag-slot
    mov rcx, [rbp - 176] # reload L20 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2616] # tag L20 from tag-slot
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 2648], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r11, [rbp - 208] # reload L24 from spill slot
    mov r10, r11 # assign L25
    mov r11, [rbp - 2648] # tag L24 from tag-slot
    mov [rbp - 2656], r11 # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 2664], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L27
    mov r11, [rbp - 2664] # tag L26 from tag-slot
    mov [rbp - 2672], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 8 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2680], rax # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb9 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb8 # jump -> then
.Lb2dd__bt_run_bb8:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 2696], rax # store tag L30
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 256], r10 # spill L30 to slot
    mov rdx, [rbp - 256] # reload L30 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L30 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb9:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2704], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov r10, [rbp - 264] # reload L31 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb11 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb10 # jump -> then
.Lb2dd__bt_run_bb10:
    mov r10, [rbp - 56] # reload L5 from spill slot
    cmp r10, 0 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2720], r11 # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r10, [rbp - 280] # reload L33 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb13 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb12 # jump -> then
.Lb2dd__bt_run_bb11:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 10 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2744], rax # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov r10, [rbp - 304] # reload L36 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb15 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb14 # jump -> then
.Lb2dd__bt_run_bb12:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 2736], rax # store tag L35
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 296], r10 # spill L35 to slot
    mov rdx, [rbp - 296] # reload L35 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L35 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb13:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb14:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    cmp r10, r11 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2760], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov r10, [rbp - 320] # reload L38 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb17 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb16 # jump -> then
.Lb2dd__bt_run_bb15:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 23 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2784], rax # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r10, [rbp - 344] # reload L41 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb19 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb18 # jump -> then
.Lb2dd__bt_run_bb16:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 2776], rax # store tag L40
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 336], r10 # spill L40 to slot
    mov rdx, [rbp - 336] # reload L40 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L40 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb17:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb18:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 2800], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 360] # reload L43 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 360] # reload L43 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2800] # tag L43 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2808], rax # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov r10, [rbp - 368] # reload L44 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb21 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb20 # jump -> then
.Lb2dd__bt_run_bb19:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2832], rax # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    mov r10, [rbp - 392] # reload L47 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb23 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb22 # jump -> then
.Lb2dd__bt_run_bb20:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 2824], rax # store tag L46
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 384], r10 # spill L46 to slot
    mov rdx, [rbp - 384] # reload L46 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L46 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb21:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb22:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    cmp r10, r11 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2848], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov r10, [rbp - 408] # reload L49 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb25 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb24 # jump -> then
.Lb2dd__bt_run_bb23:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2912], rax # store tag L57
    mov [rbp - 472], r10 # spill L57 to slot
    mov r10, [rbp - 472] # reload L57 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb30 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb29 # jump -> then
.Lb2dd__bt_run_bb24:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 2488] # tag L4 from tag-slot
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 2864], rax # store tag L51
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 424], r10 # spill L51 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 2872], rax # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov r10, [rbp - 424] # reload L51 from spill slot
    mov r11, [rbp - 432] # reload L52 from spill slot
    mov rsi, [rbp - 424] # reload L51 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2864] # tag L51 from tag-slot
    mov rcx, [rbp - 432] # reload L52 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2872] # tag L52 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2880], rax # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    mov r11, [rbp - 440] # reload L53 from spill slot
    mov r10, r11 # assign L50
    mov r11, [rbp - 2880] # tag L53 from tag-slot
    mov [rbp - 2856], r11 # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    jmp .Lb2dd__bt_run_bb26 # branch
.Lb2dd__bt_run_bb25:
    mov r11, [rbp - 408] # reload L49 from spill slot
    mov r10, r11 # assign L50
    mov r11, [rbp - 2848] # tag L49 from tag-slot
    mov [rbp - 2856], r11 # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    jmp .Lb2dd__bt_run_bb26 # branch
.Lb2dd__bt_run_bb26:
    mov r10, [rbp - 416] # reload L50 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb28 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb27 # jump -> then
.Lb2dd__bt_run_bb27:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2896], r11 # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 456] # reload L55 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L55 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 2904], rax # store tag L56
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 464], r10 # spill L56 to slot
    mov rdx, [rbp - 464] # reload L56 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L56 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb28:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb29:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    cmp r10, r11 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2928], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    mov r10, [rbp - 488] # reload L59 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb32 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb31 # jump -> then
.Lb2dd__bt_run_bb30:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2960], rax # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    mov r10, [rbp - 520] # reload L63 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb34 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb33 # jump -> then
.Lb2dd__bt_run_bb31:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2944], r11 # store tag L61
    mov [rbp - 504], r10 # spill L61 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 504] # reload L61 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L61 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 2952], rax # store tag L62
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 512], r10 # spill L62 to slot
    mov rdx, [rbp - 512] # reload L62 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L62 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb32:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb33:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    cmp r10, r11 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 2976], r11 # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    mov r10, [rbp - 536] # reload L65 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb36 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb35 # jump -> then
.Lb2dd__bt_run_bb34:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 6 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3048], rax # store tag L74
    mov [rbp - 608], r10 # spill L74 to slot
    mov r10, [rbp - 608] # reload L74 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb41 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb40 # jump -> then
.Lb2dd__bt_run_bb35:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 2992], rax # store tag L67
    mov [rbp - 552], r10 # spill L67 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3000], rax # store tag L68
    mov [rbp - 560], r10 # spill L68 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 2488] # tag L4 from tag-slot
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 3008], rax # store tag L69
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 568], r10 # spill L69 to slot
    mov rsi, [rbp - 552] # reload L67 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2992] # tag L67 from tag-slot
    mov rcx, [rbp - 560] # reload L68 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3000] # tag L68 from tag-slot
    mov r9, [rbp - 568] # reload L69 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 3008] # tag L69 from tag-slot
    call _bt_class_match # call _bt_class_match
    mov [rbp - 3016], rax # store tag L70
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 576], r10 # spill L70 to slot
    mov r11, [rbp - 576] # reload L70 from spill slot
    mov r10, r11 # assign L66
    mov r11, [rbp - 3016] # tag L70 from tag-slot
    mov [rbp - 2984], r11 # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    jmp .Lb2dd__bt_run_bb37 # branch
.Lb2dd__bt_run_bb36:
    mov r11, [rbp - 536] # reload L65 from spill slot
    mov r10, r11 # assign L66
    mov r11, [rbp - 2976] # tag L65 from tag-slot
    mov [rbp - 2984], r11 # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    jmp .Lb2dd__bt_run_bb37 # branch
.Lb2dd__bt_run_bb37:
    mov r10, [rbp - 544] # reload L66 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb39 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb38 # jump -> then
.Lb2dd__bt_run_bb38:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 3032], r11 # store tag L72
    mov [rbp - 592], r10 # spill L72 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 592] # reload L72 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L72 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3040], rax # store tag L73
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 600], r10 # spill L73 to slot
    mov rdx, [rbp - 600] # reload L73 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L73 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb39:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb40:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3064], rax # store tag L76
    mov [rbp - 624], r10 # spill L76 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3072], rax # store tag L77
    mov [rbp - 632], r10 # spill L77 to slot
    mov rsi, [rbp - 632] # reload L77 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 3072] # tag L77 from tag-slot
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2632] # tag L22 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3080], rax # store tag L78
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 640], r10 # spill L78 to slot
    mov rsi, [rbp - 624] # reload L76 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 3064] # tag L76 from tag-slot
    mov rcx, [rbp - 640] # reload L78 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3080] # tag L78 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3088], rax # store tag L79
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 648], r10 # spill L79 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 648] # reload L79 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3088] # tag L79 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3096], rax # store tag L80
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 656], r10 # spill L80 to slot
    mov rdx, [rbp - 656] # reload L80 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L80 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb41:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 7 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3104], rax # store tag L81
    mov [rbp - 664], r10 # spill L81 to slot
    mov r10, [rbp - 664] # reload L81 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb43 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb42 # jump -> then
.Lb2dd__bt_run_bb42:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3120], rax # store tag L83
    mov [rbp - 680], r10 # spill L83 to slot
    mov rsi, [rbp - 680] # reload L83 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 3120] # tag L83 from tag-slot
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2632] # tag L22 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3128], rax # store tag L84
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 688], r10 # spill L84 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 688] # reload L84 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3128] # tag L84 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3136], rax # store tag L85
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 696], r10 # spill L85 to slot
    mov r11, [rbp - 696] # reload L85 from spill slot
    mov r10, r11 # assign L86
    mov r11, 0 # tag L85 = TAG_INT (i64-local, fused)
    mov [rbp - 3144], r11 # store tag L86
    mov [rbp - 704], r10 # spill L86 to slot
    mov r10, [rbp - 704] # reload L86 from spill slot
    cmp r10, -2 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3152], r11 # store tag L87
    mov [rbp - 712], r10 # spill L87 to slot
    mov r10, [rbp - 712] # reload L87 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb45 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb44 # jump -> then
.Lb2dd__bt_run_bb43:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 5 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3208], rax # store tag L94
    mov [rbp - 768], r10 # spill L94 to slot
    mov r10, [rbp - 768] # reload L94 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb49 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb48 # jump -> then
.Lb2dd__bt_run_bb44:
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb45:
    mov r10, [rbp - 704] # reload L86 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3168], r11 # store tag L89
    mov [rbp - 728], r10 # spill L89 to slot
    mov r10, [rbp - 728] # reload L89 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb47 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb46 # jump -> then
.Lb2dd__bt_run_bb46:
    mov rdx, [rbp - 704] # reload L86 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L86 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb47:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3184], rax # store tag L91
    mov [rbp - 744], r10 # spill L91 to slot
    mov rsi, [rbp - 744] # reload L91 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 3184] # tag L91 from tag-slot
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2632] # tag L22 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3192], rax # store tag L92
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 752], r10 # spill L92 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 752] # reload L92 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3192] # tag L92 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3200], rax # store tag L93
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 760], r10 # spill L93 to slot
    mov rdx, [rbp - 760] # reload L93 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L93 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb48:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3224], rax # store tag L96
    mov [rbp - 784], r10 # spill L96 to slot
    mov rsi, [rbp - 784] # reload L96 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 3224] # tag L96 from tag-slot
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2632] # tag L22 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3232], rax # store tag L97
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 792], r10 # spill L97 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 792] # reload L97 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3232] # tag L97 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3240], rax # store tag L98
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 800], r10 # spill L98 to slot
    mov r11, [rbp - 800] # reload L98 from spill slot
    mov r10, r11 # assign L99
    mov r11, 0 # tag L98 = TAG_INT (i64-local, fused)
    mov [rbp - 3248], r11 # store tag L99
    mov [rbp - 808], r10 # spill L99 to slot
    mov r10, [rbp - 808] # reload L99 from spill slot
    cmp r10, -2 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3256], r11 # store tag L100
    mov [rbp - 816], r10 # spill L100 to slot
    mov r10, [rbp - 816] # reload L100 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb51 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb50 # jump -> then
.Lb2dd__bt_run_bb49:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3296], rax # store tag L105
    mov [rbp - 856], r10 # spill L105 to slot
    mov r10, [rbp - 856] # reload L105 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb55 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb54 # jump -> then
.Lb2dd__bt_run_bb50:
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb51:
    mov r10, [rbp - 808] # reload L99 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3272], r11 # store tag L102
    mov [rbp - 832], r10 # spill L102 to slot
    mov r10, [rbp - 832] # reload L102 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb53 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb52 # jump -> then
.Lb2dd__bt_run_bb52:
    mov rdx, [rbp - 808] # reload L99 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L99 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb53:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3288], rax # store tag L104
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 848], r10 # spill L104 to slot
    mov rdx, [rbp - 848] # reload L104 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L104 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb54:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3312], rax # store tag L107
    mov [rbp - 872], r10 # spill L107 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 3320], rax # store tag L108
    mov rcx, 21 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 3320] # tag L108 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 3320], rax # store tag L108
    mov rcx, [rbp - 872] # reload L107 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3312] # tag L107 from tag-slot
    mov rdi, [rbp - 3320] # tag L108 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 3320], rax # store tag L108
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 3320] # tag L108 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 3320], rax # store tag L108
    mov [rbp - 880], r10 # spill L108 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, [rbp - 880] # reload L108 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3320] # tag L108 from tag-slot
    call _bt_marker # call _bt_marker
    mov [rbp - 3328], rax # store tag L109
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 888], r10 # spill L109 to slot
    mov r11, [rbp - 888] # reload L109 from spill slot
    mov r10, r11 # assign L110
    mov r11, 0 # tag L109 = TAG_INT (i64-local, fused)
    mov [rbp - 3336], r11 # store tag L110
    mov [rbp - 896], r10 # spill L110 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3344], rax # store tag L111
    mov [rbp - 904], r10 # spill L111 to slot
    mov rsi, [rbp - 896] # reload L110 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L110 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2632] # tag L22 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3352], rax # store tag L112
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 912], r10 # spill L112 to slot
    mov rsi, [rbp - 904] # reload L111 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 3344] # tag L111 from tag-slot
    mov rcx, [rbp - 912] # reload L112 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3352] # tag L112 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3360], rax # store tag L113
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 920], r10 # spill L113 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 920] # reload L113 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3360] # tag L113 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3368], rax # store tag L114
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 928], r10 # spill L114 to slot
    mov r11, [rbp - 928] # reload L114 from spill slot
    mov r10, r11 # assign L115
    mov r11, 0 # tag L114 = TAG_INT (i64-local, fused)
    mov [rbp - 3376], r11 # store tag L115
    mov [rbp - 936], r10 # spill L115 to slot
    mov r10, [rbp - 936] # reload L115 from spill slot
    cmp r10, -2 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3384], r11 # store tag L116
    mov [rbp - 944], r10 # spill L116 to slot
    mov r10, [rbp - 944] # reload L116 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb57 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb56 # jump -> then
.Lb2dd__bt_run_bb55:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 4 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3424], rax # store tag L121
    mov [rbp - 984], r10 # spill L121 to slot
    mov r10, [rbp - 984] # reload L121 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb61 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb60 # jump -> then
.Lb2dd__bt_run_bb56:
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb57:
    mov r10, [rbp - 936] # reload L115 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3400], r11 # store tag L118
    mov [rbp - 960], r10 # spill L118 to slot
    mov r10, [rbp - 960] # reload L118 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb59 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb58 # jump -> then
.Lb2dd__bt_run_bb58:
    mov rdx, [rbp - 936] # reload L115 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L115 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb59:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3416], rax # store tag L120
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 976], r10 # spill L120 to slot
    mov rdx, [rbp - 976] # reload L120 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L120 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb60:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3440], rax # store tag L123
    mov [rbp - 1000], r10 # spill L123 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, [rbp - 1000] # reload L123 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3440] # tag L123 from tag-slot
    call _bt_star_view # call _bt_star_view
    mov [rbp - 3448], rax # store tag L124
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1008], r10 # spill L124 to slot
    mov r11, [rbp - 1008] # reload L124 from spill slot
    mov r10, r11 # assign L125
    mov r11, 0 # tag L124 = TAG_INT (i64-local, fused)
    mov [rbp - 3456], r11 # store tag L125
    mov [rbp - 1016], r10 # spill L125 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3464], rax # store tag L126
    mov [rbp - 1024], r10 # spill L126 to slot
    mov rsi, [rbp - 1016] # reload L125 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L125 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2632] # tag L22 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3472], rax # store tag L127
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1032], r10 # spill L127 to slot
    mov rsi, [rbp - 1024] # reload L126 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 3464] # tag L126 from tag-slot
    mov rcx, [rbp - 1032] # reload L127 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3472] # tag L127 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3480], rax # store tag L128
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1040], r10 # spill L128 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 1040] # reload L128 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3480] # tag L128 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3488], rax # store tag L129
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1048], r10 # spill L129 to slot
    mov rdx, [rbp - 1048] # reload L129 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L129 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb61:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 11 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3496], rax # store tag L130
    mov [rbp - 1056], r10 # spill L130 to slot
    mov r10, [rbp - 1056] # reload L130 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb63 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb62 # jump -> then
.Lb2dd__bt_run_bb62:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3512], rax # store tag L132
    mov [rbp - 1072], r10 # spill L132 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3520], rax # store tag L133
    mov [rbp - 1080], r10 # spill L133 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3528], rax # store tag L134
    mov [rbp - 1088], r10 # spill L134 to slot
    sub rsp, 112 # hv: reserve 7 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 96], r10 # hv stack arg 9 tag
    mov [rsp + 104], r11 # hv stack arg 9 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 80], r10 # hv stack arg 8 tag
    mov [rsp + 88], r11 # hv stack arg 8 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 64], r10 # hv stack arg 7 tag
    mov [rsp + 72], r11 # hv stack arg 7 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 1088] # reload L134 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3528] # tag L134 from tag-slot
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, [rbp - 1080] # reload L133 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3520] # tag L133 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 1072] # reload L132 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3512] # tag L132 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run_repeat # call _bt_run_repeat
    add rsp, 112 # hv: pop 7 stack arg slots
    mov [rbp - 3536], rax # store tag L135
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1096], r10 # spill L135 to slot
    mov rdx, [rbp - 1096] # reload L135 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L135 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb63:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 20 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3544], rax # store tag L136
    mov [rbp - 1104], r10 # spill L136 to slot
    mov r10, [rbp - 1104] # reload L136 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb65 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb64 # jump -> then
.Lb2dd__bt_run_bb64:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3560], rax # store tag L138
    mov [rbp - 1120], r10 # spill L138 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3568], rax # store tag L139
    mov [rbp - 1128], r10 # spill L139 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3576], rax # store tag L140
    mov [rbp - 1136], r10 # spill L140 to slot
    sub rsp, 112 # hv: reserve 7 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 96], r10 # hv stack arg 9 tag
    mov [rsp + 104], r11 # hv stack arg 9 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 80], r10 # hv stack arg 8 tag
    mov [rsp + 88], r11 # hv stack arg 8 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 64], r10 # hv stack arg 7 tag
    mov [rsp + 72], r11 # hv stack arg 7 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 1136] # reload L140 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3576] # tag L140 from tag-slot
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, [rbp - 1128] # reload L139 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3568] # tag L139 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 1120] # reload L138 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3560] # tag L138 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run_repeat # call _bt_run_repeat
    add rsp, 112 # hv: pop 7 stack arg slots
    mov [rbp - 3584], rax # store tag L141
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1144], r10 # spill L141 to slot
    mov rdx, [rbp - 1144] # reload L141 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L141 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb65:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 21 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3592], rax # store tag L142
    mov [rbp - 1152], r10 # spill L142 to slot
    mov r10, [rbp - 1152] # reload L142 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb67 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb66 # jump -> then
.Lb2dd__bt_run_bb66:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3608], rax # store tag L144
    mov [rbp - 1168], r10 # spill L144 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 1168] # reload L144 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 1168] # reload L144 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3608] # tag L144 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3616], rax # store tag L145
    mov [rbp - 1176], r10 # spill L145 to slot
    mov r10, [rbp - 1176] # reload L145 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb69 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb68 # jump -> then
.Lb2dd__bt_run_bb67:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 12 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3752], rax # store tag L162
    mov [rbp - 1312], r10 # spill L162 to slot
    mov r10, [rbp - 1312] # reload L162 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb75 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb74 # jump -> then
.Lb2dd__bt_run_bb68:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3632], rax # store tag L147
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1192], r10 # spill L147 to slot
    mov rdx, [rbp - 1192] # reload L147 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L147 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb69:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3640], rax # store tag L148
    mov [rbp - 1200], r10 # spill L148 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 3648], rax # store tag L149
    mov rcx, 21 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 3648] # tag L149 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 3648], rax # store tag L149
    mov rcx, [rbp - 1200] # reload L148 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3640] # tag L148 from tag-slot
    mov rdi, [rbp - 3648] # tag L149 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 3648], rax # store tag L149
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 3648] # tag L149 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 3648], rax # store tag L149
    mov [rbp - 1208], r10 # spill L149 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, [rbp - 1208] # reload L149 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3648] # tag L149 from tag-slot
    call _bt_marker # call _bt_marker
    mov [rbp - 3656], rax # store tag L150
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1216], r10 # spill L150 to slot
    mov r11, [rbp - 1216] # reload L150 from spill slot
    mov r10, r11 # assign L151
    mov r11, 0 # tag L150 = TAG_INT (i64-local, fused)
    mov [rbp - 3664], r11 # store tag L151
    mov [rbp - 1224], r10 # spill L151 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3672], rax # store tag L152
    mov [rbp - 1232], r10 # spill L152 to slot
    mov rsi, [rbp - 1224] # reload L151 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L151 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2632] # tag L22 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3680], rax # store tag L153
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1240], r10 # spill L153 to slot
    mov rsi, [rbp - 1232] # reload L152 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 3672] # tag L152 from tag-slot
    mov rcx, [rbp - 1240] # reload L153 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3680] # tag L153 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3688], rax # store tag L154
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1248], r10 # spill L154 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 1248] # reload L154 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3688] # tag L154 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3696], rax # store tag L155
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1256], r10 # spill L155 to slot
    mov r11, [rbp - 1256] # reload L155 from spill slot
    mov r10, r11 # assign L156
    mov r11, 0 # tag L155 = TAG_INT (i64-local, fused)
    mov [rbp - 3704], r11 # store tag L156
    mov [rbp - 1264], r10 # spill L156 to slot
    mov r10, [rbp - 1264] # reload L156 from spill slot
    cmp r10, -2 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3712], r11 # store tag L157
    mov [rbp - 1272], r10 # spill L157 to slot
    mov r10, [rbp - 1272] # reload L157 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb71 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb70 # jump -> then
.Lb2dd__bt_run_bb70:
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb71:
    mov r10, [rbp - 1264] # reload L156 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3728], r11 # store tag L159
    mov [rbp - 1288], r10 # spill L159 to slot
    mov r10, [rbp - 1288] # reload L159 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb73 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb72 # jump -> then
.Lb2dd__bt_run_bb72:
    mov rdx, [rbp - 1264] # reload L156 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L156 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb73:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3744], rax # store tag L161
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1304], r10 # spill L161 to slot
    mov rdx, [rbp - 1304] # reload L161 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L161 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb74:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3768], rax # store tag L164
    mov [rbp - 1328], r10 # spill L164 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 3776], rax # store tag L165
    mov rcx, 22 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 3776] # tag L165 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 3776], rax # store tag L165
    mov rcx, [rbp - 1328] # reload L164 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3768] # tag L164 from tag-slot
    mov rdi, [rbp - 3776] # tag L165 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 3776], rax # store tag L165
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 3776] # tag L165 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 3776], rax # store tag L165
    mov [rbp - 1336], r10 # spill L165 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, [rbp - 1336] # reload L165 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3776] # tag L165 from tag-slot
    call _bt_marker # call _bt_marker
    mov [rbp - 3784], rax # store tag L166
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1344], r10 # spill L166 to slot
    mov r11, [rbp - 1344] # reload L166 from spill slot
    mov r10, r11 # assign L167
    mov r11, 0 # tag L166 = TAG_INT (i64-local, fused)
    mov [rbp - 3792], r11 # store tag L167
    mov [rbp - 1352], r10 # spill L167 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3800], rax # store tag L168
    mov [rbp - 1360], r10 # spill L168 to slot
    mov rsi, [rbp - 1352] # reload L167 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L167 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2632] # tag L22 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3808], rax # store tag L169
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1368], r10 # spill L169 to slot
    mov rsi, [rbp - 1360] # reload L168 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 3800] # tag L168 from tag-slot
    mov rcx, [rbp - 1368] # reload L169 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3808] # tag L169 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 3816], rax # store tag L170
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1376], r10 # spill L170 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 1376] # reload L170 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 3816] # tag L170 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3824], rax # store tag L171
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1384], r10 # spill L171 to slot
    mov rdx, [rbp - 1384] # reload L171 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L171 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb75:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 22 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 3832], rax # store tag L172
    mov [rbp - 1392], r10 # spill L172 to slot
    mov r10, [rbp - 1392] # reload L172 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb77 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb76 # jump -> then
.Lb2dd__bt_run_bb76:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3848], rax # store tag L174
    mov [rbp - 1408], r10 # spill L174 to slot
    mov r11, [rbp - 1408] # reload L174 from spill slot
    mov r10, r11 # assign L175
    mov r11, [rbp - 3848] # tag L174 from tag-slot
    mov [rbp - 3856], r11 # store tag L175
    mov [rbp - 1416], r10 # spill L175 to slot
    mov r11, [rbp - 1416] # reload L175 from spill slot
    mov rsi, 2 # hv arg payload
    mov rdi, 0 # tag default = TAG_INT
    mov rcx, [rbp - 1416] # reload L175 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3856] # tag L175 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 3864], rax # store tag L176
    mov [rbp - 1424], r10 # spill L176 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rdx, [rbp - 1424] # reload L176 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 3872], rax # store tag L177
    mov [rbp - 1432], r10 # spill L177 to slot
    mov r11, [rbp - 1432] # reload L177 from spill slot
    mov r10, r11 # assign L178
    mov r11, [rbp - 3872] # tag L177 from tag-slot
    mov [rbp - 3880], r11 # store tag L178
    mov [rbp - 1440], r10 # spill L178 to slot
    mov r11, [rbp - 1416] # reload L175 from spill slot
    mov rsi, 2 # hv arg payload
    mov rdi, 0 # tag default = TAG_INT
    mov rcx, [rbp - 1416] # reload L175 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3856] # tag L175 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 3888], rax # store tag L179
    mov [rbp - 1448], r10 # spill L179 to slot
    mov r10, [rbp - 1448] # reload L179 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 3896], r11 # store tag L180
    mov [rbp - 1456], r10 # spill L180 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rdx, [rbp - 1456] # reload L180 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 3904], rax # store tag L181
    mov [rbp - 1464], r10 # spill L181 to slot
    mov r11, [rbp - 1464] # reload L181 from spill slot
    mov r10, r11 # assign L182
    mov r11, [rbp - 3904] # tag L181 from tag-slot
    mov [rbp - 3912], r11 # store tag L182
    mov [rbp - 1472], r10 # spill L182 to slot
    mov r11, [rbp - 1416] # reload L175 from spill slot
    mov rsi, 2 # hv arg payload
    mov rdi, 0 # tag default = TAG_INT
    mov rcx, [rbp - 1416] # reload L175 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3856] # tag L175 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 3920], rax # store tag L183
    mov [rbp - 1480], r10 # spill L183 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 3928], rax # store tag L184
    mov [rbp - 1488], r10 # spill L184 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rdx, [rbp - 1480] # reload L183 from spill slot
    mov rdx, rdx # hv payload
    mov r8, [rbp - 1488] # reload L184 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 3928] # tag L184 from tag-slot
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r12, rdx # index_set: store-back poly descriptor
    mov [rbp - 2464], rax # store tag L1
    mov r11, [rbp - 1416] # reload L175 from spill slot
    mov rsi, 2 # hv arg payload
    mov rdi, 0 # tag default = TAG_INT
    mov rcx, [rbp - 1416] # reload L175 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3856] # tag L175 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 3936], rax # store tag L185
    mov [rbp - 1496], r10 # spill L185 to slot
    mov r10, [rbp - 1496] # reload L185 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 3944], r11 # store tag L186
    mov [rbp - 1504], r10 # spill L186 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rdx, [rbp - 1504] # reload L186 from spill slot
    mov rdx, rdx # hv payload
    mov r8, [rbp - 56] # reload L5 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, 0 # tag L5 = TAG_INT (i64-local, fused)
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r12, rdx # index_set: store-back poly descriptor
    mov [rbp - 2464], rax # store tag L1
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 3952], rax # store tag L187
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1512], r10 # spill L187 to slot
    mov r11, [rbp - 1512] # reload L187 from spill slot
    mov r10, r11 # assign L188
    mov r11, 0 # tag L187 = TAG_INT (i64-local, fused)
    mov [rbp - 3960], r11 # store tag L188
    mov [rbp - 1520], r10 # spill L188 to slot
    mov r10, [rbp - 1520] # reload L188 from spill slot
    cmp r10, -2 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3968], r11 # store tag L189
    mov [rbp - 1528], r10 # spill L189 to slot
    mov r10, [rbp - 1528] # reload L189 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb79 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb78 # jump -> then
.Lb2dd__bt_run_bb77:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 13 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 4024], rax # store tag L196
    mov [rbp - 1584], r10 # spill L196 to slot
    mov r10, [rbp - 1584] # reload L196 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb83 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb82 # jump -> then
.Lb2dd__bt_run_bb78:
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb79:
    mov r10, [rbp - 1520] # reload L188 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 3984], r11 # store tag L191
    mov [rbp - 1544], r10 # spill L191 to slot
    mov r10, [rbp - 1544] # reload L191 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb81 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb80 # jump -> then
.Lb2dd__bt_run_bb80:
    mov rdx, [rbp - 1520] # reload L188 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L188 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb81:
    mov r11, [rbp - 1416] # reload L175 from spill slot
    mov rsi, 2 # hv arg payload
    mov rdi, 0 # tag default = TAG_INT
    mov rcx, [rbp - 1416] # reload L175 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3856] # tag L175 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 4000], rax # store tag L193
    mov [rbp - 1560], r10 # spill L193 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rdx, [rbp - 1560] # reload L193 from spill slot
    mov rdx, rdx # hv payload
    mov r8, [rbp - 1440] # reload L178 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 3880] # tag L178 from tag-slot
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r12, rdx # index_set: store-back poly descriptor
    mov [rbp - 2464], rax # store tag L1
    mov r11, [rbp - 1416] # reload L175 from spill slot
    mov rsi, 2 # hv arg payload
    mov rdi, 0 # tag default = TAG_INT
    mov rcx, [rbp - 1416] # reload L175 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 3856] # tag L175 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 4008], rax # store tag L194
    mov [rbp - 1568], r10 # spill L194 to slot
    mov r10, [rbp - 1568] # reload L194 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 4016], r11 # store tag L195
    mov [rbp - 1576], r10 # spill L195 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rdx, [rbp - 1576] # reload L195 from spill slot
    mov rdx, rdx # hv payload
    mov r8, [rbp - 1472] # reload L182 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 3912] # tag L182 from tag-slot
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r12, rdx # index_set: store-back poly descriptor
    mov [rbp - 2464], rax # store tag L1
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb82:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 4040], rax # store tag L198
    mov [rbp - 1600], r10 # spill L198 to slot
    mov r11, [rbp - 1600] # reload L198 from spill slot
    mov r10, r11 # assign L199
    mov r11, [rbp - 4040] # tag L198 from tag-slot
    mov [rbp - 4048], r11 # store tag L199
    mov [rbp - 1608], r10 # spill L199 to slot
    mov r11, [rbp - 1608] # reload L199 from spill slot
    mov rsi, 2 # hv arg payload
    mov rdi, 0 # tag default = TAG_INT
    mov rcx, [rbp - 1608] # reload L199 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4048] # tag L199 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 4056], rax # store tag L200
    mov [rbp - 1616], r10 # spill L200 to slot
    mov r10, [rbp - 1616] # reload L200 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 4064], r11 # store tag L201
    mov [rbp - 1624], r10 # spill L201 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 4072], r11 # store tag L202
    mov [rbp - 1632], r10 # spill L202 to slot
    mov r10, [rbp - 1624] # reload L201 from spill slot
    mov r11, [rbp - 1632] # reload L202 from spill slot
    mov rsi, [rbp - 1624] # reload L201 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L201 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 1632] # reload L202 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4072] # tag L202 from tag-slot
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 4080], rax # store tag L203
    mov [rbp - 1640], r10 # spill L203 to slot
    mov r10, [rbp - 1640] # reload L203 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb85 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb84 # jump -> then
.Lb2dd__bt_run_bb83:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 14 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 4320], rax # store tag L233
    mov [rbp - 1880], r10 # spill L233 to slot
    mov r10, [rbp - 1880] # reload L233 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb99 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb98 # jump -> then
.Lb2dd__bt_run_bb84:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb85:
    mov r11, [rbp - 1608] # reload L199 from spill slot
    mov rsi, 2 # hv arg payload
    mov rdi, 0 # tag default = TAG_INT
    mov rcx, [rbp - 1608] # reload L199 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4048] # tag L199 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 4096], rax # store tag L205
    mov [rbp - 1656], r10 # spill L205 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rdx, [rbp - 1656] # reload L205 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 4104], rax # store tag L206
    mov [rbp - 1664], r10 # spill L206 to slot
    mov r11, [rbp - 1664] # reload L206 from spill slot
    mov r10, r11 # assign L207
    mov r11, [rbp - 4104] # tag L206 from tag-slot
    mov [rbp - 4112], r11 # store tag L207
    mov [rbp - 1672], r10 # spill L207 to slot
    mov r11, [rbp - 1608] # reload L199 from spill slot
    mov rsi, 2 # hv arg payload
    mov rdi, 0 # tag default = TAG_INT
    mov rcx, [rbp - 1608] # reload L199 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4048] # tag L199 from tag-slot
    call hexa_mul # binop *: tag-dispatch hexa_mul
    mov r10, rdx # binop *: capture result payload
    mov [rbp - 4120], rax # store tag L208
    mov [rbp - 1680], r10 # spill L208 to slot
    mov r10, [rbp - 1680] # reload L208 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 4128], r11 # store tag L209
    mov [rbp - 1688], r10 # spill L209 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rdx, [rbp - 1688] # reload L209 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 4136], rax # store tag L210
    mov [rbp - 1696], r10 # spill L210 to slot
    mov r11, [rbp - 1696] # reload L210 from spill slot
    mov r10, r11 # assign L211
    mov r11, [rbp - 4136] # tag L210 from tag-slot
    mov [rbp - 4144], r11 # store tag L211
    mov [rbp - 1704], r10 # spill L211 to slot
    mov r10, [rbp - 1672] # reload L207 from spill slot
    mov rsi, [rbp - 1672] # reload L207 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4112] # tag L207 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 4152], rax # store tag L212
    mov [rbp - 1712], r10 # spill L212 to slot
    mov r10, [rbp - 1712] # reload L212 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb87 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb86 # jump -> then
.Lb2dd__bt_run_bb86:
    mov r11, [rbp - 1712] # reload L212 from spill slot
    mov r10, r11 # assign L213
    mov r11, [rbp - 4152] # tag L212 from tag-slot
    mov [rbp - 4160], r11 # store tag L213
    mov [rbp - 1720], r10 # spill L213 to slot
    jmp .Lb2dd__bt_run_bb88 # branch
.Lb2dd__bt_run_bb87:
    mov r10, [rbp - 1704] # reload L211 from spill slot
    mov rsi, [rbp - 1704] # reload L211 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4144] # tag L211 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 4168], rax # store tag L214
    mov [rbp - 1728], r10 # spill L214 to slot
    mov r11, [rbp - 1728] # reload L214 from spill slot
    mov r10, r11 # assign L213
    mov r11, [rbp - 4168] # tag L214 from tag-slot
    mov [rbp - 4160], r11 # store tag L213
    mov [rbp - 1720], r10 # spill L213 to slot
    jmp .Lb2dd__bt_run_bb88 # branch
.Lb2dd__bt_run_bb88:
    mov r10, [rbp - 1720] # reload L213 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb90 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb89 # jump -> then
.Lb2dd__bt_run_bb89:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 4184], rax # store tag L216
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1744], r10 # spill L216 to slot
    mov rdx, [rbp - 1744] # reload L216 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L216 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb90:
    mov r10, [rbp - 1704] # reload L211 from spill slot
    mov r11, [rbp - 1672] # reload L207 from spill slot
    mov rsi, [rbp - 1704] # reload L211 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4144] # tag L211 from tag-slot
    mov rcx, [rbp - 1672] # reload L207 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4112] # tag L207 from tag-slot
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 4192], rax # store tag L217
    mov [rbp - 1752], r10 # spill L217 to slot
    mov r11, [rbp - 1752] # reload L217 from spill slot
    mov r10, r11 # assign L218
    mov r11, [rbp - 4192] # tag L217 from tag-slot
    mov [rbp - 4200], r11 # store tag L218
    mov [rbp - 1760], r10 # spill L218 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 1760] # reload L218 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 1760] # reload L218 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4200] # tag L218 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 4208], rax # store tag L219
    mov [rbp - 1768], r10 # spill L219 to slot
    mov r10, [rbp - 1768] # reload L219 from spill slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    cmp r10, r11 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4216], r11 # store tag L220
    mov [rbp - 1776], r10 # spill L220 to slot
    mov r10, [rbp - 1776] # reload L220 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb92 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb91 # jump -> then
.Lb2dd__bt_run_bb91:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb92:
    mov r10, 0 # assign L222
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 4232], r11 # store tag L222
    mov [rbp - 1792], r10 # spill L222 to slot
    jmp .Lb2dd__bt_run_bb93 # branch
.Lb2dd__bt_run_bb93:
    mov r10, [rbp - 1792] # reload L222 from spill slot
    mov r11, [rbp - 1760] # reload L218 from spill slot
    mov rsi, [rbp - 1792] # reload L222 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L222 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 1760] # reload L218 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4200] # tag L218 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 4240], rax # store tag L223
    mov [rbp - 1800], r10 # spill L223 to slot
    mov r10, [rbp - 1800] # reload L223 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb95 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb94 # jump -> then
.Lb2dd__bt_run_bb94:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 1792] # reload L222 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, r11 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 4248], r11 # store tag L224
    mov [rbp - 1808], r10 # spill L224 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 2488] # tag L4 from tag-slot
    mov rcx, [rbp - 1808] # reload L224 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L224 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 4256], rax # store tag L225
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 1816], r10 # spill L225 to slot
    mov r10, [rbp - 1672] # reload L207 from spill slot
    mov r11, [rbp - 1792] # reload L222 from spill slot
    mov rsi, [rbp - 1672] # reload L207 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4112] # tag L207 from tag-slot
    mov rcx, [rbp - 1792] # reload L222 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L222 = TAG_INT (i64-local, fused)
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 4264], rax # store tag L226
    mov [rbp - 1824], r10 # spill L226 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 2488] # tag L4 from tag-slot
    mov rcx, [rbp - 1824] # reload L226 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4264] # tag L226 from tag-slot
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 4272], rax # store tag L227
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 1832], r10 # spill L227 to slot
    mov r10, [rbp - 1816] # reload L225 from spill slot
    mov r11, [rbp - 1832] # reload L227 from spill slot
    mov rsi, [rbp - 1816] # reload L225 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4256] # tag L225 from tag-slot
    mov rcx, [rbp - 1832] # reload L227 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4272] # tag L227 from tag-slot
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4280], r11 # store tag L228
    mov [rbp - 1840], r10 # spill L228 to slot
    mov r10, [rbp - 1840] # reload L228 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb97 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb96 # jump -> then
.Lb2dd__bt_run_bb95:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 1760] # reload L218 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 1760] # reload L218 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4200] # tag L218 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 4304], rax # store tag L231
    mov [rbp - 1864], r10 # spill L231 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 1864] # reload L231 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L231 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 4312], rax # store tag L232
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1872], r10 # spill L232 to slot
    mov rdx, [rbp - 1872] # reload L232 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L232 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb96:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb97:
    mov r10, [rbp - 1792] # reload L222 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 4296], r11 # store tag L230
    mov [rbp - 1856], r10 # spill L230 to slot
    mov r11, [rbp - 1856] # reload L230 from spill slot
    mov r10, r11 # assign L222
    mov r11, 0 # tag L230 = TAG_INT (i64-local, fused)
    mov [rbp - 4232], r11 # store tag L222
    mov [rbp - 1792], r10 # spill L222 to slot
    jmp .Lb2dd__bt_run_bb93 # branch
.Lb2dd__bt_run_bb98:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 4336], rax # store tag L235
    mov [rbp - 1896], r10 # spill L235 to slot
    mov r10, [rbp - 1896] # reload L235 from spill slot
    mov rsi, [rbp - 1896] # reload L235 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4336] # tag L235 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 4344], rax # store tag L236
    mov [rbp - 1904], r10 # spill L236 to slot
    mov r10, [rbp - 1904] # reload L236 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb101 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb100 # jump -> then
.Lb2dd__bt_run_bb99:
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2672] # tag L27 from tag-slot
    mov rcx, 15 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 4528], rax # store tag L259
    mov [rbp - 2088], r10 # spill L259 to slot
    mov r10, [rbp - 2088] # reload L259 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb111 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb110 # jump -> then
.Lb2dd__bt_run_bb100:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    call _bt_copy # call _bt_copy
    mov [rbp - 4360], rax # store tag L238
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1920], r10 # spill L238 to slot
    mov r11, [rbp - 1920] # reload L238 from spill slot
    mov r10, r11 # assign L239
    mov r11, [rbp - 4360] # tag L238 from tag-slot
    mov [rbp - 4368], r11 # store tag L239
    mov [rbp - 1928], r10 # spill L239 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 4376], rax # store tag L240
    mov [rbp - 1936], r10 # spill L240 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 4384], rax # store tag L241
    mov rcx, [rbp - 1936] # reload L240 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4376] # tag L240 from tag-slot
    mov rdi, [rbp - 4384] # tag L241 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 4384], rax # store tag L241
    mov [rbp - 1944], r10 # spill L241 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 1944] # reload L241 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 4384] # tag L241 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 4392], rax # store tag L242
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1952], r10 # spill L242 to slot
    mov r11, [rbp - 1952] # reload L242 from spill slot
    mov r10, r11 # assign L243
    mov r11, 0 # tag L242 = TAG_INT (i64-local, fused)
    mov [rbp - 4400], r11 # store tag L243
    mov [rbp - 1960], r10 # spill L243 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rcx, [rbp - 1928] # reload L239 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4368] # tag L239 from tag-slot
    call _bt_restore # call _bt_restore
    mov [rbp - 4408], rax # store tag L244
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1968], r10 # spill L244 to slot
    mov r10, [rbp - 1960] # reload L243 from spill slot
    cmp r10, -2 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4416], r11 # store tag L245
    mov [rbp - 1976], r10 # spill L245 to slot
    mov r10, [rbp - 1976] # reload L245 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb103 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb102 # jump -> then
.Lb2dd__bt_run_bb101:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 4456], rax # store tag L250
    mov [rbp - 2016], r10 # spill L250 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 4464], rax # store tag L251
    mov rcx, [rbp - 2016] # reload L250 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4456] # tag L250 from tag-slot
    mov rdi, [rbp - 4464] # tag L251 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 4464], rax # store tag L251
    mov [rbp - 2024], r10 # spill L251 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 2024] # reload L251 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 4464] # tag L251 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 4472], rax # store tag L252
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2032], r10 # spill L252 to slot
    mov r11, [rbp - 2032] # reload L252 from spill slot
    mov r10, r11 # assign L253
    mov r11, 0 # tag L252 = TAG_INT (i64-local, fused)
    mov [rbp - 4480], r11 # store tag L253
    mov [rbp - 2040], r10 # spill L253 to slot
    mov r10, [rbp - 2040] # reload L253 from spill slot
    cmp r10, -2 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4488], r11 # store tag L254
    mov [rbp - 2048], r10 # spill L254 to slot
    mov r10, [rbp - 2048] # reload L254 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb107 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb106 # jump -> then
.Lb2dd__bt_run_bb102:
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb103:
    mov r10, [rbp - 1960] # reload L243 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4432], r11 # store tag L247
    mov [rbp - 1992], r10 # spill L247 to slot
    mov r10, [rbp - 1992] # reload L247 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb105 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb104 # jump -> then
.Lb2dd__bt_run_bb104:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 4448], rax # store tag L249
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2008], r10 # spill L249 to slot
    mov rdx, [rbp - 2008] # reload L249 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L249 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb105:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb106:
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb107:
    mov r10, [rbp - 2040] # reload L253 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4504], r11 # store tag L256
    mov [rbp - 2064], r10 # spill L256 to slot
    mov r10, [rbp - 2064] # reload L256 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb109 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb108 # jump -> then
.Lb2dd__bt_run_bb108:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 4520], rax # store tag L258
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2080], r10 # spill L258 to slot
    mov rdx, [rbp - 2080] # reload L258 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L258 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb109:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb110:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 4544], rax # store tag L261
    mov [rbp - 2104], r10 # spill L261 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 4552], rax # store tag L262
    mov [rbp - 2112], r10 # spill L262 to slot
    mov rsi, [rbp - 2104] # reload L261 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4544] # tag L261 from tag-slot
    mov rcx, [rbp - 2112] # reload L262 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4552] # tag L262 from tag-slot
    call _bt_minwidth # call _bt_minwidth
    mov [rbp - 4560], rax # store tag L263
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2120], r10 # spill L263 to slot
    mov r11, [rbp - 2120] # reload L263 from spill slot
    mov r10, r11 # assign L264
    mov r11, 0 # tag L263 = TAG_INT (i64-local, fused)
    mov [rbp - 4568], r11 # store tag L264
    mov [rbp - 2128], r10 # spill L264 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 4576], rax # store tag L265
    mov [rbp - 2136], r10 # spill L265 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 4584], rax # store tag L266
    mov [rbp - 2144], r10 # spill L266 to slot
    mov rsi, [rbp - 2136] # reload L265 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4576] # tag L265 from tag-slot
    mov rcx, [rbp - 2144] # reload L266 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4584] # tag L266 from tag-slot
    call _bt_maxwidth # call _bt_maxwidth
    mov [rbp - 4592], rax # store tag L267
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2152], r10 # spill L267 to slot
    mov r11, [rbp - 2152] # reload L267 from spill slot
    mov r10, r11 # assign L268
    mov r11, 0 # tag L267 = TAG_INT (i64-local, fused)
    mov [rbp - 4600], r11 # store tag L268
    mov [rbp - 2160], r10 # spill L268 to slot
    mov r10, 0 # assign L269
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 4608], r11 # store tag L269
    mov [rbp - 2168], r10 # spill L269 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    call _bt_copy # call _bt_copy
    mov [rbp - 4616], rax # store tag L270
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2176], r10 # spill L270 to slot
    mov r11, [rbp - 2176] # reload L270 from spill slot
    mov r10, r11 # assign L271
    mov r11, [rbp - 4616] # tag L270 from tag-slot
    mov [rbp - 4624], r11 # store tag L271
    mov [rbp - 2184], r10 # spill L271 to slot
    mov r10, [rbp - 2160] # reload L268 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4632], r11 # store tag L272
    mov [rbp - 2192], r10 # spill L272 to slot
    mov r10, [rbp - 2192] # reload L272 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb113 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb112 # jump -> then
.Lb2dd__bt_run_bb111:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb112:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 4648], rax # store tag L274
    mov rcx, 23 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 4648] # tag L274 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 4648], rax # store tag L274
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 4648] # tag L274 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 4648], rax # store tag L274
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 4648] # tag L274 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 4648], rax # store tag L274
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 4648] # tag L274 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 4648], rax # store tag L274
    mov [rbp - 2208], r10 # spill L274 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, [rbp - 2208] # reload L274 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4648] # tag L274 from tag-slot
    call _bt_marker # call _bt_marker
    mov [rbp - 4656], rax # store tag L275
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2216], r10 # spill L275 to slot
    mov r11, [rbp - 2216] # reload L275 from spill slot
    mov r10, r11 # assign L276
    mov r11, 0 # tag L275 = TAG_INT (i64-local, fused)
    mov [rbp - 4664], r11 # store tag L276
    mov [rbp - 2224], r10 # spill L276 to slot
    mov r11, [rbp - 2160] # reload L268 from spill slot
    mov r10, r11 # assign L277
    mov r11, 0 # tag L268 = TAG_INT (i64-local, fused)
    mov [rbp - 4672], r11 # store tag L277
    mov [rbp - 2232], r10 # spill L277 to slot
    mov r10, [rbp - 2232] # reload L277 from spill slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    cmp r10, r11 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4680], r11 # store tag L278
    mov [rbp - 2240], r10 # spill L278 to slot
    mov r10, [rbp - 2240] # reload L278 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb115 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb114 # jump -> then
.Lb2dd__bt_run_bb113:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 4816], rax # store tag L295
    mov [rbp - 2376], r10 # spill L295 to slot
    mov r10, [rbp - 2376] # reload L295 from spill slot
    mov rsi, [rbp - 2376] # reload L295 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4816] # tag L295 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 4824], rax # store tag L296
    mov [rbp - 2384], r10 # spill L296 to slot
    mov r10, [rbp - 2384] # reload L296 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb126 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb125 # jump -> then
.Lb2dd__bt_run_bb114:
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L277
    mov r11, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rbp - 4672], r11 # store tag L277
    mov [rbp - 2232], r10 # spill L277 to slot
    jmp .Lb2dd__bt_run_bb115 # branch
.Lb2dd__bt_run_bb115:
    jmp .Lb2dd__bt_run_bb116 # branch
.Lb2dd__bt_run_bb116:
    mov r10, [rbp - 2232] # reload L277 from spill slot
    mov r11, [rbp - 2128] # reload L264 from spill slot
    cmp r10, r11 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4696], r11 # store tag L280
    mov [rbp - 2256], r10 # spill L280 to slot
    mov r10, [rbp - 2256] # reload L280 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb118 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb117 # jump -> then
.Lb2dd__bt_run_bb117:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 2232] # reload L277 from spill slot
    mov r10, r10 # binop lhs into dst
    sub r10, r11 # binop -
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 4704], r11 # store tag L281
    mov [rbp - 2264], r10 # spill L281 to slot
    mov r11, [rbp - 2264] # reload L281 from spill slot
    mov r10, r11 # assign L282
    mov r11, 0 # tag L281 = TAG_INT (i64-local, fused)
    mov [rbp - 4712], r11 # store tag L282
    mov [rbp - 2272], r10 # spill L282 to slot
    mov r10, [rbp - 2272] # reload L282 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4720], r11 # store tag L283
    mov [rbp - 2280], r10 # spill L283 to slot
    mov r10, [rbp - 2280] # reload L283 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb120 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb119 # jump -> then
.Lb2dd__bt_run_bb118:
    jmp .Lb2dd__bt_run_bb113 # branch
.Lb2dd__bt_run_bb119:
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2656] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 4736], rax # store tag L285
    mov [rbp - 2296], r10 # spill L285 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 4744], rax # store tag L286
    mov rcx, [rbp - 2296] # reload L285 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4736] # tag L285 from tag-slot
    mov rdi, [rbp - 4744] # tag L286 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 4744], rax # store tag L286
    mov rcx, [rbp - 2224] # reload L276 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L276 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 4744] # tag L286 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 4744], rax # store tag L286
    mov [rbp - 2304], r10 # spill L286 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 2272] # reload L282 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L282 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 2304] # reload L286 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 4744] # tag L286 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 4752], rax # store tag L287
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2312], r10 # spill L287 to slot
    mov r11, [rbp - 2312] # reload L287 from spill slot
    mov r10, r11 # assign L288
    mov r11, 0 # tag L287 = TAG_INT (i64-local, fused)
    mov [rbp - 4760], r11 # store tag L288
    mov [rbp - 2320], r10 # spill L288 to slot
    mov r10, [rbp - 2320] # reload L288 from spill slot
    cmp r10, -2 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4768], r11 # store tag L289
    mov [rbp - 2328], r10 # spill L289 to slot
    mov r10, [rbp - 2328] # reload L289 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb122 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb121 # jump -> then
.Lb2dd__bt_run_bb120:
    mov r10, [rbp - 2232] # reload L277 from spill slot
    mov r10, r10 # binop lhs into dst
    sub r10, 1 # binop -
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 4808], r11 # store tag L294
    mov [rbp - 2368], r10 # spill L294 to slot
    mov r11, [rbp - 2368] # reload L294 from spill slot
    mov r10, r11 # assign L277
    mov r11, 0 # tag L294 = TAG_INT (i64-local, fused)
    mov [rbp - 4672], r11 # store tag L277
    mov [rbp - 2232], r10 # spill L277 to slot
    jmp .Lb2dd__bt_run_bb116 # branch
.Lb2dd__bt_run_bb121:
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb122:
    mov r10, [rbp - 2320] # reload L288 from spill slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    cmp r10, r11 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4784], r11 # store tag L291
    mov [rbp - 2344], r10 # spill L291 to slot
    mov r10, [rbp - 2344] # reload L291 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb124 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb123 # jump -> then
.Lb2dd__bt_run_bb123:
    mov r10, 1 # assign L269
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 4608], r11 # store tag L269
    mov [rbp - 2168], r10 # spill L269 to slot
    jmp .Lb2dd__bt_run_bb118 # branch
.Lb2dd__bt_run_bb124:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rcx, [rbp - 2184] # reload L271 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4624] # tag L271 from tag-slot
    call _bt_restore # call _bt_restore
    mov [rbp - 4800], rax # store tag L293
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2360], r10 # spill L293 to slot
    jmp .Lb2dd__bt_run_bb120 # branch
.Lb2dd__bt_run_bb125:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rcx, [rbp - 2184] # reload L271 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4624] # tag L271 from tag-slot
    call _bt_restore # call _bt_restore
    mov [rbp - 4840], rax # store tag L298
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2400], r10 # spill L298 to slot
    mov rsi, [rbp - 2168] # reload L269 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 4608] # tag L269 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 4848], r11 # store tag L299
    mov [rbp - 2408], r10 # spill L299 to slot
    mov r10, [rbp - 2408] # reload L299 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb128 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb127 # jump -> then
.Lb2dd__bt_run_bb126:
    mov r10, [rbp - 2168] # reload L269 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_bb130 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_bb129 # jump -> then
.Lb2dd__bt_run_bb127:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 4864], rax # store tag L301
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2424], r10 # spill L301 to slot
    mov rdx, [rbp - 2424] # reload L301 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L301 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb128:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb129:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 2488] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 2632] # tag L22 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 2456] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 2464] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 2472] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 4880], rax # store tag L303
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2440], r10 # spill L303 to slot
    mov rdx, [rbp - 2440] # reload L303 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L303 = TAG_INT (i64-local, fused)
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_bb130:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 2464] # tag L1 from tag-slot
    mov rcx, [rbp - 2184] # reload L271 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 4624] # tag L271 from tag-slot
    call _bt_restore # call _bt_restore
    mov [rbp - 4888], rax # store tag L304
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 2448], r10 # spill L304 to slot
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 4848 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _bt_run_repeat
    .p2align 4
_bt_run_repeat:
    .loc 1 755 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 640 # prologue: alloc spill frame
    mov [rbp - 352], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 360], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 368], r8 # store tag L2
    mov r13, r9 # ingress param payload
    mov r10, [rbp + 16] # ingress stack param 3 tag
    mov [rbp - 376], r10 # store tag L3
    mov r10, [rbp + 24] # ingress stack param 3 payload
    mov r14, r10 # ingress stack param payload
    mov r10, [rbp + 32] # ingress stack param 4 tag
    mov [rbp - 384], r10 # store tag L4
    mov r10, [rbp + 40] # ingress stack param 4 payload
    mov r15, r10 # ingress stack param payload
    mov r10, [rbp + 48] # ingress stack param 5 tag
    mov [rbp - 392], r10 # store tag L5
    mov r10, [rbp + 56] # ingress stack param 5 payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp + 64] # ingress stack param 6 tag
    mov [rbp - 400], r10 # store tag L6
    mov r10, [rbp + 72] # ingress stack param 6 payload
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp + 80] # ingress stack param 7 tag
    mov [rbp - 408], r10 # store tag L7
    mov r10, [rbp + 88] # ingress stack param 7 payload
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp + 96] # ingress stack param 8 tag
    mov [rbp - 416], r10 # store tag L8
    mov r10, [rbp + 104] # ingress stack param 8 payload
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, [rbp + 112] # ingress stack param 9 tag
    mov [rbp - 424], r10 # store tag L9
    mov r10, [rbp + 120] # ingress stack param 9 payload
    mov [rbp - 88], r10 # spill L9 to slot
.Lb2dd__bt_run_repeat_bb0:
    cmp r15, 0 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 432], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_repeat_bb2 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_repeat_bb1 # jump -> then
.Lb2dd__bt_run_repeat_bb1:
    mov r10, r15 # binop lhs into dst
    sub r10, 1 # binop -
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 448], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 456], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_repeat_bb4 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_repeat_bb3 # jump -> then
.Lb2dd__bt_run_repeat_bb2:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 528], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_repeat_bb7 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_repeat_bb6 # jump -> then
.Lb2dd__bt_run_repeat_bb3:
    mov r10, -1 # assign L14
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 464], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_run_repeat_bb5 # branch
.Lb2dd__bt_run_repeat_bb4:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r10, r10 # binop lhs into dst
    sub r10, 1 # binop -
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 472], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L15 = TAG_INT (i64-local, fused)
    mov [rbp - 464], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__bt_run_repeat_bb5 # branch
.Lb2dd__bt_run_repeat_bb5:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 480], rax # store tag L16
    mov rcx, 20 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 480] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 480], rax # store tag L16
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 480] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 480], rax # store tag L16
    mov rcx, [rbp - 112] # reload L12 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L12 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 480] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 480], rax # store tag L16
    mov rcx, [rbp - 128] # reload L14 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L14 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 480] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 480], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 352] # tag L0 from tag-slot
    mov rcx, [rbp - 144] # reload L16 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 480] # tag L16 from tag-slot
    call _bt_marker # call _bt_marker
    mov [rbp - 488], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, 0 # tag L17 = TAG_INT (i64-local, fused)
    mov [rbp - 496], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L18 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 64] # reload L6 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 400] # tag L6 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 504], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 168] # reload L19 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 504] # tag L19 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 512], rax # store tag L20
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 176], r10 # spill L20 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L8 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 408] # tag L7 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 176] # reload L20 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 512] # tag L20 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 352] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 360] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 368] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 520], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov rdx, [rbp - 184] # reload L21 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L21 = TAG_INT (i64-local, fused)
    add rsp, 640 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_repeat_bb6:
    mov r10, [rbp - 56] # reload L5 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 544], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_repeat_bb9 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_repeat_bb8 # jump -> then
.Lb2dd__bt_run_repeat_bb7:
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L8 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 408] # tag L7 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 400] # tag L6 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 352] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 360] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 368] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 680], rax # store tag L41
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 344], r10 # spill L41 to slot
    mov rdx, [rbp - 344] # reload L41 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L41 = TAG_INT (i64-local, fused)
    add rsp, 640 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_repeat_bb8:
    mov r10, -1 # assign L25
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 552], r11 # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    jmp .Lb2dd__bt_run_repeat_bb10 # branch
.Lb2dd__bt_run_repeat_bb9:
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r10, r10 # binop lhs into dst
    sub r10, 1 # binop -
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 560], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L25
    mov r11, 0 # tag L26 = TAG_INT (i64-local, fused)
    mov [rbp - 552], r11 # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    jmp .Lb2dd__bt_run_repeat_bb10 # branch
.Lb2dd__bt_run_repeat_bb10:
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L27
    mov r11, 0 # tag L25 = TAG_INT (i64-local, fused)
    mov [rbp - 568], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 576], rax # store tag L28
    mov rcx, 20 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 576] # tag L28 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 576], rax # store tag L28
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 576] # tag L28 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 576], rax # store tag L28
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 576] # tag L28 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 576], rax # store tag L28
    mov rcx, [rbp - 232] # reload L27 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L27 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 576] # tag L28 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 576], rax # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 352] # tag L0 from tag-slot
    mov rcx, [rbp - 240] # reload L28 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 576] # tag L28 from tag-slot
    call _bt_marker # call _bt_marker
    mov [rbp - 584], rax # store tag L29
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 248], r10 # spill L29 to slot
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L30
    mov r11, 0 # tag L29 = TAG_INT (i64-local, fused)
    mov [rbp - 592], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov rsi, [rbp - 256] # reload L30 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L30 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 64] # reload L6 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 400] # tag L6 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 600], rax # store tag L31
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 264], r10 # spill L31 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 264] # reload L31 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 600] # tag L31 from tag-slot
    call _bt_cons # call _bt_cons
    mov [rbp - 608], rax # store tag L32
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 272], r10 # spill L32 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L8 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 408] # tag L7 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 272] # reload L32 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 608] # tag L32 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 352] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 360] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 368] # tag L2 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 616], rax # store tag L33
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 280], r10 # spill L33 to slot
    mov r11, [rbp - 280] # reload L33 from spill slot
    mov r10, r11 # assign L34
    mov r11, 0 # tag L33 = TAG_INT (i64-local, fused)
    mov [rbp - 624], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    cmp r10, -2 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 632], r11 # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov r10, [rbp - 296] # reload L35 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_repeat_bb12 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_repeat_bb11 # jump -> then
.Lb2dd__bt_run_repeat_bb11:
    mov rdx, -2 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 640 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_repeat_bb12:
    mov r10, [rbp - 288] # reload L34 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 648], r11 # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_repeat_bb14 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_repeat_bb13 # jump -> then
.Lb2dd__bt_run_repeat_bb13:
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    cmp r10, r11 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 664], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov r11, [rbp - 328] # reload L39 from spill slot
    mov r10, r11 # assign L38
    mov r11, [rbp - 664] # tag L39 from tag-slot
    mov [rbp - 656], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    jmp .Lb2dd__bt_run_repeat_bb15 # branch
.Lb2dd__bt_run_repeat_bb14:
    mov r11, [rbp - 312] # reload L37 from spill slot
    mov r10, r11 # assign L38
    mov r11, [rbp - 648] # tag L37 from tag-slot
    mov [rbp - 656], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    jmp .Lb2dd__bt_run_repeat_bb15 # branch
.Lb2dd__bt_run_repeat_bb15:
    mov r10, [rbp - 320] # reload L38 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__bt_run_repeat_bb17 # jump-if-zero -> else
    jmp .Lb2dd__bt_run_repeat_bb16 # jump -> then
.Lb2dd__bt_run_repeat_bb16:
    mov rdx, [rbp - 288] # reload L34 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L34 = TAG_INT (i64-local, fused)
    add rsp, 640 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__bt_run_repeat_bb17:
    jmp .Lb2dd__bt_run_repeat_bb7 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 640 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden bt_match_full
    .p2align 4
bt_match_full:
    .loc 1 777 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 416 # prologue: alloc spill frame
    mov [rbp - 240], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 248], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_bt_match_full_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 240] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 256], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 256] # tag L2 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r13, rdx # index_set: store-back container payload
    mov [rbp - 256], rax # store tag L2
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 240] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r14, rdx # field: capture result payload
    mov [rbp - 264], rax # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 264] # tag L3 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r15, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 272], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd_bt_match_full_bb2 # jump-if-zero -> else
    jmp .Lb2dd_bt_match_full_bb1 # jump -> then
.Lb2dd_bt_match_full_bb1:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 416 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_match_full_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 248] # tag L1 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 288], rax # store tag L6
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 64], r10 # spill L6 to slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r10, r11 # assign L7
    mov r11, [rbp - 288] # tag L6 from tag-slot
    mov [rbp - 296], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 304], rax # store tag L8
    mov rcx, 10 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 304] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 304], rax # store tag L8
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 304] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 304], rax # store tag L8
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 304] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 304], rax # store tag L8
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 304] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 304], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 240] # tag L0 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 304] # tag L8 from tag-slot
    call _bt_marker # call _bt_marker
    mov [rbp - 312], rax # store tag L9
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 88], r10 # spill L9 to slot
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r10, r11 # assign L10
    mov r11, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rbp - 320], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 240] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr9] # map key (const char*): &.LCstr9
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 328], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 328] # tag L11 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 336], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, 2 # binop lhs into dst
    imul r10, r11 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 344], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov rcx, -1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call _bt_fill # call _bt_fill
    mov [rbp - 352], rax # store tag L14
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 128], r10 # spill L14 to slot
    mov r11, [rbp - 128] # reload L14 from spill slot
    mov r10, r11 # assign L15
    mov r11, [rbp - 352] # tag L14 from tag-slot
    mov [rbp - 360], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 240] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr10] # map key (const char*): &.LCstr10
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 368], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 376], rax # store tag L17
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 376] # tag L17 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 376], rax # store tag L17
    mov rcx, [rbp - 144] # reload L16 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 368] # tag L16 from tag-slot
    mov rdi, [rbp - 376] # tag L17 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 376], rax # store tag L17
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 376] # tag L17 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 376], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 376] # tag L17 from tag-slot
    mov [rbp - 384], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 240] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr8] # map key (const char*): &.LCstr8
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 392], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 400], rax # store tag L20
    mov rcx, [rbp - 168] # reload L19 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 392] # tag L19 from tag-slot
    mov rdi, [rbp - 400] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L20
    mov rcx, [rbp - 96] # reload L10 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L10 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 400] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 296] # tag L7 from tag-slot
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, 0 # hv arg payload
    mov r10, 0 # tag default = TAG_INT
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r12 # hv arg payload
    mov r10, [rbp - 248] # tag L1 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 176] # reload L20 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 400] # tag L20 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 240] # tag L0 from tag-slot
    mov rcx, [rbp - 136] # reload L15 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 360] # tag L15 from tag-slot
    mov r9, [rbp - 160] # reload L18 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 384] # tag L18 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 408], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L22
    mov r11, 0 # tag L21 = TAG_INT (i64-local, fused)
    mov [rbp - 416], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 384] # tag L18 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 424], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    mov rsi, [rbp - 200] # reload L23 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 424] # tag L23 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 432], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_match_full_bb4 # jump-if-zero -> else
    jmp .Lb2dd_bt_match_full_bb3 # jump -> then
.Lb2dd_bt_match_full_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 240] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 448], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov rsi, [rbp - 224] # reload L26 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 448] # tag L26 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 1 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 448], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 416 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_match_full_bb4:
    mov r10, [rbp - 192] # reload L22 from spill slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov rsi, [rbp - 192] # reload L22 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L22 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 72] # reload L7 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 296] # tag L7 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 456], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov rdx, [rbp - 232] # reload L27 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 456] # tag L27 from tag-slot
    add rsp, 416 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden bt_search
    .p2align 4
bt_search:
    .loc 1 792 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 496 # prologue: alloc spill frame
    mov [rbp - 280], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 288], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_bt_search_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 296], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 296] # tag L2 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r13, rdx # index_set: store-back container payload
    mov [rbp - 296], rax # store tag L2
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r14, rdx # field: capture result payload
    mov [rbp - 304], rax # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 304] # tag L3 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r15, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 312], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd_bt_search_bb2 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_bb1 # jump -> then
.Lb2dd_bt_search_bb1:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 328], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rdx, [rbp - 64] # reload L6 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 328] # tag L6 from tag-slot
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 288] # tag L1 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 336], rax # store tag L7
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L8
    mov r11, [rbp - 336] # tag L7 from tag-slot
    mov [rbp - 344], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr10] # map key (const char*): &.LCstr10
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 352], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 360], rax # store tag L10
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 360] # tag L10 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 360], rax # store tag L10
    mov rcx, [rbp - 88] # reload L9 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 352] # tag L9 from tag-slot
    mov rdi, [rbp - 360] # tag L10 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 360], rax # store tag L10
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 360] # tag L10 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 360], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 360] # tag L10 from tag-slot
    mov [rbp - 368], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, 0 # assign L12
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 376], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd_bt_search_bb3 # branch
.Lb2dd_bt_search_bb3:
    mov r10, [rbp - 112] # reload L12 from spill slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L12 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 344] # tag L8 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 384], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_bb5 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_bb4 # jump -> then
.Lb2dd_bt_search_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr9] # map key (const char*): &.LCstr9
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 392], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 392] # tag L14 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 400], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, 2 # binop lhs into dst
    imul r10, r11 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 408], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L16 = TAG_INT (i64-local, fused)
    mov rcx, -1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call _bt_fill # call _bt_fill
    mov [rbp - 416], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 416] # tag L17 from tag-slot
    mov [rbp - 424], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr8] # map key (const char*): &.LCstr8
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 432], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 440], rax # store tag L20
    mov rcx, [rbp - 168] # reload L19 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 432] # tag L19 from tag-slot
    mov rdi, [rbp - 440] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 440], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 344] # tag L8 from tag-slot
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L12 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r12 # hv arg payload
    mov r10, [rbp - 288] # tag L1 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 176] # reload L20 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 440] # tag L20 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    mov rcx, [rbp - 160] # reload L18 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 424] # tag L18 from tag-slot
    mov r9, [rbp - 104] # reload L11 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 368] # tag L11 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 448], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L22
    mov r11, 0 # tag L21 = TAG_INT (i64-local, fused)
    mov [rbp - 456], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 368] # tag L11 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 464], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    mov rsi, [rbp - 200] # reload L23 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 464] # tag L23 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 472], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_bb7 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_bb6 # jump -> then
.Lb2dd_bt_search_bb5:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 536], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov rdx, [rbp - 272] # reload L32 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 536] # tag L32 from tag-slot
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_bb6:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 488], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov rsi, [rbp - 224] # reload L26 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 488] # tag L26 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 1 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 488], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 496], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov rdx, [rbp - 232] # reload L27 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 496] # tag L27 from tag-slot
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_bb7:
    mov r10, [rbp - 192] # reload L22 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 504], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_bb9 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_bb8 # jump -> then
.Lb2dd_bt_search_bb8:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 520], rax # store tag L30
    mov rcx, [rbp - 112] # reload L12 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L12 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 520] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 520], rax # store tag L30
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L22 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 520] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 520], rax # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov rdx, [rbp - 256] # reload L30 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 520] # tag L30 from tag-slot
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_bb9:
    mov r10, [rbp - 112] # reload L12 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 528], r11 # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov r11, [rbp - 264] # reload L31 from spill slot
    mov r10, r11 # assign L12
    mov r11, 0 # tag L31 = TAG_INT (i64-local, fused)
    mov [rbp - 376], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd_bt_search_bb3 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden bt_search_captures
    .p2align 4
bt_search_captures:
    .loc 1 816 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 480 # prologue: alloc spill frame
    mov [rbp - 272], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 280], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_bt_search_captures_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 272] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 288], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 288] # tag L2 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r13, rdx # index_set: store-back container payload
    mov [rbp - 288], rax # store tag L2
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 272] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r14, rdx # field: capture result payload
    mov [rbp - 296], rax # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 296] # tag L3 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r15, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 304], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd_bt_search_captures_bb2 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_captures_bb1 # jump -> then
.Lb2dd_bt_search_captures_bb1:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 320], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rdx, [rbp - 64] # reload L6 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 320] # tag L6 from tag-slot
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_captures_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 280] # tag L1 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 328], rax # store tag L7
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L8
    mov r11, [rbp - 328] # tag L7 from tag-slot
    mov [rbp - 336], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 272] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr10] # map key (const char*): &.LCstr10
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 344], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 352], rax # store tag L10
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 352] # tag L10 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 352], rax # store tag L10
    mov rcx, [rbp - 88] # reload L9 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 344] # tag L9 from tag-slot
    mov rdi, [rbp - 352] # tag L10 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 352], rax # store tag L10
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 352] # tag L10 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 352], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 352] # tag L10 from tag-slot
    mov [rbp - 360], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, 0 # assign L12
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 368], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd_bt_search_captures_bb3 # branch
.Lb2dd_bt_search_captures_bb3:
    mov r10, [rbp - 112] # reload L12 from spill slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L12 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 336] # tag L8 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 376], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_captures_bb5 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_captures_bb4 # jump -> then
.Lb2dd_bt_search_captures_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 272] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr9] # map key (const char*): &.LCstr9
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 384], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 384] # tag L14 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 392], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, 2 # binop lhs into dst
    imul r10, r11 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 400], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L16 = TAG_INT (i64-local, fused)
    mov rcx, -1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call _bt_fill # call _bt_fill
    mov [rbp - 408], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 408] # tag L17 from tag-slot
    mov [rbp - 416], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 272] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr8] # map key (const char*): &.LCstr8
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 424], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 432], rax # store tag L20
    mov rcx, [rbp - 168] # reload L19 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 424] # tag L19 from tag-slot
    mov rdi, [rbp - 432] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 432], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 336] # tag L8 from tag-slot
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L12 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r12 # hv arg payload
    mov r10, [rbp - 280] # tag L1 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 176] # reload L20 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 432] # tag L20 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 272] # tag L0 from tag-slot
    mov rcx, [rbp - 160] # reload L18 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 416] # tag L18 from tag-slot
    mov r9, [rbp - 104] # reload L11 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 360] # tag L11 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 440], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L22
    mov r11, 0 # tag L21 = TAG_INT (i64-local, fused)
    mov [rbp - 448], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 360] # tag L11 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 456], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    mov rsi, [rbp - 200] # reload L23 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 456] # tag L23 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 464], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_captures_bb7 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_captures_bb6 # jump -> then
.Lb2dd_bt_search_captures_bb5:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 520], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov rdx, [rbp - 264] # reload L31 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 520] # tag L31 from tag-slot
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_captures_bb6:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 272] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 480], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov rsi, [rbp - 224] # reload L26 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 480] # tag L26 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 1 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 480], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 488], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov rdx, [rbp - 232] # reload L27 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 488] # tag L27 from tag-slot
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_captures_bb7:
    mov r10, [rbp - 192] # reload L22 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 496], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_captures_bb9 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_captures_bb8 # jump -> then
.Lb2dd_bt_search_captures_bb8:
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 416] # tag L18 from tag-slot
    mov rdx, 0 # hv payload
    mov r8, [rbp - 112] # reload L12 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, 0 # tag L12 = TAG_INT (i64-local, fused)
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r10, rdx # index_set: store-back poly descriptor
    mov [rbp - 416], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 416] # tag L18 from tag-slot
    mov rdx, 1 # hv payload
    mov r8, [rbp - 192] # reload L22 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, 0 # tag L22 = TAG_INT (i64-local, fused)
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r10, rdx # index_set: store-back poly descriptor
    mov [rbp - 416], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rdx, [rbp - 160] # reload L18 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 416] # tag L18 from tag-slot
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_captures_bb9:
    mov r10, [rbp - 112] # reload L12 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 512], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov r11, [rbp - 256] # reload L30 from spill slot
    mov r10, r11 # assign L12
    mov r11, 0 # tag L30 = TAG_INT (i64-local, fused)
    mov [rbp - 368], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd_bt_search_captures_bb3 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden bt_find_all
    .p2align 4
bt_find_all:
    .loc 1 838 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 544 # prologue: alloc spill frame
    mov [rbp - 304], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 312], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_bt_find_all_bb0:
    call hexa_array_new # array_lit: new array
    mov r13, rdx # array_lit: capture new array payload
    mov [rbp - 320], rax # store tag L2
    mov r14, r13 # assign L3
    mov r11, [rbp - 320] # tag L2 from tag-slot
    mov [rbp - 328], r11 # store tag L3
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 304] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r15, rdx # field: capture result payload
    mov [rbp - 336], rax # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 336] # tag L4 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r15, rdx # index_set: store-back container payload
    mov [rbp - 336], rax # store tag L4
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 304] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 344], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 344] # tag L5 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 352], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_find_all_bb2 # jump-if-zero -> else
    jmp .Lb2dd_bt_find_all_bb1 # jump -> then
.Lb2dd_bt_find_all_bb1:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 328] # tag L3 from tag-slot
    add rsp, 544 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_find_all_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 312] # tag L1 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 368], rax # store tag L8
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L9
    mov r11, [rbp - 368] # tag L8 from tag-slot
    mov [rbp - 376], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 304] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr10] # map key (const char*): &.LCstr10
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 384], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 392], rax # store tag L11
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 392] # tag L11 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 392], rax # store tag L11
    mov rcx, [rbp - 96] # reload L10 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 384] # tag L10 from tag-slot
    mov rdi, [rbp - 392] # tag L11 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 392], rax # store tag L11
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 392] # tag L11 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 392], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 392] # tag L11 from tag-slot
    mov [rbp - 400], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, 0 # assign L13
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 408], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_bt_find_all_bb3 # branch
.Lb2dd_bt_find_all_bb3:
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 88] # reload L9 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 376] # tag L9 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 416], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_find_all_bb5 # jump-if-zero -> else
    jmp .Lb2dd_bt_find_all_bb4 # jump -> then
.Lb2dd_bt_find_all_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 304] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr9] # map key (const char*): &.LCstr9
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 424], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 424] # tag L15 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 432], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov r10, 2 # binop lhs into dst
    imul r10, r11 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 440], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L17 = TAG_INT (i64-local, fused)
    mov rcx, -1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call _bt_fill # call _bt_fill
    mov [rbp - 448], rax # store tag L18
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 160], r10 # spill L18 to slot
    mov r11, [rbp - 160] # reload L18 from spill slot
    mov r10, r11 # assign L19
    mov r11, [rbp - 448] # tag L18 from tag-slot
    mov [rbp - 456], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 304] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr8] # map key (const char*): &.LCstr8
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 464], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 472], rax # store tag L21
    mov rcx, [rbp - 176] # reload L20 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 464] # tag L20 from tag-slot
    mov rdi, [rbp - 472] # tag L21 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 472], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 376] # tag L9 from tag-slot
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r12 # hv arg payload
    mov r10, [rbp - 312] # tag L1 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 472] # tag L21 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 304] # tag L0 from tag-slot
    mov rcx, [rbp - 168] # reload L19 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 456] # tag L19 from tag-slot
    mov r9, [rbp - 112] # reload L12 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 400] # tag L12 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 480], rax # store tag L22
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 192], r10 # spill L22 to slot
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r10, r11 # assign L23
    mov r11, 0 # tag L22 = TAG_INT (i64-local, fused)
    mov [rbp - 488], r11 # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 400] # tag L12 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 496], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 208] # reload L24 from spill slot
    mov rsi, [rbp - 208] # reload L24 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 496] # tag L24 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 504], rax # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r10, [rbp - 216] # reload L25 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_find_all_bb7 # jump-if-zero -> else
    jmp .Lb2dd_bt_find_all_bb6 # jump -> then
.Lb2dd_bt_find_all_bb5:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 328] # tag L3 from tag-slot
    add rsp, 544 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_find_all_bb6:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 304] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 520], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 520] # tag L27 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 1 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 520], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 328] # tag L3 from tag-slot
    add rsp, 544 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_find_all_bb7:
    mov r10, [rbp - 200] # reload L23 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 528], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_find_all_bb9 # jump-if-zero -> else
    jmp .Lb2dd_bt_find_all_bb8 # jump -> then
.Lb2dd_bt_find_all_bb8:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 544], rax # store tag L30
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 544] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 544], rax # store tag L30
    mov rcx, [rbp - 200] # reload L23 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L23 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 544] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 544], rax # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 328] # tag L3 from tag-slot
    mov rcx, [rbp - 256] # reload L30 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 544] # tag L30 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 552], rax # store tag L31
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 264], r10 # spill L31 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    cmp r10, r11 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 560], r11 # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, [rbp - 272] # reload L32 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_find_all_bb11 # jump-if-zero -> else
    jmp .Lb2dd_bt_find_all_bb10 # jump -> then
.Lb2dd_bt_find_all_bb9:
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 584], r11 # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov r11, [rbp - 296] # reload L35 from spill slot
    mov r10, r11 # assign L13
    mov r11, 0 # tag L35 = TAG_INT (i64-local, fused)
    mov [rbp - 408], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_bt_find_all_bb13 # branch
.Lb2dd_bt_find_all_bb10:
    mov r11, [rbp - 200] # reload L23 from spill slot
    mov r10, r11 # assign L13
    mov r11, 0 # tag L23 = TAG_INT (i64-local, fused)
    mov [rbp - 408], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_bt_find_all_bb12 # branch
.Lb2dd_bt_find_all_bb11:
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 576], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov r11, [rbp - 288] # reload L34 from spill slot
    mov r10, r11 # assign L13
    mov r11, 0 # tag L34 = TAG_INT (i64-local, fused)
    mov [rbp - 408], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_bt_find_all_bb12 # branch
.Lb2dd_bt_find_all_bb12:
    jmp .Lb2dd_bt_find_all_bb13 # branch
.Lb2dd_bt_find_all_bb13:
    jmp .Lb2dd_bt_find_all_bb3 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 544 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden bt_search_from
    .p2align 4
bt_search_from:
    .loc 1 867 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 512 # prologue: alloc spill frame
    mov [rbp - 288], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 296], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 304], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd_bt_search_from_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 288] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r14, rdx # field: capture result payload
    mov [rbp - 312], rax # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 312] # tag L3 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r14, rdx # index_set: store-back container payload
    mov [rbp - 312], rax # store tag L3
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 288] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r15, rdx # field: capture result payload
    mov [rbp - 320], rax # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 320] # tag L4 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 328], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_from_bb2 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_from_bb1 # jump -> then
.Lb2dd_bt_search_from_bb1:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 344], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov rdx, [rbp - 72] # reload L7 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 344] # tag L7 from tag-slot
    add rsp, 512 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_from_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 296] # tag L1 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 352], rax # store tag L8
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L9
    mov r11, [rbp - 352] # tag L8 from tag-slot
    mov [rbp - 360], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 288] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr10] # map key (const char*): &.LCstr10
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 368], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 376], rax # store tag L11
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 376] # tag L11 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 376], rax # store tag L11
    mov rcx, [rbp - 96] # reload L10 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 368] # tag L10 from tag-slot
    mov rdi, [rbp - 376] # tag L11 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 376], rax # store tag L11
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 376] # tag L11 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 376], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 376] # tag L11 from tag-slot
    mov [rbp - 384], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, r13 # assign L13
    mov r11, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov [rbp - 392], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_bt_search_from_bb3 # branch
.Lb2dd_bt_search_from_bb3:
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 88] # reload L9 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 360] # tag L9 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 400], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_from_bb5 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_from_bb4 # jump -> then
.Lb2dd_bt_search_from_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 288] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr9] # map key (const char*): &.LCstr9
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 408], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 408] # tag L15 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 416], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov r10, 2 # binop lhs into dst
    imul r10, r11 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 424], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L17 = TAG_INT (i64-local, fused)
    mov rcx, -1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call _bt_fill # call _bt_fill
    mov [rbp - 432], rax # store tag L18
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 160], r10 # spill L18 to slot
    mov r11, [rbp - 160] # reload L18 from spill slot
    mov r10, r11 # assign L19
    mov r11, [rbp - 432] # tag L18 from tag-slot
    mov [rbp - 440], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 288] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr8] # map key (const char*): &.LCstr8
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 448], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 456], rax # store tag L21
    mov rcx, [rbp - 176] # reload L20 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 448] # tag L20 from tag-slot
    mov rdi, [rbp - 456] # tag L21 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 456], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    sub rsp, 64 # hv: reserve 4 16B stack arg slots
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 360] # tag L9 from tag-slot
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r12 # hv arg payload
    mov r10, [rbp - 296] # tag L1 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 456] # tag L21 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 288] # tag L0 from tag-slot
    mov rcx, [rbp - 168] # reload L19 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 440] # tag L19 from tag-slot
    mov r9, [rbp - 112] # reload L12 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 384] # tag L12 from tag-slot
    call _bt_run # call _bt_run
    add rsp, 64 # hv: pop 4 stack arg slots
    mov [rbp - 464], rax # store tag L22
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 192], r10 # spill L22 to slot
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r10, r11 # assign L23
    mov r11, 0 # tag L22 = TAG_INT (i64-local, fused)
    mov [rbp - 472], r11 # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 384] # tag L12 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 480], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 208] # reload L24 from spill slot
    mov rsi, [rbp - 208] # reload L24 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 480] # tag L24 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 488], rax # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r10, [rbp - 216] # reload L25 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_from_bb7 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_from_bb6 # jump -> then
.Lb2dd_bt_search_from_bb5:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 552], rax # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov rdx, [rbp - 280] # reload L33 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 552] # tag L33 from tag-slot
    add rsp, 512 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_from_bb6:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 288] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 504], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L27 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 1 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 504], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 512], rax # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov rdx, [rbp - 240] # reload L28 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 512] # tag L28 from tag-slot
    add rsp, 512 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_from_bb7:
    mov r10, [rbp - 200] # reload L23 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 520], r11 # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r10, [rbp - 248] # reload L29 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_bt_search_from_bb9 # jump-if-zero -> else
    jmp .Lb2dd_bt_search_from_bb8 # jump -> then
.Lb2dd_bt_search_from_bb8:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 536], rax # store tag L31
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 536] # tag L31 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 536], rax # store tag L31
    mov rcx, [rbp - 200] # reload L23 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L23 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 536] # tag L31 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 536], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov rdx, [rbp - 264] # reload L31 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 536] # tag L31 from tag-slot
    add rsp, 512 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_bt_search_from_bb9:
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 544], r11 # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r11, [rbp - 272] # reload L32 from spill slot
    mov r10, r11 # assign L13
    mov r11, 0 # tag L32 = TAG_INT (i64-local, fused)
    mov [rbp - 392], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_bt_search_from_bb3 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 512 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _REGEX_DEFAULT_STEP_CAP
    .p2align 4
_REGEX_DEFAULT_STEP_CAP:
    .loc 1 97 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
.Lb2dd__REGEX_DEFAULT_STEP_CAP_bb0:
    mov rdx, 1000000 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_peek
    .p2align 4
_re_peek:
    .loc 1 136 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 128 # prologue: alloc spill frame
    mov [rbp - 96], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_peek_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r12, rdx # field: capture result payload
    mov [rbp - 104], rax # store tag L1
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 104] # tag L1 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r13, rdx # index: capture element payload
    mov [rbp - 112], rax # store tag L2
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    call hexa_map_get # field: hexa_map_get
    mov r14, rdx # field: capture result payload
    mov [rbp - 120], rax # store tag L3
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 112] # tag L2 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, [rbp - 120] # tag L3 from tag-slot
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r15, rdx # binop >=: capture bool payload
    mov [rbp - 128], rax # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd__re_peek_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_peek_bb1 # jump -> then
.Lb2dd__re_peek_bb1:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 128 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_peek_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr2] # map key (const char*): &.LCstr2
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 144], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 152], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 152] # tag L7 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 160], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 144] # tag L6 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 160] # tag L8 from tag-slot
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 168], rax # store tag L9
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 88], r10 # spill L9 to slot
    mov rdx, [rbp - 88] # reload L9 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 168] # tag L9 from tag-slot
    add rsp, 128 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_adv
    .p2align 4
_re_adv:
    .loc 1 141 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 48 # prologue: alloc spill frame
    mov [rbp - 56], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_adv_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 56] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r12, rdx # field: capture result payload
    mov [rbp - 64], rax # store tag L1
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 56] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 72], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 72] # tag L2 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r14, rdx # index: capture element payload
    mov [rbp - 80], rax # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 80] # tag L3 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r15, rdx # binop +: capture result payload
    mov [rbp - 88], rax # store tag L4
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 64] # tag L1 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, r15 # hv arg payload
    mov r8, 0 # tag L4 = TAG_INT (i64-local, fused)
    call hexa_index_set # index_set: hexa_index_set
    mov r12, rdx # index_set: store-back container payload
    mov [rbp - 64], rax # store tag L1
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 48 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_emit
    .p2align 4
_re_emit:
    .loc 1 146 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 80 # prologue: alloc spill frame
    mov [rbp - 72], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 80], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__re_emit_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 72] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 88], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 88] # tag L2 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 80] # tag L1 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 96], rax # store tag L3
    mov r14, rdx # hv: unbox call result (rdx)
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 72] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r15, rdx # field: capture result payload
    mov [rbp - 104], rax # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 104] # tag L4 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 112], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 112] # tag L5 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 120], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rdx, [rbp - 64] # reload L6 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L6 = TAG_INT (i64-local, fused)
    add rsp, 80 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_class_for_escape
    .p2align 4
_re_class_for_escape:
    .loc 1 153 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 288 # prologue: alloc spill frame
    mov [rbp - 176], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_class_for_escape_bb0:
    cmp rbx, 100 # binop ==
    sete al # binop == → al
    movzx r12, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 184], r11 # store tag L1
    test r12, r12 # br_cond test
    jz .Lb2dd__re_class_for_escape_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_class_for_escape_bb1 # jump -> then
.Lb2dd__re_class_for_escape_bb1:
    call hexa_array_new # array_lit: new array
    mov r14, rdx # array_lit: capture new array payload
    mov [rbp - 200], rax # store tag L3
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 200] # tag L3 from tag-slot
    mov rsi, r14 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r14, rdx # array_lit: recapture array payload
    mov [rbp - 200], rax # store tag L3
    mov rcx, 48 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 200] # tag L3 from tag-slot
    mov rsi, r14 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r14, rdx # array_lit: recapture array payload
    mov [rbp - 200], rax # store tag L3
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 200] # tag L3 from tag-slot
    mov rsi, r14 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r14, rdx # array_lit: recapture array payload
    mov [rbp - 200], rax # store tag L3
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 200] # tag L3 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_class_for_escape_bb2:
    cmp rbx, 68 # binop ==
    sete al # binop == → al
    movzx r15, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 208], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd__re_class_for_escape_bb4 # jump-if-zero -> else
    jmp .Lb2dd__re_class_for_escape_bb3 # jump -> then
.Lb2dd__re_class_for_escape_bb3:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 224], rax # store tag L6
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 224] # tag L6 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 224], rax # store tag L6
    mov rcx, 48 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 224] # tag L6 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 224], rax # store tag L6
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 224] # tag L6 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 224], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rdx, [rbp - 64] # reload L6 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 224] # tag L6 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_class_for_escape_bb4:
    cmp rbx, 119 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 232], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_class_for_escape_bb6 # jump-if-zero -> else
    jmp .Lb2dd__re_class_for_escape_bb5 # jump -> then
.Lb2dd__re_class_for_escape_bb5:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 48 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 65 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 90 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 4
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 97 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 5
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 122 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 6
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 95 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 7
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov rcx, 95 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 8
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 248], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rdx, [rbp - 88] # reload L9 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 248] # tag L9 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_class_for_escape_bb6:
    cmp rbx, 87 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 256], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_class_for_escape_bb8 # jump-if-zero -> else
    jmp .Lb2dd__re_class_for_escape_bb7 # jump -> then
.Lb2dd__re_class_for_escape_bb7:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 48 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 65 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 90 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 4
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 97 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 5
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 122 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 6
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 95 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 7
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov rcx, 95 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 8
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 272], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rdx, [rbp - 112] # reload L12 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 272] # tag L12 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_class_for_escape_bb8:
    cmp rbx, 115 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 280], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_class_for_escape_bb10 # jump-if-zero -> else
    jmp .Lb2dd__re_class_for_escape_bb9 # jump -> then
.Lb2dd__re_class_for_escape_bb9:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 13 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 32 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov rcx, 32 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 296] # tag L15 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 4
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 296], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rdx, [rbp - 136] # reload L15 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 296] # tag L15 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_class_for_escape_bb10:
    cmp rbx, 83 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 304], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_class_for_escape_bb12 # jump-if-zero -> else
    jmp .Lb2dd__re_class_for_escape_bb11 # jump -> then
.Lb2dd__re_class_for_escape_bb11:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 13 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 32 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 32 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 4
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rdx, [rbp - 160] # reload L18 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 320] # tag L18 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_class_for_escape_bb12:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 328], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov rdx, [rbp - 168] # reload L19 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 328] # tag L19 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_lit_for_escape
    .p2align 4
_re_lit_for_escape:
    .loc 1 165 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 112 # prologue: alloc spill frame
    mov [rbp - 88], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_lit_for_escape_bb0:
    cmp rbx, 110 # binop ==
    sete al # binop == → al
    movzx r12, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 96], r11 # store tag L1
    test r12, r12 # br_cond test
    jz .Lb2dd__re_lit_for_escape_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_lit_for_escape_bb1 # jump -> then
.Lb2dd__re_lit_for_escape_bb1:
    mov rdx, 10 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_lit_for_escape_bb2:
    cmp rbx, 116 # binop ==
    sete al # binop == → al
    movzx r14, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 112], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd__re_lit_for_escape_bb4 # jump-if-zero -> else
    jmp .Lb2dd__re_lit_for_escape_bb3 # jump -> then
.Lb2dd__re_lit_for_escape_bb3:
    mov rdx, 9 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_lit_for_escape_bb4:
    cmp rbx, 114 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 128], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_lit_for_escape_bb6 # jump-if-zero -> else
    jmp .Lb2dd__re_lit_for_escape_bb5 # jump -> then
.Lb2dd__re_lit_for_escape_bb5:
    mov rdx, 13 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_lit_for_escape_bb6:
    cmp rbx, 48 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 144], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_lit_for_escape_bb8 # jump-if-zero -> else
    jmp .Lb2dd__re_lit_for_escape_bb7 # jump -> then
.Lb2dd__re_lit_for_escape_bb7:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_lit_for_escape_bb8:
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_push_class
    .p2align 4
_re_push_class:
    .loc 1 174 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 256 # prologue: alloc spill frame
    mov [rbp - 160], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 168], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 176], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd__re_push_class_bb0:
    call hexa_array_new # array_lit: new array
    mov r14, rdx # array_lit: capture new array payload
    mov [rbp - 184], rax # store tag L3
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 184] # tag L3 from tag-slot
    mov rsi, r14 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r14, rdx # array_lit: recapture array payload
    mov [rbp - 184], rax # store tag L3
    mov r15, r14 # assign L4
    mov r11, [rbp - 184] # tag L3 from tag-slot
    mov [rbp - 192], r11 # store tag L4
    mov r10, 0 # assign L5
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 200], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd__re_push_class_bb1 # branch
.Lb2dd__re_push_class_bb1:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 168] # tag L1 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 208], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L5 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 64] # reload L6 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 208] # tag L6 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 216], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_push_class_bb3 # jump-if-zero -> else
    jmp .Lb2dd__re_push_class_bb2 # jump -> then
.Lb2dd__re_push_class_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 168] # tag L1 from tag-slot
    mov rdx, [rbp - 56] # reload L5 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 224], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 192] # tag L4 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 224] # tag L8 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 232], rax # store tag L9
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 240], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L5
    mov r11, 0 # tag L10 = TAG_INT (i64-local, fused)
    mov [rbp - 200], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd__re_push_class_bb1 # branch
.Lb2dd__re_push_class_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 160] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 248], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 248] # tag L11 from tag-slot
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 192] # tag L4 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 256], rax # store tag L12
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 160] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 264], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 264] # tag L13 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 272], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 272] # tag L14 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 280], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 288], rax # store tag L16
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L16
    mov rcx, [rbp - 136] # reload L15 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L15 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 288] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L16
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L16
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L16 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 160] # tag L0 from tag-slot
    mov rcx, [rbp - 144] # reload L16 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 288] # tag L16 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 296], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov rdx, [rbp - 152] # reload L17 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L17 = TAG_INT (i64-local, fused)
    add rsp, 256 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_parse_class
    .p2align 4
_re_parse_class:
    .loc 1 183 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 1328 # prologue: alloc spill frame
    mov [rbp - 696], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_parse_class_bb0:
    call hexa_array_new # array_lit: new array
    mov r12, rdx # array_lit: capture new array payload
    mov [rbp - 704], rax # store tag L1
    mov r13, r12 # assign L2
    mov r11, [rbp - 704] # tag L1 from tag-slot
    mov [rbp - 712], r11 # store tag L2
    mov r14, 0 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 720], r11 # store tag L3
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 728], rax # store tag L4
    mov r15, rdx # hv: unbox user-call result payload
    cmp r15, 94 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 736], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb1 # jump -> then
.Lb2dd__re_parse_class_bb1:
    mov r14, 1 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 720], r11 # store tag L3
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 752], rax # store tag L7
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 72], r10 # spill L7 to slot
    jmp .Lb2dd__re_parse_class_bb2 # branch
.Lb2dd__re_parse_class_bb2:
    mov r10, 1 # assign L8
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 760], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    jmp .Lb2dd__re_parse_class_bb3 # branch
.Lb2dd__re_parse_class_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 768], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 768] # tag L9 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 776], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 784], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 776] # tag L10 from tag-slot
    mov rcx, [rbp - 104] # reload L11 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 784] # tag L11 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 792], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb5 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb4 # jump -> then
.Lb2dd__re_parse_class_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 800], rax # store tag L13
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov [rbp - 808], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, 93 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 816], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb7 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb6 # jump -> then
.Lb2dd__re_parse_class_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1368], rax # store tag L84
    mov [rbp - 688], r10 # spill L84 to slot
    mov rsi, [rbp - 688] # reload L84 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1368] # tag L84 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1368], rax # store tag L84
    mov [rbp - 688], r10 # spill L84 to slot
    mov rdx, -1 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 1328 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_class_bb6:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 760] # tag L8 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 832], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 832] # tag L17 from tag-slot
    mov [rbp - 824], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__re_parse_class_bb8 # branch
.Lb2dd__re_parse_class_bb7:
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 816] # tag L15 from tag-slot
    mov [rbp - 824], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__re_parse_class_bb8 # branch
.Lb2dd__re_parse_class_bb8:
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb10 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb9 # jump -> then
.Lb2dd__re_parse_class_bb9:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 848], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 712] # tag L2 from tag-slot
    mov r9, r14 # hv arg payload
    mov r8, 0 # tag L3 = TAG_INT (i64-local, fused)
    call _re_push_class # call _re_push_class
    mov [rbp - 856], rax # store tag L20
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 176], r10 # spill L20 to slot
    mov rdx, [rbp - 176] # reload L20 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L20 = TAG_INT (i64-local, fused)
    add rsp, 1328 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_class_bb10:
    mov r10, 0 # assign L8
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 760], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, -1 # assign L21
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 864], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov r10, 0 # assign L22
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 872], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, 92 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 880], r11 # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb12 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb11 # jump -> then
.Lb2dd__re_parse_class_bb11:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 896], rax # store tag L25
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 216], r10 # spill L25 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 904], rax # store tag L26
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 224], r10 # spill L26 to slot
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L27
    mov r11, 0 # tag L26 = TAG_INT (i64-local, fused)
    mov [rbp - 912], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 920], rax # store tag L28
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 240], r10 # spill L28 to slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L27 = TAG_INT (i64-local, fused)
    call _re_class_for_escape # call _re_class_for_escape
    mov [rbp - 928], rax # store tag L29
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 248], r10 # spill L29 to slot
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L30
    mov r11, [rbp - 928] # tag L29 from tag-slot
    mov [rbp - 936], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov rsi, [rbp - 256] # reload L30 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 936] # tag L30 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 944], r11 # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov r10, [rbp - 264] # reload L31 from spill slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 944] # tag L31 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_gt # binop >: tag-dispatch hexa_cmp_gt
    mov r10, rdx # binop >: capture bool payload
    mov [rbp - 952], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, [rbp - 272] # reload L32 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb14 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb13 # jump -> then
.Lb2dd__re_parse_class_bb12:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 1080], rax # store tag L48
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 400], r10 # spill L48 to slot
    mov r11, [rbp - 128] # reload L14 from spill slot
    mov r10, r11 # assign L21
    mov r11, 0 # tag L14 = TAG_INT (i64-local, fused)
    mov [rbp - 864], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    jmp .Lb2dd__re_parse_class_bb22 # branch
.Lb2dd__re_parse_class_bb13:
    mov r10, 1 # assign L34
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 968], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    jmp .Lb2dd__re_parse_class_bb15 # branch
.Lb2dd__re_parse_class_bb14:
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L27 = TAG_INT (i64-local, fused)
    call _re_lit_for_escape # call _re_lit_for_escape
    mov [rbp - 1048], rax # store tag L44
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 368], r10 # spill L44 to slot
    mov r11, [rbp - 368] # reload L44 from spill slot
    mov r10, r11 # assign L45
    mov r11, 0 # tag L44 = TAG_INT (i64-local, fused)
    mov [rbp - 1056], r11 # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov r10, [rbp - 376] # reload L45 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1064], r11 # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    mov r10, [rbp - 384] # reload L46 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb19 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb18 # jump -> then
.Lb2dd__re_parse_class_bb15:
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 976], r11 # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov rsi, [rbp - 256] # reload L30 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 936] # tag L30 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 984], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov r10, [rbp - 296] # reload L35 from spill slot
    mov r11, [rbp - 304] # reload L36 from spill slot
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L35 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 304] # reload L36 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 984] # tag L36 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 992], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb17 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb16 # jump -> then
.Lb2dd__re_parse_class_bb16:
    mov rsi, [rbp - 256] # reload L30 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 936] # tag L30 from tag-slot
    mov rdx, [rbp - 288] # reload L34 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 1000], rax # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 320] # reload L38 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1000] # tag L38 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1008], rax # store tag L39
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 328], r10 # spill L39 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1016], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov rsi, [rbp - 256] # reload L30 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 936] # tag L30 from tag-slot
    mov rdx, [rbp - 336] # reload L40 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 1024], rax # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 344] # reload L41 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1024] # tag L41 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1032], rax # store tag L42
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 352], r10 # spill L42 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1040], r11 # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov r11, [rbp - 360] # reload L43 from spill slot
    mov r10, r11 # assign L34
    mov r11, 0 # tag L43 = TAG_INT (i64-local, fused)
    mov [rbp - 968], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    jmp .Lb2dd__re_parse_class_bb15 # branch
.Lb2dd__re_parse_class_bb17:
    mov r10, 1 # assign L22
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 872], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    jmp .Lb2dd__re_parse_class_bb21 # branch
.Lb2dd__re_parse_class_bb18:
    mov r11, [rbp - 376] # reload L45 from spill slot
    mov r10, r11 # assign L21
    mov r11, 0 # tag L45 = TAG_INT (i64-local, fused)
    mov [rbp - 864], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    jmp .Lb2dd__re_parse_class_bb20 # branch
.Lb2dd__re_parse_class_bb19:
    mov r11, [rbp - 232] # reload L27 from spill slot
    mov r10, r11 # assign L21
    mov r11, 0 # tag L27 = TAG_INT (i64-local, fused)
    mov [rbp - 864], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    jmp .Lb2dd__re_parse_class_bb20 # branch
.Lb2dd__re_parse_class_bb20:
    jmp .Lb2dd__re_parse_class_bb21 # branch
.Lb2dd__re_parse_class_bb21:
    jmp .Lb2dd__re_parse_class_bb22 # branch
.Lb2dd__re_parse_class_bb22:
    mov rsi, [rbp - 192] # reload L22 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 872] # tag L22 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1088], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov r10, [rbp - 408] # reload L49 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb24 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb23 # jump -> then
.Lb2dd__re_parse_class_bb23:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 1104], rax # store tag L51
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 424], r10 # spill L51 to slot
    mov r10, [rbp - 424] # reload L51 from spill slot
    cmp r10, 45 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1112], r11 # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov r10, [rbp - 432] # reload L52 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb26 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb25 # jump -> then
.Lb2dd__re_parse_class_bb24:
    jmp .Lb2dd__re_parse_class_bb3 # branch
.Lb2dd__re_parse_class_bb25:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1128], rax # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov rsi, [rbp - 448] # reload L54 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1128] # tag L54 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1136], rax # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    mov r10, [rbp - 456] # reload L55 from spill slot
    mov rsi, [rbp - 456] # reload L55 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1136] # tag L55 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 1144], rax # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1152], rax # store tag L57
    mov [rbp - 472], r10 # spill L57 to slot
    mov r10, [rbp - 464] # reload L56 from spill slot
    mov r11, [rbp - 472] # reload L57 from spill slot
    mov rsi, [rbp - 464] # reload L56 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L56 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 472] # reload L57 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1152] # tag L57 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 1160], rax # store tag L58
    mov [rbp - 480], r10 # spill L58 to slot
    mov r11, [rbp - 480] # reload L58 from spill slot
    mov r10, r11 # assign L53
    mov r11, [rbp - 1160] # tag L58 from tag-slot
    mov [rbp - 1120], r11 # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    jmp .Lb2dd__re_parse_class_bb27 # branch
.Lb2dd__re_parse_class_bb26:
    mov r11, [rbp - 432] # reload L52 from spill slot
    mov r10, r11 # assign L53
    mov r11, [rbp - 1112] # tag L52 from tag-slot
    mov [rbp - 1120], r11 # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    jmp .Lb2dd__re_parse_class_bb27 # branch
.Lb2dd__re_parse_class_bb27:
    mov r10, [rbp - 440] # reload L53 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb29 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb28 # jump -> then
.Lb2dd__re_parse_class_bb28:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr2] # map key (const char*): &.LCstr2
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1176], rax # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1184], rax # store tag L61
    mov [rbp - 504], r10 # spill L61 to slot
    mov rsi, [rbp - 504] # reload L61 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1184] # tag L61 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1192], rax # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov r10, [rbp - 512] # reload L62 from spill slot
    mov rsi, [rbp - 512] # reload L62 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1192] # tag L62 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 1200], rax # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    mov rsi, [rbp - 496] # reload L60 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L60 from tag-slot
    mov rcx, [rbp - 520] # reload L63 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L63 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 1208], rax # store tag L64
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 528], r10 # spill L64 to slot
    mov r10, [rbp - 528] # reload L64 from spill slot
    mov rsi, [rbp - 528] # reload L64 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1208] # tag L64 from tag-slot
    mov rcx, 93 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1216], r11 # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    mov r11, [rbp - 536] # reload L65 from spill slot
    mov r10, r11 # assign L59
    mov r11, [rbp - 1216] # tag L65 from tag-slot
    mov [rbp - 1168], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    jmp .Lb2dd__re_parse_class_bb30 # branch
.Lb2dd__re_parse_class_bb29:
    mov r11, [rbp - 440] # reload L53 from spill slot
    mov r10, r11 # assign L59
    mov r11, [rbp - 1120] # tag L53 from tag-slot
    mov [rbp - 1168], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    jmp .Lb2dd__re_parse_class_bb30 # branch
.Lb2dd__re_parse_class_bb30:
    mov r10, [rbp - 488] # reload L59 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb32 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb31 # jump -> then
.Lb2dd__re_parse_class_bb31:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 1232], rax # store tag L67
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 552], r10 # spill L67 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 1240], rax # store tag L68
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 560], r10 # spill L68 to slot
    mov r11, [rbp - 560] # reload L68 from spill slot
    mov r10, r11 # assign L69
    mov r11, 0 # tag L68 = TAG_INT (i64-local, fused)
    mov [rbp - 1248], r11 # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 1256], rax # store tag L70
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 576], r10 # spill L70 to slot
    mov r10, [rbp - 568] # reload L69 from spill slot
    cmp r10, 92 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1264], r11 # store tag L71
    mov [rbp - 584], r10 # spill L71 to slot
    mov r10, [rbp - 584] # reload L71 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb34 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb33 # jump -> then
.Lb2dd__re_parse_class_bb32:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 184] # reload L21 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L21 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1352], rax # store tag L82
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 672], r10 # spill L82 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 184] # reload L21 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L21 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1360], rax # store tag L83
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 680], r10 # spill L83 to slot
    mov r11, [rbp - 680] # reload L83 from spill slot
    mov r10, r11 # assign L66
    mov r11, [rbp - 1360] # tag L83 from tag-slot
    mov [rbp - 1224], r11 # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    jmp .Lb2dd__re_parse_class_bb38 # branch
.Lb2dd__re_parse_class_bb33:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 1280], rax # store tag L73
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 600], r10 # spill L73 to slot
    mov r11, [rbp - 600] # reload L73 from spill slot
    mov r10, r11 # assign L74
    mov r11, 0 # tag L73 = TAG_INT (i64-local, fused)
    mov [rbp - 1288], r11 # store tag L74
    mov [rbp - 608], r10 # spill L74 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 696] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 1296], rax # store tag L75
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 616], r10 # spill L75 to slot
    mov rsi, [rbp - 608] # reload L74 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L74 = TAG_INT (i64-local, fused)
    call _re_lit_for_escape # call _re_lit_for_escape
    mov [rbp - 1304], rax # store tag L76
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 624], r10 # spill L76 to slot
    mov r11, [rbp - 624] # reload L76 from spill slot
    mov r10, r11 # assign L77
    mov r11, 0 # tag L76 = TAG_INT (i64-local, fused)
    mov [rbp - 1312], r11 # store tag L77
    mov [rbp - 632], r10 # spill L77 to slot
    mov r10, [rbp - 632] # reload L77 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 1320], r11 # store tag L78
    mov [rbp - 640], r10 # spill L78 to slot
    mov r10, [rbp - 640] # reload L78 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_class_bb36 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_class_bb35 # jump -> then
.Lb2dd__re_parse_class_bb34:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 184] # reload L21 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L21 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1336], rax # store tag L80
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 656], r10 # spill L80 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 712] # tag L2 from tag-slot
    mov rcx, [rbp - 568] # reload L69 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L69 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 1344], rax # store tag L81
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 664], r10 # spill L81 to slot
    mov r11, [rbp - 664] # reload L81 from spill slot
    mov r10, r11 # assign L66
    mov r11, [rbp - 1344] # tag L81 from tag-slot
    mov [rbp - 1224], r11 # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    jmp .Lb2dd__re_parse_class_bb38 # branch
.Lb2dd__re_parse_class_bb35:
    mov r11, [rbp - 632] # reload L77 from spill slot
    mov r10, r11 # assign L69
    mov r11, 0 # tag L77 = TAG_INT (i64-local, fused)
    mov [rbp - 1248], r11 # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    jmp .Lb2dd__re_parse_class_bb37 # branch
.Lb2dd__re_parse_class_bb36:
    mov r11, [rbp - 608] # reload L74 from spill slot
    mov r10, r11 # assign L69
    mov r11, 0 # tag L74 = TAG_INT (i64-local, fused)
    mov [rbp - 1248], r11 # store tag L69
    mov [rbp - 568], r10 # spill L69 to slot
    jmp .Lb2dd__re_parse_class_bb37 # branch
.Lb2dd__re_parse_class_bb37:
    jmp .Lb2dd__re_parse_class_bb34 # branch
.Lb2dd__re_parse_class_bb38:
    jmp .Lb2dd__re_parse_class_bb24 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 1328 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_parse_alt
    .p2align 4
_re_parse_alt:
    .loc 1 246 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 128 # prologue: alloc spill frame
    mov [rbp - 96], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_parse_alt_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    call _re_parse_concat # call _re_parse_concat
    mov [rbp - 104], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov r13, r12 # assign L2
    mov r11, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov [rbp - 112], r11 # store tag L2
    jmp .Lb2dd__re_parse_alt_bb1 # branch
.Lb2dd__re_parse_alt_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 120], rax # store tag L3
    mov r14, rdx # hv: unbox user-call result payload
    cmp r14, 124 # binop ==
    sete al # binop == → al
    movzx r15, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 128], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd__re_parse_alt_bb3 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_alt_bb2 # jump -> then
.Lb2dd__re_parse_alt_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 136], rax # store tag L5
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    call _re_parse_concat # call _re_parse_concat
    mov [rbp - 144], rax # store tag L6
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 64], r10 # spill L6 to slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r10, r11 # assign L7
    mov r11, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rbp - 152], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 160], rax # store tag L8
    mov rcx, 7 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 160] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 160], rax # store tag L8
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 160] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 160], rax # store tag L8
    mov rcx, [rbp - 72] # reload L7 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L7 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 160] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 160], rax # store tag L8
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 160] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 160], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 96] # tag L0 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 160] # tag L8 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 168], rax # store tag L9
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rbp - 112], r11 # store tag L2
    jmp .Lb2dd__re_parse_alt_bb1 # branch
.Lb2dd__re_parse_alt_bb3:
    mov rdx, r13 # hv arg payload
    mov rax, 0 # tag L2 = TAG_INT (i64-local, fused)
    add rsp, 128 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_parse_concat
    .p2align 4
_re_parse_concat:
    .loc 1 257 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 368 # prologue: alloc spill frame
    mov [rbp - 216], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_parse_concat_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 224], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov r13, r12 # assign L2
    mov r11, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov [rbp - 232], r11 # store tag L2
    cmp r13, -1 # binop ==
    sete al # binop == → al
    movzx r14, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 240], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd__re_parse_concat_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_concat_bb1 # jump -> then
.Lb2dd__re_parse_concat_bb1:
    mov r15, r14 # assign L4
    mov r11, [rbp - 240] # tag L3 from tag-slot
    mov [rbp - 248], r11 # store tag L4
    jmp .Lb2dd__re_parse_concat_bb3 # branch
.Lb2dd__re_parse_concat_bb2:
    cmp r13, 124 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 256], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r15, r10 # assign L4
    mov r11, [rbp - 256] # tag L5 from tag-slot
    mov [rbp - 248], r11 # store tag L4
    jmp .Lb2dd__re_parse_concat_bb3 # branch
.Lb2dd__re_parse_concat_bb3:
    test r15, r15 # br_cond test
    jz .Lb2dd__re_parse_concat_bb5 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_concat_bb4 # jump -> then
.Lb2dd__re_parse_concat_bb4:
    mov r10, r15 # assign L6
    mov r11, [rbp - 248] # tag L4 from tag-slot
    mov [rbp - 264], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    jmp .Lb2dd__re_parse_concat_bb6 # branch
.Lb2dd__re_parse_concat_bb5:
    cmp r13, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 272], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 272] # tag L7 from tag-slot
    mov [rbp - 264], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    jmp .Lb2dd__re_parse_concat_bb6 # branch
.Lb2dd__re_parse_concat_bb6:
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_concat_bb8 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_concat_bb7 # jump -> then
.Lb2dd__re_parse_concat_bb7:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 288], rax # store tag L9
    mov rcx, 8 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L9
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L9
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L9
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 288] # tag L9 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 288], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    mov rcx, [rbp - 88] # reload L9 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 288] # tag L9 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 296], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov rdx, [rbp - 96] # reload L10 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L10 = TAG_INT (i64-local, fused)
    add rsp, 368 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_concat_bb8:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    call _re_parse_repeat # call _re_parse_repeat
    mov [rbp - 304], rax # store tag L11
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 104], r10 # spill L11 to slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, 0 # tag L11 = TAG_INT (i64-local, fused)
    mov [rbp - 312], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__re_parse_concat_bb9 # branch
.Lb2dd__re_parse_concat_bb9:
    mov r10, 1 # materialize test cond to reg
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_concat_bb11 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_concat_bb10 # jump -> then
.Lb2dd__re_parse_concat_bb10:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 320], rax # store tag L13
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov [rbp - 328], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, -1 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 336], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_concat_bb13 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_concat_bb12 # jump -> then
.Lb2dd__re_parse_concat_bb11:
    mov rdx, [rbp - 112] # reload L12 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L12 = TAG_INT (i64-local, fused)
    add rsp, 368 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_concat_bb12:
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 336] # tag L15 from tag-slot
    mov [rbp - 344], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__re_parse_concat_bb14 # branch
.Lb2dd__re_parse_concat_bb13:
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, 124 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 352], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 352] # tag L17 from tag-slot
    mov [rbp - 344], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__re_parse_concat_bb14 # branch
.Lb2dd__re_parse_concat_bb14:
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_concat_bb16 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_concat_bb15 # jump -> then
.Lb2dd__re_parse_concat_bb15:
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 344] # tag L16 from tag-slot
    mov [rbp - 360], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__re_parse_concat_bb17 # branch
.Lb2dd__re_parse_concat_bb16:
    mov r10, [rbp - 128] # reload L14 from spill slot
    cmp r10, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 368], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 368] # tag L19 from tag-slot
    mov [rbp - 360], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__re_parse_concat_bb17 # branch
.Lb2dd__re_parse_concat_bb17:
    mov r10, [rbp - 160] # reload L18 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_concat_bb19 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_concat_bb18 # jump -> then
.Lb2dd__re_parse_concat_bb18:
    jmp .Lb2dd__re_parse_concat_bb11 # branch
.Lb2dd__re_parse_concat_bb19:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    call _re_parse_repeat # call _re_parse_repeat
    mov [rbp - 384], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L22
    mov r11, 0 # tag L21 = TAG_INT (i64-local, fused)
    mov [rbp - 392], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 400], rax # store tag L23
    mov rcx, 6 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 400] # tag L23 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L23
    mov rcx, [rbp - 112] # reload L12 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L12 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 400] # tag L23 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L23
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L22 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 400] # tag L23 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L23
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 400] # tag L23 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 400], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 216] # tag L0 from tag-slot
    mov rcx, [rbp - 200] # reload L23 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 400] # tag L23 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 408], rax # store tag L24
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 208], r10 # spill L24 to slot
    mov r11, [rbp - 208] # reload L24 from spill slot
    mov r10, r11 # assign L12
    mov r11, 0 # tag L24 = TAG_INT (i64-local, fused)
    mov [rbp - 312], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__re_parse_concat_bb9 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 368 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_parse_repeat
    .p2align 4
_re_parse_repeat:
    .loc 1 273 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 288 # prologue: alloc spill frame
    mov [rbp - 176], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_parse_repeat_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    call _re_parse_atom # call _re_parse_atom
    mov [rbp - 184], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov r13, r12 # assign L2
    mov r11, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov [rbp - 192], r11 # store tag L2
    jmp .Lb2dd__re_parse_repeat_bb1 # branch
.Lb2dd__re_parse_repeat_bb1:
    mov r10, 1 # materialize test cond to reg
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_repeat_bb3 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_repeat_bb2 # jump -> then
.Lb2dd__re_parse_repeat_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 200], rax # store tag L3
    mov r14, rdx # hv: unbox user-call result payload
    mov r15, r14 # assign L4
    mov r11, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov [rbp - 208], r11 # store tag L4
    cmp r15, 42 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 216], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_repeat_bb5 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_repeat_bb4 # jump -> then
.Lb2dd__re_parse_repeat_bb3:
    mov rdx, r13 # hv arg payload
    mov rax, 0 # tag L2 = TAG_INT (i64-local, fused)
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_repeat_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 232], rax # store tag L7
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 72], r10 # spill L7 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 240], rax # store tag L8
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 240] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 240], rax # store tag L8
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 240] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 240], rax # store tag L8
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 240] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 240], rax # store tag L8
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 240] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 240], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 240] # tag L8 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 248], rax # store tag L9
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rbp - 192], r11 # store tag L2
    jmp .Lb2dd__re_parse_repeat_bb12 # branch
.Lb2dd__re_parse_repeat_bb5:
    cmp r15, 43 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 256], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_repeat_bb7 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_repeat_bb6 # jump -> then
.Lb2dd__re_parse_repeat_bb6:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 272], rax # store tag L12
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 112], r10 # spill L12 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 280], rax # store tag L13
    mov rcx, 4 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 280] # tag L13 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 280], rax # store tag L13
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 280] # tag L13 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 280], rax # store tag L13
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 280] # tag L13 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 280], rax # store tag L13
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 280] # tag L13 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 280], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 280] # tag L13 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 288], rax # store tag L14
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L14 = TAG_INT (i64-local, fused)
    mov [rbp - 192], r11 # store tag L2
    jmp .Lb2dd__re_parse_repeat_bb11 # branch
.Lb2dd__re_parse_repeat_bb7:
    cmp r15, 63 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 296], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_repeat_bb9 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_repeat_bb8 # jump -> then
.Lb2dd__re_parse_repeat_bb8:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 312], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 5 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    mov rcx, [rbp - 160] # reload L18 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 320] # tag L18 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 328], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, [rbp - 168] # reload L19 from spill slot
    mov r13, r10 # assign L2
    mov r11, 0 # tag L19 = TAG_INT (i64-local, fused)
    mov [rbp - 192], r11 # store tag L2
    jmp .Lb2dd__re_parse_repeat_bb10 # branch
.Lb2dd__re_parse_repeat_bb9:
    jmp .Lb2dd__re_parse_repeat_bb3 # branch
.Lb2dd__re_parse_repeat_bb10:
    jmp .Lb2dd__re_parse_repeat_bb11 # branch
.Lb2dd__re_parse_repeat_bb11:
    jmp .Lb2dd__re_parse_repeat_bb12 # branch
.Lb2dd__re_parse_repeat_bb12:
    jmp .Lb2dd__re_parse_repeat_bb1 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_parse_atom
    .p2align 4
_re_parse_atom:
    .loc 1 286 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 944 # prologue: alloc spill frame
    mov [rbp - 504], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_parse_atom_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 512], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov r13, r12 # assign L2
    mov r11, 0 # tag L1 = TAG_INT (i64-local, fused)
    mov [rbp - 520], r11 # store tag L2
    cmp r13, 40 # binop ==
    sete al # binop == → al
    movzx r14, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 528], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd__re_parse_atom_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_atom_bb1 # jump -> then
.Lb2dd__re_parse_atom_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 544], rax # store tag L5
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_parse_alt # call _re_parse_alt
    mov [rbp - 552], rax # store tag L6
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 64], r10 # spill L6 to slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r10, r11 # assign L7
    mov r11, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rbp - 560], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 568], rax # store tag L8
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, [rbp - 80] # reload L8 from spill slot
    cmp r10, 41 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 576], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_atom_bb4 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_atom_bb3 # jump -> then
.Lb2dd__re_parse_atom_bb2:
    cmp r13, 91 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 608], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_atom_bb7 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_atom_bb6 # jump -> then
.Lb2dd__re_parse_atom_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 592], rax # store tag L11
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 104], r10 # spill L11 to slot
    jmp .Lb2dd__re_parse_atom_bb5 # branch
.Lb2dd__re_parse_atom_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 600], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 600] # tag L12 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 600], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    jmp .Lb2dd__re_parse_atom_bb5 # branch
.Lb2dd__re_parse_atom_bb5:
    mov rdx, [rbp - 72] # reload L7 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L7 = TAG_INT (i64-local, fused)
    add rsp, 944 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_atom_bb6:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 624], rax # store tag L15
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 136], r10 # spill L15 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_parse_class # call _re_parse_class
    mov [rbp - 632], rax # store tag L16
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 144], r10 # spill L16 to slot
    mov rdx, [rbp - 144] # reload L16 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L16 = TAG_INT (i64-local, fused)
    add rsp, 944 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_atom_bb7:
    cmp r13, 46 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 640], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_atom_bb9 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_atom_bb8 # jump -> then
.Lb2dd__re_parse_atom_bb8:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 656], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 664], rax # store tag L20
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 664] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 664], rax # store tag L20
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 664] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 664], rax # store tag L20
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 664] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 664], rax # store tag L20
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 664] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 664], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    mov rcx, [rbp - 176] # reload L20 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 664] # tag L20 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 672], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov rdx, [rbp - 184] # reload L21 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L21 = TAG_INT (i64-local, fused)
    add rsp, 944 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_atom_bb9:
    cmp r13, 94 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 680], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_atom_bb11 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_atom_bb10 # jump -> then
.Lb2dd__re_parse_atom_bb10:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 696], rax # store tag L24
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 208], r10 # spill L24 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 704], rax # store tag L25
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 704] # tag L25 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 704], rax # store tag L25
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 704] # tag L25 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 704], rax # store tag L25
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 704] # tag L25 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 704], rax # store tag L25
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 704] # tag L25 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 704], rax # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    mov rcx, [rbp - 216] # reload L25 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 704] # tag L25 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 712], rax # store tag L26
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 224], r10 # spill L26 to slot
    mov rdx, [rbp - 224] # reload L26 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L26 = TAG_INT (i64-local, fused)
    add rsp, 944 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_atom_bb11:
    cmp r13, 36 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 720], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, [rbp - 232] # reload L27 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_atom_bb13 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_atom_bb12 # jump -> then
.Lb2dd__re_parse_atom_bb12:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 736], rax # store tag L29
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 248], r10 # spill L29 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 744], rax # store tag L30
    mov rcx, 10 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 744] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 744], rax # store tag L30
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 744] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 744], rax # store tag L30
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 744] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 744], rax # store tag L30
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 744] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 744], rax # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    mov rcx, [rbp - 256] # reload L30 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 744] # tag L30 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 752], rax # store tag L31
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 264], r10 # spill L31 to slot
    mov rdx, [rbp - 264] # reload L31 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L31 = TAG_INT (i64-local, fused)
    add rsp, 944 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_atom_bb13:
    cmp r13, 92 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 760], r11 # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, [rbp - 272] # reload L32 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_atom_bb15 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_atom_bb14 # jump -> then
.Lb2dd__re_parse_atom_bb14:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 776], rax # store tag L34
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 288], r10 # spill L34 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_peek # call _re_peek
    mov [rbp - 784], rax # store tag L35
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 296], r10 # spill L35 to slot
    mov r11, [rbp - 296] # reload L35 from spill slot
    mov r10, r11 # assign L36
    mov r11, 0 # tag L35 = TAG_INT (i64-local, fused)
    mov [rbp - 792], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 800], rax # store tag L37
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 312], r10 # spill L37 to slot
    mov rsi, [rbp - 304] # reload L36 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L36 = TAG_INT (i64-local, fused)
    call _re_class_for_escape # call _re_class_for_escape
    mov [rbp - 808], rax # store tag L38
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 320], r10 # spill L38 to slot
    mov r11, [rbp - 320] # reload L38 from spill slot
    mov r10, r11 # assign L39
    mov r11, [rbp - 808] # tag L38 from tag-slot
    mov [rbp - 816], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov rsi, [rbp - 328] # reload L39 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 816] # tag L39 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 824], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    mov rsi, [rbp - 336] # reload L40 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 824] # tag L40 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_gt # binop >: tag-dispatch hexa_cmp_gt
    mov r10, rdx # binop >: capture bool payload
    mov [rbp - 832], rax # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r10, [rbp - 344] # reload L41 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_atom_bb17 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_atom_bb16 # jump -> then
.Lb2dd__re_parse_atom_bb15:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    call _re_adv # call _re_adv
    mov [rbp - 968], rax # store tag L58
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 480], r10 # spill L58 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 976], rax # store tag L59
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 976] # tag L59 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 976], rax # store tag L59
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 976] # tag L59 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 976], rax # store tag L59
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 976] # tag L59 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 976], rax # store tag L59
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 976] # tag L59 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 976], rax # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    mov rcx, [rbp - 488] # reload L59 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 976] # tag L59 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 984], rax # store tag L60
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 496], r10 # spill L60 to slot
    mov rdx, [rbp - 496] # reload L60 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L60 = TAG_INT (i64-local, fused)
    add rsp, 944 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_atom_bb16:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 848], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov rsi, [rbp - 360] # reload L43 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 848] # tag L43 from tag-slot
    mov rcx, [rbp - 328] # reload L39 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 816] # tag L39 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 856], rax # store tag L44
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 368], r10 # spill L44 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 864], rax # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov rsi, [rbp - 376] # reload L45 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 864] # tag L45 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 872], r11 # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    mov r10, [rbp - 384] # reload L46 from spill slot
    mov rsi, [rbp - 384] # reload L46 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 872] # tag L46 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 880], rax # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 888], rax # store tag L48
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 888] # tag L48 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 888], rax # store tag L48
    mov rcx, [rbp - 392] # reload L47 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L47 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 888] # tag L48 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 888], rax # store tag L48
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 888] # tag L48 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 888], rax # store tag L48
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 888] # tag L48 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 888], rax # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    mov rcx, [rbp - 400] # reload L48 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 888] # tag L48 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 896], rax # store tag L49
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 408], r10 # spill L49 to slot
    mov rdx, [rbp - 408] # reload L49 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L49 = TAG_INT (i64-local, fused)
    add rsp, 944 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_atom_bb17:
    mov rsi, [rbp - 304] # reload L36 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L36 = TAG_INT (i64-local, fused)
    call _re_lit_for_escape # call _re_lit_for_escape
    mov [rbp - 904], rax # store tag L50
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 416], r10 # spill L50 to slot
    mov r11, [rbp - 416] # reload L50 from spill slot
    mov r10, r11 # assign L51
    mov r11, 0 # tag L50 = TAG_INT (i64-local, fused)
    mov [rbp - 912], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    mov r10, [rbp - 424] # reload L51 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 920], r11 # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov r10, [rbp - 432] # reload L52 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_parse_atom_bb19 # jump-if-zero -> else
    jmp .Lb2dd__re_parse_atom_bb18 # jump -> then
.Lb2dd__re_parse_atom_bb18:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 936], rax # store tag L54
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 936] # tag L54 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 936], rax # store tag L54
    mov rcx, [rbp - 424] # reload L51 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L51 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 936] # tag L54 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 936], rax # store tag L54
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 936] # tag L54 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 936], rax # store tag L54
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 936] # tag L54 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 936], rax # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    mov rcx, [rbp - 448] # reload L54 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 936] # tag L54 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 944], rax # store tag L55
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 456], r10 # spill L55 to slot
    mov rdx, [rbp - 456] # reload L55 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L55 = TAG_INT (i64-local, fused)
    add rsp, 944 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_parse_atom_bb19:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 952], rax # store tag L56
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 952] # tag L56 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 952], rax # store tag L56
    mov rcx, [rbp - 304] # reload L36 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L36 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 952] # tag L56 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 952], rax # store tag L56
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 952] # tag L56 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 952], rax # store tag L56
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 952] # tag L56 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 3
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 952], rax # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 504] # tag L0 from tag-slot
    mov rcx, [rbp - 464] # reload L56 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 952] # tag L56 from tag-slot
    call _re_emit # call _re_emit
    mov [rbp - 960], rax # store tag L57
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 472], r10 # spill L57 to slot
    mov rdx, [rbp - 472] # reload L57 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L57 = TAG_INT (i64-local, fused)
    add rsp, 944 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _nfa_new
    .p2align 4
_nfa_new:
    .loc 1 326 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 192 # prologue: alloc spill frame
    mov [rbp - 128], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 136], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 144], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd__nfa_new_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    call hexa_map_get # field: hexa_map_get
    mov r14, rdx # field: capture result payload
    mov [rbp - 152], rax # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 152] # tag L3 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 160], rax # store tag L4
    mov r15, rdx # hv: unbox call result (rdx)
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr14] # map key (const char*): &.LCstr14
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 168], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 168] # tag L5 from tag-slot
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    call hexa_array_push # call hexa_array_push
    mov [rbp - 176], rax # store tag L6
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 64], r10 # spill L6 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 184], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 184] # tag L7 from tag-slot
    mov rcx, -1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_array_push # call hexa_array_push
    mov [rbp - 192], rax # store tag L8
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr16] # map key (const char*): &.LCstr16
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 200], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 200] # tag L9 from tag-slot
    mov rcx, -1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_array_push # call hexa_array_push
    mov [rbp - 208], rax # store tag L10
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 96], r10 # spill L10 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 216], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 216] # tag L11 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 224], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 224] # tag L12 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_sub # binop -: tag-dispatch hexa_sub
    mov r10, rdx # binop -: capture result payload
    mov [rbp - 232], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rdx, [rbp - 120] # reload L13 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L13 = TAG_INT (i64-local, fused)
    add rsp, 192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _nfa_patch
    .p2align 4
_nfa_patch:
    .loc 1 335 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 240 # prologue: alloc spill frame
    mov [rbp - 152], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 160], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 168], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd__nfa_patch_bb0:
    mov r14, 0 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 176], r11 # store tag L3
    jmp .Lb2dd__nfa_patch_bb1 # branch
.Lb2dd__nfa_patch_bb1:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 160] # tag L1 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r15, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 184], r11 # store tag L4
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 184] # tag L4 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 192], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__nfa_patch_bb3 # jump-if-zero -> else
    jmp .Lb2dd__nfa_patch_bb2 # jump -> then
.Lb2dd__nfa_patch_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 160] # tag L1 from tag-slot
    mov rdx, r14 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 200], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r10, r11 # assign L7
    mov r11, [rbp - 200] # tag L6 from tag-slot
    mov [rbp - 208], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 208] # tag L7 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_div # binop /: tag-dispatch hexa_div
    mov r10, rdx # binop /: capture result payload
    mov [rbp - 216], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L9
    mov r11, [rbp - 216] # tag L8 from tag-slot
    mov [rbp - 224], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 208] # tag L7 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_mod # binop %: tag-dispatch hexa_mod
    mov r10, rdx # binop %: capture result payload
    mov [rbp - 232], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 232] # tag L10 from tag-slot
    mov [rbp - 240], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 240] # tag L11 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 248], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__nfa_patch_bb5 # jump-if-zero -> else
    jmp .Lb2dd__nfa_patch_bb4 # jump -> then
.Lb2dd__nfa_patch_bb3:
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 240 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__nfa_patch_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 152] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 264], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 264] # tag L14 from tag-slot
    mov rcx, [rbp - 88] # reload L9 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 224] # tag L9 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, 0 # tag L2 = TAG_INT (i64-local, fused)
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 264], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__nfa_patch_bb6 # branch
.Lb2dd__nfa_patch_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 152] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr16] # map key (const char*): &.LCstr16
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 272], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 272] # tag L15 from tag-slot
    mov rcx, [rbp - 88] # reload L9 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 224] # tag L9 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, 0 # tag L2 = TAG_INT (i64-local, fused)
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 272], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    jmp .Lb2dd__nfa_patch_bb6 # branch
.Lb2dd__nfa_patch_bb6:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 280], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L16 = TAG_INT (i64-local, fused)
    mov [rbp - 176], r11 # store tag L3
    jmp .Lb2dd__nfa_patch_bb1 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 240 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _frag_dangle
    .p2align 4
_frag_dangle:
    .loc 1 347 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 112 # prologue: alloc spill frame
    mov [rbp - 88], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__frag_dangle_bb0:
    call hexa_array_new # array_lit: new array
    mov r12, rdx # array_lit: capture new array payload
    mov [rbp - 96], rax # store tag L1
    mov r13, r12 # assign L2
    mov r11, [rbp - 96] # tag L1 from tag-slot
    mov [rbp - 104], r11 # store tag L2
    mov r14, 1 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 112], r11 # store tag L3
    jmp .Lb2dd__frag_dangle_bb1 # branch
.Lb2dd__frag_dangle_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 88] # tag L0 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r15, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 120], r11 # store tag L4
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 120] # tag L4 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 128], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__frag_dangle_bb3 # jump-if-zero -> else
    jmp .Lb2dd__frag_dangle_bb2 # jump -> then
.Lb2dd__frag_dangle_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 88] # tag L0 from tag-slot
    mov rdx, r14 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 136], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 104] # tag L2 from tag-slot
    mov rcx, [rbp - 64] # reload L6 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 136] # tag L6 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 144], rax # store tag L7
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 152], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, [rbp - 80] # reload L8 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L8 = TAG_INT (i64-local, fused)
    mov [rbp - 112], r11 # store tag L3
    jmp .Lb2dd__frag_dangle_bb1 # branch
.Lb2dd__frag_dangle_bb3:
    mov rdx, r13 # hv arg payload
    mov rax, [rbp - 104] # tag L2 from tag-slot
    add rsp, 112 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _concat_int
    .p2align 4
_concat_int:
    .loc 1 355 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 224 # prologue: alloc spill frame
    mov [rbp - 144], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 152], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd__concat_int_bb0:
    call hexa_array_new # array_lit: new array
    mov r13, rdx # array_lit: capture new array payload
    mov [rbp - 160], rax # store tag L2
    mov r14, r13 # assign L3
    mov r11, [rbp - 160] # tag L2 from tag-slot
    mov [rbp - 168], r11 # store tag L3
    mov r15, 0 # assign L4
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 176], r11 # store tag L4
    jmp .Lb2dd__concat_int_bb1 # branch
.Lb2dd__concat_int_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 144] # tag L0 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 184], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov rsi, r15 # hv arg payload
    mov rdi, 0 # tag L4 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 184] # tag L5 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 192], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__concat_int_bb3 # jump-if-zero -> else
    jmp .Lb2dd__concat_int_bb2 # jump -> then
.Lb2dd__concat_int_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 144] # tag L0 from tag-slot
    mov rdx, r15 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 200], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 168] # tag L3 from tag-slot
    mov rcx, [rbp - 72] # reload L7 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 200] # tag L7 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 208], rax # store tag L8
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, r15 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 216], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r15, r10 # assign L4
    mov r11, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rbp - 176], r11 # store tag L4
    jmp .Lb2dd__concat_int_bb1 # branch
.Lb2dd__concat_int_bb3:
    mov r10, 0 # assign L10
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 224], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    jmp .Lb2dd__concat_int_bb4 # branch
.Lb2dd__concat_int_bb4:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 152] # tag L1 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 232], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L10 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 104] # reload L11 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 232] # tag L11 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 240], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__concat_int_bb6 # jump-if-zero -> else
    jmp .Lb2dd__concat_int_bb5 # jump -> then
.Lb2dd__concat_int_bb5:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 152] # tag L1 from tag-slot
    mov rdx, [rbp - 96] # reload L10 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 248], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 168] # tag L3 from tag-slot
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 248] # tag L13 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 256], rax # store tag L14
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 264], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L10
    mov r11, 0 # tag L15 = TAG_INT (i64-local, fused)
    mov [rbp - 224], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    jmp .Lb2dd__concat_int_bb4 # branch
.Lb2dd__concat_int_bb6:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 168] # tag L3 from tag-slot
    add rsp, 224 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_compile_node
    .p2align 4
_re_compile_node:
    .loc 1 365 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 2192 # prologue: alloc spill frame
    mov [rbp - 1128], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 1136], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 1144], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd__re_compile_node_bb0:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 1136] # tag L1 from tag-slot
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r14, rdx # index: capture element payload
    mov [rbp - 1152], rax # store tag L3
    mov r15, r14 # assign L4
    mov r11, [rbp - 1152] # tag L3 from tag-slot
    mov [rbp - 1160], r11 # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1168], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 1168] # tag L5 from tag-slot
    mov [rbp - 1176], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1184], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb1 # jump -> then
.Lb2dd__re_compile_node_bb1:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1200], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, [rbp - 88] # reload L9 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1200] # tag L9 from tag-slot
    call _nfa_new # call _nfa_new
    mov [rbp - 1208], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, 0 # tag L10 = TAG_INT (i64-local, fused)
    mov [rbp - 1216], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1224], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 0 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1232], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1240], rax # store tag L14
    mov rcx, [rbp - 104] # reload L11 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L11 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1240] # tag L14 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1240], rax # store tag L14
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1240] # tag L14 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1240], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov rdx, [rbp - 128] # reload L14 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 1240] # tag L14 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb2:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1248], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb4 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb3 # jump -> then
.Lb2dd__re_compile_node_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 1264], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, 0 # tag L17 = TAG_INT (i64-local, fused)
    mov [rbp - 1272], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov r10, [rbp - 160] # reload L18 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1280], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, [rbp - 168] # reload L19 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 0 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1288], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1296], rax # store tag L21
    mov rcx, [rbp - 160] # reload L18 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L18 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1296] # tag L21 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1296], rax # store tag L21
    mov rcx, [rbp - 176] # reload L20 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L20 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1296] # tag L21 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1296], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov rdx, [rbp - 184] # reload L21 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 1296] # tag L21 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb4:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1304], rax # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb6 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb5 # jump -> then
.Lb2dd__re_compile_node_bb5:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1320], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, [rbp - 208] # reload L24 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1320] # tag L24 from tag-slot
    call _nfa_new # call _nfa_new
    mov [rbp - 1328], rax # store tag L25
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 216], r10 # spill L25 to slot
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L26
    mov r11, 0 # tag L25 = TAG_INT (i64-local, fused)
    mov [rbp - 1336], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r10, [rbp - 224] # reload L26 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1344], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 0 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1352], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1360], rax # store tag L29
    mov rcx, [rbp - 224] # reload L26 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L26 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1360] # tag L29 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1360], rax # store tag L29
    mov rcx, [rbp - 240] # reload L28 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L28 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1360] # tag L29 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1360], rax # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov rdx, [rbp - 248] # reload L29 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 1360] # tag L29 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb6:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 9 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1368], rax # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov r10, [rbp - 256] # reload L30 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb8 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb7 # jump -> then
.Lb2dd__re_compile_node_bb7:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 5 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 1384], rax # store tag L32
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 272], r10 # spill L32 to slot
    mov r11, [rbp - 272] # reload L32 from spill slot
    mov r10, r11 # assign L33
    mov r11, 0 # tag L32 = TAG_INT (i64-local, fused)
    mov [rbp - 1392], r11 # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r10, [rbp - 280] # reload L33 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1400], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 0 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1408], r11 # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1416], rax # store tag L36
    mov rcx, [rbp - 280] # reload L33 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L33 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1416] # tag L36 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1416], rax # store tag L36
    mov rcx, [rbp - 296] # reload L35 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L35 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1416] # tag L36 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1416], rax # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov rdx, [rbp - 304] # reload L36 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 1416] # tag L36 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb8:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 10 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1424], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb10 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb9 # jump -> then
.Lb2dd__re_compile_node_bb9:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 6 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 1440], rax # store tag L39
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 328], r10 # spill L39 to slot
    mov r11, [rbp - 328] # reload L39 from spill slot
    mov r10, r11 # assign L40
    mov r11, 0 # tag L39 = TAG_INT (i64-local, fused)
    mov [rbp - 1448], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1456], r11 # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r10, [rbp - 344] # reload L41 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 0 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1464], r11 # store tag L42
    mov [rbp - 352], r10 # spill L42 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1472], rax # store tag L43
    mov rcx, [rbp - 336] # reload L40 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L40 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1472] # tag L43 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1472], rax # store tag L43
    mov rcx, [rbp - 352] # reload L42 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L42 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1472] # tag L43 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1472], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov rdx, [rbp - 360] # reload L43 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 1472] # tag L43 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb10:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 8 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1480], rax # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov r10, [rbp - 368] # reload L44 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb12 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb11 # jump -> then
.Lb2dd__re_compile_node_bb11:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 1496], rax # store tag L46
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 384], r10 # spill L46 to slot
    mov r11, [rbp - 384] # reload L46 from spill slot
    mov r10, r11 # assign L47
    mov r11, 0 # tag L46 = TAG_INT (i64-local, fused)
    mov [rbp - 1504], r11 # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    mov r10, [rbp - 392] # reload L47 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1512], r11 # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    mov r10, [rbp - 400] # reload L48 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 0 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1520], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov r10, [rbp - 392] # reload L47 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1528], r11 # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    mov r10, [rbp - 416] # reload L50 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1536], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1544], rax # store tag L52
    mov rcx, [rbp - 392] # reload L47 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L47 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1544] # tag L52 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1544], rax # store tag L52
    mov rcx, [rbp - 408] # reload L49 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L49 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1544] # tag L52 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1544], rax # store tag L52
    mov rcx, [rbp - 424] # reload L51 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L51 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1544] # tag L52 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1544], rax # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov rdx, [rbp - 432] # reload L52 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 1544] # tag L52 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb12:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 6 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1552], rax # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    mov r10, [rbp - 440] # reload L53 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb14 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb13 # jump -> then
.Lb2dd__re_compile_node_bb13:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1568], rax # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 1136] # tag L1 from tag-slot
    mov r9, [rbp - 456] # reload L55 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1568] # tag L55 from tag-slot
    call _re_compile_node # call _re_compile_node
    mov [rbp - 1576], rax # store tag L56
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 464], r10 # spill L56 to slot
    mov r11, [rbp - 464] # reload L56 from spill slot
    mov r10, r11 # assign L57
    mov r11, [rbp - 1576] # tag L56 from tag-slot
    mov [rbp - 1584], r11 # store tag L57
    mov [rbp - 472], r10 # spill L57 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1592], rax # store tag L58
    mov [rbp - 480], r10 # spill L58 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 1136] # tag L1 from tag-slot
    mov r9, [rbp - 480] # reload L58 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1592] # tag L58 from tag-slot
    call _re_compile_node # call _re_compile_node
    mov [rbp - 1600], rax # store tag L59
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 488], r10 # spill L59 to slot
    mov r11, [rbp - 488] # reload L59 from spill slot
    mov r10, r11 # assign L60
    mov r11, [rbp - 1600] # tag L59 from tag-slot
    mov [rbp - 1608], r11 # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov rsi, [rbp - 472] # reload L57 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1584] # tag L57 from tag-slot
    call _frag_dangle # call _frag_dangle
    mov [rbp - 1616], rax # store tag L61
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 504], r10 # spill L61 to slot
    mov rsi, [rbp - 496] # reload L60 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1608] # tag L60 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 1624], rax # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, [rbp - 504] # reload L61 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1616] # tag L61 from tag-slot
    mov r9, [rbp - 512] # reload L62 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1624] # tag L62 from tag-slot
    call _nfa_patch # call _nfa_patch
    mov [rbp - 1632], rax # store tag L63
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 520], r10 # spill L63 to slot
    mov rsi, [rbp - 472] # reload L57 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1584] # tag L57 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 1640], rax # store tag L64
    mov [rbp - 528], r10 # spill L64 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1648], rax # store tag L65
    mov rcx, [rbp - 528] # reload L64 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1640] # tag L64 from tag-slot
    mov rdi, [rbp - 1648] # tag L65 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1648], rax # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    mov rsi, [rbp - 496] # reload L60 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1608] # tag L60 from tag-slot
    call _frag_dangle # call _frag_dangle
    mov [rbp - 1656], rax # store tag L66
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 544], r10 # spill L66 to slot
    mov rsi, [rbp - 536] # reload L65 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1648] # tag L65 from tag-slot
    mov rcx, [rbp - 544] # reload L66 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1656] # tag L66 from tag-slot
    call _concat_int # call _concat_int
    mov [rbp - 1664], rax # store tag L67
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 552], r10 # spill L67 to slot
    mov rdx, [rbp - 552] # reload L67 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 1664] # tag L67 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb14:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 7 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1672], rax # store tag L68
    mov [rbp - 560], r10 # spill L68 to slot
    mov r10, [rbp - 560] # reload L68 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb16 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb15 # jump -> then
.Lb2dd__re_compile_node_bb15:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1688], rax # store tag L70
    mov [rbp - 576], r10 # spill L70 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 1136] # tag L1 from tag-slot
    mov r9, [rbp - 576] # reload L70 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1688] # tag L70 from tag-slot
    call _re_compile_node # call _re_compile_node
    mov [rbp - 1696], rax # store tag L71
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 584], r10 # spill L71 to slot
    mov r11, [rbp - 584] # reload L71 from spill slot
    mov r10, r11 # assign L72
    mov r11, [rbp - 1696] # tag L71 from tag-slot
    mov [rbp - 1704], r11 # store tag L72
    mov [rbp - 592], r10 # spill L72 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1712], rax # store tag L73
    mov [rbp - 600], r10 # spill L73 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 1136] # tag L1 from tag-slot
    mov r9, [rbp - 600] # reload L73 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1712] # tag L73 from tag-slot
    call _re_compile_node # call _re_compile_node
    mov [rbp - 1720], rax # store tag L74
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 608], r10 # spill L74 to slot
    mov r11, [rbp - 608] # reload L74 from spill slot
    mov r10, r11 # assign L75
    mov r11, [rbp - 1720] # tag L74 from tag-slot
    mov [rbp - 1728], r11 # store tag L75
    mov [rbp - 616], r10 # spill L75 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 1736], rax # store tag L76
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 624], r10 # spill L76 to slot
    mov r11, [rbp - 624] # reload L76 from spill slot
    mov r10, r11 # assign L77
    mov r11, 0 # tag L76 = TAG_INT (i64-local, fused)
    mov [rbp - 1744], r11 # store tag L77
    mov [rbp - 632], r10 # spill L77 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1752], rax # store tag L78
    mov [rbp - 640], r10 # spill L78 to slot
    mov rsi, [rbp - 592] # reload L72 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1704] # tag L72 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 1760], rax # store tag L79
    mov [rbp - 648], r10 # spill L79 to slot
    mov rsi, [rbp - 640] # reload L78 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1752] # tag L78 from tag-slot
    mov rcx, [rbp - 632] # reload L77 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L77 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 648] # reload L79 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1760] # tag L79 from tag-slot
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1752], rax # store tag L78
    mov [rbp - 640], r10 # spill L78 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr16] # map key (const char*): &.LCstr16
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1768], rax # store tag L80
    mov [rbp - 656], r10 # spill L80 to slot
    mov rsi, [rbp - 616] # reload L75 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1728] # tag L75 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 1776], rax # store tag L81
    mov [rbp - 664], r10 # spill L81 to slot
    mov rsi, [rbp - 656] # reload L80 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1768] # tag L80 from tag-slot
    mov rcx, [rbp - 632] # reload L77 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L77 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 664] # reload L81 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1776] # tag L81 from tag-slot
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1768], rax # store tag L80
    mov [rbp - 656], r10 # spill L80 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1784], rax # store tag L82
    mov rcx, [rbp - 632] # reload L77 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L77 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1784] # tag L82 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1784], rax # store tag L82
    mov [rbp - 672], r10 # spill L82 to slot
    mov rsi, [rbp - 592] # reload L72 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1704] # tag L72 from tag-slot
    call _frag_dangle # call _frag_dangle
    mov [rbp - 1792], rax # store tag L83
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 680], r10 # spill L83 to slot
    mov rsi, [rbp - 616] # reload L75 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1728] # tag L75 from tag-slot
    call _frag_dangle # call _frag_dangle
    mov [rbp - 1800], rax # store tag L84
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 688], r10 # spill L84 to slot
    mov rsi, [rbp - 680] # reload L83 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1792] # tag L83 from tag-slot
    mov rcx, [rbp - 688] # reload L84 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1800] # tag L84 from tag-slot
    call _concat_int # call _concat_int
    mov [rbp - 1808], rax # store tag L85
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 696], r10 # spill L85 to slot
    mov rsi, [rbp - 672] # reload L82 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1784] # tag L82 from tag-slot
    mov rcx, [rbp - 696] # reload L85 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1808] # tag L85 from tag-slot
    call _concat_int # call _concat_int
    mov [rbp - 1816], rax # store tag L86
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 704], r10 # spill L86 to slot
    mov rdx, [rbp - 704] # reload L86 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 1816] # tag L86 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb16:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1824], rax # store tag L87
    mov [rbp - 712], r10 # spill L87 to slot
    mov r10, [rbp - 712] # reload L87 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb18 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb17 # jump -> then
.Lb2dd__re_compile_node_bb17:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1840], rax # store tag L89
    mov [rbp - 728], r10 # spill L89 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 1136] # tag L1 from tag-slot
    mov r9, [rbp - 728] # reload L89 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1840] # tag L89 from tag-slot
    call _re_compile_node # call _re_compile_node
    mov [rbp - 1848], rax # store tag L90
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 736], r10 # spill L90 to slot
    mov r11, [rbp - 736] # reload L90 from spill slot
    mov r10, r11 # assign L91
    mov r11, [rbp - 1848] # tag L90 from tag-slot
    mov [rbp - 1856], r11 # store tag L91
    mov [rbp - 744], r10 # spill L91 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 1864], rax # store tag L92
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 752], r10 # spill L92 to slot
    mov r11, [rbp - 752] # reload L92 from spill slot
    mov r10, r11 # assign L93
    mov r11, 0 # tag L92 = TAG_INT (i64-local, fused)
    mov [rbp - 1872], r11 # store tag L93
    mov [rbp - 760], r10 # spill L93 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1880], rax # store tag L94
    mov [rbp - 768], r10 # spill L94 to slot
    mov rsi, [rbp - 744] # reload L91 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1856] # tag L91 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 1888], rax # store tag L95
    mov [rbp - 776], r10 # spill L95 to slot
    mov rsi, [rbp - 768] # reload L94 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1880] # tag L94 from tag-slot
    mov rcx, [rbp - 760] # reload L93 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L93 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 776] # reload L95 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1888] # tag L95 from tag-slot
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1880], rax # store tag L94
    mov [rbp - 768], r10 # spill L94 to slot
    mov rsi, [rbp - 744] # reload L91 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1856] # tag L91 from tag-slot
    call _frag_dangle # call _frag_dangle
    mov [rbp - 1896], rax # store tag L96
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 784], r10 # spill L96 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, [rbp - 784] # reload L96 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1896] # tag L96 from tag-slot
    mov r9, [rbp - 760] # reload L93 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L93 = TAG_INT (i64-local, fused)
    call _nfa_patch # call _nfa_patch
    mov [rbp - 1904], rax # store tag L97
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 792], r10 # spill L97 to slot
    mov r10, [rbp - 760] # reload L93 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1912], r11 # store tag L98
    mov [rbp - 800], r10 # spill L98 to slot
    mov r10, [rbp - 800] # reload L98 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1920], r11 # store tag L99
    mov [rbp - 808], r10 # spill L99 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 1928], rax # store tag L100
    mov rcx, [rbp - 760] # reload L93 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L93 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1928] # tag L100 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1928], rax # store tag L100
    mov rcx, [rbp - 808] # reload L99 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L99 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 1928] # tag L100 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 1928], rax # store tag L100
    mov [rbp - 816], r10 # spill L100 to slot
    mov rdx, [rbp - 816] # reload L100 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 1928] # tag L100 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb18:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 4 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1936], rax # store tag L101
    mov [rbp - 824], r10 # spill L101 to slot
    mov r10, [rbp - 824] # reload L101 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb20 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb19 # jump -> then
.Lb2dd__re_compile_node_bb19:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 1952], rax # store tag L103
    mov [rbp - 840], r10 # spill L103 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 1136] # tag L1 from tag-slot
    mov r9, [rbp - 840] # reload L103 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 1952] # tag L103 from tag-slot
    call _re_compile_node # call _re_compile_node
    mov [rbp - 1960], rax # store tag L104
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 848], r10 # spill L104 to slot
    mov r11, [rbp - 848] # reload L104 from spill slot
    mov r10, r11 # assign L105
    mov r11, [rbp - 1960] # tag L104 from tag-slot
    mov [rbp - 1968], r11 # store tag L105
    mov [rbp - 856], r10 # spill L105 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 1976], rax # store tag L106
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 864], r10 # spill L106 to slot
    mov r11, [rbp - 864] # reload L106 from spill slot
    mov r10, r11 # assign L107
    mov r11, 0 # tag L106 = TAG_INT (i64-local, fused)
    mov [rbp - 1984], r11 # store tag L107
    mov [rbp - 872], r10 # spill L107 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 1992], rax # store tag L108
    mov [rbp - 880], r10 # spill L108 to slot
    mov rsi, [rbp - 856] # reload L105 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1968] # tag L105 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 2000], rax # store tag L109
    mov [rbp - 888], r10 # spill L109 to slot
    mov rsi, [rbp - 880] # reload L108 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1992] # tag L108 from tag-slot
    mov rcx, [rbp - 872] # reload L107 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L107 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 888] # reload L109 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 2000] # tag L109 from tag-slot
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 1992], rax # store tag L108
    mov [rbp - 880], r10 # spill L108 to slot
    mov rsi, [rbp - 856] # reload L105 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1968] # tag L105 from tag-slot
    call _frag_dangle # call _frag_dangle
    mov [rbp - 2008], rax # store tag L110
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 896], r10 # spill L110 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, [rbp - 896] # reload L110 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2008] # tag L110 from tag-slot
    mov r9, [rbp - 872] # reload L107 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L107 = TAG_INT (i64-local, fused)
    call _nfa_patch # call _nfa_patch
    mov [rbp - 2016], rax # store tag L111
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 904], r10 # spill L111 to slot
    mov rsi, [rbp - 856] # reload L105 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1968] # tag L105 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 2024], rax # store tag L112
    mov [rbp - 912], r10 # spill L112 to slot
    mov r10, [rbp - 872] # reload L107 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2032], r11 # store tag L113
    mov [rbp - 920], r10 # spill L113 to slot
    mov r10, [rbp - 920] # reload L113 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2040], r11 # store tag L114
    mov [rbp - 928], r10 # spill L114 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2048], rax # store tag L115
    mov rcx, [rbp - 912] # reload L112 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2024] # tag L112 from tag-slot
    mov rdi, [rbp - 2048] # tag L115 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2048], rax # store tag L115
    mov rcx, [rbp - 928] # reload L114 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L114 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2048] # tag L115 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2048], rax # store tag L115
    mov [rbp - 936], r10 # spill L115 to slot
    mov rdx, [rbp - 936] # reload L115 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 2048] # tag L115 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb20:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1176] # tag L6 from tag-slot
    mov rcx, 5 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 2056], rax # store tag L116
    mov [rbp - 944], r10 # spill L116 to slot
    mov r10, [rbp - 944] # reload L116 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_compile_node_bb22 # jump-if-zero -> else
    jmp .Lb2dd__re_compile_node_bb21 # jump -> then
.Lb2dd__re_compile_node_bb21:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 1160] # tag L4 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 2072], rax # store tag L118
    mov [rbp - 960], r10 # spill L118 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 1136] # tag L1 from tag-slot
    mov r9, [rbp - 960] # reload L118 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 2072] # tag L118 from tag-slot
    call _re_compile_node # call _re_compile_node
    mov [rbp - 2080], rax # store tag L119
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 968], r10 # spill L119 to slot
    mov r11, [rbp - 968] # reload L119 from spill slot
    mov r10, r11 # assign L120
    mov r11, [rbp - 2080] # tag L119 from tag-slot
    mov [rbp - 2088], r11 # store tag L120
    mov [rbp - 976], r10 # spill L120 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 2096], rax # store tag L121
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 984], r10 # spill L121 to slot
    mov r11, [rbp - 984] # reload L121 from spill slot
    mov r10, r11 # assign L122
    mov r11, 0 # tag L121 = TAG_INT (i64-local, fused)
    mov [rbp - 2104], r11 # store tag L122
    mov [rbp - 992], r10 # spill L122 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 2112], rax # store tag L123
    mov [rbp - 1000], r10 # spill L123 to slot
    mov rsi, [rbp - 976] # reload L120 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2088] # tag L120 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 2120], rax # store tag L124
    mov [rbp - 1008], r10 # spill L124 to slot
    mov rsi, [rbp - 1000] # reload L123 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2112] # tag L123 from tag-slot
    mov rcx, [rbp - 992] # reload L122 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L122 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 1008] # reload L124 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 2120] # tag L124 from tag-slot
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 2112], rax # store tag L123
    mov [rbp - 1000], r10 # spill L123 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2128], rax # store tag L125
    mov rcx, [rbp - 992] # reload L122 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L122 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2128] # tag L125 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2128], rax # store tag L125
    mov [rbp - 1016], r10 # spill L125 to slot
    mov rsi, [rbp - 976] # reload L120 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2088] # tag L120 from tag-slot
    call _frag_dangle # call _frag_dangle
    mov [rbp - 2136], rax # store tag L126
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1024], r10 # spill L126 to slot
    mov r10, [rbp - 992] # reload L122 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2144], r11 # store tag L127
    mov [rbp - 1032], r10 # spill L127 to slot
    mov r10, [rbp - 1032] # reload L127 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2152], r11 # store tag L128
    mov [rbp - 1040], r10 # spill L128 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2160], rax # store tag L129
    mov rcx, [rbp - 1040] # reload L128 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L128 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2160] # tag L129 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2160], rax # store tag L129
    mov [rbp - 1048], r10 # spill L129 to slot
    mov rsi, [rbp - 1024] # reload L126 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2136] # tag L126 from tag-slot
    mov rcx, [rbp - 1048] # reload L129 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2160] # tag L129 from tag-slot
    call _concat_int # call _concat_int
    mov [rbp - 2168], rax # store tag L130
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1056], r10 # spill L130 to slot
    mov rsi, [rbp - 1016] # reload L125 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 2128] # tag L125 from tag-slot
    mov rcx, [rbp - 1056] # reload L130 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 2168] # tag L130 from tag-slot
    call _concat_int # call _concat_int
    mov [rbp - 2176], rax # store tag L131
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1064], r10 # spill L131 to slot
    mov rdx, [rbp - 1064] # reload L131 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 2176] # tag L131 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_compile_node_bb22:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 1128] # tag L0 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 2184], rax # store tag L132
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 1072], r10 # spill L132 to slot
    mov r11, [rbp - 1072] # reload L132 from spill slot
    mov r10, r11 # assign L133
    mov r11, 0 # tag L132 = TAG_INT (i64-local, fused)
    mov [rbp - 2192], r11 # store tag L133
    mov [rbp - 1080], r10 # spill L133 to slot
    mov r10, [rbp - 1080] # reload L133 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2200], r11 # store tag L134
    mov [rbp - 1088], r10 # spill L134 to slot
    mov r10, [rbp - 1088] # reload L134 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 0 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2208], r11 # store tag L135
    mov [rbp - 1096], r10 # spill L135 to slot
    mov r10, [rbp - 1080] # reload L133 from spill slot
    mov r10, r10 # binop lhs into dst
    imul r10, 2 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2216], r11 # store tag L136
    mov [rbp - 1104], r10 # spill L136 to slot
    mov r10, [rbp - 1104] # reload L136 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 2224], r11 # store tag L137
    mov [rbp - 1112], r10 # spill L137 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 2232], rax # store tag L138
    mov rcx, [rbp - 1080] # reload L133 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L133 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2232] # tag L138 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2232], rax # store tag L138
    mov rcx, [rbp - 1096] # reload L135 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L135 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2232] # tag L138 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2232], rax # store tag L138
    mov rcx, [rbp - 1112] # reload L137 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L137 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 2232] # tag L138 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 2
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 2232], rax # store tag L138
    mov [rbp - 1120], r10 # spill L138 to slot
    mov rdx, [rbp - 1120] # reload L138 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 2232] # tag L138 from tag-slot
    add rsp, 2192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_bad
    .p2align 4
_re_bad:
    .loc 1 416 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 80 # prologue: alloc spill frame
.Lb2dd__re_bad_bb0:
    call hexa_array_new # array_lit: new array
    mov rbx, rdx # array_lit: capture new array payload
    mov [rbp - 72], rax # store tag L0
    call hexa_array_new # array_lit: new array
    mov r12, rdx # array_lit: capture new array payload
    mov [rbp - 80], rax # store tag L1
    call hexa_array_new # array_lit: new array
    mov r13, rdx # array_lit: capture new array payload
    mov [rbp - 88], rax # store tag L2
    call hexa_array_new # array_lit: new array
    mov r14, rdx # array_lit: capture new array payload
    mov [rbp - 96], rax # store tag L3
    call hexa_array_new # array_lit: new array
    mov r15, rdx # array_lit: capture new array payload
    mov [rbp - 104], rax # store tag L4
    call bt_empty # call bt_empty
    mov [rbp - 112], rax # store tag L5
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 56], r10 # spill L5 to slot
    call hexa_map_new # struct_lit: new map
    mov r10, rdx # struct_lit: capture new map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, 0 # hv arg payload
    mov rcx, 2 # tag const_bool = TAG_BOOL
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, -1 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr18] # map key (const char*): &.LCstr18
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, 0 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr19] # map key (const char*): &.LCstr19
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, rbx # hv arg payload
    mov rcx, [rbp - 72] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, r12 # hv arg payload
    mov rcx, [rbp - 80] # tag L1 from tag-slot
    lea rdx, [rip+.LCstr14] # map key (const char*): &.LCstr14
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, r13 # hv arg payload
    mov rcx, [rbp - 88] # tag L2 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, r14 # hv arg payload
    mov rcx, [rbp - 96] # tag L3 from tag-slot
    lea rdx, [rip+.LCstr16] # map key (const char*): &.LCstr16
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, r15 # hv arg payload
    mov rcx, [rbp - 104] # tag L4 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, 0 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr20] # map key (const char*): &.LCstr20
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov r8, [rbp - 56] # reload L5 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 112] # tag L5 from tag-slot
    lea rdx, [rip+.LCstr21] # map key (const char*): &.LCstr21
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 120] # tag L6 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 120], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rdx, [rbp - 64] # reload L6 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 120] # tag L6 from tag-slot
    add rsp, 80 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_from_bt
    .p2align 4
_re_from_bt:
    .loc 1 421 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 144 # prologue: alloc spill frame
    mov [rbp - 104], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_from_bt_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 104] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r12, rdx # field: capture result payload
    mov [rbp - 112], rax # store tag L1
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 112] # tag L1 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r13, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 120], r11 # store tag L2
    test r13, r13 # br_cond test
    jz .Lb2dd__re_from_bt_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_from_bt_bb1 # jump -> then
.Lb2dd__re_from_bt_bb1:
    call _re_bad # call _re_bad
    mov [rbp - 136], rax # store tag L4
    mov r15, rdx # hv: unbox user-call result payload
    mov rdx, r15 # hv arg payload
    mov rax, [rbp - 136] # tag L4 from tag-slot
    add rsp, 144 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_from_bt_bb2:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 144], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 152], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 160], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 168], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 176], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    call hexa_map_new # struct_lit: new map
    mov r10, rdx # struct_lit: capture new map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, 1 # hv arg payload
    mov rcx, 2 # tag const_bool = TAG_BOOL
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, -1 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr18] # map key (const char*): &.LCstr18
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, 0 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr19] # map key (const char*): &.LCstr19
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, [rbp - 56] # reload L5 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 144] # tag L5 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, [rbp - 64] # reload L6 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 152] # tag L6 from tag-slot
    lea rdx, [rip+.LCstr14] # map key (const char*): &.LCstr14
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, [rbp - 72] # reload L7 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 160] # tag L7 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, [rbp - 80] # reload L8 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 168] # tag L8 from tag-slot
    lea rdx, [rip+.LCstr16] # map key (const char*): &.LCstr16
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, [rbp - 88] # reload L9 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 176] # tag L9 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, 1 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr20] # map key (const char*): &.LCstr20
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov r8, rbx # hv arg payload
    mov rcx, [rbp - 104] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr21] # map key (const char*): &.LCstr21
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 184] # tag L10 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 184], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov rdx, [rbp - 96] # reload L10 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 184] # tag L10 from tag-slot
    add rsp, 144 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_compile
    .p2align 4
regex_compile:
    .loc 1 427 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 32 # prologue: alloc spill frame
    mov [rbp - 56], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd_regex_compile_bb0:
    call _REGEX_DEFAULT_STEP_CAP # call _REGEX_DEFAULT_STEP_CAP
    mov [rbp - 64], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 56] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call regex_compile_capped # call regex_compile_capped
    mov [rbp - 72], rax # store tag L2
    mov r13, rdx # hv: unbox user-call result payload
    mov rdx, r13 # hv arg payload
    mov rax, [rbp - 72] # tag L2 from tag-slot
    add rsp, 32 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_compile_capped
    .p2align 4
regex_compile_capped:
    .loc 1 436 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 752 # prologue: alloc spill frame
    mov [rbp - 408], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 416], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_regex_compile_capped_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 408] # tag L0 from tag-slot
    call bt_needs_backtrack # call bt_needs_backtrack
    mov [rbp - 424], rax # store tag L2
    mov r13, rdx # hv: unbox user-call result payload
    test r13, r13 # br_cond test
    jz .Lb2dd_regex_compile_capped_bb2 # jump-if-zero -> else
    jmp .Lb2dd_regex_compile_capped_bb1 # jump -> then
.Lb2dd_regex_compile_capped_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 408] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call bt_compile # call bt_compile
    mov [rbp - 440], rax # store tag L4
    mov r15, rdx # hv: unbox user-call result payload
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 440] # tag L4 from tag-slot
    call _re_from_bt # call _re_from_bt
    mov [rbp - 448], rax # store tag L5
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov rdx, [rbp - 56] # reload L5 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 448] # tag L5 from tag-slot
    add rsp, 752 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_compile_capped_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 408] # tag L0 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 456], rax # store tag L6
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 64], r10 # spill L6 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 464], rax # store tag L7
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 464] # tag L7 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 464], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 472], rax # store tag L8
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 472] # tag L8 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 472], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 480], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 488], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    call hexa_map_new # struct_lit: new map
    mov r10, rdx # struct_lit: capture new map payload
    mov [rbp - 496], rax # store tag L11
    mov r8, rbx # hv arg payload
    mov rcx, [rbp - 408] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr2] # map key (const char*): &.LCstr2
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 496] # tag L11 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 496], rax # store tag L11
    mov r8, [rbp - 64] # reload L6 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 456] # tag L6 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 496] # tag L11 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 496], rax # store tag L11
    mov r8, [rbp - 72] # reload L7 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 464] # tag L7 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 496] # tag L11 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 496], rax # store tag L11
    mov r8, [rbp - 80] # reload L8 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 472] # tag L8 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 496] # tag L11 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 496], rax # store tag L11
    mov r8, [rbp - 88] # reload L9 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 480] # tag L9 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 496] # tag L11 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 496], rax # store tag L11
    mov r8, [rbp - 96] # reload L10 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 488] # tag L10 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 496] # tag L11 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 496], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 496] # tag L11 from tag-slot
    mov [rbp - 504], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L12 from tag-slot
    call _re_parse_alt # call _re_parse_alt
    mov [rbp - 512], rax # store tag L13
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov [rbp - 520], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L12 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 528], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 528] # tag L15 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 536], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L16 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 544], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_compile_capped_bb4 # jump-if-zero -> else
    jmp .Lb2dd_regex_compile_capped_bb3 # jump -> then
.Lb2dd_regex_compile_capped_bb3:
    call _re_bad # call _re_bad
    mov [rbp - 560], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov rdx, [rbp - 168] # reload L19 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 560] # tag L19 from tag-slot
    add rsp, 752 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_compile_capped_bb4:
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L12 from tag-slot
    lea rdx, [rip+.LCstr0] # map key (const char*): &.LCstr0
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 568], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov rsi, [rbp - 176] # reload L20 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 568] # tag L20 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 576], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L12 from tag-slot
    lea rdx, [rip+.LCstr1] # map key (const char*): &.LCstr1
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 584], rax # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 184] # reload L21 from spill slot
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov rsi, [rbp - 184] # reload L21 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 576] # tag L21 from tag-slot
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 584] # tag L22 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 592], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_compile_capped_bb6 # jump-if-zero -> else
    jmp .Lb2dd_regex_compile_capped_bb5 # jump -> then
.Lb2dd_regex_compile_capped_bb5:
    call _re_bad # call _re_bad
    mov [rbp - 608], rax # store tag L25
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 216], r10 # spill L25 to slot
    mov rdx, [rbp - 216] # reload L25 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 608] # tag L25 from tag-slot
    add rsp, 752 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_compile_capped_bb6:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 616], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 624], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 632], rax # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 640], rax # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    call hexa_map_new # struct_lit: new map
    mov r10, rdx # struct_lit: capture new map payload
    mov [rbp - 648], rax # store tag L30
    mov r8, [rbp - 224] # reload L26 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 616] # tag L26 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 648] # tag L30 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 648], rax # store tag L30
    mov r8, [rbp - 232] # reload L27 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 624] # tag L27 from tag-slot
    lea rdx, [rip+.LCstr14] # map key (const char*): &.LCstr14
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 648] # tag L30 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 648], rax # store tag L30
    mov r8, [rbp - 240] # reload L28 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 632] # tag L28 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 648] # tag L30 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 648], rax # store tag L30
    mov r8, [rbp - 248] # reload L29 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 640] # tag L29 from tag-slot
    lea rdx, [rip+.LCstr16] # map key (const char*): &.LCstr16
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 648] # tag L30 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 648], rax # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov r11, [rbp - 256] # reload L30 from spill slot
    mov r10, r11 # assign L31
    mov r11, [rbp - 648] # tag L30 from tag-slot
    mov [rbp - 656], r11 # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L12 from tag-slot
    lea rdx, [rip+.LCstr3] # map key (const char*): &.LCstr3
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 664], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 656] # tag L31 from tag-slot
    mov rcx, [rbp - 272] # reload L32 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 664] # tag L32 from tag-slot
    mov r9, [rbp - 128] # reload L14 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L14 = TAG_INT (i64-local, fused)
    call _re_compile_node # call _re_compile_node
    mov [rbp - 672], rax # store tag L33
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 280], r10 # spill L33 to slot
    mov r11, [rbp - 280] # reload L33 from spill slot
    mov r10, r11 # assign L34
    mov r11, [rbp - 672] # tag L33 from tag-slot
    mov [rbp - 680], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 656] # tag L31 from tag-slot
    mov rcx, 4 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _nfa_new # call _nfa_new
    mov [rbp - 688], rax # store tag L35
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 296], r10 # spill L35 to slot
    mov r11, [rbp - 296] # reload L35 from spill slot
    mov r10, r11 # assign L36
    mov r11, 0 # tag L35 = TAG_INT (i64-local, fused)
    mov [rbp - 696], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov rsi, [rbp - 288] # reload L34 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 680] # tag L34 from tag-slot
    call _frag_dangle # call _frag_dangle
    mov [rbp - 704], rax # store tag L37
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 312], r10 # spill L37 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 656] # tag L31 from tag-slot
    mov rcx, [rbp - 312] # reload L37 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 704] # tag L37 from tag-slot
    mov r9, [rbp - 304] # reload L36 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L36 = TAG_INT (i64-local, fused)
    call _nfa_patch # call _nfa_patch
    mov [rbp - 712], rax # store tag L38
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 320], r10 # spill L38 to slot
    mov rsi, [rbp - 288] # reload L34 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 680] # tag L34 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 720], rax # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 656] # tag L31 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 728], rax # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov rsi, [rbp - 336] # reload L40 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 728] # tag L40 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 736], r11 # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 656] # tag L31 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 744], rax # store tag L42
    mov [rbp - 352], r10 # spill L42 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 656] # tag L31 from tag-slot
    lea rdx, [rip+.LCstr14] # map key (const char*): &.LCstr14
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 752], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 656] # tag L31 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 760], rax # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 656] # tag L31 from tag-slot
    lea rdx, [rip+.LCstr16] # map key (const char*): &.LCstr16
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 768], rax # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L12 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 776], rax # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    call bt_empty # call bt_empty
    mov [rbp - 784], rax # store tag L47
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 392], r10 # spill L47 to slot
    call hexa_map_new # struct_lit: new map
    mov r10, rdx # struct_lit: capture new map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, 1 # hv arg payload
    mov rcx, 2 # tag const_bool = TAG_BOOL
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, [rbp - 328] # reload L39 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 720] # tag L39 from tag-slot
    lea rdx, [rip+.LCstr18] # map key (const char*): &.LCstr18
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, [rbp - 344] # reload L41 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 736] # tag L41 from tag-slot
    lea rdx, [rip+.LCstr19] # map key (const char*): &.LCstr19
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, [rbp - 352] # reload L42 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 744] # tag L42 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, [rbp - 360] # reload L43 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 752] # tag L43 from tag-slot
    lea rdx, [rip+.LCstr14] # map key (const char*): &.LCstr14
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, [rbp - 368] # reload L44 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 760] # tag L44 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, [rbp - 376] # reload L45 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 768] # tag L45 from tag-slot
    lea rdx, [rip+.LCstr16] # map key (const char*): &.LCstr16
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, [rbp - 384] # reload L46 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 776] # tag L46 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, 0 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    lea rdx, [rip+.LCstr20] # map key (const char*): &.LCstr20
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov r8, [rbp - 392] # reload L47 from spill slot
    mov r8, r8 # hv arg payload
    mov rcx, [rbp - 784] # tag L47 from tag-slot
    lea rdx, [rip+.LCstr21] # map key (const char*): &.LCstr21
    mov rsi, r10 # struct_lit: map payload
    mov rdi, [rbp - 792] # tag L48 from tag-slot
    call hexa_map_set # struct_lit: set field
    mov r10, rdx # struct_lit: recapture map payload
    mov [rbp - 792], rax # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    mov rdx, [rbp - 400] # reload L48 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 792] # tag L48 from tag-slot
    add rsp, 752 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_compile_captures
    .p2align 4
regex_compile_captures:
    .loc 1 484 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 32 # prologue: alloc spill frame
    mov [rbp - 56], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd_regex_compile_captures_bb0:
    call _REGEX_DEFAULT_STEP_CAP # call _REGEX_DEFAULT_STEP_CAP
    mov [rbp - 64], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 56] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call regex_compile_captures_capped # call regex_compile_captures_capped
    mov [rbp - 72], rax # store tag L2
    mov r13, rdx # hv: unbox user-call result payload
    mov rdx, r13 # hv arg payload
    mov rax, [rbp - 72] # tag L2 from tag-slot
    add rsp, 32 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_compile_captures_capped
    .p2align 4
regex_compile_captures_capped:
    .loc 1 489 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 32 # prologue: alloc spill frame
    mov [rbp - 56], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 64], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_regex_compile_captures_capped_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 56] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call bt_compile # call bt_compile
    mov [rbp - 72], rax # store tag L2
    mov r13, rdx # hv: unbox user-call result payload
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 72] # tag L2 from tag-slot
    call _re_from_bt # call _re_from_bt
    mov [rbp - 80], rax # store tag L3
    mov r14, rdx # hv: unbox user-call result payload
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 80] # tag L3 from tag-slot
    add rsp, 32 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_captures
    .p2align 4
regex_captures:
    .loc 1 498 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 288 # prologue: alloc spill frame
    mov [rbp - 176], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 184], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_regex_captures_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 192], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 192] # tag L2 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r14, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 200], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd_regex_captures_bb2 # jump-if-zero -> else
    jmp .Lb2dd_regex_captures_bb1 # jump -> then
.Lb2dd_regex_captures_bb1:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 216], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov rdx, [rbp - 56] # reload L5 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 216] # tag L5 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_captures_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr20] # map key (const char*): &.LCstr20
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 224], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 224] # tag L6 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 232], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_captures_bb4 # jump-if-zero -> else
    jmp .Lb2dd_regex_captures_bb3 # jump -> then
.Lb2dd_regex_captures_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr21] # map key (const char*): &.LCstr21
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 248], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 248] # tag L9 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 184] # tag L1 from tag-slot
    call bt_search_captures # call bt_search_captures
    mov [rbp - 256], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov rdx, [rbp - 96] # reload L10 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 256] # tag L10 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_captures_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 184] # tag L1 from tag-slot
    call regex_search # call regex_search
    mov [rbp - 264], rax # store tag L11
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 104], r10 # spill L11 to slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 264] # tag L11 from tag-slot
    mov [rbp - 272], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 280], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 280] # tag L13 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 288], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_captures_bb6 # jump-if-zero -> else
    jmp .Lb2dd_regex_captures_bb5 # jump -> then
.Lb2dd_regex_captures_bb5:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 304], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rdx, [rbp - 144] # reload L16 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 304] # tag L16 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_captures_bb6:
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 312], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 272] # tag L12 from tag-slot
    mov rdx, 1 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 320], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 328], rax # store tag L19
    mov rcx, [rbp - 152] # reload L17 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 312] # tag L17 from tag-slot
    mov rdi, [rbp - 328] # tag L19 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 328], rax # store tag L19
    mov rcx, [rbp - 160] # reload L18 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 320] # tag L18 from tag-slot
    mov rdi, [rbp - 328] # tag L19 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 328], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov rdx, [rbp - 168] # reload L19 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 328] # tag L19 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_cap_count
    .p2align 4
regex_cap_count:
    .loc 1 508 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 32 # prologue: alloc spill frame
    mov [rbp - 56], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd_regex_cap_count_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 56] # tag L0 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r12, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 64], r11 # store tag L1
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 64] # tag L1 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_div # binop /: tag-dispatch hexa_div
    mov r13, rdx # binop /: capture result payload
    mov [rbp - 72], rax # store tag L2
    mov rdx, r13 # hv arg payload
    mov rax, 0 # tag L2 = TAG_INT (i64-local, fused)
    add rsp, 32 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_cap_span
    .p2align 4
regex_cap_span:
    .loc 1 514 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 336 # prologue: alloc spill frame
    mov [rbp - 200], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 208], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_regex_cap_span_bb0:
    mov r13, 2 # binop lhs into dst
    imul r13, r12 # binop *
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 216], r11 # store tag L2
    mov r14, r13 # assign L3
    mov r11, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov [rbp - 224], r11 # store tag L3
    cmp r12, 0 # binop <
    setl al # binop < → al
    movzx r15, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 232], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd_regex_cap_span_bb2 # jump-if-zero -> else
    jmp .Lb2dd_regex_cap_span_bb1 # jump -> then
.Lb2dd_regex_cap_span_bb1:
    mov r10, r15 # assign L5
    mov r11, [rbp - 232] # tag L4 from tag-slot
    mov [rbp - 240], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd_regex_cap_span_bb3 # branch
.Lb2dd_regex_cap_span_bb2:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 248], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 256], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 72] # reload L7 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 256] # tag L7 from tag-slot
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 264], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L5
    mov r11, [rbp - 264] # tag L8 from tag-slot
    mov [rbp - 240], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd_regex_cap_span_bb3 # branch
.Lb2dd_regex_cap_span_bb3:
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_cap_span_bb5 # jump-if-zero -> else
    jmp .Lb2dd_regex_cap_span_bb4 # jump -> then
.Lb2dd_regex_cap_span_bb4:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 280], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov rdx, [rbp - 96] # reload L10 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 280] # tag L10 from tag-slot
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_cap_span_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    mov rdx, r14 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 288], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 288] # tag L11 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 296], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_cap_span_bb7 # jump-if-zero -> else
    jmp .Lb2dd_regex_cap_span_bb6 # jump -> then
.Lb2dd_regex_cap_span_bb6:
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L13
    mov r11, [rbp - 296] # tag L12 from tag-slot
    mov [rbp - 304], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_regex_cap_span_bb8 # branch
.Lb2dd_regex_cap_span_bb7:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 312], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    mov rdx, [rbp - 128] # reload L14 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 320], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 320] # tag L15 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 328], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov r10, r11 # assign L13
    mov r11, [rbp - 328] # tag L16 from tag-slot
    mov [rbp - 304], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_regex_cap_span_bb8 # branch
.Lb2dd_regex_cap_span_bb8:
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_cap_span_bb10 # jump-if-zero -> else
    jmp .Lb2dd_regex_cap_span_bb9 # jump -> then
.Lb2dd_regex_cap_span_bb9:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 344], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rdx, [rbp - 160] # reload L18 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 344] # tag L18 from tag-slot
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_cap_span_bb10:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    mov rdx, r14 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 352], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 360], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    mov rdx, [rbp - 176] # reload L20 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 368], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 376], rax # store tag L22
    mov rcx, [rbp - 168] # reload L19 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 352] # tag L19 from tag-slot
    mov rdi, [rbp - 376] # tag L22 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 376], rax # store tag L22
    mov rcx, [rbp - 184] # reload L21 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 368] # tag L21 from tag-slot
    mov rdi, [rbp - 376] # tag L22 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 376], rax # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rdx, [rbp - 192] # reload L22 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 376] # tag L22 from tag-slot
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_cap_text
    .p2align 4
regex_cap_text:
    .loc 1 523 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 144 # prologue: alloc spill frame
    mov [rbp - 104], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 112], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 120], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd_regex_cap_text_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 104] # tag L0 from tag-slot
    mov rcx, r13 # hv arg payload
    mov rdx, 0 # tag L2 = TAG_INT (i64-local, fused)
    call regex_cap_span # call regex_cap_span
    mov [rbp - 128], rax # store tag L3
    mov r14, rdx # hv: unbox user-call result payload
    mov r15, r14 # assign L4
    mov r11, [rbp - 128] # tag L3 from tag-slot
    mov [rbp - 136], r11 # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 136] # tag L4 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 144], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 144] # tag L5 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 152], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_cap_text_bb2 # jump-if-zero -> else
    jmp .Lb2dd_regex_cap_text_bb1 # jump -> then
.Lb2dd_regex_cap_text_bb1:
    lea rdx, [rip+.LCstr24] # hv arg payload: &str .LCstr24
    mov rax, 3 # hv arg tag = TAG_STR
    add rsp, 144 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_cap_text_bb2:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 136] # tag L4 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 168], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 136] # tag L4 from tag-slot
    mov rdx, 1 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 176], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 112] # tag L1 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 168] # tag L8 from tag-slot
    mov r9, [rbp - 88] # reload L9 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 176] # tag L9 from tag-slot
    call hexa_str_substring # call hexa_str_substring
    mov [rbp - 184], rax # store tag L10
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 96], r10 # spill L10 to slot
    mov rdx, [rbp - 96] # reload L10 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 184] # tag L10 from tag-slot
    add rsp, 144 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_class_match
    .p2align 4
_re_class_match:
    .loc 1 543 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 336 # prologue: alloc spill frame
    mov [rbp - 200], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 208], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 216], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd__re_class_match_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 200] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, 0 # tag L1 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r14, rdx # index: capture element payload
    mov [rbp - 224], rax # store tag L3
    mov r15, r14 # assign L4
    mov r11, [rbp - 224] # tag L3 from tag-slot
    mov [rbp - 232], r11 # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 232] # tag L4 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 240], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 240] # tag L5 from tag-slot
    mov [rbp - 248], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, 1 # assign L7
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 256], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, 0 # assign L8
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 264], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    jmp .Lb2dd__re_class_match_bb1 # branch
.Lb2dd__re_class_match_bb1:
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 272], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 232] # tag L4 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 280], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 96] # reload L10 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 280] # tag L10 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 288], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_class_match_bb3 # jump-if-zero -> else
    jmp .Lb2dd__re_class_match_bb2 # jump -> then
.Lb2dd__re_class_match_bb2:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 232] # tag L4 from tag-slot
    mov rcx, [rbp - 72] # reload L7 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L7 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 296], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov rsi, r13 # hv arg payload
    mov rdi, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 112] # reload L12 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 296] # tag L12 from tag-slot
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 304], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_class_match_bb5 # jump-if-zero -> else
    jmp .Lb2dd__re_class_match_bb4 # jump -> then
.Lb2dd__re_class_match_bb3:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 248] # tag L6 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 360], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_class_match_bb10 # jump-if-zero -> else
    jmp .Lb2dd__re_class_match_bb9 # jump -> then
.Lb2dd__re_class_match_bb4:
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 320], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 232] # tag L4 from tag-slot
    mov rcx, [rbp - 136] # reload L15 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L15 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 328], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov rsi, r13 # hv arg payload
    mov rdi, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 144] # reload L16 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 328] # tag L16 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 336], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 336] # tag L17 from tag-slot
    mov [rbp - 312], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__re_class_match_bb6 # branch
.Lb2dd__re_class_match_bb5:
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 304] # tag L13 from tag-slot
    mov [rbp - 312], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__re_class_match_bb6 # branch
.Lb2dd__re_class_match_bb6:
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_class_match_bb8 # jump-if-zero -> else
    jmp .Lb2dd__re_class_match_bb7 # jump -> then
.Lb2dd__re_class_match_bb7:
    mov r10, 1 # assign L8
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 264], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    jmp .Lb2dd__re_class_match_bb8 # branch
.Lb2dd__re_class_match_bb8:
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 352], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L7
    mov r11, 0 # tag L19 = TAG_INT (i64-local, fused)
    mov [rbp - 256], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    jmp .Lb2dd__re_class_match_bb1 # branch
.Lb2dd__re_class_match_bb9:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 264] # tag L8 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 376], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rdx, [rbp - 192] # reload L22 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 376] # tag L22 from tag-slot
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_class_match_bb10:
    mov rdx, [rbp - 80] # reload L8 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 264] # tag L8 from tag-slot
    add rsp, 336 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_zeros
    .p2align 4
_re_zeros:
    .loc 1 557 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 80 # prologue: alloc spill frame
    mov [rbp - 72], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__re_zeros_bb0:
    call hexa_array_new # array_lit: new array
    mov r12, rdx # array_lit: capture new array payload
    mov [rbp - 80], rax # store tag L1
    mov r13, r12 # assign L2
    mov r11, [rbp - 80] # tag L1 from tag-slot
    mov [rbp - 88], r11 # store tag L2
    mov r14, 0 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 96], r11 # store tag L3
    jmp .Lb2dd__re_zeros_bb1 # branch
.Lb2dd__re_zeros_bb1:
    cmp r14, rbx # binop <
    setl al # binop < → al
    movzx r15, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 104], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd__re_zeros_bb3 # jump-if-zero -> else
    jmp .Lb2dd__re_zeros_bb2 # jump -> then
.Lb2dd__re_zeros_bb2:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 88] # tag L2 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_array_push # call hexa_array_push
    mov [rbp - 112], rax # store tag L5
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 120], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rbp - 96], r11 # store tag L3
    jmp .Lb2dd__re_zeros_bb1 # branch
.Lb2dd__re_zeros_bb3:
    mov rdx, r13 # hv arg payload
    mov rax, [rbp - 88] # tag L2 from tag-slot
    add rsp, 80 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_add
    .p2align 4
_re_add:
    .loc 1 566 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 624 # prologue: alloc spill frame
    mov [rbp - 344], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 352], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 360], r8 # store tag L2
    mov r13, r9 # ingress param payload
    mov r10, [rbp + 16] # ingress stack param 3 tag
    mov [rbp - 368], r10 # store tag L3
    mov r10, [rbp + 24] # ingress stack param 3 payload
    mov r14, r10 # ingress stack param payload
    mov r10, [rbp + 32] # ingress stack param 4 tag
    mov [rbp - 376], r10 # store tag L4
    mov r10, [rbp + 40] # ingress stack param 4 payload
    mov r15, r10 # ingress stack param payload
    mov r10, [rbp + 48] # ingress stack param 5 tag
    mov [rbp - 384], r10 # store tag L5
    mov r10, [rbp + 56] # ingress stack param 5 payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp + 64] # ingress stack param 6 tag
    mov [rbp - 392], r10 # store tag L6
    mov r10, [rbp + 72] # ingress stack param 6 payload
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp + 80] # ingress stack param 7 tag
    mov [rbp - 400], r10 # store tag L7
    mov r10, [rbp + 88] # ingress stack param 7 payload
    mov [rbp - 72], r10 # spill L7 to slot
.Lb2dd__re_add_bb0:
    mov r10, [rbp - 56] # reload L5 from spill slot
    cmp r10, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 408], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, [rbp - 80] # reload L8 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_add_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_add_bb1 # jump -> then
.Lb2dd__re_add_bb1:
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 624 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_add_bb2:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 360] # tag L2 from tag-slot
    mov rdx, [rbp - 56] # reload L5 from spill slot
    mov rdx, rdx # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 424], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 424] # tag L10 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 432], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_add_bb4 # jump-if-zero -> else
    jmp .Lb2dd__re_add_bb3 # jump -> then
.Lb2dd__re_add_bb3:
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 624 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_add_bb4:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 360] # tag L2 from tag-slot
    mov rdx, [rbp - 56] # reload L5 from spill slot
    mov rdx, rdx # hv payload
    mov r8, r14 # hv arg payload
    mov rcx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r13, rdx # index_set: store-back poly descriptor
    mov [rbp - 360], rax # store tag L2
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 344] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 448], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 448] # tag L13 from tag-slot
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 456], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r11, [rbp - 128] # reload L14 from spill slot
    mov r10, r11 # assign L15
    mov r11, [rbp - 456] # tag L14 from tag-slot
    mov [rbp - 464], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 464] # tag L15 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 472], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_add_bb6 # jump-if-zero -> else
    jmp .Lb2dd__re_add_bb5 # jump -> then
.Lb2dd__re_add_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 344] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 488], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 488] # tag L18 from tag-slot
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 496], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    sub rsp, 80 # hv: reserve 5 16B stack arg slots
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L7 = TAG_INT (i64-local, fused)
    mov [rsp + 64], r10 # hv stack arg 7 tag
    mov [rsp + 72], r11 # hv stack arg 7 payload
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 496] # tag L19 from tag-slot
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 376] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, r14 # hv arg payload
    mov r10, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 344] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 352] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 360] # tag L2 from tag-slot
    call _re_add # call _re_add
    add rsp, 80 # hv: pop 5 stack arg slots
    mov [rbp - 504], rax # store tag L20
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 176], r10 # spill L20 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 344] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr16] # map key (const char*): &.LCstr16
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 512], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov rsi, [rbp - 184] # reload L21 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 512] # tag L21 from tag-slot
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 520], rax # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    sub rsp, 80 # hv: reserve 5 16B stack arg slots
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L7 = TAG_INT (i64-local, fused)
    mov [rsp + 64], r10 # hv stack arg 7 tag
    mov [rsp + 72], r11 # hv stack arg 7 payload
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 520] # tag L22 from tag-slot
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 376] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, r14 # hv arg payload
    mov r10, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 344] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 352] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 360] # tag L2 from tag-slot
    call _re_add # call _re_add
    add rsp, 80 # hv: pop 5 stack arg slots
    mov [rbp - 528], rax # store tag L23
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 200], r10 # spill L23 to slot
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 624 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_add_bb6:
    mov r10, [rbp - 136] # reload L15 from spill slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 464] # tag L15 from tag-slot
    mov rcx, 5 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 536], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_add_bb8 # jump-if-zero -> else
    jmp .Lb2dd__re_add_bb7 # jump -> then
.Lb2dd__re_add_bb7:
    mov r10, [rbp - 64] # reload L6 from spill slot
    cmp r10, 0 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 552], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r10, [rbp - 224] # reload L26 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_add_bb10 # jump-if-zero -> else
    jmp .Lb2dd__re_add_bb9 # jump -> then
.Lb2dd__re_add_bb8:
    mov r10, [rbp - 136] # reload L15 from spill slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 464] # tag L15 from tag-slot
    mov rcx, 6 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 592], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov r10, [rbp - 264] # reload L31 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_add_bb12 # jump-if-zero -> else
    jmp .Lb2dd__re_add_bb11 # jump -> then
.Lb2dd__re_add_bb9:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 344] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 568], rax # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov rsi, [rbp - 240] # reload L28 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 568] # tag L28 from tag-slot
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 576], rax # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    sub rsp, 80 # hv: reserve 5 16B stack arg slots
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L7 = TAG_INT (i64-local, fused)
    mov [rsp + 64], r10 # hv stack arg 7 tag
    mov [rsp + 72], r11 # hv stack arg 7 payload
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 576] # tag L29 from tag-slot
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 376] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, r14 # hv arg payload
    mov r10, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 344] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 352] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 360] # tag L2 from tag-slot
    call _re_add # call _re_add
    add rsp, 80 # hv: pop 5 stack arg slots
    mov [rbp - 584], rax # store tag L30
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 256], r10 # spill L30 to slot
    jmp .Lb2dd__re_add_bb10 # branch
.Lb2dd__re_add_bb10:
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 624 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_add_bb11:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    cmp r10, r11 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 608], r11 # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r10, [rbp - 280] # reload L33 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_add_bb14 # jump-if-zero -> else
    jmp .Lb2dd__re_add_bb13 # jump -> then
.Lb2dd__re_add_bb12:
    mov r10, [rbp - 136] # reload L15 from spill slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 464] # tag L15 from tag-slot
    mov rcx, 4 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 648], rax # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov r10, [rbp - 320] # reload L38 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_add_bb16 # jump-if-zero -> else
    jmp .Lb2dd__re_add_bb15 # jump -> then
.Lb2dd__re_add_bb13:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 344] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 624], rax # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 624] # tag L35 from tag-slot
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 632], rax # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    sub rsp, 80 # hv: reserve 5 16B stack arg slots
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L7 = TAG_INT (i64-local, fused)
    mov [rsp + 64], r10 # hv stack arg 7 tag
    mov [rsp + 72], r11 # hv stack arg 7 payload
    mov r11, [rbp - 64] # reload L6 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L6 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 304] # reload L36 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 632] # tag L36 from tag-slot
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, r15 # hv arg payload
    mov r10, [rbp - 376] # tag L4 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, r14 # hv arg payload
    mov r10, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 344] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 352] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 360] # tag L2 from tag-slot
    call _re_add # call _re_add
    add rsp, 80 # hv: pop 5 stack arg slots
    mov [rbp - 640], rax # store tag L37
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 312], r10 # spill L37 to slot
    jmp .Lb2dd__re_add_bb14 # branch
.Lb2dd__re_add_bb14:
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 624 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_add_bb15:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 376] # tag L4 from tag-slot
    mov rdx, 0 # hv payload
    mov r8, 1 # hv arg payload
    mov rcx, 0 # tag default = TAG_INT
    call hexa_arr_poly_set # index_set: hexa_arr_poly_set (runtime discriminate)
    mov r15, rdx # index_set: store-back poly descriptor
    mov [rbp - 376], rax # store tag L4
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 624 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_add_bb16:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 352] # tag L1 from tag-slot
    mov rcx, [rbp - 56] # reload L5 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L5 = TAG_INT (i64-local, fused)
    call hexa_arr_poly_push # push: hexa_arr_poly_push (runtime discriminate)
    mov r10, rdx # push: recapture poly descriptor
    mov [rbp - 664], rax # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov rdx, [rbp - 336] # reload L40 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 664] # tag L40 from tag-slot
    add rsp, 624 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _re_longest_from
    .p2align 4
_re_longest_from:
    .loc 1 593 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 960 # prologue: alloc spill frame
    mov [rbp - 512], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 520], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 528], r8 # store tag L2
    mov r13, r9 # ingress param payload
    mov r10, [rbp + 16] # ingress stack param 3 tag
    mov [rbp - 536], r10 # store tag L3
    mov r10, [rbp + 24] # ingress stack param 3 payload
    mov r14, r10 # ingress stack param payload
.Lb2dd__re_longest_from_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 512] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr19] # map key (const char*): &.LCstr19
    call hexa_map_get # field: hexa_map_get
    mov r15, rdx # field: capture result payload
    mov [rbp - 544], rax # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 544] # tag L4 from tag-slot
    call _re_zeros # call _re_zeros
    mov [rbp - 552], rax # store tag L5
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 552] # tag L5 from tag-slot
    mov [rbp - 560], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 568], rax # store tag L7
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov rdi, [rbp - 568] # tag L7 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 568], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L8
    mov r11, [rbp - 568] # tag L7 from tag-slot
    mov [rbp - 576], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, 1 # assign L9
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 584], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 592], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 592] # tag L10 from tag-slot
    mov [rbp - 600], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 512] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr18] # map key (const char*): &.LCstr18
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 608], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    sub rsp, 80 # hv: reserve 5 16B stack arg slots
    mov r11, r14 # hv arg payload
    mov r10, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov [rsp + 64], r10 # hv stack arg 7 tag
    mov [rsp + 72], r11 # hv stack arg 7 payload
    mov r11, r13 # hv arg payload
    mov r10, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 608] # tag L12 from tag-slot
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 576] # tag L8 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 512] # tag L0 from tag-slot
    mov rcx, [rbp - 104] # reload L11 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 600] # tag L11 from tag-slot
    mov r9, [rbp - 64] # reload L6 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 560] # tag L6 from tag-slot
    call _re_add # call _re_add
    add rsp, 80 # hv: pop 5 stack arg slots
    mov [rbp - 616], rax # store tag L13
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, -1 # assign L14
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 624], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 576] # tag L8 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 632], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 632] # tag L15 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 640], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb2 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb1 # jump -> then
.Lb2dd__re_longest_from_bb1:
    mov r10, r13 # assign L14
    mov r11, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov [rbp - 624], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__re_longest_from_bb2 # branch
.Lb2dd__re_longest_from_bb2:
    mov r10, r13 # assign L18
    mov r11, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov [rbp - 656], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__re_longest_from_bb3 # branch
.Lb2dd__re_longest_from_bb3:
    mov r10, [rbp - 160] # reload L18 from spill slot
    cmp r10, r14 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 664], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, [rbp - 168] # reload L19 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb7 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb6 # jump -> then
.Lb2dd__re_longest_from_bb4:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 520] # tag L1 from tag-slot
    mov rcx, [rbp - 160] # reload L18 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L18 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 696], rax # store tag L23
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 200], r10 # spill L23 to slot
    mov r11, [rbp - 200] # reload L23 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 696] # tag L23 from tag-slot
    mov [rbp - 704], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 712], r11 # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L9
    mov r11, 0 # tag L25 = TAG_INT (i64-local, fused)
    mov [rbp - 584], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 576] # tag L8 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call hexa_index_set # index_set: hexa_index_set
    mov r10, rdx # index_set: store-back container payload
    mov [rbp - 576], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 720], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L27
    mov r11, [rbp - 720] # tag L26 from tag-slot
    mov [rbp - 728], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, 0 # assign L28
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 736], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    jmp .Lb2dd__re_longest_from_bb9 # branch
.Lb2dd__re_longest_from_bb5:
    mov rdx, [rbp - 128] # reload L14 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L14 = TAG_INT (i64-local, fused)
    add rsp, 960 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__re_longest_from_bb6:
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 600] # tag L11 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 680], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov r10, [rbp - 184] # reload L21 from spill slot
    mov rsi, [rbp - 184] # reload L21 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 680] # tag L21 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_gt # binop >: tag-dispatch hexa_cmp_gt
    mov r10, rdx # binop >: capture bool payload
    mov [rbp - 688], rax # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 688] # tag L22 from tag-slot
    mov [rbp - 672], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__re_longest_from_bb8 # branch
.Lb2dd__re_longest_from_bb7:
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 664] # tag L19 from tag-slot
    mov [rbp - 672], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__re_longest_from_bb8 # branch
.Lb2dd__re_longest_from_bb8:
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb5 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb4 # jump -> then
.Lb2dd__re_longest_from_bb9:
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 600] # tag L11 from tag-slot
    call hexa_len # call hexa_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 744], r11 # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov rsi, [rbp - 240] # reload L28 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L28 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 248] # reload L29 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 744] # tag L29 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 752], rax # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov r10, [rbp - 256] # reload L30 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb11 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb10 # jump -> then
.Lb2dd__re_longest_from_bb10:
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 600] # tag L11 from tag-slot
    mov rcx, [rbp - 240] # reload L28 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L28 = TAG_INT (i64-local, fused)
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 760], rax # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov r11, [rbp - 264] # reload L31 from spill slot
    mov r10, r11 # assign L32
    mov r11, [rbp - 760] # tag L31 from tag-slot
    mov [rbp - 768], r11 # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 512] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr13] # map key (const char*): &.LCstr13
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 776], rax # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov rsi, [rbp - 280] # reload L33 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 776] # tag L33 from tag-slot
    mov rcx, [rbp - 272] # reload L32 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 768] # tag L32 from tag-slot
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 784], rax # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov r11, [rbp - 288] # reload L34 from spill slot
    mov r10, r11 # assign L35
    mov r11, [rbp - 784] # tag L34 from tag-slot
    mov [rbp - 792], r11 # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov r10, 0 # assign L36
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 800], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov r10, [rbp - 296] # reload L35 from spill slot
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 792] # tag L35 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 808], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb13 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb12 # jump -> then
.Lb2dd__re_longest_from_bb11:
    mov r11, [rbp - 232] # reload L27 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 728] # tag L27 from tag-slot
    mov [rbp - 600], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 160] # reload L18 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 976], r11 # store tag L58
    mov [rbp - 480], r10 # spill L58 to slot
    mov r11, [rbp - 480] # reload L58 from spill slot
    mov r10, r11 # assign L18
    mov r11, 0 # tag L58 = TAG_INT (i64-local, fused)
    mov [rbp - 656], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 576] # tag L8 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 984], rax # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    mov r10, [rbp - 488] # reload L59 from spill slot
    mov rsi, [rbp - 488] # reload L59 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 984] # tag L59 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 992], rax # store tag L60
    mov [rbp - 496], r10 # spill L60 to slot
    mov r10, [rbp - 496] # reload L60 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb27 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb26 # jump -> then
.Lb2dd__re_longest_from_bb12:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 512] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr14] # map key (const char*): &.LCstr14
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 824], rax # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov rsi, [rbp - 328] # reload L39 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 824] # tag L39 from tag-slot
    mov rcx, [rbp - 272] # reload L32 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 768] # tag L32 from tag-slot
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 832], rax # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    mov r11, [rbp - 208] # reload L24 from spill slot
    mov rsi, [rbp - 336] # reload L40 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 832] # tag L40 from tag-slot
    mov rcx, [rbp - 208] # reload L24 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 704] # tag L24 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 840], rax # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r10, [rbp - 344] # reload L41 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb15 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb14 # jump -> then
.Lb2dd__re_longest_from_bb13:
    mov r10, [rbp - 296] # reload L35 from spill slot
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 792] # tag L35 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 856], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov r10, [rbp - 360] # reload L43 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb17 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb16 # jump -> then
.Lb2dd__re_longest_from_bb14:
    mov r10, 1 # assign L36
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 800], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    jmp .Lb2dd__re_longest_from_bb15 # branch
.Lb2dd__re_longest_from_bb15:
    jmp .Lb2dd__re_longest_from_bb23 # branch
.Lb2dd__re_longest_from_bb16:
    mov r10, 1 # assign L36
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 800], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    jmp .Lb2dd__re_longest_from_bb22 # branch
.Lb2dd__re_longest_from_bb17:
    mov r10, [rbp - 296] # reload L35 from spill slot
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 792] # tag L35 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 872], rax # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov r10, [rbp - 376] # reload L45 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb19 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb18 # jump -> then
.Lb2dd__re_longest_from_bb18:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 512] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr4] # map key (const char*): &.LCstr4
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 888], rax # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 512] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr14] # map key (const char*): &.LCstr14
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 896], rax # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    mov rsi, [rbp - 400] # reload L48 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 896] # tag L48 from tag-slot
    mov rcx, [rbp - 272] # reload L32 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 768] # tag L32 from tag-slot
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 904], rax # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov rsi, [rbp - 392] # reload L47 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 888] # tag L47 from tag-slot
    mov rcx, [rbp - 408] # reload L49 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 904] # tag L49 from tag-slot
    mov r9, [rbp - 208] # reload L24 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 704] # tag L24 from tag-slot
    call _re_class_match # call _re_class_match
    mov [rbp - 912], rax # store tag L50
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 416], r10 # spill L50 to slot
    mov r10, [rbp - 416] # reload L50 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb21 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb20 # jump -> then
.Lb2dd__re_longest_from_bb19:
    jmp .Lb2dd__re_longest_from_bb22 # branch
.Lb2dd__re_longest_from_bb20:
    mov r10, 1 # assign L36
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 800], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    jmp .Lb2dd__re_longest_from_bb21 # branch
.Lb2dd__re_longest_from_bb21:
    jmp .Lb2dd__re_longest_from_bb19 # branch
.Lb2dd__re_longest_from_bb22:
    jmp .Lb2dd__re_longest_from_bb23 # branch
.Lb2dd__re_longest_from_bb23:
    mov r10, [rbp - 304] # reload L36 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__re_longest_from_bb25 # jump-if-zero -> else
    jmp .Lb2dd__re_longest_from_bb24 # jump -> then
.Lb2dd__re_longest_from_bb24:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 512] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr15] # map key (const char*): &.LCstr15
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 936], rax # store tag L53
    mov [rbp - 440], r10 # spill L53 to slot
    mov rsi, [rbp - 440] # reload L53 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 936] # tag L53 from tag-slot
    mov rcx, [rbp - 272] # reload L32 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 768] # tag L32 from tag-slot
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 944], rax # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov r10, [rbp - 160] # reload L18 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 952], r11 # store tag L55
    mov [rbp - 456], r10 # spill L55 to slot
    sub rsp, 80 # hv: reserve 5 16B stack arg slots
    mov r11, r14 # hv arg payload
    mov r10, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov [rsp + 64], r10 # hv stack arg 7 tag
    mov [rsp + 72], r11 # hv stack arg 7 payload
    mov r11, [rbp - 456] # reload L55 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L55 = TAG_INT (i64-local, fused)
    mov [rsp + 48], r10 # hv stack arg 6 tag
    mov [rsp + 56], r11 # hv stack arg 6 payload
    mov r11, [rbp - 448] # reload L54 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 944] # tag L54 from tag-slot
    mov [rsp + 32], r10 # hv stack arg 5 tag
    mov [rsp + 40], r11 # hv stack arg 5 payload
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 576] # tag L8 from tag-slot
    mov [rsp + 16], r10 # hv stack arg 4 tag
    mov [rsp + 24], r11 # hv stack arg 4 payload
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 512] # tag L0 from tag-slot
    mov rcx, [rbp - 232] # reload L27 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 728] # tag L27 from tag-slot
    mov r9, [rbp - 64] # reload L6 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 560] # tag L6 from tag-slot
    call _re_add # call _re_add
    add rsp, 80 # hv: pop 5 stack arg slots
    mov [rbp - 960], rax # store tag L56
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 464], r10 # spill L56 to slot
    jmp .Lb2dd__re_longest_from_bb25 # branch
.Lb2dd__re_longest_from_bb25:
    mov r10, [rbp - 240] # reload L28 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 968], r11 # store tag L57
    mov [rbp - 472], r10 # spill L57 to slot
    mov r11, [rbp - 472] # reload L57 from spill slot
    mov r10, r11 # assign L28
    mov r11, 0 # tag L57 = TAG_INT (i64-local, fused)
    mov [rbp - 736], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    jmp .Lb2dd__re_longest_from_bb9 # branch
.Lb2dd__re_longest_from_bb26:
    mov r11, [rbp - 160] # reload L18 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L18 = TAG_INT (i64-local, fused)
    mov [rbp - 624], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd__re_longest_from_bb27 # branch
.Lb2dd__re_longest_from_bb27:
    jmp .Lb2dd__re_longest_from_bb3 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 960 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_valid
    .p2align 4
regex_valid:
    .loc 1 630 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 16 # prologue: alloc spill frame
    mov [rbp - 56], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd_regex_valid_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 56] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r12, rdx # field: capture result payload
    mov [rbp - 64], rax # store tag L1
    mov rdx, r12 # hv arg payload
    mov rax, [rbp - 64] # tag L1 from tag-slot
    add rsp, 16 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_last_error
    .p2align 4
regex_last_error:
    .loc 1 638 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 80 # prologue: alloc spill frame
    mov [rbp - 72], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd_regex_last_error_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 72] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr20] # map key (const char*): &.LCstr20
    call hexa_map_get # field: hexa_map_get
    mov r12, rdx # field: capture result payload
    mov [rbp - 80], rax # store tag L1
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 80] # tag L1 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r13, rdx # binop ==: capture bool payload
    mov [rbp - 88], rax # store tag L2
    test r13, r13 # br_cond test
    jz .Lb2dd_regex_last_error_bb2 # jump-if-zero -> else
    jmp .Lb2dd_regex_last_error_bb1 # jump -> then
.Lb2dd_regex_last_error_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 72] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr21] # map key (const char*): &.LCstr21
    call hexa_map_get # field: hexa_map_get
    mov r15, rdx # field: capture result payload
    mov [rbp - 104], rax # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 104] # tag L4 from tag-slot
    lea rdx, [rip+.LCstr11] # map key (const char*): &.LCstr11
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 112], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 112] # tag L5 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 120], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov rdx, [rbp - 64] # reload L6 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 120] # tag L6 from tag-slot
    add rsp, 80 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_last_error_bb2:
    mov rdx, 0 # hv arg payload
    mov rax, 0 # tag default = TAG_INT
    add rsp, 80 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_match
    .p2align 4
regex_match:
    .loc 1 645 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 192 # prologue: alloc spill frame
    mov [rbp - 128], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 136], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_regex_match_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 144], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 144] # tag L2 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r14, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 152], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd_regex_match_bb2 # jump-if-zero -> else
    jmp .Lb2dd_regex_match_bb1 # jump -> then
.Lb2dd_regex_match_bb1:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_match_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr20] # map key (const char*): &.LCstr20
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 168], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 168] # tag L5 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 176], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_match_bb4 # jump-if-zero -> else
    jmp .Lb2dd_regex_match_bb3 # jump -> then
.Lb2dd_regex_match_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 128] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr21] # map key (const char*): &.LCstr21
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 192], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 192] # tag L8 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 136] # tag L1 from tag-slot
    call bt_match_full # call bt_match_full
    mov [rbp - 200], rax # store tag L9
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 88], r10 # spill L9 to slot
    mov rdx, [rbp - 88] # reload L9 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 200] # tag L9 from tag-slot
    add rsp, 192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_match_bb4:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 136] # tag L1 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 208], rax # store tag L10
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 208] # tag L10 from tag-slot
    mov [rbp - 216], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    sub rsp, 16 # hv: reserve 1 16B stack arg slots
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 216] # tag L11 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 128] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 136] # tag L1 from tag-slot
    mov r9, 0 # hv arg payload
    mov r8, 0 # tag default = TAG_INT
    call _re_longest_from # call _re_longest_from
    add rsp, 16 # hv: pop 1 stack arg slots
    mov [rbp - 224], rax # store tag L12
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L12 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 104] # reload L11 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 216] # tag L11 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 232], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rdx, [rbp - 120] # reload L13 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 232] # tag L13 from tag-slot
    add rsp, 192 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_search
    .p2align 4
regex_search:
    .loc 1 655 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 320 # prologue: alloc spill frame
    mov [rbp - 192], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 200], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_regex_search_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 192] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r13, rdx # field: capture result payload
    mov [rbp - 208], rax # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 208] # tag L2 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r14, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 216], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd_regex_search_bb2 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_bb1 # jump -> then
.Lb2dd_regex_search_bb1:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 232], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov rdx, [rbp - 56] # reload L5 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 232] # tag L5 from tag-slot
    add rsp, 320 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_search_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 192] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr20] # map key (const char*): &.LCstr20
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 240], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 240] # tag L6 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 248], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_bb4 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_bb3 # jump -> then
.Lb2dd_regex_search_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 192] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr21] # map key (const char*): &.LCstr21
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 264], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 264] # tag L9 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 200] # tag L1 from tag-slot
    call bt_search # call bt_search
    mov [rbp - 272], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov rdx, [rbp - 96] # reload L10 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 272] # tag L10 from tag-slot
    add rsp, 320 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_search_bb4:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 200] # tag L1 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 280], rax # store tag L11
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 104], r10 # spill L11 to slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 280] # tag L11 from tag-slot
    mov [rbp - 288], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, 0 # assign L13
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 296], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_regex_search_bb5 # branch
.Lb2dd_regex_search_bb5:
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 112] # reload L12 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 288] # tag L12 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 304], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_bb7 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_bb6 # jump -> then
.Lb2dd_regex_search_bb6:
    sub rsp, 16 # hv: reserve 1 16B stack arg slots
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 288] # tag L12 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 192] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 200] # tag L1 from tag-slot
    mov r9, [rbp - 120] # reload L13 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L13 = TAG_INT (i64-local, fused)
    call _re_longest_from # call _re_longest_from
    add rsp, 16 # hv: pop 1 stack arg slots
    mov [rbp - 312], rax # store tag L15
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L16
    mov r11, 0 # tag L15 = TAG_INT (i64-local, fused)
    mov [rbp - 320], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov r10, [rbp - 144] # reload L16 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 328], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_bb9 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_bb8 # jump -> then
.Lb2dd_regex_search_bb7:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 360], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov rdx, [rbp - 184] # reload L21 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 360] # tag L21 from tag-slot
    add rsp, 320 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_search_bb8:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 344], rax # store tag L19
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L13 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 344] # tag L19 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 344], rax # store tag L19
    mov rcx, [rbp - 144] # reload L16 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L16 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 344] # tag L19 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 344], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov rdx, [rbp - 168] # reload L19 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 344] # tag L19 from tag-slot
    add rsp, 320 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_search_bb9:
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 352], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov r11, [rbp - 176] # reload L20 from spill slot
    mov r10, r11 # assign L13
    mov r11, 0 # tag L20 = TAG_INT (i64-local, fused)
    mov [rbp - 296], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd_regex_search_bb5 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 320 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_find_all
    .p2align 4
regex_find_all:
    .loc 1 671 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 384 # prologue: alloc spill frame
    mov [rbp - 224], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 232], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_regex_find_all_bb0:
    call hexa_array_new # array_lit: new array
    mov r13, rdx # array_lit: capture new array payload
    mov [rbp - 240], rax # store tag L2
    mov r14, r13 # assign L3
    mov r11, [rbp - 240] # tag L2 from tag-slot
    mov [rbp - 248], r11 # store tag L3
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 224] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r15, rdx # field: capture result payload
    mov [rbp - 256], rax # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 256] # tag L4 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 264], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_find_all_bb2 # jump-if-zero -> else
    jmp .Lb2dd_regex_find_all_bb1 # jump -> then
.Lb2dd_regex_find_all_bb1:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 248] # tag L3 from tag-slot
    add rsp, 384 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_find_all_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 224] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr20] # map key (const char*): &.LCstr20
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 280], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 280] # tag L7 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 288], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, [rbp - 80] # reload L8 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_find_all_bb4 # jump-if-zero -> else
    jmp .Lb2dd_regex_find_all_bb3 # jump -> then
.Lb2dd_regex_find_all_bb3:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 224] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr21] # map key (const char*): &.LCstr21
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 304], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 304] # tag L10 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 232] # tag L1 from tag-slot
    call bt_find_all # call bt_find_all
    mov [rbp - 312], rax # store tag L11
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 104], r10 # spill L11 to slot
    mov rdx, [rbp - 104] # reload L11 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 312] # tag L11 from tag-slot
    add rsp, 384 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_find_all_bb4:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 232] # tag L1 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 320], rax # store tag L12
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 112], r10 # spill L12 to slot
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L13
    mov r11, [rbp - 320] # tag L12 from tag-slot
    mov [rbp - 328], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, 0 # assign L14
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 336], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd_regex_find_all_bb5 # branch
.Lb2dd_regex_find_all_bb5:
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L14 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 328] # tag L13 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 344], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_find_all_bb7 # jump-if-zero -> else
    jmp .Lb2dd_regex_find_all_bb6 # jump -> then
.Lb2dd_regex_find_all_bb6:
    sub rsp, 16 # hv: reserve 1 16B stack arg slots
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 328] # tag L13 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 224] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 232] # tag L1 from tag-slot
    mov r9, [rbp - 128] # reload L14 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L14 = TAG_INT (i64-local, fused)
    call _re_longest_from # call _re_longest_from
    add rsp, 16 # hv: pop 1 stack arg slots
    mov [rbp - 352], rax # store tag L16
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 144], r10 # spill L16 to slot
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov r10, r11 # assign L17
    mov r11, 0 # tag L16 = TAG_INT (i64-local, fused)
    mov [rbp - 360], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 368], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov r10, [rbp - 160] # reload L18 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_find_all_bb9 # jump-if-zero -> else
    jmp .Lb2dd_regex_find_all_bb8 # jump -> then
.Lb2dd_regex_find_all_bb7:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 248] # tag L3 from tag-slot
    add rsp, 384 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_find_all_bb8:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 384], rax # store tag L20
    mov rcx, [rbp - 128] # reload L14 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L14 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 384] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 384], rax # store tag L20
    mov rcx, [rbp - 152] # reload L17 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L17 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 384] # tag L20 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 384], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 248] # tag L3 from tag-slot
    mov rcx, [rbp - 176] # reload L20 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 384] # tag L20 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 392], rax # store tag L21
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 184], r10 # spill L21 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    mov r11, [rbp - 128] # reload L14 from spill slot
    cmp r10, r11 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 400], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_find_all_bb11 # jump-if-zero -> else
    jmp .Lb2dd_regex_find_all_bb10 # jump -> then
.Lb2dd_regex_find_all_bb9:
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 424], r11 # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L25 = TAG_INT (i64-local, fused)
    mov [rbp - 336], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd_regex_find_all_bb13 # branch
.Lb2dd_regex_find_all_bb10:
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L17 = TAG_INT (i64-local, fused)
    mov [rbp - 336], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd_regex_find_all_bb12 # branch
.Lb2dd_regex_find_all_bb11:
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 416], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r11, [rbp - 208] # reload L24 from spill slot
    mov r10, r11 # assign L14
    mov r11, 0 # tag L24 = TAG_INT (i64-local, fused)
    mov [rbp - 336], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    jmp .Lb2dd_regex_find_all_bb12 # branch
.Lb2dd_regex_find_all_bb12:
    jmp .Lb2dd_regex_find_all_bb13 # branch
.Lb2dd_regex_find_all_bb13:
    jmp .Lb2dd_regex_find_all_bb5 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 384 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden regex_search_from
    .p2align 4
regex_search_from:
    .loc 1 704 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 496 # prologue: alloc spill frame
    mov [rbp - 280], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 288], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 296], r8 # store tag L2
    mov r13, r9 # ingress param payload
    mov r10, [rbp + 16] # ingress stack param 3 tag
    mov [rbp - 304], r10 # store tag L3
    mov r10, [rbp + 24] # ingress stack param 3 payload
    mov r14, r10 # ingress stack param payload
.Lb2dd_regex_search_from_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr5] # map key (const char*): &.LCstr5
    call hexa_map_get # field: hexa_map_get
    mov r15, rdx # field: capture result payload
    mov [rbp - 312], rax # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 312] # tag L4 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 320], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_from_bb2 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_from_bb1 # jump -> then
.Lb2dd_regex_search_from_bb1:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 336], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov rdx, [rbp - 72] # reload L7 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 336] # tag L7 from tag-slot
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_search_from_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 288] # tag L1 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 344], rax # store tag L8
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L9
    mov r11, [rbp - 344] # tag L8 from tag-slot
    mov [rbp - 352], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    cmp r13, 0 # binop <
    setl al # binop < → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 360], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_from_bb4 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_from_bb3 # jump -> then
.Lb2dd_regex_search_from_bb3:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 376], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rdx, [rbp - 112] # reload L12 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 376] # tag L12 from tag-slot
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_search_from_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr20] # map key (const char*): &.LCstr20
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 384], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 384] # tag L13 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 392], rax # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_from_bb6 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_from_bb5 # jump -> then
.Lb2dd_regex_search_from_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    lea rdx, [rip+.LCstr21] # map key (const char*): &.LCstr21
    call hexa_map_get # field: hexa_map_get
    mov r10, rdx # field: capture result payload
    mov [rbp - 408], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 408] # tag L16 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 288] # tag L1 from tag-slot
    mov r9, r13 # hv arg payload
    mov r8, 0 # tag L2 = TAG_INT (i64-local, fused)
    call bt_search_from # call bt_search_from
    mov [rbp - 416], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov rdx, [rbp - 152] # reload L17 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 416] # tag L17 from tag-slot
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_search_from_bb6:
    mov r10, r13 # assign L18
    mov r11, 0 # tag L2 = TAG_INT (i64-local, fused)
    mov [rbp - 424], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd_regex_search_from_bb7 # branch
.Lb2dd_regex_search_from_bb7:
    mov r10, [rbp - 160] # reload L18 from spill slot
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L18 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 88] # reload L9 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 352] # tag L9 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 432], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, [rbp - 168] # reload L19 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_from_bb9 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_from_bb8 # jump -> then
.Lb2dd_regex_search_from_bb8:
    sub rsp, 16 # hv: reserve 1 16B stack arg slots
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 352] # tag L9 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 280] # tag L0 from tag-slot
    mov rcx, r12 # hv arg payload
    mov rdx, [rbp - 288] # tag L1 from tag-slot
    mov r9, [rbp - 160] # reload L18 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L18 = TAG_INT (i64-local, fused)
    call _re_longest_from # call _re_longest_from
    add rsp, 16 # hv: pop 1 stack arg slots
    mov [rbp - 440], rax # store tag L20
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 176], r10 # spill L20 to slot
    mov r11, [rbp - 176] # reload L20 from spill slot
    mov r10, r11 # assign L21
    mov r11, 0 # tag L20 = TAG_INT (i64-local, fused)
    mov [rbp - 448], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov r10, [rbp - 184] # reload L21 from spill slot
    cmp r10, 0 # binop >=
    setge al # binop >= → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 456], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_from_bb11 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_from_bb10 # jump -> then
.Lb2dd_regex_search_from_bb9:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 536], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov rdx, [rbp - 272] # reload L32 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 536] # tag L32 from tag-slot
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_regex_search_from_bb10:
    test r14, r14 # br_cond test
    jz .Lb2dd_regex_search_from_bb13 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_from_bb12 # jump -> then
.Lb2dd_regex_search_from_bb11:
    mov r10, [rbp - 160] # reload L18 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 528], r11 # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov r11, [rbp - 264] # reload L31 from spill slot
    mov r10, r11 # assign L18
    mov r11, 0 # tag L31 = TAG_INT (i64-local, fused)
    mov [rbp - 424], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd_regex_search_from_bb7 # branch
.Lb2dd_regex_search_from_bb12:
    mov r10, [rbp - 160] # reload L18 from spill slot
    cmp r10, 0 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 480], r11 # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 480] # tag L25 from tag-slot
    mov [rbp - 472], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd_regex_search_from_bb14 # branch
.Lb2dd_regex_search_from_bb13:
    mov r10, r14 # assign L24
    mov r11, [rbp - 304] # tag L3 from tag-slot
    mov [rbp - 472], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd_regex_search_from_bb14 # branch
.Lb2dd_regex_search_from_bb14:
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_from_bb16 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_from_bb15 # jump -> then
.Lb2dd_regex_search_from_bb15:
    mov r10, [rbp - 184] # reload L21 from spill slot
    cmp r10, 0 # binop ==
    sete al # binop == → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 496], r11 # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r11, [rbp - 232] # reload L27 from spill slot
    mov r10, r11 # assign L26
    mov r11, [rbp - 496] # tag L27 from tag-slot
    mov [rbp - 488], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    jmp .Lb2dd_regex_search_from_bb17 # branch
.Lb2dd_regex_search_from_bb16:
    mov r11, [rbp - 208] # reload L24 from spill slot
    mov r10, r11 # assign L26
    mov r11, [rbp - 472] # tag L24 from tag-slot
    mov [rbp - 488], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    jmp .Lb2dd_regex_search_from_bb17 # branch
.Lb2dd_regex_search_from_bb17:
    mov r10, [rbp - 224] # reload L26 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_regex_search_from_bb19 # jump-if-zero -> else
    jmp .Lb2dd_regex_search_from_bb18 # jump -> then
.Lb2dd_regex_search_from_bb18:
    mov r10, [rbp - 160] # reload L18 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 512], r11 # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L18
    mov r11, 0 # tag L29 = TAG_INT (i64-local, fused)
    mov [rbp - 424], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd_regex_search_from_bb7 # branch
.Lb2dd_regex_search_from_bb19:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 520], rax # store tag L30
    mov rcx, [rbp - 160] # reload L18 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L18 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 520] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 520], rax # store tag L30
    mov rcx, [rbp - 184] # reload L21 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L21 = TAG_INT (i64-local, fused)
    mov rdi, [rbp - 520] # tag L30 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 520], rax # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov rdx, [rbp - 256] # reload L30 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 520] # tag L30 from tag-slot
    add rsp, 496 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _rt_re_fold_byte
    .p2align 4
_rt_re_fold_byte:
    .loc 1 53 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 64 # prologue: alloc spill frame
    mov [rbp - 64], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__rt_re_fold_byte_bb0:
    cmp rbx, 65 # binop >=
    setge al # binop >= → al
    movzx r12, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 72], r11 # store tag L1
    test r12, r12 # br_cond test
    jz .Lb2dd__rt_re_fold_byte_bb2 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_fold_byte_bb1 # jump -> then
.Lb2dd__rt_re_fold_byte_bb1:
    cmp rbx, 90 # binop <=
    setle al # binop <= → al
    movzx r14, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 88], r11 # store tag L3
    mov r13, r14 # assign L2
    mov r11, [rbp - 88] # tag L3 from tag-slot
    mov [rbp - 80], r11 # store tag L2
    jmp .Lb2dd__rt_re_fold_byte_bb3 # branch
.Lb2dd__rt_re_fold_byte_bb2:
    mov r13, r12 # assign L2
    mov r11, [rbp - 72] # tag L1 from tag-slot
    mov [rbp - 80], r11 # store tag L2
    jmp .Lb2dd__rt_re_fold_byte_bb3 # branch
.Lb2dd__rt_re_fold_byte_bb3:
    test r13, r13 # br_cond test
    jz .Lb2dd__rt_re_fold_byte_bb5 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_fold_byte_bb4 # jump -> then
.Lb2dd__rt_re_fold_byte_bb4:
    mov r10, rbx # binop lhs into dst
    add r10, 32 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 104], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov rdx, [rbp - 56] # reload L5 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, 0 # tag L5 = TAG_INT (i64-local, fused)
    add rsp, 64 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__rt_re_fold_byte_bb5:
    mov rdx, rbx # hv arg payload
    mov rax, 0 # tag L0 = TAG_INT (i64-local, fused)
    add rsp, 64 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _rt_re_ascii_fold
    .p2align 4
_rt_re_ascii_fold:
    .loc 1 61 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 144 # prologue: alloc spill frame
    mov [rbp - 104], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__rt_re_ascii_fold_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 104] # tag L0 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 112], rax # store tag L1
    mov r12, rdx # hv: unbox call result (rdx)
    mov r13, r12 # assign L2
    mov r11, [rbp - 112] # tag L1 from tag-slot
    mov [rbp - 120], r11 # store tag L2
    lea r14, [rip+.LCstr24] # assign L3: &str .LCstr24
    mov r11, 3 # tag const_str = TAG_STR
    mov [rbp - 128], r11 # store tag L3
    mov r15, 0 # assign L4
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 136], r11 # store tag L4
    jmp .Lb2dd__rt_re_ascii_fold_bb1 # branch
.Lb2dd__rt_re_ascii_fold_bb1:
    mov rsi, r15 # hv arg payload
    mov rdi, 0 # tag L4 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 120] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 144], rax # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_ascii_fold_bb3 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_ascii_fold_bb2 # jump -> then
.Lb2dd__rt_re_ascii_fold_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 104] # tag L0 from tag-slot
    mov rcx, r15 # hv arg payload
    mov rdx, 0 # tag L4 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 152], rax # store tag L6
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 64], r10 # spill L6 to slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 152] # tag L6 from tag-slot
    call _rt_re_fold_byte # call _rt_re_fold_byte
    mov [rbp - 160], rax # store tag L7
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 72], r10 # spill L7 to slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L7 = TAG_INT (i64-local, fused)
    call hexa_chr_byte # call hexa_chr_byte
    mov [rbp - 168], rax # store tag L8
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 128] # tag L3 from tag-slot
    mov rcx, [rbp - 80] # reload L8 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 168] # tag L8 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 176], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r14, r10 # assign L3
    mov r11, [rbp - 176] # tag L9 from tag-slot
    mov [rbp - 128], r11 # store tag L3
    mov r10, r15 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 184], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    mov r15, r10 # assign L4
    mov r11, 0 # tag L10 = TAG_INT (i64-local, fused)
    mov [rbp - 136], r11 # store tag L4
    jmp .Lb2dd__rt_re_ascii_fold_bb1 # branch
.Lb2dd__rt_re_ascii_fold_bb3:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 128] # tag L3 from tag-slot
    add rsp, 144 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _rt_re_strip_iflag
    .p2align 4
_rt_re_strip_iflag:
    .loc 1 76 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 288 # prologue: alloc spill frame
    mov [rbp - 176], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__rt_re_strip_iflag_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 184], rax # store tag L1
    mov r12, rdx # hv: unbox call result (rdx)
    mov r13, r12 # assign L2
    mov r11, [rbp - 184] # tag L1 from tag-slot
    mov [rbp - 192], r11 # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 192] # tag L2 from tag-slot
    mov rcx, 4 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r14, rdx # binop >=: capture bool payload
    mov [rbp - 200], rax # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd__rt_re_strip_iflag_bb2 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_strip_iflag_bb1 # jump -> then
.Lb2dd__rt_re_strip_iflag_bb1:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 216], rax # store tag L5
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 216] # tag L5 from tag-slot
    mov rcx, 40 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 224], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov r15, r10 # assign L4
    mov r11, [rbp - 224] # tag L6 from tag-slot
    mov [rbp - 208], r11 # store tag L4
    jmp .Lb2dd__rt_re_strip_iflag_bb3 # branch
.Lb2dd__rt_re_strip_iflag_bb2:
    mov r15, r14 # assign L4
    mov r11, [rbp - 200] # tag L3 from tag-slot
    mov [rbp - 208], r11 # store tag L4
    jmp .Lb2dd__rt_re_strip_iflag_bb3 # branch
.Lb2dd__rt_re_strip_iflag_bb3:
    test r15, r15 # br_cond test
    jz .Lb2dd__rt_re_strip_iflag_bb5 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_strip_iflag_bb4 # jump -> then
.Lb2dd__rt_re_strip_iflag_bb4:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 240], rax # store tag L8
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, [rbp - 80] # reload L8 from spill slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 240] # tag L8 from tag-slot
    mov rcx, 63 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 248], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r10, r11 # assign L7
    mov r11, [rbp - 248] # tag L9 from tag-slot
    mov [rbp - 232], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    jmp .Lb2dd__rt_re_strip_iflag_bb6 # branch
.Lb2dd__rt_re_strip_iflag_bb5:
    mov r10, r15 # assign L7
    mov r11, [rbp - 208] # tag L4 from tag-slot
    mov [rbp - 232], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    jmp .Lb2dd__rt_re_strip_iflag_bb6 # branch
.Lb2dd__rt_re_strip_iflag_bb6:
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_strip_iflag_bb8 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_strip_iflag_bb7 # jump -> then
.Lb2dd__rt_re_strip_iflag_bb7:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    mov rcx, 2 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 264], rax # store tag L11
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 264] # tag L11 from tag-slot
    mov rcx, 105 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 272], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L10
    mov r11, [rbp - 272] # tag L12 from tag-slot
    mov [rbp - 256], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    jmp .Lb2dd__rt_re_strip_iflag_bb9 # branch
.Lb2dd__rt_re_strip_iflag_bb8:
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L10
    mov r11, [rbp - 232] # tag L7 from tag-slot
    mov [rbp - 256], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    jmp .Lb2dd__rt_re_strip_iflag_bb9 # branch
.Lb2dd__rt_re_strip_iflag_bb9:
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_strip_iflag_bb11 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_strip_iflag_bb10 # jump -> then
.Lb2dd__rt_re_strip_iflag_bb10:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    mov rcx, 3 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 288], rax # store tag L14
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 288] # tag L14 from tag-slot
    mov rcx, 41 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 296], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L13
    mov r11, [rbp - 296] # tag L15 from tag-slot
    mov [rbp - 280], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd__rt_re_strip_iflag_bb12 # branch
.Lb2dd__rt_re_strip_iflag_bb11:
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L13
    mov r11, [rbp - 256] # tag L10 from tag-slot
    mov [rbp - 280], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    jmp .Lb2dd__rt_re_strip_iflag_bb12 # branch
.Lb2dd__rt_re_strip_iflag_bb12:
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_strip_iflag_bb14 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_strip_iflag_bb13 # jump -> then
.Lb2dd__rt_re_strip_iflag_bb13:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 176] # tag L0 from tag-slot
    mov rcx, 4 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    mov r9, r13 # hv arg payload
    mov r8, [rbp - 192] # tag L2 from tag-slot
    call hexa_str_substring # call hexa_str_substring
    mov [rbp - 312], rax # store tag L17
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 152], r10 # spill L17 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 320], rax # store tag L18
    mov rcx, [rbp - 152] # reload L17 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 312] # tag L17 from tag-slot
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    lea rcx, [rip+.LCstr25] # hv arg payload: &str .LCstr25
    mov rdx, 3 # hv arg tag = TAG_STR
    mov rdi, [rbp - 320] # tag L18 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 320], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rdx, [rbp - 160] # reload L18 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 320] # tag L18 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__rt_re_strip_iflag_bb14:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 328], rax # store tag L19
    mov rcx, rbx # hv arg payload
    mov rdx, [rbp - 176] # tag L0 from tag-slot
    mov rdi, [rbp - 328] # tag L19 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 328], rax # store tag L19
    lea rcx, [rip+.LCstr26] # hv arg payload: &str .LCstr26
    mov rdx, 3 # hv arg tag = TAG_STR
    mov rdi, [rbp - 328] # tag L19 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 328], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov rdx, [rbp - 168] # reload L19 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 328] # tag L19 from tag-slot
    add rsp, 288 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _rt_re_pcre_literal
    .p2align 4
_rt_re_pcre_literal:
    .loc 1 94 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 752 # prologue: alloc spill frame
    mov [rbp - 408], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__rt_re_pcre_literal_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 408] # tag L0 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 416], rax # store tag L1
    mov r12, rdx # hv: unbox call result (rdx)
    mov r13, r12 # assign L2
    mov r11, [rbp - 416] # tag L1 from tag-slot
    mov [rbp - 424], r11 # store tag L2
    lea r14, [rip+.LCstr24] # assign L3: &str .LCstr24
    mov r11, 3 # tag const_str = TAG_STR
    mov [rbp - 432], r11 # store tag L3
    mov r15, 0 # assign L4
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 440], r11 # store tag L4
    mov r10, 0 # assign L5
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 448], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb1 # branch
.Lb2dd__rt_re_pcre_literal_bb1:
    mov rsi, r15 # hv arg payload
    mov rdi, 0 # tag L4 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 424] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 456], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb3 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb2 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 408] # tag L0 from tag-slot
    mov rcx, r15 # hv arg payload
    mov rdx, 0 # tag L4 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 464], rax # store tag L7
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L8
    mov r11, [rbp - 464] # tag L7 from tag-slot
    mov [rbp - 472], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r10, [rbp - 80] # reload L8 from spill slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 472] # tag L8 from tag-slot
    mov rcx, 92 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 480], rax # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb5 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb4 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb3:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 432] # tag L3 from tag-slot
    add rsp, 752 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__rt_re_pcre_literal_bb4:
    mov r10, r15 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 496], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov r10, [rbp - 104] # reload L11 from spill slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L11 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 424] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 504], rax # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L10
    mov r11, [rbp - 504] # tag L12 from tag-slot
    mov [rbp - 488], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb6 # branch
.Lb2dd__rt_re_pcre_literal_bb5:
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r10, r11 # assign L10
    mov r11, [rbp - 480] # tag L9 from tag-slot
    mov [rbp - 488], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb6 # branch
.Lb2dd__rt_re_pcre_literal_bb6:
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb8 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb7 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb7:
    mov r10, r15 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 520], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 408] # tag L0 from tag-slot
    mov rcx, [rbp - 128] # reload L14 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L14 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 528], rax # store tag L15
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 136], r10 # spill L15 to slot
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 528] # tag L15 from tag-slot
    mov [rbp - 536], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 448] # tag L5 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 544], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r10, [rbp - 152] # reload L17 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb10 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb9 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb8:
    mov r10, [rbp - 80] # reload L8 from spill slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 472] # tag L8 from tag-slot
    mov rcx, 91 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 720], rax # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov r10, [rbp - 328] # reload L39 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb30 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb29 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb9:
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L16 from tag-slot
    mov rcx, 100 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 560], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, [rbp - 168] # reload L19 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb12 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb11 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb10:
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 544] # tag L17 from tag-slot
    mov [rbp - 552], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb26 # branch
.Lb2dd__rt_re_pcre_literal_bb11:
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 560] # tag L19 from tag-slot
    mov [rbp - 568], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb13 # branch
.Lb2dd__rt_re_pcre_literal_bb12:
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L16 from tag-slot
    mov rcx, 68 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 576], rax # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 576] # tag L21 from tag-slot
    mov [rbp - 568], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb13 # branch
.Lb2dd__rt_re_pcre_literal_bb13:
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb15 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb14 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb14:
    mov r11, [rbp - 176] # reload L20 from spill slot
    mov r10, r11 # assign L22
    mov r11, [rbp - 568] # tag L20 from tag-slot
    mov [rbp - 584], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb16 # branch
.Lb2dd__rt_re_pcre_literal_bb15:
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L16 from tag-slot
    mov rcx, 119 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 592], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r11, [rbp - 200] # reload L23 from spill slot
    mov r10, r11 # assign L22
    mov r11, [rbp - 592] # tag L23 from tag-slot
    mov [rbp - 584], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb16 # branch
.Lb2dd__rt_re_pcre_literal_bb16:
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb18 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb17 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb17:
    mov r11, [rbp - 192] # reload L22 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 584] # tag L22 from tag-slot
    mov [rbp - 600], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb19 # branch
.Lb2dd__rt_re_pcre_literal_bb18:
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L16 from tag-slot
    mov rcx, 87 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 608], rax # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r11, [rbp - 216] # reload L25 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 608] # tag L25 from tag-slot
    mov [rbp - 600], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb19 # branch
.Lb2dd__rt_re_pcre_literal_bb19:
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb21 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb20 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb20:
    mov r11, [rbp - 208] # reload L24 from spill slot
    mov r10, r11 # assign L26
    mov r11, [rbp - 600] # tag L24 from tag-slot
    mov [rbp - 616], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb22 # branch
.Lb2dd__rt_re_pcre_literal_bb21:
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L16 from tag-slot
    mov rcx, 115 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 624], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r11, [rbp - 232] # reload L27 from spill slot
    mov r10, r11 # assign L26
    mov r11, [rbp - 624] # tag L27 from tag-slot
    mov [rbp - 616], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb22 # branch
.Lb2dd__rt_re_pcre_literal_bb22:
    mov r10, [rbp - 224] # reload L26 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb24 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb23 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb23:
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L28
    mov r11, [rbp - 616] # tag L26 from tag-slot
    mov [rbp - 632], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb25 # branch
.Lb2dd__rt_re_pcre_literal_bb24:
    mov r10, [rbp - 144] # reload L16 from spill slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L16 from tag-slot
    mov rcx, 83 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 640], rax # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L28
    mov r11, [rbp - 640] # tag L29 from tag-slot
    mov [rbp - 632], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb25 # branch
.Lb2dd__rt_re_pcre_literal_bb25:
    mov r11, [rbp - 240] # reload L28 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 632] # tag L28 from tag-slot
    mov [rbp - 552], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb26 # branch
.Lb2dd__rt_re_pcre_literal_bb26:
    mov r10, [rbp - 160] # reload L18 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb28 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb27 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb27:
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L16 from tag-slot
    call hexa_chr_byte # call hexa_chr_byte
    mov [rbp - 656], rax # store tag L31
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 264], r10 # spill L31 to slot
    mov r11, [rbp - 264] # reload L31 from spill slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 432] # tag L3 from tag-slot
    mov rcx, [rbp - 264] # reload L31 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 656] # tag L31 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 664], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, [rbp - 272] # reload L32 from spill slot
    mov r14, r10 # assign L3
    mov r11, [rbp - 664] # tag L32 from tag-slot
    mov [rbp - 432], r11 # store tag L3
    mov r10, r15 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 672], r11 # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r10, [rbp - 280] # reload L33 from spill slot
    mov r15, r10 # assign L4
    mov r11, 0 # tag L33 = TAG_INT (i64-local, fused)
    mov [rbp - 440], r11 # store tag L4
    jmp .Lb2dd__rt_re_pcre_literal_bb1 # branch
.Lb2dd__rt_re_pcre_literal_bb28:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 472] # tag L8 from tag-slot
    call hexa_chr_byte # call hexa_chr_byte
    mov [rbp - 680], rax # store tag L34
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 288], r10 # spill L34 to slot
    mov r11, [rbp - 288] # reload L34 from spill slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 432] # tag L3 from tag-slot
    mov rcx, [rbp - 288] # reload L34 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 680] # tag L34 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 688], rax # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov r10, [rbp - 296] # reload L35 from spill slot
    mov r14, r10 # assign L3
    mov r11, [rbp - 688] # tag L35 from tag-slot
    mov [rbp - 432], r11 # store tag L3
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L16 from tag-slot
    call hexa_chr_byte # call hexa_chr_byte
    mov [rbp - 696], rax # store tag L36
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 304], r10 # spill L36 to slot
    mov r11, [rbp - 304] # reload L36 from spill slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 432] # tag L3 from tag-slot
    mov rcx, [rbp - 304] # reload L36 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 696] # tag L36 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 704], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    mov r14, r10 # assign L3
    mov r11, [rbp - 704] # tag L37 from tag-slot
    mov [rbp - 432], r11 # store tag L3
    mov r10, r15 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 712], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov r10, [rbp - 320] # reload L38 from spill slot
    mov r15, r10 # assign L4
    mov r11, 0 # tag L38 = TAG_INT (i64-local, fused)
    mov [rbp - 440], r11 # store tag L4
    jmp .Lb2dd__rt_re_pcre_literal_bb1 # branch
.Lb2dd__rt_re_pcre_literal_bb29:
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 448] # tag L5 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 736], r11 # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r11, [rbp - 344] # reload L41 from spill slot
    mov r10, r11 # assign L40
    mov r11, [rbp - 736] # tag L41 from tag-slot
    mov [rbp - 728], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb31 # branch
.Lb2dd__rt_re_pcre_literal_bb30:
    mov r11, [rbp - 328] # reload L39 from spill slot
    mov r10, r11 # assign L40
    mov r11, [rbp - 720] # tag L39 from tag-slot
    mov [rbp - 728], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb31 # branch
.Lb2dd__rt_re_pcre_literal_bb31:
    mov r10, [rbp - 336] # reload L40 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb33 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb32 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb32:
    mov r10, 1 # assign L5
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 448], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb39 # branch
.Lb2dd__rt_re_pcre_literal_bb33:
    mov r10, [rbp - 80] # reload L8 from spill slot
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 472] # tag L8 from tag-slot
    mov rcx, 93 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 752], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov r10, [rbp - 360] # reload L43 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb35 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb34 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb34:
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L44
    mov r11, [rbp - 448] # tag L5 from tag-slot
    mov [rbp - 760], r11 # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb36 # branch
.Lb2dd__rt_re_pcre_literal_bb35:
    mov r11, [rbp - 360] # reload L43 from spill slot
    mov r10, r11 # assign L44
    mov r11, [rbp - 752] # tag L43 from tag-slot
    mov [rbp - 760], r11 # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb36 # branch
.Lb2dd__rt_re_pcre_literal_bb36:
    mov r10, [rbp - 368] # reload L44 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_pcre_literal_bb38 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_pcre_literal_bb37 # jump -> then
.Lb2dd__rt_re_pcre_literal_bb37:
    mov r10, 0 # assign L5
    mov r11, 2 # tag const_bool = TAG_BOOL
    mov [rbp - 448], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd__rt_re_pcre_literal_bb38 # branch
.Lb2dd__rt_re_pcre_literal_bb38:
    jmp .Lb2dd__rt_re_pcre_literal_bb39 # branch
.Lb2dd__rt_re_pcre_literal_bb39:
    mov rsi, [rbp - 80] # reload L8 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 472] # tag L8 from tag-slot
    call hexa_chr_byte # call hexa_chr_byte
    mov [rbp - 776], rax # store tag L46
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 384], r10 # spill L46 to slot
    mov r11, [rbp - 384] # reload L46 from spill slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 432] # tag L3 from tag-slot
    mov rcx, [rbp - 384] # reload L46 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 776] # tag L46 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 784], rax # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    mov r10, [rbp - 392] # reload L47 from spill slot
    mov r14, r10 # assign L3
    mov r11, [rbp - 784] # tag L47 from tag-slot
    mov [rbp - 432], r11 # store tag L3
    mov r10, r15 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 792], r11 # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    mov r10, [rbp - 400] # reload L48 from spill slot
    mov r15, r10 # assign L4
    mov r11, 0 # tag L48 = TAG_INT (i64-local, fused)
    mov [rbp - 440], r11 # store tag L4
    jmp .Lb2dd__rt_re_pcre_literal_bb1 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 752 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _rt_re_has_backref_lookaround
    .p2align 4
_rt_re_has_backref_lookaround:
    .loc 1 132 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 1072 # prologue: alloc spill frame
    mov [rbp - 568], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__rt_re_has_backref_lookaround_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 576], rax # store tag L1
    mov r12, rdx # hv: unbox call result (rdx)
    mov r13, r12 # assign L2
    mov r11, [rbp - 576] # tag L1 from tag-slot
    mov [rbp - 584], r11 # store tag L2
    mov r14, 0 # assign L3
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 592], r11 # store tag L3
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb1 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb1:
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 584] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r15, rdx # binop <: capture bool payload
    mov [rbp - 600], rax # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb3 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb2 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb2:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 608], rax # store tag L5
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 56], r10 # spill L5 to slot
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 608] # tag L5 from tag-slot
    mov [rbp - 616], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 616] # tag L6 from tag-slot
    mov rcx, 92 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 624], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb5 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb4 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb3:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 1072 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__rt_re_has_backref_lookaround_bb4:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 640], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L9 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 584] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 648], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r10, [rbp - 96] # reload L10 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb7 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb6 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb5:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 616] # tag L6 from tag-slot
    mov rcx, 91 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 728], rax # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb14 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb13 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb6:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 664], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, [rbp - 112] # reload L12 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L12 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 672], rax # store tag L13
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 120], r10 # spill L13 to slot
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L14
    mov r11, [rbp - 672] # tag L13 from tag-slot
    mov [rbp - 680], r11 # store tag L14
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 680] # tag L14 from tag-slot
    mov rcx, 49 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_ge # binop >=: tag-dispatch hexa_cmp_ge
    mov r10, rdx # binop >=: capture bool payload
    mov [rbp - 688], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov r10, [rbp - 136] # reload L15 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb9 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb8 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb7:
    mov r10, r14 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 720], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov r10, [rbp - 168] # reload L19 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L19 = TAG_INT (i64-local, fused)
    mov [rbp - 592], r11 # store tag L3
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb1 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb8:
    mov r10, [rbp - 128] # reload L14 from spill slot
    mov rsi, [rbp - 128] # reload L14 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 680] # tag L14 from tag-slot
    mov rcx, 57 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 704], rax # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 704] # tag L17 from tag-slot
    mov [rbp - 696], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb10 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb9:
    mov r11, [rbp - 136] # reload L15 from spill slot
    mov r10, r11 # assign L16
    mov r11, [rbp - 688] # tag L15 from tag-slot
    mov [rbp - 696], r11 # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb10 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb10:
    mov r10, [rbp - 144] # reload L16 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb12 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb11 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb11:
    mov rdx, 1 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 1072 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__rt_re_has_backref_lookaround_bb12:
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb7 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb13:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 744], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L22 = TAG_INT (i64-local, fused)
    mov [rbp - 592], r11 # store tag L3
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 584] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 752], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb16 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb15 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb14:
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 616] # tag L6 from tag-slot
    mov rcx, 40 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 944], rax # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    mov r10, [rbp - 392] # reload L47 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb37 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb36 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb15:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 768], rax # store tag L25
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 216], r10 # spill L25 to slot
    mov r10, [rbp - 216] # reload L25 from spill slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 768] # tag L25 from tag-slot
    mov rcx, 94 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 776], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 776] # tag L26 from tag-slot
    mov [rbp - 760], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb17 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb16:
    mov r11, [rbp - 200] # reload L23 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 752] # tag L23 from tag-slot
    mov [rbp - 760], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb17 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb17:
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb19 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb18 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb18:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 792], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L28 = TAG_INT (i64-local, fused)
    mov [rbp - 592], r11 # store tag L3
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb19 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb19:
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 584] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 800], rax # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r10, [rbp - 248] # reload L29 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb21 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb20 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb20:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 816], rax # store tag L31
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 264], r10 # spill L31 to slot
    mov r10, [rbp - 264] # reload L31 from spill slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 816] # tag L31 from tag-slot
    mov rcx, 93 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 824], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r11, [rbp - 272] # reload L32 from spill slot
    mov r10, r11 # assign L30
    mov r11, [rbp - 824] # tag L32 from tag-slot
    mov [rbp - 808], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb22 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb21:
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L30
    mov r11, [rbp - 800] # tag L29 from tag-slot
    mov [rbp - 808], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb22 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb22:
    mov r10, [rbp - 256] # reload L30 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb24 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb23 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb23:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 840], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L34 = TAG_INT (i64-local, fused)
    mov [rbp - 592], r11 # store tag L3
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb24 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb24:
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb25 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb25:
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 584] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 848], rax # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov r10, [rbp - 296] # reload L35 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb29 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb28 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb26:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 880], rax # store tag L39
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 328], r10 # spill L39 to slot
    mov r10, [rbp - 328] # reload L39 from spill slot
    mov rsi, [rbp - 328] # reload L39 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 880] # tag L39 from tag-slot
    mov rcx, 92 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 888], rax # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb32 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb31 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb27:
    mov rsi, r14 # hv arg payload
    mov rdi, 0 # tag L3 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 584] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 920], rax # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov r10, [rbp - 368] # reload L44 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb35 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb34 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb28:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, r14 # hv arg payload
    mov rdx, 0 # tag L3 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 864], rax # store tag L37
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    mov rsi, [rbp - 312] # reload L37 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 864] # tag L37 from tag-slot
    mov rcx, 93 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 872], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov r11, [rbp - 320] # reload L38 from spill slot
    mov r10, r11 # assign L36
    mov r11, [rbp - 872] # tag L38 from tag-slot
    mov [rbp - 856], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb30 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb29:
    mov r11, [rbp - 296] # reload L35 from spill slot
    mov r10, r11 # assign L36
    mov r11, [rbp - 848] # tag L35 from tag-slot
    mov [rbp - 856], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb30 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb30:
    mov r10, [rbp - 304] # reload L36 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb27 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb26 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb31:
    mov r10, r14 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 904], r11 # store tag L42
    mov [rbp - 352], r10 # spill L42 to slot
    mov r10, [rbp - 352] # reload L42 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L42 = TAG_INT (i64-local, fused)
    mov [rbp - 592], r11 # store tag L3
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb33 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb32:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 912], r11 # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov r10, [rbp - 360] # reload L43 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L43 = TAG_INT (i64-local, fused)
    mov [rbp - 592], r11 # store tag L3
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb33 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb33:
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb25 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb34:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 936], r11 # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    mov r10, [rbp - 384] # reload L46 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L46 = TAG_INT (i64-local, fused)
    mov [rbp - 592], r11 # store tag L3
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb35 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb35:
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb1 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb36:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 960], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov r10, [rbp - 408] # reload L49 from spill slot
    mov rsi, [rbp - 408] # reload L49 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L49 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 584] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 968], rax # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    mov r11, [rbp - 416] # reload L50 from spill slot
    mov r10, r11 # assign L48
    mov r11, [rbp - 968] # tag L50 from tag-slot
    mov [rbp - 952], r11 # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb38 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb37:
    mov r11, [rbp - 392] # reload L47 from spill slot
    mov r10, r11 # assign L48
    mov r11, [rbp - 944] # tag L47 from tag-slot
    mov [rbp - 952], r11 # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb38 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb38:
    mov r10, [rbp - 400] # reload L48 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb40 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb39 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb39:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 984], r11 # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, [rbp - 432] # reload L52 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L52 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 992], rax # store tag L53
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 440], r10 # spill L53 to slot
    mov r10, [rbp - 440] # reload L53 from spill slot
    mov rsi, [rbp - 440] # reload L53 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 992] # tag L53 from tag-slot
    mov rcx, 63 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1000], rax # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov r11, [rbp - 448] # reload L54 from spill slot
    mov r10, r11 # assign L51
    mov r11, [rbp - 1000] # tag L54 from tag-slot
    mov [rbp - 976], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb41 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb40:
    mov r11, [rbp - 400] # reload L48 from spill slot
    mov r10, r11 # assign L51
    mov r11, [rbp - 952] # tag L48 from tag-slot
    mov [rbp - 976], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb41 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb41:
    mov r10, [rbp - 424] # reload L51 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb43 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb42 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb42:
    mov r10, r14 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1016], r11 # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    mov r10, [rbp - 464] # reload L56 from spill slot
    mov rsi, [rbp - 464] # reload L56 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L56 = TAG_INT (i64-local, fused)
    mov rcx, r13 # hv arg payload
    mov rdx, [rbp - 584] # tag L2 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 1024], rax # store tag L57
    mov [rbp - 472], r10 # spill L57 to slot
    mov r10, [rbp - 472] # reload L57 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb45 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb44 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb43:
    mov r10, r14 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1112], r11 # store tag L68
    mov [rbp - 560], r10 # spill L68 to slot
    mov r10, [rbp - 560] # reload L68 from spill slot
    mov r14, r10 # assign L3
    mov r11, 0 # tag L68 = TAG_INT (i64-local, fused)
    mov [rbp - 592], r11 # store tag L3
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb1 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb44:
    mov r10, r14 # binop lhs into dst
    add r10, 2 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1040], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 568] # tag L0 from tag-slot
    mov rcx, [rbp - 488] # reload L59 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L59 = TAG_INT (i64-local, fused)
    call hexa_str_byte_at # call hexa_str_byte_at
    mov [rbp - 1048], rax # store tag L60
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 496], r10 # spill L60 to slot
    mov r11, [rbp - 496] # reload L60 from spill slot
    mov r10, r11 # assign L61
    mov r11, [rbp - 1048] # tag L60 from tag-slot
    mov [rbp - 1056], r11 # store tag L61
    mov [rbp - 504], r10 # spill L61 to slot
    mov r10, [rbp - 504] # reload L61 from spill slot
    mov rsi, [rbp - 504] # reload L61 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1056] # tag L61 from tag-slot
    mov rcx, 61 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1064], rax # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov r10, [rbp - 512] # reload L62 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb47 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb46 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb45:
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb43 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb46:
    mov r11, [rbp - 512] # reload L62 from spill slot
    mov r10, r11 # assign L63
    mov r11, [rbp - 1064] # tag L62 from tag-slot
    mov [rbp - 1072], r11 # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb48 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb47:
    mov r10, [rbp - 504] # reload L61 from spill slot
    mov rsi, [rbp - 504] # reload L61 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1056] # tag L61 from tag-slot
    mov rcx, 33 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1080], rax # store tag L64
    mov [rbp - 528], r10 # spill L64 to slot
    mov r11, [rbp - 528] # reload L64 from spill slot
    mov r10, r11 # assign L63
    mov r11, [rbp - 1080] # tag L64 from tag-slot
    mov [rbp - 1072], r11 # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb48 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb48:
    mov r10, [rbp - 520] # reload L63 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb50 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb49 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb49:
    mov r11, [rbp - 520] # reload L63 from spill slot
    mov r10, r11 # assign L65
    mov r11, [rbp - 1072] # tag L63 from tag-slot
    mov [rbp - 1088], r11 # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb51 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb50:
    mov r10, [rbp - 504] # reload L61 from spill slot
    mov rsi, [rbp - 504] # reload L61 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 1056] # tag L61 from tag-slot
    mov rcx, 60 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 1096], rax # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    mov r11, [rbp - 544] # reload L66 from spill slot
    mov r10, r11 # assign L65
    mov r11, [rbp - 1096] # tag L66 from tag-slot
    mov [rbp - 1088], r11 # store tag L65
    mov [rbp - 536], r10 # spill L65 to slot
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb51 # branch
.Lb2dd__rt_re_has_backref_lookaround_bb51:
    mov r10, [rbp - 536] # reload L65 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_has_backref_lookaround_bb53 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb52 # jump -> then
.Lb2dd__rt_re_has_backref_lookaround_bb52:
    mov rdx, 1 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 1072 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd__rt_re_has_backref_lookaround_bb53:
    jmp .Lb2dd__rt_re_has_backref_lookaround_bb45 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 1072 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.hidden _rt_re_prep_pat
    .p2align 4
_rt_re_prep_pat:
    .loc 1 171 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 160 # prologue: alloc spill frame
    mov [rbp - 112], rdi # store tag L0
    mov rbx, rsi # ingress param payload
.Lb2dd__rt_re_prep_pat_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 112] # tag L0 from tag-slot
    call _rt_re_strip_iflag # call _rt_re_strip_iflag
    mov [rbp - 120], rax # store tag L1
    mov r12, rdx # hv: unbox user-call result payload
    mov r13, r12 # assign L2
    mov r11, [rbp - 120] # tag L1 from tag-slot
    mov [rbp - 128], r11 # store tag L2
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 128] # tag L2 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r14, rdx # index: capture element payload
    mov [rbp - 136], rax # store tag L3
    mov r15, r14 # assign L4
    mov r11, [rbp - 136] # tag L3 from tag-slot
    mov [rbp - 144], r11 # store tag L4
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 144] # tag L4 from tag-slot
    call _rt_re_pcre_literal # call _rt_re_pcre_literal
    mov [rbp - 152], rax # store tag L5
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov r15, r10 # assign L4
    mov r11, [rbp - 152] # tag L5 from tag-slot
    mov [rbp - 144], r11 # store tag L4
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 128] # tag L2 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 160], rax # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 160] # tag L6 from tag-slot
    lea rcx, [rip+.LCstr25] # hv arg payload: &str .LCstr25
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 168], rax # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd__rt_re_prep_pat_bb2 # jump-if-zero -> else
    jmp .Lb2dd__rt_re_prep_pat_bb1 # jump -> then
.Lb2dd__rt_re_prep_pat_bb1:
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 144] # tag L4 from tag-slot
    call _rt_re_ascii_fold # call _rt_re_ascii_fold
    mov [rbp - 184], rax # store tag L9
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov r15, r10 # assign L4
    mov r11, [rbp - 184] # tag L9 from tag-slot
    mov [rbp - 144], r11 # store tag L4
    jmp .Lb2dd__rt_re_prep_pat_bb2 # branch
.Lb2dd__rt_re_prep_pat_bb2:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 128] # tag L2 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 192], rax # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 200], rax # store tag L11
    mov rcx, r15 # hv arg payload
    mov rdx, [rbp - 144] # tag L4 from tag-slot
    mov rdi, [rbp - 200] # tag L11 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 200], rax # store tag L11
    mov rcx, [rbp - 96] # reload L10 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 192] # tag L10 from tag-slot
    mov rdi, [rbp - 200] # tag L11 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 200], rax # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rdx, [rbp - 104] # reload L11 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 200] # tag L11 from tag-slot
    add rsp, 160 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.globl rt_regex_match
.hidden rt_regex_match
    .p2align 4
rt_regex_match:
    .loc 1 188 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 480 # prologue: alloc spill frame
    mov [rbp - 272], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 280], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_rt_regex_match_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 272] # tag L0 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 288], rax # store tag L2
    mov r13, rdx # hv: unbox call result (rdx)
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 288] # tag L2 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r14, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 296], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd_rt_regex_match_bb2 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_bb1 # jump -> then
.Lb2dd_rt_regex_match_bb1:
    mov r15, r14 # assign L4
    mov r11, [rbp - 296] # tag L3 from tag-slot
    mov [rbp - 304], r11 # store tag L4
    jmp .Lb2dd_rt_regex_match_bb3 # branch
.Lb2dd_rt_regex_match_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 280] # tag L1 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 312], rax # store tag L5
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 312] # tag L5 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 320], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov r15, r10 # assign L4
    mov r11, [rbp - 320] # tag L6 from tag-slot
    mov [rbp - 304], r11 # store tag L4
    jmp .Lb2dd_rt_regex_match_bb3 # branch
.Lb2dd_rt_regex_match_bb3:
    test r15, r15 # br_cond test
    jz .Lb2dd_rt_regex_match_bb5 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_bb4 # jump -> then
.Lb2dd_rt_regex_match_bb4:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_match_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 272] # tag L0 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 336], rax # store tag L8
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L9
    mov r11, [rbp - 336] # tag L8 from tag-slot
    mov [rbp - 344], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 280] # tag L1 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 352], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 352] # tag L10 from tag-slot
    mov [rbp - 360], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 344] # tag L9 from tag-slot
    call _rt_re_has_backref_lookaround # call _rt_re_has_backref_lookaround
    mov [rbp - 368], rax # store tag L12
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_bb7 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_bb6 # jump -> then
.Lb2dd_rt_regex_match_bb6:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_match_bb7:
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 344] # tag L9 from tag-slot
    call _rt_re_prep_pat # call _rt_re_prep_pat
    mov [rbp - 384], rax # store tag L14
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 128], r10 # spill L14 to slot
    mov r11, [rbp - 128] # reload L14 from spill slot
    mov r10, r11 # assign L15
    mov r11, [rbp - 384] # tag L14 from tag-slot
    mov [rbp - 392], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 392] # tag L15 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 400], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 400] # tag L16 from tag-slot
    call regex_compile # call regex_compile
    mov [rbp - 408], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 408] # tag L17 from tag-slot
    mov [rbp - 416], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 416] # tag L18 from tag-slot
    call regex_valid # call regex_valid
    mov [rbp - 424], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, [rbp - 168] # reload L19 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 424] # tag L19 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 432], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_bb9 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_bb8 # jump -> then
.Lb2dd_rt_regex_match_bb8:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_match_bb9:
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L22
    mov r11, [rbp - 360] # tag L11 from tag-slot
    mov [rbp - 448], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 392] # tag L15 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 456], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    mov rsi, [rbp - 200] # reload L23 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 456] # tag L23 from tag-slot
    lea rcx, [rip+.LCstr25] # hv arg payload: &str .LCstr25
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 464], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_bb11 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_bb10 # jump -> then
.Lb2dd_rt_regex_match_bb10:
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 360] # tag L11 from tag-slot
    call _rt_re_ascii_fold # call _rt_re_ascii_fold
    mov [rbp - 480], rax # store tag L26
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 224], r10 # spill L26 to slot
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L22
    mov r11, [rbp - 480] # tag L26 from tag-slot
    mov [rbp - 448], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    jmp .Lb2dd_rt_regex_match_bb11 # branch
.Lb2dd_rt_regex_match_bb11:
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 416] # tag L18 from tag-slot
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 448] # tag L22 from tag-slot
    call regex_search # call regex_search
    mov [rbp - 488], rax # store tag L27
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 232], r10 # spill L27 to slot
    mov r11, [rbp - 232] # reload L27 from spill slot
    mov r10, r11 # assign L28
    mov r11, [rbp - 488] # tag L27 from tag-slot
    mov [rbp - 496], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov rsi, [rbp - 240] # reload L28 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 496] # tag L28 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 504], r11 # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r10, [rbp - 248] # reload L29 from spill slot
    mov rsi, [rbp - 248] # reload L29 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L29 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 512], rax # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov r10, [rbp - 256] # reload L30 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_bb13 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_bb12 # jump -> then
.Lb2dd_rt_regex_match_bb12:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_match_bb13:
    mov rdx, 1 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 480 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.globl rt_regex_match_full
.hidden rt_regex_match_full
    .p2align 4
rt_regex_match_full:
    .loc 1 205 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 592 # prologue: alloc spill frame
    mov [rbp - 328], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 336], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_rt_regex_match_full_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 328] # tag L0 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 344], rax # store tag L2
    mov r13, rdx # hv: unbox call result (rdx)
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 344] # tag L2 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r14, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 352], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd_rt_regex_match_full_bb2 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_full_bb1 # jump -> then
.Lb2dd_rt_regex_match_full_bb1:
    mov r15, r14 # assign L4
    mov r11, [rbp - 352] # tag L3 from tag-slot
    mov [rbp - 360], r11 # store tag L4
    jmp .Lb2dd_rt_regex_match_full_bb3 # branch
.Lb2dd_rt_regex_match_full_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 336] # tag L1 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 368], rax # store tag L5
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 368] # tag L5 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 376], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov r15, r10 # assign L4
    mov r11, [rbp - 376] # tag L6 from tag-slot
    mov [rbp - 360], r11 # store tag L4
    jmp .Lb2dd_rt_regex_match_full_bb3 # branch
.Lb2dd_rt_regex_match_full_bb3:
    test r15, r15 # br_cond test
    jz .Lb2dd_rt_regex_match_full_bb5 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_full_bb4 # jump -> then
.Lb2dd_rt_regex_match_full_bb4:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_match_full_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 328] # tag L0 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 392], rax # store tag L8
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L9
    mov r11, [rbp - 392] # tag L8 from tag-slot
    mov [rbp - 400], r11 # store tag L9
    mov [rbp - 88], r10 # spill L9 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 336] # tag L1 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 408], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 408] # tag L10 from tag-slot
    mov [rbp - 416], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 400] # tag L9 from tag-slot
    call _rt_re_has_backref_lookaround # call _rt_re_has_backref_lookaround
    mov [rbp - 424], rax # store tag L12
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_full_bb7 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_full_bb6 # jump -> then
.Lb2dd_rt_regex_match_full_bb6:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_match_full_bb7:
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 400] # tag L9 from tag-slot
    call _rt_re_prep_pat # call _rt_re_prep_pat
    mov [rbp - 440], rax # store tag L14
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 128], r10 # spill L14 to slot
    mov r11, [rbp - 128] # reload L14 from spill slot
    mov r10, r11 # assign L15
    mov r11, [rbp - 440] # tag L14 from tag-slot
    mov [rbp - 448], r11 # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 448] # tag L15 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 456], rax # store tag L16
    mov [rbp - 144], r10 # spill L16 to slot
    mov rsi, [rbp - 144] # reload L16 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 456] # tag L16 from tag-slot
    call regex_compile # call regex_compile
    mov [rbp - 464], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 464] # tag L17 from tag-slot
    mov [rbp - 472], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 472] # tag L18 from tag-slot
    call regex_valid # call regex_valid
    mov [rbp - 480], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, [rbp - 168] # reload L19 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 480] # tag L19 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 488], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov r10, [rbp - 176] # reload L20 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_full_bb9 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_full_bb8 # jump -> then
.Lb2dd_rt_regex_match_full_bb8:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_match_full_bb9:
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L22
    mov r11, [rbp - 416] # tag L11 from tag-slot
    mov [rbp - 504], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov rsi, [rbp - 136] # reload L15 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 448] # tag L15 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 512], rax # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    mov rsi, [rbp - 200] # reload L23 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 512] # tag L23 from tag-slot
    lea rcx, [rip+.LCstr25] # hv arg payload: &str .LCstr25
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 520], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov r10, [rbp - 208] # reload L24 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_full_bb11 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_full_bb10 # jump -> then
.Lb2dd_rt_regex_match_full_bb10:
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 416] # tag L11 from tag-slot
    call _rt_re_ascii_fold # call _rt_re_ascii_fold
    mov [rbp - 536], rax # store tag L26
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 224], r10 # spill L26 to slot
    mov r11, [rbp - 224] # reload L26 from spill slot
    mov r10, r11 # assign L22
    mov r11, [rbp - 536] # tag L26 from tag-slot
    mov [rbp - 504], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    jmp .Lb2dd_rt_regex_match_full_bb11 # branch
.Lb2dd_rt_regex_match_full_bb11:
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 472] # tag L18 from tag-slot
    mov rcx, [rbp - 192] # reload L22 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 504] # tag L22 from tag-slot
    call regex_search # call regex_search
    mov [rbp - 544], rax # store tag L27
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 232], r10 # spill L27 to slot
    mov r11, [rbp - 232] # reload L27 from spill slot
    mov r10, r11 # assign L28
    mov r11, [rbp - 544] # tag L27 from tag-slot
    mov [rbp - 552], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov rsi, [rbp - 240] # reload L28 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 552] # tag L28 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 560], r11 # store tag L29
    mov [rbp - 248], r10 # spill L29 to slot
    mov r10, [rbp - 248] # reload L29 from spill slot
    mov rsi, [rbp - 248] # reload L29 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 560] # tag L29 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 568], rax # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov r10, [rbp - 256] # reload L30 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_full_bb13 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_full_bb12 # jump -> then
.Lb2dd_rt_regex_match_full_bb12:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_match_full_bb13:
    mov rsi, [rbp - 240] # reload L28 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 552] # tag L28 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 584], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, [rbp - 272] # reload L32 from spill slot
    mov rsi, [rbp - 272] # reload L32 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 584] # tag L32 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 592], rax # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r10, [rbp - 280] # reload L33 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_full_bb15 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_full_bb14 # jump -> then
.Lb2dd_rt_regex_match_full_bb14:
    mov rsi, [rbp - 240] # reload L28 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 552] # tag L28 from tag-slot
    mov rdx, 1 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 608], rax # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov rsi, [rbp - 192] # reload L22 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L22 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 616], rax # store tag L36
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 304], r10 # spill L36 to slot
    mov r10, [rbp - 296] # reload L35 from spill slot
    mov r11, [rbp - 304] # reload L36 from spill slot
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 608] # tag L35 from tag-slot
    mov rcx, [rbp - 304] # reload L36 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 616] # tag L36 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 624], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov r11, [rbp - 312] # reload L37 from spill slot
    mov r10, r11 # assign L34
    mov r11, [rbp - 624] # tag L37 from tag-slot
    mov [rbp - 600], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    jmp .Lb2dd_rt_regex_match_full_bb16 # branch
.Lb2dd_rt_regex_match_full_bb15:
    mov r11, [rbp - 280] # reload L33 from spill slot
    mov r10, r11 # assign L34
    mov r11, [rbp - 592] # tag L33 from tag-slot
    mov [rbp - 600], r11 # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    jmp .Lb2dd_rt_regex_match_full_bb16 # branch
.Lb2dd_rt_regex_match_full_bb16:
    mov r10, [rbp - 288] # reload L34 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_match_full_bb18 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_match_full_bb17 # jump -> then
.Lb2dd_rt_regex_match_full_bb17:
    mov rdx, 1 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_match_full_bb18:
    mov rdx, 0 # hv arg payload
    mov rax, 2 # tag const_bool = TAG_BOOL
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.globl rt_regex_search
.hidden rt_regex_search
    .p2align 4
rt_regex_search:
    .loc 1 223 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 592 # prologue: alloc spill frame
    mov [rbp - 328], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 336], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_rt_regex_search_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 328] # tag L0 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 344], rax # store tag L2
    mov r13, rdx # hv: unbox call result (rdx)
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 344] # tag L2 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r14, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 352], r11 # store tag L3
    test r14, r14 # br_cond test
    jz .Lb2dd_rt_regex_search_bb2 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_search_bb1 # jump -> then
.Lb2dd_rt_regex_search_bb1:
    mov r15, r14 # assign L4
    mov r11, [rbp - 352] # tag L3 from tag-slot
    mov [rbp - 360], r11 # store tag L4
    jmp .Lb2dd_rt_regex_search_bb3 # branch
.Lb2dd_rt_regex_search_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 336] # tag L1 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 368], rax # store tag L5
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    mov rsi, [rbp - 56] # reload L5 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 368] # tag L5 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 376], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov r15, r10 # assign L4
    mov r11, [rbp - 376] # tag L6 from tag-slot
    mov [rbp - 360], r11 # store tag L4
    jmp .Lb2dd_rt_regex_search_bb3 # branch
.Lb2dd_rt_regex_search_bb3:
    test r15, r15 # br_cond test
    jz .Lb2dd_rt_regex_search_bb5 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_search_bb4 # jump -> then
.Lb2dd_rt_regex_search_bb4:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 392], rax # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov rdx, [rbp - 80] # reload L8 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 392] # tag L8 from tag-slot
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_search_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 328] # tag L0 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 400], rax # store tag L9
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 88], r10 # spill L9 to slot
    mov r11, [rbp - 88] # reload L9 from spill slot
    mov r10, r11 # assign L10
    mov r11, [rbp - 400] # tag L9 from tag-slot
    mov [rbp - 408], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 336] # tag L1 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 416], rax # store tag L11
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 104], r10 # spill L11 to slot
    mov r11, [rbp - 104] # reload L11 from spill slot
    mov r10, r11 # assign L12
    mov r11, [rbp - 416] # tag L11 from tag-slot
    mov [rbp - 424], r11 # store tag L12
    mov [rbp - 112], r10 # spill L12 to slot
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 408] # tag L10 from tag-slot
    call _rt_re_has_backref_lookaround # call _rt_re_has_backref_lookaround
    mov [rbp - 432], rax # store tag L13
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_search_bb7 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_search_bb6 # jump -> then
.Lb2dd_rt_regex_search_bb6:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 448], rax # store tag L15
    mov [rbp - 136], r10 # spill L15 to slot
    mov rdx, [rbp - 136] # reload L15 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 448] # tag L15 from tag-slot
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_search_bb7:
    mov rsi, [rbp - 96] # reload L10 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 408] # tag L10 from tag-slot
    call _rt_re_prep_pat # call _rt_re_prep_pat
    mov [rbp - 456], rax # store tag L16
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 144], r10 # spill L16 to slot
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov r10, r11 # assign L17
    mov r11, [rbp - 456] # tag L16 from tag-slot
    mov [rbp - 464], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 464] # tag L17 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 472], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 472] # tag L18 from tag-slot
    call regex_compile # call regex_compile
    mov [rbp - 480], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 480] # tag L19 from tag-slot
    mov [rbp - 488], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov rsi, [rbp - 176] # reload L20 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 488] # tag L20 from tag-slot
    call regex_valid # call regex_valid
    mov [rbp - 496], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov rsi, [rbp - 184] # reload L21 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 496] # tag L21 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 504], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_search_bb9 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_search_bb8 # jump -> then
.Lb2dd_rt_regex_search_bb8:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 520], rax # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov rdx, [rbp - 208] # reload L24 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 520] # tag L24 from tag-slot
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_search_bb9:
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L25
    mov r11, [rbp - 424] # tag L12 from tag-slot
    mov [rbp - 528], r11 # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 464] # tag L17 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 536], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r10, [rbp - 224] # reload L26 from spill slot
    mov rsi, [rbp - 224] # reload L26 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L26 from tag-slot
    lea rcx, [rip+.LCstr25] # hv arg payload: &str .LCstr25
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 544], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, [rbp - 232] # reload L27 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_search_bb11 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_search_bb10 # jump -> then
.Lb2dd_rt_regex_search_bb10:
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 424] # tag L12 from tag-slot
    call _rt_re_ascii_fold # call _rt_re_ascii_fold
    mov [rbp - 560], rax # store tag L29
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 248], r10 # spill L29 to slot
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L25
    mov r11, [rbp - 560] # tag L29 from tag-slot
    mov [rbp - 528], r11 # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    jmp .Lb2dd_rt_regex_search_bb11 # branch
.Lb2dd_rt_regex_search_bb11:
    mov rsi, [rbp - 176] # reload L20 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 488] # tag L20 from tag-slot
    mov rcx, [rbp - 216] # reload L25 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 528] # tag L25 from tag-slot
    call regex_search # call regex_search
    mov [rbp - 568], rax # store tag L30
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 256], r10 # spill L30 to slot
    mov r11, [rbp - 256] # reload L30 from spill slot
    mov r10, r11 # assign L31
    mov r11, [rbp - 568] # tag L30 from tag-slot
    mov [rbp - 576], r11 # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 576] # tag L31 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 584], r11 # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, [rbp - 272] # reload L32 from spill slot
    mov rsi, [rbp - 272] # reload L32 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 584] # tag L32 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 592], rax # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r10, [rbp - 280] # reload L33 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_search_bb13 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_search_bb12 # jump -> then
.Lb2dd_rt_regex_search_bb12:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 608], rax # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov rdx, [rbp - 296] # reload L35 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 608] # tag L35 from tag-slot
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_search_bb13:
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 576] # tag L31 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 616], rax # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 576] # tag L31 from tag-slot
    mov rdx, 1 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 624], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 632], rax # store tag L38
    mov rcx, [rbp - 304] # reload L36 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 616] # tag L36 from tag-slot
    mov rdi, [rbp - 632] # tag L38 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 632], rax # store tag L38
    mov rcx, [rbp - 312] # reload L37 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 624] # tag L37 from tag-slot
    mov rdi, [rbp - 632] # tag L38 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 632], rax # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov rdx, [rbp - 320] # reload L38 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 632] # tag L38 from tag-slot
    add rsp, 592 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.globl rt_regex_findall
.hidden rt_regex_findall
    .p2align 4
rt_regex_findall:
    .loc 1 243 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 736 # prologue: alloc spill frame
    mov [rbp - 400], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 408], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_rt_regex_findall_bb0:
    call hexa_array_new # array_lit: new array
    mov r13, rdx # array_lit: capture new array payload
    mov [rbp - 416], rax # store tag L2
    mov r14, r13 # assign L3
    mov r11, [rbp - 416] # tag L2 from tag-slot
    mov [rbp - 424], r11 # store tag L3
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 400] # tag L0 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 432], rax # store tag L4
    mov r15, rdx # hv: unbox call result (rdx)
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 432] # tag L4 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 440], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_findall_bb2 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_findall_bb1 # jump -> then
.Lb2dd_rt_regex_findall_bb1:
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 440] # tag L5 from tag-slot
    mov [rbp - 448], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    jmp .Lb2dd_rt_regex_findall_bb3 # branch
.Lb2dd_rt_regex_findall_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 408] # tag L1 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 456], rax # store tag L7
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 456] # tag L7 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 464], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 464] # tag L8 from tag-slot
    mov [rbp - 448], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    jmp .Lb2dd_rt_regex_findall_bb3 # branch
.Lb2dd_rt_regex_findall_bb3:
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_findall_bb5 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_findall_bb4 # jump -> then
.Lb2dd_rt_regex_findall_bb4:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 424] # tag L3 from tag-slot
    add rsp, 736 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_findall_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 400] # tag L0 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 480], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 480] # tag L10 from tag-slot
    mov [rbp - 488], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 408] # tag L1 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 496], rax # store tag L12
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 112], r10 # spill L12 to slot
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L13
    mov r11, [rbp - 496] # tag L12 from tag-slot
    mov [rbp - 504], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 488] # tag L11 from tag-slot
    call _rt_re_has_backref_lookaround # call _rt_re_has_backref_lookaround
    mov [rbp - 512], rax # store tag L14
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_findall_bb7 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_findall_bb6 # jump -> then
.Lb2dd_rt_regex_findall_bb6:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 424] # tag L3 from tag-slot
    add rsp, 736 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_findall_bb7:
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 488] # tag L11 from tag-slot
    call _rt_re_prep_pat # call _rt_re_prep_pat
    mov [rbp - 528], rax # store tag L16
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 144], r10 # spill L16 to slot
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov r10, r11 # assign L17
    mov r11, [rbp - 528] # tag L16 from tag-slot
    mov [rbp - 536], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L17 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 544], rax # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 544] # tag L18 from tag-slot
    call regex_compile # call regex_compile
    mov [rbp - 552], rax # store tag L19
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 168], r10 # spill L19 to slot
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L20
    mov r11, [rbp - 552] # tag L19 from tag-slot
    mov [rbp - 560], r11 # store tag L20
    mov [rbp - 176], r10 # spill L20 to slot
    mov rsi, [rbp - 176] # reload L20 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 560] # tag L20 from tag-slot
    call regex_valid # call regex_valid
    mov [rbp - 568], rax # store tag L21
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 184], r10 # spill L21 to slot
    mov rsi, [rbp - 184] # reload L21 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 568] # tag L21 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 576], r11 # store tag L22
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_findall_bb9 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_findall_bb8 # jump -> then
.Lb2dd_rt_regex_findall_bb8:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 424] # tag L3 from tag-slot
    add rsp, 736 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_findall_bb9:
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 504] # tag L13 from tag-slot
    mov [rbp - 592], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L17 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 600], rax # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov r10, [rbp - 216] # reload L25 from spill slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 600] # tag L25 from tag-slot
    lea rcx, [rip+.LCstr25] # hv arg payload: &str .LCstr25
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 608], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov r10, [rbp - 224] # reload L26 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_findall_bb11 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_findall_bb10 # jump -> then
.Lb2dd_rt_regex_findall_bb10:
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L13 from tag-slot
    call _rt_re_ascii_fold # call _rt_re_ascii_fold
    mov [rbp - 624], rax # store tag L28
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 240], r10 # spill L28 to slot
    mov r11, [rbp - 240] # reload L28 from spill slot
    mov r10, r11 # assign L24
    mov r11, [rbp - 624] # tag L28 from tag-slot
    mov [rbp - 592], r11 # store tag L24
    mov [rbp - 208], r10 # spill L24 to slot
    jmp .Lb2dd_rt_regex_findall_bb11 # branch
.Lb2dd_rt_regex_findall_bb11:
    mov rsi, [rbp - 208] # reload L24 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L24 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 632], rax # store tag L29
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 248], r10 # spill L29 to slot
    mov r11, [rbp - 248] # reload L29 from spill slot
    mov r10, r11 # assign L30
    mov r11, [rbp - 632] # tag L29 from tag-slot
    mov [rbp - 640], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov r10, 0 # assign L31
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 648], r11 # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    jmp .Lb2dd_rt_regex_findall_bb12 # branch
.Lb2dd_rt_regex_findall_bb12:
    mov r10, [rbp - 264] # reload L31 from spill slot
    mov r11, [rbp - 256] # reload L30 from spill slot
    mov rsi, [rbp - 264] # reload L31 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L31 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 256] # reload L30 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 640] # tag L30 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 656], rax # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, [rbp - 272] # reload L32 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_findall_bb14 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_findall_bb13 # jump -> then
.Lb2dd_rt_regex_findall_bb13:
    mov r10, [rbp - 264] # reload L31 from spill slot
    cmp r10, 0 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 664], r11 # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    sub rsp, 16 # hv: reserve 1 16B stack arg slots
    mov r11, [rbp - 280] # reload L33 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 664] # tag L33 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, [rbp - 176] # reload L20 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 560] # tag L20 from tag-slot
    mov rcx, [rbp - 208] # reload L24 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 592] # tag L24 from tag-slot
    mov r9, [rbp - 264] # reload L31 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L31 = TAG_INT (i64-local, fused)
    call regex_search_from # call regex_search_from
    add rsp, 16 # hv: pop 1 stack arg slots
    mov [rbp - 672], rax # store tag L34
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 288], r10 # spill L34 to slot
    mov r11, [rbp - 288] # reload L34 from spill slot
    mov r10, r11 # assign L35
    mov r11, [rbp - 672] # tag L34 from tag-slot
    mov [rbp - 680], r11 # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 680] # tag L35 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 688], r11 # store tag L36
    mov [rbp - 304], r10 # spill L36 to slot
    mov r10, [rbp - 304] # reload L36 from spill slot
    mov rsi, [rbp - 304] # reload L36 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L36 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 696], rax # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov r10, [rbp - 312] # reload L37 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_findall_bb16 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_findall_bb15 # jump -> then
.Lb2dd_rt_regex_findall_bb14:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 424] # tag L3 from tag-slot
    add rsp, 736 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_findall_bb15:
    jmp .Lb2dd_rt_regex_findall_bb14 # branch
.Lb2dd_rt_regex_findall_bb16:
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 680] # tag L35 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 712], rax # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov r11, [rbp - 328] # reload L39 from spill slot
    mov r10, r11 # assign L40
    mov r11, [rbp - 712] # tag L39 from tag-slot
    mov [rbp - 720], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov rsi, [rbp - 296] # reload L35 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 680] # tag L35 from tag-slot
    mov rdx, 1 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 728], rax # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r11, [rbp - 344] # reload L41 from spill slot
    mov r10, r11 # assign L42
    mov r11, [rbp - 728] # tag L41 from tag-slot
    mov [rbp - 736], r11 # store tag L42
    mov [rbp - 352], r10 # spill L42 to slot
    mov r10, [rbp - 352] # reload L42 from spill slot
    mov r11, [rbp - 336] # reload L40 from spill slot
    mov rsi, [rbp - 352] # reload L42 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 736] # tag L42 from tag-slot
    mov rcx, [rbp - 336] # reload L40 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 720] # tag L40 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 744], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov r10, [rbp - 360] # reload L43 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_findall_bb18 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_findall_bb17 # jump -> then
.Lb2dd_rt_regex_findall_bb17:
    mov r10, [rbp - 264] # reload L31 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 760], r11 # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov r11, [rbp - 376] # reload L45 from spill slot
    mov r10, r11 # assign L31
    mov r11, 0 # tag L45 = TAG_INT (i64-local, fused)
    mov [rbp - 648], r11 # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    jmp .Lb2dd_rt_regex_findall_bb12 # branch
.Lb2dd_rt_regex_findall_bb18:
    call hexa_array_new # array_lit: new array
    mov r10, rdx # array_lit: capture new array payload
    mov [rbp - 768], rax # store tag L46
    mov rcx, [rbp - 336] # reload L40 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 720] # tag L40 from tag-slot
    mov rdi, [rbp - 768] # tag L46 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 0
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 768], rax # store tag L46
    mov rcx, [rbp - 352] # reload L42 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 736] # tag L42 from tag-slot
    mov rdi, [rbp - 768] # tag L46 from tag-slot
    mov rsi, r10 # array_lit: arr payload
    call hexa_array_push # array_lit: push elem 1
    mov r10, rdx # array_lit: recapture array payload
    mov [rbp - 768], rax # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 424] # tag L3 from tag-slot
    mov rcx, [rbp - 384] # reload L46 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 768] # tag L46 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 776], rax # store tag L47
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 392], r10 # spill L47 to slot
    mov r11, [rbp - 352] # reload L42 from spill slot
    mov r10, r11 # assign L31
    mov r11, [rbp - 736] # tag L42 from tag-slot
    mov [rbp - 648], r11 # store tag L31
    mov [rbp - 264], r10 # spill L31 to slot
    jmp .Lb2dd_rt_regex_findall_bb12 # branch
    mov eax, 4 # value-less return: tag = TAG_VOID
    xor edx, edx # value-less return: payload = 0
    add rsp, 736 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.globl rt_regex_split
.hidden rt_regex_split
    .p2align 4
rt_regex_split:
    .loc 1 275 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 832 # prologue: alloc spill frame
    mov [rbp - 448], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 456], rdx # store tag L1
    mov r12, rcx # ingress param payload
.Lb2dd_rt_regex_split_bb0:
    call hexa_array_new # array_lit: new array
    mov r13, rdx # array_lit: capture new array payload
    mov [rbp - 464], rax # store tag L2
    mov r14, r13 # assign L3
    mov r11, [rbp - 464] # tag L2 from tag-slot
    mov [rbp - 472], r11 # store tag L3
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 448] # tag L0 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 480], rax # store tag L4
    mov r15, rdx # hv: unbox call result (rdx)
    mov rsi, r15 # hv arg payload
    mov rdi, [rbp - 480] # tag L4 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 488], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_split_bb2 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_split_bb1 # jump -> then
.Lb2dd_rt_regex_split_bb1:
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 488] # tag L5 from tag-slot
    mov [rbp - 496], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    jmp .Lb2dd_rt_regex_split_bb3 # branch
.Lb2dd_rt_regex_split_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 456] # tag L1 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 504], rax # store tag L7
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 72], r10 # spill L7 to slot
    mov r10, [rbp - 72] # reload L7 from spill slot
    mov rsi, [rbp - 72] # reload L7 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 504] # tag L7 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 512], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    mov r11, [rbp - 80] # reload L8 from spill slot
    mov r10, r11 # assign L6
    mov r11, [rbp - 512] # tag L8 from tag-slot
    mov [rbp - 496], r11 # store tag L6
    mov [rbp - 64], r10 # spill L6 to slot
    jmp .Lb2dd_rt_regex_split_bb3 # branch
.Lb2dd_rt_regex_split_bb3:
    mov r10, [rbp - 64] # reload L6 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_split_bb5 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_split_bb4 # jump -> then
.Lb2dd_rt_regex_split_bb4:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 472] # tag L3 from tag-slot
    add rsp, 832 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_split_bb5:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 448] # tag L0 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 528], rax # store tag L10
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L11
    mov r11, [rbp - 528] # tag L10 from tag-slot
    mov [rbp - 536], r11 # store tag L11
    mov [rbp - 104], r10 # spill L11 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 456] # tag L1 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 544], rax # store tag L12
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 112], r10 # spill L12 to slot
    mov r11, [rbp - 112] # reload L12 from spill slot
    mov r10, r11 # assign L13
    mov r11, [rbp - 544] # tag L12 from tag-slot
    mov [rbp - 552], r11 # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L11 from tag-slot
    call _rt_re_has_backref_lookaround # call _rt_re_has_backref_lookaround
    mov [rbp - 560], rax # store tag L14
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 128], r10 # spill L14 to slot
    mov r10, [rbp - 128] # reload L14 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_split_bb7 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_split_bb6 # jump -> then
.Lb2dd_rt_regex_split_bb6:
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 472] # tag L3 from tag-slot
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 552] # tag L13 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 576], rax # store tag L16
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 144], r10 # spill L16 to slot
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 472] # tag L3 from tag-slot
    add rsp, 832 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_split_bb7:
    mov rsi, [rbp - 104] # reload L11 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 536] # tag L11 from tag-slot
    call _rt_re_prep_pat # call _rt_re_prep_pat
    mov [rbp - 584], rax # store tag L17
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 152], r10 # spill L17 to slot
    mov r11, [rbp - 152] # reload L17 from spill slot
    mov r10, r11 # assign L18
    mov r11, [rbp - 584] # tag L17 from tag-slot
    mov [rbp - 592], r11 # store tag L18
    mov [rbp - 160], r10 # spill L18 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L18 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 600], rax # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, [rbp - 168] # reload L19 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 600] # tag L19 from tag-slot
    call regex_compile # call regex_compile
    mov [rbp - 608], rax # store tag L20
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 176], r10 # spill L20 to slot
    mov r11, [rbp - 176] # reload L20 from spill slot
    mov r10, r11 # assign L21
    mov r11, [rbp - 608] # tag L20 from tag-slot
    mov [rbp - 616], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov rsi, [rbp - 184] # reload L21 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 616] # tag L21 from tag-slot
    call regex_valid # call regex_valid
    mov [rbp - 624], rax # store tag L22
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 192], r10 # spill L22 to slot
    mov rsi, [rbp - 192] # reload L22 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 624] # tag L22 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 632], r11 # store tag L23
    mov [rbp - 200], r10 # spill L23 to slot
    mov r10, [rbp - 200] # reload L23 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_split_bb9 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_split_bb8 # jump -> then
.Lb2dd_rt_regex_split_bb8:
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 472] # tag L3 from tag-slot
    mov rcx, [rbp - 120] # reload L13 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 552] # tag L13 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 648], rax # store tag L25
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 216], r10 # spill L25 to slot
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 472] # tag L3 from tag-slot
    add rsp, 832 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_split_bb9:
    mov r11, [rbp - 120] # reload L13 from spill slot
    mov r10, r11 # assign L26
    mov r11, [rbp - 552] # tag L13 from tag-slot
    mov [rbp - 656], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov rsi, [rbp - 160] # reload L18 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 592] # tag L18 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 664], rax # store tag L27
    mov [rbp - 232], r10 # spill L27 to slot
    mov r10, [rbp - 232] # reload L27 from spill slot
    mov rsi, [rbp - 232] # reload L27 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 664] # tag L27 from tag-slot
    lea rcx, [rip+.LCstr25] # hv arg payload: &str .LCstr25
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 672], rax # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov r10, [rbp - 240] # reload L28 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_split_bb11 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_split_bb10 # jump -> then
.Lb2dd_rt_regex_split_bb10:
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 552] # tag L13 from tag-slot
    call _rt_re_ascii_fold # call _rt_re_ascii_fold
    mov [rbp - 688], rax # store tag L30
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 256], r10 # spill L30 to slot
    mov r11, [rbp - 256] # reload L30 from spill slot
    mov r10, r11 # assign L26
    mov r11, [rbp - 688] # tag L30 from tag-slot
    mov [rbp - 656], r11 # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    jmp .Lb2dd_rt_regex_split_bb11 # branch
.Lb2dd_rt_regex_split_bb11:
    mov rsi, [rbp - 224] # reload L26 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 656] # tag L26 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 696], rax # store tag L31
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 264], r10 # spill L31 to slot
    mov r11, [rbp - 264] # reload L31 from spill slot
    mov r10, r11 # assign L32
    mov r11, [rbp - 696] # tag L31 from tag-slot
    mov [rbp - 704], r11 # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov r10, 0 # assign L33
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 712], r11 # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    jmp .Lb2dd_rt_regex_split_bb12 # branch
.Lb2dd_rt_regex_split_bb12:
    mov r10, [rbp - 280] # reload L33 from spill slot
    mov r11, [rbp - 272] # reload L32 from spill slot
    mov rsi, [rbp - 280] # reload L33 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L33 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 272] # reload L32 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 704] # tag L32 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 720], rax # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_split_bb14 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_split_bb13 # jump -> then
.Lb2dd_rt_regex_split_bb13:
    mov r10, [rbp - 280] # reload L33 from spill slot
    cmp r10, 0 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 728], r11 # store tag L35
    mov [rbp - 296], r10 # spill L35 to slot
    sub rsp, 16 # hv: reserve 1 16B stack arg slots
    mov r11, [rbp - 296] # reload L35 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 728] # tag L35 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, [rbp - 184] # reload L21 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 616] # tag L21 from tag-slot
    mov rcx, [rbp - 224] # reload L26 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 656] # tag L26 from tag-slot
    mov r9, [rbp - 280] # reload L33 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L33 = TAG_INT (i64-local, fused)
    call regex_search_from # call regex_search_from
    add rsp, 16 # hv: pop 1 stack arg slots
    mov [rbp - 736], rax # store tag L36
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 304], r10 # spill L36 to slot
    mov r11, [rbp - 304] # reload L36 from spill slot
    mov r10, r11 # assign L37
    mov r11, [rbp - 736] # tag L36 from tag-slot
    mov [rbp - 744], r11 # store tag L37
    mov [rbp - 312], r10 # spill L37 to slot
    mov rsi, [rbp - 312] # reload L37 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 744] # tag L37 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 752], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov r10, [rbp - 320] # reload L38 from spill slot
    mov rsi, [rbp - 320] # reload L38 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 752] # tag L38 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 760], rax # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    mov r10, [rbp - 328] # reload L39 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_split_bb16 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_split_bb15 # jump -> then
.Lb2dd_rt_regex_split_bb14:
    mov r10, [rbp - 280] # reload L33 from spill slot
    mov r11, [rbp - 272] # reload L32 from spill slot
    mov rsi, [rbp - 280] # reload L33 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L33 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 272] # reload L32 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 704] # tag L32 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 848], rax # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    mov r10, [rbp - 416] # reload L50 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_split_bb20 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_split_bb19 # jump -> then
.Lb2dd_rt_regex_split_bb15:
    jmp .Lb2dd_rt_regex_split_bb14 # branch
.Lb2dd_rt_regex_split_bb16:
    mov rsi, [rbp - 312] # reload L37 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 744] # tag L37 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 776], rax # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r11, [rbp - 344] # reload L41 from spill slot
    mov r10, r11 # assign L42
    mov r11, [rbp - 776] # tag L41 from tag-slot
    mov [rbp - 784], r11 # store tag L42
    mov [rbp - 352], r10 # spill L42 to slot
    mov rsi, [rbp - 312] # reload L37 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 744] # tag L37 from tag-slot
    mov rdx, 1 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 792], rax # store tag L43
    mov [rbp - 360], r10 # spill L43 to slot
    mov r11, [rbp - 360] # reload L43 from spill slot
    mov r10, r11 # assign L44
    mov r11, [rbp - 792] # tag L43 from tag-slot
    mov [rbp - 800], r11 # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov r10, [rbp - 368] # reload L44 from spill slot
    mov r11, [rbp - 352] # reload L42 from spill slot
    mov rsi, [rbp - 368] # reload L44 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 800] # tag L44 from tag-slot
    mov rcx, [rbp - 352] # reload L42 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 784] # tag L42 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 808], rax # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov r10, [rbp - 376] # reload L45 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_split_bb18 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_split_bb17 # jump -> then
.Lb2dd_rt_regex_split_bb17:
    mov r10, [rbp - 280] # reload L33 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 824], r11 # store tag L47
    mov [rbp - 392], r10 # spill L47 to slot
    mov r11, [rbp - 392] # reload L47 from spill slot
    mov r10, r11 # assign L33
    mov r11, 0 # tag L47 = TAG_INT (i64-local, fused)
    mov [rbp - 712], r11 # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    jmp .Lb2dd_rt_regex_split_bb12 # branch
.Lb2dd_rt_regex_split_bb18:
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 552] # tag L13 from tag-slot
    mov rcx, [rbp - 280] # reload L33 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L33 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 352] # reload L42 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 784] # tag L42 from tag-slot
    call hexa_str_substring # call hexa_str_substring
    mov [rbp - 832], rax # store tag L48
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 400], r10 # spill L48 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 472] # tag L3 from tag-slot
    mov rcx, [rbp - 400] # reload L48 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 832] # tag L48 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 840], rax # store tag L49
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 408], r10 # spill L49 to slot
    mov r11, [rbp - 368] # reload L44 from spill slot
    mov r10, r11 # assign L33
    mov r11, [rbp - 800] # tag L44 from tag-slot
    mov [rbp - 712], r11 # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    jmp .Lb2dd_rt_regex_split_bb12 # branch
.Lb2dd_rt_regex_split_bb19:
    mov rsi, [rbp - 120] # reload L13 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 552] # tag L13 from tag-slot
    mov rcx, [rbp - 280] # reload L33 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L33 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 272] # reload L32 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 704] # tag L32 from tag-slot
    call hexa_str_substring # call hexa_str_substring
    mov [rbp - 864], rax # store tag L52
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 432], r10 # spill L52 to slot
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 472] # tag L3 from tag-slot
    mov rcx, [rbp - 432] # reload L52 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 864] # tag L52 from tag-slot
    call hexa_array_push # call hexa_array_push
    mov [rbp - 872], rax # store tag L53
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 440], r10 # spill L53 to slot
    jmp .Lb2dd_rt_regex_split_bb20 # branch
.Lb2dd_rt_regex_split_bb20:
    mov rdx, r14 # hv arg payload
    mov rax, [rbp - 472] # tag L3 from tag-slot
    add rsp, 832 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.globl rt_regex_replace
.hidden rt_regex_replace
    .p2align 4
rt_regex_replace:
    .loc 1 317 0
    push rbp # prologue: save rbp
    mov rbp, rsp # prologue: set rbp
    push rbx # prologue: save rbx
    push r12 # prologue: save r12
    push r13 # prologue: save r13
    push r14 # prologue: save r14
    push r15 # prologue: save r15
    sub rsp, 8 # prologue: callee-save align pad
    sub rsp, 1040 # prologue: alloc spill frame
    mov [rbp - 552], rdi # store tag L0
    mov rbx, rsi # ingress param payload
    mov [rbp - 560], rdx # store tag L1
    mov r12, rcx # ingress param payload
    mov [rbp - 568], r8 # store tag L2
    mov r13, r9 # ingress param payload
.Lb2dd_rt_regex_replace_bb0:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 552] # tag L0 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 576], rax # store tag L3
    mov r14, rdx # hv: unbox call result (rdx)
    mov rsi, r14 # hv arg payload
    mov rdi, [rbp - 576] # tag L3 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r15, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 584], r11 # store tag L4
    test r15, r15 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb2 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb1 # jump -> then
.Lb2dd_rt_regex_replace_bb1:
    mov r10, r15 # assign L5
    mov r11, [rbp - 584] # tag L4 from tag-slot
    mov [rbp - 592], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd_rt_regex_replace_bb3 # branch
.Lb2dd_rt_regex_replace_bb2:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 560] # tag L1 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 600], rax # store tag L6
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 64], r10 # spill L6 to slot
    mov r10, [rbp - 64] # reload L6 from spill slot
    mov rsi, [rbp - 64] # reload L6 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 600] # tag L6 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 608], r11 # store tag L7
    mov [rbp - 72], r10 # spill L7 to slot
    mov r11, [rbp - 72] # reload L7 from spill slot
    mov r10, r11 # assign L5
    mov r11, [rbp - 608] # tag L7 from tag-slot
    mov [rbp - 592], r11 # store tag L5
    mov [rbp - 56], r10 # spill L5 to slot
    jmp .Lb2dd_rt_regex_replace_bb3 # branch
.Lb2dd_rt_regex_replace_bb3:
    mov r10, [rbp - 56] # reload L5 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb5 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb4 # jump -> then
.Lb2dd_rt_regex_replace_bb4:
    mov r11, [rbp - 56] # reload L5 from spill slot
    mov r10, r11 # assign L8
    mov r11, [rbp - 592] # tag L5 from tag-slot
    mov [rbp - 616], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    jmp .Lb2dd_rt_regex_replace_bb6 # branch
.Lb2dd_rt_regex_replace_bb5:
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 568] # tag L2 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 624], rax # store tag L9
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 88], r10 # spill L9 to slot
    mov r10, [rbp - 88] # reload L9 from spill slot
    mov rsi, [rbp - 88] # reload L9 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 624] # tag L9 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop !=: tag-dispatch hexa_eq
    mov rdi, rax # binop !=: eq.tag → truthy arg lo
    mov rsi, rdx # binop !=: eq.payload → truthy arg hi
    call hexa_truthy # binop !=: truthy(eq) → rax
    xor rax, 1 # binop !=: !truthy
    mov r10, rax # binop !=: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 632], r11 # store tag L10
    mov [rbp - 96], r10 # spill L10 to slot
    mov r11, [rbp - 96] # reload L10 from spill slot
    mov r10, r11 # assign L8
    mov r11, [rbp - 632] # tag L10 from tag-slot
    mov [rbp - 616], r11 # store tag L8
    mov [rbp - 80], r10 # spill L8 to slot
    jmp .Lb2dd_rt_regex_replace_bb6 # branch
.Lb2dd_rt_regex_replace_bb6:
    mov r10, [rbp - 80] # reload L8 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb8 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb7 # jump -> then
.Lb2dd_rt_regex_replace_bb7:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 560] # tag L1 from tag-slot
    call hexa_type_of # call hexa_type_of
    mov [rbp - 648], rax # store tag L12
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 112], r10 # spill L12 to slot
    mov r10, [rbp - 112] # reload L12 from spill slot
    mov rsi, [rbp - 112] # reload L12 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 648] # tag L12 from tag-slot
    lea rcx, [rip+.LCstr27] # hv arg payload: &str .LCstr27
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 656], rax # store tag L13
    mov [rbp - 120], r10 # spill L13 to slot
    mov r10, [rbp - 120] # reload L13 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb10 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb9 # jump -> then
.Lb2dd_rt_regex_replace_bb8:
    mov rsi, rbx # hv arg payload
    mov rdi, [rbp - 552] # tag L0 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 680], rax # store tag L16
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 144], r10 # spill L16 to slot
    mov r11, [rbp - 144] # reload L16 from spill slot
    mov r10, r11 # assign L17
    mov r11, [rbp - 680] # tag L16 from tag-slot
    mov [rbp - 688], r11 # store tag L17
    mov [rbp - 152], r10 # spill L17 to slot
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 560] # tag L1 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 696], rax # store tag L18
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 160], r10 # spill L18 to slot
    mov r11, [rbp - 160] # reload L18 from spill slot
    mov r10, r11 # assign L19
    mov r11, [rbp - 696] # tag L18 from tag-slot
    mov [rbp - 704], r11 # store tag L19
    mov [rbp - 168], r10 # spill L19 to slot
    mov rsi, r13 # hv arg payload
    mov rdi, [rbp - 568] # tag L2 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 712], rax # store tag L20
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 176], r10 # spill L20 to slot
    mov r11, [rbp - 176] # reload L20 from spill slot
    mov r10, r11 # assign L21
    mov r11, [rbp - 712] # tag L20 from tag-slot
    mov [rbp - 720], r11 # store tag L21
    mov [rbp - 184], r10 # spill L21 to slot
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L17 from tag-slot
    call _rt_re_has_backref_lookaround # call _rt_re_has_backref_lookaround
    mov [rbp - 728], rax # store tag L22
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 192], r10 # spill L22 to slot
    mov r10, [rbp - 192] # reload L22 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb12 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb11 # jump -> then
.Lb2dd_rt_regex_replace_bb9:
    mov rsi, r12 # hv arg payload
    mov rdi, [rbp - 560] # tag L1 from tag-slot
    call hexa_to_string # call hexa_to_string
    mov [rbp - 672], rax # store tag L15
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 136], r10 # spill L15 to slot
    mov rdx, [rbp - 136] # reload L15 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 672] # tag L15 from tag-slot
    add rsp, 1040 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_replace_bb10:
    lea rdx, [rip+.LCstr24] # hv arg payload: &str .LCstr24
    mov rax, 3 # hv arg tag = TAG_STR
    add rsp, 1040 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_replace_bb11:
    mov rdx, [rbp - 168] # reload L19 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 704] # tag L19 from tag-slot
    add rsp, 1040 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_replace_bb12:
    mov rsi, [rbp - 152] # reload L17 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 688] # tag L17 from tag-slot
    call _rt_re_prep_pat # call _rt_re_prep_pat
    mov [rbp - 744], rax # store tag L24
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 208], r10 # spill L24 to slot
    mov r11, [rbp - 208] # reload L24 from spill slot
    mov r10, r11 # assign L25
    mov r11, [rbp - 744] # tag L24 from tag-slot
    mov [rbp - 752], r11 # store tag L25
    mov [rbp - 216], r10 # spill L25 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 752] # tag L25 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 760], rax # store tag L26
    mov [rbp - 224], r10 # spill L26 to slot
    mov rsi, [rbp - 224] # reload L26 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 760] # tag L26 from tag-slot
    call regex_compile # call regex_compile
    mov [rbp - 768], rax # store tag L27
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 232], r10 # spill L27 to slot
    mov r11, [rbp - 232] # reload L27 from spill slot
    mov r10, r11 # assign L28
    mov r11, [rbp - 768] # tag L27 from tag-slot
    mov [rbp - 776], r11 # store tag L28
    mov [rbp - 240], r10 # spill L28 to slot
    mov rsi, [rbp - 240] # reload L28 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 776] # tag L28 from tag-slot
    call regex_valid # call regex_valid
    mov [rbp - 784], rax # store tag L29
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 248], r10 # spill L29 to slot
    mov rsi, [rbp - 248] # reload L29 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 784] # tag L29 from tag-slot
    call hexa_truthy # unop !: truthy(x) → rax
    xor rax, 1 # unop !: !truthy
    mov r10, rax # unop !: capture bool payload
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 792], r11 # store tag L30
    mov [rbp - 256], r10 # spill L30 to slot
    mov r10, [rbp - 256] # reload L30 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb14 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb13 # jump -> then
.Lb2dd_rt_regex_replace_bb13:
    mov rdx, [rbp - 168] # reload L19 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 704] # tag L19 from tag-slot
    add rsp, 1040 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.Lb2dd_rt_regex_replace_bb14:
    mov r11, [rbp - 168] # reload L19 from spill slot
    mov r10, r11 # assign L32
    mov r11, [rbp - 704] # tag L19 from tag-slot
    mov [rbp - 808], r11 # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    mov rsi, [rbp - 216] # reload L25 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 752] # tag L25 from tag-slot
    mov rcx, 1 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_index_get # index: hexa_index_get
    mov r10, rdx # index: capture element payload
    mov [rbp - 816], rax # store tag L33
    mov [rbp - 280], r10 # spill L33 to slot
    mov r10, [rbp - 280] # reload L33 from spill slot
    mov rsi, [rbp - 280] # reload L33 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 816] # tag L33 from tag-slot
    lea rcx, [rip+.LCstr25] # hv arg payload: &str .LCstr25
    mov rdx, 3 # hv arg tag = TAG_STR
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 824], rax # store tag L34
    mov [rbp - 288], r10 # spill L34 to slot
    mov r10, [rbp - 288] # reload L34 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb16 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb15 # jump -> then
.Lb2dd_rt_regex_replace_bb15:
    mov rsi, [rbp - 168] # reload L19 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L19 from tag-slot
    call _rt_re_ascii_fold # call _rt_re_ascii_fold
    mov [rbp - 840], rax # store tag L36
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 304], r10 # spill L36 to slot
    mov r11, [rbp - 304] # reload L36 from spill slot
    mov r10, r11 # assign L32
    mov r11, [rbp - 840] # tag L36 from tag-slot
    mov [rbp - 808], r11 # store tag L32
    mov [rbp - 272], r10 # spill L32 to slot
    jmp .Lb2dd_rt_regex_replace_bb16 # branch
.Lb2dd_rt_regex_replace_bb16:
    mov rsi, [rbp - 272] # reload L32 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 808] # tag L32 from tag-slot
    call hexa_byte_len # call hexa_byte_len
    mov [rbp - 848], rax # store tag L37
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 312], r10 # spill L37 to slot
    mov r11, [rbp - 312] # reload L37 from spill slot
    mov r10, r11 # assign L38
    mov r11, [rbp - 848] # tag L37 from tag-slot
    mov [rbp - 856], r11 # store tag L38
    mov [rbp - 320], r10 # spill L38 to slot
    mov r10, 0 # assign L39
    mov r11, 0 # tag default = TAG_INT
    mov [rbp - 864], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    lea r10, [rip+.LCstr24] # assign L40: &str .LCstr24
    mov r11, 3 # tag const_str = TAG_STR
    mov [rbp - 872], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    jmp .Lb2dd_rt_regex_replace_bb17 # branch
.Lb2dd_rt_regex_replace_bb17:
    mov r10, [rbp - 328] # reload L39 from spill slot
    mov r11, [rbp - 320] # reload L38 from spill slot
    mov rsi, [rbp - 328] # reload L39 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L39 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 320] # reload L38 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 856] # tag L38 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 880], rax # store tag L41
    mov [rbp - 344], r10 # spill L41 to slot
    mov r10, [rbp - 344] # reload L41 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb19 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb18 # jump -> then
.Lb2dd_rt_regex_replace_bb18:
    mov r10, [rbp - 328] # reload L39 from spill slot
    cmp r10, 0 # binop >
    setg al # binop > → al
    movzx r10, al # zero-extend al into dst
    mov r11, 2 # materialize tag imm 2
    mov [rbp - 888], r11 # store tag L42
    mov [rbp - 352], r10 # spill L42 to slot
    sub rsp, 16 # hv: reserve 1 16B stack arg slots
    mov r11, [rbp - 352] # reload L42 from spill slot
    mov r11, r11 # hv arg payload
    mov r10, [rbp - 888] # tag L42 from tag-slot
    mov [rsp], r10 # hv stack arg 3 tag
    mov [rsp + 8], r11 # hv stack arg 3 payload
    mov rsi, [rbp - 240] # reload L28 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 776] # tag L28 from tag-slot
    mov rcx, [rbp - 272] # reload L32 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 808] # tag L32 from tag-slot
    mov r9, [rbp - 328] # reload L39 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L39 = TAG_INT (i64-local, fused)
    call regex_search_from # call regex_search_from
    add rsp, 16 # hv: pop 1 stack arg slots
    mov [rbp - 896], rax # store tag L43
    mov r10, rdx # hv: unbox user-call result payload
    mov [rbp - 360], r10 # spill L43 to slot
    mov r11, [rbp - 360] # reload L43 from spill slot
    mov r10, r11 # assign L44
    mov r11, [rbp - 896] # tag L43 from tag-slot
    mov [rbp - 904], r11 # store tag L44
    mov [rbp - 368], r10 # spill L44 to slot
    mov rsi, [rbp - 368] # reload L44 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 904] # tag L44 from tag-slot
    call hexa_arr_poly_len # call hexa_arr_poly_len
    mov r10, rax # hv: unbox call result (rax)
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 912], r11 # store tag L45
    mov [rbp - 376], r10 # spill L45 to slot
    mov r10, [rbp - 376] # reload L45 from spill slot
    mov rsi, [rbp - 376] # reload L45 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 912] # tag L45 from tag-slot
    mov rcx, 0 # hv arg payload
    mov rdx, 0 # tag default = TAG_INT
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 920], rax # store tag L46
    mov [rbp - 384], r10 # spill L46 to slot
    mov r10, [rbp - 384] # reload L46 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb21 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb20 # jump -> then
.Lb2dd_rt_regex_replace_bb19:
    mov r10, [rbp - 328] # reload L39 from spill slot
    mov r11, [rbp - 320] # reload L38 from spill slot
    mov rsi, [rbp - 328] # reload L39 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L39 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 320] # reload L38 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 856] # tag L38 from tag-slot
    call hexa_cmp_le # binop <=: tag-dispatch hexa_cmp_le
    mov r10, rdx # binop <=: capture bool payload
    mov [rbp - 1056], rax # store tag L63
    mov [rbp - 520], r10 # spill L63 to slot
    mov r10, [rbp - 520] # reload L63 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb27 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb26 # jump -> then
.Lb2dd_rt_regex_replace_bb20:
    jmp .Lb2dd_rt_regex_replace_bb19 # branch
.Lb2dd_rt_regex_replace_bb21:
    mov rsi, [rbp - 368] # reload L44 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 904] # tag L44 from tag-slot
    mov rdx, 0 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 936], rax # store tag L48
    mov [rbp - 400], r10 # spill L48 to slot
    mov r11, [rbp - 400] # reload L48 from spill slot
    mov r10, r11 # assign L49
    mov r11, [rbp - 936] # tag L48 from tag-slot
    mov [rbp - 944], r11 # store tag L49
    mov [rbp - 408], r10 # spill L49 to slot
    mov rsi, [rbp - 368] # reload L44 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 904] # tag L44 from tag-slot
    mov rdx, 1 # hv payload
    call hexa_arr_poly_get # index: hexa_arr_poly_get (runtime discriminate)
    mov r10, rdx # index: capture poly element payload
    mov [rbp - 952], rax # store tag L50
    mov [rbp - 416], r10 # spill L50 to slot
    mov r11, [rbp - 416] # reload L50 from spill slot
    mov r10, r11 # assign L51
    mov r11, [rbp - 952] # tag L50 from tag-slot
    mov [rbp - 960], r11 # store tag L51
    mov [rbp - 424], r10 # spill L51 to slot
    mov r10, [rbp - 424] # reload L51 from spill slot
    mov r11, [rbp - 408] # reload L49 from spill slot
    mov rsi, [rbp - 424] # reload L51 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 960] # tag L51 from tag-slot
    mov rcx, [rbp - 408] # reload L49 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 944] # tag L49 from tag-slot
    call hexa_eq # binop ==: tag-dispatch hexa_eq
    mov r10, rdx # binop ==: capture bool payload
    mov [rbp - 968], rax # store tag L52
    mov [rbp - 432], r10 # spill L52 to slot
    mov r10, [rbp - 432] # reload L52 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb23 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb22 # jump -> then
.Lb2dd_rt_regex_replace_bb22:
    mov r10, [rbp - 328] # reload L39 from spill slot
    mov r11, [rbp - 320] # reload L38 from spill slot
    mov rsi, [rbp - 328] # reload L39 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, 0 # tag L39 = TAG_INT (i64-local, fused)
    mov rcx, [rbp - 320] # reload L38 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 856] # tag L38 from tag-slot
    call hexa_cmp_lt # binop <: tag-dispatch hexa_cmp_lt
    mov r10, rdx # binop <: capture bool payload
    mov [rbp - 984], rax # store tag L54
    mov [rbp - 448], r10 # spill L54 to slot
    mov r10, [rbp - 448] # reload L54 from spill slot
    test r10, r10 # br_cond test
    jz .Lb2dd_rt_regex_replace_bb25 # jump-if-zero -> else
    jmp .Lb2dd_rt_regex_replace_bb24 # jump -> then
.Lb2dd_rt_regex_replace_bb23:
    mov rsi, [rbp - 168] # reload L19 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L19 from tag-slot
    mov rcx, [rbp - 328] # reload L39 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L39 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 408] # reload L49 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 944] # tag L49 from tag-slot
    call hexa_str_substring # call hexa_str_substring
    mov [rbp - 1032], rax # store tag L60
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 496], r10 # spill L60 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    mov r11, [rbp - 496] # reload L60 from spill slot
    mov rsi, [rbp - 336] # reload L40 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 872] # tag L40 from tag-slot
    mov rcx, [rbp - 496] # reload L60 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1032] # tag L60 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 1040], rax # store tag L61
    mov [rbp - 504], r10 # spill L61 to slot
    mov r11, [rbp - 504] # reload L61 from spill slot
    mov r10, r11 # assign L40
    mov r11, [rbp - 1040] # tag L61 from tag-slot
    mov [rbp - 872], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    mov r11, [rbp - 184] # reload L21 from spill slot
    mov rsi, [rbp - 336] # reload L40 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 872] # tag L40 from tag-slot
    mov rcx, [rbp - 184] # reload L21 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 720] # tag L21 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 1048], rax # store tag L62
    mov [rbp - 512], r10 # spill L62 to slot
    mov r11, [rbp - 512] # reload L62 from spill slot
    mov r10, r11 # assign L40
    mov r11, [rbp - 1048] # tag L62 from tag-slot
    mov [rbp - 872], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    mov r11, [rbp - 424] # reload L51 from spill slot
    mov r10, r11 # assign L39
    mov r11, [rbp - 960] # tag L51 from tag-slot
    mov [rbp - 864], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    jmp .Lb2dd_rt_regex_replace_bb17 # branch
.Lb2dd_rt_regex_replace_bb24:
    mov r10, [rbp - 328] # reload L39 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1000], r11 # store tag L56
    mov [rbp - 464], r10 # spill L56 to slot
    mov rsi, [rbp - 168] # reload L19 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L19 from tag-slot
    mov rcx, [rbp - 328] # reload L39 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L39 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 464] # reload L56 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, 0 # tag L56 = TAG_INT (i64-local, fused)
    call hexa_str_substring # call hexa_str_substring
    mov [rbp - 1008], rax # store tag L57
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 472], r10 # spill L57 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    mov r11, [rbp - 472] # reload L57 from spill slot
    mov rsi, [rbp - 336] # reload L40 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 872] # tag L40 from tag-slot
    mov rcx, [rbp - 472] # reload L57 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1008] # tag L57 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 1016], rax # store tag L58
    mov [rbp - 480], r10 # spill L58 to slot
    mov r11, [rbp - 480] # reload L58 from spill slot
    mov r10, r11 # assign L40
    mov r11, [rbp - 1016] # tag L58 from tag-slot
    mov [rbp - 872], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    jmp .Lb2dd_rt_regex_replace_bb25 # branch
.Lb2dd_rt_regex_replace_bb25:
    mov r10, [rbp - 328] # reload L39 from spill slot
    mov r10, r10 # binop lhs into dst
    add r10, 1 # binop +
    mov r11, 0 # materialize tag imm 0
    mov [rbp - 1024], r11 # store tag L59
    mov [rbp - 488], r10 # spill L59 to slot
    mov r11, [rbp - 488] # reload L59 from spill slot
    mov r10, r11 # assign L39
    mov r11, 0 # tag L59 = TAG_INT (i64-local, fused)
    mov [rbp - 864], r11 # store tag L39
    mov [rbp - 328], r10 # spill L39 to slot
    jmp .Lb2dd_rt_regex_replace_bb17 # branch
.Lb2dd_rt_regex_replace_bb26:
    mov rsi, [rbp - 168] # reload L19 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 704] # tag L19 from tag-slot
    mov rcx, [rbp - 328] # reload L39 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, 0 # tag L39 = TAG_INT (i64-local, fused)
    mov r9, [rbp - 320] # reload L38 from spill slot
    mov r9, r9 # hv arg payload
    mov r8, [rbp - 856] # tag L38 from tag-slot
    call hexa_str_substring # call hexa_str_substring
    mov [rbp - 1072], rax # store tag L65
    mov r10, rdx # hv: unbox call result (rdx)
    mov [rbp - 536], r10 # spill L65 to slot
    mov r10, [rbp - 336] # reload L40 from spill slot
    mov r11, [rbp - 536] # reload L65 from spill slot
    mov rsi, [rbp - 336] # reload L40 from spill slot
    mov rsi, rsi # hv arg payload
    mov rdi, [rbp - 872] # tag L40 from tag-slot
    mov rcx, [rbp - 536] # reload L65 from spill slot
    mov rcx, rcx # hv arg payload
    mov rdx, [rbp - 1072] # tag L65 from tag-slot
    call hexa_add_slow # binop +: tag-dispatch hexa_add_slow
    mov r10, rdx # binop +: capture result payload
    mov [rbp - 1080], rax # store tag L66
    mov [rbp - 544], r10 # spill L66 to slot
    mov r11, [rbp - 544] # reload L66 from spill slot
    mov r10, r11 # assign L40
    mov r11, [rbp - 1080] # tag L66 from tag-slot
    mov [rbp - 872], r11 # store tag L40
    mov [rbp - 336], r10 # spill L40 to slot
    jmp .Lb2dd_rt_regex_replace_bb27 # branch
.Lb2dd_rt_regex_replace_bb27:
    mov rdx, [rbp - 336] # reload L40 from spill slot
    mov rdx, rdx # hv arg payload
    mov rax, [rbp - 872] # tag L40 from tag-slot
    add rsp, 1040 # epilogue: free spill frame
    add rsp, 8 # epilogue: drop callee-save align pad
    pop r15 # epilogue: restore r15
    pop r14 # epilogue: restore r14
    pop r13 # epilogue: restore r13
    pop r12 # epilogue: restore r12
    pop rbx # epilogue: restore rbx
    pop rbp # epilogue: restore rbp
    ret # return
.section .rodata
.LCstr0:
    .byte 0x70, 0x6f, 0x73, 0x00
.section .rodata
.LCstr1:
    .byte 0x70, 0x6c, 0x65, 0x6e, 0x00
.section .rodata
.LCstr2:
    .byte 0x70, 0x61, 0x74, 0x00
.section .rodata
.LCstr3:
    .byte 0x6e, 0x6f, 0x64, 0x65, 0x73, 0x00
.section .rodata
.LCstr4:
    .byte 0x63, 0x6c, 0x61, 0x73, 0x73, 0x65, 0x73, 0x00
.section .rodata
.LCstr5:
    .byte 0x6f, 0x6b, 0x00
.section .rodata
.LCstr6:
    .byte 0x6e, 0x67, 0x00
.section .rodata
.LCstr7:
    .byte 0x42, 0x74, 0x50, 0x72, 0x6f, 0x67, 0x00
.section .rodata
.LCstr8:
    .byte 0x72, 0x6f, 0x6f, 0x74, 0x00
.section .rodata
.LCstr9:
    .byte 0x6e, 0x67, 0x72, 0x6f, 0x75, 0x70, 0x73, 0x00
.section .rodata
.LCstr10:
    .byte 0x6d, 0x61, 0x78, 0x5f, 0x73, 0x74, 0x65, 0x70, 0x73, 0x00
.section .rodata
.LCstr11:
    .byte 0x73, 0x74, 0x00
.section .rodata
.LCstr12:
    .byte 0x42, 0x74, 0x50, 0x61, 0x72, 0x73, 0x65, 0x00
.section .rodata
.LCstr13:
    .byte 0x6e, 0x6b, 0x69, 0x6e, 0x64, 0x00
.section .rodata
.LCstr14:
    .byte 0x6e, 0x63, 0x00
.section .rodata
.LCstr15:
    .byte 0x6e, 0x6f, 0x75, 0x74, 0x00
.section .rodata
.LCstr16:
    .byte 0x6e, 0x6f, 0x75, 0x74, 0x31, 0x00
.section .rodata
.LCstr17:
    .byte 0x52, 0x65, 0x67, 0x65, 0x78, 0x00
.section .rodata
.LCstr18:
    .byte 0x73, 0x74, 0x61, 0x72, 0x74, 0x00
.section .rodata
.LCstr19:
    .byte 0x6e, 0x73, 0x74, 0x61, 0x74, 0x65, 0x73, 0x00
.section .rodata
.LCstr20:
    .byte 0x65, 0x6e, 0x67, 0x69, 0x6e, 0x65, 0x00
.section .rodata
.LCstr21:
    .byte 0x62, 0x74, 0x00
.section .rodata
.LCstr22:
    .byte 0x52, 0x65, 0x50, 0x61, 0x72, 0x73, 0x65, 0x00
.section .rodata
.LCstr23:
    .byte 0x4e, 0x66, 0x61, 0x42, 0x00
.section .rodata
.LCstr24:
    .byte 0x00
.section .rodata
.LCstr25:
    .byte 0x31, 0x00
.section .rodata
.LCstr26:
    .byte 0x30, 0x00
.section .rodata
.LCstr27:
    .byte 0x73, 0x74, 0x72, 0x69, 0x6e, 0x67, 0x00
.section .hexa.cap,"",@progbits
_hexa_cap_manifest:
.section .hexa.abi,"",@progbits
_hexa_abi_stamp:
    .byte 0x01, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00
