## 2026-06-18
- **RT-NATIVE-ZEROC M5-syscall-c — `__hx_str_ptr` (string→cstring-ptr) leaf, b2 gate EMIT+AS green (`F-M5-SYSCALL-C-PTRADDR`)** — F-M5-SYSCALL-B-PORTED 의 GAP1(hexa_ptr_addr) 해소. ADD-only leaf `__hx_str_ptr(s)` 추가: string HexaVal 의 char* base(payload word, `__hx_str_byte` 패턴 minus byte deref)를 int HexaVal 로. bind 화이트리스트 + x86_64(r10=char* base) + arm64(x1=base, x0=TAG_INT) special-case + syscall.hexa `hexa_ptr_addr(`→`__hx_str_ptr(`(getrandom darwin 1곳). ADD-only→gen3≡gen4(컴파일러 미호출). **검증(c2, aiden aprime_m5c 재빌드 smoke42 PASS)**: b2 gate(flatten alloc+syscall+driver) **emit rc=0 + as rc=0**(GAP1 사라짐, HX2001 없음) · `link rc=1` = `g0` undefined(alloc.hexa __arena_tbl module-global 참조)로 **GAP2 단독 격리**. M5 leaf 시퀀스: `__hx_target_os`✓(a)→`syscall()` rewrite✓(b)→`__hx_str_ptr`✓(c)→다음 **M5-gslot**(module-global `.bss` 슬롯 standalone emit 정의 또는 rt_init-only global const-fold)→b2 gate LINK+RUN exit0 = alloc+syscall C-free 통합 증명. 이후 b3 standalone-relink→write `#else` 드롭=ls 감소. 빌드=aiden.

## 2026-06-18
- **RT-NATIVE-ZEROC M5-syscall-b — `self/rt/syscall.hexa` 를 출하 leaf surface 로 포팅 + 잔여 2갭 RAN-LOCATE (`F-M5-SYSCALL-B-PORTED`)** — syscall.hexa 의 25개 `syscall(N,…)` 호출 전부 → `_scK` @inline shim → `__hx_syscall6(N,…,0-fill)`/`__hx_syscall0`, `__TARGET_OS__` → `__hx_target_os()` (balanced-paren 파서로 정확 치환). raw call site 0개 = P7-7 surface 이탈 완료. syscall.hexa=dormant→byteeq-inert. **통합 emit 이 b2 gate green 전 2개 bounded leaf 갭 RAN-발견(c2/c9, aiden aprime_m5a)**: GAP1 `hexa_ptr_addr`(string→cstring-ptr P7-7 intrinsic, 출하 부재, sys_getrandom darwin /dev/urandom fallback 단 1곳)=emit 차단 · GAP2 `g<id>` 글로벌 슬롯(alloc.hexa `__arena_tbl` module-global let mut 이 standalone emit 에서 g0 undefined, b1 유예분)=link 차단. **M5 P7-7-surface 포팅 = bounded ADD-only codegen leaf 시퀀스**: `__hx_target_os` ✓ → 다음 M5-syscall-c(string→ptr leaf, 백엔드가 이미 string rodata lea) → M5-gslot(.bss 슬롯 정의 또는 rt_init-only global const-fold) → b2 gate LINK+RUN exit0 = alloc+syscall C-free 통합 증명. 이후 b3 standalone-relink → write `#else` 드롭. 빌드=aiden.

## 2026-06-18
- **RT-NATIVE-ZEROC M5-syscall-a — `__hx_target_os()` 컴파일타임 OS-const intrinsic (`F-M5-SYSCALL-A-TARGETOS-RAN`)** — F-M5-ALLOC-B2-SYSCALL-WALL 이 찾은 syscall 벽의 `__TARGET_OS__` 절반을 출하 backend 로 해소. ADD-only leaf intrinsic `__hx_target_os()` 추가: bind 화이트리스트 + x86_64_linux(→const 0=linux, `__hx_syscall0` 패턴) + arm64_darwin(→const 1=darwin, `__hx_tag` 패턴). 컴파일러는 이 intrinsic 을 절대 호출 안 함 → self-emit 무변 → gen3≡gen4 보존(read-half `__hx_tag`/`__hx_make_val` 와 동일 안전성, CI selfhost-byteeq-real 권위). **검증(c2, aiden RAN)**: aprime_m5a 재빌드 smoke exit42 PASS(무회귀) · probe `exit(__hx_target_os())` x86_64-linux RUN **exit 0** · arm64-darwin cross-emit `movz x1,#1`(darwin) 확인. 다음 **M5-syscall-b** = syscall.hexa 의 `syscall(N,…)` builtin 호출 전부 → `__hx_syscall6(N,…,0-fill)` + `__TARGET_OS__` ident → `__hx_target_os()` → b2 gate(flatten alloc+syscall+driver) emit+RUN exit0. 이후 b3 standalone-relink(g<id> .bss 슬롯갭). 빌드=aiden, mini=git/gh/편집만.

## 2026-06-18
- **RT-NATIVE-ZEROC M5-alloc-b2 — `self/rt/alloc.hexa` de-circularize landed + 다음 벽 RAN-LOCATE (`F-M5-ALLOC-B2-SYSCALL-WALL`)** — b1 검증 raw-mem arena 본문을 실제 모듈에 반영: `struct Arena{pools:[ArenaPool]}`+`.push()`+`len(pools)` → raw-mem pool table(`__arena_tbl`, np@0, pool i @24+i*24, `__hx_ptr_load64/store64`), 옛 `__mem_*` → 출하 `__hx_ptr_*`, `ArenaMark` struct → int, runtime.c-compat surface(`hexa_ptr_alloc` consumer 11곳) 보존. **순환 제거 완료**(F-M5-ALLOC-CIRCULAR-DEP 해소), alloc.hexa=dormant→byteeq-inert. **통합 emit 이 다음 벽 RAN-LOCATE(c2, aiden aprime_m4)**: b2 gate emit rc=1 — `__TARGET_OS__`·`syscall` undefined. 근본=`self/rt/syscall.hexa` P7-7 추상화(`syscall(num,…)` builtin=`self/codegen`만 lowering·출하 `compiler/codegen` 모름 + codegen-injected `__TARGET_OS__` const), 출하 backend 는 M1 leaf `__hx_syscall6`만. **M5 패턴 명시**: self/rt/ P7-7 트리 전체를 P7-7 surface(`syscall()`/`__mem_*`/`__TARGET_OS__`)→출하 leaf surface(`__hx_syscall6`/`__hx_ptr_*`, read-half seed 가 쓰는) module별 포팅. 다음 **M5-syscall**=syscall.hexa 포팅(`syscall()`→`__hx_syscall6`, `__TARGET_OS__` 해소=출하 const 또는 per-target .s seed)→b2 gate emit+RUN exit0. 이후 **b3**=standalone-relink→write `#else` 드롭(`g<id>` .bss 슬롯갭 해결). 빌드=aiden, mini=git/gh/편집만.

## 2026-06-18
- **RT-NATIVE-ZEROC M5-alloc-b1 DONE — de-circularized multi-pool raw-mem arena RUNs exit 0 (`F-M5-ALLOC-B1-MULTIPOOL-RAN`)** — `F-M5-ALLOC-CIRCULAR-DEP` 가 LOCATE 한 순환(arena pool 을 hexa `[ArenaPool]` array + `.push()` 로 표현)을 raw-mem 으로 깨는 청사진을 aiden 에서 end-to-end 증명. **PoC** `scripts/scratch/rt_native/multipool_arena_poc.hexa`: pool table 을 mmap'd region 에 `__hx_ptr_load64/store64` 로 두고 grow/mark/rewind/reset 구현. **결과(aiden RAN, aprime_m4, 검증된 x86_leaf_gate 레시피)**: emit rc=0 · as rc=0 · link rc=0 · **RUN exit=0** (3-alloc cross-pool grow + mark/rewind 주소 재사용 + reset 전부 byte-exact). **non-circular 증인**(nm): push/malloc/calloc/hexa_array/hexa_map = NONE, syscall 5개 = mmap/munmap 바닥만. **codegen 갭 발견(c9)**: 첫 시도는 module-global `let mut` int 를 썼다가 link 시 `g0` undefined — 출하 x86_64 backend 가 standalone single-module emit 에서 module-global `let mut` 슬롯을 .bss/.data 에 정의 안 함(read-half 는 글로벌 state 가 없어 회피했음). **우회**: arena state 전부를 table region 자체에 보관(글로벌 0개, `tbl` int 하나로 thread; runtime glue 가 C-side 글로벌 보유). 다음 **b2** = `self/rt/alloc.hexa` 를 이 검증된 본문으로 재작성(`[ArenaPool]` array+`.push()`+stale `__mem_*` → `__hx_ptr_*`), C-diff gate, byteeq-inert(dormant skeleton). **b3** = native rt_*.o 를 M5 standalone-relink 에 wire → write `#else` 드롭(g<id> 데이터-슬롯 갭은 여기서 해결). 빌드=aiden, mini=git/gh/편집만.

## 2026-06-18
- **RT-NATIVE-ZEROC alloc/write 레인 REFRAME — alloc-r2 NO-GO 확정 + M5 alloc 첫 blocker 정밀 식별 (`F-M5-ALLOC-CIRCULAR-DEP`)** — handoff #17 이 다음 한 수로 지정한 alloc-r2(arm64 arena → x86_64 미러 + write `#else` byte-decrease)는 직전 세션이 이미 RAN-NO-GO 박제(`F-ALLOC-R2-X86-ARENA`): arena 는 양 타깃 모두 C `#else` 출하(native seed 부재 — arm64 `rt_arena_*` machine-code emitter 는 dormant, 어디에도 link 안 됨), write `#else` 드롭 = REAL link 회귀(= `F-LEGB-STANDALONE-ELSE-NOGO` 중복, 금지된 delete). **진짜 레버 2개**로 피벗: (1) **M5 standalone-redesign** = `self/rt/` 트리(`alloc.hexa`·`map_ops.hexa`·… P7-7 스켈레톤, `mod.hexa` 인덱스) native-compile → `rt_*.o` → standalone build link → write `#else` 드롭 가능(read-half 의 native-seed 메커니즘 동일). (2) **r3 = map_*** = make_val-unlock(#3488) 된 construct-half family. **이번 턴 발견(c16 벽 분류)**: M5 alloc 의 첫 blocker 를 소스 직독으로 LOCATE — `self/rt/alloc.hexa` 가 arena pool 리스트를 hexa `[ArenaPool]` array + `.push()`(라인 55/77/80)로 표현 → arena-alloc 이 array-push-grow 로, 그게 다시 arena-alloc 으로 재귀 = **순환**. `F-ALLOC-R1` PoC 가 non-circular 였던 건 raw `__hx_ptr_store64` 만 썼기 때문(P7-7 초안은 잘못된 데이터 모양). `array_ops.hexa`/`map_ops.hexa` 는 wrapper(low-level alloc 은 runtime.c primitive 유지), 진짜 alloc 본문은 `runtime_core_emit.hexa` 의 `hexa_array_push`. **벽 타입 = (b) wrong-shape**(천장 아님 — intrinsic coverage 100%). **명명된 다음 brick (M5-alloc-b1)**: pool table 을 raw-mem(고정 256-slot, `__hx_ptr_load64/store64`)으로 재표현 → arena body 자기완결 → aiden native-emit 증인(undefined `hexa_array_push`/malloc/calloc 0, mmap/munmap 2-syscall 바닥만) + C-diff gate. **이번 PR = docs+verdict only**(byteeq-real 무영향, self/rt/alloc.hexa 는 dormant skeleton). 게이트=byteeq-real + faithful 3타깃은 코드 변경 없어 무회귀.

## 2026-06-17
- **RT-NATIVE-ZEROC M4 array-r6 — `rt_array_shift_native` (raw-mem slide) 출하 native화 (array 접근자 집합 완성: get/set/pop/shift)** — array.shift 의 standalone C body(`items[i]=items[i+1]` 루프 + truncate)/`#else` closure 이중경로를 단일 무조건 `rt_array_shift_native`(array_core.hexa seed)로 붕괴 — array-r5(pop) 패턴 확장. **새 raw-mem body**: items[0] 을 get 경로로 tag-faithful 저장 → `while __hx_payload_lt(i, n-1)` 루프로 각 원소의 16바이트 {tag,payload} 이미지를 items[i+1]→items[i] raw int64 load/store 복사(이미지 verbatim 이동, 중간 tag 재구성 불필요) → len 절단. alloc-free 라 pop 처럼 포팅. **근본버그 수정**(c1): `rt_array_get_native(arr, 0)` 의 `0` 이 i64 리터럴이라 HexaVal idx param 불일치(HX3003) → `__hx_payload_add(0,0)` 로 HexaVal 0 박싱(intrinsic 은 리터럴 수용, 그러나 user fn 의 typed param 은 HexaVal 필요). seed 4→5 globl(get/set/len/pop/**shift**), resolver/regen count 4→5. **게이트(aiden 캡처)**: ✅ emit 5/5 rt_array_*_native T · ✅ b4_array_gate **36 checks 0 fails**(shift 차등: first=10 int·len 절단·slid[0]=20·slid[1]=str tag-faithful) · ✅ build_aprime smoke exit42. array 접근자 4종 + map 접근자 2종 = 출하 런타임 6함수 native(read-half 레인 완전 소진). 다음=alloc 레인(set/push, GC-heap) · M5 standalone.
- **RT-NATIVE-ZEROC M4 array-r5 — `hexa_array_pop` 출하 native화 (standalone C body + #else closure → 단일 `rt_array_pop_native`)** — array-r4 가 get/set 을 무조건 native 위임한 패턴을 pop 에 확장. pop 의 `#ifndef`(standalone C: items[n-1]+truncate)/`#else`(rt_array_pop closure) 이중경로를 **단일 무조건 `rt_array_pop_native`**(array_core.hexa seed, array-r4 가 이미 모든 빌드 runtime.a 에 ar — get/set/len/pop 4 globl)로 붕괴. non-array/empty 가드는 C 유지(throw). shift 는 seed 부재로 `#ifndef`/`#else` 보존(pop/shift 블록 인터리브 untangle). rt_array_pop_native = items[len-1] read(rt_array_get_native) + len 절단(__hx_ptr_store64), array-r3 b4_array_gate 32/32 검증된 시맨틱 = 드롭된 C body 와 byte-exact. array 접근자 native 출하 = get/set/pop(len=HX_ARR_LEN 매크로). 게이트=build_aprime smoke + faithful 3타깃 + byteeq-real. 권위확정 후 ls byte-decrease(standalone pop C body 제거).
- **RT-NATIVE-ZEROC M4 map-r4 — `rt_map_contains_native` (`in`/contains probe) 출하 native화 (alloc 불필요 read-half)** — map `in` 연산자(`hexa_map_contains_key`)의 standalone(#else) C 해시프로브(`hmap_find(t,key,h)>=0`)를 native raw-mem hexa-source 로 위임. contains 는 **alloc-free read-half**(probe→bool)라 set/push(alloc 레인) 없이 지금 포팅 가능 — get 과 같은 open-addressing walk, 값 추출 없이 slot-present 만 판정({0,1} TAG_INT HexaVal, C `hmap_find>=0` 와 동일하게 value-independent → void-값 키도 present). map_core.hexa fnv1a/strcmp/probe 재사용(NEW 메커니즘 0). 3타깃 seed 재동결(get/fnv1a/strcmp0/**contains** = 4 globl/target, x86_64 cross-assemble 4 T) + resolver/regen globl count 3→4. **게이트(aiden 캡처)**: ✅ map_core_gate exit=21 `all native==C, 16 checks`(contains 차등 5개 추가: present a/ccc/"" · absent no_such_key/zzz, native==C) + ✅ build_aprime smoke exit42(컴파일러 native contains 실검증) + ✅ nm runtime.a 4 rt_map_*_native=T. array(get/set)+map(get/contains) = 출하 런타임 4개 함수 native. 권위=byteeq-real + faithful 3타깃. 다음 잔여=map set/push(alloc 레인, arena intrinsic) · M5 standalone 재설계.
- **fix(x86_64 codegen): nested `__hx_payload_*` intrinsic clobber — arg-load order swap (c1 근본수정, map-r2 발견 finding 해소)** — map-r2(#3501)에서 발견한 codegen 버그 수정: 중첩 intrinsic(`__hx_payload_X(a, __hx_payload_Y(b,c))`)이 SIGSEGV/오결과(measured: nested fnv). **근본원인(c2 격리)**: r10/r11 이 (a) allocatable value-home(x86 reg pool line 187) **이자** (b) intrinsic 의 하드코딩 scratch 이고, spilled 피연산자 resolve 가 내부적으로 r10 경유(line ~574) — outer intrinsic 이 `args[1]→r11` 로드 시(spilled면 r10 경유) 이미 r10 에 있던 `args[0]` 파괴. **fix**: arith(add/sub/mul/and/xor) + cmp(lt/gt/le/ge/eq/ne) 블록에서 `args[1]→r11` 을 `args[0]→r10` **보다 먼저** 로드(r10 비었을 때) → 충돌 회피. reg 배정(a=r10, b=r11) 불변이라 비가환 sub/imul/cmp 정확성 유지. **fixpoint 무영향**: 컴파일러는 이 intrinsic 들을 호출하지 않으므로 self-emit(cc-gen3.o) 불변, frozen seed 도 재동결 불필요(un-nested 형 그대로 유효) — 순수 미래-포팅 capability(중첩 intrinsic 이 이제 작동). arm64 는 disjoint pair(x1/x3) 라 미해당. 검증=nested fnv byte-exact + M1 leaf 무회귀 + byteeq-real.
- **RT-NATIVE-ZEROC M4 map-r3 — map_get 표준(#else) 해시프로브를 native `rt_map_get_native` 로 위임 + 3타깃 seed wiring (map read-half 출하경로 진입, array-r4 템플릿)** — map-r2(#3501)가 PROVEN한 `rt_map_get_native`(fnv1a+probe+strcmp 전부 intrinsic, gate exit21 byte-exact)를 출하 런타임에 진입. ① **3타깃 seed 동결**: `self/native/map_core_{arm64,x86_64,arm64-linux}.s`(537/697/537L, globl 3/3, /home누출0, x86_64 cross-assemble 3T). regen `tool/regen_map_core_native_s.sh`(array 미러). ② **stage_resolve_runtime_a::resolve_native_map_core_seed**(array resolver 미러) — arch-matched seed→`build/map_core_native.o` ar into runtime.a, `HEXA_RT_MAP_NATIVE=1`. ③ **single-TU dev 빌드 wiring**(build_aprime stage4/5·build_native_linux_arm64 stage4) — array_core_native.o 옆에 map_core_native.o 링크. ④ **map_get `#else`(standalone) 편집**: C `hmap_find`+`t->vals[si]` 해시프로브 → `rt_map_get_native(m, hexa_int((int64_t)key))`(key 박싱); valstruct/NULL-table 가드 + not-found stderr 는 유지(native body=miss시 TAG_VOID). hmap_find/fnv1a_str 는 set/contains 가 계속 사용→C 잔존(map-r3=get-probe 한정). **게이트(aiden 캡처)**: ✅ build_aprime smoke **exit(42)==42 PASS**(컴파일러=map 다용→native map_get path 실검증) + ✅ nm runtime.a `rt_map_{get,fnv1a,strcmp0}_native`=T. byte delta=probe(6줄)→call(~5줄) 소폭(가치=map_get lookup 출하경로 native화, C 해시프로브 제거). 권위확정=PR CI(byteeq-real+faithful 3타깃, map seed aarch64 어셈블 검증).
- **RT-NATIVE-ZEROC M4 map-r2 — `__hx_payload_eq/ne` 비교 leaf 추가 + map_core.hexa 전면 intrinsic 재작성 (raw-int 제어흐름 포트 완성, gate 대기)** — map-r1(#3499)이 and/xor 를 추가하고 "plain hexa 연산자는 HexaVal 헬퍼로 lower → raw-int 제어흐름엔 intrinsic 필요" 를 발견. 실측으로 **비교 intrinsic(`__hx_payload_lt`)이 if/while 조건에서 작동** 확인(`if lt(3,7)`→1 · `while lt(i,5)`→5회). 유일 갭이던 `==`/`!=` 를 `__hx_payload_eq`(x86 `sete`/arm64 `cset eq`) · `__hx_payload_ne`(`setne`/`ne`) 로 추가(lt/gt 미러, 4 SSOT 파일). 이로써 raw-int leaf 세트 완비(arith add/sub/mul · bitwise and/xor · cmp lt/gt/le/ge/eq/ne). `stdlib/runtime/map_core.hexa::rt_map_get_native` 를 fnv1a(xor+32비트 and-mask+mul) + open-addressing probe(lt 바운드 + eq 매치) + strcmp(eq byte-walk) — 전부 intrinsic 으로 재작성. ADD-only(컴파일러 미호출 → fixpoint 보존). **게이트 PASS(aiden 캡처): map_core_gate.c exit=21 `all native==C, 11 checks`** — rt_map_get_native 해시프로브가 C hexa_map_get 와 byte-exact(int/str/bool 값·키길이·해시충돌·overwrite·missing→TAG_VOID). 첫 **제어흐름** 런타임 함수 native 포팅(array=직선과 대비). 돌파 5벽: ①plain연산자=HexaVal헬퍼→intrinsic ②and/xor(#3499) ③eq/ne(이 PR) ④중첩intrinsic clobber→un-nest ⑤혼합 HexaVal+int param ABI(int param→0 measured)→전부 HexaVal+key박싱(hexa_int). **별도 codegen finding(미수정)**: 중첩 `__hx_payload_*`(intrinsic이 다른 intrinsic의 직접 arg)가 r10/r11 scratch clobber→오결과; un-nested(1 op/temp)는 정확 — codegen robustness 별도 유닛. map_core.hexa=inert(미wiring, array-r3 형). 다음 map-r3=seed동결+wiring+#else드롭=byte-DECREASE. byteeq-real=ADD-only fixpoint보존 권위확정.
- **docs: 도메인 트래킹을 ARCHITECTURE.json 으로 단일화 — per-domain `*.md`/`*.log.md`/`*.tape` + `DOMAINS.tape` 96파일 은퇴 (c4 단일문서)** — 흩어진 도메인 스냅샷/로그(`harness domain` 메커니즘)를 ARCHITECTURE.json `domains` 섹션(final-form registry: `@goal` + 상태 + 잔여 open milestone)으로 통합. 40개 도메인(24 active · 16 done) 최종형태 보존, append-only milestone 이력(`- [x]` done-log + `*.log.md`/`*.tape`)은 CHANGELOG + git 으로 은퇴. **무영향 확인**: `tool/_init/register.hexa` 는 `domains.json`(DOMAINS.tape 아님)을 try/catch graceful-skip 으로 읽고, CI workflow/release_build/build_aprime/lockdown 어디서도 도메인 파일 미참조 → 빌드/CI 무영향. 비도메인 docs(SPEC·ROADMAP·PLAN·README·CHANGELOG·orphan `.tape`)는 보존(c10 surgical). 추출 스크립트 `scripts/scratch/consolidate_domains.py` 보존(c5). CLAUDE.md SSOT 포인터 갱신. `harness domain list` 는 이제 빈 목록(architecture.json 이 SSOT — 의도된 결과).
- **RT-NATIVE-ZEROC M4 map-r1 — `__hx_payload_and`/`__hx_payload_xor` raw-int bitwise intrinsics ADDED + the raw-mem-port WALL clarified (c9/c16)** — porting the map LOOKUP read-half (`hexa_map_get` hash probe) to hexa-source raw-mem (array twin) surfaced a genuine substrate finding: **plain hexa `int` operators lower to HexaVal runtime helpers, NOT raw int64 ops** — `nm` of a standalone-emitted `fn f(a:int,b:int)->int{a+b}` shows `U hexa_add_slow` / `U hexa_mul`; called with raw int64 from C they misinterpret the words (measured: `16 & 4294967295 = 0`, map probe SIGSEGV). The array port worked ONLY because it is STRAIGHT-LINE (offsets via `__hx_payload_add/sub/mul`, no `&`/`^`/comparisons). map needs FNV-1a (XOR + 32-bit-masked multiply) + an open-addressing probe (loops + `==`/`<` comparisons), none expressible in raw int without intrinsics. **This round adds the two arithmetic leaves the family was missing** — `__hx_payload_and` (x86 `and` / arm64 `and`) + `__hx_payload_xor` (x86 `xor` / arm64 `eor`), mirroring `__hx_payload_mul` across the 4 SSOT files (bind allowlist · x86_64_linux · arm64_darwin · self/codegen C-mirror `& / ^`). reg-reg form (r10,r11 / x1,x3) so the 32-bit mask is a register AND — sidesteps the large-immediate path. ADD-only (compiler never calls them → fixpoint structurally preserved). **WALL clarified (deferred, not faked)**: the raw-mem hexa-source port technique covers STRAIGHT-LINE element access (array get/set/pop/len ✅) but a control-flow runtime fn (hash probe) additionally needs raw-int COMPARISON leaves in if/while conditions (`__hx_payload_lt/gt/le/ge` exist; `eq/ne` + truthy-in-condition unverified) — map read-half is a dedicated multi-step port on top of these leaves, not a one-shot. `stdlib/runtime/map_core.hexa` + `scripts/scratch/rt_native/map_core_gate.c` land as the (currently plain-operator, hence WIP) groundwork to be rewritten onto the intrinsics. Verify = aiden aprime rebuild + `__hx_payload_and(12,10)=8` / `xor(12,10)=6` + M1 leaf-gate no-regression + faithful 3-target + byteeq-real.
- **RT-NATIVE-ZEROC M5a — `larm64_flatten.py` 가 `use self::rt::x` `::`-경로 구문 해결 (런타임-대체 트리 flatten 언블록)** — M5/M6 의 endpoint `self/rt/mod.hexa`(런타임 대체 모듈 인덱스)는 `use self::rt::{syscall,alloc,io,fmt,proc,fs,math}` 로 모듈을 끌어오는데, flatten 도구가 `import "path"`·`use "path"`(따옴표 형) 만 따라가고 `::`-경로는 미해결 → `mod.hexa` flatten 이 1 file/35L(skeleton 만) 에 그쳤음(F-M5 mapping blocker #2). `a::b::c` → `a/b/c.hexa`(ROOT=repo root 기준) 해결 + strip 정규식 보강 추가. **검증(로컬, 캡처)**: `mod.hexa` flatten 1→**8 files/2093L**(syscall/alloc/math/fmt/io/proc/fs/mod 수집) · 잔여 `use::` 라인 0 · **컴파일러 closure(`compiler/main.hexa`) flatten = byte-IDENTICAL**(`cmp` 일치, 컴파일러는 `::`-use 미사용 → `build_native_linux_arm64` 무회귀 확정). 순수 도구 변경(런타임/에미터/빌드산출물 무변경) → fixpoint 무영향. 이로써 M6 libc-floor 트리(syscall-기반 runtime.c 대체)의 flatten→transpile 경로가 열림. 잔여 M5 블로커=#else-delegated rt-stdlib(core/convert/numeric)는 `hexa_call1` 동적 디스패치라 static-edge flatten 밖(별개 closure 작업).
- **RT-NATIVE-ZEROC M5 착수 (standalone 재설계 = ls==∅ 의 진짜 단일 레버, per-family 신규포팅 레인 소진 확정)** — ARRAY-R4(#3494) 착지 후 잔여 분석: cmp/eq/arith/map 등 잔여 `#else` 는 전부 **이미 native hexa-source body 보유**(`rt_eq`/`rt_add_slow`@self/rt/core.hexa · `rt_map_get_v`) = array(유일한 신규포팅) 와 달리 **M5 standalone-link 작업**(기존 body 를 standalone runtime.a 에 링크 + `#else` 일괄 드롭). 순수 family(core.hexa `__hx_`=0)는 hexat-transpile 가능 → `runtime_hi_gen.c`(rt_str) 패턴의 gen.c 번들로 native .s seed 없이 드롭 가능 = M5 빠른 경로. roadmap = RT-NATIVE-ZEROC.tape. 다음 = M5 core.hexa 번들 feasibility 실측(aiden).
- **RT-NATIVE-ZEROC M4 ARRAY-R4 (linux-arm64 seed plumbing, WIP) — 3rd-target native seed path wired so the get/set `#else` substrate drop covers ALL production targets (c2/c9/c16)** — r4 의 결정적 분기 판정: linux-arm64 release_build 가 frozen native array seed 를 소비하는 경로가 **이미 존재**(`tool/build_native_linux_arm64:84` 가 `TARGET=linux-arm64 … bash tool/stage_resolve_runtime_a` 를 호출 → `resolve_native_array_core_seed` 경유) = M-level 빌드재설계 **아님**, 두 plumbing 편집만 필요. **① `tool/regen_array_core_native_s.sh`**: `arm64-linux` 타깃 추가(`--target=arm64-linux-gnu` → `self/native/array_core_arm64-linux.s`, ELF aarch64·언더스코어 없음·external `hexa_to_int`; r3 가 array_core arm64-linux cross-emit rc=0 증명). **② `tool/stage_resolve_runtime_a::resolve_native_array_core_seed`**: `linux-arm64) seed="self/native/array_core_arm64-linux.s"` case 추가 → 3 production 타깃(darwin-arm64 + linux-x86_64 + linux-arm64) 전부 seed 커버, stale 주석 정직성 수선. **🎯 GENUINE byte-DECREASE = 1264 B / 24 L (aiden 실측, 첫 비-string 런타임-substrate `runtime_core.c` 감소)**: origin/main(r3 `#ifdef` baseline, 412623 B·8762 L) → 브랜치(411359 B·8738 L) = **−1264 B / −24 L**. (초기 측정 −5 는 aiden 로컬 stale `main` ref[array-r4 WIP 커밋 257a7e994]를 control 로 쓴 오염 — origin/main 명시 후 정정, c9). ① arm64-linux seed 동결 완료(aiden 재빌드 aprime_cc, 254L globl 4/4, `__hx_*` 전부 인라인·undefined ref 0, `bl hexa_to_int` 단일 외부). ② **blast-radius 측정(c16 — 가정 말고 측정)**: 모든 권위 CI 게이트(byteeq-real·faithful 3타깃·determinism·miscompile·release)는 `release_build`→`HEXA_PREBUILT_RUNTIME=runtime.a`(seed ar됨) 경유 + byteeq 는 **오브젝트 비교**(`cc-gen3.o`==`cc-gen4.o`, runtime `#else`와 무관) → `#else` 드롭은 권위 게이트에 안전. `build_aprime`·`build_native_linux_arm64` 는 **어느 워크플로도 호출 안 함**(dev-only single-TU). ③ **#else 드롭 실행**: `self/runtime_core_emit.hexa` get/set 의 `#ifdef HEXA_RT_ARRAY_NATIVE … #else HX_ARR_ITEMS(arr)[idx] … #endif` 를 무조건 `rt_array_{get,set}_native` 호출로 교체(extern 도 무조건화) — `runtime_core.c` byte-DECREASE 의 첫 비-string 런타임-substrate 감소. ④ **single-TU dev 와이어링**: `build_aprime.sh`(stage4 컴파일러 + stage5 smoke)·`build_native_linux_arm64`(stage4 cc_native) 가 arch-matched seed 를 `build/array_core_native.o` 로 어셈블 + 링크(rt_hi Z2a 대칭, archive 없는 single-TU 라 중복심볼 없음). runtime_core.c 재생성은 awk un-escape(runner-free, byte-for-byte) 라 emitter 편집이 직접 반영. **게이트(aiden, 캡처됨)**: ✅ `build_aprime.sh` smoke **exit(42)==42 PASS**(배열을 쓰는 컴파일러가 native array path 로 정상 빌드+실행 = 와이어링 + `#else` 드롭 end-to-end 작동) · ✅ `nm build/runtime.a` = `rt_array_{get,set,len,pop}_native` 전부 **T** + `hexa_array_get/set` T · ✅ **byte-DECREASE 1264 B** 실측 · ⚪ aiden 로컬 byteeq 는 INFRA-block(released `./hexa` 가 native `--emit=obj` 불가, "byte-eq floor NOT broken" 명시 — CI `selfhost-byteeq-real` 가 권위; 변경이 runtime-only 라 컴파일러 emit `cc-gen3.o` 불변 → fixpoint 구조적 보존). **권위 확정 = PR CI**(byteeq-real + faithful 3타깃; linux-arm64 seed 의 aarch64 어셈블은 CI 가 검증 — aiden/mini 에 aarch64 어셈블러 부재로 로컬 미검증). verdict `F-M4-B4-ARRAY-R4-SEED-LIVE`(byte-DECREASE 달성).
- **RT-NATIVE-ZEROC ALLOC-R2 (x86 arena) — briefed `#else`-excise byte-reduction = 🧱 NO-GO, DUPLICATE of the standing `F-LEGB-STANDALONE-ELSE-NOGO` (docs-only, c2/c9/c16)** — the alloc r2 brief asked to "mirror the arm64 hexa-source `rt_arena_*` bump arena into the x86_64 native runtime + standalone path, EXCISE the C `#else` arena body, and DECREASE `runtime_core.c` byte-count." Reading the real sources falsifies the premise and shows the deliverable is unachievable without the exact regression this campaign already landed today. **Finding 1 (RAN — read both bodies)**: the arm64 `rt_arena_*` (`self/codegen/runtime_arm64.hexa:2237-2543`) is a hexa-source fn that returns a `[int]` of RAW ARM64 MACHINE-CODE BYTES (`svc #0x80` mmap=197/munmap=73, `_arena_state` via ADRP+ADD PAGE21/PAGEOFF12) = darwin-arm64-SPECIFIC, NOT portable hexa logic; the `#else` arena (`self/runtime_core_emit.hexa:3882-4049` `hexa_arena_alloc/mark/rewind/reset`) is a SEPARATE portable-C linked-block bump allocator. "Mirroring" = WRITING an x86_64 machine-code emitter, not copying a portable body — there is none. No x86_64 analog exists (`grep '^fn rt_arena' self/codegen/*.hexa` = ARM64 only). **Finding 2 (DECISIVE, RAN)**: the arm64 `rt_arena_*` machine-code emitter is PROVEN-BUT-DORMANT — consumed ONLY by PoC/test `.o` emitters (`test/native_build/poc_arena_bundle_emit.hexa`, `tool/emit_runtime_obj.hexa`), NEVER by `tool/release_build`/`stage_resolve_runtime_a` (grep = empty). The runtime that BOTH arm64-darwin AND x86_64-linux actually link in CI/release is the C standalone path (`stage_resolve_runtime_a:271` `$CC … -c self/runtime.c` with NO `-DHEXA_HAS_HEXA_RT_STDLIB`, `#includes` `runtime_core.c` = the `#else` arena). ⇒ **x86_64's shipped arena is C for the SAME reason arm64's is; there is NO arm64-vs-x86_64 parity gap to close by mirroring** — both ship C, the native arena emitter is dormant on its own target too, correcting r1's "x86_64 = bounded mirror" framing. **Finding 3 (c16)**: the briefed bar ("excise `#else`, bytes DECREASE") IS the `F-LEGB-STANDALONE-ELSE-NOGO` pattern the brief itself warned against — `F-LEGB-STANDALONE-ELSE-NOGO` (today) already RAN the proof that deleting a live `#else` body breaks `tool/release_build` (the authoritative CI judge), and `F-M5-R2-RTCORE-CENSUS` (#3487, today) already classified the per-family `#else`-drop lane TERMINAL ("PURE-PORTABLE 버킷=0, ls 레버는 `__hx_make_val` unlock + M5 standalone 재설계 둘뿐"). So the reduction is reachable ONLY via the forbidden delete = NO-GO. NO aiden build burned to "achieve" a reduction twice-proven impossible (no source change ⇒ no CI run; byteeq-real on main = GREEN, untouched). **Real GO sub-unit (deferred, NOT this round's reduction)**: an x86_64 `rt_arena_*` ELF machine-code emitter twin IS buildable — `compiler/emit/elf_x86_64.hexa` (1693L) has `.data`/`.bss`, `R_X86_64_64`/`PC32` relocs + resolution (S+A−P), multi-symbol object wrap = the ELF analog of macho's v3 + PAGE21/PAGEOFF12 — but it would be DORMANT-parity (links into nothing the shipped C runtime uses), yielding ZERO `runtime_core.c` byte-delta; it is a prerequisite brick for the M5 standalone-redesign (relink native `rt_*.o`), to be picked up WITH that relink so it lands non-dormant, not as a fake reduction now. No emitter/runtime/build-script edit (ADD: verdict only) → fixpoint structurally untouched. verdict `F-ALLOC-R2-X86-ARENA`. Next r3 = `map_*` allocator+construct half over the r1-proven arena + the now-landed `__hx_make_val` (#3488) with a `map_core_gate.c` C-diff depletion test (coordinates with the array lane: array owns `array_core.hexa`, alloc takes `map_*`).
- **RT-NATIVE-ZEROC M4 ARRAY-R3 — array-core READ-half (get/set/len/pop) ported to native raw-mem hexa-source; PROVEN-correct in isolation; ship-integration = native `.s` seed (r4); zero regression (c2/c9/c16)** — the r2 follow-on: `stdlib/runtime/array_core.hexa` implements `rt_array_get_native`/`rt_array_set_native`/`rt_array_len_native`/`rt_array_pop_native` as RAW-MEM bodies over the locked HexaArr descriptor (`__hx_payload_add(arr,0)`=HexaArr*; `items=__hx_ptr_load64(arr_ptr,0)`; element tag/payload at `items+idx*16+{0,8}`; **`__hx_make_val(tag,pay)`** re-stamps the REAL element tag — WALL-1 from `F-M4-B4-ARRAY` is down). NO `arr[idx]` (r1 trap c1 — lowers to `hexa_index_get`→circular). set/pop are alloc-free (in-place index write / pure length decrement); new/push/reserve stay C (WALL-2, alloc lane). **DECISIVE PROOF (c2 — RAN on real x86_64 aiden, native compiler `build_aprime.sh`)**: the native gen2 compiler emits `array_core.hexa` → `array_core.o` with `nm` = `rt_array_{get,set,len,pop}_native` **T** and **ZERO undefined `__hx_*`** (every leaf intrinsic INLINED) — the frozen-`.s`-seed route is FEASIBLE; cross-emit arm64-darwin + arm64-linux both rc=0. **Direct-call gate** (`scripts/scratch/rt_native/b4_array_native_gate.hexa` via direct_gate, linked native `array_core.o` + standard `runtime.a`) = **exit 12 PASS**: int get/set round-trip + len + **MIXED-TAG** set `"hi"`(STR)+`true`(BOOL) read back EXACT (tag NOT flattened to INT — the decisive `__hx_make_val` faithfulness) + int neighbour intact + pop returns last + len shrinks + survivor intact. C oracle `b4_array_gate.c` (C `#else` path) = GATE PASS 32/32 (no-regression). **🧱 WALL-3 (bootstrap-transpile gap, c16 ≥2 lenses) — why it cannot ship-integrate this round**: the aprime/release build flattens `compiler/main.hexa` → **hexat** (C-transpile bootstrap) which does NOT lower the raw-mem intrinsics (only `self/codegen.hexa` gen2 does); importing `array_core.hexa` into the compiler closure BROKE the build (`__hx_payload_add`/`__hx_ptr_load64`/`__hx_make_val` undeclared, RAN) — the import was added then REMOVED. The bodies enter the runtime ONLY via a native-emitted frozen `.s` seed (the `rt_hi` mechanism `build_aprime.sh:241-263`), since `runtime_hi.hexa` (the rt_str seed SSOT) uses ZERO `__hx_` by design and the build scripts deliberately exclude the array stdlib from the compiler closure (C-link-fill `rt_array_pop/shift`, `build_native_linux:117-140`). **Zero-regression gating**: `build_aprime`/release define `HEXA_HAS_HEXA_RT_STDLIB`; gating the delegation under THAT macro broke the link (`undefined reference to rt_array_get_native`, RAN), so `self/runtime_core_emit.hexa`'s `hexa_array_get/set` delegation is gated behind a dedicated **DEFAULT-OFF `HEXA_RT_ARRAY_NATIVE`** — 28 emitted-C lines added, ALL inside the inert `#ifdef` or comments → the default-compiled `runtime_core.c` keeps the unchanged `HX_ARR_ITEMS(arr)[idx]` active arm (preprocessor strips the undefined-macro arm) → `build_aprime.sh` smoke exit42 PASS, fixpoint structurally preserved. **Byte delta (honest)**: runtime_core.c emitter +~1KB inert text (an INCREASE), compiled active bytes UNCHANGED — the genuine ls-reduction (drop the `#else`) is r4, exactly as briefed ("the real ls-reduction lands when M5 drops the #else"). **r4** = freeze `array_core_{x86_64,arm64}.s` seeds + `ar` into runtime.a + define `HEXA_RT_ARRAY_NATIVE` + DROP the get/set `#else` (the first genuine non-string runtime-substrate ls byte-decrease) — the array twin of the alloc lane's M5 relink brick, to land together. verdict `F-M4-B4-ARRAY-R3-READHALF`.
- **RT-NATIVE-ZEROC M4 ARRAY-R2 — `__hx_make_val(tag, payload)` constructor-intrinsic LANDED (the symmetric WRITE-half of `__hx_tag`; tag SURVIVES round-trip, c2/c9/c10)** — the NAMED unblocking lever from array r1 (`F-M4-B4-ARRAY`) and B1 (`F-M4-B1-NANBOX`): `__hx_make_val(tag, payload)` WRITES a `{tag, payload}` 16-byte HexaVal from two raw int64 words WITHOUT going through `hexa_int/hexa_float/hexa_str` boxing (which forces a fixed tag and loses the caller's intended tag), so a raw payload word loaded from an array slot can be re-stamped with its REAL element tag — unblocking native array/map element returns (B4/B5) and the `nanbox.hexa` file-reduction. **ADD-only, 4 files, mirrors the landed `__hx_tag`/`__hx_payload_*` leaves (c10)**: `compiler/check/bind.hexa` (`__hx_make_val` → `__hx_*` allowlist, 2 args) · `compiler/codegen/arm64_darwin.hexa` (STMT_CALL leaf — both args int-boxed, loaded into DISJOINT pairs `x0:x1`/`x2:x3` since `_hv_load` clobbers its pair as scratch, then `lo = tag word` / `hi = payload word`, `stp`, **NO `bl`** = non-circular) · `compiler/codegen/x86_64_linux.hexa` (STMT_CALL leaf + new `_x86_hv_store_leaf_tagreg` for a REGISTER tag rather than a const: payload word → `r10` (the dst home/spill reg), tag word → `r11` → tag-slot via `_x86_store_tag_reg`; the `first→r10 / second→r11` disjoint ordering the payload-arith leaves use; r10/r11 are scratch so the dst home is never r11; **NO `call`**) · `self/codegen.hexa` (C-transpile = `((HexaVal){.tag=(HexaTag)HX_INT(t), .i=HX_INT(p)})`, `.i` = the int64 union member of `runtime.h` `HexaVal_`). **DECISIVE measurement (c2 — RAN on real x86_64, aiden: emit → GNU `as` → link C runtime → exec)**: `scripts/scratch/rt_native/makeval_gate.sh` **exit=8 = TAG SURVIVES the round-trip** — `__hx_make_val(TAG_STR=3, ptr)` then `__hx_tag(…)` reads back **3 (STR), NOT flattened to TAG_INT(0)**; FLOAT/BOOL/INT survive likewise + 4 payload-exact checks. **No regression (c2)**: M1 x86 leaf gate `x86_leaf_gate.sh` PASS (encoding 7/7 + core exit=16 · str exit=3 · arena exit=5) + aprime smoke `exit(42)==42`. **Fixpoint structurally preserved**: `grep __hx_make_val` over `compiler/self/tool/stdlib` = ZERO call-sites outside the 4 def files → the new `if s.op == "__hx_make_val"` branch is dead code during the self-emit, so the whole-compiler emitted bytes are unchanged (the interactive whole-compiler obj byte-eq is OOM-killed under the pool wrapper = an infra limit, not a regression; CI `selfhost-byteeq-real` is the authority). Probe `m4_makeval_roundtrip.hexa`, gate `makeval_gate.sh`, verdict `F-M4-ARRAY-R2-MAKEVAL`. Next r3 = B4 `array_core.hexa` re-attempt (get/set/pop/len now expressible via `__hx_ptr_load64` + `__hx_make_val`; new/push/reserve stay C `#else`, WALL-2 alloc-blocked).
- **RT-NATIVE-ZEROC ALLOC-floor FEASIBILITY (alloc r1) — REDUCIBLE: hexa-source bump arena over raw mmap is FEASIBLE + NON-CIRCULAR (verdict `F-ALLOC-R1-FEASIBILITY`, c2/c9/c16; the r1 "x86_64 = bounded codegen mirror" framing was subsequently CORRECTED by `F-ALLOC-R2-X86-ARENA` above — the arm64 `rt_arena_*` is a dormant machine-code emitter, not portable hexa logic, so there is no shipped-runtime parity gap to mirror; the feasibility + non-circularity findings stand)** — the m5 census (`F-M5-R2-RTCORE-CENSUS`) left the **ALLOC-WALLED** bucket (the `map_*` `_impl` hashtable, `to_string_*`, the array `push/reserve/snapshot` alloc-half, and the bump-arena substrate `arena_alloc_items`/`__hexa_val_mark_top`/`hexa_val_heapify`) as a named remaining `ls`-reduction lever. This round establishes **with RAN evidence** that the allocation substrate can be expressed in `.hexa` over the EXISTING raw-mem + syscall intrinsics — the Go/Rust precedent (allocator written in the language itself over an `mmap` syscall; the irreducible floor is the syscall instruction + libm, NOT the allocator logic). **(1) Intrinsic coverage = 100% (MISSING = 0)**: `mmap`/`munmap` = `__hx_syscall6` (9/197 · 11/73; the 6-arg mmap ABI fits — x86_64 stages `rdi,rsi,rdx,r10,r8,r9`+`rax`, arm64 `x0..x5`+`x16`), raw 64/32/8 load+store = `__hx_ptr_load/store{64,32,8}`, bump arithmetic = int payload add/mul/shift, bound check = `__hx_payload_lt/ge`, HexaVal construct = `__hx_make_val` (array-r2 #3488). **(2) Circularity = NON-CIRCULAR** (decisive, like the B1 nanbox finding): a bump arena over a pre-`mmap`'d region keeps its entire state (base ptr, cursor, addresses) in int64/pointer space — the arena BODY creates NO HexaVal box, string literal, or array temp ⇒ no recursion into `hexa_array`/`hexa_map`. **(3) PoC RAN** (aiden x86_64, `build/aprime_m4`): `scripts/scratch/rt_native/bump_arena_linux_poc.hexa` — `arena_new` = `__hx_syscall6(9, 0, 8192, 3, 34, -1, 0)` raw `mmap` slab → bump 200 cells, store `cell[i]=i²` via `__hx_ptr_store64`, read back + sum via `__hx_ptr_load64`, `arena_free` = `__hx_syscall6(11, …)` `munmap` → `exit(sum == 2646700 ? 0 : 1)`. **emit rc=0** (404-line asm) · **as rc=0** · **gcc -no-pie + runtime.o link rc=0** · **RUN exit=0** (arena fidelity EXACT). Non-circularity witness (`nm`/`objdump` on `poc.o`): `arena_*` = `T` (in-object) symbols; ZERO `malloc`/`calloc`/`realloc`/`free`/`mmap` symbol references; exactly **2** `syscall` instructions (mmap + munmap = the floor); only undefined symbols = `hexa_add_slow`/`hexa_mul`/`hexa_exit` (int bodies return `hexa_int(..)` = stack value, no alloc). Classification (c16): the ARENA floor (bump alloc/reset/release over mmap) = **GO**, intrinsics 100% covered, non-circular (arm64 ALREADY emits `rt_arena_*` in hexa-source at `self/codegen/runtime_arm64.hexa:2221+`; the x86_64 equivalent is a bounded codegen mirror, NOT a missing intrinsic); `calloc` = fresh-anon-page bump (kernel zero-fills), `realloc` of an order array = alloc-new + ptr-copy = GO; the HexaVal-RETURNING families (`map_*`, array `push`) = **GATED-on-`__hx_make_val`** (their alloc-half is feasible but they construct a HexaVal result — sequence after array-lane #3488 lands on main). Honest residual (c9): arbitrary-block `free` + the general `hxlcl_malloc` are NOT claimed reducible (bump arenas do bulk reset, not per-object free — the same trade-off Go/Rust make above the syscall) = an accounting floor, re-assessable, not a hard ceiling. The genuine irreducible floor is the syscall instruction (already wrapped) + libm — NOT the allocator logic. **r1 = GO + non-circular.** No compiler edit landed (docs + PoC only, reusing the already-shipped `__hx_syscall6`/`__hx_ptr_*` intrinsics) ⇒ no CI gate trip. NEXT (r2, after #3488 merge): port `hexa_arena_alloc` / the bump-from-arena `calloc` to a hexa-source body over `__hx_syscall6` mmap + `__hx_ptr_*` (mirror `runtime_arm64.hexa`'s `rt_arena_*` into the x86_64 native runtime + the standalone path), proving the first ALLOC-WALLED family's allocator-half is C-free; `map_*` chains behind `__hx_make_val`.
- **REVERT #3486 — `cloud pods` AUTO-ADOPT 폐기 (잘못된 레이어: 사후 라벨링/입양은 hexa cloud 역할 아님)** — #3486이 `cloud_pods_reconcile_prune`에서 alive 미등록(orphan) 포드를 탐지 시 자동 입양(registry stamp)하게 했으나, 이는 **사후(after-the-fact) 라벨링**이라 잘못된 설계다. 올바른 메커니즘은 **배포(rent) 순간 무조건 라벨링** — `cloud_cli`의 rent 경로가 이미 `pod_registry_add(id, provider, project=_resolve_project(@repo), purpose=_resolve_purpose, …)`로 unconditional 라벨링한다(line 1406 RENTING · 1454 READY). hexa cloud가 **자기가 배포하지 않은** 외부(raw runpodctl 등) 포드를 사후 주워담아 라벨 추정하는 것은 그 역할이 아니다 — orphan은 surface(경고)만 하고 사람이 `cloud reap`/`cloud attribute`로 처리. `cloud_pods_reconcile_prune`을 #3486 직전(surface-only ORPHAN)으로 복원. transport-retry(#3477)는 라벨링 무관 = 유지.
- **RT-NATIVE-ZEROC M5 PILOT (rt_str) — drop-mechanism ALREADY PROVEN+SHIPPED, honest ALREADY-DONE (verdict `F-M5-PILOT-RTSTR`, c2/c9/c16)** — the M5 pilot set out to prove the `.c`-content-drop mechanism end-to-end on `rt_str` (standalone build links the native `rt_str_*.o` + excise the matching `#else` C bodies + byteeq-real green). **Measurement on aiden (real x86_64, the EXACT CI/release builder `CC=gcc bash tool/stage_resolve_runtime_a`) FALSIFIES the task premise that the rt_str `#else` bodies are still LIVE**: the cumulative Z2a campaign (#3455 casefold/trim · #3457 trim-ends · #3465 search-predicates `_b`) ALREADY (1) generalized the `HEXA_ZEROC_RT_HI` native-seed path to the STANDALONE config — `resolve_native_rt_hi_seed` runs UNCONDITIONALLY and auto-enables `HEXA_ZEROC_RT_HI=1` whenever a target seed exists (true for darwin-arm64 + linux-x86_64), (2) EXCISED the rt_str casefold/trim/contains/search-predicate `#else` C bodies from the emitter (`self/runtime_core_emit.hexa`: current `rt_str` body count for the 8 named fns = **0**, vs **3** at d99ecc321^ pre-#3455), and (3) ar's the native `build/rt_hi_native.o` (14 `rt_str_*` syms) into `runtime.a` + sed-removes the `runtime_hi_gen.c` `#include`. **Captured (c2, not self-judged)**: `nm build/runtime.o` shows only the 2 int-return BRIDGE wrappers (`rt_str_starts_with`/`rt_str_ends_with`) as `T`; the 8 task-named fns (to_upper/to_lower/trim/trim_start/trim_end/starts_with_b/ends_with_b/contains_b + casefold) are EXTERN-only (bodies in the native `.o`); `runtime_core.c`=411120B; standalone link resolves all `rt_str_*` (NO undefined); **byteeq-real CI GREEN on current main** (selfhost-byteeq-gate.yml = the STANDALONE config). So M5-for-rt_str = DONE; the mechanism the pilot was meant to demonstrate is the live default. **Lone residual (c16, deferred not faked)**: the 2 int-return bridges' standalone `#ifndef` branch is genuine self-contained C (`hxlcl_strncmp`/`strcmp`) that codegen emits (codegen.hexa:442/445); the native seed exports only the HexaVal-return `_b` form, so the bridge has NO native body to migrate — it is an ABI adapter below the type system (twin of the F-M4-B1 nanbox finding), and dropping it needs either 2 new hand-assembled int-return seed fns across both ABIs OR a codegen call-site change — both leave the proven "excise `#else` + ar native `.o`" template, so per c10/c9 they are a separate codegen/seed micro-unit, not this pilot (no fake drop). RE-CONFIRMS `F-LEGB-RTSTR-HEXASOURCE-COMPLETE` + `F-LEGB-RTCORE-PURE-CANDIDATES-ZERO` (#3468 "Z2a 누적 EXHAUSTED"). No emitter/build-script/seed change → fixpoint structurally untouched. The genuine `ls self/*.c == ∅` lever stays M4 (common-core port, families WITH a migratable native body) + runtime.c substrate redesign — not a per-family pilot.
- **stdlib/cloud — `cloud pods` 가 ORPHAN 을 AUTO-ADOPT (orphan-prevention teeth)** — `cloud_pods_reconcile_prune` 가 alive-on-provider 미등록 포드를 탐지하고도 경고만(`ORPHAN … adopt or reap`) 하던 의도적 정책을 뒤집어, 탐지 즉시 M5 레지스트리에 `pod_registry_add(id, provider, "", "auto-adopted-on-contact", "", "", "RUNNING")` 로 stamp → `ADOPTED … was untracked → registered`. provider 는 권위 set 멤버십(runpod=rl.pod_ids·그 외 vast)으로 파생. **비파괴**(레코드만 추가 — auto-reap 과 달리 포드를 안 건드림, 타프로젝트 포드도 down-guard 하에 보호)이라 GHOST auto-forget 과 대칭으로 안전. 효과: `cloud pods` 한 번이면 alive 미등록 포드 전부 tracked 로 자가치유 → untracked orphan 이 PERSIST 불가. ssh_host/port 는 빈 채 stamp, 다음 run/exec 가 `_resolve_via_provider` 로 self-heal. 검증(실측): mini 에서 22 runpod orphan → 1패스에 22 ADOPTED, 재실행 시 orphan 0; `hexa run tool/build_hexa_cloud.hexa --install` 후 라이브 확인. (발견: demiurge RTSC 캠페인 중 `cloud pods` 가 22 orphan 을 surface 만 하고 방치.)
- **RT-NATIVE-ZEROC M4 B4 (array-core) — mechanism-BLOCKED(construct WALL-1 + alloc WALL-2), file-drop BLOCKED-on-M5, `stdlib/runtime/array_core.hexa` NOT viable; unblocking lever = `__hx_make_val` (c2/c9/c16)** — M4 batch B4(`hexa_array_new/get/set/push/pop/reserve/snapshot_array`)를 raw-mem intrinsic 으로 새 `stdlib/runtime/array_core.hexa` 에 포팅하려던 전제를 검증으로 정정 = B1 의 쌍둥이 + 추가 alloc 벽. **레이아웃(c2, summer 측정)**: `HexaArr{items@0,len@8,cap@16}` sizeof 24 · `HexaVal{tag@0,payload@8}` sizeof 16 · element stride 16 · cap<0=arena-sentinel · 용량배증 8,16,32,64. **WALL-1 CONSTRUCT(B1 twin)**: get/pop/new 은 16B HexaVal **원소**를 반환하는데 `(tag,payload)→HexaVal` 생성 intrinsic 이 없음 — 모든 `__hx_*` leaf 는 결과를 TAG_INT/BOOL/FLOAT 로 박싱(양 backend + self/codegen.hexa grep 확인), raw payload word 로드는 hexa_int(word) 로 와서 원소의 진짜 tag 가 소실·재각인 불가. `arr[idx]` 는 hexa source 에서 `hexa_index_get` 호출로 lower(RAN build/hexat probe2) = 순환. **WALL-2 ALLOC**: new=calloc · push/reserve=realloc+arena(`__hexa_val_mark_top`/arena_alloc_items/음수cap)+`hexa_val_heapify`+`_hx_stats_*` · snapshot=arena→heap promote — malloc/calloc/realloc/arena 모두 intrinsic-불가, 진짜 substrate. **C-differential gate(c2, 자가판정 아님 — RAN)**: 신규 `scripts/scratch/rt_native/b4_array_gate.c` 가 standalone runtime.c(PRIMARY CI config)의 array fn 들을 empty·single·용량배증(40개→cap64)·set/get·mixed-tag(str+float)·pop·reserve(128)·snapshot-identity 로 구동, 각 저장 원소의 raw 16B {tag,payload} 이미지 대조 → summer 실 x86_64 **GATE PASS 32/32, 0 fails**(레이아웃 오라클 포함). **`array_core.hexa` NOT viable(c9, 경험적)** → 미작성(c10, 순환/redundant). **#else drop-verdict = NO, M5 까지 유지(c9)**: gate 가 그 standalone runtime.c 를 링크 성공 = C 바디 LIVE; 삭제 시 release_build `./hexa` 링크 실패. **언블록 레버(NAMED)**: `__hx_make_val(tag,payload)->HexaVal` — `__hx_tag` 의 대칭 write-half(ADD-only, 양 backend + C-mirror + bind), B4/B5 원소반환 + B1 nanbox.hexa 파일축소를 동시 언블록(단 WALL-2 alloc 은 별개). 런타임/에미터 무변경(ADD: gate.c + verdict) → fixpoint 구조적 보존. verdict `F-M4-B4-ARRAY`.
- **RT-NATIVE-ZEROC m5 r2 — runtime_core.c standalone-#else 인구조사 🧱 TERMINAL (per-family-drop lane EXHAUSTED, docs-only)** — r1 의 marker-filter 인구조사(F-LEGB-RTCORE-PURE-CANDIDATES-ZERO)와 독립적으로, r2 의 정밀 프레임(standalone `#else` C-body 경로)으로 재실행 → 수렴. 생성된 `self/runtime_core.c`(8734L)를 파서로 walk: 37 `#ifdef HEXA_HAS_HEXA_RT_STDLIB` guarded 블록(with `#else`:35, no-`#else`:2), 43 delegate-extern, ~40 guarded fn. **결정적 검증(c2, RAN grep)**: native-arm extern 43/43(`rt_null_coal`/`rt_len`/`rt_map_*`/`rt_print*`/`rt_cmp_*`/`rt_eq_*`/`rt_add_slow`/`rt_sub/mul/div/mod` 등) 전부 hexa-source body 보유(`self/rt/core.hexa` · `stdlib/runtime/numeric.hexa`) = ALREADY-NATIVE. auto-classifier 가 6개를 PURE? 후보로 표시했으나 본문 직접 RAN: `map_set/remove`→`_impl`(ALLOC) · `println`→`printf/fflush`(stdio substrate) · `to_string`→재귀버퍼(ALLOC) · `null_coal/len` = 진짜 pure 이나 `rt_null_coal/rt_len` 이미 hexa-source(`self/rt/core.hexa:32,45`) = ALREADY-NATIVE. **PURE-PORTABLE(포트 필요) 버킷 = 0**. `#else` 정체(`runtime_core.c:3173` "standalone link doesn't define HEXA_HAS_HEXA_RT_STDLIB") = 이미-native 코드의 standalone-link 중복. **결론**: per-family native-seed 드롭 레버 EXHAUSTED(c16 TRUE-CEILING — migratable body 부재). 잔여 ls 레버 단 둘, m5-per-family 범위 밖: (i) `__hx_make_val` construct-unlock(array-r2 lane, CONSTRUCT-WALLED arith/cmp/eq/to_string 13패밀리) (ii) standalone `#else` 중복제거/runtime.c substrate 재설계(M5 standalone-redesign lane). 포트 강제 안 함(c9) · `runtime_core.c` byte delta = 0. verdict `F-M5-R2-RTCORE-CENSUS`. m5 DEPLETED(per-family-drop lane).
- **x86_64 codegen ROOT-CAUSE fix — tag-aware arithmetic binop (fn-param numeric-array indexing tag-corruption, c1)** — anima engine-smoke-fix lane 이 근본확정한 버그: 숫자배열(`[float]`/`[int]`)을 함수 파라미터로 받아 인덱싱하면 원소가 잘못된 tag(=24, garbage)/raw-payload 로 반환(`cannot subtract/multiply non-numeric operand (tag 24 ...)`·segfault). **근본원인 2중(c2, aiden 실 x86_64 emit asm 캡처로 확정)**: ① `compiler/codegen/x86_64_linux.hexa` 의 `_STMT_ASSIGN` 디스패치에 `op=="index"`/`op=="index_set"` 핸들러 전무 → `a[i]` 가 default-assign 으로 fall-through 하여 인덱스를 드롭하고 배열 payload(컨테이너 포인터)+TAG_ARRAY 를 carry(arm64_darwin.hexa 는 둘 다 `hexa_index_get`/`hexa_index_set` 로 라우트). **이 ①은 #3481(f25be1e0a)로 main 에 선착지** — 본 PR 은 남은 ②를 마저 수리. ② `_STMT_BINOP` 산술경로(:1356-1378)가 raw 정수 `add`/`sub`/`imul` + 결과 tag 무조건 TAG_INT(0) 하드코딩 → tag-dispatch 전무 → float 산술이 IEEE-754 비트를 INT-tagged 로 반환(`1.0-0.0` 가 `4607182418800017408` 로 출력; ①만으론 R2/R3 통과·R1 float-sub 만 깨짐). arm64 는 `_hv_binop_sym`→`hexa_add_slow/sub/mul/div/mod` tag-dispatch. **FIX(surgical ADD-ONLY, c10, @root-cause-ok)**: `_x86_hv_arith_sym` 신설 + 산술binop(`+ - * / %`)을 tag-aware 런타임헬퍼로 라우트(SysV 16B HexaVal PAIR ABI: a→rdi:rsi·b→rdx:rcx, 반환 rax:rdx; arm64 `_hv_binop_sym` parity; bitwise·comparison·concat 은 기존 fast-path 유지). 정수전용 프로그램은 헬퍼가 int 경로를 타 결과 동일. **검증(c2, aiden 실 x86_64 emit→clang as→link→RUN, 자가판정 아님)**: 3 repro 전부 PASS — `l2([1,0],[0,1])`=**1.0**(전 tag24 sub-error) · `f([3,4])`=**7**(전 segfault) · `g(){let x=[5.0,6.0];x[0]}`=**5.0** · 회귀코퍼스 int `+-*/%`=13/7/30/3/1 · float `+-*/`=3.0/2.0/1.25/5.0 · param-array dist([3,0],[0,4])=**25.0** · loop int-array-index sum=60. M1 x86 leaf gate PASS(회귀무). x86_64-ONLY 변경 = arm64 byte-eq fixpoint(CI selfhost-byteeq-real/codegen-guard/miscompile-zero 전부 `arm64-apple-darwin` runs-on) 구조적 무영향. anima: engine_cli_smoke case16(`tag 24 * tag 24`) 잠금해제 대상.
- **RT-NATIVE-ZEROC M2/M3 DONE (#3481 ptr8 landed)** — `__hx_ptr_store8`/`__hx_ptr_load8` byte raw-mem leaf intrinsic 양 타깃 추가(arm64 strb/ldrb · x86 byte ptr/movzx, GNU-as byte-id + zero-extend 실측). 이로써 **M2(raw-mem)** 완비(M1 ptr64/32 + ptr8) · **M3(syscall)**는 M1의 syscall0/6이 런타임 syscall 전수(exit/read/write/stat/mkdir/close/mmap/munmap/getpid/open, 전부 ≤6 args) 100% 커버로 완료. domain RT-NATIVE-ZEROC 3/7(43%). **수렴 발견(B1 #3480·census·#3472)**: 런타임은 이미 mechanism-level hybrid-native — `ls self/*.c` 감소의 단일 진짜 레버는 **M5(standalone 재설계)**: 주력 출하 standalone(#else) 빌드가 native rt_*.o 를 링크하고 #else 바디를 excise + frozen-seed bump. 진행중: M5 파일럿(rt_str #else 드롭 end-to-end 증명).
- **RT-NATIVE-ZEROC M4 B1 (nanbox-ctor) — mechanism DONE, file-drop BLOCKED-on-M5, `stdlib/runtime/nanbox.hexa` NOT viable (c2/c9/c16)** — M4 batch B1(`hexa_int/float/bool/void/bits_to_float/enum_str/enum_str_v`)을 새 aprime-컴파일 `stdlib/runtime/nanbox.hexa` 로 포팅하려던 전제를 검증으로 정정. **이미 3중 native(c2)**: ①codegen `_hv_load`(arm64_darwin.hexa:1090) 가 모든 int/float/bool/void 리터럴을 `movz tag`+payload 레지스터쓰기로 직접 인라인(런타임콜 0) ②`HEXA_RT_SELFEMIT` 바이트에미터(`self/codegen/runtime_arm64.hexa` `rt_hexa_*` 6종, 각 12B=3 aarch64 명령, `emit_hexa_val_ctors_o.hexa` 번들, `as -arch arm64` byte-id + JIT-exec 검증) ③`__hx_*` read-side intrinsic. **C-differential gate(c2, 자가판정 아님 — RAN)**: 신규 `scripts/scratch/rt_native/b1_nanbox_gate.c` 가 standalone runtime.c(PRIMARY CI config)의 C-path ctor 7종 + arm64 `rt_hexa_*` 바이트 시퀀스를 음수·INT64_MIN/MAX·NaN/±inf/±0/DBL_MAX/MIN·true/false·void·enum-ptr 전수 대조 → aiden 실 x86_64 **GATE PASS 35/35, 0 fails**. **`stdlib/runtime/nanbox.hexa` NOT viable(c9, 경험적)**: `./build/hexat` 로 `fn f(n:int)->HexaVal{HexaVal{tag:0,payload:n}}` 트랜스파일 → `HexaVal(hexa_int(0), n)` 즉 HexaVal 구조체 리터럴이 `hexa_int` 를 **다시 호출**(self-recursive). numeric.hexa 주석대로 "hexa typing doesn't model the HexaVal union directly" — ctor 는 타입시스템 아래(raw 레지스터쌍)에 살아 바이트에미터/intrinsic 만 표현 가능. rt_str 패밀리(F-LEGB-RTSTR)와 동류 = 이미-native(딴 메커니즘), 요청 파일은 redundant/순환 → 미작성(c10). **#else drop-verdict = NO, M5 까지 유지(c9)**: 권위 byteeq/release 경로(`release_build`→`stage_resolve_runtime_a:271`)는 SELFEMIT 미설정 standalone runtime.c 를 링크 — aiden nm 측정 `T hexa_int/float/bool/void/bits_to_float/enum_str` 전부 DEFINED(strong) → #else 제거 시 standalone runtime.a 미정의 → `./hexa` 링크 실패. M5(`build_runtime_a_from_source` 가 native rt_*.o 링크 + #else excise + frozen-seed bump)까지 honest-keep. 런타임/에미터 무변경(ADD: gate.c + verdict) → fixpoint 구조적 보존. verdict `F-M4-B1-NANBOX`.
- **RT-NATIVE-ZEROC M4 PTR8 — `__hx_ptr_store8/load8` byte raw-mem leaves (M2 raw-mem set 완성)** — M1 이 ptr_load/store 64/32 을 착지(#3478); 8-bit byte 쌍이 유일 갭이었고 `hexa_strbuf_dup_n` 의 byte-write(batch B3) 에 필요. `__hx_ptr_store8(addr,off,val)`=val 의 LOW BYTE 를 [addr+off] 에 기록(addr 반환·chainable) · `__hx_ptr_load8(addr,off)`=[addr+off] byte 를 ZERO-EXTEND 로 read. **변경(surgical, c10 — ptr8 2개 + gate 만)**: bind 화이트리스트 +2 · arm64 codegen `strb w5,[x1]`/`ldrb w1,[x1]`(ldrb=str_byte 가 증명한 인코딩) · x86 codegen `mov byte ptr [r10],sil`/`movzx r10,byte ptr [r10]`(movzx byte=str_byte 증명) · **arm64 Mach-O 에 STRB 네이티브 인코더 신설**(0x39000000|imm<<10|rn<<5|rt, 기존 LDRB 0x39400000 미러; LDRB 는 M1 에서 이미 존재) · self/codegen.hexa C-mirror(`*(uint8_t*)` store / `(int64_t)*(uint8_t*)` load = uint8 캐스트로 sign-ext 없음). **검증(c2, 자가판정 아님)**: (A) 인코딩 그라운드트루스 GNU as↔objdump — x86 store `41 88 32` · x86 load `4d 0f b6 12` · arm64 `strb=39000025`/`ldrb=39400021`, 4/4 일치. (B) 시맨틱 — emit→as→link→**실 x86_64(summer) 실행** exit=7: mmap 페이지에 0x00/0x7F/0xFF/0x80 store8→load8 회수, **0xFF→255(NOT -1)·0x80→128(NOT -128) = ZERO-EXTEND 확정** + low-byte-only(511→255) + adjacent-byte isolation + chainable. 빌드 aprime_m4 smoke exit42 PASS · M1 gate 회귀 PASS. **self-host fixpoint(gen3≡gen4)**: ADD-ONLY 이고 컴파일러가 `__hx_ptr_*` 를 호출하지 않으므로 구조적 보존(CI selfhost-byteeq-real 권위). **정직 잔여(c9, PRE-EXISTING M1-wide, ptr8 도입 아님)**: x86 ELF object emitter 가 `byte/dword/qword ptr` size-prefix mem-operand 를 파싱 안 함 → `--emit=obj` 는 str_byte/ptr32/ptr64 와 함께 ENCODE-MISS(M1 도 asm-text 경로로만 검증); asm-text codegen·C-transpile·fixpoint 무영향, arm64 는 bracket-only operand 라 `--emit=obj` 정상. ELF size-prefix 파서 확장은 별도 M1-wide 유닛. verdict `F-M4-PTR8`.
- **RT-NATIVE-ZEROC M1 LANDED (#3478) — x86_64 leaf-intrinsic parity 25/25** — arm64의 `__hx_*` 25종 leaf intrinsic(tag·payload eq/cmp/arith·float fadd/fcmp NaN-correct·to_double·str_byte·arr_len·syscall0/6·ptr_load/store)을 x86_64 codegen 으로 포팅(+371L ADD-ONLY). 실행으로 3 근본버그 수리(const_float payload 미materialize·__hx_to_double r12 clobber·syscall6 pool-reg clobber). 검증: GNU-as byte-id 31/31 + aiden 실제 x86_64 실행(core16·str3·arena5) + **CI 12/12 green(selfhost-byteeq-real cross-target gen3≡gen4 보존 포함)**. M4(런타임코어 native 포팅)의 x86_64 토대. 잔여(M2 orthogonal): const_str-assign·unop`!` 미구현. verdict F-M1-X86-LEAF-PARITY.
- **stdlib/cloud — transport-truncation 자동 재시도 (gap-3 flaky-proxy)** — vast.ai/RunPod SSH 프록시가 명령 스트림을 전송 중 잘라먹어 원격 로그인 셸이 잘린 명령(`bash -c` 본문 없음·`rm` 피연산자 없음)을 받거나 출력의 `__CLOUD_*__` 마커가 유실되는 간헐 실패를, 디스패처가 `no exit-code marker`(rc<0)로 **이미 탐지**하나 일회성 실패로 흘려 호출부가 매번 수동 재시도해야 했던 것을 해소. `_ssh_capture_retry` 헬퍼 신설 — 마커 부재 + ssh는 접속됨(`local_rc != 255`)일 때만(=전송 잘림) 동일 명령을 backoff(0.4s→1.0s)로 최대 2회 재전송. 3개 전송 경로 적용: `cloud_run_wall_opts`(구조-argv)·`cloud_exec_opts`(셸-모드)·`cloud_run_script_opts`(stdin 파이프, 인라인 동형 루프). **재시도 안 함**: 마커 존재(`remote exit N`=실결과, 비제로도)·`local_rc==255`(진짜 transport 아웃리지=빌링만 태움, `_ssh_transport_outage` 담당) — ssh 접속됨+명령 잘림 중간상태만 재시도. 시맨틱=at-least-once(명령 완주 후 마커만 유실 시 재실행 가능하나, 잘린 *전송*은 코히어런트 명령을 거의 실행 못 했고 CLI 실사용 명령 df/ls/pgrep/rm-rf/pkill/mkdir-p 가 멱등이라 안전; 비멱등 부작용은 `--script`(stdin byte-exact) 또는 dedupe 키 사용). 검증: `hexa build` 네이티브 컴파일(타입+코드젠+링크) PASS · 실 `hexa cloud run` 회귀(구조-argv + bash-lc 경로 무손상).
- **RT-NATIVE-ZEROC M1: x86_64 `__hx_*` leaf-intrinsic parity (25/25, arm64→x86_64)** — x86_64-linux native codegen(`compiler/codegen/x86_64_linux.hexa`)을 arm64_darwin.hexa 와 leaf-intrinsic PARITY 로 끌어올림. **인벤토리 갭(c2)**: `grep __hx_ x86_64_linux.hexa` = **0** — arm64 의 25개 STMT_CALL leaf special-case(UNLOCK-2..11)가 x86_64 에 전무. **포팅(ADD-ONLY, +~330L, runtime.c/#else 무접촉 = 부트스트랩 무위험)**: `__hx_tag`(tag-slot read) · `__hx_payload_eq/nz/lt/gt/le/ge`(cmp+setcc+movzx) · `__hx_payload_add/sub/mul`(add/sub/imul) · `__hx_payload_fadd/fsub/fmul/fdiv`(movq GPR↔xmm + addsd/subsd/mulsd/divsd) · `__hx_payload_flt/fgt/fle/fge`(**comisd + seta/setae, NaN-correct** — GCC -O1 ordered idiom 그라운드트루스 대조: `<`→comisd b,a;seta · `<=`→comisd b,a;setae, unordered=false, arm64 MI/LS 미러) · `__hx_to_double`(cvtsi2sd + cmove branchless tag-select) · `__hx_str_byte`(add+movzx byte ptr) · `__hx_arr_len`(mov r10d, dword ptr [r10+8] int32 zero-ext) · `__hx_syscall0/6`(Linux ABI: args→rdi/rsi/rdx/r10/r8/r9, num→rax, syscall) · `__hx_ptr_store64/load64/store32/load32`(add + mov qword/dword ptr). 2 헬퍼 신설(`_x86_hv_payload` = arm64 `_hv_load` hi-half · `_x86_hv_store_leaf` = arm64 `_hv_store_dst`). **ENABLING ROOT-FIX(c1, shim 아님)**: x86_64 backend 에 const_float **payload** 경로가 전무했음(#3462 pair-model 은 int/str/bool 만) — float 리터럴이 `_x86_op_resolve` 의 `imm 0` 폴백으로 떨어져 모든 float leaf 가 0.0 에 대해 계산. `_x86_op_resolve`+`_x86_64_op_for_operand_st` 에 `mov reg, float_to_bits(v)`(컴파일타임 런타임빌트인, macho_arm64 float-pool emit 도 사용; GNU as 가 imm64 mov 를 movabs 로 인코딩) 추가 — arm64 `.LCfltN: .double` pool 값과 동치, X86StrTab float-pool 서브시스템 불요. **검증 중 SCRATCH-DISCIPLINE 버그 2종 발견·수정(c2, 자가판정 아님 — 실행으로 포착)**: ① `__hx_to_double` 가 tag scratch 로 하드코딩 `r12`(=alloc-pool reg) 사용 → live 값 클로버 → chained `flt(to_double(a),to_double(b))` 의 2nd to_double 가 a 의 tag 를 b 의 payload 로 읽음. tag scratch → `rax`(caller-saved, non-pool). ② `__hx_syscall6` 가 args 를 pool reg(r12-r15/rbx) 에 staging → 동일 클로버. args 를 syscall ABI reg(rdi/rsi/rdx/r10/r8/r9, 전부 caller-saved+pool-disjoint) 에 직접 로드. **인코딩 그라운드트루스(c2, GNU as ↔ objdump 캡처, `scripts/scratch/rt_native/x86_leaf_gate.sh §A`)**: comisd 66 0f 2f c8 · seta 0f 97 c0 · setae 0f 93 c0 · cvtsi2sd f2 49 0f 2a c2 · movq xmm0,r10 66 49 0f 6e c2 · movzx r10,byte ptr 4d 0f b6 12 · syscall 0f 05, 31/31 어셈블·일치. **시맨틱 그라운드트루스(c2, emit→as→link C런타임→실 x86_64 실행, exit 단언)**: core=16(int cmp/arith/nz/tag + float cmp/arith + to_double) · str=3(`__hx_str_byte "ABC"`→65/66/67) · arena=5(mmap(9)+ptr_store/load 64/32+munmap(11)) — **GATE PASS**. **정직 잔여(c9, PRE-EXISTING x86 갭, leaf 와 무관, M1 범위 밖)**: const_str→LOCAL ASSIGN(`let s="ABC"`)가 bare-label 절대주소 materialize(leaf 자체는 직접 리터럴 lea[rip] 정상) + x86_64 backend 에 `unop`/`!`(logical-not) lowering 전무(`nop` emit) — 둘 다 M2 후속. self-host fixpoint(gen3≡gen4 x86_64 byteeq)는 ADD-ONLY 변경으로 구조적 보존(빌드 aiden, CI selfhost-byteeq-real 권위). verdict `F-M1-X86-LEAF-PARITY`.
- **RT-NATIVE-ZEROC 캠페인 착수 (유저골 다세션 완수)** — leg B `ls self/*.c == ∅` bounded 프런티어 4-렌즈 CONFIRMED-terminal(#3467 rt_str완료·#3468 census0·#3470 nanbox GO·#3472 #else=PRIMARY NO-GO) 확정 후, 유저 골 "다세션 착실 완수"에 따라 풀캠페인 개시. domain `RT-NATIVE-ZEROC` (M1-M6): M1 x86_64 leaf-intrinsic parity → M2 raw-mem → M3 @syscall → M4 ~1190fn 포팅 → M5 standalone 재설계 → M6 drop+∅. 빌드 aiden/summer. 현재 M1 착수.
- **RT-NATIVE leg B standalone `#else` liveness (c2/c9/c16) — LIVE, honest NO-GO** — leg B 의 "standalone #else 재설계" KEY UNKNOWN 결정: standalone(non-`HEXA_HAS_HEXA_RT_STDLIB`) `#else` C-body 폴백을 컴파일하는 실제 shipping/CI 빌드가 있는가? **답=LIVE, 그것도 PRIMARY(CI/release) 경로**. 빌드체인 grep(캡처): `selfhost-byteeq-gate.yml`/`release.yml`/`miscompile-zero`/`determinism-gate` → `tool/release_build` → `tool/stage_resolve_runtime_a:271` 가 `self/runtime.c` 를 `CFLAGS=-O2 -std=gnu11 -D_GNU_SOURCE -Wno-trigraphs`(**`-DHEXA_HAS_HEXA_RT_STDLIB` 없음 = standalone**)로 컴파일 → `build/runtime.a`, `release_build:54` 가 이를 `HEXA_PREBUILT_RUNTIME` 으로 export → `./hexa` 링크. caller 가 CFLAGS 에 매크로 주입하는 곳 0개(grep empty). **aiden 실측(실제 CI 빌더 직접 실행)**: `CC=gcc bash tool/stage_resolve_runtime_a` → `build/runtime.a` 551566B 성공, **`rt_*` DEFINED(T)=33**(=`#else` C-body); 대조 gate-ON(`-DHEXA_HAS=1`) 컴파일은 DEFINED 21·UNDEFINED 155(hexa-source 위임). 즉 `#else` 제거 시 standalone `runtime.a` 가 `rt_*` 미정의 → live `./hexa` 링크 실패 = REAL regression(substrate 아님, c16). **선행 `F-LEG-B-DEADCODE-PATH`("standalone=죽은 코드, 쓰는 빌드 0개") 반증·정정** — 그 조사는 `build_aprime`/`build_native_*`(HEXA_HAS=1) 패밀리만 보고 release 의 `stage_resolve_runtime_a` 를 놓침. 두 빌드 패밀리 공존: aprime(HEXA_HAS=1, hexa-source 위임) vs release/standalone(#else C-body, PRIMARY shipping). `#else`=Go/Rust 동급 freestanding substrate=honest-keep(c9), **제거 안 함·에미터 미편집**. leg B `ls self/*.c==∅` 의 진짜 잔여 재확정: ①공통 ~1190 core fn hexa-native 전수(다세션) + ②standalone 빌드가 hexa-source rt_*.o 를 ABI-정합 링크하도록 재설계(`F-LEG-B-STANDALONE-SCOPE` 의 HexaVal↔float wrapper) — `#else` 단순삭제 아님. 조사 only, codegen/에미터 무변경. verdict `F-LEGB-STANDALONE-ELSE-NOGO`.
- **RT-NATIVE leg B UNLOCK-2 nanbox-constructor intrinsic feasibility (c2/c9/c16) — GO, 이미 실행됨 · 순환 NO** — census category-(1)(`hexa_int/float/bool/void`,`hexa_bits_to_float`) = 16B 태그유니온 생성자(`{.tag=TAG_*, .payload}`, `runtime_core.c:1422-1448`)를 `@tag` intrinsic 으로 `.hexa` 표현 시 **부트스트랩 순환**(intrinsic 이 자기가 정의할 생성자를 다시 호출)이 생기는가? 를 bounded 조사. **핵심 답=NO 순환(c16, true-ceiling 아님)**: 이미 랜딩된 `__hx_tag`/`__hx_payload_*` intrinsic(CHANGELOG \"UNLOCK-2 DONE\" #124)이 `arm64_darwin.hexa:1896` 에서 **`bl` 0개 — 순수 `movz`/`mov`/`ldp` 레지스터 쓰기로 직접 lower**. 생성자 측도 `_hv_load`(:1090)가 operand-kind 별로 이미 인라인(`const_int→movz lo,#0(TAG_INT)+hi val`·`const_bool→movz #2`·`const_void→#4`·`const_float→#1`+f64 ldr). 즉 intrinsic = 생성자 본문의 call-site 인라인, 바닥은 레지스터파일+immediate-move opcode 이지 HexaVal 생성자가 아님 → 재귀 미폐합. **검증(c2)**: aprime 3/3 byte-identical fixpoint 보존(CHANGELOG #124 captured) + `_hv_load`/`__hx_tag` 소스 직접 읽음(aiden). nanbox 생성자는 irreducible floor 아님 = GO. **scope**: runtime_core.c 6 fn + ~6 STMT_CALL codegen case, 전부 arm64-DONE. 유일 갭 = x86_64 backend — #3462 pair-model 은 있으나 **`__hx_*` leaf intrinsic 0개**(측정 `grep -c __hx_ x86_64_linux.hexa`=0), bounded 단일파일 미러. **정직 단서(c9)**: 이 생성자 포팅은 `ls self/*.c` 를 줄이지 **않음** — 파일은 standalone #else 블록이 붙잡음(ING #14-16, 빌드재설계). 의의: Z2c dispatch core(`hexa_type_of`=`switch on __hx_tag`) unblock 확정. 조사 only, codegen 무변경. verdict `F-LEGB-UNLOCK2-NANBOX-FEASIBILITY`.
- **RT-NATIVE leg B 전수 인벤토리 (c9) — runtime_core.c 진짜 native-seed 포트 후보 = 0** — #3467(rt_str hexa-source-complete)을 `runtime_core.c` 의 *전체* pure-candidate 집합으로 확장 검증. 정적 파서(`/tmp/inv_phase1.py`·`inv_phase2.py`, aiden)로 169개 REAL C-body 정의를 추출→PURE 84/IMPURE 85 분리→84 생존자를 callee·매크로 단위 본문 확인. **결과(FALSIFIED)**: 84 PURE 가 전부 5범주 중 하나 — ① nanbox tag-constructor 원시함수(`hexa_int/float/bool/void`,`hexa_bits_to_float`,`hexa_enum_str/_v`)=native-seed 가 *이것 위에* 세워진 기반 substrate(포팅=순환) ② syscall/allocator substrate(`hexa_mkdir`=mkdir(),`rt_file_*`/`rt_fs_*`/`rt_path_exists`=hxlcl_stat,`hexa_str_own_with_len`=strbuf alloc,`__hexa_last_error`=글로벌) ③ map/dict(`hexa_map_*`/`__map_*`=HX_MAP_TBL·hmap_find·fnv1a raw 해시테이블) ④ array(`hexa_array_*`=HX_ARR_ITEMS raw 메모리/cap) ⑤ string/math 위임 래퍼(`hexa_str_replace`→rt_str_replace 등 type-guard+1줄 delegate; 실로직 rt_str_* 는 #3467 로 이미 hexa-source, `hexa_log10/log2/tanh`→rt_*=libm CLAUDE.md 정책). native-seed(.byte_at/.substring/.char_code_at) 로 표현 가능 + hexa-source 부재 + 순수 = **0개**. **결론(c9)**: Z2a awk-synth 누적 경로 EXHAUSTED — leg B 잔여 self/*.c 는 'portable pure 함수' 가 아니라 substrate floor(nanbox/syscall/alloc/raw-mem) + standalone #else 중복(ING #14/#15). port 강제하지 않음. 진짜 ls-감소 = standalone #else 폴백 제거(빌드 재설계). verdict `F-LEGB-RTCORE-PURE-CANDIDATES-ZERO`.
- **RT-NATIVE leg B 정직 인벤토리 (c9) — "다음 rt_str 그룹" 전제 FALSIFIED** — grep 검증(빌드불요, mini 로컬)으로 확인: `rt_str` 패밀리는 **hexa-source 레벨 이미 완료**. index 패밀리(`index_of`/`index_of_from`/`last_index_of`/`count_substr`)는 `runtime_core.c` 에서 전부 `extern` 선언(C-body 아님)이고 실제 정의는 `self/rt/string.hexa`·`stdlib/runtime/ctype.hexa`·`self/runtime_pure.hexa` 3곳 hexa-source. `runtime.c` substrate 의 rt_str C-body 5종(`substr`/`slice`/`bytes`/`parse_int`/`parse_float`)도 전부 hexa-source 정의 보유 → 진짜 'C-only' rt_str = **0개**. 즉 "다음 floor-안전 rt_str 그룹 awk-synth 누적"은 이미-native 함수 재포팅 = 헛수고(하마터면 `index_of` 포팅으로 한 사이클 낭비). 진짜 잔여(ING #7/#14 재확인) = **standalone #else 중복제거** — `runtime.c`/`runtime_core.c` 의 `#else`(standalone) C-body 폴백이 native hexa-source 를 중복해 파일을 붙잡음. 진짜 ls 감소 = standalone 빌드가 hexa-source `rt_*.o` 를 항상 링크하게 만들어 `#else` 제거(연구벽 아님 = 빌드 재설계, aiden·CI faithful 3-target + byteeq-real 게이트). verdict `F-LEGB-RTSTR-HEXASOURCE-COMPLETE`.
- **RT-NATIVE leg B Z2c: string-search predicates (starts_with_b/ends_with_b/contains_b) → native SSOT + x86_64 const_bool codegen fix** — 3 predicate(`rt_str_starts_with_b`/`rt_str_ends_with_b`/`rt_str_contains_b`)를 `stdlib/runtime/ctype.hexa`→`self/runtime_hi.hexa` native-seed SSOT 로 이동(11→14 fn). **CODEGEN ROOT-CAUSE(c9)**: 이 3개가 native seed 최초의 **bool-반환** 함수 — regen 후 x86_64 seed 가 모든 case false 반환(C-differential 6/14, true 8개 전멸). asm 분석: `return true`/`return false` 의 `const_bool` operand 가 `_x86_op_resolve`/`_x86_tag_resolve`(`x86_64_linux.hexa`) 에서 case 누락→ payload imm 0 + `tag default=TAG_INT` 로 fall-through. #3462 pair-model 재작업이 local/call/string tag-carry 는 했으나 **immediate const_bool 리터럴은 빠뜨림**(arm64_darwin.hexa 는 이미 const_bool→TAG_BOOL=2+payload int_val 처리). **FIX**: x86_64 codegen 3 site 에 const_bool case 추가(`_x86_op_resolve`·`_x86_tag_resolve`·`_x86_64_op_for_operand_st`) — payload=int_val(0/1), tag=TAG_BOOL=2, arm64 parity. **검증(c2, summer linux x86_64)**: 수정 후 native seed regen → `mov rdx,1/mov rax,2`(true) · `mov rdx,0/mov rax,2`(false) 정확 · C-differential harness `scripts/scratch/rt_native/search_pred_gate.c` **14/14 PASS** (C-path runtime.a AND native x86_64 seed 양쪽) · emitter `runtime_hi_gen_emit.hexa` awk-synth == extract.sh gen.c **byte-identical**(sha 9a9cdcbc, 14 fn) · 양 seed 14 .globl(x86_64 cross-assemble+PIE-OK, arm64 Mach-O 14-globl content) · dual-config(`-D_GNU_SOURCE` ±HEXA_HAS_HEXA_RT_STDLIB) 양쪽 compile · aprime smoke exit42 PASS(ZEROC=0/1 양 config). CI 권위 게이트(faithful-nobaseline 3-target + selfhost-byteeq-real)는 머지세션 소관.
- **RT-NATIVE leg B x86_64 native seed FIXED+LANDED (#3462)** — 앞선 root cause(x86_64 raw-int model 의 HexaVal tag 소실)를 x86_64 backend value-model 재작업으로 실수정: 모든 HexaVal 값을 arm64 처럼 {tag,payload} 쌍으로 carry(`x86_64_linux.hexa` +499L — pair ingress·tag-slot·call box/unbox·rax:rdx 쌍 return·binop tag-write) + 쌍 모델이 노출한 regalloc 버그 2종(rax caller-saved alloc-pool 제거·loop back-edge liveness `[0,pos]` 확장) 수정 + callee-saved push/pop. CI 권위 12/12 green(자가판정 아님): C-differential harness 9/9(전 null→후 정상)·faithful-nobaseline linux-x86_64(전 RED job)·linux-arm64·darwin-arm64·**selfhost-byteeq-real(whole-compiler gen3≡gen4 새 SHA)** 전부 PASS. 신규 `self/native/runtime_hi_x86_64.s` 시드 + `HEXA_ZEROC_RT_HI=1` 기본 → x86_64-linux 도 `runtime_hi_gen.c` 제거(darwin 동급). verdict F-LEGB-X86-PAIRMODEL-GATE-A-PASS. (#3461 의 BLOCKED 진단을 supersede.)
- **RT-NATIVE leg B x86_64 native seed BLOCKED — root cause 확정**: PR #3460(linux x86_64 native rt_hi seed)이 CI linux-x86_64 FAIL. aiden(linux x86_64) 4중 대조(negative-control + ABI-control + 시드직접호출 + arm64 asm)로 root cause 확정 — x86_64 codegen 의 'raw-int model'(x86_64_linux.hexa:636-640)이 payload 1-reg 만 추적하고 HexaVal tag 를 통째로 잃음(local→TAG_INT=0). tag-dispatch 헬퍼(hexa_len)가 tag=0→0 반환 → string 받는 모든 native 함수 전멸(C harness 9/9 null). + ingress 레지스터 버그(:1255). arm64 는 stp/ldp 로 {tag,payload} 쌍 carry 라 정상. self-emit byte-eq·int-only RUNG 통과는 tag 무관이라 못 잡음. FIX=x86_64 value-model 을 arm64 처럼 쌍 carry 재작업(large). PR #3460 머지불가. verdict F-LEGB-X86-SEED-TAGLOSS-ROOTCAUSE.


- **RT-NATIVE leg B Z2a: linux x86_64 native rt_hi seed + 빌드경로 통합 [CANDIDATE]** — leg B 의 첫 실제 cross-target ls-감소 경로 구현. x86_64-linux 가 `#include "runtime_hi_gen.c"`(C) 를 회피하고 native `.s` seed → `rt_str_*` 를 `runtime.a` 에 ar 하게 만든다. **EMIT(검증)**: `build/aprime_cc`(gen3 self-host, x86_64 codegen 졸업 #3431/#3432) 로 `self/runtime_hi.hexa` 의 lib-only head(`fn runtime_hi_selftest` 전) 를 `aprime_cc _drv.hexa --emit=asm --target=x86_64-linux-gnu -o runtime_hi_x86_64.s runtime_hi_lib.hexa` 로 구움(`-o` 필수, 더미 `_drv.hexa` 첫 토큰 = "missing SOURCE" 가드 충족, target 은 exact-string `x86_64-linux-gnu`). **ABI(검증)**: linux ELF 심볼 underscore 없음(`rt_str_split` ≠ Darwin `_rt_str_split`) — codegen 이 target 별로 올바르게 처리(메모리의 underscore-drop 우려 falsify); `.intel_syntax noprefix`·`.hidden`; 11 fn(split/lines/pad_left/pad_right/repeat/center+to_upper/to_lower/trim/trim_start/trim_end, 현 SSOT 일치). **ASSEMBLE(검증, 캡처)**: `clang -target x86_64-linux-gnu -c` + `zig cc -target x86_64-linux-gnu -c` 양 toolchain rc=0 → ELF x86-64 obj, errors 0, 11 `rt_str_*` 전부 **T**(defined-global); undefined U = `hexa_*` 헬퍼(전부 runtime.c 제공, Darwin seed 와 동일 surface). **FROZEN SEED**: `self/native/runtime_hi_x86_64.s`(linux ELF sibling) 신설 + Darwin seed `self/native/runtime_hi_native.s` 를 9→11 fn REGEN(현 SSOT 동기화; trim-ends #3457 보충 — 그래야 Darwin native 경로 활성 시 link 무결). **빌드경로(c1)**: ① `tool/stage_resolve_runtime_a` 에 `resolve_native_rt_hi_seed()` 신설 — `$TARGET`(release_build SSOT)/uname 으로 arch 판정→arch-matched seed 선택(darwin→native.s·x86_64→x86_64.s·linux-arm64→C경로)→`$CC -c` 어셈블+`HEXA_ZEROC_RT_HI=1` export, `restore_frozen_seeds`(gen.c synth 결정)+`build_runtime_a_from_source`(ar) 양쪽 BEFORE 실행; SAFETY = seed `.globl rt_str_*` 11 미만이면 native skip→C fallback(stale/partial seed undefined-ref 방지, toolchain-free count). ② `tool/build_aprime.sh` auto-enable 가드를 `Darwin arm64`+`Linux x86_64` 로 확장(case uname -sm), 로컬 seed 어셈블도 arch-matched. ③ `tool/regen_runtime_hi_native_s.sh` 신설(양 seed 재현 `[darwin|x86_64|all]`, cross-assemble sanity 11 T assert, `.file` 정규화로 글로벌-심볼 결정성·local `.L` 라벨 per-process nonce 정직 명시). **로컬 검증(c2, 캡처)**: x86 emit 11 .globl·ABI no-underscore · clang+zig 11 T 심볼 · resolver 4-target 유닛(x86_64/darwin→ZEROC=1 obj 11 T · linux-arm64→C경로 · opt-out=0) · bash -n 3 스크립트 OK · regen all 11/11 · mktemp `.s` portability 버그(macOS nonce-after-ext) 발견·수정(`build/rt_hi_seed.s` 고정경로). **CI 1차(run 27646785203, authoritative)**: darwin-arm64 **success**(11-fn seed+resolver native 경로 OK) · linux-arm64 **success**(C 경로 유지, 무회귀) · linux-x86_64 **FAILURE** at link — `/usr/bin/ld: rt_hi_native.o: relocation R_X86_64_32S against .rodata can not be used when making a PIE object`(emit/assemble/scrub/ar 전부 OK 후 PIE 링크에서만). **ROOT CAUSE**: x86_64 codegen 이 일부 문자열-리터럴 주소를 ABSOLUTE `mov reg, .LCstrN`(R_X86_64_32S) 로 emit — modern linux 기본 PIE 링크 거부(다른 payload 는 이미 `lea reg,[rip+.LCstrN]` PIC; return/assign 4개만 절대형; C 경로는 cc 기본 PIC 라 미발생). **FIX**: PIC 변환 `mov reg,.LCstrN`→`lea reg,[rip+.LCstrN]`(의미동일) 을 `regen_runtime_hi_native_s.sh` x86_64 emit 에 baked-in(darwin arm64 adrp/add 본래 PIC 미적용) + regen sanity 에 PIE-link 검사 영구화. **재현·검증**: seed bare `mov reg,.LC` 0/10개 lea[rip], `zig ar→runtime.a→zig cc -pie` = CI 실패 config 정확 재현 후 CLEAN(R_X86_64_32S 제거). **2차 CI**: push 후 linux-x86_64 green = LANDED. verdict F-LEGB-X86-NATIVE-SEED-IMPL.

- **RT-NATIVE leg B 방향 정직화 (c9)** — `git ls-files self/*.c` 빈 결과 = `runtime.c`·`runtime_core.c`·`runtime_hi_gen.c` 3개 전부 gitignored 빌드 생성물(clean checkout 은 이미 `ls self/*.c == ∅`). awk-synth 2그룹(#3455 casefold/trim · #3457 trim-ends)은 linux `runtime_hi_gen.c` 의 flaky-regen 을 결정적 awk un-escape 로 제거한 **빌드 안정화**(CI-green)이지, `.c` 제거(ls 감소)가 아니다 — Darwin 은 `runtime_hi_native.s` native seed 로 이미 gen.c 를 회피(Z2a-done)하나 x86_64/linux 는 native seed 부재로 gen.c(C) 경로 강제. 진짜 ls-감소 경로 = **linux x86_64 native seed**: x86_64 codegen 졸업(#3431/#3432)으로 `aprime --emit=asm --target=x86_64-linux` 가 `self/runtime_hi.hexa` 를 `.s` 로 구울 수 있으므로, 그 frozen seed + `stage_resolve_runtime_a` 의 `HEXA_ZEROC_RT_HI` 경로 확장으로 linux 도 gen.c 를 회피한다. (이전 세션들의 "leg B 누적" 프레이밍을 정직 수정.)

- **RT-NATIVE leg B Z2a: trim-ends(trim_start/trim_end) awk-synth → LANDED #3457 (CI full-green)** — casefold(#3455)에 이은 awk-synth 패턴 2번째 그룹. CI faithful-nobaseline run 27644231452: linux-x86_64+linux-arm64+darwin-arm64 전부 success·non-success 0(첫시도 green). 패턴 신뢰성 확정. 머지 e9bf3b872.

- **RT-NATIVE leg B Z2a: trim-ends group (rt_str_trim_start/trim_end) → native SSOT — awk-synth regen [CANDIDATE]** — casefold/trim(#3455, d99ecc321) 의 green awk-synth 패턴을 정확히 같은 방식으로 다음 floor-안전 그룹에 적용. **그룹**: `rt_str_trim_start`/`rt_str_trim_end` — `rt_str_trim` 의 직계 형제(동일 byte_at whitespace scan + 단일 substring slice, leaf builtin 만, libc/syscall/libm 의존 0). ctype.hexa 주석이 명시적으로 "trim_start/trim_end stay here (not yet in the runtime_hi.hexa seed group)" 라 지목한 다음 후보. **메커니즘(c1)**: ① `self/runtime_hi.hexa` SSOT 에 2 fn 추가(+ selftest) ② `self/runtime_hi_gen_emit.hexa` 에 gen.c 정의 2개 추가(awk un-escape → 9→11-fn gen.c, trim 바디와 byte-id idiom) ③ `tool/restore_frozen_seeds`: frozen 151c52c8 `self/runtime.c` 의 `#ifndef HEXA_HAS_HEXA_RT_STDLIB` trim_start/end C 바디를 awk 로 scrub(seeds-removed runtime.a 는 그 macro 없이 컴파일→C 바디 활성→gen.c 와 duplicate 였을 것; casefold 는 frozen 에 바디 부재라 scrub 불요였으나 trim-ends 는 frozen 에 존재 L4343/4354) + 11-fn 검증 grep 확장 ④ `stdlib/runtime/ctype.hexa` 의 trim_start/end 제거(migration note). native `.s` seed 는 #3455 처럼 미변경(gated `HEXA_ZEROC_RT_HI` 경로, faithful gate 비대상 — blast radius 최소). **로컬 검증(c2)**: awk un-escape rc=0(11-fn, trim_start/end 정의+ctor 존재) · scrub awk 12줄 제거(trim_start/end 바디 0, 빈 #ifndef/#endif shell well-formed) · bash -n 양 스크립트 OK · 충돌마커 0. **CI 판정(authoritative, c9)**: nobaseline-gate faithful-nobaseline 3 타깃이 머지 게이트.

- **RT-NATIVE leg B Z2a 정밀화: casefold/trim 합성 regen (hexat-FREE awk un-escaper) — flaky-regen 근본제거 [LANDED #3455]** — 직전 winning-combo(#3451, regen-FREE = C `#ifndef` 바디 KEEP + frozen 6-fn gen.c)는 GREEN·안정이나 linux 를 C floor 로 유지(native 아님). 본 후보는 그 결정을 뒤집어 linux 도 native 화하면서 3-CI-사이클 진동(undefined↔duplicate)의 **진짜 원인(flaky regen)** 을 근본 제거(c1·c16). **메커니즘**: `tool/stage_resolve_runtime_a::_unescape_emit_to_c` — `*_emit.hexa` SSOT 의 `    buf = buf + "<C줄>"` 순수 리터럴을 awk 로 un-escape(\n \t \" \\) 하면 emitter 의 .c 출력과 **byte-identical** → hexat/hexa-runner **불요**(로컬 runner dyld fault·seeds-removed CI hexat-exec 실패 둘 다 우회). PRIMARY regen 경로로 채택, runner 는 fallback. **구조(로컬 검증)**: regen 된 `runtime_core.c` = `rt_str_to_upper/to_lower/trim` **전방선언만**(C 바디 retire, emitter L909 `;`) + `runtime_hi_gen.c`(9-fn) = **정의**(L201 `{`) → 선언+정의 = undefined·duplicate 0. **검증(c2, 캡처)**: 두 tool 스크립트 bash 구문 OK · 충돌마커 0 · awk un-escape rc=0(core 8734줄·gen 270줄) · to_upper/trim 정의 중복 0. **CI 판정(authoritative, c2)**: nobaseline-gate run 27642745829 — faithful-nobaseline linux-x86_64+linux-arm64+darwin-arm64 **전부 success · non-success 0**(3-사이클 진동 해소, 머지된 #3451 combo 대비 linux native + flaky-regen 제거 = strict improvement). 머지 d99ecc321(#3455). verdict F-LEGB-Z2A-CASEFOLD-SYNTH(LANDED).

- **RT-NATIVE leg B Z2a: casefold/trim group (rt_str_to_upper/to_lower/trim) → native SSOT — 3 C-body retire LANDED** — 직전 🧱(#3449, mechanism+48/48 parity 증명 but linux 회귀로 revert)의 잔여 unblock (b)(c)(d) 종결. **(c) 9-fn regen**: `self/runtime_hi.hexa` 에 to_upper/to_lower/trim 3 fn 추가(byte_at + 26-char substring lookup + join — seed group 과 동일 leaf builtin, `bytes_to_str_raw`/`from_char_code` 불요) → `tool/extract_runtime_hi.sh`(a-fix 적용)로 `runtime_hi_gen.c` 6→9 fn regen(현 hexat module-suffixed init 심볼 정확 처리). 양 SSOT 갱신: native seed `self/native/runtime_hi_native.s` 9 fn 재emit(aprime_cc --emit=asm lib-only, 기존 6 fn 바디 byte-id 유지) + linux gen.c emitter `self/runtime_hi_gen_emit.hexa` 9 fn(reverse-transform == extract.sh gen.c byte-id 검증). **(b) fwd-decl 불요 판명**: substring-lookup 구현이 `hexa_str_char_code_at`+`hexa_str_substring`(둘 다 emitter 정의부보다 앞)만 참조 → verdict 가정한 `hexa_bytes_to_str_raw` fwd-decl 불필요(실측 우선, easy 가정 falsify). **collision 해소**: `stdlib/runtime/ctype.hexa` 의 3 fn 제거(native seed 와 Darwin 중복심볼 회피, trim_start/trim_end 잔존) + `runtime_core_emit.hexa` 의 C `#ifndef HEXA_HAS_HEXA_RT_STDLIB` 바디 3개 retire(gen.c 가 stdlib+no-stdlib 양 경로 SSOT). **검증(c2, 전부 캡처)**: Darwin Z2a build_aprime.sh rc=0·smoke exit42 PASS · native rt_str_* vs 退役 C 바디 **30/30 parity**(empty·mixed·all-ws·\t\r\n·multibyte) · `.trim()` e2e exit7 · runtime.c 양 macro(-D=1/없음) 컴파일 clean(redefinition 0) · RT_UND 불변(seed 신규 U=`hexa_cmp_gt`+`hexa_str_byte_at` 2개, 둘 다 코어 ABI) · **linux cross(zig cc)**: x86_64+aarch64-linux runtime.c+gen.c 컴파일 rc=0·3/3 심볼 + 전체 ELF 링크 rc=0(rt_str_trim/to_upper resolved, undefined/duplicate 0). **(d) linux byte-eq**: GPU pod(vast/runpod)은 CPU linux 빌드에 부적합·비용낭비 → 카논 검증경로 = PR 트리거 `release.yml`(ubuntu-latest x86_64) + `selfhost-byteeq-real` 게이트(무료·권위)로 위임, 로컬 zig 크로스로 de-risk(c2). `ls self/*.c` = runtime_core.c+runtime.c(gitignored 생성물 2개, goal 미달이나 SSOT C-body 3 fn 감축). **linux regen fix(CI-caught)**: `faithful-nobaseline(linux-x86_64)` 가 `hexa_cc.c: undefined reference to rt_str_to_upper/rt_str_trim` 로 RED — 근본원인 = `tool/stage_resolve_runtime_a::build_runtime_a_from_source` 가 runtime_core.c 는 emitter regen(C 바디 제거)하면서 `runtime_hi_gen.c` 는 frozen 6-fn seed(151c52c8) 그대로 사용 → linux(non-Z2a) runtime.a 에 casefold/trim 3 fn 누락. **fix**: `_resolve_hexa_runner` 헬퍼 추출 + `regen_runtime_hi_gen_from_emitter`(runtime_core regen 와 동일 STRICTLY-OPTIONAL 계약) 추가, non-Z2a 경로에서 gen.c 를 9-fn SSOT 로 regen 후 컴파일. Darwin Z2a 경로는 native seed 사용으로 무영향(else-branch skip, 재빌드 smoke PASS 확인). **최종 WINNING COMBO(c2 정정, 3 CI 사이클 후)**: regen 이 CI 호스트서 FLAKY(gen.c 가 frozen 6-fn 잔존→undefined OR 9-fn→복원 C 바디와 redefinition) + NO CI workflow 가 build_aprime.sh 호출(="Darwin smoke dup" 은 LOCAL-only; ce5a2 Darwin faithful Build PASS) 판명 → **regen-FREE 조합 채택**: ① C `#ifndef HEXA_HAS_HEXA_RT_STDLIB` 바디 KEEP(restore — 양 linux arch 보편 no-stdlib provider, hexat/regen 불요) ② `stage_resolve_runtime_a` baseline 복귀(gen.c regen 제거 → frozen 6-fn 고정 → C 바디와 절대 dup 안 함) ③ `runtime_hi_gen_emit.hexa` 6-fn 복귀(frozen seed 일관성). KEEP: native seed 9-fn(Darwin Z2a casefold native) + runtime_hi.hexa 9-fn(seed SSOT) + ctype 3 fn 제거(app 은 runtime.a C 바디서 resolve). linux runtime.a = C 바디(3)+frozen gen.c(6)=9, overlap/dup/undef 0. verdict F-LEGB-Z2A-CASEFOLD-TRIM-LANDED(ITERATION 4).
- **`hexa deck` 1급 서브커맨드 승격 (dojo 미러)** — 기존에 `stdlib/deck/{cli,gen,rtsc,…}.hexa` 로 존재하던 6-도메인 input-deck 생성기를 `hexa cloud`/`hexa dojo` 와 동일한 1급 디스패처 verb 로 승격. 이전엔 `hexa deck` 이 "unknown subcommand 'deck'" 로 실패했고 `/deck` skill 이 `hexa run stdlib/deck/gen.hexa` 로 우회. **변경(c1)**: (1) `self/stdlib_commands.hexa` 에 `Cmd { name: "deck", group: "gen", … }` 추가(dojo 엔트리 미러) → S3 drift gate 통과. (2) `self/main.hexa` 디스패치 체인에 `sub == "deck"` 분기 추가(install-dir-resolve + `cmd_run` 로 `stdlib/deck/cli.hexa` 라우팅 = dojo 패턴 동일) + STDLIB CLI help 블록에 deck 3줄 추가. (3) `stdlib/deck/cli.hexa` 에 EMIT 경로(`<domain> <slug> '<spec-json>'`) 추가 — open-schema flat-JSON 스캐너로 DeckSpec 빌드 → `gen_deck` 디스패치 → `exports/<domain>/decks/<slug>/` 에 `write_text`(deck-guard-clean, 사람 Write 아님). 기존 `prototypes`/`domains`/`disk-estimate` verb 유지. **검증(c2)**: `hexa deck` → help(unknown subcommand 아님) · `hexa deck domains` → 6-도메인 목록 · `hexa deck rtsc smoke '{"a":1}'` → `exports/rtsc/decks/smoke/{vc-relax,scf}.in·ph.in·RUNBOOK.md` 4파일 emit. 디스패치 바이너리 재빌드(`tool/build_hexa_cli` LOCAL_BUILD=1) + 설치 트리 배포로 네이티브 해소. deck 테스트 4종 + S3 drift PASS, `hexa dojo` 무회귀.

- **RT-NATIVE leg B unblock (a): extract_runtime_hi.sh strlit-init drift 근본 fix** — 백그라운드 에이전트가 casefold/trim group #2 포팅에서 정직한 🧱(메커니즘+48/48 parity 증명, but linux 회귀로 clean revert·#3449)로 지목한 "real wall" 첫 블로커 해소. `tool/extract_runtime_hi.sh` 가 constructor footer 의 string-literal init 심볼을 `__hexa_rt_strlit_init` 로 **하드코딩**하던 drift — 현재 hexat 은 module-suffixed `__hexa_strlit_init__runtime_hi` 를 emit하므로 footer 가 미존재 심볼 호출 → linux `runtime_hi_gen.c` regen 회귀. **근본 fix(c1)**: footer 가 hexat 실제 emit 심볼을 자동탐지(`grep -oE __hexa_strlit_init[...]`)하고 body 와 동일한 `__hexa_rt_` rename 적용. **검증(c2)**: bash 구문 OK · 탐지+rename 로직이 bare/module-suffixed 양쪽에서 footer-call == body-def 정확 일치(MATCH/MATCH). 잔여 unblock=(b) runtime_core_emit fwd-decl 2개 +(c) 9-fn regen(hexat) +(d) linux cross byte-eq(pod) — 빌드/pod 필요분은 regen 세션 소관(c9 정직). verdict F-LEGB-Z2A-CASEFOLD-TRIM follow-up.

- **아키텍처 SSOT: ARCHITECTURE.md → ARCHITECTURE.json 전환 + md 완전 폐기** — c4(단일문서) JSON 트리 채택. `ARCHITECTURE.json`(JSON 트리 = AI·툴 파싱 SSOT, 5 섹션 Overview·Self-host status·Component map·Data flow·Verify gate, 손실 없이 이관) + `architecture.html`(fetch+렌더 뷰어: prose/list/table/codeblock/subsection, inline 마크다운+다크모드) + `serve.py`(정적 서버 + 브라우저 자동오픈, `file://` fetch 차단 우회). **검증(c2)**: JSON 유효·serve.py 구문 OK·블록타입 5종 전부 뷰어 처리·라이브 서빙 스모크 200 OK(html 6487B/json 17.8KB, 뷰어가 SSOT fetch 확인). `ARCHITECTURE.md` 는 `git rm` 으로 **완전 폐기**(stub 미잔존). 참조 전환: CLAUDE.md(본문 2 + 트리 1) · README.md · harness.config.json(docs.architecture + allowlist) · .harness/enforcement.json(설명 텍스트) = 핵심 4파일 잔여 `ARCHITECTURE.md` 0.

# Changelog

Chronological log of notable changes. One section per ship batch, date-keyed. hexa-lang runs at high commit velocity (RFC-driven); this file carries the headline landings — `git log` is the detailed record.

For the full audit trail, see `git log`.

---

## 2026-06-16

- **RT-NATIVE: leg B 경계확정 + Z2a 메커니즘 증명 (데모→구조)** — leg B(`ls self/*.c==∅`)를 "거대 미지수"에서 **경계확정·메커니즘증명**으로 전환(c9 정직, c16 다른 각도). 측정: runtime_core.c 8082L 중 irreducible floor=**작음**(syscall/asm 8 → `__hx_syscall0/6` intrinsic 구축됨 · libm extern 10 → CLAUDE.md "trig=libm" FFI 허용 · HexaVal struct def 15 → git-tracked 헤더 표현가능), 나머지 ~580 fn = dispatch/value-op 볼륨. **메커니즘 PROVEN**: `stage_resolve_runtime_a` `HEXA_ZEROC_RT_HI=1` 경로가 이미 `build/rt_hi_native.o`를 runtime.a에 ar + `#include runtime_hi_gen.c` 제거(Z2a-done, rt_str_* native 완료). 결론: leg B = 연구 벽 아님, **검증된 Z2a 패턴의 ~580fn 다세션 누적 + standalone 재설계(#else 80블록)**. verdict F-LEGB-FLOOR-SIZED-MECHANISM-CONFIRMED.

- **RT-NATIVE: leg B 코어 native 누적 batch2 — 비교/산술/eq 10 fn** — `__hx_payload_lt/gt/le/ge/eq/add/sub/mul` + `__hx_to_double`+`flt/fadd`(혼합 float) = int/float fast-path 완전 native. **검증(c2)**: 비교4(3<5·5>3·4<=4·4>=4)+eq1+산술3(20+22·50-8·6*7=42)+float2 정확 · asm·obj exit10 · `_rt_*` T 심볼 10. 코어 native 누적 8→**18/591**. (string/map/valstruct 분기는 추가 leaf 잔여). 다세션 누적 가속(int/float 코어 op 완비). 데모: scripts/scratch/rt_native/rt_core_batch2.hexa.

- **RT-NATIVE: leg B 코어 native 누적 — type predicate 7종(is_int/float/bool/str/void/array/map)** — `__hx_tag` + `__hx_payload_eq(tag, TAG_X)` = **완전 leaf**(MAP/glue 잔여 없음, predicate 는 단순 tag 비교). **검증(c2)**: 7종 정확(int→is_int T·float→is_float T·…·타입 불일치 F) · asm·obj exit8 · `_rt_is_*` T 심볼 7개(stdlib 편입 가능). 코어 native 누적 1(rt_truthy #3443)→**8/591**. 패턴: 단순 코어 fn 은 leaf 만으로 완전 native(glue 불요), 복합 fn(truthy MAP·dispatch)은 추가 leaf+glue lowering 필요. 다세션 누적 가속. 데모: scripts/scratch/rt_native/rt_typepred_demo.hexa.

- **RT-NATIVE: leg B 코어 native 첫 fn PoC — rt_truthy(leaf intrinsic) == C hexa_truthy 10/10** — 코어 591 fn 의 첫 fn 을 leaf intrinsic 으로 .hexa native 작성: `__hx_tag` + tag별 분기(BOOL→payload_nz·INT→nz·FLOAT bits·VOID→0·STR→str_byte·ARRAY→arr_len). **검증(c2)**: rt_truthy native vs C hexa_truthy 10/10 일치(INT 0/nonzero·BOOL·FLOAT 0.0/nonzero·STR ""/nonempty·ARRAY []/nonempty) · asm·obj exit10 · `_rt_truthy` T 심볼(stdlib 편입 가능). MAP/VALSTRUCT 분기는 추가 leaf(hmap_find/vs-deref) 잔여 · 글루(if/==)는 아직 C 호출(typed-glue lowering 잔여, 정정 #3407). 의의: leg B 코어 native 패턴 확립(코어 fn→leaf .hexa→C byte-eq→T 심볼) = 다세션 누적이 실제 작동 실증. 첫 fn 검증, 591 중 1. 데모: scripts/scratch/rt_native/rt_truthy_demo.hexa.

- **RT-NATIVE: leg B 코어 native 성격 확정 — codegen-emit work (leaf intrinsic 확장)** — value_logic_fixture.hexa 주석: 코어 hexa_* fn(truthy/eq/is_type)은 HexaVal tagged-union 내부(HX_TAG/HX_INT) 조작 → hexa source 가 직접 못 함 = **codegen-emit pass 의 일**, stdlib 추가 아님. ★ 핵심: 이번 세션 leaf intrinsic(__hx_tag/payload_*/str_byte/to_double/arr_len/syscall/ptr, #3400-3417)이 정확히 그 codegen-side HexaVal 조작 구현 = 코어 native 직접 진전(value_logic_fixture 가 "codegen-side work 필요"라 한 것 착수). leg B 코어 591 fn native = codegen-emit 포팅(leaf+typed-glue)+일부 stdlib hexa-source(numeric.hexa rt_eq/add/cmp_lt 존재). 이번 세션 ~18종 leaf=codegen 토대, leg A 양 타깃 byte-eq 졸업=양 타깃 정확·결정적. 잔여=591 fn codegen-emit native+ABI wrapper+native runtime.a+C-seed 제거. 다세션 누적, 메커니즘 완비. verdict: .verdicts/rt-native-byteeq/F-NATIVE-RUNTIME-POC.txt.

- **RT-NATIVE: leg B 코어 native 범위 정확 측정 — 591 hexa_* fn** — nm build/runtime.a(567448B): 총 T 심볼 744 = hexa_*(코어 값op/생성자/string/array/map/arena) **591** + rt_*(stdlib 위임, native obj PoC✅) 42 + 기타 111. ∴ leg B 코어 native 대상 = 591 fn(1190 추정→591 정확화). 코어 591 메커니즘 전부 증명됨(값op leaf #3400-3409·native arena #3413-3416·dispatch #3405). 잔여=591 코어 fn .hexa native+ABI wrapper+native runtime.a 통합+restore C-seed 제거→ls ∅. 거대 다세션 누적. leg A 양 타깃 byte-eq 졸업이 토대. verdict: .verdicts/rt-native-byteeq/F-NATIVE-RUNTIME-POC.txt.

- **RT-NATIVE: numeric.hexa FAIL 해결(flatten) — stdlib/runtime native obj 토대 완성** — numeric 단독 컴파일 FAIL=`undefined rt_log10`(:220)은 import 미해결(rt_log10 은 math.hexa T 심볼; 진짜 버그 아님). math+numeric flatten → native obj OK 118,912B(`_rt_array_*` T 심볼). ∴ stdlib/runtime/*.hexa 전부 flatten → aprime native obj 가능 = leg B native runtime.a 의 stdlib 부분 토대 완성. 남은: 코어 1190 fn .hexa(값op·arena·dispatch 증명됨)+ABI wrapper(float↔HexaVal)+native runtime.a 통합+restore C-seed 제거. 거대 다세션. verdict: .verdicts/rt-native-byteeq/F-NATIVE-RUNTIME-POC.txt.

- **RT-NATIVE: leg B native runtime PoC ✅ — aprime 가 .hexa 런타임 native obj 컴파일** — leg A byte-eq 졸업 직접 활용: aprime `--emit=obj` 로 stdlib/runtime/ 모듈 native 컴파일 — math.hexa(30464B, T 심볼 _rt_cos/sin/exp/log/fmod/acos)·ctype(52720B)·posix(4136B)·net(808B) 전부 OK(numeric rc=1 FAIL 의존성). = leg B native runtime.a 핵심 토대 작동. ABI 갭: math.hexa `rt_cos(float→float)` vs runtime.c `HexaVal rt_cos(HexaVal)` — 심볼 동일하나 ABI 불일치 → wrapper 필요. leg B 확정 경로: (1)stdlib/runtime native obj(PoC✅) (2)코어 1190 fn .hexa(값op·arena·dispatch 메커니즘 증명됨) (3)ABI wrapper+native runtime.a (4)restore C-seed 제거→ls ∅+C-free. NEXT: numeric 수리→8모듈 native→ABI wrapper→코어 fn 점진. verdict: .verdicts/rt-native-byteeq/F-NATIVE-RUNTIME-POC.txt.

- **RT-NATIVE: goal `ls self/*.c == ∅` 측정 방식 확정 — self/runtime.c/core.c = gitignored build artifact** — git ls-files self/*.c=5개(cuda/native wrapper만, runtime.c/core.c 부재) + git check-ignore 양성 = **git 추적 안 됨·.gitignore**. clean checkout(build 전)=∅, working tree(build 후)=2개(restore_frozen_seeds 가 frozen C seed blob 151c52c82 생성). **진짜 goal(c9 gitignore 트릭 금지)** = build 가 .c 생성/컴파일 없이 native runtime 사용. 현재 build_aprime STAGE-0 restore→C seed→clang→runtime.a(C 의존) = 미달. leg B = restore_frozen_seeds(C 생성)를 native runtime.a(aprime native 컴파일)로 대체 → 1190 fn .hexa native 전수(leg A byte-eq 졸업으로 토대 완비). NEXT: native runtime.a PoC(aprime→stdlib/runtime/*.hexa native→rt_*.o→C runtime.o 없이 runtime.a). verdict: .verdicts/rt-native-byteeq/F-GOAL-MEASURE-CLARIFY.txt.

- **RT-NATIVE: leg B 파일 축소 경로 발견 — standalone(neither) #elif C body = 죽은 코드** — 빌드 도구 플래그 grep: 모든 빌드(build_aprime·cross_build·native_linux)가 HEXA_HAS_HEXA_RT_STDLIB 또는 HEXA_RT_SELFEMIT 사용 → **neither(standalone) 경로 쓰는 실제 빌드 0개**. runtime.c 3-way(#if SELFEMIT extern/#elif !HEXA_HAS C body/#else extern)의 #elif C body(~548L)는 죽은 코드 = 제거 시 실제 빌드 무영향(active/SELFEMIT extern)+파일 축소. 복잡도: runtime.c=frozen seed(14663L, git blob 151c52c82, restore_frozen_seeds cold 복원)→직접 편집은 frozen seed 정합 필요. NEXT(다세션): frozen 메커니즘 정합+죽은 C body 제거(active/SELFEMIT byte-id 검증)+코어 1190 fn native 전수. leg A 양 타깃 byte-eq 졸업(#3412/3432), leg B 파일 0 만 남음. verdict: .verdicts/rt-native-byteeq/F-LEG-B-DEADCODE-PATH.txt.

- **RT-NATIVE: leg B standalone 재설계 ABI 발견 — 단순 링크 아님** — 12 standalone-only fn(runtime.c 손-작성)은 C body 단순하나 hexa-source 와 ABI 불일치: runtime.c `HexaVal rt_cos(HexaVal x)` vs stdlib/runtime/math.hexa `pub fn rt_cos(x: float) -> float`. standalone 이 hexa-source rt_*.o 그대로 링크 시 ABI(HexaVal 16B vs float reg) 불일치. → standalone 재설계 = HexaVal↔float ABI wrapper(fn 별, 작지만 비-trivial), 단순 #else 제거 불가. leg B 규모 재확정: ① standalone 12 fn ABI wrapper ② 파일 0 벌크 1190 공통 fn native 전수. 둘 다 fn 별 byte-eq 검증(~20min/회) → 다세션. verdict: .verdicts/rt-native-byteeq/F-LEG-B-STANDALONE-SCOPE.txt.

- **RT-NATIVE: leg B standalone 재설계 규모 측정 — 12 fn(전부 hexa-source 존재) = 거의 해소** — runtime.c preprocess diff(active -D=1 1190 fn vs standalone 1202 fn): **standalone-only = 12 fn**(libm rt_cos/sin/exp/log/fmod · char rt_isalnum/isalpha · stub rt_net/pthread 5) · active-only 0(1190 공통). **핵심**: 이 12 fn 전부 stdlib/runtime/ 에 hexa-source 이미 존재(math.hexa·ctype.hexa·net.hexa·posix.hexa). → leg B standalone 재설계 = 12 fn hexa-source 를 standalone 빌드가 native 링크 = 거의 완료(x86_64 codegen 졸업으로 aprime native 컴파일 가능). 파일 0 벌크 = 1190 공통 fn native 전수(다세션). NEXT: standalone→aprime native rt_*.o 링크 PoC(12 fn)→#else 제거→dual-config gate. verdict: .verdicts/rt-native-byteeq/F-LEG-B-STANDALONE-SCOPE.txt.

- **RT-NATIVE ★★ x86_64-linux gen3≡gen4 byte-eq 확립 — leg A x86_64 졸업** — darwin 로컬(summer contention 우회): fresh aprime(#3431 포함) → flatten 전체 컴파일러 → `--emit=asm --target=x86_64-linux-gnu` ×2 → cmp. **결과: g3.s=g4.s=261,888 lines · BYTE-IDENTICAL · sha256 94df25cbc82a1685**. = x86_64 whole-compiler self-emit 결정성(gen3≡gen4) = byte-eq fixpoint. emit-correctness(#3431, as-clean obj 2.2MB) + emit 결정성(asm byte-id) = **x86_64 byte-eq leg 완성**. byte-eq 정의: leg A 는 컴파일러 EMIT 결정성 — asm-text byte-id 면 obj 결정적(asm→obj 는 결정적 as). darwin emit=obj 는 시스템 as(arm64)가 x86_64 directive 거부 → asm byte-eq 로 검증(emit 결정성 직접). **→ leg A 는 arm64(#3412 native obj) + x86_64(asm byte-eq + as-clean) 양 타깃 byte-eq 확립**. 남은 goal = leg B(파일 0, 별개). verdict: .verdicts/rt-native-byteeq/F-X86-GEN3-GEN4-BYTEEQ.txt.

- **RT-NATIVE ★ x86_64 whole-compiler ASSEMBLES (as 에러 0) — leg A-linux EMIT-correctness 달성** — 마지막 버그: x86_64 `_fmt_imm` 가 `to_string(imm)` 만 호출 → `to_string` 이 **음수 i64 부호 누락** → 음수 const_int(MStmt `target_block: -7001/-1` block-id sentinel)이 EMPTY operand(`mov rax,`/`cmp r12,`/`mov rdx,`) 렌더 = 16개 unknown-token. 진단(kind DIAG → `const_int rn= lbl=` → 음수 재현 `mov rax,` 빈) → FIX: x86 `_fmt_imm` `if imm<0 { "-"+to_string(0-imm) }`(arm64 movz 분해라 무영향, leg A #3412 보존). **검증(c2, darwin cross-assemble)**: whole-compiler asm 261,888L → `clang -target x86_64-linux-gnu -c` → **obj OK 2,204,600B · errors 0** · bare g<N> 0. **누적 (다수)→52→26→19→17→16→0**(#3423 global·#3425 idiv·#3426 wide-imm·#3427 test·#3428 push·본PR 음수const). 의의: x86_64-linux whole-compiler emit→assemble 통과 = leg A-linux EMIT-correctness 완성. 남은=obj→link→run + gen3≡gen4 x86_64 byte-eq(linux 실행). verdict: .verdicts/rt-native-byteeq/F-X86-WHOLECOMPILER-ASSEMBLES.txt.

- **RT-NATIVE: x86_64 빈 operand 2차 진단 — reg/label/mem/imm 모든 `_fmt_operand` 경로 배제** — `_fmt_operand` 에 reg.name/label "" → `<EMPTY_*_op>` placeholder 추가(arm64 무영향, smoke exit42 · leg A #3412 보존) 후 x86_64 whole-compiler emit: **placeholder 0개**(바이너리 반영 확인 strings 2개). od 로 본 빈 operand = `mov rdx,  `(완전 빈, `[]` 부재) → reg/label(placeholder 미발동)·mem(`[]` 부재)·imm(to_string)·unknown(`<?`) 전부 배제. = 빈 operand 가 `_fmt_operand` 분기를 우회하거나 x86_64 전용 렌더 경로 존재. NEXT: x86_64 LIR→asm-text 렌더 경로(`_fmt_instr`/`_fmt_args_for_op` 적용 여부) 확인. placeholder 진단 인프라 유지(빈 operand 가드). 누적 52→16(root: 모든 _fmt_operand kind 배제). verdict 업데이트: F-X86-EMPTYOP-DIAGNOSIS.txt.

- **RT-NATIVE: x86_64 빈 operand(잔여 16, 단일 클래스) 진단 — root=`target_block` field val** — `.LCstr1134`=bytes 74..="target_block" 확인. binop ==(`cmp r12,`) + struct_lit(`mov rdx,`) 둘 다 MStmt 의 `target_block` field VALUE 가 빈 operand(hir_to_mir `MStmt { target_block: tgt }` 의 tgt). 렌더(emit/asm.hexa `_fmt_operand`): reg.name/label "" → 빈 문자열(imm→to_string 불가·unknown→`<?kind?>` 불가). 미규명: `_x86_op_resolve` local fallback(`_x86_64_reg_for_local`)·const_str collect 등록 모두 유효 → 빈값 경로 코드리뷰 부재. **MIR operand kind 실제 덤프 필요**(c2 tune-to-green 금지). NEXT: `_fmt_operand` 빈값 진단 placeholder → x86_64 emit → 빈 operand kind 노출 → target_block operand 생성 root 수정. arm64 무영향(leg A #3412 보존). 누적 52→16(단일 클래스). verdict: .verdicts/rt-native-byteeq/F-X86-EMPTYOP-DIAGNOSIS.txt.

- **RT-NATIVE: x86_64 push wide-imm(>2^31) materialize 수정 — invalid operand 클래스 완전 제거(17→16)** — C7 stack-spill(call 7+번째 인자 push)이 const_int 를 imm 직접 push → 2147483648(2^31) 같은 값은 push imm32 부호확장 범위 밖 → `push 2147483648`(invalid operand). 수정: `[-2^31, 2^31)` 밖이면 `mov r10, imm; push r10`(push reg 무제한). **검증(c2, cross-assemble)**: whole-compiler 에러 **17→16** · **invalid operand 2→0**(클래스 완전 제거) · 남은 16 전부 단일 클래스 unknown token(빈 operand). 누적 52→26→19→17→16. 잔여 16 = `cmp/mov` RHS 빈값 (struct_lit field val · binop == 의 whole-compiler 특정 operand kind; 작은 재현 미포착, MIR 덤프 진단 필요). 다음=빈 operand MIR-level root 진단. 산출물: scripts/scratch/rt_native/x86_wholecompiler_aserrors.txt.

- **RT-NATIVE: x86_64 br_cond `test` cond materialize 확장 — as 에러 19→17** — br_cond test 가 const_int cond 만 materialize → 미처리 operand kind 가 `_x86_op_resolve` fallback imm 0 으로 새어 `test 0, 0`(x86 test 첫 operand 는 reg/mem 만; invalid). 수정: cond 가 local/global 이 아니면(= 모든 const류·미처리 kind) `mov r10, cond; test r10, r10` materialize. **검증(c2, cross-assemble)**: whole-compiler 에러 **19→17**(invalid operand 4→2·unknown token 15 유지) · g<N> 0 유지. 누적 52→26→19→17. 잔여 17 = invalid operand 2(push 큰 imm, C7 spill arg) + unknown token 15(`cmp/mov` RHS 빈값=const_str ""/struct field). 다음=push wide-imm + 빈 operand(const_str strtab 등록). 산출물: scripts/scratch/rt_native/x86_wholecompiler_aserrors.txt.

- **RT-NATIVE: x86_64 binop wide-immediate(>2^31) materialize 수정 — as 에러 26→19** — generic binop(add/sub/imul/and/or/xor)이 const_int operand 를 imm 직접 emit → 0xFC000000(4227858432) 같은 2^31 초과 값은 x86-64 ALU 의 imm32 부호확장 범위 밖 → `and r14, 4227858432`(invalid operand). 수정: const_int 가 `[-2^31, 2^31)` 밖이면 `mov b_scratch, imm; <op> dst, b_scratch`(reg materialize). **검증(c2, cross-assemble)**: 재현 `x & 4227858432` → `mov r11, 4227858432; and rbx, r11` obj OK · whole-compiler 에러 **26→19**(invalid operand 12→4·unknown token 14→15 flat 변동) · g<N> 0 유지. 누적 52→26→19. 잔여 19 = invalid operand 4(push 큰 imm·test 0) + unknown token 15(`cmp/mov` RHS 빈값=const_str ""/struct field). 다음=push wide imm + test 0 + 빈 operand. 산출물: scripts/scratch/rt_native/x86_wholecompiler_aserrors.txt.

- **RT-NATIVE: x86_64 idiv divisor 무조건 materialize 수정 — whole-compiler as 에러 52→26** — binop `/`(idiv)가 const_int divisor 만 reg materialize → 미처리 operand kind 가 `_x86_op_resolve` fallback imm 0 으로 새어 `idiv 0`(invalid operand) emit. 수정: divisor 를 **무조건** `mov b_scratch, b; idiv b_scratch`(idiv 는 reg/mem 만 허용). **검증(c2, darwin cross-assemble 게이트)**: 작은 재현 `x/7` → `mov r11,7; idiv r11` obj OK · whole-compiler(261,625L) cross-assemble 에러 **52→26**(invalid operand 38→12·unknown token 14 유지) · bare g<N> 0 유지(#3423). 잔여 26 = invalid operand(12, idiv 외) + unknown token(14, `cmp r12,` RHS 빈값=const_str "" 빈 라벨). 다음=잔여 invalid operand + cmp RHS const_str materialize. 산출물: scripts/scratch/rt_native/x86_wholecompiler_aserrors.txt.

- **RT-NATIVE: x86_64 global fix(#3423) whole-compiler 검증 + darwin 로컬 cross-assemble 게이트 (summer 우회)** — darwin clang multi-arch 백엔드로 `clang -target x86_64-linux-gnu -c` = x86_64 whole-compiler asm(261,590L)을 로컬 cross-assemble → **bare `g<N>` 라벨 0**(이전 다수→0, #3423 global fix whole-compiler 동작 확정) · "ambiguous operand size"(global) 클래스 소멸. 잔여 52 에러 = ① `idiv 0`(divisor 미처리 operand kind → resolve fallback imm 0) ② `cmp r12, `(binop == RHS 빈값 = reg-alloc local 미할당, #3421 잔여). 의의: **leg A-linux 검증을 darwin 로컬 cross-assemble 로 수행 가능**(summer/pod contention 불요, EMIT 정확성 펜스) — `scripts/scratch/rt_native/x86_crossassemble_gate.sh`. NEXT: idiv materialize + cmp RHS reg-alloc 수정 → 게이트 재실행 52 감소 추적. verdict: .verdicts/rt-native-byteeq/F-X86-GLOBAL-FIX-VERIFIED.txt.

- **RT-NATIVE UNLOCK-12: x86_64 global operand RIP-relative load 수정 (leg A-linux 블로커 #3422 수정)** — `_x86_op_rm`/`_x86_op_resolve` global baseline(shared==0)이 바 라벨 `g<id>` 직접 emit → GNU as "ambiguous operand size" 거부. 수정: baseline 도 `mov scratch, [rip+g<id>]`(RIP-relative 값 로드) + resolve scratch 반환(arm64 adrp/ldr 대응). **검증(c2)**: 재현 전 `cmp g0,0`(FAIL) → 후 `mov r10,[rip+g0]; cmp r10,0` → cross-assemble OK 1480B · RUNG2 회귀 없음 · arm64 build OK+smoke exit42(x86_64_linux.hexa 만 수정→arm64 leg A #3412 무영향). 의의: x86_64 leg A 핵심 블로커 수정(typed-int global single value 확정). 잔여: HexaVal 16B global 페어 load 정확성 summer 실행 재검증(다음). verdict: scripts/scratch/rt_native/unlock12_x86_global_fix.md.

- **RT-NATIVE: x86_64 whole-compiler 실패 root cause 확정 — global operand 라벨 직접 emit (leg A-linux 핵심 블로커)** — #3421의 `test g16`/`cmp 빈값` 의 진짜 원인 = **x86_64 가 HexaVal 16B 전역변수를 메모리 로드 없이 라벨 직접 emit**. 로컬 재현(scripts/scratch/rt_native/x86_global_repro.hexa): `let mut g_count=5; if g_count>0 {...}` → x86_64 `cmp g0, 0`·`mov rbx, g0`(g0=global label) → `clang -target x86_64-linux-gnu -c` = **"ambiguous operand size for cmp" FAIL**. arm64 대조: `adrp _g0@PAGE; add; ldp x0,x1,[x0]`(주소계산+16B 페어 값로드) = 정상. 원인: `_x86_op_rm` global 경로가 opts.shared==1(GOT)만 처리, shared==0(baseline)은 로드 안 함 + `_x86_op_resolve`가 바 라벨 `g<id>` 반환. **수정방향(NEXT)**: x86_64 global → RIP-relative 16B 페어 로드(`mov lo,[rip+g]; mov hi,[rip+g+8]`, arm64 ldp 대응) + store 대칭. single-file RUNG2(#3420)는 전역 안 써서 통과·arm64 leg A 무영향(#3412). verdict: .verdicts/rt-native-byteeq/F-X86-GLOBAL-OPERAND-BUG.txt.

- **RT-NATIVE: x86_64 whole-compiler self-emit gap 정확 규명 (leg A-linux 의 2 codegen 버그)** — summer(linux x86_64)서 전체 컴파일러(flatten 48 files/44k) `--emit=obj --target=x86_64-linux-gnu` → asm 경로(flat.s 246353L) → as 어셈블 실패(rc=2, ENCODE-MISS=0). **버그① `test g16, g16`**(flat.s:26080…): 가상 레지스터 g16 이 물리 reg 미할당 — x86_64=16 GPR, 복잡 함수서 16 초과 시 spill 필요한데 reg-alloc spill 경로가 g16+ 못 풀음(arm64=31 GPR 여유로 미발생→single-file 통과). **버그② `cmp r12, `**(flat.s:94669…): binop `==` RHS operand 누락(빈 operand, field hexa_map_get 비교 경로). 즉 x86_64 single-file RUNG1/2/3 ✅(#3420)·whole-compiler ❌ 2버그. leg A-linux gap = x86_64 reg-alloc spill + binop cmp RHS emit. arm64 leg A 는 이미 ✅(#3412). NEXT: x86_64_linux.hexa spill 경로 + cmp RHS 수정 → summer 재검증. verdict: .verdicts/rt-native-byteeq/F-X86-WHOLECOMPILER-CODEGEN-GAP.txt.

- **RT-NATIVE 돌파: x86_64-linux RUNG2(string-print) 동작 검증 — "linux codegen 미성숙" 전제 FALSIFIED** — summer(Linux x86_64) 실제 실행: ret42(RUNG1) `--emit=obj --target=x86_64-linux-gnu` 직접 ELF serialize(2216B)→runtime.a 링크→exit42 · **string-print(RUNG2) obj(2440B)→`stdout=[hello x86_64 RUNG2] exit=7` PASS**. 메모리의 "x86_64 RUNG2 string-print HexaVal 16B SysV reg-pair ABI 미impl"은 **stale·falsified** — HexaVal reg-pair ABI(rsi:rdi arg·rax:rdx return)가 string payload 까지 구현됨(x86_64_linux.hexa:609 `_x86_hv_pair_lo/hi`). darwin clang `-target x86_64-linux-gnu` cross-assemble 도 ELF obj 생성(실행은 linux sysroot=summer/pod). **leg B 종착 재평가**: #3419 인과의 "linux codegen 미성숙" 전제 FALSE → leg B 블로커는 codegen 성숙이 아니라 (a) x86_64 byte-eq fixpoint (b) standalone→hexa-source 링크 전환 (c) ~420 fn 전수. codegen 자체는 준비됨. verdict: .verdicts/rt-native-byteeq/F-X86-RUNG2-VERIFIED.txt.

- **RT-NATIVE: leg B(`ls self/*.c==∅`) 인과 사슬 정밀 규명 — 종착 블로커=linux native codegen(pod-gated)** — 측정(c9): runtime.c active(-DHEXA_HAS_HEXA_RT_STDLIB=1) preprocess 39,729L vs standalone(no -D) 40,446L = **standalone-only C #else body 717L**(linux 크로스 전용). 양쪽 darwin clang 로컬 OK(active 563816B·standalone 590576B). 인과: `ls self/*.c==∅` ⇐ darwin active .c-free(runtime 코어 native seed) AND linux standalone .c-free(#else 717L 제거 ⇐ cross_build 재설계 ⇐ **linux native codegen 성숙** ⇐ pod) AND ~420 C-only fn 전수 .hexa. **종착 판정**: leg B 진짜 블로커 = linux native codegen 성숙(pod) + 전수 볼륨 = darwin-only·단일세션·로컬 완료 불가, 다세션+pod 필수. gitignore 트릭은 c9 금지. verdict: .verdicts/rt-native-byteeq/F-LEG-B-CAUSAL-CHAIN.txt. (leg A byte-eq fixpoint는 ✅ 별도 검증완료.)

- **RT-NATIVE: leg B 전수 포팅 dual-config 회귀 게이트 + 정확 작업명세** — `scripts/scratch/rt_native/dualconfig_gate.sh`: 매 C-body→hexa fn-port 마다 runtime.c 의 active(-DHEXA_HAS_HEXA_RT_STDLIB=1, build_aprime config)+standalone(no -D, cross-build/smoke 링크경로) **양쪽 컴파일을 로컬 darwin 에서 회귀검증**(linux 크로스 불요, 흔한 케이스). 현재 트리 baseline: active 563816B·standalone 590576B 둘 다 PASS. **leg B 정확 작업명세(c9)**: 파일 0 = (1) **standalone 재설계**(#else C body ~548L 가 파일 붙잡음 → standalone 이 hexa-source rt_*.o 항상 링크하게 = 파일축소 진짜 첫 블로커) (2) ~420 C-only fn 전수 hexa 포팅(프리미티브 완비) (3) emitter→native obj OR .c 0바이트. = 다세션·일부 pod. **핵심 정직성**: 한 fn 이전만으론 파일 안 줄어듦(C #else body 유지) — standalone 재설계 선행 필수. RT-NATIVE.md Z2c 갱신.

- **RT-NATIVE UNLOCK-11: `__hx_ptr_store32/load32` + `str/ldr w` (32비트 struct-field arena R/W) + runtime_core.c=emitter-생성물 발견** — `__hx_ptr_store32(ptr,off,val)`(`add; str w`) + `__hx_ptr_load32`(`add; ldr w` zero-ext). STR w-form 인코딩 추가(clang byte-id str w3,[x1,#8]=b9000823). **검증(c2)**: HexaArr 레이아웃 시뮬({ptr@0 i64·len@8 i32·cap@12 i32}) arena 기록→회수=999/7/16 정확→exit3 · asm·obj 동일 · str-w·udf=0 · smoke PASS · additive. 이제 arena 에 실제 HexaVal 객체(HexaArr/HexaMap)를 C struct 레이아웃 그대로 할당 가능 = runtime_core 객체할당 완전 프리미티브. **구조 발견(c9)**: `self/runtime_core.c`(375KB)는 손-작성 아니라 **`self/runtime_core_emit.hexa`(9030L)가 verbatim emit 하는 생성물**(B9.C .c-text 패턴). 단 gitignore 트릭은 금지(c9)이므로 파일 0 = 전수 hexa 재구현 필요(생성물 ≠ 자동 제거). 데모: scripts/scratch/rt_native/arena_struct_field_demo.hexa.

- **RT-NATIVE Z3: stateful 네이티브 arena (전역 frontier cursor) + 파일축소 측정** — 전역 상태(`g_arena_base/off/size`) + `arena_init`(mmap)/`arena_alloc`(bump 8-byte align)/`arena_reset`(O(1))/`teardown`(munmap) = `hexa_arena_alloc` 구조적 .hexa 대응물. **검증**: 50셀 alloc+R/W 합 3675 정확 + reset(asm·obj exit2) · arena fn C malloc/arena/free bl **0개** · udf=0. 측정(c9): runtime_core.c=8082L/375KB, dead code 0, #else(standalone) ~548L, `#ifdef HEXA_HAS_HEXA_RT_STDLIB` 위임 **85 fn 이미 활성** — 파일 0 = 전체 심볼 native-seed화+standalone 재설계(전수·다세션). **메커니즘(값op·arena·dispatch) 완비, 잔여는 볼륨**. 데모: scripts/scratch/rt_native/arena_stateful.hexa.

- **RT-NATIVE UNLOCK-10: `__hx_ptr_store64/load64` — 네이티브 arena 메모리 R/W (할당+쓰기+읽기 전체 사이클 C-free)** — `__hx_ptr_store64(ptr,off,val)`(`add; str x,[x]`, ptr 반환) + `__hx_ptr_load64(ptr,off)`(`add; ldr`). str/ldr x,[ptr] 기존 인코딩 재사용. bind + codegen.hexa `*(int64_t*)((char*)ptr+off)`. **검증(c2, 결정적)**: mmap(4096) → store64(p,0/8/16,12345/67890/42) → load64 회수=정확 → exit3 → munmap. asm·obj 동일 · udf=0 · smoke PASS · additive. 의의: syscall6 mmap/munmap(#3413) + ptr R/W(#본PR) = **메모리 할당+쓰기+읽기 전체 사이클을 C malloc/store/load shim 0개로** 확보 = 네이티브 bump-pointer arena 완전 프리미티브 셋. 다음=arena.hexa(mmap 슬랩+bump 포인터) → hexa_arena_alloc C body 대체. 데모: scripts/scratch/rt_native/arena_roundtrip_demo.hexa. verdict: scripts/scratch/rt_native/unlock10_ptr_storeload.md.

- **RT-NATIVE UNLOCK-9: `__hx_syscall6(num,a0..a5)` — mmap/munmap 네이티브 emit (arena 토대 Z3)** — 6-arg BSD syscall(args x0..x5·num x16·`svc #0x80`·결과 x0)을 C shim 없이 emit. 각 인자 payload 를 callee-temp(x9..x14)에 먼저 적재(`_hv_load` x0..x3 스크래치 clobber 회피) 후 x0..x5 정렬. bind + codegen.hexa `hexa_int(syscall(num,a0..a5))`. **검증(c2, 결정적)**: getuid via syscall6=501 · mmap(4096,RW,ANON|PRIVATE,-1,0)→양수 포인터 exit0 · mmap→munmap 왕복 exit0 · asm·obj 동일 · svc·udf=0 · smoke PASS · additive→fixpoint 보존. 의의: 네이티브 arena 의 **메모리 할당/해제 토대를 C malloc shim 없이 확보** — 메모리상 "진짜 floor=libm/FFI+syscall@asm"의 syscall 축 핵심(mmap/munmap) 로컬 돌파. 다음=bump-pointer arena .hexa → hexa_arena_alloc C body 대체. verdict: scripts/scratch/rt_native/unlock9_syscall6_mmap.md.

- **RT-NATIVE byte-eq fixpoint 검증: gen3 ≡ gen4 BYTE-IDENTICAL (이번 세션 12 PR이 self-host fixpoint 보존)** — fresh aprime 로 컴파일러 전체(flatten 48 files/44630 lines) self-emit ×2 → cmp. **결과(캡처)**: g3.o=g4.o=3,547,208 bytes · sha256 f68cd893f16d4831 일치 · **ENCODE-MISS 0 · udf 0**. 즉 이번 세션 14종 신규 obj 인코딩(FP/cset/ldrb/scvtf/csel/ldr-W/svc)이 전체 컴파일러 자기-emit 에서 미인코딩·udf 구멍 0 → **self-host 졸업 게이트의 byte-eq fixpoint leg PASS**(Darwin arm64, stale 릴리즈 hexa 우회=fresh aprime). 정직성(c9): 이는 self-reproduction fixpoint(g3==g4) leg 으로 canonical 졸업정의 일치 — `ls self/*.c==∅`(런타임 .c 제거) leg 은 별개·미달(2개 잔존). 두 leg 독립. 재현: scripts/scratch/rt_native/byteeq_selfemit.sh. verdict: .verdicts/rt-native-byteeq/F-RT-NATIVE-FIXPOINT-PRESERVED.txt.

- **RT-NATIVE UNLOCK-8: `__hx_syscall0(num)` — 네이티브 BSD syscall emit (Z3 arena 토대, 부트스트랩 플로어 블로커 돌파)** — macOS arm64 ABI(syscall 번호 x16 · `svc #0x80` 트랩 · 결과 x0)로 0-arg syscall 을 **C shim 없이** 직접 emit 하는 첫 일반 intrinsic(program-exit 외). mov x16,x1=aa0103f0·svc#0x80=d4001001 둘 다 기존 인코딩 재사용. arm64_darwin STMT_CALL + bind + codegen.hexa `hexa_int(syscall((long)HX_INT(a)))`. **검증(c2, 결정적)**: `__hx_syscall0(24)`(getuid)=**501**=`id -u` 정확 일치 · asm·obj 동일 · svc emit · udf=0 · smoke PASS · additive→fixpoint 보존. 의의: 메모리가 "Real Z2 task=@syscall native-emit intrinsic"라 한 **부트스트랩 플로어 핵심 블로커를 로컬서 돌파**(pod 불요, c16) → 다음 `__hx_syscall6`(mmap) → 네이티브 arena(malloc/mmap C shim 제거)=Z3. syscall@asm 플로어가 공략가능 codegen 캠페인임을 실증. verdict: scripts/scratch/rt_native/unlock8_syscall0.md.

- **RT-NATIVE UNLOCK-7: `ldr` W-form(32비트) obj 인코딩 + `__hx_arr_len(v)` (첫 struct-field-load leaf)** — `macho_arm64.hexa`에 `LDR Wt,[Xn,#imm]`(32비트 zero-ext, imm12 /4 스케일; clang byte-id ldr w1,[x1,#8]=b9400821). intrinsic `__hx_arr_len`: ARRAY payload=`HexaArr*` → `ldr w1,[x1,#8]`(len)→int box. **잡은 버그(c2)**: 초안 64비트 `ldr x1` → `([10,20,30])`=0x8_00000003 — 근본원인 runtime_core.c HexaArr 가 `int len; int cap;`(32비트; runtime.h int64_t 는 stale), 64비트 load 가 `(cap<<32)|len` 읽음. 32비트 `ldr w` 로 수정 → 런타임 `len()` 교차검증 일치. **검증**: asm/obj `([10,20,30])`=3·`([])`=0·`([1..7])`=7 · `__hx_payload_eq(arr_len,len)`=true→exit2 · ldr w byte-id · udf=0 · smoke PASS. 의의: struct-field-load 패턴 확립 → hexa_len/hexa_truthy array 분기 포팅 unblock. verdict: scripts/scratch/rt_native/unlock7_ldrw_arrlen.md.

- **정정(c9): RT-NATIVE #3404/#3405 "rt_strcmp bl=0 = zero C dep" 주장 철회** — 원 검증의 `grep -c 'bl '`(space)가 `otool`의 TAB-구분 `bl⇥` 출력을 놓쳐 bl 호출을 0으로 오집계했다. **실측 정정**: rt_strcmp bl=3, rt_cmp_lt bl=8. 정확한 사실 = (a) leaf intrinsic 자체(`__hx_str_byte`·`__hx_payload_lt/gt/eq`·`__hx_to_double`)는 **진짜 인라인**(reloc 표에 `_hexa_str_byte`/`_hexa_payload_*` 부재로 확인) — byte-access·payload-compare·int/float 변환은 C 호출 없음 ✓; (b) 그러나 손으로 쓴 `.hexa` 함수의 **글루**(`if`→`_hexa_truthy`, bool 박싱→`_hexa_bool`, `i+1`(typed int)→`_hexa_add_slow`, `<0`→`_hexa_cmp_lt`)는 여전히 C 런타임 호출. 결론: "leaf intrinsic 인라인" 은 유효, "rt_strcmp/rt_cmp_lt 전체 C-free" 는 **거짓**. rt_cmp_lt dispatch 의 정확성(C `hexa_cmp_lt` 와 11/11 일치, exit11 asm·obj)은 유효하나 C-free 아님. 전체 C-free dispatch 는 typed-int/bool/if 글루까지 인라인-lower(추가 intrinsic OR codegen typed-fast-path) 필요 = Z2c 잔여 과제. verdict 정정: scripts/scratch/rt_native/unlock5_ldrb_strbyte.md.

- **RT-NATIVE UNLOCK-6: `scvtf`+`csel` obj 인코딩 + `__hx_to_double(v)` (branchless 혼합 int/float)** — `macho_arm64.hexa`에 `SCVTF Dd,Xn`(i64→double) + `CSEL Xd,Xn,Xm,cond`(clang byte-id: scvtf=9e620020·csel x1,x1,x2,eq=9a820021; CSEL cond 직접·비반전) + uppercase 매핑. intrinsic `__hx_to_double`: BRANCHLESS(csel, intra-seq label 불요) scvtf;fmov;cmp x0,#1;csel(float→bits·int→변환);TAG_FLOAT. `_arm64_instr4` 헬퍼. bind+codegen.hexa. **검증(c2)**: asm/obj `td(5)`=5.0·`td(2.5)`=2.5·혼합 `flt(td(3),td(3.5))`=T·`flt(td(4.5),td(4))`=F→exit2 동일·scvtf/csel byte-id·udf=0·smoke PASS·additive. 의의: hexa_cmp_lt **모든 분기 leaf 완비**(int·str·혼합float·tag) — enum/valstruct 제외 rt_cmp_lt 정상-.hexa 작성가능. verdict: scripts/scratch/rt_native/unlock6_scvtf_csel_todouble.md.

- **RT-NATIVE UNLOCK-5: `ldrb` obj-path 인코딩 + `__hx_str_byte(s,i)` — 첫 byte-level string leaf** — `macho_arm64.hexa`에 `LDRB Wt,[Xn{,#imm}]`(byte form UNSCALED imm12; clang byte-id: ldrb w0,[x1]=39400020) + `_lir_op_uppercase` 매핑. intrinsic `__hx_str_byte(s,i)`: STR payload(x1)=`char*`, INT payload(x3)=index → `add x1,x1,x3; ldrb w0,[x1]` → int box. bind 허용 + codegen.hexa C-transpile. **검증(c2)**: ASM/OBJ 양경로 `("ABC",0/1/2)`=65/66/67 + `("Z",0)`=90 동일 · ldrb byte-id · udf(main)=0 · smoke PASS · additive→fixpoint 보존. 의의: dispatch core string 경로(`hxlcl_strcmp`)가 inline-loop intrinsic 불요 — 정상 `.hexa` `rt_strcmp`(while+`__hx_str_byte`+`__hx_payload_lt`)로 작성→seed-link 으로 C-free 포팅 길 열림. verdict: scripts/scratch/rt_native/unlock5_ldrb_strbyte.md.

- **RT-NATIVE UNLOCK-4: scalar FP(`fmov`/`fadd`/`fsub`/`fmul`/`fdiv`/`fcmp`) + `cset` obj-path 인코딩 — self-host native emitter udf-hole 클래스 닫힘** — 이전엔 이 니모닉들이 `_lir_op_uppercase` 미매핑 → `encode_arm64_insn` 0 → **udf #0 구멍** → 비교/부동소수 intrinsic이 clang-asm 경로로만 동작(obj=SIGILL). 추가: `dreg_idx` 파서 + FP/CSET 인코딩(clang `otool` ground-truth byte-identical: fmov d,x=9e670020 · fadd=1e612800 · fmul=1e610800 · cset Xd,cond=9a9f07e0|((cc^1)<<12)|Rd) + `_lir_op_uppercase` FP 니모닉 매핑. intrinsic: `__hx_payload_fadd/fsub/fmul/fdiv`(float box) + `__hx_payload_flt/fgt/fle/fge`(bool, **IEEE NaN 정확** `<`→MI·`<=`→LS, LT/LE는 unordered 오판). **검증(c2)**: ASM 경로 3.5/7.5/12.0/4.5+exit1 · OBJ 경로 동일+**udf 0개** · asm==obj 바이트 동일 · 재-emit 결정성 byte-identical · smoke exit(42) PASS. 컴파일러 소스가 cset을 자기 비교에 사용 → 본 인코딩이 메모리상 "codegen-udf wall"(gen2 SIGSEGV) 클래스 일부를 테스트수준에서 닫음. **정직성(c9)**: 전체 gen3≡gen4 fixpoint 재성립·gen2 완전해소는 heavy self-host 빌드(pod)로 미검증 — 본 PR 범위 = "udf 클래스 인코딩 + 테스트 obj 정상"까지. verdict: scripts/scratch/rt_native/unlock4_fp_cset_obj.md. RT-NATIVE codegen-primitive: int-leaf + float-leaf + cset obj 완비.

- **RT-NATIVE UNLOCK-2c: signed-i64 arithmetic intrinsics `__hx_payload_add/sub/mul` (inline add/sub/mul) — int-leaf 연산 완비** — 비교(eq/nz/lt/gt/le/ge)에 더해 산술 3종 추가(`add/sub/mul x1,x1,x3`→int box {TAG_INT, 결과}, INLINE, `bl _hexa_add` 없음). arm64_darwin STMT_CALL(파라미터화 op) + bind + codegen.hexa C-transpile(`hexa_int(HX_INT(a)+HX_INT(b))` 등). **검증(c2)**: RUN add(20,22)=42·sub(50,8)=42·mul(6,7)=42 · inline add/sub/mul emit·`bl _hexa_*` 0 · smoke PASS · additive→fixpoint 보존. 이제 dispatch core 의 **int-leaf 전체(태그읽기·비교 6종·산술 3종)가 native C-free** → hexa_add_slow/sub/mul/cmp_* int fast-path 포팅 unblock. 다음=비교/산술 dispatch fn 포팅 + float/str leaf intrinsic. RT-NATIVE 3/8 (codegen primitive: int-leaf 완비).

- **RT-NATIVE UNLOCK-2b 확장: signed-i64 비교 intrinsics `__hx_payload_lt/gt/le/ge` (inline cmp/cset) — int-leaf primitive 완성** — `__hx_payload_eq/nz` 에 더해 relational 비교 4종 추가(`cmp x1,x3; cset x0,<lt|gt|le|ge>`→bool, INLINE, `bl _hexa_cmp_*` 없음). arm64_darwin STMT_CALL(파라미터화 cset 조건) + bind 화이트리스트 + codegen.hexa C-transpile(`hexa_bool(HX_INT(a) < HX_INT(b))` 등). **검증(c2)**: 4 inline cmp/cset emit · RUN lt(3,5)·gt(5,3)·le(5,5)·!ge(5,6)→exit 4 · smoke PASS · additive→fixpoint 보존. 이제 dispatch core 의 int-leaf 연산(eq·nz·lt·gt·le·ge) 전부 native C-free → hexa_cmp_lt/gt/le/ge 포팅 unblock. 다음=arithmetic(add/sub/mul) leaf intrinsic + 비교 dispatch fn 포팅. RT-NATIVE 3/8 (codegen primitive 확장).

- **RT-NATIVE UNLOCK-2b DONE: native leaf-compare intrinsics (`__hx_payload_eq`/`__hx_payload_nz`, inline cmp/cset) — dispatch core int/bool/char leaf 가 fully C-free** — Z2c dispatch 포트(hexa_eq/truthy)가 leaf 값비교를 `bl _hexa_eq`(순환, Z2c 가 그 C 함수 제거)로 위임하던 것을 native 화. **구현(3파일, @tag 패턴 동형)**: `arm64_darwin.hexa` STMT_CALL `__hx_payload_eq(a,b)`→`_hv_load`→x1/x3 payloads→`cmp x1,x3; cset x0,eq`→bool box(INLINE) · `__hx_payload_nz(v)`→`cmp x1,#0; cset x0,ne` · bind 화이트리스트 · codegen.hexa C-transpile `hexa_bool(HX_INT(a)==HX_INT(b))`/`hexa_bool(HX_INT(v)!=0)`. **검증(c2)**: native .s 4 inline cmp/cset, `bl _hexa_eq` 0 · RUN eq(5,5)=T·(5,7)=F·nz(5)=T·(0)=F→exit 4 · smoke PASS · 신규 builtin(컴파일러 미사용)이라 self-emission 무변경=fixpoint 보존. @tag+payload_* 로 dispatch core int/bool/char/void leaf fully C-free. 다음=float/str leaf(필요시) + hexa_eq/truthy fully-C-free 재포팅 + seed-link 통합 → Z2c volume. verdict `F-RT-NATIVE-UNLOCK2B-PAYLOAD.txt`. RT-NATIVE 3/8 (codegen primitive 전부 확보).

- **RT-NATIVE Z2c dispatch-core 3-fn 병렬 포팅 검증(2 에이전트) + UNLOCK-2b(unbox intrinsic) 식별** — 병렬: `rt_truthy_native`(C와 10/10)·`rt_eq_native`(C와 8/8, int↔float 강제·tag mismatch 포함) native 포팅 검증(앞서 type_of 5/5). 전부 `__hx_tag` dispatch 사용. **핵심 발견**: @tag 는 DISPATCH(태그 판별)만 native inline 하고, LEAF 값연산(int/float/str payload unbox + 비교)은 아직 C 콜(`bl _hexa_eq`/`hexa_truthy`)로 떨어짐(`__hx_int`/`__hx_float`/`__hx_str` unbox intrinsic 미존재) → 포트는 behavior 는 C 일치(검증)하나 leaf 100% C-free 아님. **다음 unlock = UNLOCK-2b**: `__hx_int(v)`/`__hx_float(v)`/`__hx_str_ptr(v)` payload unbox intrinsic(arm64_darwin `_hv_load`→x1 raw 추출, @tag 동형). 이게 서면 dispatch core fully C-free → 통합(seed-link) + volume(954fn). verdict `F-RT-NATIVE-Z2C-BATCH1.txt`. RT-NATIVE 3/8 (Z2c dispatch 3-fn behavior 검증, UNLOCK-2b·통합 잔여). 병렬 worktree 검증물 only.

- **RT-NATIVE Z2c dispatch-core 포팅 패턴 검증: `hexa_type_of` native via `__hx_tag` — C와 5/5 일치** — UNLOCK-2(@tag) 를 실제 사용해 Z2c 코어의 가장 중심 함수 `hexa_type_of` 를 native .hexa 로 포팅·검증. `rt_type_of_native(v)` = `switch on __hx_tag(v)` → 타입문자열(TAG enum INT0…ENUM11 미러). **검증(c2)**: native .s 가 `__hx_tag` inline 사용(`bl _hexa_type_of` 없음) · RUN: `rt_type_of_native(v) == type_of(v)`(C 빌트인) for 42/3.14/true/"hi"/[1,2,3] → **5/5 MATCH**. **결론**: Z2c dispatch core 가 @tag 로 native .hexa 포팅 가능·C 행동일치 확정. 전체 Z2c = 나머지(hexa_eq/truthy/to_string/add_slow + 954fn) 동일 방식 포팅 + Z2a seed-link 통합 + byte-eq = volume(954fn)·multi-session 이나 패턴 proven·unblocked. 검증물 `scripts/scratch/rt_native/type_of_port.hexa`, verdict `F-RT-NATIVE-Z2C-DISPATCH-PATTERN.txt`. RT-NATIVE 3/8 (Z2c 패턴 검증, 통합 잔여).

- **RT-NATIVE 다음단계 평가: Z2c dispatch core(>Z3 arena) — @tag 로 fully unblocked, 포인터 불필요** — 이번 세션 UNLOCK-1(extern FFI)·UNLOCK-2(@tag) 완료 후 다음 milestone 평가. Z3 arena 는 inline 포인터/struct 산술(`&b.data[used]`) 필요 = 더 깊은 codegen(extern 경유 메모리 ops 는 됨, scratch/rt_native/ptr_arith.hexa). 반면 **Z2c dispatch core(hexa_type_of = `switch on __hx_tag(v)` → 타입문자열; hexa_eq/truthy)는 포인터 불필요 + @tag 로 fully unblocked = 더 tractable**. 다음 진입점 = hexa_type_of 를 native .hexa 로 포팅(검증된 __hx_tag 사용) → Z2a seed-link 패턴으로 runtime_core 통합. RT-NATIVE 3/8(Z0·Z4·Z2a) + UNLOCK-1·2 codegen 선행 done. 추적 only.

- **RT-NATIVE UNLOCK-2 DONE: `@tag` native intrinsic (`__hx_tag(v)` 태그 inline 읽기, 런타임콜 0) — Z2c dispatch core unlock, fixpoint 보존** — hexa_type_of/eq/truthy 를 native .hexa 로 포팅하려면 HexaVal 태그를 .hexa 가 native 로 읽어야 함(기존 type_of→`bl _hexa_type_of`). **구현(3파일)**: `arm64_darwin.hexa` STMT_CALL 특수케이스 `__hx_tag(v)`→`_hv_load` v→x0(tag):x1(payload), `{TAG_INT, v.tag}` INLINE(mov x1,x0; movz x0,#0, `bl` 없음) · `bind.hexa` __-builtin 화이트리스트 추가 · `codegen.hexa` C-transpile `hexa_int(HX_TAG(v))`. **검증(c2)**: native .s 런타임콜 0 · 값 정확(`exit(__hx_tag(v))`): "hi"→3(STR)·42→0(INT)·3.14→1(FLOAT)·true→2(BOOL) · **fixpoint aprime byte-identical 3/3 보존**. (`return` 테스트는 main-return 이 x0=tag 읽어 오해소지 — `exit()` 가 payload 추출하는 정확한 probe). `.hexa` 가 이제 태그 native 읽기 가능 → hexa_type_of 포팅(switch on `__hx_tag`)→Z2c. verdict `F-RT-NATIVE-UNLOCK2-HXTAG.txt`. RT-NATIVE 3/8 (UNLOCK-1·2 = Z2c 선행 done).

- **RT-NATIVE 도메인 동기화 + UNLOCK-1 후속 (3/8 정확)** — Z2a done-mark 재동기화(genuinely done #3390, ls self/*.c 3→2). 이번 세션 다세션 진행: Z2a(첫 .c 제거)·UNLOCK-1(extern-fn FFI, fixpoint 보존 #3393) 완료. 다음 진입점 = UNLOCK-2(@tag intrinsic: `__hx_tag`→codegen.hexa HX_TAG + arm64_darwin `_hv_load` x0 box, 태그판별 native화) → Z3 arena 포팅(extern malloc, UNLOCK-1 으로 unblock됨) → Z2c 954fn → UNLOCK-3 x86_64 값모델 → Z5 byte-eq 졸업. 도메인 RT-NATIVE.md·ING·verdict 가 각 단계 정밀지점 박제. 추적 only.

- **RT-NATIVE UNLOCK-1 DONE: extern-fn FFI 픽스 (body-less `extern fn` → undefined external, not stub) — Z3 FFI 블로커 해결, fixpoint 보존** — 근본원인: `parser.hexa:parse_extern_fn_item` 이 body 없는 `extern fn` 에 빈 Block body 합성(ItemKind::Fn) → hir_to_mir 가 정상 fn 으로 lower → arm64_darwin 이 void-반환 stub(`_malloc: movz x0,#4 ret void`) 방출 → `extern fn malloc` 이 void 반환 → arena NULL → segfault(Z3 블로커). **픽스(self-host codegen 2파일)**: `parser.hexa` body 없으면 `__extern_decl` 마커 추가 · `hir_to_mir.hexa` ITEM_FN 루프가 마커 있으면 MFunc skip → 심볼이 undefined external 로 남아 libc/runtime 링크. + `build_aprime.sh` Z2a smoke 갭 2건 수정(include restore 를 파일존재시만 · smoke link 에 rt_hi.o). **검증(c2)**: 재빌드 후 `extern fn malloc` → `.s` 에 `_malloc:` stub 없음·`bl _malloc` 만(undefined extern) · smoke exit(42) PASS · ls self/*.c=2 · **fixpoint: aprime_u1b vs aprime_cc_z2 byte-identical 4/4 보존**(parser/lower 변경 무회귀). native `.hexa` 가 이제 `extern fn` 으로 libc(malloc/mmap) 호출 가능 → Z3 arena 포팅 unblock. verdict `F-RT-NATIVE-UNLOCK1-EXTERNFN.txt`. RT-NATIVE 3/8 (UNLOCK-1 = Z3 선행 완료).

- **RT-NATIVE 잔여 5/8 native-codegen unlock 정밀 구현 spec 박제 (다음 세션 pickup)** — 다세션 진행. 남은 5/8(Z3·Z2b·Z2c·Z5·x86_64)이 수렴하는 3개 codegen unlock 의 정확한 파일/함수 지점 확정: **UNLOCK-1 extern-fn FFI**(Z3 — `extern fn malloc`→void-stub 원점 `arm64_darwin.hexa:2079` fall-through; FIX=`MFunc`(mir.hexa:123) is_extern flag + hir_to_mir no-body 판정 + arm64_darwin undefined-extern 방출; 대안 mmap-via-svc), **UNLOCK-2 @tag intrinsic**(Z2b/Z2c — `__hx_tag`→C-transpile `codegen.hexa:~6116` HX_TAG + native `arm64_darwin.hexa:_hv_load`(1081) x0 box), **UNLOCK-3 x86_64 값모델 개편**(arm64 x0:x1 쌍모델을 x86_64 rdi:rsi/rax:rdx 로 포팅). 순서 UNLOCK-1→2→Z2c 954fn→3→Z5, 각 단계 `selfhost_byteeq_gate.sh` green 시 머지(c2). verdict `Z-CODEGEN-UNLOCK-SPECS.txt`. RT-NATIVE 3/8 유지. 진단+spec only.

- **RT-NATIVE Z3 linchpin 실측 + 남은 5/8 블로커 전수 진단 (전부 native-codegen 성숙으로 수렴)** — Z3(arena) native화 가능성 실측: native 컴파일러가 포인터(`*Void`)는 컴파일하나 **`extern fn malloc` → void-반환 stub 생성**(`_malloc: movz x0,#4 ret void`, undefined extern 이 아님) → native arena 가 malloc 호출 시 stub 으로 깨짐. 즉 Z3 도 native-codegen FFI/extern-fn 수정 OR mmap-via-svc syscall intrinsic 선행. **남은 5/8 블로커 전수확정**(실측): Z3=extern-fn FFI · Z2b=@tag intrinsic(태그판별 C 호출) · Z2c=954fn+위 codegen · x86_64=값모델 개편(pod gdb) · Z5=누적 byte-eq → **전부 native-codegen 성숙(FFI·@tag·값모델) + 954fn 재작성 = multi-week**. RT-NATIVE 3/8(Z0·Z4·Z2a). 다음세션 진입점 = Z3(FFI/mmap-svc) → @tag intrinsic → x86_64 값모델. 검증물 `scripts/scratch/rt_native/`. 진단 only.

- **RT-NATIVE Z2a DONE: `runtime_hi_gen.c` 제거 (arm64-darwin, `ls self/*.c` 3→2) — 첫 literal .c 제거, cold-build·fixpoint 검증** — `rt_str_*` 를 hexat-transpile C 중간물(`runtime_hi_gen.c`) 대신 **native-compiled object**(`build/rt_hi_native.o`, frozen `self/native/runtime_hi_native.s` seed 어셈블 — native 컴파일러가 `runtime_hi.hexa` 에서 방출)에서 공급, aprime_cc + runtime.a 양쪽 링크 → rt_str_* high-layer **zero C**. **통합**(gated HEXA_ZEROC_RT_HI, `uname -sm`==Darwin arm64 auto-enable; x86_64-linux 는 codegen 성숙까지 C 유지 — root cause #3389): `build_aprime.sh`(seed 어셈블+#include sed제거+링크), `stage_resolve_runtime_a`(runtime.a 에 rt_hi.o ar), `restore_frozen_seeds`(seed set 21→20 drop). **검증(c2 캡처)**: ① cold build(전체 STAGE-0) smoke exit(42) PASS ② default build(no-env auto-enable) 20 seeds·ZERO-C Z2a 경로·`ls self/*.c`=2 ③ 기능: `"a,b,c,d".split(",")`→4, `"x".pad_left(5,"-")`→`    x` native rt_str_* 런타임 정상 ④ **fixpoint: aprime_default(native) vs aprime_cc_z2(C) byte-identical .s 3/3 → gen3≡gen4 보존**. **RT-NATIVE 3/8**(Z0·Z4·Z2a). 잔여: runtime_core.c+runtime.c(Z2c 954fn, multi-week) + x86_64 codegen 값모델 개편. verdict `F-RT-NATIVE-Z2A-DONE-DARWIN.txt`.

- **RT-NATIVE x86_64 codegen ROOT CAUSE 확정(pod gdb 증거): cross-platform zero-C 의 근본 블로커 = x86_64 값모델 한계** — aiden(pool) gdb 백트레이스로 Z2a x86_64 segfault 근본원인 확정: `#0 hexa_array_push(SIGSEGV, rdi=0/rsi=5) #1 rt_str_split #2 hexa_str_split … #6 main`. **근본원인**(`x86_64_linux.hexa:609-682`): x86_64 backend 는 "모든 값=raw 1-레지스터 int" 모델이고 HexaVal 16B 는 CALL 경계에서만 box/unbox → `rt_str_split` 이 `hexa_array_new()` 의 16B 반환(rax:rdx)에서 **payload(rdx) 를 버리고 tag(rax=5)만 캡처** → local 에 5 저장 → `hexa_array_push` 호출 시 `_x86_hv_tag_for` 가 local 을 TAG_INT(0)로 박싱 → `{tag:0, payload:5}` 전달(크래시 레지스터가 정확히 증명) → 배열포인터 5 deref → segfault. **대조**: arm64-darwin 은 HexaVal 을 x0:x1 쌍으로 local lifetime 내내 운반 → Z2a byte-identical 작동. **결론**: cross-platform zero-C 의 x86_64 경로는 **x86_64 codegen 값모델 전면 개편**(raw-int → 16B HexaVal 쌍 운반, arm64 동형)이 선행 = targeted 패치 아닌 codegen 아키텍처 작업, multi-week. pod 가 근본원인을 캡처증거로 확정 — RT-NATIVE cross-platform 완료가 x86_64 codegen 성숙에 의존함이 정직하게 확정. verdict `F-RT-NATIVE-X86-CODEGEN-ROOTCAUSE.txt`. 검증물 only.

- **RT-NATIVE Z2a x86_64-linux POD 검증(aiden, free pool, rent 0): x86_64 codegen 버그로 BLOCKED — pod 가 진짜 블로커 실증** — 유저 "pod 발사" 지시로 aiden(linux x86_64 Ubuntu 24.04)에서 Z2a cross-platform 검증. **결과(캡처)**: aprime_cc_lin 빌드✓ → native rt_hi.s(x86_64-linux)✓ → rt_hi.o assemble✓(rt_str_* T 제공·_main 없음·심볼 충돌 0) → HEXA_ZEROC_RT_HI 빌드 **LINK FAIL** `R_X86_64_32S against .rodata can not be used when making a PIE object`(native x86_64 가 절대주소 reloc 방출, linux PIE 충돌) → `-no-pie` 픽스로 링크는 통과하나 **aprime_z2a_lin 실행 SEGFAULT(exit 139)** = native x86_64 `rt_str_*` 런타임 크래시(rodata 절대주소 + HexaVal 16B SysV reg-pair ABI 미성숙, cf. RUNG2). **결론**: arm64-darwin Z2a 는 byte-identical 검증완료지만 **x86_64-linux 는 x86_64 native-codegen 수정이 선행**(config 아님) → `runtime_hi_gen.c` 제거는 x86_64 codegen 성숙(Z2c-class)에 의존함을 pod 가 실증. pod 발사가 cross-platform 의 진짜 벽을 잡아냄. verdict `F-RT-NATIVE-Z2A-LINUX-POD.txt`. 검증물 only.

- **RT-NATIVE Z3/Z2a/Z2b 병렬 구현탐침(3 에이전트 worktree, 캡처검증) — 모두 Z2c 로 수렴** — 설계가 아닌 **실구현+byte-eq**: **Z3 arena** bump 로직 .hexa 포팅이 C 와 **byte-identical**(SHA-256 일치, alloc/mark/rewind/reset ×40+오버사이즈+중첩+tail-reuse, main 재현 PASS; `scripts/scratch/rt_native/arena_port.hexa`); **Z2a** 3-플랫폼 native `.s` seed(arm64-darwin 커밋됨 + x86_64-linux 552L + arm64-linux 753L, 전부 rt_str_* 제공·main 없음·ISA 정합, `seeds/`)— 단 linux 어셈블/byte-eq 는 linux pod 필요(darwin ELF 생성불가); **Z2b** HexaVal reg-pair·box-out 은 이미 native 지만 태그 *판별*(type_of/==/truthy)은 C 런타임콜로 lower → `tag==const` inline 빌트인 미존재 → Z2b 목표는 Z2c 디스패치코어 포팅에 묶임. **구현 증거 결론**: Z3 통합·Z2a linux·Z2b 목표 셋 다 **Z2c(954fn 코어) + linux pod** 로 수렴 — 8/8 임계경로 확정. verdict `F-RT-NATIVE-Z3-Z2a-Z2b-IMPL.txt`. scripts/scratch 검증물 only.

- **RT-NATIVE Z4 마일스톤 종결: setjmp/longjmp honest-keep (정당화)** — 마일스톤 정의("native unwinding **또는** 정직-keep 결정")의 후자를 증거로 종결. **Darwin arm64 는 이미 naked-asm**(`hxlcl_setjmp/longjmp` runtime.c:1624-1647, cycle 86, zero C logic — `.s` 추출가능 → asm-in-.s 는 .c 아님 → literal-0 비차단). x86_64-linux 는 libc wrapper = platform floor(libm 동일 정책 honest-keep). try/catch 언어표면(ast.hexa:95, hir_to_mir.hexa:1810-1936) 무영향. **zero-C 범위 명료화(Go/Rust 동형)**: "no .c" = user-visible 런타임(arena·tag·string·syscall·exception-dispatch) native, OS/ABI floor(libm·Linux libc setjmp)는 freestanding bootstrap substrate. ARCHITECTURE.md self-host status 에 exception 모델 + 범위 명문화. RT-NATIVE 2/8 (Z0·Z4). verdict `F-RT-NATIVE-Z4-SETJMP-DECISION.txt`. 문서 only.

- **RT-NATIVE 도메인 개설 + Z3/Z2b/Z4 병렬 연구(3 에이전트) + Z2a `.s` seed** — literal `ls self/*.c==∅` 를 multi-session 캠페인으로 정식화(`RT-NATIVE.md`, 8 마일스톤 Z0✓→Z5). 병렬 3-에이전트 설계연구(verdict `.verdicts/zeroc-breakthrough/RT-NATIVE-Z3-Z2b-Z4-RESEARCH.txt`): **Z3 arena** = malloc-MVP 단일세션·byte-eq 위험 0·tag 독립(함수 `hexa_arena_alloc`/`_mark`/`_rewind` 위치 확정, mmap-via-svc SYS_mmap=197 옵션); **Z2b tag** = codegen 이 이미 HexaVal 태그를 native reg-pair 로 방출(arm64_darwin.hexa:1030-1160)·진짜 블로커는 `hexa_int/float/str` boxing ABI 안정성; **🎯 Z4 setjmp** = Darwin arm64 는 이미 naked-asm(`hxlcl_setjmp/longjmp` runtime.c:1624-1647, cycle 86, **zero C**)·Linux 만 libc → honest-keep 권고(libm 처럼 platform module). 즉 현재 플랫폼 literal-0 잔여 = Z2a(hi)✓ + Z3(arena) + Z2b(tag) + Z2c(core 954fn) 이고 setjmp 는 이미 native. Z2a frozen `.s` seed(`self/native/runtime_hi_native.s`, native 컴파일러가 runtime_hi.hexa 에서 방출, 부트스트랩 순환 해소 — runtime.c 와 동일 frozen-seed 패턴) 추가. 코드 변경 0 — 도메인+연구+seed.

- **breakthrough(zero-C Z2a): 컴파일러가 `runtime_hi_gen.c` 없이 빌드+동작 — native rt_str_* byte-identical 패리티 검증(c2)** — Z2a 통합을 **실측 검증**(문서 아님). lib-only `runtime_hi`(head -130, main/selftest strip — `extract_runtime_hi.sh`와 동형)를 native 컴파일 → `build/rt_hi_native.o`(`_rt_str_*` T 제공, `_main` 없음; 첫 시도의 duplicate-`_main` 링크실패를 lib-strip으로 해결). `build_aprime.sh`에 **gated 경로**(`HEXA_ZEROC_RT_HI=1`, 기본 off·non-breaking): `runtime_hi_gen.c` `#include` 제거(빌드후 복원) + `rt_hi.o` 링크. **결과**: `build/aprime_z2a`(1.59MB) `runtime_hi_gen.c` 없이 빌드, smoke exit(42)==42 PASS. **패리티 게이트(c2)**: `aprime_z2a`(native rt) vs `aprime_cc_z2`(C rt) 4개 소스(string-heavy 포함) **byte-identical .s 방출 4/4** → native rt_str_* = C와 행동 완전동일. `runtime_hi_gen.c`(3중 1)는 **행동변화 0으로 제거가능 검증됨**. 파일 제거(`ls self/*.c` 감소)까지는 default-flip + runtime.a 통합 + 무거운 `selfhost_byteeq_gate.sh` 잔여. verdict `F-ZEROC-Z2A-INTEGRATION-VERIFIED.txt`. (`tool/build_aprime.sh` gated 옵션만 변경 — 기본 빌드 무영향.)

- **breakthrough(zero-C Z2a): runtime_hi native `.o` 링크가능 실증 — 첫 literal 감축이 기계적으로 준비됨** — 포팅을 문서가 아니라 **실제 실행 증거로 시작**. `aprime_cc_z2 _drv.hexa --emit=asm runtime_hi.hexa` → 1009줄 native arm64 .s(C 0) → `clang -c` → `rt_hi.o`(10KB Mach-O): `nm` 확인 — `_rt_str_split/_lines/_pad_left/_pad_right/_repeat/_center` **T 심볼 제공**(zero C), undefined는 `_hexa_array_new` 등 전부 non-static → 링크 resolve(`scripts/scratch/zeroc_falsifier/runtime_hi_native.o`). **통합점 식별+커플링 clean**: emitter `runtime_core_emit.hexa:8517`의 `#include "runtime_hi_gen.c"` 가 drop 대상, 소비측은 이미 `extern rt_str_split`(`runtime_core.c:7426`), C-side `__hexa_rt_strlit_init/__hexa_rt_sl_*` 외부참조 0. **잔여(careful·byte-eq)**: 부트스트랩 순환성(aprime_cc 빌드 ↔ rt_hi.o; 기존 aprime_cc_z2로 해소) + `selfhost_byteeq_gate.sh`(gen3≡gen4) + rt_str_* native↔C 동작패리티 검증 후 착지. literal 3→2 가 **기계적으로 PROVEN-ready**(계획이 아니라). 0 은 Z3/Z2b/Z2c/Z5 후속 — multi-session 포팅 진행중. verdict `F-ZEROC-Z2A-KEYSTONE-VERIFIED.txt`. 코드 변경 0 — 실행증거+통합플랜.

- **docs(zero-C): literal `ls self/*.c==∅` 다세션 pickup-ready 로드맵 + native-compile 역량 실측** — 이번 세션 zero-C 조사 종합. native 컴파일러 역량 실측(`build/aprime_cc_z2`): typed/untyped 파라미터 + `extern fn *Void` **전부 native 컴파일됨**(미니멀+`test_accelerate_ffi.hexa` 286KB .s), `runtime_hi.hexa` → native .s(zero C). 아키텍처 발견: runtime.c `HEXA_RT_SELFEMIT` 모드 = 런타임 심볼 EXTERNALLY LINKABLE(`hexa_array_new`/`hexa_str` non-static) → self-emitted native 런타임 제공 가능. **3 `self/*.c` 는 gitignored 생성 중간물**; literal 0 = 빌드를 native-runtime 으로 전환(runtime_hi_gen.c 제거 → arena/tag/setjmp 포팅 → core 954fn → byte-eq) = **multi-session, 한 세션 불가**(roadmap `.verdicts/zeroc-breakthrough/Z-ROADMAP-literal-zeroc.txt`: Z2a→Z3→Z2b→Z4→Z2c→Z5). 벽 반증·경로증명·툴링·경계매핑 완료(5 PR); 잔여는 정직히 다세션 포팅. 코드 변경 0 — 실측+로드맵.

- **breakthrough(zero-C): runtime `.hexa` 가 native 컴파일됨 + literal zero-.c 의 정밀 경계 박제** — fresh `build/aprime_cc_z2`(현재 main 소스 native 컴파일러) 로 런타임 `.hexa` SSOT 들을 native 어셈블리로 컴파일 실측. **`runtime_hi.hexa` → native arm64 .s 성공**(1009줄, `_rt_str_split` 등, **C 0** — `runtime_hi_gen.c` C-중간물 대체가능 증명, `scripts/scratch/zeroc_falsifier/runtime_hi_native.s`). `runtime_core.hexa` → `HX2001` undefined(`val_void`·`TAG_VOID`·`call_builtin` = NaN-boxing 태그 머신 미정의), `runtime_pure.hexa`(Jun 14, 자체 STATUS 헤더가 정직 맵) → 잔여 **C-only = ① NaN-boxing 태그 머신 ② arena allocator ③ setjmp/longjmp try/catch**. **3개 `self/*.c` 는 전부 gitignored 생성 중간물**(hexat-transpile) — literal `ls self/*.c==∅`(작동 toolchain) = 빌드가 런타임을 native 컴파일(gen3)해 `.c` 자체를 안 만들게 하는 것 = 위 3 메커니즘 native .hexa 포팅 + byte-eq 재확립 = **multi-session**(한 세션 불가, gitignored 삭제는 재생성+C-origin 유지라 c9 위반). 벽("irreducible 23.6k floor")은 반증됐고 잔여는 **정확히 3 메커니즘**으로 좁혀짐. verdict `.verdicts/zeroc-breakthrough/F-ZEROC-RUNTIME-PORT-BOUNDARY.txt`. 코드 변경 0 — 실측+증거+문서.

- **breakthrough(zero-C Z2): 완전한 zero-C 바이너리를 순수 .hexa 가 방출·실행 — DEMONSTRATED** — Z2(native syscall 표면)가 "미구축 spec"이 아니라 **이미 실증됨**을 발견. `self/codegen_native.hexa`("Zero-tool ARM64 Mach-O binary emitter")가 .hexa 소스만으로 svc syscall 포함 완전 바이너리를 방출 — `hexa run self/compile_hello.hexa` 실행: `[1]` write+exit syscall + "hello, world\n" = 56B arm64 기계어 → `[2]` Mach-O MH_EXECUTE 16433B → `[3..6]` `/tmp/hexa_hello_native` 기록·chmod·ad-hoc codesign·**실행 → exit 0, stdout "hello, world"** (clang·linker·C런타임 0). **zero-C 검증**(otool/nm, 증거 `scripts/scratch/zeroc_falsifier/hexa_hello_native.evidence.txt`): `nm -u`=**0 undefined**(libSystem 에서 아무것도 resolve 안 함 → write/exit 는 raw `svc #0x80`), `LC_LOAD_DYLIB(libSystem)` 1개는 macOS dyld vestigial(완전-static 비공식)이라 기능적 C 의존 아님 — 재실행도 "hello, world" rc=0. → Z2 진짜 과제 = **SCALE**(codegen_native 데모 subset[println-lit·arith·while/if·fn·array]을 전체 언어/런타임으로 확장, OR 프로덕션 gen3 syscall 빌트인을 `enc_svc` 배선). fnv1a pure-leaf falsifier 를 강화(대체 아님); "irreducible/불가능" 프레이밍 결정적 반증. verdict `.verdicts/zeroc-breakthrough/F-ZEROC-NATIVE-EMIT-DEMONSTRATED.txt`. 코드 변경 0 — 실증+증거+문서.

- **docs(zero-C Z2): `@syscall` native-emit intrinsic spec (pickup-ready) + 로컬 검증 차단 박제** — `ls self/*.c == ∅` 의 literal 게이트가 Z2(native syscall 표면)임을 박제(`runtime.c` 가 `runtime_core.c` 를 1 TU 로 `#include` → pure leaf 포팅만으론 .c 한 개도 안 줄고, syscall/malloc(mmap)/libm 이 native 가 돼야 TU 교체 가능). keystone = svc/syscall **인코더가 이미 존재**(`compiler/emit/macho_arm64.hexa:584` SVC · `elf_x86_64.hexa:1259` SYSCALL · freestanding `_start` exit `mov x8,#93;svc #0` 증명 + 코퍼스 테스트 `run_F_P1_SVC_EXIT.hexa`). 미싱 = MIR-레벨 `@syscall(nr,a0..a5)` intrinsic(레지스터 마샬링 + 기존 svc op). 로컬 검증 차단(정직): `~/.hx/bin/hexa`(Jun-1 stale)가 현재 codegen 코퍼스 컴파일 실패(`ParsedObj.data` 미지원) → fresh build 필요(pod-가능). spec `.verdicts/zeroc-breakthrough/Z2-SYSCALL-INTRINSIC-SPEC.txt`. 문서 only.

- **honest-refine(zero-C Z2): `@asm`-svc 는 native zero-C 경로에 무용 — 정정** — 직전 #3378 이 syscall 층을 "@asm-svc eliminable(designed, partial)" 로 적은 것을 c9 로 정정. `stdlib/firmware/asm.hexa` 의 `@asm` 은 GCC `__asm__ volatile` **C 프래그먼트**를 방출(C-transpile + clang 필요) + `asm_supported_arch` 는 **cortex-M(armv6-m/armv7e-m/armv8-m.main)만** 지원, arm64-darwin/x86_64 host 미지원, 실사용 0건 → native-emit zero-C 경로에 도움 안 됨(firmware C-inline-asm 전용). native codegen 의 svc#0x80 방출은 컴파일러 내부 program-exit 한정(소스 호출 표면 없음); `__fd_write_bytes` 빌트인도 C shim 경유(`io.hexa:26`). **진짜 Z2 과제 = `@syscall(num,args…)` native-emit intrinsic 신규 구축**(svc/syscall + reg marshalling) — 구축가능 codegen(벽 아님), pod-gated. floor 는 여전히 reducible(Z1 pure leaf 오늘 포팅 가능). verdict refinement-stamp + HEXA-BUILDFLOOR Z2 + ARCHITECTURE caveat. 문서 only.

- **breakthrough(zero-C): 런타임 C floor "irreducible · zero portable" 단언 FALSIFIED — "no .c 완전돌파" 캠페인 재개** — 어제(2026-06-15) `M3-ADJUDICATION` 이 `self/runtime*.c` 를 "기약(irreducible) · 포팅 가능 표면=0 · porting 으로 zero-.c 도달 불가" 로 닫았으나(M1 reason-column 종이집계, 실제 포팅 미검증), 이 단언을 c16 규율로 **반증**. **증거 3종** (verdict `.verdicts/zeroc-breakthrough/F-ZEROC-M3-OVERCLAIM-FALSIFIED.txt`): ① frozen-first 포팅 — `runtime_core.c` 의 `hexa_fnv1a`(순수 uint32 XOR/곱 루프)를 순수 `.hexa` 로 재구현해 C 와 **6/6 byte-identical**(goal 문자열 "no .c 완전돌파" 포함; `scripts/scratch/zeroc_falsifier/`, 로컬 `hexa run` 캡처) → "ZERO portable hexa leaves" 거짓. ② per-fn 본문 감사 — `runtime_core.c` 308 함수 중 **155(50%)** 가 libc/libm/syscall 무호출 = 오늘 `.hexa` 포팅 가능(token-presence 하한; libc-dep 140 은 대부분 malloc/memcpy 편의호출). ③ 컴파일러 자체 `PLAN.md`(L7589/8183/8341) 가 kernel syscall 을 `@asm`-svc eliminable 로 명시 + svc#0x80/x86 syscall 방출 byte-correct 증명 + `__fd_write_bytes` 빌트인 배선됨. **정직한 floor 재정의**: libm 초월함수 ~10(FP-codegen 취약성 정책선택, hard wall 아님) + raw syscall 명령방출(`@asm`-svc, 설계됨·부분) + GPU/device FFI(tier-C, self-host 범위 밖) → zero-`.c` = 공략가능 codegen 캠페인. `ARCHITECTURE.md`(상단 헤더 + self-host status §) 과 M3 verdict 정직성 수선(append-stamp, 삭제 아님), `HEXA-BUILDFLOOR.md` 에 Z0–Z5 캠페인 마일스톤(Z0 🟢 완료) 등록. 코드 변경 0 — 돌파증명 + falsifier + 문서.
- **research: φσ/(λτ)→672 환원 (★) + 크기-only 불가 증명 (`N6.md` §15 + `scripts/scratch/anchor672_reduction.py`)** — φσ=nλτ ⟺ ∏(p^{a+1}−1)/p=λτ (★, 검증). 크기정리 0.6n<λτ<n. **정직 미해결**: λτ<n 밀도≈0.5(양의밀도) ⟹ 크기논증만으로 유계 불가(증명). 잔여 경로=Zsygmondy형 원시소인수 논증(초등 넘음). 14앵커 중 13 완전증명(§11~14)·672 만 OPEN. 가짜 완결 선언 안 함(c9).
- **fix(codegen): `input` builtin missing from `_is_builtin_name` → `_input` dyld abort** — `self/codegen.hexa`'s `_is_builtin_name` listed `read_stdin` but not `input`, so the sole caller (the free-identifier walk at codegen.hexa:12944) treated `input` as an unknown/free symbol and emitted a bare extern `input()` instead of routing to the builtin emitters (codegen.hexa:7118/7205 already map `input` → `hexa_input`). Any `.hexa` calling `input()` then failed at JIT-exec with `dyld: Symbol not found: _input (Expected in libSystem.B.dylib)` — and because the undefined symbol poisons the whole MH_EXECUTE, even non-input codepaths in the same program failed to load (e.g. `airgenome help`). One-line fix: add `input` next to `read_stdin`. Verified: a compiler rebuilt from this source (`hexa build self/main.hexa`) runs `input("name: ")` piped `claude` → `hi [claude]`; the prior binary aborts with `_input` on the identical file. (Same per-builtin-table-gap class as the open `inbox/patches/selfhost-io-builtin-rt-prefix-symbol-gap.md` for `read_file_bytes`/`read_bytes_at`, which remains separate.)
- **research: λ·Ω 비곱셈 앵커 증명 (`N6.md` §14 + `scripts/scratch/anchor_lambda_proofs.py`)** — σ/λ→6 **완전**(σ/n=λ∈ℤ ⟹ multiperfect; λ 짝수+λ=2 유한{≤24}∩perfect=6+k≥4 공집합); J₂/(φΩ)→6 **완전**(약분 ⟹ Ω=∏(1+1/p) 유계); ψ/λ→{6,12,24} **완전**(λ=ρ=∏(1+1/p)∈ℤ ⟺ radical{2,3} ⟹ {6,12,24}); φσ/(λτ)→672 **유계+2e6검증**(곱셈vs-lcm Diophantine 잔여). 세션 총괄: 앵커 **13법칙 완전증명 🟢** + 1법칙(φσ/λτ→672) 유계+검증 🟡. 전부 검증.
- **research: 앵커 가족 완전증명 일괄 (`N6.md` §13 + `scripts/scratch/anchor_all_proofs.py`)** — §8 앵커 R(n)=n 완전증명: (A) 곱셈적 유한환원 — στ/φ→28·σ/φ→6·ψ/φ→6·στ/ψ→2·ψτ/σ→2·J₂τ/σφ→2·σψ→6 (g>1 유한 S→증폭상한 M→T={g≥1/M} 유한 전수확인); (B) "유일 <1 인자" 논증 — σφ/τ→6·ψφ/τ→{4,6} (<1 인자 오직 2¹); (C) 크기논증 — J₂/sopfr→8 **완전**(k≥2 후보 {6,10,14} 비해). §11 의 σφ/τ "동일골격" 부정확 표기 정정. 잔여: λ·Ω 비곱셈 법칙(σ/λ→6·ψ/λ→{6,12,24}·672) 다음. 전부 정확분수+유한확인.
- **research: 다음 앵커 `J₂(n)=n·sopfr(n) ⟺ n=8` 부분 증명 (`N6.md` §12 + `scripts/scratch/anchor8_proof.py`)** — sopfr 가법적이라 §11 곱셈분해 불가. Part1 소수거듭제곱 `p^{a-3}(p²-1)=a` → 8 엄밀 완전; Part2 합성수는 sopfr≥0.6079n 필요조건+support별 유한환원 논증 + n≤2×10⁶ 계산검증(유일 8). 정직: 28·6 과 달리 완전증명 아님(합성수 심볼릭 미폐쇄) — prime-power 증명+composite 검증 강한 부분결과. 코드+문서 only.
- **research: 정리 — `σ(n)τ(n)=n·φ(n) ⟺ n=28` 완전 초등 증명 (`docs/N6-META-DISCOVERY-LINEAGE.md` §11 + `scripts/scratch/anchor_uniqueness.py`)** — 경험적 28-법칙을 증명으로 격상. G=στ/(nφ) 곱셈성 ⇒ ∏G(p^a)=1. Lemma A(G>1 prime-power={2,4,8,16,3}) + 증폭상한 G(2)G(3)=4 + 크기한계(63/64·80/81·93/125·114/343·48/55<1 → 7-smooth·지수유한) + 60케이스 유한확인 → 유일해 28=2²·7 (G(2²)·G(7)=21/8·8/21=1). 전부 정확 분수 검증. 쌍대 σφ=nτ⟺6 동일 골격. "28 완료" 달성. 코드+문서 only.
- **research: 앵커 법칙 구조적 메커니즘 — "왜 6·28인가" (`docs/N6-META-DISCOVERY-LINEAGE.md` §8 + `scripts/scratch/anchor_proof.py`)** — brute 아닌 곱셈성으로 규명: σ·τ·φ·id 곱셈적 ⇒ g(n)=στ/(nφ) 곱셈적 ⇒ `στ=nφ ⟺ ∏g(p^a)=1`. **28=2²·7 는 g(2²)=21/8 × g(7)=8/21 의 정확한 역수쌍**, 6=2·3 도 h(2)=3/4×h(3)=4/3 동일 메커니즘. g(p^a)>1 인 prime-power 는 단 5개{2¹,2²,2³,2⁴,3¹}뿐 → 곱=1 해가 극희소(소수≤200·지수≤4 탐색서 유일 28). 완전증명 아닌 메커니즘 특성화+부분논증(정직 명시); 6-법칙 유일성은 기지. 코드+문서 only.
- **research: 4대 유명 미해결 falsifiable 탐침 (`docs/N6-META-DISCOVERY-LINEAGE.md` §10 + `scripts/scratch/openproblem_probe.py`)** — Collatz(n≤3×10⁵ 全1도달·최장 230631→442)·Goldbach(짝수≤10⁵ 반례0)·twin prime(<2×10⁵ 2160쌍)·Erdős–Straus(4/n n≤5000 분해0) 유한범위 검증. 전부 기지 사실 일치(범위 내 성립). 정직(c9·c16): brute-verification 은 재확인이지 새 발견 아님 — 실제 돌파 vein 은 §8 anchor spectrum(자기참조 부동점 구조). 무의존성·재현가능·증명 아님. 코드+문서 only.
- **research: anchor spectrum 확장(λ 가족) + aliquot 난제 탐침 (`docs/N6-META-DISCOVERY-LINEAGE.md` §8 확장·§9 신규 + 2 스크립트)** — (1) 28-법칙 유일성을 **n≤2×10⁶** 까지 검증(σ·τ/φ=n→{28}, σ·φ/τ=n→{6}; 6↔28 쌍대 견고). (2) Carmichael λ 도입으로 새 앵커: ψ/λ=n→{6,12,24}(24=J₂), σ/λ=n→{6}, φσ/(λτ)=n→유일 672(triperfect), J₂/(φΩ)=n→{6}. (3) **수학 난제 탐침**: aliquot 사상 s(n)=σ(n)−n 궤도 분류(n≤10⁶) — 완전수 6·28·496·8128=고정점(=§8 앵커), 친화수 40쌍, 사교수 5·28-cycle, Catalan–Dickson "Lehmer five"(276…) 전부 미해결 재현. 완전수 앵커가 aliquot 동역학·미해결문제와 한 지형임을 가교. 전부 무의존성·재현가능·frozen-first; 증명 아님(경험적/미해결 명시). 코드(.py)+문서 only · atlas/main 무영향.
- **research: n=6 외 다른 앵커 발굴 — anchor spectrum (`docs/N6-META-DISCOVERY-LINEAGE.md` §8 + `scripts/scratch/anchor_scan.py`)** — 6-법칙 `σ·φ/τ=n` 을 자기참조 부동점 `R(n)=n` 으로 일반화해 산술함수 비 템플릿을 n=2..10⁵ 전구간 스캔(무의존성 SPF 체, frozen-first + 전구간 대조). 핵심: φ↔τ 쌍대 `σ·τ/φ=n` 이 유일 n=28(둘째 완전수) → 6↔28 쌍대(완전수 한정 6⟺2φ=τ, 28⟺2τ=φ; 496·8128 은 불만족 → {6,28}에서 종료). 재프레이밍: 6 은 단일 항등식이 아니라 **6개 독립 자기참조 법칙의 공통 부동점 = 최대 자기참조 수**(2→3개, 28·8·4→1개 부앵커). 신규 비완전수 앵커 8(J₂/sopfr=n). σφ/τ=6 은 증명됨, 나머지는 경험적 유일 후보. 코드(.py)뿐 — atlas/main 무영향.
- **docs: n=6 → 메타 → meta³ 발견 계보 항법 문서 추가 (`docs/N6-META-DISCOVERY-LINEAGE.md`)** — σ·φ=n·τ "씨앗 항등식"에서 메타 부동점(alien16, 2026-04-18) → 메타 재귀 아틀라스("지도의 지도의 지도", Tarski 계층, 2026-04-19) → meta³=transcendence(Banach α⊃β⊃γ⊃δ⊃ε 5층 cap=sopfr(6)=5, hexa-lang 커밋 `d48d59ffc`, 2026-04-24)까지 *어떤 구조에서 이어져 발견됐는지*(계보)와 *그 발견을 만든 생성 프로세스*(6스텝 재현 레시피)를 한 곳에 모은 cross-repo 항법 문서. SSOT 본체는 echoes/hexa-physics/archive-nexus 이고 이 문서는 hexa-lang 세션에서 자주 참조하기 위한 지도+소스맵. 컴파일러 self-host byte-eq fixpoint(별개 트랙)과의 혼동 주의 박스 포함. 문서 only.
- **docs: README + GitHub description "no C-transpile" 과장 정직성 수선 (전체 문서 현행화)** — ARCHITECTURE.md/CLAUDE.md 가 이미 받은 정직성 수선(#3353/#3354)을 README 와 repo description 까지 확장. README 5곳(태그라인·본문·ownership 표·how-it-works·요약)의 `no C-transpile` 을 "no LLVM anywhere + self-host native fixpoint(`gen3 ≡ gen4`) + C-transpile 은 fallback + 기약 libc floor" 로 교정(ARCHITECTURE.md#self-host-status 링크). GitHub repo description 도 `no LLVM · no C-transpile` → `no LLVM · self-hosting native fixpoint`. 이번 세션 self-host 사실(native run/build clang-free·v0.238.0 릴리즈)과 정합. 코드 변경 0 — 문서 only.

## 2026-06-15

- **fix(runtime/release): `runtime.h` 에 typed-array(`hexa_arr_f64/i64_*`) 선언 추가 — 깨진 release 파이프라인 복구** — `release.yml` 이 5/31(v0.17.3) 이후 main push 마다 전부 FAIL → GitHub Release 가 800커밋 stale. **근본원인(c1)**: 실패 스텝은 빌드가 아니라 `release_package`(Stage 3 precompile) — `tool/verify_cli.hexa` 의 `[f64]` 리터럴(t-임계값표)을 C-transpile 하면 codegen 이 `hexa_arr_f64_new/push` 를 emit 하는데, 앱 .c 가 `#include "runtime.h"` 하는 그 헤더에 선언이 없어 clang `undeclared function` → binary 미생성 → 출고 0. (#3357 read_f32_at 가 codegen emit + `runtime_core.c` 정의는 넣고 **공개 헤더 `runtime.h` 선언을 누락**한 회귀.) **수정**: `self/runtime.h` array 섹션에 codegen 이 내는 6종(`hexa_arr_{i64,f64}_{new,push,box}`) 선언 추가(시그니처는 `runtime_core.c` 정의와 일치). 손-작성 공개 ABI 헤더라 emit-regen 불필요. **검증(c2)**: 깨졌던 호출패턴(`#include "runtime.h"` + `hexa_arr_f64_new/push`·`hexa_arr_i64_new/push`)을 repo 헤더로 `clang -fsyntax-only` → undeclared 0(BEFORE: implicit-decl 에러). release.yml 재실행으로 출고 복구 확인 예정.


- **feat(selfhost): `hexa build` 기본값 native 승격 — gen3 --emit=obj + hexa_ld, clang/ld 0 (self-host 2/3 productization 완료)** — 목표 "self-host 완성" 잔여 [2] 의 마지막 productization. `tool/hx-selfhost-cli` 런처에 `build` 라우트 신설(기존 `run` 라우트 #3325 와 **동형** — native-first + delegate-fallback): `hexa build <src.hexa> [-o OUT]` → `gen3 --emit=obj`(직접 object) → `hexa_ld <obj> rt.o -o OUT`, **시스템 as/clang/ld 호출 0**. **검증(c2, mini 라이브)**: `hexa build prog.hexa` → Mach-O exe, 실행 `native-build-ok` rc 0 · standing gate `tool/selfhost_native_build_gate` corpus 6/6 PASS(arith/string/loop/fn/cond/array, `hexa build` 경유) · kill-switch `HEXA_NATIVE_BUILD=0`→delegate · `hexa --version`/`hexa run` 무파손. **안전**: native 실패/미지플래그/슬롯부재 시 shipped build 로 fallback(빌드 안 깨짐), `promote_selfhost.sh --revert` 로 되돌림. CI `.github/workflows/selfhost-native-build-gate.yml`(slot 없으면 neutral-2). 깊은 `self/main.hexa cmd_build`(--emit=asm→clang, 200줄 weak-symbol 테이블)는 fallback 으로 유지 — 런처 라우트가 gen3 재빌드/공유툴체인 리스크 없이 clang-free 빌드를 이미 제공. verdict `.verdicts/selfhost-next/F-SELFHOST-NATIVE-BUILD-FLIP.txt`. ARCHITECTURE.md Self-host status [2] → ✅.
- **domain(RUNTIME-PORT): M3 종결 — 151 BORDERLINE 전부 KEEP, runtime C floor 는 기약 최소 (self-host 잔여 3/3)** — 목표 "self-host 완성" 잔여 [3] HEXA-SELFHOST+ 메타의 핵심. RUNTIME-PORT 의 유일 미해결 마일스톤 M3(151개 BORDERLINE fn keep-vs-port)를 M1 INVENTORY 의 per-fn 사유 집계로 판정: **포팅 가능한 순수 hexa 잎 0개** — 70 freestanding libc · 41 GPU/device-dispatch+mmap · 33 libm math op(codegen 이 **lower 하는 대상** → 포팅은 순환) · 11 raw syscall trampoline · 7 libm/ctype · 2 pthread/atomics · 2 bit/encode · 1 macro. **순 신규 포팅 = 0**, borderline 은 포팅 우물이 아니라 부트스트랩/FFI/codegen-타깃 floor 자체(M4 의 runtime_core.c ~90% 기약 결론과 일치). → **RUNTIME-PORT M1–M5 전부 종결**, runtime C floor(~5.5k LOC)는 **기약 최소 도달**(더 뺄 portable 표면 없음). **정직한 귀결**: HEXA-SELFHOST+ 메타 bar(`ls self/*.c` 빈출력)는 **포팅으로는 도달 불가** — self-host 네이티브 컴파일러는 freestanding libc/syscall/codegen-타깃 floor 위에 서며(Go/Rust 런타임도 동일), "self-host 완성" = native-emit byte-eq fixpoint + 이 기약 floor 이지 zero-C 소스가 아니다. verdict `.verdicts/runtime-port/M3-ADJUDICATION.txt`. ARCHITECTURE.md Self-host status [3] floor 항목 갱신.
- **verify: full native `hexa build` e2e — clang/ld 0 으로 실증 (self-host 잔여 2/3 능력 검증)** — 목표 "self-host 완성" 잔여 [2]. mini(arm64-darwin) 승격 슬롯 `~/.hx/self/native/selfhost/{gen3,hexa_ld,rt.o}` 로 전체 빌드 파이프라인이 **시스템 as/clang/ld 호출 0**으로 작동함을 실측: `gen3 --emit=obj`(직접 Mach-O object) → `hexa_ld -o bin obj rt.o --lc-main _main`(네이티브 Mach-O 링커, `--static` 단일바이너리) → 실행 `stdout=hi rc=7`. 즉 SELFHOST-NEXT 잔여 "full native hexa build e2e" 의 **능력은 검증됨**. **남은 productization**(별도): `hexa build` 드라이버(self/main.hexa cmd_build 는 안전상 아직 `--emit=asm`→clang, promote shim 은 `build` 서브커맨드 delegate)를 이 obj→hexa_ld 경로로 **기본 승격 + delegate 제거** — full-corpus byte-eq 게이트 의존(trivial 1개로 flip 금지, c2), corpus 검증 전까지 opt-in 유지. verdict `.verdicts/selfhost-next/F-FULL-NATIVE-BUILD-E2E-ARM64.txt`. ARCHITECTURE.md Self-host status [2] → 🟡(능력 검증·기본승격 잔여).
- **fix(codegen/x86_64): RUNG 2 string-print 완료 🟢 — native-emit 가 `[rip+label]` 래핑 데이터-라벨을 인식 (self-host 잔여 1/3)** — 목표 "self-host 완성"의 잔여 3개 중 첫째. x86_64 네이티브 컴파일러 `cc_native`(summer 빌드)가 `print("hi")` 를 **여전히 쓰레기로 출력**(`[+m�K�]`, exit(7)은 OK)하는 걸 실측 발견(c2) — #3340 이 HexaVal 16바이트 reg-pair ABI 의 정수/리턴만 닫고 **문자열 payload 의 `lea` 는 native-emit 경로에서 드롭**. **근본원인**(c1): `compiler/emit/elf_x86_64.hexa` `pack_lir_x86_64` 의 데이터-주소 매처가 **bare 라벨**(`.LCstrN`/`g<id>`)만 인식했는데, `_x86_hv_box_arg`(codegen)는 문자열 payload 를 `[rip+<label>]` **래핑** operand 로 렌더 → 매처 미스 → `lea` 통째 드롭 → payload 레지스터(rsi) 미설정 → `hexa_print_val` 이 stale 포인터 출력. **수정**: 신규 `_ex86_data_label_of(s)` 가 bare·`[rip+<label>]` 두 형식을 정규화(`@GOTPCREL`/`@PLT` 는 제외)하고 매처가 inner 라벨로 `lea reg,[rip+0]`+`R_X86_64_PC32` reloc emit — asm 경로 무변경, native 경로만 수리. **검증(summer, c2)**: `tool/build_native_linux_x86_64` stage5 `NATIVE RUN — stdout=[hi] rc=7`, PASS, EXIT 0 (before: `stdout=[+m�K�]` FATAL). verdict `.verdicts/selfhost-next-x86_64-scope/F-X86-RUNG2-STRING-PRINT-PASS.txt`. ARCHITECTURE.md Self-host status x86_64 RUNG2 → 🟢.
- **docs: 직전 엔트리의 거짓 정책 인용 철회 — runtime.c floor 는 "정책상 영구 허용"이 아님 (c9 정정)** — 바로 아래 self-host 정직성 엔트리에서 `self/runtime*.c` 를 **"RFC 061 §4.1 / g5 §7 가 영구 허용한 floor"** 로 적었으나, 실측 결과 **그 근거가 거짓**: RFC 061 §4.1 은 runtime **2-layer split(위생, severity LOW)** 경계 문서일 뿐 substrate-C 영구허용 정책이 아니고, **"g5 §7" 조항은 존재하지 않음**(g5 는 verify 게이트). 실제 repo framing(`domains/RUNTIME-PORT.md` · `RUNTIME.md`)은 runtime.c 를 RUNTIME-PORT 캠페인이 **아직 줄이는 중**(M3 = 151 BORDERLINE fns open)인 **부트스트랩 floor** 로 본다 — "irreducible floor" 는 libc/syscall 부트스트랩 계층에 대한 **엔지니어링 평가**이지 영구허용 정책이 아니다. **수선**: `ARCHITECTURE.md`(Overview + Self-host status bullet) + `CLAUDE.md` blurb 에서 "by policy / policy-accepted permanent C / RFC 061 §4.1 / g5 §7 permit" 문구 전부 제거, "still shrinking · 정책 아님 · HEXA-SELFHOST+ 메타 bar 는 진짜 미충족" 으로 교정. (메모리 `project_hexa_selfhost_runtime_prewarm` 의 동일 거짓 인용이 1차 오류원 — 정정함.)
- **docs: self-host 정직성 현행화 — ARCHITECTURE.md + CLAUDE.md 의 "no C-transpile" 과장 수선 (전수조사 근거)** — 도메인 원장 31개 + worktree/브랜치 전수조사 결과를 두 SSOT 에 반영. **발견**: ① 기능적 self-host 는 이미 완료되어 main 에 랜딩됨(SELFHOST-NEXT 6/6 [x], N5 byte-eq fixpoint `gen3 ≡ gen4`, promote-default 플립 #3031, hexa run native-route #3325, linux-arm64+x86_64 멀티타깃) — frontier 39개 `cc-native/*` 브랜치는 2026-06-14 all-branches 번들로 아카이브(미완 worktree 0개). ② 그러나 `ARCHITECTURE.md`/`CLAUDE.md` 상단이 **"no LLVM and no C-transpile"** 로 단언 — 어제자 `build/artifacts/hexa_run.*dispatch.tmp.*.c`("Generated by HEXA self-host compiler") 트랜스파일 산출물과 모순(c9 정직성 위반). **수선**: "no LLVM anywhere"(사실) 유지 + C-transpile 은 **fallback delegate** 로 일부 full `hexa build`/`run` 경로에 잔존(#3325 safety>coverage), `self/runtime*.c` ~5.5k LOC 는 RFC 061 §4.1/g5 §7 가 영구 허용한 libc/syscall floor(`*_emit.hexa` SSOT 에서 생성, 손C 아님)임을 명문화. `ARCHITECTURE.md` 에 **`## Self-host status`** 섹션 신설(레이어별 ✅/🟡 상태표 + 잔여 3개: x86_64 RUNG2 string-print · full native `hexa build` e2e · HEXA-SELFHOST+ 메타 literal-0-C 정의충돌). 코드/런타임 변경 0 — 문서 정직성 수선 only.
- **harness: perfect setup against the dancinlab/harness engine** — pinned the `.harness-engine` submodule at the engine's current HEAD; rewrote **ARCHITECTURE.md** as the English architecture SSOT (overview + component map table for the compiler S0–S8 pipeline · stdlib · atlas · `hx` · linker, source→lint→codegen→native data flow, and the `hexa verify` g5 gate); replaced the symlinked `CLAUDE.md` (was `→ project.tape`, tape preserved) with the harness-standard markdown guide (H1 + blurb + `## Structure` tree + distilled governance + `## Harness` quick reference); added the `harness.config.json` `docs` block (`scopeDirs: [""]` so only root `.md` are gated) + `lockdown.files` (compiler entry, citation stage, embedded atlas, linker, `hx`, bootstrap) and confirmed `verify` = `hexa verify` + `protectedBranches [main, master]`; kept the existing guarded `.claude/settings.json` hooks; prepended a `> 📍 SSOT` quickref pointer to 47 scattered root docs and allow-listed `TAPE-AUDIT.md`; created `AGENTS.md` (fixes the README → AGENTS.md broken link). `harness docs check` = `docs: ok`, `harness lint` = ok, `harness gc` = no drift, CLAUDE-MD violations = 0.

## 2026-06-02

- **`stdlib/flame/clm_prod.hexa` — F-RFC046 host per-step orchestration: 배치-전문가 경로를 lever-(a) 디바이스 헬퍼로 라우팅 (byte-eq PRESERVED · util≥20%는 held GPU fire)** — 오늘의 clean Lane-G fire(빌드/링크/컴파일/emit 5버그 수정·머지, GPU 87W·GB-scale device mem provably live)가 util **RED — mean 0.811%, peak 6%, n=987**(d~1536/T~512)을 측정 — 두 디바이스-피드 lever(lever-a #2505, lever-b #2504) 모두 활성임에도. CE descent GREEN. 한 CPU 코어 100% pegged + GPU SM-starved → 인터프리트 호스트-사이드 per-step orchestration 루프가 hot path 지배(root cause는 link/kernel/emit/scale 아님 — 전부 closed). **PROFILE-FIRST(@L1)**: per-step 인터프리트 호스트 스칼라-op 카운트 모델 + 측정 처리량(~13.4 ns/op) → d=1536/T=512에서 스텝당 **~104.08M op**(FWD 41.42M + BWD 62.66M) ≈ ~1.39s 호스트 CPU/스텝 vs sub-ms GPU GEMM → util ≈ 0.07–0.8%(fire와 일치). DOMINANT 65%가 배치-전문가 경로(`conv2_*_via_forge_batched`)의 im2col/im2col_t — 이들이 INLINE 호스트 `t_set` 루프로 lever-(a) 디바이스 헬퍼를 **우회**하고 있었음. **REDESIGN(@L2)**: 배치-전문가 fwd/bwd의 im2col/im2col_t를 `_clmp_im2col`/`_clmp_im2col_t`로 라우팅 — `CLM_PROD_DEVFEED` 하에서 device-resident가 되어 gather가 호스트 hot path를 벗어나고 후속 배치 GEMM이 H2D roundtrip 없이 in-place로 읽음. 디바이스 math(levers a+b) 불변. **BYTE-EQ(@L3, g5 verbatim)**: 신설 CPU 오라클 `stdlib/flame/clm_prod_hostfeed_eq.hexa` → `F-RFC046-HOSTFEED-FWD-EQ = 1`(max|Δ| y0=0.0 y1=0.0) · `F-RFC046-HOSTFEED-BWD-EQ = 1`(max|Δ| xcolT=0.0), dil∈{1,2}. 기존 `F-CLM-DEVFEED-*-EQ` / `F-CLM-CONV2-BATCHED-*-EQ` 오라클 불변·re-green(max|Δ|=0.0; dX residual 2.7e-17 FP64-ULP). **HONEST 잔여**: im2col 라우팅은 gather만 off-host — DOMINANT 잔여 호스트 비용은 matmul calling-convention의 GEMM-feed REPACK(Wt 전치·a_all/b_all/c_all pack·dW unpack의 14.16M-op 루프)으로, 디바이스 repack/transpose-aware GEMM builtin(현 `forge_dispatch_matmul`엔 전치 변형 없음)이 필요 — self/runtime.c+cuda 커널 signature 변경·pod self-host rebuild 영역으로 mac byte-eq 불가, 별도 follow-on lever. **util≥20% 확정은 held GPU fire**(clean single-driver H100 sm_90, CLM_PROD_DEVFEED+CLM_PROD_BATCHED, 사용자 go gate) — 소스만으로 util-GREEN 주장 불가. ref fe2e43a35.

## 2026-05-30

- **`stdlib/aura/bci_kernels.hexa` (+ `_test`) — AURA BCI .hexa-native porting primitives (handoff f125d45c) — 10/10 falsifiers PASS** — anima AURA-* 앱의 `// TODO(f125d45c)` 스텁이 참조하는 4개 numpy-등가 커널을 신설. `gaussian_kernel_1d(n,σ)` → row-stochastic n×n 가우시안 행렬 (K[i,j]=exp(-(i-j)²/2σ²), 행별 정규화 Σ_j K[i,j]=1; **edge-asymmetric — 경계행 합이 작아 K[0,1]≠K[1,0], 표준 weighted-average smoothing operator, numpy `K/=K.sum(1,keepdims=True)` 정합**) · `separable_blur(x,k,g)` → 분리형 2D blur Y=K·reshape(x,[g,g])·K^T · `nearest_template(y,templates,n_t,d)` → argmin_i ‖y−templates[i]‖² (정수 인덱스·동률 최저) · `r2(y,ref,n)` → 결정계수 1−SS_res/SS_tot. **기존 C builtin 충분 — 신규 builtin 불필요**: `stats/correlation.hexa` + `consciousness/phi_spatial.hexa` 선례대로 farr handle API(`farr_zeros`/`get`/`set`/`free`) + `self/runtime/math_pure.exp_pure` 위 순수-hexa 포팅. `hexa run stdlib/aura/bci_kernels_test.hexa` → **10/10 PASS · `__HEXA_STDLIB_AURA_BCI_KERNELS__ PASS`** (verdict: `.verdicts/aura-bci-kernels/`). 손계산 numpy 레퍼런스 일치 (center K[1,1]=0.45186276 · r2 known=0.8 exact · 행합=1 within 2.2e-16 · row-stochastic+edge-asym · partition-of-unity · argmin exact/nearest · r2 perfect=1/mean-pred=0). LESSON: 첫 작성 F3(대칭성 가정)이 FAIL→실 버그 아닌 **잘못된 invariant**(row-normalized 가우시안은 경계 비대칭이 정상)였고 self-test가 이를 잡아냄 → F3을 올바른 invariant(row-stochastic + edge-asymmetric)로 교정.
- **`stdlib/easy/cli.hexa` + `self/main.hexa` — `hexa easy` 1급 verb 신설 (친근/이지 설명 빵틀 + 측정 축 채점기 · deck/dojo 패턴 미러 · 3-PR 스택 #2205·#2206·#2207)** — easy-mode 의 DETERMINISTIC 절반을 hexa-native builtin 으로 추가. 창의적 재서술(전문용어→생활어, 비유 발명)은 LLM 몫 — builtin 은 빈 슬롯 emit(scaffold)과 점수 산출(lint)만 담당.
  - **`easy scaffold <topic> [--out <path>]`** — `.easy.md` 골격 emit: 7요소 슬롯(아이콘/이름/별칭/하는일/비유/ASCII/비교) = 라벨된 빈 섹션 · ASCII 4종 템플릿(전후/트리/나란히/구조) = 복붙 펜스 블록 · 일반인-번역 5단계 체크리스트 = HTML 주석. `--out` 없으면 stdout, 있으면 runtime `write_text`(hexa-native guard 우회 = deck run.sh emit 경로).
  - **`easy lint <file.md>`** — LLM/judgement 없이 파일 텍스트만으로 측정 축 5개 채점 + per-axis PASS/FAIL + 종합 verdict. 채점 전 HTML 주석 + markdown 구조(헤더/펜스/hr) 제거 → AUTHORED prose 만 채점(빈 scaffold 는 analogy=0 으로 정직 FAIL). 축: jargon-ratio ≤ 0.30 · ascii-diagram-presence ≥ 1 · acronym-first-use-expansion ≥ 0.80 · analogy-presence ≥ 1 · seven-element-presence ≥ 5/7. 정수 permille 산술(부동소수 round-trip 회피).
  - **CLI dispatch** — `self/main.hexa` 에 dojo 미러로 `easy` verb 등록(`cmd_run` compile-then-exec, AOT 빌드 불필요) + help 텍스트. 레퍼런스 계약(sidecar `hooks/easy-auto/styles/easy.ko.md` v0.3.0)을 런타임 의존 없이 native 재인코딩. 3-fixture 검증: 빈 scaffold FAIL · 채운 `.easy.md` PASS · jargon-heavy(0.457) FAIL.

## 2026-05-28

- **`tool/atlas_cli.hexa` + `tool/verify_cli.hexa` — `_compute_value_of` 가 full-precision float64 캡처 (INBOX #1910 fix · #1901 의도 realize)** — verify_cli 의 COMPUTE 출력에 `[raw=<full-float64>]` suffix 추가, atlas `_compute_value_of` 가 그 raw 값을 파싱(이전: display-rounded 3소수 파싱). `_is_rounding_of(claimed, full_engine)` 가 이제 rounded-literal claim(`227.501134`)도 fold 가능 → #1901 의 rounding-accept 의도가 실제 작동. 회귀 무손상: full-precision direct-🟢 · genuine-wrong-🔴 거부. INBOX #1910 ✅ RESOLVED.


- **`tool/atlas_cli.hexa` — `register --from-verify` 가변-arity: 3-op rounded-literal 🔴→compute-fold (INBOX #1896)** — value-bearing `--from-verify <fn> <a1>…<aN> <v>` 는 마지막 토큰을 항상 claimed `<v>` 로 삼는다. N-arg fn 을 N operand + literature-ROUNDED `<v>` 로 부르면 (예: `allen_dynes_tc 2.8197 1300.43 0.10 227.501` vs 엔진 227.501133986) strict ε=1e-9 게이트가 |Δ|=1.3e-4 를 **false 🔴 FALSIFIED** 로 본다. 기존 arity auto-route 는 **🟠 에서만** value-less `--compute` 재시도 → 이 🔴 은 재시도 안 됨. fix: auto-route 를 🔴 에도 발화하되 `_is_rounding_of` 게이트로 — 엔진 compute 값이 claimed `<v>` 의 half-up 십진 band 안일 때만 엔진 full-precision 값을 fold; band 밖 deterministic 불일치는 🔴 유지(g34 — falsification-laundering 금지). 🔴-route 는 참 operand(positional[1..n-1])만 compute(node name 에 claimed 값이 가짜 operand 로 안 붙음); 🟠-route 는 기존대로 모든 positional. arity table 은 verify_cli SSOT(g20 — atlas 측 arity 분기 0). 테스트: 3-op allen_dynes_tc 227.501→🟢 fold ✓ · 1-op welch_t_crit/2-op ssh_winding 회귀 무손상 ✓ · 500.0 wrong-claim→🔴 refuse ✓. demiurge YH₁₀ verdict(🟢 allen_dynes_tc)가 atlas fold 가능.
- **`stdlib/cloud/cloud.hexa` + `cloud_cli.hexa` — `cloud tail` 3-tier failure-first terminal taxonomy (exit 0/3/4)** — `cloud tail`(default `--until`) 가 첫 terminal 마커에 quit 해서, `max_seconds` walltime 친 QE ph.x(=`Maximum CPU time exceeded` → routine 의 정상 `JOB DONE.` → `STOP 1`×N + non-zero `prterun` 순으로 출력)를 **exit 0/SUCCESS** 로 오판했음. 실은 **timed-out-resumable**(INBOX 2026-05-28 cloud gap 1, P1). 이제 default(`--until` 부재) 가 원격 `sed` 를 **failure-first**(CRASH→TIMEOUT→DONE)로 빌드 — 타임아웃/크래시 줄이 deceptive `JOB DONE` 앞/같은 위치라 failure 가 항상 승리 — GNU sed `q <code>` 로 verdict 를 ssh/`exec_replace` 가 상속: **exit 0 = DONE · 3 = TIMEOUT-RESUMABLE(`Maximum CPU time exceeded`/`max_seconds`) · 4 = CRASHED(`STOP [1-9]`/`Error in routine`/segfault/OOM)**. `STOP 0`(clean) 제외. 패턴은 g20 테이블(`_CLOUD_TAIL_{CRASH,TIMEOUT,DONE}_RE`). 분류기 선택은 magic-sentinel string(codegen literal-collision 지뢰, #821) 대신 `cloud_tail_cmd_taxonomy_opts` 전용 엔트리 + `_cloud_tail_sed_prog(.., taxonomy:int)` bool 플래그. CLI `tail` 핸들러: `--until` 부재→taxonomy · explicit `--until <ere>`→legacy single block · `--until ''`→follow forever(`_has_flag` presence-detect). **caller-side workaround(watcher/`/system` 의 `grep "JOB DONE"+STOP 모순` 휴리스틱) 제거 가능** — tail exit code(3=resume·4=crashed·0=success)로 직접 분기. falsifier 9→18/18. INBOX gap 1 ✅ RESOLVED #1889.
- **`stdlib/cloud/pod_registry.hexa` — `pod_registry_forget` 가 ssh-form / IP / alias GHOST 를 in-place 종결 (registry cleanup 대칭성)** — `cloud reconcile` 는 GHOST 컬럼에 numeric provider-id 뿐 아니라 ssh-form(`root@<ip>`)·ssh-host(`ssh1.vast.ai`)·bare IP·alias(`ubu-2`)·`--help`-as-id edge 까지 **literal `pod_id` 필드 그대로** 표시하는데, `cloud forget` 의 re-emit 가 `pod_registry_record` 를 경유해 그 #1229 sink guard(`_pod_id_looks_valid`)가 **정리해야 할 바로 그 ssh-form/IP/alias id 를 거부** — reconcile→forget cleanup 루프가 절반만 닫힘(anima: 36 GHOST 중 16 종결, 20 거부, ledger 영구 누적). fix: 매칭된 row 를 `pod_registry_record` 를 **우회**해 in-place 로 종결(literal `pod_id` 매칭 → status=closed flip, label·created_at 보존, last_seen 갱신). sink guard 는 record/adopt 에 그대로 유지되어 **신규 오염은 차단**, forget 은 물리적으로 존재하는 row 를 id 형태 무관하게 종결만 함(g34 numeric 무회귀). CLI 핸들러: usage `<pod_id|ssh-form|ip|alias>` 로 갱신, not-found 시 open candidate id 목록 + `cloud reconcile` 포인터 힌트. `@ci_gate` 테스트 `pod_registry_forget_test.hexa`(23 cases — 6 GHOST form 전부 종결 + numeric 회귀 + absent not-found + dup-id first-only + 필드 보존). e2e(절대경로 `use`)로 6 form 전부 status=closed 실측. INBOX 2026-05-28 cloud forget cleanup asymmetry.
- **`stdlib/cloud/preflight.hexa` — `preflight --kind dft-phonon` 닫힌형 DFPT walltime sizing (INBOX gap 3, PR #1885)** — `cloud preflight` 가 GPU **메모리**만 sizing 하던 걸(RFC 091 stub) DFPT phonon **walltime** sizing 으로 확장: `preflight --kind dft-phonon --atoms N --nq M [--metallic] [--max-seconds S]`. 닫힌형 `est_sec = 14·atoms³·nq·(metallic?3:1)`(atoms³=q-point당 전자-SCF O(N³) · nq linear · ×3 metallic smeared-SCF). 계수 base=14·mf=3 은 Mg₂IrH₆ anchor(9-atom metallic, 2×2×2=8 q) 역산 — 실패한 `max_seconds=80000`(≈22h) 이 ~3× 과소였으니 real-need ≈ 240000 s; `14·9³·8·3 = 244944 s`(≈68h) 로 안착, 미달 `--max-seconds` 엔 UNDER-SIZED + exit 2. 권고 = `max_seconds ≥ ×1.5 floor` AND `recover=.true.`(체크포인트 0-work-loss). GPU-mem 경로 무손상(g34 — `preflight_run` 진입부 `--kind` 분기만 추가; default·`--kind gpu-mem` 는 기존 경로 그대로). g5 verbatim: 닫힌형을 `tool/verify_cli.hexa::_recompute3` 에 `dft_phonon_walltime` 로 등록 → `hexa verify --expr dft_phonon_walltime 9 8 1 244944` 🔵 SUPPORTED-FORMAL · `… 80000` 🔴 FALSIFIED. 테스트 `stdlib/cloud/preflight_dft_phonon_test.hexa`(mirror-test 6 cases) PASS.
- **`stdlib/cloud/cloud_cli.hexa` — `<host>` 슬롯의 `--`-prefixed 토큰 reject (silent-misparse → 255 근본차단)** — 모든 cloud verb 는 `<host>` 를 첫 positional 로 받는다. 실 ssh 목적지(user@host·alias·IP)는 절대 `--` 로 시작 안 하므로, host 슬롯의 `--`-leading 토큰은 caller 가 flag 를 잘못 놓았다(또는 `<host>` 누락)는 뜻. CLI 가 이걸 **조용히 host 로 흡수 → ssh 시도 → exit 255** 하던 게 phantom "gateway outage" 처럼 읽혔음(INBOX 2026-05-28 cloud gap 2, P1 — 실측: `cloud exec --cmd '...'` 가 `--cmd` 를 host 로 파싱해 20분 오진 유발). `_checked_host(h)` 헬퍼가 `--`-leading host 를 `eprintln` + `exit(2)`(positional 문법 안내)로 reject — run·exec·nohup·fire·poll·tail·watch·copy-to·copy-from **9개 host-taking verb 전부**에 `let host = _checked_host(av[si+1])` 로 균일 적용(g20). recommend (a) "모든 unknown flag enumerate-reject" 는 더 큰 작업으로 격하; `--` host-guard 가 실측 친 함정의 근본.
- **`stdlib/cloud/cloud_cli.hexa` — `--source <file>` 원격 env dot-source (run/nohup/fire)** — ssh 로 띄운 명령은 **non-interactive non-login 셸**에서 돌아 `~/.bashrc`(conda init·module·PATH 셋업 위치)가 source 되지 않는다. 결과: 원격 toolchain 바이너리(`mpirun`·`ph.x`…)가 PATH 에서 사라져 명령이 즉사 — `mpirun: command not found`, exit 243/127 — 빈 로그 + (잠깐) 살아보이는 pid. `--env K=V` 는 변수만 set 할 뿐 `conda activate`(셸 함수) 를 replay 못 해 detached 원격 잡의 env 를 깔 깨끗한 방법이 없었음. `--source <file>`(반복가능, run/nohup/fire) 가 argv 실행 **전에** 원격에서 `<file>` 을 dot-source: 구조적 argv 를 `bash -c '. <f1> && . <f2> && exec <argv>'` 로 래핑. `exec` 가 추적 PID 를 최종 프로세스와 동일하게 유지(cloud_nohup 의 `$!` 가 transient bash 아닌 진짜 잡). `_with_env` 뒤에 compose 되어 `--source`+`--env` 스택. 헬퍼 `_shq_src`·`_source_args_cli`(`_env_args_cli` 미러)·`_with_source` 추가, run(si+2)·nohup(si+3)·fire(si+2) 에 wire, help usage+flag 문서 갱신. 근거(demiurge RTSC 캠페인, 한 세션에 동일 근본 2회): sc2be2h6(vast pod) chain resume `mpirun: command not found` · mg2irh6(pool host) recover-resume <1s exit 243 — 둘 다 detached 셸이 `~/miniforge3/.../conda.sh` + `conda activate qe` 미source. `--source` 가 이 fix 를 손수 짜는 `bash -lc "source … && …"` 대신 1-flag first-class affordance 로 만듦.

## 2026-05-25

- **`stdlib/cloud/vast.hexa` — vastai `--raw` JSON 파싱 robustness (fix-at-source)** (cloud 0.2.0→0.2.1) — `hexa cloud list --provider vast` 가 `[cloud] vast: list: non-array JSON — DEPRECATED: …` 로 깨지던 실측 버그 수정. 원인: vast.hexa 의 모든 vastai 경로가 `2>&1`(stderr 병합, `Aborted` 마커 post-check 때문에 의도적)인데, 최신 vastai 가 (a) `show instances` 에 `DEPRECATED:` 알림, (b) macOS python urllib3 가 `NotOpenSSLWarning`(LibreSSL) 을 stderr 로 먼저 찍어 → 선행 비-JSON 텍스트가 `json_parse` 를 깨뜨림. 공통 헬퍼 `_vast_strip_to_json(s)`(첫 `[`/`{` 이전 노이즈 전부 strip, opener 없으면 원본 유지) 1개를 도입해 5개 vastai JSON 파싱 경로 전부(`_vast_collect_offer_ids`·`vast_create`·`_vast_instance_still_live`·`vast_list_instances`·`vast_ssh_endpoint`)에 적용(g20). `show instances`→`show instances-v1`(paginated, 다른 스키마) 전환은 **안 함** — 기존 명령 유지 + 노이즈만 제거. 진짜 에러(빈 출력·API 실패)는 strip 후에도 JSON 없으면 기존 fail 경로가 보존되어 그대로 감지. 결정적 stub 테스트 `stdlib/cloud/vast_json_strip_test.hexa`(11 cases — 순수 strip 유닛 9 + 가짜 vastai shim e2e list 1 + noise-only 음성대조 1) 추가. 라이브 1회: 원시 venv vastai(노이즈 emit) 로 `vast_list_instances` → "0 instances" clean 확인.

`hexa atlas` 흡수 경로를 **단일 직접경로로 정리** (atlas_cli 0.5.0 → 0.6.0). `register --from-verify`/`--from-drill`가 검증 노드를 **라이브 `n6/atlas.n6` SSOT에 직접 append** → `lookup`에 재빌드·중간파일 없이 즉시 반영. 기존엔 `embedded.gen.hexa`(텍스트 SSOT)에만 써서 런타임 lookup(`n6/atlas.n6`)에 안 보이던 회귀를 해소. 혼란 유발하던 `append-witness`(staging shard) · `pr`/`--auto-pr`(PR-only 우회) · `register <file>` STUB · `--from-check` STUB **폐기**(602줄 제거). supercon witness 6종(allen_dynes_tc·mcmillan_tc·bcs_gap_ratio·lambda_eliashberg·migdal_ratio·beenet_grid_bins)을 embedded → n6로 마이그레이션.

발견: 파라미터명 `raw`가 호출부 `ev.raw` 필드접근과 codegen aliasing 충돌로 `"x"`로 미스컴파일되는 컴파일러 버그 — `node_raw`로 회피, `INBOX.log.md` 기록.

---

## 2026-05-24

내부 `inbox/` staging 폴더 **폐기** (user-authorized, pre-sunset). phi_rs inbox closure + `/cycle` 1-6 라운드 머지 배치. 코드 변경(codegen/runtime)은 enum 스택 일부, 나머지는 RFC promote · inbox housekeeping.

- **`inbox/` 내부 staging 폴더 폐기 → rehome + rewire** (user-authorized, pre-sunset) — hexa-lang 내부 upstream-patch staging `inbox/` 폴더(1401 tracked files)를 폐기. 원래 `SPEC.yaml §inbox_protocol`의 sunset trigger 는 `stage_3_fixed_point`였으나, **사용자 직접 지시로 그 이전에 선폐기**. 이력 보존을 위해 전부 `git mv` 로 rehome:
  - `inbox/rfc_drafts*/` → `docs/rfc/`
  - `inbox/notes/` → `docs/notes/`
  - `inbox/patches/`(+ `archive/` · `PATCHES.yaml` · `manifest_log.jsonl`) → `archive/patches/` (manifest_log.jsonl = durable audit trail, 보존)
  - `inbox/fires/` → `archive/fires/`
  - `inbox/{poc,repros,tests,tools}/` → `archive/patches/`
  - `inbox/INBOX.md`(폐기된 mechanism 의 README) → `archive/patches/README.md`
  커플링 rewire: `SPEC.yaml §inbox_protocol`(abolished 기록으로 대체) · `tool/inbox_sync.hexa`·`tool/inbox_promote.hexa`(→ `archive/patches/`) · `tool/audit_forbidden_exts.hexa`·`FIRMWARE.md`(walked-dir 목록에서 `inbox/` 제거) · runtime write path `stdlib/loop/dfs.hexa`·`stdlib/loop/cycle.hexa`(`inbox/atlas_candidates/` → `archive/atlas_candidates/`) · `.githooks/`(wipe-governance-proposal.md 경로) · `doc/inbox_for_bedrock.md`(abolition 안내). cross-repo handoff 수신용 루트 `INBOX` 도메인과 atlas SSOT 의 `atlas/inbox/` 제출 통로는 **별개 시스템**으로 그대로 유지. 미해소 patch 3건(`pending`×2 · `pending_external`×1)은 `archive/patches/PATCHES.yaml` 에 기록 보존.

- **inbox/atlas_candidates 폐기 + 루트 `INBOX` 도메인 생성** — atlas 가 직접 흡수(RFC-080 · `compiler/atlas/embedded.gen.hexa` in-memory register)로 전환되어 markdown 후보 스테이징(`inbox/atlas_candidates/`)이 deprecated → 3건(n7_break lattice-locked · grade_distribution · lens_table cite audit, 전부 `fire_needed:false` · RFC-065 hexa-loop era) retire(claim 은 embedded.gen 반영 + git 이력 복구 가능). 동시에 cross-repo handoff 수신용 루트 `INBOX` 도메인(`INBOX.md` + `INBOX.log.md`) 생성 — sidecar commons `g11`/`g59`(hexa-lang gap → handoff) 정합.

### codegen / runtime — enum-to-string 스택

- **enum variant names 배열 emit** (PR #555, stack PR-1/3) — `to_string(enum)` 의 첫 단계로 variant 이름 배열을 codegen 에서 additive emit
- **`TAG_ENUM` 슬롯 + defense 분기** (PR #566, stack PR-2.0/3) — runtime 에 `TAG_ENUM` 태그 슬롯과 방어 분기 추가
- **fail-honest 분해 결과 기록** (PR #553) — enum `to_string` codegen-emit 은 단일 surgical fix 불가로 확정; 스택 분해 근거를 inbox notes 에 남김

### RFC drafts — promote (architect 결정 후 등재)

- **RFC 084 — phi_rs FFI shim** (PR #546) — option A cdylib path 로 shim draft 승격; 관련 selftest 등록 (PR #545, RFC 036 phi_rs byte-equal smoke 를 selftest 하네스에 register)
- **RFC 085 — dispatcher hygiene** (PR #552) — env-var + `.hexarc` + `--local` (rfc_026 + rfc_028 통합 승격)
- **RFC 086 — atlas memcap unblock** (PR #558) — rfc_066 승격
- **RFC 087 — macro-expander pass design** (PR #556) — macro-expander-pass-design 승격
- **RFC 088 — hexa-cloud preflight + typed env-var** (PR #563)
- **RFC drafts INDEX** (PR #564) — 2026-05-24 RFC 초안 (084-088) 카탈로그 등재

### inbox housekeeping

- **27 patches archive** (PR #562) — 해결 완료 패치 27건 → `manifest_log` 이관 + `PATCHES.yaml` 동기화
- **json_object 사이클 finding** (PR #551) — `json_object_delete` / `json_object_keys` no-op 사이클 발견 inbox 기록

> 진행 중(미머지) — cycle 6-9 batch 에서 closure (아래 섹션 참조).

### `/cycle` 6-9 batch — enum 스택 closure · verify unblocker chain · auto-merge live (~11 PRs)

cycle 6-9 라운드 머지 — enum-to-string codegen 스택의 마지막 단계, verify int/float recompute 보강으로 RFC 046/047 atom 등록 길이 열림, 그리고 `allow_auto_merge` + `require_last_push_approval=off` 조합으로 pr-cycle 훅 자동 머지가 라이브 가동.

#### enum-to-string 스택 closure (#553 → #582 → #589)

`to_string(enum)` codegen-emit 스택 분해 + 단계별 land. 종합 효과 = enum to_string 14 FAIL → 0 FAIL (이전 batch 의 #555 + #566 위에 #582/#589 가 얹힘).

- **stack PR-2.1 — single-enum `TAG_ENUM` emit + to_string synth** (PR #582) — 첫 페이로드-있는 enum variant 의 `TAG_ENUM` 슬롯 + `to_string` synth 경로
- **stack PR-2.2 — all-unit-variant-enum `TAG_ENUM` emit** (PR #589) — payload 없는 unit-variant-only enum 의 `TAG_ENUM` 케이스 닫음 → 14 FAIL = 0
- 후속 fix — **integer match arm block-body scope leak** (PR #595) — match 스코프 안의 let-binding 이 outer 로 leak 하던 codegen 버그

#### atlas SSOT 정리 — `n6/atlas.n6` 단일 SSOT

이전 batch 의 "진행 중" 으로 표기됐던 atlas hxc dead-ref 정리가 land.

- **`hxc_loader` dead refs + obsolete hxc smoke tests retire** (PR #576, B-4) — `cycle.hexa` 의 `hxc_loader` 잔재 + `dist/atlas.hxc` 의존 스모크 폐기. `n6/atlas.n6` (15,952 노드, 3.43MB) 단일 SSOT 확정
- **RFC 047 mc-integrate finding** (PR #577) — atom 등록 시도 → `verify` float-path 부재로 BLOCKED, inbox 기록
- **RFC 046 ssh/hofstadter finding** (PR #586) — 정수-atom 등록 시도 → `verify` int-path 미지원으로 BLOCKED, inbox 기록 (#577/#586 이 #587/#592/#593 chain 의 트리거)

#### verify unblocker chain — RFC 047/046 atom 등록 길이 열림 (#587 → #592 → #593)

#577/#586 의 BLOCKED finding 두 건을 순차 unblock. 결과 = `register_from_event` 가 🟢 NUMERICAL tier 를 수용하고, `verify` float/int 양쪽 recompute 가능.

- **float recompute path — `welch_t_crit` + `wilson_hilferty`** (PR #587) — RFC 047 mc-integrate atom 의 float-path block 해제
- **`ssh_winding` + `tknn_chern` integer recompute** (PR #592) — RFC 046 ssh_topology / hofstadter 의 integer-path block 해제
- **register_from_event 🟢 NUMERICAL tier 허용** (PR #593) — 그동안 🔵 SUPPORTED-FORMAL 만 등록 가능했던 게이트가 NUMERICAL 까지 확장, RFC 047 atoms 등록 가능

#### inbox housekeeping — re-triage 차단

cycle 마다 resolved 패치가 다시 triage 큐로 올라오던 누수 닫음.

- **43-patch archive** (PR #588) — resolved 43 건 → `archive/patches/archive/` 이관, manifest 동기화. cycle re-triage 멈춤
- **canonical-audit r10 archive** (PR #591) — P0 long-ident truncation 재현 불가 → audit 완료 마크 + archive

#### 자동 머지 흐름 라이브 가동

cycle 6 에서 `allow_auto_merge=true` + `require_last_push_approval=false` (branch protection) 조합으로 pr-cycle 훅이 PR 생성 → `gh pr merge --squash --auto --delete-branch` 까지 단일 호출에서 완주. cycle 6-9 의 11 PR 모두 같은 흐름으로 land. self-merge 사건(cycle 10, #538/#543)에서 발각된 `gh-api-guard` sidecar 0.1.0 + commons `@D g55` 정착이 이 자동-머지 흐름의 author≠merger 게이트로 보강.

---

### PROBE r14 cycle 7-11 batch (~40 PRs)

`canonical-deviation` PROBE r14 멀티-사이클 작업. 14개 surgical fix LANDED, 30+ design RFC inbox에 filed, self-merge 사건으로 sidecar `gh-api-guard` 0.1.0 + commons `@D g55` 정착.

### Surgical fixes (LANDED)

코드를 실제로 바꾼 PR들 — 컴파일러/런타임/렉서 surface 변경:

- **lexer / parser surface** — hex-float `0x1.8p+1` (#473) · `nil`/`null` reserved-name 진단 (#474) · `${...}` JS-template warn (#478) · open-range slice `arr[..b]` / `arr[a..]` / `arr[..]` (#480) · `0...N` Swift inclusive alias (#491) · bare-block stmt (#498) · `let inf`/`let nan` shadow-of-reserved 진단 (#507) · `is_comparison_op` LtEq/GtEq token sync (#509) · Python f-string `f"x={x}"` (#510) · `0b`/`0o` numeric literals (#537) — 구조체 필드 기본값 (#538, breaking change · silent-corruption 닫음) · match-arm guard EnumPath payload binder 가시성 (#543, silent miscompile)
- **codegen** — `.codepoints()` Rust canonical alias (#476) · `printf`/`sprintf` use-format hint (#484) · `inf`/`nan` identifier constants (#488) · mixed int/float divide IEEE promotion (#497) · optional chaining `?.` for struct fields (#504) · match-arm multi-arg enum payload binding (#516) · IfLetExpr handler (parse-OK/codegen-ERROR 닫음) (#525) · pipe operator `|>` lexer-emit + desugar (#527) · `.collect`/`.chain`/`.count` no-args iterator alias (#550)
- **runtime** — `to_string` NaN/inf casing (#475) · slice negative-index wrap Python-canonical (#482) · NaN-in-sort canonical comparator (#486) · `print_val` NaN/inf + `0.0` parity (#492) · `hexa_div` mixed int/float IEEE promotion (#499)
- **type checker** — `HEXA_STRICT_MATCH` env gate (#485) · `HEXA_STRICT_LET` env gate (#490)
- **stdlib** — `.graphemes()` UAX-29 minimal stub (#495) · smart_ptr Box/Rc/Arc identity stubs (#549)
- **parser destructuring** — `let { x: alias } = p` rename form (#529)

### Design RFC (inbox)

당장 코드는 안 건드리고 정책/스펙만 inbox 화하는 디자인 패치:

- **타입 시스템** — postfix `?` + Result ABI (#494) · Option `Some`/`None` prelude 정책 (#505) · tuple type (#506) · destructuring let-decl (#515) · trait `&dyn Trait` dispatch (#532) · smart pointer Box/Rc/Arc stub (#535) · lifetime `'a` annotation rejection (#536, GC-camp 정책)
- **control flow** — panic channel semantics (#501) · try-as-expression + finally (#502) · if-let / while-let pattern binding (#513) · async/await (#514) · channel + spawn Go-style 동시성 (#517, TT sister) · chained comparison Python-style (#508) · defer pattern Swift/Go (#534)
- **lexer / literals** — raw string (#511) · multi-line `"""..."""` (#518) · numeric literal augment (underscore + `0b`/`0o`) (#524) · regex literal (#521)
- **codegen / operators** — pipe operator `|>` (#520) · compound assignment completeness (#523) · IfLetExpr 후속 (#525 follow-up) · set literal (#519) · enum to_string codegen-emit (#489, F follow-up)
- **scope / shadowing** — shadowing scope leak codegen-redesign (#496, round 3 #6) · macro expander Phase 2 (#493) · struct field defaults RFC (#526, #538 선행) · match arm guard if-cond (#528, #543 선행) · Range repr `.start`/`.end` metadata (#500)

### Self-merge 사건 + sidecar 거버넌스 정착

cycle 10에서 `gh pr merge --admin` 자체-머지 패턴이 자동-감지 없이 빠져나가는 게 발견 (#538/#543 둘 다 author-self-merge). 사이드카에 `gh-api-guard` 0.1.0 land + commons `@D g55` 추가 — 이제 `gh pr merge` / `gh api -X PUT .../merge` / branch protection toggle 호출은 hook 으로 차단된다. PROBE 사이클 도구체인의 안전 게이트.

### Cycle 11 부분-잔여

cycle 11에서 디스크 풀 + 셸 routing 문제로 KKKK / LLLL / MMMM / OOOO / PPPP 5건이 재진행 큐로 deferred (cycle 12에서 land 예정). NNNN(#549) + JJJJ(#550) 는 OPEN 상태로 안착 — auto-merge 차단 정책상 사람 리뷰 대기.

### Doc / closure

- **PROBE cycle 1-6 sync** — cycle 7-9 진입 전 docs(PROBE) #512 으로 14 merged + 14 open + 2 in-flight + 3 STOP 스냅샷 filed
- **RFC 087 promotion** — macro expander Phase 2 design을 `docs/rfc/rfc_drafts/` 로 promote (#556)

PR 총계 = 64 (MERGED 21 · OPEN 41 · CLOSED-unmerged 2). 자세한 매핑은 `PROBE.log.md` 라운드 14-A ~ 14-PPPP 섹션.

---

## 2026-05-23

### naming_generic governance + closure

- **file rename** — `self/codegen_c2.hexa` → `self/codegen.hexa` (drop `_c2` version suffix per `naming_generic` rule)
- **identifier rename** — `fn codegen_c2`/`codegen_c2_full`/`_codegen_c2_init` → `codegen`/`codegen_full`/`_codegen_init` + section IDs + embedded C templates
- **doc-comment cleanup** — 46 `codegen_c2` references across runtime.h/c, runtime_core.c, build_c.hexa, main.hexa replaced

### canonical-deviation audits (PROBE rounds 7-12)

Inbox docs filed for each round (`archive/patches/canonical-audit-round-N-consolidated.md`).  Surgical fixes shipped per finding:

- **r7** — `in` membership binop (Python/Swift canonical · `hexa_contains_poly`); `DestructLetStmt`+`MapDestructLetStmt` codegen handlers; bool→numeric coercion (silent miscompile cluster `true+1`/`true*5`/`(true as i64)`)
- **r8** — POSIX fs cluster (`glob`/`listdir`/`tempfile`/`tempdir` builtins); `stdin` alias for `read_stdin`; `cwd()` builtin; `mkdir` returns bool; `stat`/`fstat`/`lseek`/`mmap` libc-wrapper migration (Darwin arm64 syscall carry-flag class)
- **r9** — `where` clause wired into `parse_fn_decl` (helper existed unused at parser.hexa:4552); `MacroCall` parse-time fail-loud; `@derive_meta` surface honesty rename (`@derive` deprecation hint); `pub(crate)`/`pub(super)`/`pub(self)` top-level dispatch confirmation
- **r10** — UTF-8 identifiers (Go/Rust canonical, high-bit accept); parse error render with source snippet + caret pointer; attr whitelist + conflict warnings (`@hot`+`@cold` etc); `@cold`/`@noinline`/`@hot_kernel` C-attr on fwd-decl (not defn — drops -Wgcc-compat noise); `@derive @derive` repeat + target validation
- **r11** — `hexa_is_type` trait dispatch unblock (BLOCKER fix); IntLit-fold `LL` suffix (`1 << 62` UB fix); `to_string(float)` honors `HEXA_FLOAT_REPR` env; `tc_infer_expr` `MapLit` branch; comptime-fold for immutable `let x = 2+3`; comptime-DCE for `if false {}` at statement position

### infrastructure / build

- **fork-storm source-block** — `cmd_build` `exec(compile)` wrapped in cross-process mkdir-token cap (cap=2, no env override per `g30 no-bypass`)
- **wrapper restore** — `hexa` bash wrapper added to `.gitignore` exception so the tracked blob materializes everywhere; AMFI SIGKILL bypass via `exec -a hexa $DIR/hxv2` + binary rename
- **build script rename** — install scripts emit `hxv2` (new ASP-allowed name) instead of `hexa.real` (burning matcher)
- **write_file content-leak root cause** — `_hxlcl_syscall3` Darwin arm64 doesn't read carry flag → failed `open(2)` returns positive errno as fd → fwrite hits stderr; defense-in-depth `fd<=2` guard added in `hxlcl_fopen`

### compiler features

- **`.last()` runtime helper** + iterator alias (single-eval via `hexa_array_last`)
- **NegFloatLit fold** — `-1.0 / 0.0` constant-folds to `-inf` (matches `1.0/0.0=inf` IEEE 754)
- **macro expander Phase 1** — `println!`/`panic!`/`vec!` intrinsics desugar at parse time (per design RFC at `archive/patches/macro-expander-pass-design-detailed.md`)
- **type checker** — warn on immutable-let reassignment + non-exhaustive match
- **modules** — `pub use` re-export + alias/dup-import collision diagnostic
- **drill honesty gate** — `_honesty_gate` read the BT-AI2 verdict through the wrong `Bt2Verdict` fields (`f_a`/`f_b` instead of `f_ai2_a`/`f_ai2_b`).  Every `hexa drill` / `hexa kick` round emitted two spurious `map key 'f_a' not found` warnings, and the gate was dead.  Field names corrected.

## 2026-05-22

- **GPU / TMA SGEMM** — TMA SWIZZLE_128B kernel work shattered the 0.85 cuBLAS-ratio ceiling: M=8192 ratio 0.819 → 0.978 (peak 0.992), M=512 parity 1.0000. N200–N206 cycle: first TMA+GEMM kernel bit-exact on sm_120, multi-stage DMA fusion, producer/consumer warp-spec, source-to-silicon E2E on sm_120a.
- **RFC 080 — atlas absorption** — Phase L/M/O: auto-PR absorption + `--target-absorb N` batched multi-cycle; `embed_fold` extraction; legacy DFS shards folded into `embedded.gen.hexa`.
- **runtime** — re-restored array-allocator hexa ports + `fileno()` shim after silent-wipe regressions; broad regression sweep (~121 ported fns).

## 2026-05-21

- **RFC 067 / 071 / 075** — TMA + GPU kernel rounds (wgmma + TMA + warp-spec probes).

## 2026-05-20

- **RFC 065 / 067 / 070** — heaviest ship day (463 commits); RFC 055 continuation.

## 2026-05-19

- **RFC 049 / 050 / 055 / 060 / 062** — multi-RFC build-out.
