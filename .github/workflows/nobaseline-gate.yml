name: nobaseline-gate

# ── .c-graduation faithful-nobaseline gate ──────────────────────────────────
#
# `.c=0` has landed on main (tracked `.c` = 0; real 3-platform release green).
# The old seed-present DRY-RUN jobs (nobaseline-* / final3-*) ASSUMED the
# bootstrap seeds were present at checkout and temp-removed / cold-compiled
# them; on a `.c=0` tree those seeds no longer exist, so those jobs failed
# forever and were self-redundant. They have been RETIRED.
#
# The faithful-nobaseline-* jobs below ARE the authoritative seed-removed gate:
# they remove ALL seeds FIRST (via $SEEDS), then build via the SAME shared
# tool/release_build + tool/release_package the real release runs, edge-pulling
# runtime.a + build/hexat. gate-green ≡ release-green by shared definition.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  CFLAGS_COMMON: "-O2 -std=gnu11 -D_GNU_SOURCE -Wno-trigraphs"
  # ALL 23 seeds — the 18 warm-regen seeds + 2 root bootstrap seeds + the 3
  # extra graduation seeds (hexa_cc.c, forge_tier_v1.c, the classd test
  # harness). The faithful jobs TEMP-remove ALL of them FIRST (via $SEEDS),
  # then run the VERBATIM shared release path (release_build + release_package)
  # edge-pulling runtime.a + build/hexat — so NO seed is ever compiled.
  SEEDS: >-
    self/runtime_core.c self/runtime_hi_gen.c self/runtime.c
    self/bootstrap_compiler.c
    self/native/crypto_openssl.c self/native/crypto_sodium.c
    self/native/exec_argv_sha256.c self/native/exec_pipe.c
    self/native/fp_init.c self/native/mount.c self/native/namespace.c
    self/native/net.c self/native/persistent_pipe.c self/native/proc_fork.c
    self/native/pty.c self/native/signal_flock.c self/native/tensor_kernels.c
    self/native/term_ffi.c self/native/thread.c self/native/wait.c
    self/native/hexa_cc.c self/forge/forge_tier_v1.c
    test/native_build/classd_behavioral_harness.c

concurrency:
  # Cancel a SUPERSEDED run only on PR re-push (a newer PR run replaces it and
  # supplies the fresh required check); push/dispatch runs always complete so
  # main's required-gate history stays intact. Frees runner minutes + shortens
  # the PR queue on rapid re-pushes. Byteeq-inert (cancels stale work; never
  # changes a build's inputs/outputs).
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # ── dispatch — fallback-dispatch runner picker (free · always available) ─────
  # Probes whether a matching self-hosted runner is ONLINE and emits the heavy
  # linux job's `runs-on` at runtime: self-hosted pool (aiden/summer · 12c/30G,
  # far faster) when online + same-repo, else the free GitHub-hosted fallback.
  # Pool-offline therefore NEVER wedges this gate — it just falls back. Fork PRs
  # are forced to GitHub-hosted (no self-hosted RCE). See tool/ci_pick_runner.sh.
  dispatch:
    name: dispatch (pick runner)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      linux_label: ${{ steps.linux.outputs.label }}
    steps:
      - uses: actions/checkout@v6
      - id: linux
        uses: ./.github/actions/pick-runner
        with:
          kind: linux
          # self-hosted only for same-repo events; fork PRs → GitHub-hosted.
          eligible: ${{ (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && '1' || '0' }}
          token: ${{ github.token }}

  # ── faithful-nobaseline — the authoritative seed-removed release-safety gate.
  #
  # These faithful-* jobs model a `.c=0` checkout FAITHFULLY: remove ALL seeds
  # FIRST (via $SEEDS), then resolve runtime.a AND build/hexat from the prior
  # edge tarball (NEVER prebuild either from seeds), then build + package via
  # the IDENTICAL shared entry points release.yml calls (tool/release_build then
  # tool/release_package) — with NO seed ever compiled. There is no separate
  # gate copy of the stage sequence to drift from release; this IS a dynamic
  # seed-removed run of the real release path. gate-green ≡ release-green by
  # shared definition.
  faithful-nobaseline-darwin-arm64:
    name: faithful-nobaseline (darwin-arm64)
    runs-on: macos-15
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6

      # FAITHFUL: remove ALL 23 seeds BEFORE anything builds — models the
      # .c=0 checkout (seeds git-removed). No source compile is possible.
      - name: Faithful — TEMP-remove ALL 23 seeds FIRST (working copy only)
        shell: bash
        run: |
          set -euo pipefail
          for f in $SEEDS; do rm -f "$f"; done
          echo "removed seeds BEFORE any build; tracked .c now:"
          git ls-files '*.c' | wc -l
          ls self/*.c 2>/dev/null || echo "  (no self/*.c — expected on .c=0)"

      # ── SINGLE-JOB-FAITHFUL: this job now runs the IDENTICAL shared entry
      # points release.yml calls — tool/release_build then tool/release_package —
      # with the 23 seeds REMOVED. There is no separate gate copy of the stage
      # sequence to drift from release; this IS a dynamic seed-removed run of the
      # real release path. gate-green ≡ release-green by shared definition.
      # Free CI accelerator: ccache the per-TU compiles (byteeq-safe — HEXA_CCACHE
      # only reroutes the compile through the cache, never the link/emit bytes).
      - name: ccache setup
        uses: ./.github/actions/ccache-setup

      - name: Build — shared release_build (seeds removed, edge-pull)
        shell: bash
        env:
          TARGET: darwin-arm64
          CC: clang
          LIBS: ""
          GH_TOKEN: ${{ github.token }}
          HEXA_CCACHE: "1"
        run: bash tool/release_build

      - name: ccache stats
        if: always()
        shell: bash
        run: ccache -s || true

      - name: Assert — ./hexa runs with seeds removed
        shell: bash
        run: |
          set -euo pipefail
          ./hexa --version
          echo "faithful-nobaseline OK (darwin-arm64)"

      # ── OP-34: machine-independence FOLD regression gate ──────────────────
      # The built ./hexa runs the two self-contained determinism oracles and
      # asserts the recorded golden folds (dt_exp 7679248634312321699 ·
      # dt_erf FWD 1088281529475491513 · dt_erf BWD 2045849491002578884 —
      # OP-138 re-pin to the float-fix-correct invariant, proven byte-identical
      # across 4 environments, F-OP19/19b/19c/19d/138; old pre-fix goldens were
      # 4548590605583584556 / 4249661408190172843, stale under the lossy
      # %g/%.17e const-fold serialize fixed by OP-37/40/132).
      # LOW BLAST RADIUS: only a REAL fold drift (or the oracle failing to
      # run) can fail this step — tool/fold_ci_gate.sh is the SSOT. <5 s.
      - name: OP-34 — fold gate (dt_exp/dt_erf golden folds, darwin-arm64)
        shell: bash
        run: |
          set -euo pipefail
          sh tool/fold_ci_gate.sh ./hexa

      # ── OP-66: flame production step-path libm-FREE tripwire ──────────────
      # OP-34 locks the dt_exp/dt_erf golden VALUES; OP-66 locks that the flame
      # CLMConvMoE PRODUCTION step actually CALLS those dt_ twins and never a raw
      # libm exp/erf/cos/sin/log/sqrt — determinism-contract LAYER 2 (libm-free).
      # libm is not correctly-rounded -> diverges glibc vs Darwin -> breaks the
      # machine-independent byte-eq training guarantee (F-OP19/19b/33). A pure
      # source grep (no ./hexa, no seed) over clm_prod.hexa + its production-
      # reachable lib surface; the deliberate _libm/_sel_ differential-oracle
      # references are exempt by name. LOW BLAST RADIUS: tool/flame_steppath_libm_
      # gate.sh is the SSOT; verified PASS on the clean tree, fails ONLY if a raw
      # libm call regresses onto the step. <1 s. BLOCKING (no seed dependency).
      - name: OP-66 — flame step-path libm-free gate (darwin-arm64)
        shell: bash
        run: |
          set -euo pipefail
          sh tool/flame_steppath_libm_gate.sh

      # ── OP-69: flame trainer step-path FMA-fused-matmul tripwire ───────────
      # OP-66 locks determinism LAYER 2 (no libm on the step); OP-69 locks
      # LAYER 3 — the CLMConvMoE PRODUCTION trainer must NOT route a determinism-
      # path matmul through the raw FMA-fused C farr_matmul/t_matmul kernel.
      # clang -O2 fuses a*b+c into a single fma on arm64 (1 rounding) but emits
      # mul+add on x86 (2 roundings) -> an accumulating matmul returns divergent
      # bytes per ISA on byte-identical fp64 inputs (F-OP29 measured arm64
      # 241449363 vs x86 1401117690). The trainer complies by routing every conv
      # GEMM through the forge_dispatch_matmul DISPATCHER seam (cuBLAS on a CUDA
      # host, cross-ISA-folded on CPU). A pure source grep (no ./hexa, no seed)
      # over clm_prod.hexa + its matmul-free production libs; the dispatcher seam
      # is the compliant path and is NOT flagged; the attention/decoder-block
      # architectures (a DIFFERENT model + the OP-29 deferred 2nd arch) are not
      # scanned. LOW BLAST RADIUS: tool/flame_steppath_fma_gate.sh is the SSOT;
      # verified PASS on the clean tree, fails ONLY if a raw FMA-fused matmul
      # regresses onto the trainer. <1 s. BLOCKING (no seed dependency).
      - name: OP-69 — flame step-path FMA-matmul gate (darwin-arm64)
        shell: bash
        run: |
          set -euo pipefail
          sh tool/flame_steppath_fma_gate.sh

      # ── OP-39: float const-fold byte-eq tripwire (ADVISORY until promote) ──
      # Locks the OP-37 (#3069) + OP-37b (#3073) SILENT float const-fold fixes
      # (0.0-X negation idiom byte-exact + computed-fold operands correctly-
      # rounded). The built ./hexa runs one self-contained oracle and asserts
      # each compile-time-folded float's IEEE-754 LE bit pattern == golden
      # (machine-independent; same goldens on every arch). LOW BLAST RADIUS:
      # tool/op39_constfold_gate.sh is the SSOT; only a REAL const-fold drift
      # (re-broken %g serialize / naive-atof parse) can fail it. <1 s.
      #
      # OP-42 extended the SAME gate/oracle with 5 hex-float cases (MUL_HF*) that
      # exercise the OP-40 (#3084) bit-exact hex-float fold-serialize fix — products
      # the old hand-rolled %.17e serialize mis-rounded by 1 ULP. No YAML change:
      # they run via the same tool/op39_constfold_gate.sh ./hexa invocation and
      # share the advisory below (the seed drifts on them identically). See
      # F-OP42-HEXFLOAT-CONTRACT-GATE.txt + docs/flame-determinism-contract.md.
      #
      # continue-on-error: TRUE — DELIBERATE. CI's ./hexa is built from the
      # FROZEN seed self/native/hexa_cc.c (151c52c8 bootstrap), which PRE-DATES
      # the OP-37/OP-37b/OP-40 source fixes in self/codegen.hexa — the fixes landed
      # in repo SOURCE (#3069/#3073/#3084) but are NOT YET PROMOTED into the seed
      # hexat (the verdicts flag this as a separate promote/rebuild step). So the
      # gate currently REPORTS the seed-toolchain bug (DRIFT to the lossy %g bits)
      # rather than passing. It is wired ADVISORY now so the regression
      # infrastructure is in place + visible; flip to blocking (drop this line)
      # in the same PR that promotes the fixes into the seed hexa_cc.c, at which
      # point this step auto-goes-GREEN. See F-OP39-CONSTFOLD-CI-GATE.txt.
      - name: OP-39 — float const-fold byte-eq gate (darwin-arm64, advisory)
        shell: bash
        continue-on-error: true
        run: |
          set -euo pipefail
          sh tool/op39_constfold_gate.sh ./hexa

      - name: Package — shared release_package (seeds removed)
        shell: bash
        env:
          TARGET: darwin-arm64
          CC: clang
          LIBS: ""
          GH_TOKEN: ${{ github.token }}
        run: bash tool/release_package

  faithful-nobaseline-linux-x86_64:
    name: faithful-nobaseline (linux-x86_64)
    needs: dispatch
    # FALLBACK-DISPATCH (tool/ci_pick_runner.sh via `dispatch` job): runs on the
    # self-hosted x86_64 pool (aiden/summer · 12c/30G, far faster than the 2-core
    # GitHub-hosted runner) WHEN a `hexa-build` runner is online + the event is
    # same-repo; otherwise falls back to the free GitHub-hosted ubuntu-latest.
    # This is the heaviest x86_64 leg (~13–19 min on cloud). Pool-offline no
    # longer sits this job pending — it falls back, so the gate never wedges.
    # Fork PRs are forced to GitHub-hosted (no self-hosted RCE). arm64 + darwin
    # legs stay on their cloud runners (no arm64 self-hosted host).
    runs-on: ${{ fromJSON(needs.dispatch.outputs.linux_label) }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Faithful — TEMP-remove ALL 23 seeds FIRST (working copy only)
        shell: bash
        run: |
          set -euo pipefail
          for f in $SEEDS; do rm -f "$f"; done
          echo "removed seeds BEFORE any build; tracked .c now:"
          git ls-files '*.c' | wc -l

      # ── SINGLE-JOB-FAITHFUL — same shared release_build + release_package the
      # real release runs, with seeds removed. Dynamic seed-removed run of the
      # real release path (no separate copy). See darwin job for full rationale.
      - name: Build — shared release_build (seeds removed, edge-pull)
        shell: bash
        env:
          TARGET: linux-x86_64
          CC: gcc
          LIBS: "-lm -ldl"
          GH_TOKEN: ${{ github.token }}
        run: bash tool/release_build

      - name: Assert — ./hexa runs with seeds removed
        shell: bash
        run: |
          set -euo pipefail
          ./hexa --version
          echo "faithful-nobaseline OK (linux-x86_64)"

      # ── OP-34: machine-independence FOLD regression gate (see darwin job
      # for full rationale). SSOT = tool/fold_ci_gate.sh; <5 s; fails ONLY on
      # a real deterministic-fold drift vs the 4-environment goldens.
      - name: OP-34 — fold gate (dt_exp/dt_erf golden folds, linux-x86_64)
        shell: bash
        run: |
          set -euo pipefail
          sh tool/fold_ci_gate.sh ./hexa

      # ── OP-69: flame trainer step-path FMA-fused-matmul tripwire (LAYER 3 —
      # cross-ISA). Run on x86_64 too: this gate is the cross-ISA invariant
      # (arm64 fma vs x86 mul+add), so enforcing it on the x86 leg as well as
      # the arm64 leg makes the layer-3 lock symmetric. Pure source grep (no
      # ./hexa, no seed) over clm_prod.hexa + its matmul-free production libs;
      # forge_dispatch_matmul dispatcher seam is compliant + not flagged. SSOT =
      # tool/flame_steppath_fma_gate.sh; <1 s. BLOCKING. See darwin job + F-OP29.
      - name: OP-69 — flame step-path FMA-matmul gate (linux-x86_64)
        shell: bash
        run: |
          set -euo pipefail
          sh tool/flame_steppath_fma_gate.sh

      # ── OP-39/OP-42: float const-fold byte-eq tripwire (ADVISORY until promote;
      # see darwin job for full rationale). 18 cases: OP-39's 13 + OP-42's 5
      # hex-float MUL_HF* (OP-40 #3084 bit-exact hex-float serialize). continue-on-
      # error TRUE: CI's ./hexa is the frozen seed hexat (pre-OP-37/37b/40); the
      # gate reports the seed-toolchain bug until the fix is promoted into the seed
      # hexa_cc.c, then auto-goes-GREEN. SSOT = tool/op39_constfold_gate.sh; <1 s.
      - name: OP-39 — float const-fold byte-eq gate (linux-x86_64, advisory)
        shell: bash
        continue-on-error: true
        run: |
          set -euo pipefail
          sh tool/op39_constfold_gate.sh ./hexa

      - name: Package — shared release_package (seeds removed)
        shell: bash
        env:
          TARGET: linux-x86_64
          CC: gcc
          LIBS: "-lm -ldl"
          GH_TOKEN: ${{ github.token }}
        run: bash tool/release_package

  faithful-nobaseline-linux-arm64:
    name: faithful-nobaseline (linux-arm64)
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Faithful — TEMP-remove ALL 23 seeds FIRST (working copy only)
        shell: bash
        run: |
          set -euo pipefail
          for f in $SEEDS; do rm -f "$f"; done
          echo "removed seeds BEFORE any build; tracked .c now:"
          git ls-files '*.c' | wc -l

      # ── SINGLE-JOB-FAITHFUL — same shared release_build + release_package the
      # real release runs, with seeds removed. See darwin job for full rationale.
      # Free CI accelerator: ccache the per-TU compiles (byteeq-safe — HEXA_CCACHE
      # only reroutes the compile through the cache, never the link/emit bytes).
      - name: ccache setup
        uses: ./.github/actions/ccache-setup

      - name: Build — shared release_build (seeds removed, edge-pull)
        shell: bash
        env:
          TARGET: linux-arm64
          CC: gcc
          LIBS: "-lm -ldl"
          GH_TOKEN: ${{ github.token }}
          HEXA_CCACHE: "1"
        run: bash tool/release_build

      - name: ccache stats
        if: always()
        shell: bash
        run: ccache -s || true

      - name: Assert — ./hexa runs with seeds removed
        shell: bash
        run: |
          set -euo pipefail
          ./hexa --version
          echo "faithful-nobaseline OK (linux-arm64)"

      # ── OP-34: machine-independence FOLD regression gate (see darwin job
      # for full rationale). SSOT = tool/fold_ci_gate.sh; <5 s; fails ONLY on
      # a real deterministic-fold drift vs the 4-environment goldens.
      - name: OP-34 — fold gate (dt_exp/dt_erf golden folds, linux-arm64)
        shell: bash
        run: |
          set -euo pipefail
          sh tool/fold_ci_gate.sh ./hexa

      # ── OP-69: flame trainer step-path FMA-fused-matmul tripwire (LAYER 3 —
      # cross-ISA). Pure source grep (no ./hexa, no seed) over clm_prod.hexa + its
      # matmul-free production libs; forge_dispatch_matmul dispatcher seam compliant
      # + not flagged. SSOT = tool/flame_steppath_fma_gate.sh; <1 s. BLOCKING. See
      # darwin job + F-OP29.
      - name: OP-69 — flame step-path FMA-matmul gate (linux-arm64)
        shell: bash
        run: |
          set -euo pipefail
          sh tool/flame_steppath_fma_gate.sh

      # ── OP-39/OP-42: float const-fold byte-eq tripwire (ADVISORY until promote;
      # see darwin job for full rationale). 18 cases: OP-39's 13 + OP-42's 5
      # hex-float MUL_HF* (OP-40 #3084 bit-exact hex-float serialize). continue-on-
      # error TRUE: CI's ./hexa is the frozen seed hexat (pre-OP-37/37b/40); the
      # gate reports the seed-toolchain bug until the fix is promoted into the seed
      # hexa_cc.c, then auto-goes-GREEN. SSOT = tool/op39_constfold_gate.sh; <1 s.
      - name: OP-39 — float const-fold byte-eq gate (linux-arm64, advisory)
        shell: bash
        continue-on-error: true
        run: |
          set -euo pipefail
          sh tool/op39_constfold_gate.sh ./hexa

      - name: Package — shared release_package (seeds removed)
        shell: bash
        env:
          TARGET: linux-arm64
          CC: gcc
          LIBS: "-lm -ldl"
          GH_TOKEN: ${{ github.token }}
        run: bash tool/release_package

  # ── SELFEMIT live-link smoke — RFC061 §M8 foundation verification (ADVISORY) ──
  #
  # NON-REQUIRED · independent job. It is NOT wired into any required status check
  # (selfhost-gates-required / faithful-nobaseline-*), so a RED here NEVER blocks a
  # merge and the release/gate semantics above are UNCHANGED — it only REPORTS
  # whether the §M8 HEXA_RT_SELFEMIT native-emit runtime.a actually live-links a
  # real consumer binary, end-to-end.
  #
  # WHY this job exists: the §M8 ladder (r2..r10) has landed 21/55 libc-substrate
  # symbols as hexa-NATIVE self-emitted .o members of build/runtime.a —
  # hxlcl_{close,read,strlen,memcpy,memset,memcmp,strcmp,strcpy,strncmp,strncpy,
  # strcat,strchr,lseek,dup2,mkdir,waitpid,stat,atoi,strdup,fcntl,poll}. r10 adds
  # fcntl/poll (svc no-errno leaves) + atoi/strdup (BRANCH26 composites whose
  # callees atoll/malloc stay shim-provided). Until now the only proof was the
  # script's internal `ld -r`
  # member merge (multidef==0). This job adds the missing surface: a FULL SELFEMIT
  # runtime.a built + linked into exit42/hello and RUN — the undefined==0 +
  # behavior proof — before more leaves are stacked on an unverified foundation.
  #
  # DARWIN-ONLY: the emit fixtures bake Mach-O arm64 syscall numbers + ld64
  # PAGE21/PAGEOFF12 relocs, so HEXA_RT_SELFEMIT engages only on darwin-arm64
  # (aiden/summer linux cannot exercise it). macos-latest cloud runner (no
  # arm64 self-hosted host); mirrors the faithful-nobaseline-darwin-arm64 leg.
  #
  # CODE-NEUTRAL: this PR adds ONLY this CI job. HEXA_RT_SELFEMIT stays default-OFF
  # everywhere (release_build never sets it), so shipping binaries + byteeq +
  # tracked `.c`=0 are untouched.
  selfemit-livelink-smoke-darwin-arm64:
    name: SELFEMIT live-link smoke (darwin-arm64, advisory)
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      # Free CI accelerator: ccache the per-TU compiles (byteeq-safe — HEXA_CCACHE
      # only reroutes the compile through the cache, never the link/emit bytes).
      - name: ccache setup
        uses: ./.github/actions/ccache-setup

      # ── (1) Build ./hexa via the SHARED release path (DEFAULT runtime.a =
      #    libc-shim members; HEXA_RT_SELFEMIT unset). This both produces the
      #    ./hexa consumer compiler AND restores the gitignored self/runtime.c
      #    seed (.c=0 checkout → restore_frozen_seeds), which the SELFEMIT rebuild
      #    in step (2) needs present. A 2-pass shape is REQUIRED: stage_resolve_
      #    runtime_a runs FIRST inside release_build (before ./hexa exists), and the
      #    SELFEMIT pass needs ./hexa as HEXA_SELFEMIT_BIN to run the emit fixtures.
      - name: (1) Build ./hexa — shared release_build (default runtime.a)
        shell: bash
        env:
          TARGET: darwin-arm64
          CC: clang
          LIBS: ""
          GH_TOKEN: ${{ github.token }}
          HEXA_CCACHE: "1"
        run: |
          set -euo pipefail
          bash tool/release_build
          ./hexa --version
          echo "(1) ./hexa built + seeds restored OK"

      - name: ccache stats
        if: always()
        shell: bash
        run: ccache -s || true

      # ── (2) Rebuild build/runtime.a WITH the §M8 SELFEMIT native members.
      #    SELFEMIT is nested INSIDE the HEXA_RT_MULTIOBJ packaging branch — BOTH
      #    must be 1. With HEXA_SELFEMIT_BIN=./hexa it runs the 21
      #    test/native_build/emit_hxlcl_*_o.hexa fixtures to emit native .o members,
      #    drops the matching libc-shim defs (-DHEXA_RT_SELFEMIT_<SYM>), adds the
      #    1-cell errno provider for close/read, and runs its OWN `ld -r` multidef
      #    safety gate (FATAL on any duplicate def). Seeds present (restored in
      #    step 1) → build_runtime_a_from_source path (no edge-pull / GH_TOKEN).
      - name: (2) Rebuild runtime.a — HEXA_RT_SELFEMIT=1 (21 native members)
        shell: bash
        env:
          TARGET: darwin-arm64
          CC: clang
          LIBS: ""
          HEXA_RT_MULTIOBJ: "1"
          HEXA_RT_SELFEMIT: "1"
          HEXA_SELFEMIT_BIN: ./hexa
        run: |
          set -euo pipefail
          mkdir -p build
          bash tool/stage_resolve_runtime_a 2>&1 | tee build/_selfemit_resolve.log
          echo "── build/runtime.a members ──"
          ar t build/runtime.a

      # ── (3) Assert the 21 native members + errno provider landed, and the
      #    self-emitted hxlcl_* symbols are DEFINED (T) — not the dropped shim.
      - name: (3) Assert 21 SELFEMIT members present + symbols defined
        shell: bash
        run: |
          set -euo pipefail
          members="$(ar t build/runtime.a)"
          syms="$(nm build/runtime.a 2>/dev/null || true)"
          missing_member=0; undef_sym=0; ok_member=0; ok_sym=0
          # NOTE: match via here-string (<<<), NOT `printf … | grep -q`. Under
          # `set -o pipefail`, grep -q exits on the FIRST match and closes the
          # pipe while printf is still writing the large $syms blob → SIGPIPE →
          # printf exits 141 → pipefail makes the whole pipeline non-zero → the
          # `if` flips to the else branch and a DEFINED symbol is mis-reported as
          # "NOT DEFINED" (race on symbol position in the nm stream — same broken
          # -pipe class as #3833). A here-string has no writer process to SIGPIPE.
          for s in close read strlen memcpy memset memcmp strcmp strcpy strncmp strncpy strcat strchr lseek dup2 mkdir waitpid stat atoi strdup fcntl poll; do
            if grep -qx "hxlcl_${s}_native.o" <<<"$members"; then
              ok_member=$((ok_member+1))
            else
              echo "  MISSING MEMBER: hxlcl_${s}_native.o"; missing_member=$((missing_member+1))
            fi
            # Mach-O external text def shows as ` T _hxlcl_<s>`.
            if grep -qE " T _hxlcl_${s}\$" <<<"$syms"; then
              ok_sym=$((ok_sym+1))
            else
              echo "  SYMBOL NOT DEFINED (T): _hxlcl_${s}"; undef_sym=$((undef_sym+1))
            fi
          done
          echo "(3) members ok=$ok_member/21 missing=$missing_member ; defined syms ok=$ok_sym/21 undef=$undef_sym"
          if grep -qx "hxlcl_errno_provider.o" <<<"$members"; then
            echo "(3) errno provider member present (close/read external _hxlcl_errno satisfied)"
          else
            echo "(3) FAIL: hxlcl_errno_provider.o missing (close/read would be undefined at consumer link)"; exit 1
          fi
          [ "$missing_member" -eq 0 ] && [ "$undef_sym" -eq 0 ] || { echo "(3) FAIL — SELFEMIT member/symbol shortfall"; exit 1; }

      # ── (4) Surface the script's OWN ld -r multidef gate result (==0 expected;
      #    a nonzero would already have FATAL-failed step 2 — this asserts it too).
      - name: (4) Assert ld -r multidef == 0
        shell: bash
        run: |
          set -euo pipefail
          md="$(grep -oE 'multiple_definition=[0-9]+' build/_selfemit_resolve.log | tail -1 | cut -d= -f2)"
          echo "(4) ld -r multiple_definition=${md:-<not-found>}"
          [ "${md:-1}" = "0" ] || { echo "(4) FAIL — multidef != 0"; exit 1; }

      # ── (5) CONSUMER LIVE-LINK SMOKE — the missing surface. Build exit42 + hello
      #    with ./hexa linking the SELFEMIT runtime.a (HEXA_PREBUILT_RUNTIME), then
      #    RUN. No --target → the prebuilt-runtime link path engages. A clean
      #    build+run is the undefined==0 + behavior proof for the 21 native members.
      - name: (5) exit42 + hello live-link smoke (SELFEMIT runtime.a alone)
        shell: bash
        env:
          # exit42/hello write under /tmp, which `hexa build` REFUSES on Darwin
          # (kernel-panic-path guard, self/main.hexa:3004). HEXA_MAC_BUILD_OK=1 is
          # the documented CI bypass for non-heavy dry-run builds — same as every
          # tool/build_hexa_*.sh and release_package's darwin-arm64 export.
          HEXA_MAC_BUILD_OK: "1"
        run: |
          set -euo pipefail
          export HEXA_PREBUILT_RUNTIME="$PWD/build/runtime.a"
          echo "  HEXA_PREBUILT_RUNTIME=$HEXA_PREBUILT_RUNTIME"

          # exit42 → rc 42
          printf 'fn main() {\n  exit(6 * 7)\n}\n' > /tmp/exit42.hexa
          ./hexa build /tmp/exit42.hexa -o /tmp/exit42 2>&1 | tee /tmp/exit42.build.log
          if /tmp/exit42; then e42rc=0; else e42rc=$?; fi
          echo "  exit42 rc=$e42rc (expect 42)"

          # hello → "hello"
          printf 'fn main() {\n  println("hello")\n}\n' > /tmp/hello.hexa
          ./hexa build /tmp/hello.hexa -o /tmp/hello 2>&1 | tee /tmp/hello.build.log
          hello_out="$(/tmp/hello || true)"
          echo "  hello output: '$hello_out' (expect hello)"

          # undefined==0 proof: scan the link logs for any unresolved hxlcl_*.
          undef="$( (grep -iE 'undefined|Undefined symbols|symbol not found' /tmp/exit42.build.log /tmp/hello.build.log || true) | grep -c hxlcl || true)"
          echo "── verdict ──"
          echo "  exit42_rc=$e42rc hello_ok=$([ "$hello_out" = "hello" ] && echo true || echo false) hxlcl_undefined=$undef"
          [ "$e42rc" = "42" ] || { echo "FAIL: exit42 rc != 42"; exit 1; }
          [ "$hello_out" = "hello" ] || { echo "FAIL: hello output != hello"; exit 1; }
          [ "$undef" = "0" ] || { echo "FAIL: $undef undefined hxlcl_* at consumer link"; exit 1; }
          echo "SELFEMIT live-link smoke GREEN — 21/55 §M8 members link + run (darwin-arm64)"

  # ── ROUTE C ON-PATH emit smoke — RFC061 §M8 verify-done gap closure (ADVISORY) ──
  #
  # SIBLING to the SELFEMIT smoke above, but exercises the OTHER §M8 path: the
  # Route C per-fn C-ABI codegen (`HEXA_CABI_HXLCL`). The SELFEMIT job runs the
  # hand-assembled SEED objects; this job runs the actual Route C EMIT of
  # stdlib/runtime/hxlcl_core.hexa — emit with HEXA_CABI_HXLCL=1, link the
  # emitted .o against a self-contained C harness (which supplies the composite
  # inner-callee leaves hxlcl_malloc/hxlcl_atoll and CALLS every Route-C-emitted
  # symbol with raw C-ABI prototypes + behaviour asserts), then RUN. A clean
  # build + all-asserts-pass is the direct ON-path proof the Route C emit produces
  # correct, linkable, behaviourally-faithful raw-C-ABI objects — the coverage the
  # SELFEMIT (seed) smoke does NOT provide (seed shape-match was the prior, weaker,
  # inferential evidence). Advisory first; promote to required after measured green.
  #
  # DARWIN-ONLY: aprime_cc builds Mach-O arm64 (clang -arch arm64) — the single
  # host where the patched compiler builds. macos-15 cloud runner (github-hosted;
  # no blacksmith). CODE-NEUTRAL: adds ONLY this job + tool/routec_cabi_smoke.sh;
  # HEXA_CABI_HXLCL stays default-OFF everywhere (release_build never sets it), so
  # shipping binaries + byteeq + tracked `.c`=0 are untouched.
  routec-cabi-smoke-darwin-arm64:
    name: Route C ON-path emit smoke (darwin-arm64, advisory)
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: ccache setup
        uses: ./.github/actions/ccache-setup

      # (1) Build ./hexa via the shared release path — also restores the gitignored
      #     self/native/hexat transpiler + self/runtime.c seeds that build_aprime
      #     needs present.
      - name: (1) Build ./hexa — shared release_build (restores hexat + seeds)
        shell: bash
        env:
          TARGET: darwin-arm64
          CC: clang
          LIBS: ""
          GH_TOKEN: ${{ github.token }}
          HEXA_CCACHE: "1"
        run: |
          set -euo pipefail
          bash tool/release_build
          ./hexa --version
          echo "(1) ./hexa built + hexat/seeds restored OK"

      # (2) Build aprime_cc (the native arm64-asm compiler) — the binary that emits
      #     the Route C .s under HEXA_CABI_HXLCL=1.
      - name: (2) Build aprime_cc
        shell: bash
        run: |
          set -euo pipefail
          bash tool/build_aprime.sh -o build/aprime_cc
          [ -x build/aprime_cc ] && echo "(2) aprime_cc built OK"

      # (3) Emit hxlcl_core.hexa with HEXA_CABI_HXLCL=1, link the Route C .o against
      #     the behaviour harness, run + assert. The script is a loud no-op off
      #     darwin-arm64 (it is the only host here, so it runs).
      - name: (3) Route C emit + link + behaviour run
        shell: bash
        env:
          APRIME: build/aprime_cc
          CC: clang
        run: |
          set -euo pipefail
          bash tool/routec_cabi_smoke.sh

  # ── Route C ON-path errno smoke (LINUX) — the errno-store value-exact gate ───
  # The darwin smoke above DCE's out hxlcl_close's errno-store leg (its
  # target_is_linux() guard is a compile-time FALSE const on darwin), so the ②
  # extern-data substrate (errno SET via __errno_location + __hx_ptr_store32) has
  # NO ON-path coverage there. This LINUX sibling runs the errno-store LIVE and
  # asserts close's errno contract VALUE-EXACT (errno == EBADF) — the substrate's
  # core claim. Self-hosted x86_64 pool (aiden/summer) when online; fork PRs +
  # offline pool fall back to GitHub-hosted via the dispatch job, same as the
  # faithful-nobaseline linux-x86_64 leg. Advisory first (promote after green).
  # CODE-NEUTRAL: adds ONLY this job + tool/routec_cabi_smoke_linux.sh;
  # HEXA_CABI_HXLCL stays default-OFF (release_build never sets it).
  routec-cabi-smoke-linux-x86_64:
    name: Route C ON-path errno smoke (linux-x86_64, advisory)
    needs: dispatch
    runs-on: ${{ fromJSON(needs.dispatch.outputs.linux_label) }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: ccache setup
        uses: ./.github/actions/ccache-setup

      # (1) Build ./hexa via the shared release path — restores the gitignored
      #     hexat transpiler + self/runtime.c seeds that build_aprime needs.
      - name: (1) Build ./hexa — shared release_build (restores hexat + seeds)
        shell: bash
        env:
          TARGET: linux-x86_64
          CC: gcc
          LIBS: "-lm -ldl"
          GH_TOKEN: ${{ github.token }}
          HEXA_CCACHE: "1"
        run: |
          set -euo pipefail
          bash tool/release_build
          ./hexa --version
          echo "(1) ./hexa built + hexat/seeds restored OK (linux-x86_64)"

      # (2) Build aprime_cc (the native x86_64-asm compiler) — the binary that
      #     emits the Route C .s under HEXA_CABI_HXLCL=1 for the linux target.
      - name: (2) Build aprime_cc
        shell: bash
        run: |
          set -euo pipefail
          bash tool/build_aprime.sh -o build/aprime_cc
          [ -x build/aprime_cc ] && echo "(2) aprime_cc built OK"

      # (3) Emit hxlcl_core.hexa (--target=x86_64-linux), link the Route C .o
      #     against the errno harness, run + assert errno == EBADF value-exact.
      - name: (3) Route C errno emit + link + value-exact run
        shell: bash
        env:
          APRIME: build/aprime_cc
          CC: gcc
        run: |
          set -euo pipefail
          bash tool/routec_cabi_smoke_linux.sh
